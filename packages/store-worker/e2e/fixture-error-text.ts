/**
 * Stable fixture failure text proves meaningful Worker causes without treating test location as
 * domain text.
 */
export const StoreWorkerFixtureErrorText = {
  calculationFailed: 'Unable to calculate the requested value'
} as const
