{
  "version": 1,
  "library": {
    "slug": "store-react",
    "description": "React Provider and hooks for the Store runtime.",
    "version": "0.0.2",
    "exports": ["."],
    "documentation": {
      "readme": {
        "sections": [
          {
            "id": "migaia-store-react",
            "heading": "@migaia/store-react",
            "blocks": [
              {
                "type": "paragraph",
                "text": "把 `@migaia/store-light`（甜 API Store）、`@migaia/store-keyed`（Atom 定义系统）、`@migaia/resource`（可取消异步值）接到 React 组件树上的驱动程序：一组基于 `useSyncExternalStore` 的 hook，外加一个负责作用域/生命周期/就绪时机的 `StoreProvider`。这几个底层包本身完全不认识 React；本包只做\"接线\"，不重新定义状态模型。"
              }
            ]
          },
          {
            "id": "适用与不适用场景",
            "heading": "适用与不适用场景",
            "blocks": [
              {
                "type": "paragraph",
                "text": "适用：已经在用 `@migaia/store-light` 的 Store、`@migaia/store-keyed` 的 Atom，或 `@migaia/resource` 的 `Resource`，要在 React 里渲染它们；需要按 Provider/请求作用域隔离状态（SSR 每请求一份、多棵独立子树互不干扰）；需要在渲染子树前挡住一个异步初始化（比如等 wasm 模块 ready）。"
              },
              {
                "type": "paragraph",
                "text": "不适用：不要把它当 Redux/Zustand 那种\"全局单一 store + reducer\"框架——这里的 Store 是可任意拆分的对象字面量甜 API；也不要指望它能让\"读到共享变量但从不检查 signal/依赖\"的代码自动响应式化，本包只负责把 `@migaia/reactive` 的依赖图接到 React 的订阅模型上。"
              }
            ]
          },
          {
            "id": "安装",
            "heading": "安装",
            "blocks": [
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm add @migaia/store-react react react-dom"
              },
              {
                "type": "paragraph",
                "text": "`react`/`react-dom` 是 peer dependency（`>=18`）。本包依赖 `@migaia/store-light`（`createStore` 产出的 Store 靠 `$runtime`/`$async`/`$snapshot()`/`$batch()` 这几个元字段与 `storeReady()` 对接，见源码 `reactive-store.ts`）、`@migaia/store-keyed`（Atom 定义/AtomStore 协议）、`@migaia/resource`（`Resource`）、`@migaia/reactive`（响应式图）、`@migaia/lifecycle`、`@migaia/utils`；这些包按需装即可，不是全部都要在业务代码里直接 import。产物是纯 ESM，`module."
              },
              {
                "type": "paragraph",
                "text": "json` 当前只公开根入口，所有 API 都从 `@migaia/store-react` 导入（没有子路径）。"
              }
            ]
          },
          {
            "id": "目录",
            "heading": "目录",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "StoreProvider",
                  "核心订阅 hook：`useStore` / `useSignal` / `useTracked` / `useNodeValue`",
                  "异步值 hook：`useResource` / `useResourceValue` / `useStoreResource` / `useAsyncAtomValue`",
                  "Atom hook：`useAtomValue` / `useSetAtom` / `useAtom` / `useAtomDefinition` / `useSetAtomDefinition`",
                  "依赖注入：StoreToken / StoreRegistry",
                  "Config / Feature flag",
                  "高阶组合示例",
                  "构建门禁"
                ]
              },
              {
                "type": "paragraph",
                "text": "完整签名、边界行为、错误码与生命周期细节，见 USEGUIDE.md。"
              }
            ]
          },
          {
            "id": "storeprovider",
            "heading": "StoreProvider",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import { StoreProvider, useStore } from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';"
              },
              {
                "type": "paragraph",
                "text": "`StoreProvider`｜5 秒上手 —— 给子树建立 Registry/Runtime 作用域；只用 `useStore`/`useSignal` 读取模块级单例时理论上可以不套它，但项目里建议根部始终套一层："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const counterStore = createStore({\n  count: 0,\n  increment() {\n    this.count += 1;\n  }\n});\n\nfunction Counter() {\n  const count = useStore(counterStore, (state) => state.count);\n  return <button onClick={() => counterStore.increment()}>count: {count}</button>;\n}\n\nfunction App() {\n  return (\n    <StoreProvider>\n      <Counter />\n    </StoreProvider>\n  );\n}"
              },
              {
                "type": "paragraph",
                "text": "全部 props（`IStoreProviderProps`）："
              },
              {
                "type": "list",
                "items": [
                  "`children: ReactNode`（必填）",
                  "`registry?: StoreRegistry` —— 不传则内部自建；传了默认不由 Provider 释放（见下）",
                  "`runtime?: IRuntime` —— 不传则内部 `createRuntime()`；无论谁创建 Registry，`runtime` 本身的生命周期永远由调用方负责，Provider 从不释放它",
                  "`disposeOnUnmount?: boolean` —— 都不传 `registry`/`runtime` 时默认 `true`（自建自释放）；只传 `registry` 时默认 `false`（外部对象默认外部拥有）",
                  "`config?: IStoreProviderConfig` —— `{ features？, ready？, fallback？, defaults？ }`，见下方 Config / Feature flag"
                ]
              },
              {
                "type": "paragraph",
                "text": "同时传 `registry` 与 `runtime` 且 `registry.runtime !== runtime` 会立刻抛错；完整所有权规则、`config.ready` 的身份锁定语义、StrictMode 下的候选 Registry 回收机制，见 USEGUIDE §2、§6。"
              }
            ]
          },
          {
            "id": "核心订阅-hook",
            "heading": "核心订阅 hook",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import { useStore, useSignal, useTracked, useNodeValue } from '@migaia/store-react';"
              },
              {
                "type": "paragraph",
                "text": "`useStore(store, selector, isEqual?)`｜5 秒上手 —— 订阅 `@migaia/store-light` Store 的一个 selector，只在读到的字段变化时重渲染："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const count = useStore(counterStore, (state) => state.count);"
              },
              {
                "type": "paragraph",
                "text": "全部参数：`store: IReactiveStore<S>`（必填）、`selector: (state: IStoreShape<S>) => R`（必填，引用应稳定）、`isEqual?: (a: R, b: R) => boolean`（默认 `Object.is`）。不需要 `StoreProvider`；`store.$async === true` 时会先 `use(storeReady(store))`，组件在 Store 就绪前 Suspend。"
              },
              {
                "type": "paragraph",
                "text": "`useSignal(signal)`｜3 秒上手 —— 订阅单个 `ISignal`，返回 `[值, 设置函数]`："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const [name, setName] = useSignal(nameSignal);"
              },
              {
                "type": "paragraph",
                "text": "单参数 `signal: ISignal<T>`（必填），无其他选项；依赖集合恒为该 Signal 自己，走比 `useTracked` 更轻的直连路径。"
              },
              {
                "type": "paragraph",
                "text": "`useTracked(read, isEqual?, runtime?)`｜10 秒上手 —— 最底层的通用订阅 hook，`read()` 内访问到的任意响应式节点都会被自动追踪为依赖；`useStore`/`useResource` 都是它的薄封装："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const total = useTracked(() => priceSignal.value + taxSignal.value);"
              },
              {
                "type": "paragraph",
                "text": "全部参数：`read: () => T`（必填，引用应稳定）、`isEqual?: (a: T, b: T) => boolean`（默认 `Object.is`）、`runtime?: IRuntime`（默认 `defaultRuntime`）。"
              },
              {
                "type": "paragraph",
                "text": "`useNodeValue(node, runtime, enabled?)`｜5 秒上手 —— 最底层的稳定节点订阅，适用于依赖集合恒为\"这一个节点自己\"的场景："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const value = useNodeValue(mySignal, runtime);"
              },
              {
                "type": "paragraph",
                "text": "全部参数：`node: IStableNode<T>`（必填，形状 `{ value: T; peek(): T }`）、`runtime: IRuntime`（必填）、`enabled?: boolean`（默认 `true`，`false` 时订阅是空操作）；`node` 不属于传入的 `runtime` 会在 render 阶段同步抛错（可被 Error Boundary 捕获）。"
              }
            ]
          },
          {
            "id": "异步值-hook",
            "heading": "异步值 hook",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import {\n  useResource,\n  useResourceValue,\n  useStoreResource,\n  useAsyncAtomValue\n} from '@migaia/store-react';"
              },
              {
                "type": "paragraph",
                "text": "`useResource(resource)` / `useResourceValue(resource)`｜10 秒上手 —— 订阅 `@migaia/resource` 的 `Resource<T>`；前者拿完整状态机，后者是 Suspense-safe 的取值版本："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const state = useResource(profileResource); // { status: 'idle'|'pending'|'success'|'error'|'cancelled', ... }\nconst profile = useResourceValue(profileResource); // 成功返回 data；pending/idle 时 throw promise；error/cancelled 时 throw error"
              },
              {
                "type": "paragraph",
                "text": "均为单参数 `resource: Resource<T>`（必填），无可选项；`resource.runtime` 决定追踪用的 Runtime。"
              },
              {
                "type": "paragraph",
                "text": "`useStoreResource(resource)`｜5 秒上手 —— 订阅 `@migaia/store-light` Store 内部声明的异步字段（`IStoreResource<T>`），带渲染期版本租约，StrictMode 下不会提前释放正在用的资源："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const value = useStoreResource(store.someAsyncField);"
              },
              {
                "type": "paragraph",
                "text": "单参数 `resource: IStoreResource<T>`（必填），无可选项。与 `Resource<T>` 是两套不同协议，不能互换，见 USEGUIDE §1.4。"
              },
              {
                "type": "paragraph",
                "text": "`useAsyncAtomValue(atom)`｜3 秒上手 —— 给形如 `{ resource: Resource<T> }` 的异步 atom 提供 Suspense-safe 取值："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const value = useAsyncAtomValue(asyncAtom); // 内部即 useResourceValue(atom.resource)"
              },
              {
                "type": "paragraph",
                "text": "单参数 `atom: { readonly resource: Resource<T> }`（必填），无可选项。"
              }
            ]
          },
          {
            "id": "atom-hook",
            "heading": "Atom hook",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import {\n  useAtomValue,\n  useSetAtom,\n  useAtom,\n  useAtomDefinition,\n  useSetAtomDefinition\n} from '@migaia/store-react';"
              },
              {
                "type": "paragraph",
                "text": "`useAtomValue(atom)` / `useSetAtom(atom)` / `useAtom(atom)`｜10 秒上手 —— 协议 hook：只要求对象实现 `IReadableAtom`/`IWritableAtom`（`@migaia/store-keyed/reactive/atom`），Provider 可选："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const value = useAtomValue(myAtom);\nconst write = useSetAtom(myWritableAtom);\nconst [value2, write2] = useAtom(myWritableAtom);"
              },
              {
                "type": "paragraph",
                "text": "全部参数：`atom: IReadableAtom<T>` 或 `IWritableAtom<T, Args, Result>`（必填），无其他选项。有 `StoreProvider` 且 `atom.atomDefinition` 存在时读写路由到 `registry.atomStore`；否则直接订阅/写入 atom 节点本身。`useAtom` 就是 `[useAtomValue(atom), useSetAtom(atom)]`。"
              },
              {
                "type": "paragraph",
                "text": "`useAtomDefinition(definition)` / `useSetAtomDefinition(definition)`｜10 秒上手 —— 必须有 `StoreProvider`，固定读写 `registry.atomStore`，配合 `atomDef`/`derivedDef`/`writableDef`/`familyDef`（`@migaia/store-keyed`）产出的定义使用："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const value = useAtomDefinition(countDef);\nconst increment = useSetAtomDefinition(incrementDef);"
              },
              {
                "type": "paragraph",
                "text": "全部参数：`definition: IAtomDefinition<T>` 或 `IWritableAtomDefinition<T, Args, Result>`（必填），无其他选项。无 Provider 时抛 `[store] hook requires a StoreProvider`。"
              }
            ]
          },
          {
            "id": "依赖注入-storetoken-storeregistry",
            "heading": "依赖注入：StoreToken / StoreRegistry",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import {\n  createStoreToken,\n  StoreRegistry,\n  createStoreRegistry,\n  useStoreRegistry,\n  useStoreRuntime,\n  useStoreFromProvider,\n  useProvidedStore\n} from '@migaia/store-react';"
              },
              {
                "type": "paragraph",
                "text": "`createStoreToken(debugName)`｜3 秒上手 —— 定义一把带类型参数的 key（内部是 `Symbol`）："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const sessionToken = createStoreToken<IReactiveStore<ISession>>('session');"
              },
              {
                "type": "paragraph",
                "text": "单参数 `debugName: string`（必填，非空——空字符串抛 `[store] StoreToken requires a debug name`），同时作为报错信息里的可读定位名。"
              },
              {
                "type": "paragraph",
                "text": "`useStoreFromProvider(token)` / `useProvidedStore(token, selector, isEqual?)`｜10 秒上手 —— 按 token 从当前 Provider 的 Registry 取回注册实例；后者额外做一次 `useStore` 订阅："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const sessionStore = useStoreFromProvider(sessionToken);\nconst userId = useProvidedStore(sessionToken, (s) => s.userId);"
              },
              {
                "type": "paragraph",
                "text": "`useStoreFromProvider` 单参数 `token: StoreToken<T>`（必填），未注册抛 `[store] missing provider store: X`。`useProvidedStore` 额外参数 `selector: (state: IStoreShape<S>) => Result`（必填）、`isEqual?`（默认 `Object.is`）。均必须有 Provider。"
              },
              {
                "type": "paragraph",
                "text": "`useStoreRegistry()` / `useStoreRuntime()`｜3 秒上手 —— 取当前 Provider 的 Registry / 其所属 Runtime，均必须有 Provider（否则抛 `[store] hook requires a StoreProvider`），无参数："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const registry = useStoreRegistry();\nconst runtime = useStoreRuntime(); // = useStoreRegistry().runtime"
              },
              {
                "type": "paragraph",
                "text": "`StoreRegistry` / `createStoreRegistry(runtime?)`｜10 秒上手 —— 手动构造作用域化容器，常见于 SSR 每请求一份、或测试："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const registry = createStoreRegistry(); // runtime 不传则内部 createRuntime()\nconst unregister = registry.register(sessionToken, sessionStore, { owned: true });\nregistry.dispose(); // 同步、幂等；owned 实体按注册顺序的逆序释放"
              },
              {
                "type": "paragraph",
                "text": "`createStoreRegistry` 单参数 `runtime?: IRuntime`（默认新建）。`StoreRegistry` 实例方法：`register(token, value, { owned? })`（同 token 重复注册抛错，`owned: true` 时 Registry 释放会调用其 `$dispose()`/`dispose()`）、`replace(token, value, options?)`（无重复检查，直接覆盖）、`get`/`require`/`has`（查询）、`remove(token, disposeOwned = true)`、`dispose()`（同步）、`disposeAsync()`（异步、单飞）、只读 `runtime`/`atomStore`/`lifecycle`/`disposed`、`whenTerminal()`。"
              },
              {
                "type": "paragraph",
                "text": "跨 Runtime 注册会抛 `[store] provider store \"X\" belongs to a different Runtime`。完整生命周期语义见 USEGUIDE §4、§6。"
              }
            ]
          },
          {
            "id": "config-feature-flag",
            "heading": "Config / Feature flag",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';"
              },
              {
                "type": "paragraph",
                "text": "`useStoreConfig()`｜3 秒上手 —— 取归一化后的完整配置快照："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "const config = useStoreConfig(); // 无 Provider 时返回 null"
              },
              {
                "type": "paragraph",
                "text": "无参数。"
              },
              {
                "type": "paragraph",
                "text": "`useStoreFeature(path)` / `useAssertStoreFeature(path, apiName?)`｜5 秒上手 —— 查询/断言某个 feature 是否开启，用于给实验性 API 设置入口守卫："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
              },
              {
                "type": "paragraph",
                "text": "`path: 'wasm' | \\`experimental.${string}\\`` 是两者共同的必填参数；`useAssertStoreFeature`额外的`apiName?: string`（默认 `'this API'`）出现在错误信息里。`useStoreFeature`无 Provider 时返回`false`，不抛；`useAssertStoreFeature`未开启（含无 Provider）一律抛`Error`。这两个都要通过 `StoreProvider`的`config.features.wasm`/`config.features.experimental`显式开启才算生效（只认字面`true`）。"
              },
              {
                "type": "paragraph",
                "text": "`StoreProviderState`｜3 秒上手 —— 就绪屏障三态的稳定取值集合，常量对象，无调用参数："
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
              },
              {
                "type": "paragraph",
                "text": "业务代码一般不需要直接用它，除非要复刻类似的三态就绪追踪逻辑。"
              }
            ]
          },
          {
            "id": "高阶组合示例-1-storeprovider-就绪屏障-storetoken-注入-suspense-异步读取",
            "heading": "高阶组合示例 · 1. StoreProvider + 就绪屏障 + StoreToken 注入 + Suspense 异步读取",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\n\nconst sessionToken = createStoreToken('session');\n\nfunction App() {\n  const ready = useMemo(() => [() => Promise.resolve()], []); // 引用必须稳定\n  const sessionStore = useMemo(() => createStore({ userId: '' }), []);\n\n  return (\n    <StoreProvider config={{ ready, fallback: <div>加载中…</div> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<div>加载中…</div>}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\nfunction SessionInjector({ store, children }) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return children;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const runtime = useStoreRuntime();\n  const showCharts = useStoreFeature('experimental.charts');\n  const profile = useResourceValue(new Resource(async () => ({ name: userId }), runtime));\n  return (\n    <p>\n      {profile.name}\n      {showCharts ? ' · 图表已开启' : ''}\n    </p>\n  );\n}"
              }
            ]
          },
          {
            "id": "高阶组合示例-2-usetracked-手写跨节点组合读取-不经过-store",
            "heading": "高阶组合示例 · 2. useTracked 手写跨节点组合读取（不经过 Store）",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import { useTracked } from '@migaia/store-react';\nimport { Signal } from '@migaia/reactive';\n\nconst priceSignal = new Signal(10);\nconst qtySignal = new Signal(2);\n\nfunction Total() {\n  const total = useTracked(() => priceSignal.value * qtySignal.value);\n  return <span>合计：{total}</span>;\n}"
              }
            ]
          },
          {
            "id": "高阶组合示例-3-atomstore-useatomdefinition-精细订阅",
            "heading": "高阶组合示例 · 3. AtomStore + useAtomDefinition 精细订阅",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import { StoreProvider, useAtomDefinition, useSetAtomDefinition } from '@migaia/store-react';\nimport { atomDef, writableDef } from '@migaia/store-keyed/atom/definition';\n\nconst countDef = atomDef(0);\nconst incrementDef = writableDef(\n  (get) => get(countDef),\n  (get, set) => set(countDef, get(countDef) + 1)\n);\n\nfunction Counter() {\n  const count = useAtomDefinition(countDef);\n  const increment = useSetAtomDefinition(incrementDef);\n  return <button onClick={() => increment()}>count: {count}</button>;\n}\n\nfunction App() {\n  return (\n    <StoreProvider>\n      <Counter />\n    </StoreProvider>\n  );\n}"
              }
            ]
          },
          {
            "id": "高阶组合示例-4-ssr-请求作用域-手动-registry-storeprovider-接管",
            "heading": "高阶组合示例 · 4. SSR 请求作用域：手动 Registry + StoreProvider 接管",
            "blocks": [
              {
                "type": "code",
                "language": "tsx",
                "code": "import { StoreProvider, createStoreRegistry } from '@migaia/store-react';\nimport { createRuntime } from '@migaia/reactive';\n\nasync function renderForRequest(children: React.ReactNode) {\n  const registry = createStoreRegistry(createRuntime()); // 每个请求独立一份\n  const html = renderToString(<StoreProvider registry={registry}>{children}</StoreProvider>); // disposeOnUnmount 默认 false：手动负责释放\n  await registry.disposeAsync();\n  return html;\n}"
              }
            ]
          },
          {
            "id": "构建门禁",
            "heading": "构建门禁",
            "blocks": [
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm --filter @migaia/store-react fmt\npnpm --filter @migaia/store-react lint\npnpm --filter @migaia/store-react typecheck\npnpm --filter @migaia/store-react typecheck:test\npnpm --filter @migaia/store-react test\npnpm --filter @migaia/store-react typecheck:e2e\npnpm --filter @migaia/store-react test:e2e\npnpm --filter @migaia/store-react build"
              }
            ]
          }
        ]
      },
      "guide": {
        "sections": [
          {
            "id": "migaia-store-react-使用指南",
            "heading": "@migaia/store-react 使用指南",
            "blocks": [
              {
                "type": "paragraph",
                "text": "本指南面向已经读过 README.md、决定要用这个包的人，聚焦每个 API 的精确行为、边界条件与排错方式。想先了解\"这是什么、要不要用\"，请回 README。"
              }
            ]
          },
          {
            "id": "目录",
            "heading": "目录",
            "blocks": [
              {
                "type": "paragraph",
                "text": "1. 核心概念详解 2. StoreProvider 完整参考 3. Hook 完整参考 4. 依赖注入：StoreToken / StoreRegistry 5. 错误处理完整参考 6. 生命周期与资源释放细节 7. 注意事项详解 8. 贴近生产的完整示例 9. 常见问题排查 10. 构建、格式化与测试"
              }
            ]
          },
          {
            "id": "1-1-runtime-与节点所有权",
            "heading": "1.1 Runtime 与节点所有权",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`@migaia/reactive` 的 `Runtime` 是一张依赖图的容器：Signal、Computed、Effect、Store、Atom 实例都\"属于\"某一个 Runtime（`claimOwnership`/`ownerOf` 建立这条唯一所有权关系）。只有同一个 Runtime 上的节点才能互相订阅——跨 Runtime 订阅是编程错误，本包在能做到的地方会主动检测并抛错（见 `useNodeValue`、`StoreRegistry.register`）。"
              },
              {
                "type": "paragraph",
                "text": "大多数场景不需要手动创建 Runtime：不传 `runtime` 时，`StoreProvider` 会用 `createRuntime()` 新建一个；未套 `StoreProvider` 的 `useSignal`/`useTracked` 默认落到 `@migaia/reactive` 的 `defaultRuntime`。只有需要多份互相隔离的状态图（比如测试、SSR 多请求）时才需要显式传入。"
              }
            ]
          },
          {
            "id": "1-2-store-migaia-store-light",
            "heading": "1.2 Store（@migaia/store-light）",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`createStore(shape)` 产出的对象是\"甜 API\"：普通字段是 Signal，`get` 访问器是惰性缓存的 Computed，方法是自动 batch 的 Action。除了业务字段，实例上还挂了几个以 `$` 开头的元字段，本包的 hook 会用到："
              },
              {
                "type": "table",
                "headers": ["字段/方法", "参数类型", "同步/异步", "用途"],
                "rows": [
                  [
                    "`store.$runtime`",
                    "—（只读字段，无参数）",
                    "同步",
                    "这个 Store 所属的 Runtime；`useStore` 用它作为 `useTracked` 的第三个参数"
                  ],
                  [
                    "`store.$async`",
                    "—（只读字段，无参数）",
                    "同步",
                    "是否含有异步初始化字段；为 `true` 时 `useStore` 会先 `use(storeReady(store))`"
                  ],
                  ["`store.$snapshot()`", "无", "同步", "取当前状态的一份普通对象快照（非响应式）"],
                  [
                    "`store.$batch(recipe)`",
                    "`recipe: (draft: IStoreShape<S>) => void`",
                    "同步",
                    "手动合并多次写操作为一次通知"
                  ]
                ]
              }
            ]
          },
          {
            "id": "1-3-atom-定义-vs-atomstore-定义是-token-不含状态",
            "heading": "1.3 Atom 定义 vs AtomStore：定义是 token，不含状态",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`@migaia/store-keyed` 的 `atomDef`/`atomDefFactory`/`derivedDef`/`writableDef`/`familyDef`/`derivedFamilyDef` 产出的都是冻结的描述对象（`IAtomDefinition<T>`／`IWritableAtomDefinition<T, Args, Result>`），不含任何运行时状态。真正的值要靠一个 `IAtomStore`（`createAtomStore(runtime)`）去实例化："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const store = createAtomStore(runtime);\nstore.get(countDef); // 首次访问才实例化\nstore.set(countDef, (n) => n + 1);\nstore.sub(countDef, () => {}); // 订阅"
              },
              {
                "type": "paragraph",
                "text": "这个设计的意义是作用域化：同一份 `countDef` 在不同的 `AtomStore`（比如不同 `StoreProvider`、不同 SSR 请求）下各自独立实例化、互不污染。`useAtomDefinition`/`useSetAtomDefinition` 固定走 `StoreProvider` 自带的 `registry.atomStore`；`useAtomValue`/`useSetAtom`/`useAtom` 则是更通用的协议 hook，见 3.6。"
              }
            ]
          },
          {
            "id": "1-4-两种-异步值-resource-与-istoreresource",
            "heading": "1.4 两种\"异步值\"：Resource 与 IStoreResource",
            "blocks": [
              {
                "type": "paragraph",
                "text": "本包同时对接两种不同来源的异步容器，名字很像但不是一回事："
              },
              {
                "type": "table",
                "headers": ["", "来自", "对应 hook", "语义"],
                "rows": [
                  [
                    "`Resource<T>`",
                    "`@migaia/resource`",
                    "`useResource`、`useResourceValue`、`useAsyncAtomValue`（读 `atom.resource`）",
                    "独立的、可取消可重试的异步值容器，天然支持 Suspense"
                  ],
                  [
                    "`IStoreResource<T>`",
                    "`@migaia/store-light`（Store 内部的异步字段）",
                    "`useStoreResource`",
                    "Store 定义里声明出的异步字段，带渲染期版本租约（`captureSnapshot`/`commitCapture`），避免并发渲染读到\"已经被释放\"的版本"
                  ]
                ]
              },
              {
                "type": "paragraph",
                "text": "两者都能配合 `<Suspense>` 使用，但订阅协议、返回形状都不同，不能互换：`useStoreResource` 需要一个实现了 `read/preload/retry/captureSnapshot/commitCapture/.` 完整协议的 `IStoreResource`，`useResource`/`useResourceValue` 需要的是 `@migaia/resource` 的 `Resource` 实例。"
              }
            ]
          },
          {
            "id": "1-5-storeregistry-storetoken-作用域化依赖注入",
            "heading": "1.5 StoreRegistry / StoreToken：作用域化依赖注入",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`StoreRegistry` 是挂在某个 Runtime 上的 key → value 容器，`StoreProvider` 内部会创建（或接收）一个，通过 React Context 下发给子树。`createStoreToken<T>(debugName)` 定义一把类型化的 key；`registry.register(token, value, { owned })` 把一个实例注册进去；子树里用 `useStoreFromProvider(token)` 或 `useProvidedStore(token, selector)` 取回。`owned: true` 表示这份实例的生命周期交给 Registry——Registry 释放时会调用它的 `$dispose()`/`dispose()`。"
              },
              {
                "type": "paragraph",
                "text": "这套机制的价值是不用到处 import 单例：不同的 `StoreProvider` 子树（比如 SSR 每请求一份、多个独立小组件树）可以往同一个 token 注册不同的实例。只用模块级单例 Store/Signal（如 README 五分钟上手示例里的 `counterStore`）完全不需要 `StoreToken`，直接 `useStore`/`useSignal` 即可。"
              }
            ]
          },
          {
            "id": "1-6-就绪屏障-config-ready",
            "heading": "1.6 就绪屏障（config.ready）",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`StoreProvider` 的 `config.ready` 是一组 `IStoreReadyBarrier`（`Promise<unknown>` 或零参工厂 `() => Promise<unknown> | unknown`），子树渲染前必须全部 settle。内部用 `Promise.all` 归一化成一个 `ITrackedReady`：pending 时渲染 `config.fallback`（默认 `null`），全部成功后渲染 `children`，任意一个 reject 则在 `ReadyBoundary` 里 `throw`（交给上层 Error Boundary）。精确语义见 2.3。"
              }
            ]
          },
          {
            "id": "1-7-feature-flag",
            "heading": "1.7 Feature flag",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`config.features.wasm` 与 `config.features.experimental` 是应用级\"这棵树允许用什么增强\"的声明，与 Registry/Runtime 的所有权正交。`useStoreFeature(path)` 查询（无 Provider 时返回 `false`，不抛），`useAssertStoreFeature(path)` 断言（未开启或无 Provider 都抛错），用于给实验性/需要额外初始化的 API 设置入口守卫。`path` 的类型是 `'wasm' | \\`experimental.${string}\\``。"
              }
            ]
          },
          {
            "id": "2-storeprovider-完整参考",
            "heading": "2. StoreProvider 完整参考",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IStoreProviderProps = {\n  readonly children: ReactNode;\n  readonly registry?: StoreRegistry;\n  readonly runtime?: IRuntime;\n  readonly disposeOnUnmount?: boolean;\n  readonly config?: IStoreProviderConfig;\n};"
              }
            ]
          },
          {
            "id": "2-1-registry-runtime-的所有权规则",
            "heading": "2.1 registry / runtime 的所有权规则",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "都不传：`StoreProvider` 内部 `createRuntime()` + `createStoreRegistry()`，自己创建、自己在 unmount 时释放（`disposeOnUnmount` 默认 `true`）。",
                  "只传 `runtime`：Provider 仍然自己创建 Registry，但挂在传入的 Runtime 上；`disposeOnUnmount` 依然默认 `true`——但它只释放 Provider 创建的 Registry（含其 `atomStore` 与注册的 owned 实例），传入的 `runtime` 本身永远不会被这里释放，生命周期由调用方负责。",
                  "只传 `registry`：Provider 使用调用方给的 Registry，`disposeOnUnmount` 默认改为 `false`（外部对象默认外部拥有）；显式传 `true` 才会在 unmount 时释放它。",
                  "同时传 `registry` 和 `runtime` 且 `registry.runtime !== runtime`：立刻抛 `Error('[store] StoreProvider registry/runtime ownership mismatch')`。要么只传 `registry`，要么保证两者引用同一个 Runtime。",
                  "`runtime` 在多次渲染间发生变化（切换到另一个 Runtime 实例）：Provider 会为新 Runtime 创建一份新的候选 Registry，通过内部的\"候选/已提交\"机制在 effect 提交前后安全切换，不会污染旧 Registry 的状态。"
                ]
              }
            ]
          },
          {
            "id": "2-2-config-的四个字段",
            "heading": "2.2 config 的四个字段",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IStoreProviderConfig = {\n  readonly features?: {\n    readonly wasm?: boolean;\n    readonly experimental?: Readonly<Record<string, boolean>>;\n  };\n  readonly ready?: readonly (Promise<unknown> | (() => Promise<unknown> | unknown))[];\n  readonly fallback?: ReactNode;\n  readonly defaults?: { readonly warnAsyncActions?: boolean };\n};"
              },
              {
                "type": "list",
                "items": [
                  "`features.wasm`：声明本树会用到 wasm 字段。为 `true` 且 `ready` 为空数组时，会自动追加一个立刻 reject 的屏障（见 5），避免\"开了开关却没人 init\"。",
                  "`features.experimental`：白名单对象，只有值严格为 `true` 的 key 才算开启；未列出或非 `true` 一律视为关闭。",
                  "`ready`：见 2.3。",
                  "`fallback`：`ready` 屏障 pending 期间渲染的占位内容，默认 `null`（不是 `undefined`——不传 `fallback` 且有 `ready` 时，pending 阶段渲染的是\"什么都不渲染\"而不是上一次的 children）。",
                  "`defaults.warnAsyncActions`：只是配置快照的一部分，通过 `useStoreConfig()` 暴露给业务代码读取；`StoreProvider` 不会把它自动传给 `createStore`——各个 Store 仍要自己在 `createStore(shape, { warnAsyncActions: true })` 里显式声明。这个字段更适合用作\"业务代码统一读一个地方决定要不要传\"的约定，而不是自动生效的开关。"
                ]
              }
            ]
          },
          {
            "id": "2-3-ready-屏障的精确语义",
            "heading": "2.3 ready 屏障的精确语义",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "元素身份在挂载后被锁定。`config.ready` 数组第一次渲染时的值会被存入 `ref`，后续渲染即使传入新数组，只要按元素浅比较（`===`，逐项）与旧数组相等就视为同一份；只要有一项引用变了，就会触发身份变化处理——但新数组不会生效，Provider 继续用挂载时那份旧屏障，同时在开发环境 `console.warn`、生产环境 `console.error` 一条 `[store] StoreProvider ready barriers changed identity; ...`。这意味着内联字面量 `config={{ ready: [fetchUser()] }}` 每次 render 都会打印警告且不会重新等待——务必用 `useMemo` 或模块级常量稳住 `ready` 数组与其元素的引用。",
                  "工厂函数只执行一次（按屏障列表身份缓存）。`ready: [() => ensureWasm()]` 里的工厂在同一个 barrier 数组的生命周期内只会被调用一次，结果被 `WeakMap` 缓存复用；但缓存 key 是\"归一化后的屏障数组\"本身，不是全局按工厂函数缓存——不同的 Provider/SSR 请求作用域各自独立执行一次，互不共享。工厂执行不保证在\"React 放弃的渲染\"下恰好执行一次；非幂等的初始化逻辑应该自己用一个模块级 memoized Promise，而不是依赖这里的缓存语义。",
                  "reject 会变成一个真实抛出的 `Error`。非 `Error` 类型的 reject 原因会被包装成 `new Error(\\`[store] ready barrier rejected: ${String(reason)}\\`)`；`ReadyBoundary`在 render 阶段`throw` 它，只能被外层 React Error Boundary 捕获，`StoreProvider` 自己不提供错误 UI。",
                  "已经 settle 的 Promise 不保证首帧同步生效。`ITrackedReady.status()` 是同步查询，但 track 逻辑是在 Promise then 回调（微任务）里更新状态，所以哪怕传入的 Promise 已经 resolve，`ReadyBoundary` 首次渲染仍可能读到 `pending`，随后一个 effect 里同步刷新到 `ready`——多数场景感知不到，但如果你依赖\"reload 后立刻同步渲染最终内容、不闪 fallback\"，需要注意这个微任务延迟。"
                ]
              }
            ]
          },
          {
            "id": "2-4-storeproviderstate-istoreproviderstate",
            "heading": "2.4 StoreProviderState / IStoreProviderState",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "const StoreProviderState = { pending: 'pending', ready: 'ready', error: 'error' } as const;\ntype IStoreProviderState = 'pending' | 'ready' | 'error';"
              },
              {
                "type": "paragraph",
                "text": "根入口公开导出的常量对象，是 2.3 里 `ready` 屏障三种状态的稳定取值集合。本包内部（`ReadyBoundary`）用它驱动 fallback/children/throw 三路分支；业务代码一般不需要直接使用它——除非在自己的代码里复刻类似\"三态就绪追踪\"的逻辑，想复用同一套状态命名。"
              }
            ]
          },
          {
            "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
            "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
              },
              {
                "type": "paragraph",
                "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
              },
              {
                "type": "list",
                "items": [
                  "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                  "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                  "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                ]
              }
            ]
          },
          {
            "id": "3-1-usestore-store-selector-isequal",
            "heading": "3.1 useStore(store, selector, isEqual?)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useStore<S extends Record<string, unknown>, R>(\n  store: IReactiveStore<S>,\n  selector: (state: IStoreShape<S>) => R,\n  isEqual?: (a: R, b: R) => boolean\n): R;"
              },
              {
                "type": "list",
                "items": [
                  "不需要 `StoreProvider`；直接订阅传入的 `store` 实例，`store.$runtime` 决定用哪个 Runtime 追踪依赖。",
                  "若 `store.$async === true`（Store 定义里含异步初始化字段），会先 `use(storeReady(store))`——组件在 Store 就绪前直接 Suspend，交给外层 `<Suspense>` 处理；不含异步字段的 Store 不受影响。",
                  "`selector` 通过 `useTracked` 走\"渲染期捕获、commit 期提交\"的两段式协议：并发渲染中途被 React 丢弃的那一次不会污染真正提交的订阅状态。`selector`/`isEqual` 的引用应该稳定（模块级函数或 `useCallback`），否则每次渲染都会重新捕获依赖。",
                  "默认 `isEqual` 是 `Object.is`；想要浅比较对象结果时自己传一个。"
                ]
              }
            ]
          },
          {
            "id": "3-2-usesignal-signal",
            "heading": "3.2 useSignal(signal)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useSignal<T>(s: ISignal<T>): readonly [T, (next: T) => void];"
              },
              {
                "type": "paragraph",
                "text": "订阅单个 `ISignal`，返回 `[当前值, 设置函数]`。因为依赖集合恒为\"这一个 Signal 自己\"，内部直接走 `useNodeValue` 快路径，不经过 `useTracked` 的捕获/提交记账——比 `useTracked(() => signal.value)` 更轻。`set` 函数引用在 `signal` 不变时保持稳定。"
              }
            ]
          },
          {
            "id": "3-3-usetracked-read-isequal-runtime",
            "heading": "3.3 useTracked(read, isEqual?, runtime?)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useTracked<T>(\n  read: () => T,\n  isEqual?: (a: T, b: T) => boolean, // 默认 Object.is\n  runtime?: IRuntime // 默认 defaultRuntime\n): T;"
              },
              {
                "type": "paragraph",
                "text": "最底层的通用订阅 hook：`read()` 内部访问到的任意响应式节点都会被自动追踪为依赖，不限于单个 Signal。`useStore`/`useResource` 都是基于它实现的一层薄封装。"
              },
              {
                "type": "list",
                "items": [
                  "`runtime` 变化时会整体重建内部追踪实例——旧实例留给旧 effect 用到 cleanup 为止，新实例从当前 `read`/`isEqual` 初始化，绝不会让新 Runtime 执行旧 Runtime 捕获到的 selector。",
                  "内部使用 `@migaia/reactive/runtime` 导出的公开 `createObserverBinding` 三段式绑定（capture/commit/retrack），不依赖任何内部私有 API——第三方要写等价的自定义适配层也能做到同样的并发安全。",
                  "自己直接用这个 hook 时，把 `read`/`isEqual` 的引用稳定下来（`useCallback`/模块级函数），身份变化会触发一次重新捕获依赖（仍然正确，但多一次工作）。"
                ]
              }
            ]
          },
          {
            "id": "3-4-useresource-resource-useresourcevalue-resource",
            "heading": "3.4 useResource(resource) / useResourceValue(resource)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useResource<T>(resource: Resource<T>): IResourceState<T>;\nfunction useResourceValue<T>(resource: Resource<T>): T;"
              },
              {
                "type": "paragraph",
                "text": "`Resource<T>` 来自 `@migaia/resource`。`useResource` 订阅完整状态机："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IResourceState<T> =\n  | { status: 'idle' }\n  | { status: 'pending' }\n  | { status: 'success'; data: T; refreshing?: boolean }\n  | { status: 'error'; error: unknown }\n  | { status: 'cancelled'; error: DOMException };"
              },
              {
                "type": "paragraph",
                "text": "`useResourceValue` 是 Suspense-safe 的取值版本：`success` 时返回 `data`；`error`/`cancelled` 时把 `error` 从 render 里 `throw`（走 Error Boundary）；`pending`/`idle` 时 `throw resource.promise`（走 `<Suspense>`）。二者共用同一个基于 `useTracked` 的订阅，`resource.runtime` 决定追踪所用的 Runtime；这个订阅只观察状态机本身的变化，Promise/错误的抛出永远发生在 render 阶段，不会从调度器的 Effect 回调里抛出。"
              }
            ]
          },
          {
            "id": "3-5-usestoreresource-resource",
            "heading": "3.5 useStoreResource(resource)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useStoreResource<T>(resource: IStoreResource<T>): T;"
              },
              {
                "type": "paragraph",
                "text": "订阅 `@migaia/store-light` 的 `IStoreResource`（见 1.4），带渲染期版本租约：`resource.captureSnapshot(existingLease)` 在 render 阶段拿到当前快照与一个待提交的 capture 令牌；一个 `useLayoutEffect` 把它 `commitCapture` 成正式租约，释放上一个租约；另一个 `useLayoutEffect` 负责在真正 unmount（而非 StrictMode 的探测性 mount→unmount→remount）时异步释放租约——用一个自增的 `epoch` + `queueMicrotask` 判断\"这次 cleanup 是否被紧接着的重新 mount 追上\"，避免 StrictMode 下把还在用的资源提前释放。"
              },
              {
                "type": "paragraph",
                "text": "业务代码不需要关心这些细节，只需知道：每次拿到的返回值都对应一个当下受保护、不会被提前回收的版本。"
              }
            ]
          },
          {
            "id": "3-6-useatomvalue-atom-usesetatom-atom-useatom-atom",
            "heading": "3.6 useAtomValue(atom) / useSetAtom(atom) / useAtom(atom)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useAtomValue<T>(atom: IReadableAtom<T>): T;\nfunction useSetAtom<T, Args extends readonly unknown[], Result>(\n  atom: IWritableAtom<T, Args, Result>\n): (...args: Args) => Result;\nfunction useAtom<T, Args extends readonly unknown[], Result>(\n  atom: IWritableAtom<T, Args, Result>\n): readonly [T, (...args: Args) => Result];"
              },
              {
                "type": "paragraph",
                "text": "这三个是协议 hook：只要求传入的对象实现 `IReadableAtom`/`IWritableAtom`（`@migaia/store-keyed/reactive/atom`）协议，不强制来自任何特定工厂函数。行为分两种情况："
              },
              {
                "type": "list",
                "items": [
                  "有 `StoreProvider` 且 `atom.atomDefinition` 存在：读写都路由到 `registry.atomStore`（`sub`/`peek`/`set`），即这个 atom 实例在当前 Provider 作用域下的那一份状态。",
                  "无 Provider，或 atom 不携带 `atomDefinition`：直接对 atom 节点本身做 `useNodeValue` 订阅、`atom.write(...args)` 写入——退化为\"就是订阅这一个对象\"，不经过任何 Registry。"
                ]
              },
              {
                "type": "paragraph",
                "text": "两条路径在同一次 hook 调用里通过 `enabled` 参数互斥启用，hook 调用顺序始终稳定，不会因为 Provider 有无而改变 hook 数量。`useAtom` 就是 `[useAtomValue(atom), useSetAtom(atom)]` 的组合，两个子调用各自独立判断是否走 Provider。"
              }
            ]
          },
          {
            "id": "3-7-useasyncatomvalue-atom",
            "heading": "3.7 useAsyncAtomValue(atom)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useAsyncAtomValue<T>(atom: { readonly resource: Resource<T> }): T;"
              },
              {
                "type": "paragraph",
                "text": "给形如 `{ resource: Resource<T> }` 的异步 atom 提供 Suspense-safe 取值，内部就是 `useResourceValue(atom.resource)`，见 3.4。"
              }
            ]
          },
          {
            "id": "3-8-useatomdefinition-definition-usesetatomdefinition-definition",
            "heading": "3.8 useAtomDefinition(definition) / useSetAtomDefinition(definition)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useAtomDefinition<T>(definition: IAtomDefinition<T>): T;\nfunction useSetAtomDefinition<T, Args extends readonly unknown[], Result>(\n  definition: IWritableAtomDefinition<T, Args, Result>\n): (...args: Args) => Result;"
              },
              {
                "type": "paragraph",
                "text": "必须有 `StoreProvider`（内部调用 `useStoreRegistry()`，无 Provider 直接抛错）。固定读写 `registry.atomStore`："
              },
              {
                "type": "list",
                "items": [
                  "`useAtomDefinition` 订阅走 `store.sub(definition, onChange)`，取值走 `store.preview(definition)`——`preview` 是\"可被 React 重复调用的推测性读取\"：不会为派生值建立正式依赖边，但在同一个 Runtime version 内引用保持稳定，避免因为重复调用 `getSnapshot` 触发多余渲染。",
                  "`useSetAtomDefinition` 就是 `store.set(definition, ...args)` 的稳定包装。"
                ]
              },
              {
                "type": "paragraph",
                "text": "与 `useAtomValue`/`useSetAtom` 的区别：后者是\"给任意 atom 协议对象接线，Provider 可选\"；这一对是\"专门读写 Provider 自己的 `atomStore`，Provider 必需\"。业务里更常见的是用 `atomDef`/`derivedDef`/`writableDef`/`familyDef` 定义出 token，然后配这一对 hook。"
              }
            ]
          },
          {
            "id": "3-9-usenodevalue-node-runtime-enabled",
            "heading": "3.9 useNodeValue(node, runtime, enabled?)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IStableNode<T> = { readonly value: T; peek(): T };\nfunction useNodeValue<T>(node: IStableNode<T>, runtime: IRuntime, enabled?: boolean): T;"
              },
              {
                "type": "paragraph",
                "text": "最底层的稳定节点订阅：`useSignal`/`useAtomValue` 的直连路径都基于它。适用于依赖集合恒为\"就是这一个节点自己\"的场景（不适合动态依赖，那种场景用 `useTracked`）。"
              },
              {
                "type": "list",
                "items": [
                  "所有权在 render 阶段同步校验：`assertReactiveOwnedBy(node, runtime, 'reactive node')` 若发现 `node` 不属于传入的 `runtime`，立刻在 render 里抛错，能被 Error Boundary 捕获；不会等到订阅 Effect 真正跑起来才在 commit 阶段爆出这个问题。",
                  "订阅内部创建一个 `Effect`，首次运行只登记依赖、不触发 `onChange`；取值用 `node.peek()`——非追踪、恒为当前值，因此 React 在 render 与 subscribe 之间、或某次 transition 被中止后重新调用它，拿到的都是最新值，不存在撕裂窗口，也没有额外的快照缓存。",
                  "`enabled=false` 时订阅函数直接返回一个空操作的取消函数——用于 `useAtomValue` 内部按需关闭这条直连路径而不改变 hook 调用顺序。"
                ]
              }
            ]
          },
          {
            "id": "3-10-usestoreregistry-usestoreruntime",
            "heading": "3.10 useStoreRegistry() / useStoreRuntime()",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useStoreRegistry(): StoreRegistry; // 无 Provider 抛错\nfunction useStoreRuntime(): IRuntime; // = useStoreRegistry().runtime"
              },
              {
                "type": "paragraph",
                "text": "取当前 `StoreProvider` 提供的 Registry / 其所属 Runtime。都必须有 Provider，否则抛 `[store] hook requires a StoreProvider`。"
              }
            ]
          },
          {
            "id": "3-11-usestorefromprovider-token-useprovidedstore-token-selector-isequal",
            "heading": "3.11 useStoreFromProvider(token) / useProvidedStore(token, selector, isEqual?)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useStoreFromProvider<T>(token: StoreToken<T>): T;\nfunction useProvidedStore<S extends Record<string, unknown>, Result>(\n  token: StoreToken<IReactiveStore<S>>,\n  selector: (state: IStoreShape<S>) => Result,\n  isEqual?: (left: Result, right: Result) => boolean\n): Result;"
              },
              {
                "type": "paragraph",
                "text": "`useStoreFromProvider` 就是 `useStoreRegistry().require(token)`——按 token 取回注册实例，未注册则抛 `[store] missing provider store: X`。`useProvidedStore` 是\"取回 + `useStore` 订阅\"的组合，专门给注册在 Registry 里的 `IReactiveStore` 用。两者都必须有 Provider。"
              }
            ]
          },
          {
            "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
            "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
              },
              {
                "type": "list",
                "items": [
                  "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                  "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                  "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                ]
              }
            ]
          },
          {
            "id": "4-1-createstoretoken-t-debugname",
            "heading": "4.1 createStoreToken<T>(debugName)",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createStoreToken<T>(debugName: string): StoreToken<T>;"
              },
              {
                "type": "paragraph",
                "text": "产出一个冻结的、带类型参数 `T` 的 key（内部是一个 `Symbol`）。`debugName` 为空字符串会抛 `[store] StoreToken requires a debug name`——它同时是调试信息，出现在所有相关报错消息里，务必取一个能定位到业务含义的名字。"
              }
            ]
          },
          {
            "id": "4-2-storeregistry-类",
            "heading": "4.2 StoreRegistry 类",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "class StoreRegistry {\n  readonly runtime: IRuntime;\n  readonly atomStore: IAtomStore;\n  get lifecycle(): 'open' | 'closing' | 'terminal';\n  get disposed(): boolean;\n  whenTerminal(): Promise<void>;\n\n  register<T>(token: StoreToken<T>, value: T, options?: { owned?: boolean }): () => void;\n  replace<T>(token: StoreToken<T>, value: T, options?: { owned?: boolean }): void;\n  get<T>(token: StoreToken<T>): T | undefined;\n  require<T>(token: StoreToken<T>): T;\n  has<T>(token: StoreToken<T>): boolean;\n  remove<T>(token: StoreToken<T>, disposeOwned?: boolean): boolean;\n\n  dispose(): void;\n  disposeAsync(): Promise<void>;\n}\nfunction createStoreRegistry(runtime?: IRuntime): StoreRegistry;"
              },
              {
                "type": "paragraph",
                "text": "一般不需要在 React 组件树里手动实例化——`StoreProvider` 会替你创建（或接收你传入的一份）。手动创建常见于 SSR 请求作用域（每个请求一份独立的 Registry，请求结束后 `disposeAsync()`）或测试代码，见 `test/exports.test.ts`。"
              },
              {
                "type": "list",
                "items": [
                  "`register(token, value, { owned })`：同一个 token 不能重复注册，重复会抛 `[store] duplicate provider store token: X`；先前注册想替换请用 `replace`。返回一个注销函数（幂等，重复调用只有第一次生效）；`owned: true` 且实体带 `$dispose()`/`dispose()` 方法时，注销或整体 `dispose()` 会自动调用它。",
                  "`replace(token, value, options)`：无重复检查，直接覆盖；若旧值 `owned` 且与新值不是同一个引用，会释放旧值。",
                  "`require(token)`：未注册时抛 `[store] missing provider store: X`；`get`/`has` 是不抛的查询版本。",
                  "`remove(token, disposeOwned = true)`：从 Registry 摘除并按需释放；返回是否真的移除了什么。",
                  "所有写操作（`register`/`replace`/`get`/`require`/`has`/`remove`）在 Registry 已 `disposed` 后调用都会抛 `[store] provider registry is disposed`。",
                  "跨 Runtime 校验：`register`/`replace` 时会检查 `value` 自己声明的所属 Runtime（通过唯一所有权协议 `ownerOf`，不是靠约定字段名，伪造不了也不会漏判新节点类型）是否与 `registry.runtime` 一致，不一致直接抛 `[store] provider store \"X\" belongs to a different Runtime`——防止把属于别的 Runtime 的 Store/Atom 注册进一个不相关的 Registry。",
                  "`dispose()` 是同步、幂等的：反向遍历已注册的 owned 实体逐个释放，再释放 `atomStore`；某个实体的 `dispose()` 若返回一个 thenable，`dispose()` 不会等它（本身是同步方法），但会跟踪这个 Promise 并在其 reject 时经 `runtime.reportError` 上报，不会变成未处理的 rejection。多个实体报错时抛 `AggregateError('[store] provider registry disposal failed')`。",
                  "`disposeAsync()` 是异步、单飞（多次并发调用共享同一次执行）的对应版本：会真正 `await` 每个 thenable 释放结果；对一个已经同步 `dispose()` 过的 Registry 调用，不会当作\"已经完成\"直接 resolve，而是等待 `dispose()` 当时来不及等待、仍在跑的那些异步释放全部 settle。",
                  "thenable 采用 lifecycle 的统一接纳语义：只读取一次 `then` getter，并以原 thenable 作为 receiver 调用捕获到的函数。getter 抛出的原始异常不会被替换；同步 `dispose()` 会原样抛出，之后的 `disposeAsync()` 会通过其稳定 Promise 原样重放。",
                  "`whenTerminal()` / `lifecycle`：暴露统一的 `AsyncLifecycle` 形状（`open` → `closing` → `terminal`），`whenTerminal()` 在真正走到 `terminal`（含所有异步释放都 settle）才 resolve。"
                ]
              }
            ]
          },
          {
            "id": "5-错误处理完整参考",
            "heading": "5. 错误处理完整参考",
            "blocks": [
              {
                "type": "paragraph",
                "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
              },
              {
                "type": "paragraph",
                "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
              },
              {
                "type": "table",
                "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                "rows": [
                  [
                    "`registryDisposed`",
                    "`REGISTRY_DISPOSED`",
                    "Registry 终结后继续读写；创建新 Registry"
                  ],
                  [
                    "`registryDisposalFailed`",
                    "`REGISTRY_DISPOSAL_FAILED`",
                    "owned 实体释放失败；展开 `AggregateError.errors`"
                  ],
                  [
                    "`providerRequired`",
                    "`PROVIDER_REQUIRED`",
                    "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                  ],
                  [
                    "`featureDisabled`",
                    "`FEATURE_DISABLED`",
                    "所需 feature 未显式开启；修正 `config.features`"
                  ],
                  ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                  [
                    "`storeDuplicate`",
                    "`STORE_DUPLICATE`",
                    "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                  ],
                  [
                    "`crossRuntime`",
                    "`CROSS_RUNTIME`",
                    "Store 与 Registry 不属于同一 Runtime；统一所有权"
                  ],
                  [
                    "`invalidConfig`",
                    "`INVALID_CONFIG`",
                    "token/Provider/features 配置非法；按 message 修正"
                  ],
                  [
                    "`readyRejected`",
                    "`READY_REJECTED`",
                    "readiness barrier reject；检查保留在 `cause` 的底层失败"
                  ]
                ]
              },
              {
                "type": "table",
                "headers": ["消息", "触发条件", "处理方式"],
                "rows": [
                  [
                    "`[store] StoreProvider registry/runtime ownership mismatch`",
                    "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                    "只传其中一个，或保证 `registry.runtime === runtime`"
                  ],
                  [
                    "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                    "`config.ready` 数组在挂载后按元素比较发生了变化",
                    "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                  ],
                  [
                    "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                    "`features.wasm: true` 但没提供 `ready`",
                    "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                  ],
                  [
                    "`[store] ready barrier rejected: ...`",
                    "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                    "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                  ],
                  [
                    "`[store] hook requires a StoreProvider`",
                    "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                    "在组件树更靠上的位置套一层 `<StoreProvider>`"
                  ],
                  [
                    "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                    "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                    "套 `StoreProvider`"
                  ],
                  [
                    "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                    "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                    "在 `StoreProvider` 的 `config.features` 里显式开启"
                  ],
                  [
                    "`[store] provider registry is disposed`",
                    "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                    "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                  ],
                  [
                    "`[store] duplicate provider store token: X`",
                    "同一个 `StoreToken` 被 `register` 两次",
                    "改用 `replace`，或确认没有重复挂载注册逻辑"
                  ],
                  [
                    "`[store] missing provider store: X`",
                    "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                    "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                  ],
                  [
                    "`[store] provider store \"X\" belongs to a different Runtime`",
                    "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                    "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                  ],
                  [
                    "`[store] StoreToken requires a debug name`",
                    "`createStoreToken('')`",
                    "传一个非空的调试名"
                  ],
                  [
                    "`AggregateError('[store] provider registry disposal failed')`",
                    "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                    "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                  ]
                ]
              }
            ]
          },
          {
            "id": "6-1-谁拥有-registry",
            "heading": "6.1 谁拥有 Registry",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "Provider 自建（未传 `registry` prop）：`disposeOnUnmount` 默认 `true`，unmount 时自动 `dispose()`。",
                  "外部传入（`registry` prop）：`disposeOnUnmount` 默认 `false`，调用方负责在合适的时机自己 `dispose()`/`disposeAsync()`；显式传 `disposeOnUnmount={true}` 才会交给 Provider 释放。",
                  "`runtime` prop：无论 Registry 是谁创建的，`runtime` 本身的生命周期始终由调用方负责，`StoreProvider` 从不释放它——它只释放自己拥有的 Registry（及其 `atomStore`、注册的 owned 实体）。"
                ]
              }
            ]
          },
          {
            "id": "6-2-strictmode-下不误伤",
            "heading": "6.2 StrictMode 下不误伤",
            "blocks": [
              {
                "type": "paragraph",
                "text": "开发模式下 React StrictMode 会对每个组件的 effect 做\"挂载 → 卸载 → 再挂载\"的探测性重放。`RegistryBoundary` 的 `useEffect` 调用 `registry.retain(disposeOnRelease, deferTask)`："
              },
              {
                "type": "list",
                "items": [
                  "`retain()` 让内部 `retainCount` 自增，`release`（cleanup 函数）自减；只有 `retainCount` 归零且 `disposeOnRelease` 为真才会真正 `dispose()`，且这个 dispose 动作是延后执行的（`queueMicrotask`，或者当存在 ready 屏障时用 `setTimeout(0)`，即 `deferTask`）。",
                  "StrictMode 的\"卸载 → 立刻重新挂载\"发生在这个延后窗口之内：重新挂载会先把 `retainCount` 加回去，延后的 dispose 检查时发现计数不为零就放弃——不会把 Provider 内部创建的 Registry 提前销毁。",
                  "这套延后机制只保护 Provider 内部创建的 Registry；外部传入的 `registry`（`disposeOnUnmount` 显式为 `true`）走同一个 `retain`/`release` 路径，同样受益。"
                ]
              }
            ]
          },
          {
            "id": "6-3-被丢弃的并发渲染不会泄漏",
            "heading": "6.3 被丢弃的并发渲染不会泄漏",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`OwnedRegistryBoundary` 不再调用 `registry.prepareForRender()` 通过 wall-clock 猜测 abandoned render。React 没有可靠的“该 render 永久放弃”回调，任何定时器都可能在合法的 Suspense/concurrent commit 之前 dispose Registry，导致后续 `retain()` 失败。Registry 由可观察的 commit/unmount owner 释放；未提交候选的确定性回收需由外层 owner 或未来 GC-backed 机制承接。"
              }
            ]
          },
          {
            "id": "6-4-owned-实体的释放顺序",
            "heading": "6.4 owned 实体的释放顺序",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`StoreRegistry.dispose()`/`disposeAsync()` 按注册顺序的逆序释放 owned 实体（后注册的先释放）——如果 B 依赖 A（B 在 A 之后注册），这个顺序能让 B 在 A 之前被清理。非 owned 的实体（`register(token, value)` 不传 `owned: true`）永远不会被 Registry 触碰其生命周期，只是从内部 map 里摘除引用。"
              }
            ]
          },
          {
            "id": "7-注意事项详解",
            "heading": "7. 注意事项详解",
            "blocks": [
              {
                "type": "paragraph",
                "text": "1. `config.ready` 只在挂载时读取一次，之后的新引用会被忽略并打印警告。原因见 2.3：身份追踪只能在挂载时锁定一次基准，允许运行时静默切换等价于允许子树的初始化条件不可预测地重置——所以设计上选择了\"忽略新值 + 显式警告\"而不是\"偷偷重新初始化\"。修复方式是把数组包进 `useMemo(() => [ensureWasm], [])` 或提到模块作用域。 2. `features.wasm: true` 但 `ready` 为空会立刻触发一个本地报错屏障。这不是必须使用 `@migaia/store-wasm` 的强制耦合——你可以传任何等价的就绪 Promise——但这个检查本身是刻意的：wasm 初始化几乎总是异步的，\"打开开关却没有对应的就绪等待\"几乎总是遗漏了 `ready`，本包选择在开发期就报错而不是让子树在 wasm 未就绪时静默渲染出错误状态。 3."
              },
              {
                "type": "paragraph",
                "text": "Registry 释放不等于 Runtime 释放（见 2.1）。这个区分是为了支持\"多个 Provider 共用同一个 Runtime\"的场景（比如同一个应用里多棵独立子树共享底层响应式图，但各自有独立的作用域化 Registry）；如果 Provider 顺手把传入的 Runtime 也释放了，会让这种共享场景在任意一个 Provider 卸载时炸掉其它还在用这个 Runtime 的子树。 4."
              },
              {
                "type": "paragraph",
                "text": "`useAtomDefinition`/`useSetAtomDefinition`/`useStoreFromProvider`/`useProvidedStore`/`useStoreRegistry`/`useStoreRuntime` 必须有 `StoreProvider`，没有直接抛错；而 `useAtomValue`/`useSetAtom`/`useAtom` 容忍缺失 Provider，退化为直接订阅 atom 自身（见 3.6）。区分原则是：前一组的语义本来就是\"读写 Provider 的作用域化状态\"，没有 Provider 这件事本身没有意义；后一组的语义是\"给任意 atom 协议对象接线\"，Provider 只是可选的增强。 5. render 阶段不要写状态。`useStore`/`useSignal`/`useTracked` 等的写操作（Action、`signal."
              },
              {
                "type": "paragraph",
                "text": "value = x`、`store.field = x`）只应该出现在事件回调或 effect 里。这不是本包特有的规则，而是 React 对\"渲染必须是纯函数\"的一般要求叠加上响应式图的具体后果：在 render 期间写入会在同一次渲染的求值窗口内触发一次新的通知，容易和当前正在进行的捕获/提交流程产生竞态，表现为额外的重渲染甚至 `useSyncExternalStore` 的一致性警告。 6. selector/equality/atom 引用要稳定，不要在 render 里 `new` 一个 Store/Signal/Resource。每次渲染都创建新实例，等于每次渲染都在订阅一个全新的、状态清零的对象——不仅浪费（每次都要重新走一遍 `useTracked` 的依赖捕获），而且如果这个实例本身持有资源（比如 `Resource` 发起请求），会在每次渲染时重复触发副作用。"
              },
              {
                "type": "paragraph",
                "text": "正确的做法是让这些实例来自模块作用域单例、`useMemo`，或者通过 `StoreProvider`/`StoreRegistry` 注入。"
              }
            ]
          },
          {
            "id": "8-贴近生产的完整示例",
            "heading": "8. 贴近生产的完整示例",
            "blocks": [
              {
                "type": "paragraph",
                "text": "下面这个例子把 `StoreProvider`、就绪屏障、feature flag、`StoreToken` 依赖注入、`useProvidedStore`、Suspense-safe 的异步读取（`useResourceValue`）组合在一起，模拟\"应用启动前先恢复本地状态，某个子树按需开启 wasm 能力，业务 Store 通过 token 注入而不是全局单例\"的场景。"
              },
              {
                "type": "code",
                "language": "tsx",
                "code": "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport type { IReactiveStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\nimport type { IRuntime } from '@migaia/reactive';\n\ntype ISession = { userId: string; setUserId(id: string): void };\nconst sessionToken = createStoreToken<IReactiveStore<ISession>>('session');\n\nfunction restoreSession(): Promise<void> {\n  // 例如从 localStorage/远端恢复登录态；调用方负责把它 memo 住。\n  return Promise.resolve();\n}\n\nfunction App() {\n  // ready 数组的引用必须稳定，否则每次渲染都会命中 §2.3 的身份检查警告。\n  const ready = useMemo(() => [restoreSession], []);\n  const sessionStore = useMemo(\n    () =>\n      createStore<ISession>({\n        userId: '',\n        setUserId(id) {\n          this.userId = id;\n        }\n      }),\n    []\n  );\n\n  return (\n    <StoreProvider config={{ ready, fallback: <FullPageSpinner /> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<FullPageSpinner />}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\n// 用 register 而非全局单例：不同的 Provider 子树可以注入不同的 session 实例（比如测试）。\nfunction SessionInjector({\n  store,\n  children\n}: {\n  store: IReactiveStore<ISession>;\n  children: React.ReactNode;\n}) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return <>{children}</>;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const experimentalCharts = useStoreFeature('experimental.charts');\n  return (\n    <div>\n      <p>当前用户：{userId || '未登录'}</p>\n      {experimentalCharts ? <ChartsPanel userId={userId} /> : null}\n    </div>\n  );\n}\n\nconst profileResources = new Map<string, Resource<{ name: string }>>();\nfunction profileResource(userId: string, runtime: IRuntime): Resource<{ name: string }> {\n  let resource = profileResources.get(userId);\n  if (!resource) {\n    resource = new Resource(\n      async ({ signal }) => {\n        const res = await fetch(`/api/profile/${userId}`, { signal });\n        return res.json() as Promise<{ name: string }>;\n      },\n      runtime,\n      { debugName: `profile:${userId}` }\n    );\n    profileResources.set(userId, resource);\n  }\n  return resource;\n}\n\nfunction ChartsPanel({ userId }: { userId: string }) {\n  const runtime = useStoreRuntime();\n  // profileResource 按 userId 缓存同一个 Resource 实例，避免每次渲染都创建新对象（见 §7.6）。\n  const profile = useResourceValue(profileResource(userId, runtime)); // 未就绪时 throw promise，交给外层 Suspense\n  return <div>欢迎，{profile.name}</div>;\n}\n\nfunction FullPageSpinner() {\n  return <div role=\"status\">加载中…</div>;\n}"
              },
              {
                "type": "paragraph",
                "text": "要点对照："
              },
              {
                "type": "list",
                "items": [
                  "`ready`/`sessionStore` 都用 `useMemo` 稳住引用，避免命中 §2.3、§7.6 的坑。",
                  "`SessionInjector` 用 `registry.register(token, store, { owned: false })` 注入——`owned: false` 是因为 `sessionStore` 的生命周期由 `App` 组件自己的 `useMemo` 管理，不希望 Provider 卸载时把它一并释放。",
                  "`useProvidedStore` 一步做完\"从 Registry 取回 + `useStore` 订阅\"。",
                  "`experimental.charts` 需要在更上层的 `StoreProvider` 里显式 `config={{ features: { experimental: { charts: true } } }}` 才会为 `true`；示例里省略了这层配置，实际接入时按需打开。",
                  "`ChartsPanel` 里的 `useResourceValue` 展示的是 Suspense 集成，不是本包创建 `Resource` 的推荐写法——生产代码通常会把 `Resource` 的创建与缓存策略（`ttl`、`autoStart` 等）放进专门的数据层模块。"
                ]
              }
            ]
          },
          {
            "id": "9-常见问题排查",
            "heading": "9. 常见问题排查",
            "blocks": [
              {
                "type": "paragraph",
                "text": "Q: 组件树里到处出现 `[store] hook requires a StoreProvider`，但我明明套了 `<StoreProvider>`。 A: 确认调用 hook 的组件确实在 `<StoreProvider>` 的 `children` 内部渲染，而不是通过 `createPortal` 挂到了 DOM 树的其它位置——React Context 跟的是组件树而不是 DOM 树，这种情况一般没问题；更常见的原因是把 `StoreProvider` 放在了某个条件渲染分支之外，实际渲染路径没有经过它。"
              },
              {
                "type": "paragraph",
                "text": "Q: `useStore`/`useAtomValue` 明明数据变了，组件却没有重渲染。 A: 先确认 `selector`/`read` 里真的读取了发生变化的字段——`useTracked`/`useStore` 只追踪 selector 函数体内实际访问到的路径，没有读到的字段变化不会触发重渲染。其次检查是否在 render 之外（比如某个不受追踪的回调里）读取后缓存了值。"
              },
              {
                "type": "paragraph",
                "text": "Q: `StoreProvider` 套了 `config.ready`，但页面一直卡在 `fallback` 不消失。 A: 打开浏览器控制台确认 ready 屏障对应的 Promise 是否真的 resolve；如果它一直 pending（比如接口挂起没返回），`ReadyBoundary` 会一直渲染 `fallback`。如果屏障 reject 了但页面既没有报错也没有渲染子树，检查是否缺少外层 Error Boundary——`ReadyBoundary` 在 reject 时会 `throw`，没有 Error Boundary 捕获时会导致 React 整棵树卸载并在控制台报未捕获错误。"
              },
              {
                "type": "paragraph",
                "text": "Q: `useAssertStoreFeature('experimental.xxx')` 一直抛错，我确实在 `StoreProvider` 上配置了这个 feature。 A: 检查 `config.features.experimental` 里对应 key 的值是否严格是布尔 `true`——`freezeExperimental`/`readStoreFeature` 只认字面 `true`，`'true'`（字符串）、`1`、`undefined` 都会被当作关闭。另外确认调用 `useAssertStoreFeature` 的组件是在目标 `StoreProvider` 的子树内，而不是外层某个没配置这个 feature 的 Provider 下。"
              },
              {
                "type": "paragraph",
                "text": "Q: SSR 场景下每个请求要一份独立状态，`StoreProvider` 够用吗？ A: `StoreProvider` 是给单次渲染树用的；多请求隔离通常直接用 `createStoreRegistry(createRuntime())` 手动为每个请求建一份 Registry，渲染时通过 `registry` prop 传给 `StoreProvider`（此时 `disposeOnUnmount` 默认为 `false`，请求处理完毕后自己调用 `registry.disposeAsync()`）。具体的 SSR 集成方式请参考 `@migaia/store-ssr` 的文档。"
              }
            ]
          },
          {
            "id": "10-构建-格式化与测试",
            "heading": "10. 构建、格式化与测试",
            "blocks": [
              {
                "type": "paragraph",
                "text": "在仓库根目录运行："
              },
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm --filter @migaia/store-react fmt\npnpm --filter @migaia/store-react lint\npnpm --filter @migaia/store-react typecheck\npnpm --filter @migaia/store-react typecheck:test\npnpm --filter @migaia/store-react test\npnpm --filter @migaia/store-react typecheck:e2e\npnpm --filter @migaia/store-react test:e2e\npnpm --filter @migaia/store-react build"
              },
              {
                "type": "paragraph",
                "text": "`test` 覆盖 hook、Provider、Registry 与错误边界；`test:e2e` 用 Playwright 验证真实 React 挂载路径。运行后者前需有 Playwright 浏览器。"
              }
            ]
          }
        ]
      }
    }
  },
  "apis": [
    {
      "id": "store-react:index",
      "library": "store-react",
      "module": "index",
      "exportPath": ".",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/store-react/getting-started",
      "architecturePath": "/en/architecture/store-react",
      "symbols": [
        {
          "name": "assertStoreFeature",
          "kind": "function",
          "signature": "export declare function assertStoreFeature(config: IStoreConfigValue | null, path: IStoreFeaturePath, apiName?: string): void;",
          "declarationLine": 63,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "assertStoreFeature is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for assertStoreFeature.",
          "advanced": "No additional advanced behavior is declared for assertStoreFeature.",
          "parameters": [
            "config: IStoreConfigValue | null",
            "path: IStoreFeaturePath",
            "apiName: string (optional)"
          ],
          "parameterDetails": [
            {
              "name": "config",
              "type": "IStoreConfigValue | null",
              "optional": false
            },
            {
              "name": "path",
              "type": "IStoreFeaturePath",
              "optional": false
            },
            {
              "name": "apiName",
              "type": "string",
              "optional": true
            }
          ],
          "returns": "void",
          "errors": ["No documented errors are declared for assertStoreFeature."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for assertStoreFeature.",
          "examples": [],
          "members": [],
          "aliasOf": "assertStoreFeature",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function assertStoreFeature(config: IStoreConfigValue | null, path: IStoreFeaturePath, apiName?: string): void;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "config",
                "type": "IStoreConfigValue | null",
                "optional": false
              },
              {
                "name": "path",
                "type": "IStoreFeaturePath",
                "optional": false
              },
              {
                "name": "apiName",
                "type": "string",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["void"],
          "whenToUse": "Use assertStoreFeature when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when assertStoreFeature does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "assertStoreFeature is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import assertStoreFeature from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use assertStoreFeature when its function contract and declared inputs match the result you need. Choose a different public symbol when assertStoreFeature does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for assertStoreFeature."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for assertStoreFeature."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:assertStoreFeature:export declare function assertStoreFeature(config: IStoreConfigValue | null, path: IStoreFeaturePath, apiName?: string): void;",
          "guidance": [
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            },
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 30,
          "configuration": [],
          "fragment": "store-react-index--assertStoreFeature"
        },
        {
          "name": "createStoreReactAggregateError",
          "kind": "function",
          "signature": "export declare function createStoreReactAggregateError(code: IStoreReactErrorCode, errors: readonly unknown[], message: string): AggregateError;",
          "declarationLine": 10,
          "source": "packages/store-react/dist/errors.d.ts",
          "purpose": "Builds a `(source, code)`-tagged `AggregateError`, preserving `errors[]` as a cause path.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreReactAggregateError.",
          "advanced": "No additional advanced behavior is declared for createStoreReactAggregateError.",
          "parameters": [
            "code: IStoreReactErrorCode",
            "errors: readonly unknown[]",
            "message: string"
          ],
          "parameterDetails": [
            {
              "name": "code",
              "type": "IStoreReactErrorCode",
              "optional": false
            },
            {
              "name": "errors",
              "type": "readonly unknown[]",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "AggregateError",
          "errors": ["No documented errors are declared for createStoreReactAggregateError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createStoreReactAggregateError.",
          "examples": [],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createStoreReactAggregateError(code: IStoreReactErrorCode, errors: readonly unknown[], message: string): AggregateError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "code",
                "type": "IStoreReactErrorCode",
                "optional": false
              },
              {
                "name": "errors",
                "type": "readonly unknown[]",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["AggregateError"],
          "whenToUse": "Use createStoreReactAggregateError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createStoreReactAggregateError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Builds a `(source, code)`-tagged `AggregateError`, preserving `errors[]` as a cause path."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createStoreReactAggregateError from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createStoreReactAggregateError when its function contract and declared inputs match the result you need. Choose a different public symbol when createStoreReactAggregateError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreReactAggregateError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createStoreReactAggregateError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/errors.d.ts:createStoreReactAggregateError:export declare function createStoreReactAggregateError(code: IStoreReactErrorCode, errors: readonly unknown[], message: string): AggregateError;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 4,
          "configuration": [],
          "fragment": "store-react-index--createStoreReactAggregateError"
        },
        {
          "name": "createStoreReactError",
          "kind": "function",
          "signature": "export declare function createStoreReactError(code: IStoreReactErrorCode, message: string, options?: { readonly cause?: unknown; }): Error;",
          "declarationLine": 6,
          "source": "packages/store-react/dist/errors.d.ts",
          "purpose": "Builds a `(source, code)`-tagged `Error` without touching `stack`.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreReactError.",
          "advanced": "No additional advanced behavior is declared for createStoreReactError.",
          "parameters": [
            "code: IStoreReactErrorCode",
            "message: string",
            "options: { readonly cause?: unknown; } (optional)"
          ],
          "parameterDetails": [
            {
              "name": "code",
              "type": "IStoreReactErrorCode",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly cause?: unknown; }",
              "optional": true
            }
          ],
          "returns": "Error",
          "errors": ["No documented errors are declared for createStoreReactError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createStoreReactError.",
          "examples": [],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createStoreReactError(code: IStoreReactErrorCode, message: string, options?: { readonly cause?: unknown; }): Error;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "code",
                "type": "IStoreReactErrorCode",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly cause?: unknown; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["Error"],
          "whenToUse": "Use createStoreReactError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createStoreReactError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Builds a `(source, code)`-tagged `Error` without touching `stack`."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createStoreReactError from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createStoreReactError when its function contract and declared inputs match the result you need. Choose a different public symbol when createStoreReactError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreReactError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createStoreReactError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/errors.d.ts:createStoreReactError:export declare function createStoreReactError(code: IStoreReactErrorCode, message: string, options?: { readonly cause?: unknown; }): Error;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 4,
          "configuration": [],
          "fragment": "store-react-index--createStoreReactError"
        },
        {
          "name": "createStoreRegistry",
          "kind": "function",
          "signature": "export declare function createStoreRegistry(runtime?: IRuntime): StoreRegistry;",
          "declarationLine": 72,
          "source": "packages/store-react/dist/provider-registry.d.ts",
          "purpose": "createStoreRegistry is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreRegistry.",
          "advanced": "No additional advanced behavior is declared for createStoreRegistry.",
          "parameters": ["runtime: IRuntime (optional)"],
          "parameterDetails": [
            {
              "name": "runtime",
              "type": "IRuntime",
              "optional": true
            }
          ],
          "returns": "StoreRegistry",
          "errors": ["No documented errors are declared for createStoreRegistry."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createStoreRegistry.",
          "examples": [
            "const registry = createStoreRegistry(); // runtime 不传则内部 createRuntime()\nconst unregister = registry.register(sessionToken, sessionStore, { owned: true });\nregistry.dispose(); // 同步、幂等；owned 实体按注册顺序的逆序释放",
            "import { StoreProvider, createStoreRegistry } from '@migaia/store-react';\nimport { createRuntime } from '@migaia/reactive';\n\nasync function renderForRequest(children: React.ReactNode) {\n  const registry = createStoreRegistry(createRuntime()); // 每个请求独立一份\n  const html = renderToString(<StoreProvider registry={registry}>{children}</StoreProvider>); // disposeOnUnmount 默认 false：手动负责释放\n  await registry.disposeAsync();\n  return html;\n}"
          ],
          "members": [],
          "aliasOf": "createStoreRegistry",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createStoreRegistry(runtime?: IRuntime): StoreRegistry;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "runtime",
                "type": "IRuntime",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["StoreRegistry"],
          "whenToUse": "Use createStoreRegistry when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createStoreRegistry does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createStoreRegistry is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createStoreRegistry from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createStoreRegistry when its function contract and declared inputs match the result you need. Choose a different public symbol when createStoreRegistry does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreRegistry."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createStoreRegistry."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider-registry.d.ts:createStoreRegistry:export declare function createStoreRegistry(runtime?: IRuntime): StoreRegistry;",
          "guidance": [],
          "usageScore": 42,
          "configuration": [],
          "fragment": "store-react-index--createStoreRegistry"
        },
        {
          "name": "createStoreToken",
          "kind": "function",
          "signature": "export declare function createStoreToken<T>(debugName: string): IStoreToken<T>;",
          "declarationLine": 22,
          "source": "packages/store-react/dist/provider-registry.d.ts",
          "purpose": "createStoreToken is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreToken.",
          "advanced": "No additional advanced behavior is declared for createStoreToken.",
          "parameters": ["debugName: string"],
          "parameterDetails": [
            {
              "name": "debugName",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "IStoreToken<T>",
          "errors": ["No documented errors are declared for createStoreToken."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createStoreToken.",
          "examples": [
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\n\nconst sessionToken = createStoreToken('session');\n\nfunction App() {\n  const ready = useMemo(() => [() => Promise.resolve()], []); // 引用必须稳定\n  const sessionStore = useMemo(() => createStore({ userId: '' }), []);\n\n  return (\n    <StoreProvider config={{ ready, fallback: <div>加载中…</div> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<div>加载中…</div>}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\nfunction SessionInjector({ store, children }) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return children;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const runtime = useStoreRuntime();\n  const showCharts = useStoreFeature('experimental.charts');\n  const profile = useResourceValue(new Resource(async () => ({ name: userId }), runtime));\n  return (\n    <p>\n      {profile.name}\n      {showCharts ? ' · 图表已开启' : ''}\n    </p>\n  );\n}"
          ],
          "members": [],
          "aliasOf": "createStoreToken",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createStoreToken<T>(debugName: string): IStoreToken<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "debugName",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IStoreToken<T>"],
          "whenToUse": "Use createStoreToken when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createStoreToken does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createStoreToken is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createStoreToken from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createStoreToken when its function contract and declared inputs match the result you need. Choose a different public symbol when createStoreToken does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStoreToken."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createStoreToken."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider-registry.d.ts:createStoreToken:export declare function createStoreToken<T>(debugName: string): IStoreToken<T>;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 66,
          "configuration": [],
          "fragment": "store-react-index--createStoreToken"
        },
        {
          "name": "IStableNode",
          "kind": "type",
          "signature": "export type IStableNode<T> = { readonly value: T; peek(): T; };",
          "declarationLine": 17,
          "source": "packages/store-react/dist/useNode.d.ts",
          "purpose": "IStableNode is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStableNode.",
          "advanced": "No additional advanced behavior is declared for IStableNode.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStableNode."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStableNode.",
          "examples": [
            "type IStableNode<T> = { readonly value: T; peek(): T };\nfunction useNodeValue<T>(node: IStableNode<T>, runtime: IRuntime, enabled?: boolean): T;"
          ],
          "members": [],
          "aliasOf": "IStableNode",
          "exportPath": ".",
          "overloadSignatures": ["export type IStableNode<T> = { readonly value: T; peek(): T; };"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStableNode when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStableNode does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStableNode is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStableNode from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStableNode when its type contract and declared inputs match the result you need. Choose a different public symbol when IStableNode does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStableNode."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStableNode."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useNode.d.ts:IStableNode:export type IStableNode<T> = { readonly value: T; peek(): T; };",
          "guidance": [],
          "usageScore": 16,
          "configuration": [],
          "fragment": "store-react-index--IStableNode"
        },
        {
          "name": "IStoreConfigValue",
          "kind": "type",
          "signature": "export type IStoreConfigValue = { readonly features: { readonly wasm: boolean; readonly experimental: Readonly<Record<string, boolean>>; }; readonly defaults: IStoreProviderDefaults; /** 归一化后的就绪跟踪；无屏障时为 null。 */ readonly ready: ITrackedReady | null; };",
          "declarationLine": 43,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "IStoreConfigValue is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreConfigValue.",
          "advanced": "No additional advanced behavior is declared for IStoreConfigValue.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreConfigValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreConfigValue.",
          "examples": [
            "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;",
            "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
          ],
          "members": [],
          "aliasOf": "IStoreConfigValue",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreConfigValue = { readonly features: { readonly wasm: boolean; readonly experimental: Readonly<Record<string, boolean>>; }; readonly defaults: IStoreProviderDefaults; /** 归一化后的就绪跟踪；无屏障时为 null。 */ readonly ready: ITrackedReady | null; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreConfigValue when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreConfigValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreConfigValue is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreConfigValue from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreConfigValue when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreConfigValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreConfigValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreConfigValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:IStoreConfigValue:export type IStoreConfigValue = { readonly features: { readonly wasm: boolean; readonly experimental: Readonly<Record<string, boolean>>; }; readonly defaults: IStoreProviderDefaults; /** 归一化后的就绪跟踪；无屏障时为 null。 */ readonly ready: ITrackedReady | null; };",
          "guidance": [
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            },
            {
              "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
              "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
                },
                {
                  "type": "list",
                  "items": [
                    "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                    "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                    "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 32,
          "configuration": [],
          "fragment": "store-react-index--IStoreConfigValue"
        },
        {
          "name": "IStoreFeatureExperimental",
          "kind": "type",
          "signature": "export type IStoreFeatureExperimental = Readonly<Record<string, boolean>>;",
          "declarationLine": 11,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "应用级 Store 配置（挂在 StoreProvider 上）。 与 Runtime / AtomStore 隔离正交： - Provider 的 registry/runtime = 图的所有权 - config = 这棵树声明要哪些增强、就绪前是否挡住子树 `features.wasm === true` 时必须同时提供 `ready`（通常放入 `ensureWasm` from `@migaia/store-wasm`），否则在开发期直接抛错——避免「开了开关却没人 init」。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreFeatureExperimental.",
          "advanced": "No additional advanced behavior is declared for IStoreFeatureExperimental.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreFeatureExperimental."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreFeatureExperimental.",
          "examples": [],
          "members": [],
          "aliasOf": "IStoreFeatureExperimental",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreFeatureExperimental = Readonly<Record<string, boolean>>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreFeatureExperimental when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreFeatureExperimental does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "应用级 Store 配置（挂在 StoreProvider 上）。 与 Runtime / AtomStore 隔离正交： - Provider 的 registry/runtime = 图的所有权 - config = 这棵树声明要哪些增强、就绪前是否挡住子树 `features.wasm === true` 时必须同时提供 `ready`（通常放入 `ensureWasm` from `@migaia/store-wasm`），否则在开发期直接抛错——避免「开了开关却没人 init」。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreFeatureExperimental from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreFeatureExperimental when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreFeatureExperimental does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreFeatureExperimental."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreFeatureExperimental."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:IStoreFeatureExperimental:export type IStoreFeatureExperimental = Readonly<Record<string, boolean>>;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "store-react-index--IStoreFeatureExperimental"
        },
        {
          "name": "IStoreFeaturePath",
          "kind": "type",
          "signature": "export type IStoreFeaturePath = 'wasm' | `experimental.${string}`;",
          "declarationLine": 61,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "IStoreFeaturePath is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreFeaturePath.",
          "advanced": "No additional advanced behavior is declared for IStoreFeaturePath.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreFeaturePath."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreFeaturePath.",
          "examples": [],
          "members": [],
          "aliasOf": "IStoreFeaturePath",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreFeaturePath = 'wasm' | `experimental.${string}`;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreFeaturePath when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreFeaturePath does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreFeaturePath is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreFeaturePath from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreFeaturePath when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreFeaturePath does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreFeaturePath."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreFeaturePath."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:IStoreFeaturePath:export type IStoreFeaturePath = 'wasm' | `experimental.${string}`;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "store-react-index--IStoreFeaturePath"
        },
        {
          "name": "IStoreFeatures",
          "kind": "type",
          "signature": "export type IStoreFeatures = { /** 声明本树会使用 wasm 字段。 默认 false：不强制就绪屏障。 true：必须配合 `ready` 完成 init（见 StoreProvider）。 */ readonly wasm?: boolean; /** 实验特性白名单。未列出或非严格 `true` 视为关闭。 实验 API 应 `assertStoreFeature('experimental.xxx')`。 */ readonly experimental?: IStoreFeatureExperimental; };",
          "declarationLine": 12,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "IStoreFeatures is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreFeatures.",
          "advanced": "No additional advanced behavior is declared for IStoreFeatures.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreFeatures."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreFeatures.",
          "examples": [],
          "members": [],
          "aliasOf": "IStoreFeatures",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreFeatures = { /** 声明本树会使用 wasm 字段。 默认 false：不强制就绪屏障。 true：必须配合 `ready` 完成 init（见 StoreProvider）。 */ readonly wasm?: boolean; /** 实验特性白名单。未列出或非严格 `true` 视为关闭。 实验 API 应 `assertStoreFeature('experimental.xxx')`。 */ readonly experimental?: IStoreFeatureExperimental; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreFeatures when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreFeatures does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreFeatures is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreFeatures from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreFeatures when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreFeatures does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreFeatures."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreFeatures."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:IStoreFeatures:export type IStoreFeatures = { /** 声明本树会使用 wasm 字段。 默认 false：不强制就绪屏障。 true：必须配合 `ready` 完成 init（见 StoreProvider）。 */ readonly wasm?: boolean; /** 实验特性白名单。未列出或非严格 `true` 视为关闭。 实验 API 应 `assertStoreFeature('experimental.xxx')`。 */ readonly experimental?: IStoreFeatureExperimental; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "store-react-index--IStoreFeatures"
        },
        {
          "name": "IStoreProviderConfig",
          "kind": "type",
          "signature": "export type IStoreProviderConfig = { readonly features?: IStoreFeatures; /** 子树渲染前必须 settle 的屏障。 例：`ready: [ensureWasm]`（从 `@migaia/store-wasm` 引入）。 */ readonly ready?: readonly IStoreReadyBarrier[]; /** 屏障未完成时的占位。 */ readonly fallback?: ReactNode; /** 只读业务默认值快照；Provider 不会把这些值自动写入各 Store。 */ readonly defaults?: IStoreProviderDefaults; };",
          "declarationLine": 27,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "IStoreProviderConfig is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderConfig.",
          "advanced": "No additional advanced behavior is declared for IStoreProviderConfig.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreProviderConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreProviderConfig.",
          "examples": [
            "type IStoreProviderProps = {\n  readonly children: ReactNode;\n  readonly registry?: StoreRegistry;\n  readonly runtime?: IRuntime;\n  readonly disposeOnUnmount?: boolean;\n  readonly config?: IStoreProviderConfig;\n};",
            "type IStoreProviderConfig = {\n  readonly features?: {\n    readonly wasm?: boolean;\n    readonly experimental?: Readonly<Record<string, boolean>>;\n  };\n  readonly ready?: readonly (Promise<unknown> | (() => Promise<unknown> | unknown))[];\n  readonly fallback?: ReactNode;\n  readonly defaults?: { readonly warnAsyncActions?: boolean };\n};",
            "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
          ],
          "members": [],
          "aliasOf": "IStoreProviderConfig",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreProviderConfig = { readonly features?: IStoreFeatures; /** 子树渲染前必须 settle 的屏障。 例：`ready: [ensureWasm]`（从 `@migaia/store-wasm` 引入）。 */ readonly ready?: readonly IStoreReadyBarrier[]; /** 屏障未完成时的占位。 */ readonly fallback?: ReactNode; /** 只读业务默认值快照；Provider 不会把这些值自动写入各 Store。 */ readonly defaults?: IStoreProviderDefaults; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreProviderConfig when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreProviderConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreProviderConfig is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreProviderConfig from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreProviderConfig when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreProviderConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreProviderConfig."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:IStoreProviderConfig:export type IStoreProviderConfig = { readonly features?: IStoreFeatures; /** 子树渲染前必须 settle 的屏障。 例：`ready: [ensureWasm]`（从 `@migaia/store-wasm` 引入）。 */ readonly ready?: readonly IStoreReadyBarrier[]; /** 屏障未完成时的占位。 */ readonly fallback?: ReactNode; /** 只读业务默认值快照；Provider 不会把这些值自动写入各 Store。 */ readonly defaults?: IStoreProviderDefaults; };",
          "guidance": [
            {
              "id": "2-2-config-的四个字段",
              "heading": "2.2 config 的四个字段",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IStoreProviderConfig = {\n  readonly features?: {\n    readonly wasm?: boolean;\n    readonly experimental?: Readonly<Record<string, boolean>>;\n  };\n  readonly ready?: readonly (Promise<unknown> | (() => Promise<unknown> | unknown))[];\n  readonly fallback?: ReactNode;\n  readonly defaults?: { readonly warnAsyncActions?: boolean };\n};"
                },
                {
                  "type": "list",
                  "items": [
                    "`features.wasm`：声明本树会用到 wasm 字段。为 `true` 且 `ready` 为空数组时，会自动追加一个立刻 reject 的屏障（见 5），避免\"开了开关却没人 init\"。",
                    "`features.experimental`：白名单对象，只有值严格为 `true` 的 key 才算开启；未列出或非 `true` 一律视为关闭。",
                    "`ready`：见 2.3。",
                    "`fallback`：`ready` 屏障 pending 期间渲染的占位内容，默认 `null`（不是 `undefined`——不传 `fallback` 且有 `ready` 时，pending 阶段渲染的是\"什么都不渲染\"而不是上一次的 children）。",
                    "`defaults.warnAsyncActions`：只是配置快照的一部分，通过 `useStoreConfig()` 暴露给业务代码读取；`StoreProvider` 不会把它自动传给 `createStore`——各个 Store 仍要自己在 `createStore(shape, { warnAsyncActions: true })` 里显式声明。这个字段更适合用作\"业务代码统一读一个地方决定要不要传\"的约定，而不是自动生效的开关。"
                  ]
                }
              ]
            },
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 36,
          "configuration": [],
          "fragment": "store-react-index--IStoreProviderConfig"
        },
        {
          "name": "IStoreProviderDefaults",
          "kind": "type",
          "signature": "export type IStoreProviderDefaults = { /** * 仅挂在 config 快照上供业务读取（useStoreConfig）， **不会**自动传给 createStore——createStore 仍要自己写 * options.warnAsyncActions。 */ readonly warnAsyncActions?: boolean; };",
          "declarationLine": 20,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "IStoreProviderDefaults is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderDefaults.",
          "advanced": "No additional advanced behavior is declared for IStoreProviderDefaults.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreProviderDefaults."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreProviderDefaults.",
          "examples": [],
          "members": [],
          "aliasOf": "IStoreProviderDefaults",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreProviderDefaults = { /** * 仅挂在 config 快照上供业务读取（useStoreConfig）， **不会**自动传给 createStore——createStore 仍要自己写 * options.warnAsyncActions。 */ readonly warnAsyncActions?: boolean; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreProviderDefaults when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreProviderDefaults does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreProviderDefaults is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreProviderDefaults from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreProviderDefaults when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreProviderDefaults does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderDefaults."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreProviderDefaults."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:IStoreProviderDefaults:export type IStoreProviderDefaults = { /** * 仅挂在 config 快照上供业务读取（useStoreConfig）， **不会**自动传给 createStore——createStore 仍要自己写 * options.warnAsyncActions。 */ readonly warnAsyncActions?: boolean; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "store-react-index--IStoreProviderDefaults"
        },
        {
          "name": "IStoreProviderProps",
          "kind": "type",
          "signature": "export type IStoreProviderProps = { readonly children: ReactNode; readonly registry?: StoreRegistry; readonly runtime?: IRuntime; /** * Internal registries dispose on unmount by default. External registries remain caller-owned * unless this flag is explicitly true. */ readonly disposeOnUnmount?: boolean; /** 应用级配置：特性开关 + 就绪屏障。 省略时与历史行为一致（无屏障、features 全关）。 */ readonly config?: IStoreProviderConfig; };",
          "declarationLine": 7,
          "source": "packages/store-react/dist/StoreProvider.d.ts",
          "purpose": "IStoreProviderProps is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderProps.",
          "advanced": "No additional advanced behavior is declared for IStoreProviderProps.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreProviderProps."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreProviderProps.",
          "examples": [
            "type IStoreProviderProps = {\n  readonly children: ReactNode;\n  readonly registry?: StoreRegistry;\n  readonly runtime?: IRuntime;\n  readonly disposeOnUnmount?: boolean;\n  readonly config?: IStoreProviderConfig;\n};"
          ],
          "members": [],
          "aliasOf": "IStoreProviderProps",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreProviderProps = { readonly children: ReactNode; readonly registry?: StoreRegistry; readonly runtime?: IRuntime; /** * Internal registries dispose on unmount by default. External registries remain caller-owned * unless this flag is explicitly true. */ readonly disposeOnUnmount?: boolean; /** 应用级配置：特性开关 + 就绪屏障。 省略时与历史行为一致（无屏障、features 全关）。 */ readonly config?: IStoreProviderConfig; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreProviderProps when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreProviderProps does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreProviderProps is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreProviderProps from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreProviderProps when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreProviderProps does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderProps."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreProviderProps."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/StoreProvider.d.ts:IStoreProviderProps:export type IStoreProviderProps = { readonly children: ReactNode; readonly registry?: StoreRegistry; readonly runtime?: IRuntime; /** * Internal registries dispose on unmount by default. External registries remain caller-owned * unless this flag is explicitly true. */ readonly disposeOnUnmount?: boolean; /** 应用级配置：特性开关 + 就绪屏障。 省略时与历史行为一致（无屏障、features 全关）。 */ readonly config?: IStoreProviderConfig; };",
          "guidance": [],
          "usageScore": 12,
          "configuration": [],
          "fragment": "store-react-index--IStoreProviderProps"
        },
        {
          "name": "IStoreProviderState",
          "kind": "type",
          "signature": "export type IStoreProviderState = (typeof StoreProviderState)[keyof typeof StoreProviderState];",
          "declarationLine": 9,
          "source": "packages/store-react/dist/provider-state-constants.d.ts",
          "purpose": "IStoreProviderState is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderState.",
          "advanced": "No additional advanced behavior is declared for IStoreProviderState.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreProviderState."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreProviderState.",
          "examples": [
            "const StoreProviderState = { pending: 'pending', ready: 'ready', error: 'error' } as const;\ntype IStoreProviderState = 'pending' | 'ready' | 'error';"
          ],
          "members": [],
          "aliasOf": "IStoreProviderState",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreProviderState = (typeof StoreProviderState)[keyof typeof StoreProviderState];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreProviderState when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreProviderState does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreProviderState is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreProviderState from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreProviderState when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreProviderState does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreProviderState."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreProviderState."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider-state-constants.d.ts:IStoreProviderState:export type IStoreProviderState = (typeof StoreProviderState)[keyof typeof StoreProviderState];",
          "guidance": [],
          "usageScore": 22,
          "configuration": [],
          "fragment": "store-react-index--IStoreProviderState"
        },
        {
          "name": "IStoreReadyBarrier",
          "kind": "type",
          "signature": "export type IStoreReadyBarrier = Promise<unknown> | (() => Promise<unknown> | unknown);",
          "declarationLine": 19,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "就绪屏障：Promise 或零参工厂（便于传 `ensureWasm`）。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreReadyBarrier.",
          "advanced": "No additional advanced behavior is declared for IStoreReadyBarrier.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreReadyBarrier."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreReadyBarrier.",
          "examples": [],
          "members": [],
          "aliasOf": "IStoreReadyBarrier",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreReadyBarrier = Promise<unknown> | (() => Promise<unknown> | unknown);"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreReadyBarrier when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreReadyBarrier does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "就绪屏障：Promise 或零参工厂（便于传 `ensureWasm`）。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreReadyBarrier from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreReadyBarrier when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreReadyBarrier does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreReadyBarrier."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreReadyBarrier."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:IStoreReadyBarrier:export type IStoreReadyBarrier = Promise<unknown> | (() => Promise<unknown> | unknown);",
          "guidance": [
            {
              "id": "1-6-就绪屏障-config-ready",
              "heading": "1.6 就绪屏障（config.ready）",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "`StoreProvider` 的 `config.ready` 是一组 `IStoreReadyBarrier`（`Promise<unknown>` 或零参工厂 `() => Promise<unknown> | unknown`），子树渲染前必须全部 settle。内部用 `Promise.all` 归一化成一个 `ITrackedReady`：pending 时渲染 `config.fallback`（默认 `null`），全部成功后渲染 `children`，任意一个 reject 则在 `ReadyBoundary` 里 `throw`（交给上层 Error Boundary）。精确语义见 2.3。"
                }
              ]
            }
          ],
          "usageScore": 4,
          "configuration": [],
          "fragment": "store-react-index--IStoreReadyBarrier"
        },
        {
          "name": "IStoreRegistrationOptions",
          "kind": "type",
          "signature": "export type IStoreRegistrationOptions = { readonly owned?: boolean; };",
          "declarationLine": 19,
          "source": "packages/store-react/dist/provider-registry.d.ts",
          "purpose": "IStoreRegistrationOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreRegistrationOptions.",
          "advanced": "No additional advanced behavior is declared for IStoreRegistrationOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreRegistrationOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreRegistrationOptions.",
          "examples": [],
          "members": [],
          "aliasOf": "IStoreRegistrationOptions",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreRegistrationOptions = { readonly owned?: boolean; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreRegistrationOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreRegistrationOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreRegistrationOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreRegistrationOptions from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreRegistrationOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreRegistrationOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreRegistrationOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreRegistrationOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider-registry.d.ts:IStoreRegistrationOptions:export type IStoreRegistrationOptions = { readonly owned?: boolean; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "store-react-index--IStoreRegistrationOptions"
        },
        {
          "name": "IStoreToken",
          "kind": "type",
          "signature": "export type IStoreToken<T> = Readonly<{ key: symbol; debugName: string; readonly [STORE_TOKEN_VALUE]?: (value: T) => T; }>;",
          "declarationLine": 14,
          "source": "packages/store-react/dist/provider-registry.d.ts",
          "purpose": "IStoreToken is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreToken.",
          "advanced": "No additional advanced behavior is declared for IStoreToken.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IStoreToken."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IStoreToken.",
          "examples": [],
          "members": [],
          "aliasOf": "IStoreToken",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IStoreToken<T> = Readonly<{ key: symbol; debugName: string; readonly [STORE_TOKEN_VALUE]?: (value: T) => T; }>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IStoreToken when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IStoreToken does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IStoreToken is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IStoreToken from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IStoreToken when its type contract and declared inputs match the result you need. Choose a different public symbol when IStoreToken does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IStoreToken."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IStoreToken."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider-registry.d.ts:IStoreToken:export type IStoreToken<T> = Readonly<{ key: symbol; debugName: string; readonly [STORE_TOKEN_VALUE]?: (value: T) => T; }>;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "store-react-index--IStoreToken"
        },
        {
          "name": "normalizeStoreConfig",
          "kind": "function",
          "signature": "export declare function normalizeStoreConfig(config?: IStoreProviderConfig, barrierScope?: object): IStoreConfigValue;",
          "declarationLine": 53,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "normalizeStoreConfig is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for normalizeStoreConfig.",
          "advanced": "No additional advanced behavior is declared for normalizeStoreConfig.",
          "parameters": [
            "config: IStoreProviderConfig (optional)",
            "barrierScope: object (optional)"
          ],
          "parameterDetails": [
            {
              "name": "config",
              "type": "IStoreProviderConfig",
              "optional": true
            },
            {
              "name": "barrierScope",
              "type": "object",
              "optional": true
            }
          ],
          "returns": "IStoreConfigValue",
          "errors": ["No documented errors are declared for normalizeStoreConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for normalizeStoreConfig.",
          "examples": [],
          "members": [],
          "aliasOf": "normalizeStoreConfig",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function normalizeStoreConfig(config?: IStoreProviderConfig, barrierScope?: object): IStoreConfigValue;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "config",
                "type": "IStoreProviderConfig",
                "optional": true
              },
              {
                "name": "barrierScope",
                "type": "object",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IStoreConfigValue"],
          "whenToUse": "Use normalizeStoreConfig when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when normalizeStoreConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "normalizeStoreConfig is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import normalizeStoreConfig from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use normalizeStoreConfig when its function contract and declared inputs match the result you need. Choose a different public symbol when normalizeStoreConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for normalizeStoreConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for normalizeStoreConfig."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "features",
              "type": "IStoreFeatures",
              "optional": true,
              "description": ""
            },
            {
              "name": "ready",
              "type": "readonly IStoreReadyBarrier[]",
              "optional": true,
              "description": "子树渲染前必须 settle 的屏障。 例：`ready: [ensureWasm]`（从 `@migaia/store-wasm` 引入）。"
            },
            {
              "name": "fallback",
              "type": "ReactNode",
              "optional": true,
              "description": "屏障未完成时的占位。"
            },
            {
              "name": "defaults",
              "type": "IStoreProviderDefaults",
              "optional": true,
              "description": "只读业务默认值快照；Provider 不会把这些值自动写入各 Store。"
            }
          ],
          "identity": "packages/store-react/dist/store-config.d.ts:normalizeStoreConfig:export declare function normalizeStoreConfig(config?: IStoreProviderConfig, barrierScope?: object): IStoreConfigValue;",
          "guidance": [
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 26,
          "configuration": [
            {
              "name": "features",
              "type": "IStoreFeatures",
              "optional": true,
              "description": "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
              "descriptionEn": "",
              "descriptionZh": "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。"
            },
            {
              "name": "ready",
              "type": "readonly IStoreReadyBarrier[]",
              "optional": true,
              "description": "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。",
              "descriptionEn": "",
              "descriptionZh": "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
            },
            {
              "name": "fallback",
              "type": "ReactNode",
              "optional": true,
              "description": "屏障未完成时的占位。",
              "descriptionEn": "",
              "descriptionZh": "屏障未完成时的占位。"
            },
            {
              "name": "defaults",
              "type": "IStoreProviderDefaults",
              "optional": true,
              "description": "只读业务默认值快照；Provider 不会把这些值自动写入各 Store。",
              "descriptionEn": "",
              "descriptionZh": "只读业务默认值快照；Provider 不会把这些值自动写入各 Store。"
            }
          ],
          "fragment": "store-react-index--normalizeStoreConfig"
        },
        {
          "name": "readStoreFeature",
          "kind": "function",
          "signature": "export declare function readStoreFeature(config: IStoreConfigValue, path: IStoreFeaturePath): boolean;",
          "declarationLine": 62,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "readStoreFeature is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for readStoreFeature.",
          "advanced": "No additional advanced behavior is declared for readStoreFeature.",
          "parameters": ["config: IStoreConfigValue", "path: IStoreFeaturePath"],
          "parameterDetails": [
            {
              "name": "config",
              "type": "IStoreConfigValue",
              "optional": false
            },
            {
              "name": "path",
              "type": "IStoreFeaturePath",
              "optional": false
            }
          ],
          "returns": "boolean",
          "errors": ["No documented errors are declared for readStoreFeature."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for readStoreFeature.",
          "examples": [],
          "members": [],
          "aliasOf": "readStoreFeature",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function readStoreFeature(config: IStoreConfigValue, path: IStoreFeaturePath): boolean;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "config",
                "type": "IStoreConfigValue",
                "optional": false
              },
              {
                "name": "path",
                "type": "IStoreFeaturePath",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["boolean"],
          "whenToUse": "Use readStoreFeature when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when readStoreFeature does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "readStoreFeature is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import readStoreFeature from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use readStoreFeature when its function contract and declared inputs match the result you need. Choose a different public symbol when readStoreFeature does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for readStoreFeature."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for readStoreFeature."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:readStoreFeature:export declare function readStoreFeature(config: IStoreConfigValue, path: IStoreFeaturePath): boolean;",
          "guidance": [
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 34,
          "configuration": [],
          "fragment": "store-react-index--readStoreFeature"
        },
        {
          "name": "STORE_REACT_SOURCE",
          "kind": "const",
          "signature": "export declare const STORE_REACT_SOURCE = \"@migaia/store-react\";",
          "declarationLine": 4,
          "source": "packages/store-react/dist/errors.d.ts",
          "purpose": "`source` value stamped onto every error this module throws.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for STORE_REACT_SOURCE.",
          "advanced": "No additional advanced behavior is declared for STORE_REACT_SOURCE.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for STORE_REACT_SOURCE."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for STORE_REACT_SOURCE.",
          "examples": [],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const STORE_REACT_SOURCE = \"@migaia/store-react\";"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use STORE_REACT_SOURCE when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when STORE_REACT_SOURCE does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "`source` value stamped onto every error this module throws."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import STORE_REACT_SOURCE from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use STORE_REACT_SOURCE when its const contract and declared inputs match the result you need. Choose a different public symbol when STORE_REACT_SOURCE does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for STORE_REACT_SOURCE."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for STORE_REACT_SOURCE."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/errors.d.ts:STORE_REACT_SOURCE:export declare const STORE_REACT_SOURCE = \"@migaia/store-react\";",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 8,
          "configuration": [],
          "fragment": "store-react-index--STORE_REACT_SOURCE"
        },
        {
          "name": "StoreProvider",
          "kind": "function",
          "signature": "export declare function StoreProvider({ children, registry, runtime, disposeOnUnmount, config }: IStoreProviderProps): import(\"react\").JSX.Element;",
          "declarationLine": 19,
          "source": "packages/store-react/dist/StoreProvider.d.ts",
          "purpose": "StoreProvider is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for StoreProvider.",
          "advanced": "No additional advanced behavior is declared for StoreProvider.",
          "parameters": [
            "{ children, registry, runtime, disposeOnUnmount, config }: IStoreProviderProps: unknown"
          ],
          "parameterDetails": [
            {
              "name": "{ children, registry, runtime, disposeOnUnmount, config }: IStoreProviderProps",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "import(\"react\").JSX.Element",
          "errors": ["No documented errors are declared for StoreProvider."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for StoreProvider.",
          "examples": [],
          "members": [],
          "aliasOf": "StoreProvider",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function StoreProvider({ children, registry, runtime, disposeOnUnmount, config }: IStoreProviderProps): import(\"react\").JSX.Element;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "{ children, registry, runtime, disposeOnUnmount, config }: IStoreProviderProps",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["import(\"react\").JSX.Element"],
          "whenToUse": "Use StoreProvider when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when StoreProvider does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "StoreProvider is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import StoreProvider from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use StoreProvider when its function contract and declared inputs match the result you need. Choose a different public symbol when StoreProvider does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for StoreProvider."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for StoreProvider."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/StoreProvider.d.ts:StoreProvider:export declare function StoreProvider({ children, registry, runtime, disposeOnUnmount, config }: IStoreProviderProps): import(\"react\").JSX.Element;",
          "guidance": [
            {
              "id": "1-6-就绪屏障-config-ready",
              "heading": "1.6 就绪屏障（config.ready）",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "`StoreProvider` 的 `config.ready` 是一组 `IStoreReadyBarrier`（`Promise<unknown>` 或零参工厂 `() => Promise<unknown> | unknown`），子树渲染前必须全部 settle。内部用 `Promise.all` 归一化成一个 `ITrackedReady`：pending 时渲染 `config.fallback`（默认 `null`），全部成功后渲染 `children`，任意一个 reject 则在 `ReadyBoundary` 里 `throw`（交给上层 Error Boundary）。精确语义见 2.3。"
                }
              ]
            },
            {
              "id": "2-2-config-的四个字段",
              "heading": "2.2 config 的四个字段",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IStoreProviderConfig = {\n  readonly features?: {\n    readonly wasm?: boolean;\n    readonly experimental?: Readonly<Record<string, boolean>>;\n  };\n  readonly ready?: readonly (Promise<unknown> | (() => Promise<unknown> | unknown))[];\n  readonly fallback?: ReactNode;\n  readonly defaults?: { readonly warnAsyncActions?: boolean };\n};"
                },
                {
                  "type": "list",
                  "items": [
                    "`features.wasm`：声明本树会用到 wasm 字段。为 `true` 且 `ready` 为空数组时，会自动追加一个立刻 reject 的屏障（见 5），避免\"开了开关却没人 init\"。",
                    "`features.experimental`：白名单对象，只有值严格为 `true` 的 key 才算开启；未列出或非 `true` 一律视为关闭。",
                    "`ready`：见 2.3。",
                    "`fallback`：`ready` 屏障 pending 期间渲染的占位内容，默认 `null`（不是 `undefined`——不传 `fallback` 且有 `ready` 时，pending 阶段渲染的是\"什么都不渲染\"而不是上一次的 children）。",
                    "`defaults.warnAsyncActions`：只是配置快照的一部分，通过 `useStoreConfig()` 暴露给业务代码读取；`StoreProvider` 不会把它自动传给 `createStore`——各个 Store 仍要自己在 `createStore(shape, { warnAsyncActions: true })` 里显式声明。这个字段更适合用作\"业务代码统一读一个地方决定要不要传\"的约定，而不是自动生效的开关。"
                  ]
                }
              ]
            },
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            },
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            },
            {
              "id": "storeprovider",
              "heading": "StoreProvider",
              "blocks": [
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "import { StoreProvider, useStore } from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';"
                },
                {
                  "type": "paragraph",
                  "text": "`StoreProvider`｜5 秒上手 —— 给子树建立 Registry/Runtime 作用域；只用 `useStore`/`useSignal` 读取模块级单例时理论上可以不套它，但项目里建议根部始终套一层："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "const counterStore = createStore({\n  count: 0,\n  increment() {\n    this.count += 1;\n  }\n});\n\nfunction Counter() {\n  const count = useStore(counterStore, (state) => state.count);\n  return <button onClick={() => counterStore.increment()}>count: {count}</button>;\n}\n\nfunction App() {\n  return (\n    <StoreProvider>\n      <Counter />\n    </StoreProvider>\n  );\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部 props（`IStoreProviderProps`）："
                },
                {
                  "type": "list",
                  "items": [
                    "`children: ReactNode`（必填）",
                    "`registry?: StoreRegistry` —— 不传则内部自建；传了默认不由 Provider 释放（见下）",
                    "`runtime?: IRuntime` —— 不传则内部 `createRuntime()`；无论谁创建 Registry，`runtime` 本身的生命周期永远由调用方负责，Provider 从不释放它",
                    "`disposeOnUnmount?: boolean` —— 都不传 `registry`/`runtime` 时默认 `true`（自建自释放）；只传 `registry` 时默认 `false`（外部对象默认外部拥有）",
                    "`config?: IStoreProviderConfig` —— `{ features？, ready？, fallback？, defaults？ }`，见下方 Config / Feature flag"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "同时传 `registry` 与 `runtime` 且 `registry.runtime !== runtime` 会立刻抛错；完整所有权规则、`config.ready` 的身份锁定语义、StrictMode 下的候选 Registry 回收机制，见 USEGUIDE §2、§6。"
                }
              ]
            },
            {
              "id": "config-feature-flag",
              "heading": "Config / Feature flag",
              "blocks": [
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreConfig()`｜3 秒上手 —— 取归一化后的完整配置快照："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "const config = useStoreConfig(); // 无 Provider 时返回 null"
                },
                {
                  "type": "paragraph",
                  "text": "无参数。"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreFeature(path)` / `useAssertStoreFeature(path, apiName?)`｜5 秒上手 —— 查询/断言某个 feature 是否开启，用于给实验性 API 设置入口守卫："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
                },
                {
                  "type": "paragraph",
                  "text": "`path: 'wasm' | \\`experimental.${string}\\`` 是两者共同的必填参数；`useAssertStoreFeature`额外的`apiName?: string`（默认 `'this API'`）出现在错误信息里。`useStoreFeature`无 Provider 时返回`false`，不抛；`useAssertStoreFeature`未开启（含无 Provider）一律抛`Error`。这两个都要通过 `StoreProvider`的`config.features.wasm`/`config.features.experimental`显式开启才算生效（只认字面`true`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`StoreProviderState`｜3 秒上手 —— 就绪屏障三态的稳定取值集合，常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
                },
                {
                  "type": "paragraph",
                  "text": "业务代码一般不需要直接用它，除非要复刻类似的三态就绪追踪逻辑。"
                }
              ]
            }
          ],
          "usageScore": 342,
          "configuration": [],
          "fragment": "store-react-index--StoreProvider"
        },
        {
          "name": "StoreProviderState",
          "kind": "const",
          "signature": "export declare const StoreProviderState: { readonly pending: \"pending\"; readonly ready: \"ready\"; readonly error: \"error\"; };",
          "declarationLine": 2,
          "source": "packages/store-react/dist/provider-state-constants.d.ts",
          "purpose": "Store provider readiness states used during hydration and startup.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for StoreProviderState.",
          "advanced": "No additional advanced behavior is declared for StoreProviderState.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for StoreProviderState."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for StoreProviderState.",
          "examples": [
            "const StoreProviderState = { pending: 'pending', ready: 'ready', error: 'error' } as const;\ntype IStoreProviderState = 'pending' | 'ready' | 'error';",
            "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';",
            "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
          ],
          "members": [],
          "aliasOf": "StoreProviderState",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const StoreProviderState: { readonly pending: \"pending\"; readonly ready: \"ready\"; readonly error: \"error\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use StoreProviderState when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when StoreProviderState does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Store provider readiness states used during hydration and startup."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import StoreProviderState from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use StoreProviderState when its const contract and declared inputs match the result you need. Choose a different public symbol when StoreProviderState does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for StoreProviderState."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for StoreProviderState."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider-state-constants.d.ts:StoreProviderState:export declare const StoreProviderState: { readonly pending: \"pending\"; readonly ready: \"ready\"; readonly error: \"error\"; };",
          "guidance": [
            {
              "id": "config-feature-flag",
              "heading": "Config / Feature flag",
              "blocks": [
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreConfig()`｜3 秒上手 —— 取归一化后的完整配置快照："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "const config = useStoreConfig(); // 无 Provider 时返回 null"
                },
                {
                  "type": "paragraph",
                  "text": "无参数。"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreFeature(path)` / `useAssertStoreFeature(path, apiName?)`｜5 秒上手 —— 查询/断言某个 feature 是否开启，用于给实验性 API 设置入口守卫："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
                },
                {
                  "type": "paragraph",
                  "text": "`path: 'wasm' | \\`experimental.${string}\\`` 是两者共同的必填参数；`useAssertStoreFeature`额外的`apiName?: string`（默认 `'this API'`）出现在错误信息里。`useStoreFeature`无 Provider 时返回`false`，不抛；`useAssertStoreFeature`未开启（含无 Provider）一律抛`Error`。这两个都要通过 `StoreProvider`的`config.features.wasm`/`config.features.experimental`显式开启才算生效（只认字面`true`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`StoreProviderState`｜3 秒上手 —— 就绪屏障三态的稳定取值集合，常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
                },
                {
                  "type": "paragraph",
                  "text": "业务代码一般不需要直接用它，除非要复刻类似的三态就绪追踪逻辑。"
                }
              ]
            }
          ],
          "usageScore": 34,
          "configuration": [],
          "fragment": "store-react-index--StoreProviderState"
        },
        {
          "name": "StoreRegistry",
          "kind": "class",
          "signature": "export declare class StoreRegistry implements IDisposable { #private; readonly runtime: IRuntime; /** Provider-local atom state/override boundary. Same Runtime may host many registries. */ readonly atomStore: IAtomStore; constructor(runtime?: IRuntime); /** Part of the project's unified lifecycle shape (see `@migaia/lifecycle`'s `ILifecycleState`). */ get lifecycle(): ILifecycleState; /** * Resolves once this registry (and any thenable disposer results) has actually finished tearing * down. */ whenTerminal(): Promise<void>; get disposed(): boolean; register<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): IDisposer; replace<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): void; get<T>(token: IStoreToken<T>): T | undefined; require<T>(token: IStoreToken<T>): T; has<T>(token: IStoreToken<T>): boolean; remove<T>(token: IStoreToken<T>, disposeOwned?: boolean): boolean; /** * React StrictMode probes effect cleanup/setup. Delayed release avoids disposing an * internally-owned registry between those two phases. */ retain(disposeOnRelease: boolean, _deferTask?: boolean): IDisposer; /** * Retained for source compatibility, but deliberately does not reclaim a render candidate. React * exposes no reliable abandoned-render signal; any timer, including one armed after a readiness * promise settles, can dispose a valid render before its effect commits. Candidate reclamation * belongs to an observable owner or a future GC-backed mechanism. */ prepareForRender(after?: Promise<void>): void; /** * Idempotent, immediately enters `closing`. If an owned value's disposer returns a thenable, this * cannot block on it (it's sync) — the thenable is tracked and observed (never left as an * unhandled rejection) so a later `disposeAsync()` call still waits for it instead of resolving * early just because `dispose()` already ran (see `disposeAsync()`). `lifecycle` only reaches * `terminal` once every tracked thenable has actually settled. */ dispose(): void; /** * Awaitable counterpart. Single-flight: concurrent calls share one completion instead of each * racing their own pass over `#entries`. Calling this after a prior synchronous `dispose()` does * not resolve early — it waits for whatever thenable disposer results that `dispose()` started * but could not block on (tracked by the lifecycle disposal ledger). */ disposeAsync(): Promise<void>; }",
          "declarationLine": 24,
          "source": "packages/store-react/dist/provider-registry.d.ts",
          "purpose": "Runtime-bound registry shared by React Provider and SSR request scopes.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for StoreRegistry.",
          "advanced": "No additional advanced behavior is declared for StoreRegistry.",
          "parameters": ["runtime: IRuntime (optional)"],
          "parameterDetails": [
            {
              "name": "runtime",
              "type": "IRuntime",
              "optional": true
            }
          ],
          "returns": "No explicit return annotation is declared for this class.",
          "errors": ["No documented errors are declared for StoreRegistry."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for StoreRegistry.",
          "examples": [],
          "members": [
            {
              "name": "runtime",
              "kind": "property",
              "signature": "readonly runtime: IRuntime;",
              "description": null,
              "parameterDetails": [],
              "returns": "IRuntime"
            },
            {
              "name": "atomStore",
              "kind": "property",
              "signature": "readonly atomStore: IAtomStore;",
              "description": "Provider-local atom state/override boundary. Same Runtime may host many registries.",
              "parameterDetails": [],
              "returns": "IAtomStore"
            },
            {
              "name": "lifecycle",
              "kind": "get",
              "signature": "get lifecycle(): ILifecycleState;",
              "description": "Part of the project's unified lifecycle shape (see `@migaia/lifecycle`'s `ILifecycleState`).",
              "parameterDetails": [],
              "returns": "ILifecycleState"
            },
            {
              "name": "whenTerminal",
              "kind": "method",
              "signature": "whenTerminal(): Promise<void>;",
              "description": "Resolves once this registry (and any thenable disposer results) has actually finished tearing down.",
              "parameterDetails": [],
              "returns": "Promise<void>"
            },
            {
              "name": "disposed",
              "kind": "get",
              "signature": "get disposed(): boolean;",
              "description": null,
              "parameterDetails": [],
              "returns": "boolean"
            },
            {
              "name": "register",
              "kind": "method",
              "signature": "register<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): IDisposer;",
              "description": null,
              "parameterDetails": [
                {
                  "name": "token",
                  "type": "IStoreToken<T>",
                  "optional": false
                },
                {
                  "name": "value",
                  "type": "T",
                  "optional": false
                },
                {
                  "name": "options",
                  "type": "IStoreRegistrationOptions",
                  "optional": true
                }
              ],
              "returns": "IDisposer"
            },
            {
              "name": "replace",
              "kind": "method",
              "signature": "replace<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): void;",
              "description": null,
              "parameterDetails": [
                {
                  "name": "token",
                  "type": "IStoreToken<T>",
                  "optional": false
                },
                {
                  "name": "value",
                  "type": "T",
                  "optional": false
                },
                {
                  "name": "options",
                  "type": "IStoreRegistrationOptions",
                  "optional": true
                }
              ],
              "returns": "void"
            },
            {
              "name": "get",
              "kind": "method",
              "signature": "get<T>(token: IStoreToken<T>): T | undefined;",
              "description": null,
              "parameterDetails": [
                {
                  "name": "token",
                  "type": "IStoreToken<T>",
                  "optional": false
                }
              ],
              "returns": "T | undefined"
            },
            {
              "name": "require",
              "kind": "method",
              "signature": "require<T>(token: IStoreToken<T>): T;",
              "description": null,
              "parameterDetails": [
                {
                  "name": "token",
                  "type": "IStoreToken<T>",
                  "optional": false
                }
              ],
              "returns": "T"
            },
            {
              "name": "has",
              "kind": "method",
              "signature": "has<T>(token: IStoreToken<T>): boolean;",
              "description": null,
              "parameterDetails": [
                {
                  "name": "token",
                  "type": "IStoreToken<T>",
                  "optional": false
                }
              ],
              "returns": "boolean"
            },
            {
              "name": "remove",
              "kind": "method",
              "signature": "remove<T>(token: IStoreToken<T>, disposeOwned?: boolean): boolean;",
              "description": null,
              "parameterDetails": [
                {
                  "name": "token",
                  "type": "IStoreToken<T>",
                  "optional": false
                },
                {
                  "name": "disposeOwned",
                  "type": "boolean",
                  "optional": true
                }
              ],
              "returns": "boolean"
            },
            {
              "name": "retain",
              "kind": "method",
              "signature": "retain(disposeOnRelease: boolean, _deferTask?: boolean): IDisposer;",
              "description": "React StrictMode probes effect cleanup/setup. Delayed release avoids disposing an internally-owned registry between those two phases.",
              "parameterDetails": [
                {
                  "name": "disposeOnRelease",
                  "type": "boolean",
                  "optional": false
                },
                {
                  "name": "_deferTask",
                  "type": "boolean",
                  "optional": true
                }
              ],
              "returns": "IDisposer"
            },
            {
              "name": "prepareForRender",
              "kind": "method",
              "signature": "prepareForRender(after?: Promise<void>): void;",
              "description": "Retained for source compatibility, but deliberately does not reclaim a render candidate. React exposes no reliable abandoned-render signal; any timer, including one armed after a readiness promise settles, can dispose a valid render before its effect commits. Candidate reclamation belongs to an observable owner or a future GC-backed mechanism.",
              "parameterDetails": [
                {
                  "name": "after",
                  "type": "Promise<void>",
                  "optional": true
                }
              ],
              "returns": "void"
            },
            {
              "name": "dispose",
              "kind": "method",
              "signature": "dispose(): void;",
              "description": "Idempotent, immediately enters `closing`. If an owned value's disposer returns a thenable, this cannot block on it (it's sync) — the thenable is tracked and observed (never left as an unhandled rejection) so a later `disposeAsync()` call still waits for it instead of resolving early just because `dispose()` already ran (see `disposeAsync()`). `lifecycle` only reaches `terminal` once every tracked thenable has actually settled.",
              "parameterDetails": [],
              "returns": "void"
            },
            {
              "name": "disposeAsync",
              "kind": "method",
              "signature": "disposeAsync(): Promise<void>;",
              "description": "Awaitable counterpart. Single-flight: concurrent calls share one completion instead of each racing their own pass over `#entries`. Calling this after a prior synchronous `dispose()` does not resolve early — it waits for whatever thenable disposer results that `dispose()` started but could not block on (tracked by the lifecycle disposal ledger).",
              "parameterDetails": [],
              "returns": "Promise<void>"
            }
          ],
          "aliasOf": "StoreRegistry",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare class StoreRegistry implements IDisposable { #private; readonly runtime: IRuntime; /** Provider-local atom state/override boundary. Same Runtime may host many registries. */ readonly atomStore: IAtomStore; constructor(runtime?: IRuntime); /** Part of the project's unified lifecycle shape (see `@migaia/lifecycle`'s `ILifecycleState`). */ get lifecycle(): ILifecycleState; /** * Resolves once this registry (and any thenable disposer results) has actually finished tearing * down. */ whenTerminal(): Promise<void>; get disposed(): boolean; register<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): IDisposer; replace<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): void; get<T>(token: IStoreToken<T>): T | undefined; require<T>(token: IStoreToken<T>): T; has<T>(token: IStoreToken<T>): boolean; remove<T>(token: IStoreToken<T>, disposeOwned?: boolean): boolean; /** * React StrictMode probes effect cleanup/setup. Delayed release avoids disposing an * internally-owned registry between those two phases. */ retain(disposeOnRelease: boolean, _deferTask?: boolean): IDisposer; /** * Retained for source compatibility, but deliberately does not reclaim a render candidate. React * exposes no reliable abandoned-render signal; any timer, including one armed after a readiness * promise settles, can dispose a valid render before its effect commits. Candidate reclamation * belongs to an observable owner or a future GC-backed mechanism. */ prepareForRender(after?: Promise<void>): void; /** * Idempotent, immediately enters `closing`. If an owned value's disposer returns a thenable, this * cannot block on it (it's sync) — the thenable is tracked and observed (never left as an * unhandled rejection) so a later `disposeAsync()` call still waits for it instead of resolving * early just because `dispose()` already ran (see `disposeAsync()`). `lifecycle` only reaches * `terminal` once every tracked thenable has actually settled. */ dispose(): void; /** * Awaitable counterpart. Single-flight: concurrent calls share one completion instead of each * racing their own pass over `#entries`. Calling this after a prior synchronous `dispose()` does * not resolve early — it waits for whatever thenable disposer results that `dispose()` started * but could not block on (tracked by the lifecycle disposal ledger). */ disposeAsync(): Promise<void>; }"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "runtime",
                "type": "IRuntime",
                "optional": true
              }
            ]
          ],
          "overloadReturns": [],
          "whenToUse": "Use StoreRegistry when its class contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when StoreRegistry does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Runtime-bound registry shared by React Provider and SSR request scopes."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import StoreRegistry from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use StoreRegistry when its class contract and declared inputs match the result you need. Choose a different public symbol when StoreRegistry does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained class example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for StoreRegistry."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for StoreRegistry."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider-registry.d.ts:StoreRegistry:export declare class StoreRegistry implements IDisposable { #private; readonly runtime: IRuntime; /** Provider-local atom state/override boundary. Same Runtime may host many registries. */ readonly atomStore: IAtomStore; constructor(runtime?: IRuntime); /** Part of the project's unified lifecycle shape (see `@migaia/lifecycle`'s `ILifecycleState`). */ get lifecycle(): ILifecycleState; /** * Resolves once this registry (and any thenable disposer results) has actually finished tearing * down. */ whenTerminal(): Promise<void>; get disposed(): boolean; register<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): IDisposer; replace<T>(token: IStoreToken<T>, value: T, options?: IStoreRegistrationOptions): void; get<T>(token: IStoreToken<T>): T | undefined; require<T>(token: IStoreToken<T>): T; has<T>(token: IStoreToken<T>): boolean; remove<T>(token: IStoreToken<T>, disposeOwned?: boolean): boolean; /** * React StrictMode probes effect cleanup/setup. Delayed release avoids disposing an * internally-owned registry between those two phases. */ retain(disposeOnRelease: boolean, _deferTask?: boolean): IDisposer; /** * Retained for source compatibility, but deliberately does not reclaim a render candidate. React * exposes no reliable abandoned-render signal; any timer, including one armed after a readiness * promise settles, can dispose a valid render before its effect commits. Candidate reclamation * belongs to an observable owner or a future GC-backed mechanism. */ prepareForRender(after?: Promise<void>): void; /** * Idempotent, immediately enters `closing`. If an owned value's disposer returns a thenable, this * cannot block on it (it's sync) — the thenable is tracked and observed (never left as an * unhandled rejection) so a later `disposeAsync()` call still waits for it instead of resolving * early just because `dispose()` already ran (see `disposeAsync()`). `lifecycle` only reaches * `terminal` once every tracked thenable has actually settled. */ dispose(): void; /** * Awaitable counterpart. Single-flight: concurrent calls share one completion instead of each * racing their own pass over `#entries`. Calling this after a prior synchronous `dispose()` does * not resolve early — it waits for whatever thenable disposer results that `dispose()` started * but could not block on (tracked by the lifecycle disposal ledger). */ disposeAsync(): Promise<void>; }",
          "guidance": [
            {
              "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
              "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
                },
                {
                  "type": "list",
                  "items": [
                    "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                    "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                    "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                  ]
                }
              ]
            },
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 112,
          "configuration": [],
          "fragment": "store-react-index--StoreRegistry"
        },
        {
          "name": "useAssertStoreFeature",
          "kind": "function",
          "signature": "export declare function useAssertStoreFeature(path: IStoreFeaturePath, apiName?: string): void;",
          "declarationLine": 69,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "实验 / 增强 API 入口处调用：未开启则抛明确错误。 无 Provider 也抛（实验 API 不允许「默默在树外半开」）。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAssertStoreFeature.",
          "advanced": "No additional advanced behavior is declared for useAssertStoreFeature.",
          "parameters": ["path: IStoreFeaturePath", "apiName: string (optional)"],
          "parameterDetails": [
            {
              "name": "path",
              "type": "IStoreFeaturePath",
              "optional": false
            },
            {
              "name": "apiName",
              "type": "string",
              "optional": true
            }
          ],
          "returns": "void",
          "errors": ["No documented errors are declared for useAssertStoreFeature."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useAssertStoreFeature.",
          "examples": [
            "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
          ],
          "members": [],
          "aliasOf": "useAssertStoreFeature",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useAssertStoreFeature(path: IStoreFeaturePath, apiName?: string): void;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "path",
                "type": "IStoreFeaturePath",
                "optional": false
              },
              {
                "name": "apiName",
                "type": "string",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["void"],
          "whenToUse": "Use useAssertStoreFeature when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useAssertStoreFeature does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "实验 / 增强 API 入口处调用：未开启则抛明确错误。 无 Provider 也抛（实验 API 不允许「默默在树外半开」）。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useAssertStoreFeature from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useAssertStoreFeature when its function contract and declared inputs match the result you need. Choose a different public symbol when useAssertStoreFeature does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAssertStoreFeature."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useAssertStoreFeature."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:useAssertStoreFeature:export declare function useAssertStoreFeature(path: IStoreFeaturePath, apiName?: string): void;",
          "guidance": [
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            },
            {
              "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
              "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
                },
                {
                  "type": "list",
                  "items": [
                    "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                    "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                    "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                  ]
                }
              ]
            },
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            },
            {
              "id": "config-feature-flag",
              "heading": "Config / Feature flag",
              "blocks": [
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreConfig()`｜3 秒上手 —— 取归一化后的完整配置快照："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "const config = useStoreConfig(); // 无 Provider 时返回 null"
                },
                {
                  "type": "paragraph",
                  "text": "无参数。"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreFeature(path)` / `useAssertStoreFeature(path, apiName?)`｜5 秒上手 —— 查询/断言某个 feature 是否开启，用于给实验性 API 设置入口守卫："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
                },
                {
                  "type": "paragraph",
                  "text": "`path: 'wasm' | \\`experimental.${string}\\`` 是两者共同的必填参数；`useAssertStoreFeature`额外的`apiName?: string`（默认 `'this API'`）出现在错误信息里。`useStoreFeature`无 Provider 时返回`false`，不抛；`useAssertStoreFeature`未开启（含无 Provider）一律抛`Error`。这两个都要通过 `StoreProvider`的`config.features.wasm`/`config.features.experimental`显式开启才算生效（只认字面`true`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`StoreProviderState`｜3 秒上手 —— 就绪屏障三态的稳定取值集合，常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
                },
                {
                  "type": "paragraph",
                  "text": "业务代码一般不需要直接用它，除非要复刻类似的三态就绪追踪逻辑。"
                }
              ]
            }
          ],
          "usageScore": 66,
          "configuration": [],
          "fragment": "store-react-index--useAssertStoreFeature"
        },
        {
          "name": "useAsyncAtomValue",
          "kind": "function",
          "signature": "export declare function useAsyncAtomValue<T>(atom: IAsyncAtom<T>): T;",
          "declarationLine": 16,
          "source": "packages/store-react/dist/useAtom.d.ts",
          "purpose": "useAsyncAtomValue is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAsyncAtomValue.",
          "advanced": "No additional advanced behavior is declared for useAsyncAtomValue.",
          "parameters": ["atom: IAsyncAtom<T>"],
          "parameterDetails": [
            {
              "name": "atom",
              "type": "IAsyncAtom<T>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useAsyncAtomValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useAsyncAtomValue.",
          "examples": [
            "const value = useAsyncAtomValue(asyncAtom); // 内部即 useResourceValue(atom.resource)"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useAsyncAtomValue<T>(atom: IAsyncAtom<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "atom",
                "type": "IAsyncAtom<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useAsyncAtomValue when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useAsyncAtomValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useAsyncAtomValue is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useAsyncAtomValue from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useAsyncAtomValue when its function contract and declared inputs match the result you need. Choose a different public symbol when useAsyncAtomValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAsyncAtomValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useAsyncAtomValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useAtom.d.ts:useAsyncAtomValue:export declare function useAsyncAtomValue<T>(atom: IAsyncAtom<T>): T;",
          "guidance": [],
          "usageScore": 40,
          "configuration": [],
          "fragment": "store-react-index--useAsyncAtomValue"
        },
        {
          "name": "useAtom",
          "kind": "function",
          "signature": "export declare function useAtom<T, Args extends readonly unknown[], Result>(atom: IWritableAtom<T, Args, Result>): readonly [T, (...args: Args) => Result];",
          "declarationLine": 10,
          "source": "packages/store-react/dist/useAtom.d.ts",
          "purpose": "useAtom is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAtom.",
          "advanced": "No additional advanced behavior is declared for useAtom.",
          "parameters": ["atom: IWritableAtom<T, Args, Result>"],
          "parameterDetails": [
            {
              "name": "atom",
              "type": "IWritableAtom<T, Args, Result>",
              "optional": false
            }
          ],
          "returns": "readonly [T, (...args: Args) => Result]",
          "errors": ["No documented errors are declared for useAtom."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useAtom.",
          "examples": [
            "const value = useAtomValue(myAtom);\nconst write = useSetAtom(myWritableAtom);\nconst [value2, write2] = useAtom(myWritableAtom);"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useAtom<T, Args extends readonly unknown[], Result>(atom: IWritableAtom<T, Args, Result>): readonly [T, (...args: Args) => Result];"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "atom",
                "type": "IWritableAtom<T, Args, Result>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["readonly [T, (...args: Args) => Result]"],
          "whenToUse": "Use useAtom when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useAtom does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useAtom is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useAtom from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useAtom when its function contract and declared inputs match the result you need. Choose a different public symbol when useAtom does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAtom."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useAtom."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useAtom.d.ts:useAtom:export declare function useAtom<T, Args extends readonly unknown[], Result>(atom: IWritableAtom<T, Args, Result>): readonly [T, (...args: Args) => Result];",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 50,
          "configuration": [],
          "fragment": "store-react-index--useAtom"
        },
        {
          "name": "useAtomDefinition",
          "kind": "function",
          "signature": "export declare function useAtomDefinition<T>(definition: IAtomDefinition<T>): T;",
          "declarationLine": 2,
          "source": "packages/store-react/dist/useAtomDefinition.d.ts",
          "purpose": "useAtomDefinition is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAtomDefinition.",
          "advanced": "No additional advanced behavior is declared for useAtomDefinition.",
          "parameters": ["definition: IAtomDefinition<T>"],
          "parameterDetails": [
            {
              "name": "definition",
              "type": "IAtomDefinition<T>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useAtomDefinition."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useAtomDefinition.",
          "examples": [
            "const value = useAtomDefinition(countDef);\nconst increment = useSetAtomDefinition(incrementDef);",
            "import { StoreProvider, useAtomDefinition, useSetAtomDefinition } from '@migaia/store-react';\nimport { atomDef, writableDef } from '@migaia/store-keyed/atom/definition';\n\nconst countDef = atomDef(0);\nconst incrementDef = writableDef(\n  (get) => get(countDef),\n  (get, set) => set(countDef, get(countDef) + 1)\n);\n\nfunction Counter() {\n  const count = useAtomDefinition(countDef);\n  const increment = useSetAtomDefinition(incrementDef);\n  return <button onClick={() => increment()}>count: {count}</button>;\n}\n\nfunction App() {\n  return (\n    <StoreProvider>\n      <Counter />\n    </StoreProvider>\n  );\n}"
          ],
          "members": [],
          "aliasOf": "useAtomDefinition",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useAtomDefinition<T>(definition: IAtomDefinition<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "definition",
                "type": "IAtomDefinition<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useAtomDefinition when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useAtomDefinition does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useAtomDefinition is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useAtomDefinition from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useAtomDefinition when its function contract and declared inputs match the result you need. Choose a different public symbol when useAtomDefinition does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAtomDefinition."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useAtomDefinition."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useAtomDefinition.d.ts:useAtomDefinition:export declare function useAtomDefinition<T>(definition: IAtomDefinition<T>): T;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 72,
          "configuration": [],
          "fragment": "store-react-index--useAtomDefinition"
        },
        {
          "name": "useAtomValue",
          "kind": "function",
          "signature": "export declare function useAtomValue<T>(atom: IReadableAtom<T>): T;",
          "declarationLine": 7,
          "source": "packages/store-react/dist/useAtom.d.ts",
          "purpose": "Subscribe to any synchronous atom with Runtime-safe dependency tracking.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAtomValue.",
          "advanced": "No additional advanced behavior is declared for useAtomValue.",
          "parameters": ["atom: IReadableAtom<T>"],
          "parameterDetails": [
            {
              "name": "atom",
              "type": "IReadableAtom<T>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useAtomValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useAtomValue.",
          "examples": [
            "const value = useAtomValue(myAtom);\nconst write = useSetAtom(myWritableAtom);\nconst [value2, write2] = useAtom(myWritableAtom);"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useAtomValue<T>(atom: IReadableAtom<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "atom",
                "type": "IReadableAtom<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useAtomValue when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useAtomValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Subscribe to any synchronous atom with Runtime-safe dependency tracking."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useAtomValue from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useAtomValue when its function contract and declared inputs match the result you need. Choose a different public symbol when useAtomValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useAtomValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useAtomValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useAtom.d.ts:useAtomValue:export declare function useAtomValue<T>(atom: IReadableAtom<T>): T;",
          "guidance": [],
          "usageScore": 66,
          "configuration": [],
          "fragment": "store-react-index--useAtomValue"
        },
        {
          "name": "useNodeValue",
          "kind": "function",
          "signature": "export declare function useNodeValue<T>(node: IStableNode<T>, runtime: IRuntime, enabled?: boolean): T;",
          "declarationLine": 21,
          "source": "packages/store-react/dist/useNode.d.ts",
          "purpose": "useNodeValue is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useNodeValue.",
          "advanced": "No additional advanced behavior is declared for useNodeValue.",
          "parameters": [
            "node: IStableNode<T>",
            "runtime: IRuntime",
            "enabled: boolean (optional)"
          ],
          "parameterDetails": [
            {
              "name": "node",
              "type": "IStableNode<T>",
              "optional": false
            },
            {
              "name": "runtime",
              "type": "IRuntime",
              "optional": false
            },
            {
              "name": "enabled",
              "type": "boolean",
              "optional": true
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useNodeValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useNodeValue.",
          "examples": ["const value = useNodeValue(mySignal, runtime);"],
          "members": [],
          "aliasOf": "useNodeValue",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useNodeValue<T>(node: IStableNode<T>, runtime: IRuntime, enabled?: boolean): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "node",
                "type": "IStableNode<T>",
                "optional": false
              },
              {
                "name": "runtime",
                "type": "IRuntime",
                "optional": false
              },
              {
                "name": "enabled",
                "type": "boolean",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useNodeValue when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useNodeValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useNodeValue is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useNodeValue from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useNodeValue when its function contract and declared inputs match the result you need. Choose a different public symbol when useNodeValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useNodeValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useNodeValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useNode.d.ts:useNodeValue:export declare function useNodeValue<T>(node: IStableNode<T>, runtime: IRuntime, enabled?: boolean): T;",
          "guidance": [],
          "usageScore": 48,
          "configuration": [],
          "fragment": "store-react-index--useNodeValue"
        },
        {
          "name": "useProvidedStore",
          "kind": "function",
          "signature": "export declare function useProvidedStore<S extends Record<string, unknown>, Result>(token: IStoreToken<IReactiveStore<S>>, selector: (state: IStoreShape<S>) => Result, isEqual?: (left: Result, right: Result) => boolean): Result;",
          "declarationLine": 13,
          "source": "packages/store-react/dist/provider.d.ts",
          "purpose": "useProvidedStore is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useProvidedStore.",
          "advanced": "No additional advanced behavior is declared for useProvidedStore.",
          "parameters": [
            "token: IStoreToken<IReactiveStore<S>>",
            "selector: (state: IStoreShape<S>) => Result",
            "isEqual: (left: Result, right: Result) => boolean (optional)"
          ],
          "parameterDetails": [
            {
              "name": "token",
              "type": "IStoreToken<IReactiveStore<S>>",
              "optional": false
            },
            {
              "name": "selector",
              "type": "(state: IStoreShape<S>) => Result",
              "optional": false
            },
            {
              "name": "isEqual",
              "type": "(left: Result, right: Result) => boolean",
              "optional": true
            }
          ],
          "returns": "Result",
          "errors": ["No documented errors are declared for useProvidedStore."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useProvidedStore.",
          "examples": [
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport type { IReactiveStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\nimport type { IRuntime } from '@migaia/reactive';\n\ntype ISession = { userId: string; setUserId(id: string): void };\nconst sessionToken = createStoreToken<IReactiveStore<ISession>>('session');\n\nfunction restoreSession(): Promise<void> {\n  // 例如从 localStorage/远端恢复登录态；调用方负责把它 memo 住。\n  return Promise.resolve();\n}\n\nfunction App() {\n  // ready 数组的引用必须稳定，否则每次渲染都会命中 §2.3 的身份检查警告。\n  const ready = useMemo(() => [restoreSession], []);\n  const sessionStore = useMemo(\n    () =>\n      createStore<ISession>({\n        userId: '',\n        setUserId(id) {\n          this.userId = id;\n        }\n      }),\n    []\n  );\n\n  return (\n    <StoreProvider config={{ ready, fallback: <FullPageSpinner /> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<FullPageSpinner />}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\n// 用 register 而非全局单例：不同的 Provider 子树可以注入不同的 session 实例（比如测试）。\nfunction SessionInjector({\n  store,\n  children\n}: {\n  store: IReactiveStore<ISession>;\n  children: React.ReactNode;\n}) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return <>{children}</>;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const experimentalCharts = useStoreFeature('experimental.charts');\n  return (\n    <div>\n      <p>当前用户：{userId || '未登录'}</p>\n      {experimentalCharts ? <ChartsPanel userId={userId} /> : null}\n    </div>\n  );\n}\n\nconst profileResources = new Map<string, Resource<{ name: string }>>();\nfunction profileResource(userId: string, runtime: IRuntime): Resource<{ name: string }> {\n  let resource = profileResources.get(userId);\n  if (!resource) {\n    resource = new Resource(\n      async ({ signal }) => {\n        const res = await fetch(`/api/profile/${userId}`, { signal });\n        return res.json() as Promise<{ name: string }>;\n      },\n      runtime,\n      { debugName: `profile:${userId}` }\n    );\n    profileResources.set(userId, resource);\n  }\n  return resource;\n}\n\nfunction ChartsPanel({ userId }: { userId: string }) {\n  const runtime = useStoreRuntime();\n  // profileResource 按 userId 缓存同一个 Resource 实例，避免每次渲染都创建新对象（见 §7.6）。\n  const profile = useResourceValue(profileResource(userId, runtime)); // 未就绪时 throw promise，交给外层 Suspense\n  return <div>欢迎，{profile.name}</div>;\n}\n\nfunction FullPageSpinner() {\n  return <div role=\"status\">加载中…</div>;\n}",
            "const sessionStore = useStoreFromProvider(sessionToken);\nconst userId = useProvidedStore(sessionToken, (s) => s.userId);",
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\n\nconst sessionToken = createStoreToken('session');\n\nfunction App() {\n  const ready = useMemo(() => [() => Promise.resolve()], []); // 引用必须稳定\n  const sessionStore = useMemo(() => createStore({ userId: '' }), []);\n\n  return (\n    <StoreProvider config={{ ready, fallback: <div>加载中…</div> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<div>加载中…</div>}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\nfunction SessionInjector({ store, children }) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return children;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const runtime = useStoreRuntime();\n  const showCharts = useStoreFeature('experimental.charts');\n  const profile = useResourceValue(new Resource(async () => ({ name: userId }), runtime));\n  return (\n    <p>\n      {profile.name}\n      {showCharts ? ' · 图表已开启' : ''}\n    </p>\n  );\n}"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useProvidedStore<S extends Record<string, unknown>, Result>(token: IStoreToken<IReactiveStore<S>>, selector: (state: IStoreShape<S>) => Result, isEqual?: (left: Result, right: Result) => boolean): Result;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "token",
                "type": "IStoreToken<IReactiveStore<S>>",
                "optional": false
              },
              {
                "name": "selector",
                "type": "(state: IStoreShape<S>) => Result",
                "optional": false
              },
              {
                "name": "isEqual",
                "type": "(left: Result, right: Result) => boolean",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["Result"],
          "whenToUse": "Use useProvidedStore when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useProvidedStore does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useProvidedStore is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useProvidedStore from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useProvidedStore when its function contract and declared inputs match the result you need. Choose a different public symbol when useProvidedStore does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useProvidedStore."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useProvidedStore."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider.d.ts:useProvidedStore:export declare function useProvidedStore<S extends Record<string, unknown>, Result>(token: IStoreToken<IReactiveStore<S>>, selector: (state: IStoreShape<S>) => Result, isEqual?: (left: Result, right: Result) => boolean): Result;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 84,
          "configuration": [],
          "fragment": "store-react-index--useProvidedStore"
        },
        {
          "name": "useResource",
          "kind": "function",
          "signature": "export declare function useResource<T>(res: Resource<T>): IResourceState<T>;",
          "declarationLine": 8,
          "source": "packages/store-react/dist/useStore.d.ts",
          "purpose": "useResource is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useResource.",
          "advanced": "No additional advanced behavior is declared for useResource.",
          "parameters": ["res: Resource<T>"],
          "parameterDetails": [
            {
              "name": "res",
              "type": "Resource<T>",
              "optional": false
            }
          ],
          "returns": "IResourceState<T>",
          "errors": ["No documented errors are declared for useResource."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useResource.",
          "examples": [
            "const state = useResource(profileResource); // { status: 'idle'|'pending'|'success'|'error'|'cancelled', ... }\nconst profile = useResourceValue(profileResource); // 成功返回 data；pending/idle 时 throw promise；error/cancelled 时 throw error"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useResource<T>(res: Resource<T>): IResourceState<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "res",
                "type": "Resource<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IResourceState<T>"],
          "whenToUse": "Use useResource when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useResource does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useResource is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useResource from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useResource when its function contract and declared inputs match the result you need. Choose a different public symbol when useResource does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useResource."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useResource."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useStore.d.ts:useResource:export declare function useResource<T>(res: Resource<T>): IResourceState<T>;",
          "guidance": [],
          "usageScore": 54,
          "configuration": [],
          "fragment": "store-react-index--useResource"
        },
        {
          "name": "useResourceValue",
          "kind": "function",
          "signature": "export declare function useResourceValue<T>(resource: Resource<T>): T;",
          "declarationLine": 15,
          "source": "packages/store-react/dist/useAtom.d.ts",
          "purpose": "Suspense-safe Resource read. The subscription observes only the state machine; the Promise/error is thrown from render, never from the scheduler's Effect callback.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useResourceValue.",
          "advanced": "No additional advanced behavior is declared for useResourceValue.",
          "parameters": ["resource: Resource<T>"],
          "parameterDetails": [
            {
              "name": "resource",
              "type": "Resource<T>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useResourceValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useResourceValue.",
          "examples": [
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport type { IReactiveStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\nimport type { IRuntime } from '@migaia/reactive';\n\ntype ISession = { userId: string; setUserId(id: string): void };\nconst sessionToken = createStoreToken<IReactiveStore<ISession>>('session');\n\nfunction restoreSession(): Promise<void> {\n  // 例如从 localStorage/远端恢复登录态；调用方负责把它 memo 住。\n  return Promise.resolve();\n}\n\nfunction App() {\n  // ready 数组的引用必须稳定，否则每次渲染都会命中 §2.3 的身份检查警告。\n  const ready = useMemo(() => [restoreSession], []);\n  const sessionStore = useMemo(\n    () =>\n      createStore<ISession>({\n        userId: '',\n        setUserId(id) {\n          this.userId = id;\n        }\n      }),\n    []\n  );\n\n  return (\n    <StoreProvider config={{ ready, fallback: <FullPageSpinner /> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<FullPageSpinner />}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\n// 用 register 而非全局单例：不同的 Provider 子树可以注入不同的 session 实例（比如测试）。\nfunction SessionInjector({\n  store,\n  children\n}: {\n  store: IReactiveStore<ISession>;\n  children: React.ReactNode;\n}) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return <>{children}</>;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const experimentalCharts = useStoreFeature('experimental.charts');\n  return (\n    <div>\n      <p>当前用户：{userId || '未登录'}</p>\n      {experimentalCharts ? <ChartsPanel userId={userId} /> : null}\n    </div>\n  );\n}\n\nconst profileResources = new Map<string, Resource<{ name: string }>>();\nfunction profileResource(userId: string, runtime: IRuntime): Resource<{ name: string }> {\n  let resource = profileResources.get(userId);\n  if (!resource) {\n    resource = new Resource(\n      async ({ signal }) => {\n        const res = await fetch(`/api/profile/${userId}`, { signal });\n        return res.json() as Promise<{ name: string }>;\n      },\n      runtime,\n      { debugName: `profile:${userId}` }\n    );\n    profileResources.set(userId, resource);\n  }\n  return resource;\n}\n\nfunction ChartsPanel({ userId }: { userId: string }) {\n  const runtime = useStoreRuntime();\n  // profileResource 按 userId 缓存同一个 Resource 实例，避免每次渲染都创建新对象（见 §7.6）。\n  const profile = useResourceValue(profileResource(userId, runtime)); // 未就绪时 throw promise，交给外层 Suspense\n  return <div>欢迎，{profile.name}</div>;\n}\n\nfunction FullPageSpinner() {\n  return <div role=\"status\">加载中…</div>;\n}",
            "const state = useResource(profileResource); // { status: 'idle'|'pending'|'success'|'error'|'cancelled', ... }\nconst profile = useResourceValue(profileResource); // 成功返回 data；pending/idle 时 throw promise；error/cancelled 时 throw error",
            "const value = useAsyncAtomValue(asyncAtom); // 内部即 useResourceValue(atom.resource)",
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\n\nconst sessionToken = createStoreToken('session');\n\nfunction App() {\n  const ready = useMemo(() => [() => Promise.resolve()], []); // 引用必须稳定\n  const sessionStore = useMemo(() => createStore({ userId: '' }), []);\n\n  return (\n    <StoreProvider config={{ ready, fallback: <div>加载中…</div> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<div>加载中…</div>}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\nfunction SessionInjector({ store, children }) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return children;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const runtime = useStoreRuntime();\n  const showCharts = useStoreFeature('experimental.charts');\n  const profile = useResourceValue(new Resource(async () => ({ name: userId }), runtime));\n  return (\n    <p>\n      {profile.name}\n      {showCharts ? ' · 图表已开启' : ''}\n    </p>\n  );\n}"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useResourceValue<T>(resource: Resource<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "resource",
                "type": "Resource<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useResourceValue when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useResourceValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Suspense-safe Resource read. The subscription observes only the state machine; the Promise/error is thrown from render, never from the scheduler's Effect callback."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useResourceValue from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useResourceValue when its function contract and declared inputs match the result you need. Choose a different public symbol when useResourceValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useResourceValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useResourceValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useAtom.d.ts:useResourceValue:export declare function useResourceValue<T>(resource: Resource<T>): T;",
          "guidance": [],
          "usageScore": 86,
          "configuration": [],
          "fragment": "store-react-index--useResourceValue"
        },
        {
          "name": "useSetAtom",
          "kind": "function",
          "signature": "export declare function useSetAtom<T, Args extends readonly unknown[], Result>(atom: IWritableAtom<T, Args, Result>): (...args: Args) => Result;",
          "declarationLine": 9,
          "source": "packages/store-react/dist/useAtom.d.ts",
          "purpose": "Stable writer callback; writes remain batched by writable derived atoms.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useSetAtom.",
          "advanced": "No additional advanced behavior is declared for useSetAtom.",
          "parameters": ["atom: IWritableAtom<T, Args, Result>"],
          "parameterDetails": [
            {
              "name": "atom",
              "type": "IWritableAtom<T, Args, Result>",
              "optional": false
            }
          ],
          "returns": "(...args: Args) => Result",
          "errors": ["No documented errors are declared for useSetAtom."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useSetAtom.",
          "examples": [
            "const value = useAtomValue(myAtom);\nconst write = useSetAtom(myWritableAtom);\nconst [value2, write2] = useAtom(myWritableAtom);"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useSetAtom<T, Args extends readonly unknown[], Result>(atom: IWritableAtom<T, Args, Result>): (...args: Args) => Result;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "atom",
                "type": "IWritableAtom<T, Args, Result>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["(...args: Args) => Result"],
          "whenToUse": "Use useSetAtom when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useSetAtom does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Stable writer callback; writes remain batched by writable derived atoms."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useSetAtom from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useSetAtom when its function contract and declared inputs match the result you need. Choose a different public symbol when useSetAtom does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useSetAtom."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useSetAtom."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useAtom.d.ts:useSetAtom:export declare function useSetAtom<T, Args extends readonly unknown[], Result>(atom: IWritableAtom<T, Args, Result>): (...args: Args) => Result;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 68,
          "configuration": [],
          "fragment": "store-react-index--useSetAtom"
        },
        {
          "name": "useSetAtomDefinition",
          "kind": "function",
          "signature": "export declare function useSetAtomDefinition<T, Args extends readonly unknown[], Result>(definition: IWritableAtomDefinition<T, Args, Result>): (...args: Args) => Result;",
          "declarationLine": 3,
          "source": "packages/store-react/dist/useAtomDefinition.d.ts",
          "purpose": "useSetAtomDefinition is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useSetAtomDefinition.",
          "advanced": "No additional advanced behavior is declared for useSetAtomDefinition.",
          "parameters": ["definition: IWritableAtomDefinition<T, Args, Result>"],
          "parameterDetails": [
            {
              "name": "definition",
              "type": "IWritableAtomDefinition<T, Args, Result>",
              "optional": false
            }
          ],
          "returns": "(...args: Args) => Result",
          "errors": ["No documented errors are declared for useSetAtomDefinition."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useSetAtomDefinition.",
          "examples": [
            "const value = useAtomDefinition(countDef);\nconst increment = useSetAtomDefinition(incrementDef);",
            "import { StoreProvider, useAtomDefinition, useSetAtomDefinition } from '@migaia/store-react';\nimport { atomDef, writableDef } from '@migaia/store-keyed/atom/definition';\n\nconst countDef = atomDef(0);\nconst incrementDef = writableDef(\n  (get) => get(countDef),\n  (get, set) => set(countDef, get(countDef) + 1)\n);\n\nfunction Counter() {\n  const count = useAtomDefinition(countDef);\n  const increment = useSetAtomDefinition(incrementDef);\n  return <button onClick={() => increment()}>count: {count}</button>;\n}\n\nfunction App() {\n  return (\n    <StoreProvider>\n      <Counter />\n    </StoreProvider>\n  );\n}"
          ],
          "members": [],
          "aliasOf": "useSetAtomDefinition",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useSetAtomDefinition<T, Args extends readonly unknown[], Result>(definition: IWritableAtomDefinition<T, Args, Result>): (...args: Args) => Result;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "definition",
                "type": "IWritableAtomDefinition<T, Args, Result>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["(...args: Args) => Result"],
          "whenToUse": "Use useSetAtomDefinition when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useSetAtomDefinition does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useSetAtomDefinition is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useSetAtomDefinition from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useSetAtomDefinition when its function contract and declared inputs match the result you need. Choose a different public symbol when useSetAtomDefinition does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useSetAtomDefinition."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useSetAtomDefinition."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useAtomDefinition.d.ts:useSetAtomDefinition:export declare function useSetAtomDefinition<T, Args extends readonly unknown[], Result>(definition: IWritableAtomDefinition<T, Args, Result>): (...args: Args) => Result;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 62,
          "configuration": [],
          "fragment": "store-react-index--useSetAtomDefinition"
        },
        {
          "name": "useSignal",
          "kind": "function",
          "signature": "export declare function useSignal<T>(s: ISignal<T>): readonly [T, (next: T) => void];",
          "declarationLine": 6,
          "source": "packages/store-react/dist/useStore.d.ts",
          "purpose": "useSignal is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useSignal.",
          "advanced": "No additional advanced behavior is declared for useSignal.",
          "parameters": ["s: ISignal<T>"],
          "parameterDetails": [
            {
              "name": "s",
              "type": "ISignal<T>",
              "optional": false
            }
          ],
          "returns": "readonly [T, (next: T) => void]",
          "errors": ["No documented errors are declared for useSignal."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useSignal.",
          "examples": ["const [name, setName] = useSignal(nameSignal);"],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useSignal<T>(s: ISignal<T>): readonly [T, (next: T) => void];"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "s",
                "type": "ISignal<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["readonly [T, (next: T) => void]"],
          "whenToUse": "Use useSignal when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useSignal does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useSignal is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useSignal from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useSignal when its function contract and declared inputs match the result you need. Choose a different public symbol when useSignal does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useSignal."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useSignal."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useStore.d.ts:useSignal:export declare function useSignal<T>(s: ISignal<T>): readonly [T, (next: T) => void];",
          "guidance": [],
          "usageScore": 54,
          "configuration": [],
          "fragment": "store-react-index--useSignal"
        },
        {
          "name": "useStore",
          "kind": "function",
          "signature": "export declare function useStore<S extends Record<string, unknown>, R>(store: IReactiveStore<S>, selector: (state: IStoreShape<S>) => R, isEqual?: (a: R, b: R) => boolean): R;",
          "declarationLine": 7,
          "source": "packages/store-react/dist/useStore.d.ts",
          "purpose": "useStore is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStore.",
          "advanced": "No additional advanced behavior is declared for useStore.",
          "parameters": [
            "store: IReactiveStore<S>",
            "selector: (state: IStoreShape<S>) => R",
            "isEqual: (a: R, b: R) => boolean (optional)"
          ],
          "parameterDetails": [
            {
              "name": "store",
              "type": "IReactiveStore<S>",
              "optional": false
            },
            {
              "name": "selector",
              "type": "(state: IStoreShape<S>) => R",
              "optional": false
            },
            {
              "name": "isEqual",
              "type": "(a: R, b: R) => boolean",
              "optional": true
            }
          ],
          "returns": "R",
          "errors": ["No documented errors are declared for useStore."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useStore.",
          "examples": [
            "const counterStore = createStore({\n  count: 0,\n  increment() {\n    this.count += 1;\n  }\n});\n\nfunction Counter() {\n  const count = useStore(counterStore, (state) => state.count);\n  return <button onClick={() => counterStore.increment()}>count: {count}</button>;\n}\n\nfunction App() {\n  return (\n    <StoreProvider>\n      <Counter />\n    </StoreProvider>\n  );\n}",
            "const count = useStore(counterStore, (state) => state.count);"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useStore<S extends Record<string, unknown>, R>(store: IReactiveStore<S>, selector: (state: IStoreShape<S>) => R, isEqual?: (a: R, b: R) => boolean): R;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "store",
                "type": "IReactiveStore<S>",
                "optional": false
              },
              {
                "name": "selector",
                "type": "(state: IStoreShape<S>) => R",
                "optional": false
              },
              {
                "name": "isEqual",
                "type": "(a: R, b: R) => boolean",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["R"],
          "whenToUse": "Use useStore when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useStore does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useStore is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useStore from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useStore when its function contract and declared inputs match the result you need. Choose a different public symbol when useStore does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStore."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useStore."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useStore.d.ts:useStore:export declare function useStore<S extends Record<string, unknown>, R>(store: IReactiveStore<S>, selector: (state: IStoreShape<S>) => R, isEqual?: (a: R, b: R) => boolean): R;",
          "guidance": [
            {
              "id": "2-2-config-的四个字段",
              "heading": "2.2 config 的四个字段",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IStoreProviderConfig = {\n  readonly features?: {\n    readonly wasm?: boolean;\n    readonly experimental?: Readonly<Record<string, boolean>>;\n  };\n  readonly ready?: readonly (Promise<unknown> | (() => Promise<unknown> | unknown))[];\n  readonly fallback?: ReactNode;\n  readonly defaults?: { readonly warnAsyncActions?: boolean };\n};"
                },
                {
                  "type": "list",
                  "items": [
                    "`features.wasm`：声明本树会用到 wasm 字段。为 `true` 且 `ready` 为空数组时，会自动追加一个立刻 reject 的屏障（见 5），避免\"开了开关却没人 init\"。",
                    "`features.experimental`：白名单对象，只有值严格为 `true` 的 key 才算开启；未列出或非 `true` 一律视为关闭。",
                    "`ready`：见 2.3。",
                    "`fallback`：`ready` 屏障 pending 期间渲染的占位内容，默认 `null`（不是 `undefined`——不传 `fallback` 且有 `ready` 时，pending 阶段渲染的是\"什么都不渲染\"而不是上一次的 children）。",
                    "`defaults.warnAsyncActions`：只是配置快照的一部分，通过 `useStoreConfig()` 暴露给业务代码读取；`StoreProvider` 不会把它自动传给 `createStore`——各个 Store 仍要自己在 `createStore(shape, { warnAsyncActions: true })` 里显式声明。这个字段更适合用作\"业务代码统一读一个地方决定要不要传\"的约定，而不是自动生效的开关。"
                  ]
                }
              ]
            },
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            },
            {
              "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
              "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
                },
                {
                  "type": "list",
                  "items": [
                    "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                    "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                    "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                  ]
                }
              ]
            },
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            },
            {
              "id": "config-feature-flag",
              "heading": "Config / Feature flag",
              "blocks": [
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreConfig()`｜3 秒上手 —— 取归一化后的完整配置快照："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "const config = useStoreConfig(); // 无 Provider 时返回 null"
                },
                {
                  "type": "paragraph",
                  "text": "无参数。"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreFeature(path)` / `useAssertStoreFeature(path, apiName?)`｜5 秒上手 —— 查询/断言某个 feature 是否开启，用于给实验性 API 设置入口守卫："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
                },
                {
                  "type": "paragraph",
                  "text": "`path: 'wasm' | \\`experimental.${string}\\`` 是两者共同的必填参数；`useAssertStoreFeature`额外的`apiName?: string`（默认 `'this API'`）出现在错误信息里。`useStoreFeature`无 Provider 时返回`false`，不抛；`useAssertStoreFeature`未开启（含无 Provider）一律抛`Error`。这两个都要通过 `StoreProvider`的`config.features.wasm`/`config.features.experimental`显式开启才算生效（只认字面`true`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`StoreProviderState`｜3 秒上手 —— 就绪屏障三态的稳定取值集合，常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
                },
                {
                  "type": "paragraph",
                  "text": "业务代码一般不需要直接用它，除非要复刻类似的三态就绪追踪逻辑。"
                }
              ]
            }
          ],
          "usageScore": 114,
          "configuration": [],
          "fragment": "store-react-index--useStore"
        },
        {
          "name": "useStoreConfig",
          "kind": "function",
          "signature": "export declare function useStoreConfig(): IStoreConfigValue | null;",
          "declarationLine": 65,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "完整配置；无 Provider 时返回 null（与 registry 不同，配置是可选的）。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreConfig.",
          "advanced": "No additional advanced behavior is declared for useStoreConfig.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IStoreConfigValue | null",
          "errors": ["No documented errors are declared for useStoreConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useStoreConfig.",
          "examples": ["const config = useStoreConfig(); // 无 Provider 时返回 null"],
          "members": [],
          "aliasOf": "useStoreConfig",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useStoreConfig(): IStoreConfigValue | null;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IStoreConfigValue | null"],
          "whenToUse": "Use useStoreConfig when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useStoreConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "完整配置；无 Provider 时返回 null（与 registry 不同，配置是可选的）。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useStoreConfig from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useStoreConfig when its function contract and declared inputs match the result you need. Choose a different public symbol when useStoreConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useStoreConfig."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:useStoreConfig:export declare function useStoreConfig(): IStoreConfigValue | null;",
          "guidance": [
            {
              "id": "2-2-config-的四个字段",
              "heading": "2.2 config 的四个字段",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IStoreProviderConfig = {\n  readonly features?: {\n    readonly wasm?: boolean;\n    readonly experimental?: Readonly<Record<string, boolean>>;\n  };\n  readonly ready?: readonly (Promise<unknown> | (() => Promise<unknown> | unknown))[];\n  readonly fallback?: ReactNode;\n  readonly defaults?: { readonly warnAsyncActions?: boolean };\n};"
                },
                {
                  "type": "list",
                  "items": [
                    "`features.wasm`：声明本树会用到 wasm 字段。为 `true` 且 `ready` 为空数组时，会自动追加一个立刻 reject 的屏障（见 5），避免\"开了开关却没人 init\"。",
                    "`features.experimental`：白名单对象，只有值严格为 `true` 的 key 才算开启；未列出或非 `true` 一律视为关闭。",
                    "`ready`：见 2.3。",
                    "`fallback`：`ready` 屏障 pending 期间渲染的占位内容，默认 `null`（不是 `undefined`——不传 `fallback` 且有 `ready` 时，pending 阶段渲染的是\"什么都不渲染\"而不是上一次的 children）。",
                    "`defaults.warnAsyncActions`：只是配置快照的一部分，通过 `useStoreConfig()` 暴露给业务代码读取；`StoreProvider` 不会把它自动传给 `createStore`——各个 Store 仍要自己在 `createStore(shape, { warnAsyncActions: true })` 里显式声明。这个字段更适合用作\"业务代码统一读一个地方决定要不要传\"的约定，而不是自动生效的开关。"
                  ]
                }
              ]
            },
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            },
            {
              "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
              "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
                },
                {
                  "type": "list",
                  "items": [
                    "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                    "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                    "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                  ]
                }
              ]
            },
            {
              "id": "config-feature-flag",
              "heading": "Config / Feature flag",
              "blocks": [
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreConfig()`｜3 秒上手 —— 取归一化后的完整配置快照："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "const config = useStoreConfig(); // 无 Provider 时返回 null"
                },
                {
                  "type": "paragraph",
                  "text": "无参数。"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreFeature(path)` / `useAssertStoreFeature(path, apiName?)`｜5 秒上手 —— 查询/断言某个 feature 是否开启，用于给实验性 API 设置入口守卫："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
                },
                {
                  "type": "paragraph",
                  "text": "`path: 'wasm' | \\`experimental.${string}\\`` 是两者共同的必填参数；`useAssertStoreFeature`额外的`apiName?: string`（默认 `'this API'`）出现在错误信息里。`useStoreFeature`无 Provider 时返回`false`，不抛；`useAssertStoreFeature`未开启（含无 Provider）一律抛`Error`。这两个都要通过 `StoreProvider`的`config.features.wasm`/`config.features.experimental`显式开启才算生效（只认字面`true`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`StoreProviderState`｜3 秒上手 —— 就绪屏障三态的稳定取值集合，常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
                },
                {
                  "type": "paragraph",
                  "text": "业务代码一般不需要直接用它，除非要复刻类似的三态就绪追踪逻辑。"
                }
              ]
            }
          ],
          "usageScore": 46,
          "configuration": [],
          "fragment": "store-react-index--useStoreConfig"
        },
        {
          "name": "useStoreFeature",
          "kind": "function",
          "signature": "export declare function useStoreFeature(path: IStoreFeaturePath): boolean;",
          "declarationLine": 67,
          "source": "packages/store-react/dist/store-config.d.ts",
          "purpose": "查询特性开关。 - 无 Provider：一律 false（不抛），便于可选增强 - 有 Provider：读归一化后的 features",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreFeature.",
          "advanced": "No additional advanced behavior is declared for useStoreFeature.",
          "parameters": ["path: IStoreFeaturePath"],
          "parameterDetails": [
            {
              "name": "path",
              "type": "IStoreFeaturePath",
              "optional": false
            }
          ],
          "returns": "boolean",
          "errors": ["No documented errors are declared for useStoreFeature."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useStoreFeature.",
          "examples": [
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport type { IReactiveStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\nimport type { IRuntime } from '@migaia/reactive';\n\ntype ISession = { userId: string; setUserId(id: string): void };\nconst sessionToken = createStoreToken<IReactiveStore<ISession>>('session');\n\nfunction restoreSession(): Promise<void> {\n  // 例如从 localStorage/远端恢复登录态；调用方负责把它 memo 住。\n  return Promise.resolve();\n}\n\nfunction App() {\n  // ready 数组的引用必须稳定，否则每次渲染都会命中 §2.3 的身份检查警告。\n  const ready = useMemo(() => [restoreSession], []);\n  const sessionStore = useMemo(\n    () =>\n      createStore<ISession>({\n        userId: '',\n        setUserId(id) {\n          this.userId = id;\n        }\n      }),\n    []\n  );\n\n  return (\n    <StoreProvider config={{ ready, fallback: <FullPageSpinner /> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<FullPageSpinner />}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\n// 用 register 而非全局单例：不同的 Provider 子树可以注入不同的 session 实例（比如测试）。\nfunction SessionInjector({\n  store,\n  children\n}: {\n  store: IReactiveStore<ISession>;\n  children: React.ReactNode;\n}) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return <>{children}</>;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const experimentalCharts = useStoreFeature('experimental.charts');\n  return (\n    <div>\n      <p>当前用户：{userId || '未登录'}</p>\n      {experimentalCharts ? <ChartsPanel userId={userId} /> : null}\n    </div>\n  );\n}\n\nconst profileResources = new Map<string, Resource<{ name: string }>>();\nfunction profileResource(userId: string, runtime: IRuntime): Resource<{ name: string }> {\n  let resource = profileResources.get(userId);\n  if (!resource) {\n    resource = new Resource(\n      async ({ signal }) => {\n        const res = await fetch(`/api/profile/${userId}`, { signal });\n        return res.json() as Promise<{ name: string }>;\n      },\n      runtime,\n      { debugName: `profile:${userId}` }\n    );\n    profileResources.set(userId, resource);\n  }\n  return resource;\n}\n\nfunction ChartsPanel({ userId }: { userId: string }) {\n  const runtime = useStoreRuntime();\n  // profileResource 按 userId 缓存同一个 Resource 实例，避免每次渲染都创建新对象（见 §7.6）。\n  const profile = useResourceValue(profileResource(userId, runtime)); // 未就绪时 throw promise，交给外层 Suspense\n  return <div>欢迎，{profile.name}</div>;\n}\n\nfunction FullPageSpinner() {\n  return <div role=\"status\">加载中…</div>;\n}",
            "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错",
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\n\nconst sessionToken = createStoreToken('session');\n\nfunction App() {\n  const ready = useMemo(() => [() => Promise.resolve()], []); // 引用必须稳定\n  const sessionStore = useMemo(() => createStore({ userId: '' }), []);\n\n  return (\n    <StoreProvider config={{ ready, fallback: <div>加载中…</div> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<div>加载中…</div>}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\nfunction SessionInjector({ store, children }) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return children;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const runtime = useStoreRuntime();\n  const showCharts = useStoreFeature('experimental.charts');\n  const profile = useResourceValue(new Resource(async () => ({ name: userId }), runtime));\n  return (\n    <p>\n      {profile.name}\n      {showCharts ? ' · 图表已开启' : ''}\n    </p>\n  );\n}"
          ],
          "members": [],
          "aliasOf": "useStoreFeature",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useStoreFeature(path: IStoreFeaturePath): boolean;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "path",
                "type": "IStoreFeaturePath",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["boolean"],
          "whenToUse": "Use useStoreFeature when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useStoreFeature does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "查询特性开关。 - 无 Provider：一律 false（不抛），便于可选增强 - 有 Provider：读归一化后的 features"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useStoreFeature from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useStoreFeature when its function contract and declared inputs match the result you need. Choose a different public symbol when useStoreFeature does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreFeature."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useStoreFeature."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/store-config.d.ts:useStoreFeature:export declare function useStoreFeature(path: IStoreFeaturePath): boolean;",
          "guidance": [
            {
              "id": "2-5-assertstorefeature-config-path-apiname-normalizestoreconfig-config-barrierscope-readstorefeature-config-path",
              "heading": "2.5 assertStoreFeature(config, path, apiName?) / normalizeStoreConfig(config?, barrierScope?) / readStoreFeature(config, path)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readStoreFeature(\n  config: IStoreConfigValue,\n  path: 'wasm' | `experimental.${string}`\n): boolean;\nfunction assertStoreFeature(\n  config: IStoreConfigValue | null,\n  path: 'wasm' | `experimental.${string}`,\n  apiName?: string // 默认 'this API'，出现在错误信息里\n): void;\nfunction normalizeStoreConfig(\n  config?: IStoreProviderConfig,\n  barrierScope?: object // 默认 {}；用于隔离不同调用方各自的 ready-barrier 缓存\n): IStoreConfigValue;"
                },
                {
                  "type": "paragraph",
                  "text": "这三个是 `useStoreFeature`/`useAssertStoreFeature`/`StoreProvider` 内部依赖的非 hook 版本，公开导出是为了让\"不在组件里、但需要读取/校验同一份配置语义\"的代码复用同一套逻辑（例如自定义的 SSR 请求作用域装配代码，或者非 React 的适配层）："
                },
                {
                  "type": "list",
                  "items": [
                    "`readStoreFeature(config, path)`：纯查询，`path === 'wasm'` 直接读 `config.features.wasm`；`path` 形如 `` `experimental.${key}` `` 时读 `config.features.experimental[key] === true`；其他 `path` 一律返回 `false`。",
                    "`assertStoreFeature(config, path, apiName?)`：`config` 为 `null` 时抛 `[store] ${apiName} requires a StoreProvider (feature \"path\")`；`config` 非空但 `readStoreFeature` 返回 `false` 时抛 ` [store] ${apiName} requires feature \"path\" to be explicitly enabled on StoreProvider config`。`useAssertStoreFeature` 就是\"取 `useStoreConfig()` 后调用这个函数\"的薄封装。",
                    "`normalizeStoreConfig(config?, barrierScope?)`：`StoreProvider` 内部用来把 `IStoreProviderConfig` 归一化成 `IStoreConfigValue`（`features` 补全默认值、`ready` 屏障数组转换成惰性求值+缓存的 `ITrackedReady`）的同一段逻辑，独立导出后可以在 Provider 之外复现相同的归一化行为；`barrierScope` 决定就绪 Promise 与工厂结果缓存的隔离边界——不同调用传不同的 `barrierScope` 对象，各自独立求值，不会共享缓存。`config.ready` 里出现既不是函数也不是 thenable 的元素会抛 `[store] config.ready must be an array of Promises or zero-argument functions`。"
                  ]
                }
              ]
            },
            {
              "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
              "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
                },
                {
                  "type": "list",
                  "items": [
                    "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                    "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                    "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                  ]
                }
              ]
            },
            {
              "id": "config-feature-flag",
              "heading": "Config / Feature flag",
              "blocks": [
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "import {\n  useStoreConfig,\n  useStoreFeature,\n  useAssertStoreFeature,\n  StoreProviderState\n} from '@migaia/store-react';"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreConfig()`｜3 秒上手 —— 取归一化后的完整配置快照："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "const config = useStoreConfig(); // 无 Provider 时返回 null"
                },
                {
                  "type": "paragraph",
                  "text": "无参数。"
                },
                {
                  "type": "paragraph",
                  "text": "`useStoreFeature(path)` / `useAssertStoreFeature(path, apiName?)`｜5 秒上手 —— 查询/断言某个 feature 是否开启，用于给实验性 API 设置入口守卫："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "if (useStoreFeature('experimental.charts')) {\n  /* 渲染实验特性 */\n}\nuseAssertStoreFeature('wasm', 'myWasmApi'); // 未开启（含无 Provider）直接抛错"
                },
                {
                  "type": "paragraph",
                  "text": "`path: 'wasm' | \\`experimental.${string}\\`` 是两者共同的必填参数；`useAssertStoreFeature`额外的`apiName?: string`（默认 `'this API'`）出现在错误信息里。`useStoreFeature`无 Provider 时返回`false`，不抛；`useAssertStoreFeature`未开启（含无 Provider）一律抛`Error`。这两个都要通过 `StoreProvider`的`config.features.wasm`/`config.features.experimental`显式开启才算生效（只认字面`true`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`StoreProviderState`｜3 秒上手 —— 就绪屏障三态的稳定取值集合，常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "tsx",
                  "code": "StoreProviderState.pending; // 'pending' | 'ready' | 'error' 之一"
                },
                {
                  "type": "paragraph",
                  "text": "业务代码一般不需要直接用它，除非要复刻类似的三态就绪追踪逻辑。"
                }
              ]
            }
          ],
          "usageScore": 72,
          "configuration": [],
          "fragment": "store-react-index--useStoreFeature"
        },
        {
          "name": "useStoreFromProvider",
          "kind": "function",
          "signature": "export declare function useStoreFromProvider<T>(token: IStoreToken<T>): T;",
          "declarationLine": 12,
          "source": "packages/store-react/dist/provider.d.ts",
          "purpose": "useStoreFromProvider is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreFromProvider.",
          "advanced": "No additional advanced behavior is declared for useStoreFromProvider.",
          "parameters": ["token: IStoreToken<T>"],
          "parameterDetails": [
            {
              "name": "token",
              "type": "IStoreToken<T>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useStoreFromProvider."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useStoreFromProvider.",
          "examples": [
            "const sessionStore = useStoreFromProvider(sessionToken);\nconst userId = useProvidedStore(sessionToken, (s) => s.userId);"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useStoreFromProvider<T>(token: IStoreToken<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "token",
                "type": "IStoreToken<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useStoreFromProvider when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useStoreFromProvider does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useStoreFromProvider is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useStoreFromProvider from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useStoreFromProvider when its function contract and declared inputs match the result you need. Choose a different public symbol when useStoreFromProvider does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreFromProvider."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useStoreFromProvider."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider.d.ts:useStoreFromProvider:export declare function useStoreFromProvider<T>(token: IStoreToken<T>): T;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 56,
          "configuration": [],
          "fragment": "store-react-index--useStoreFromProvider"
        },
        {
          "name": "useStoreRegistry",
          "kind": "function",
          "signature": "export declare function useStoreRegistry(): import(\"./provider-registry.js\").StoreRegistry;",
          "declarationLine": 10,
          "source": "packages/store-react/dist/provider.d.ts",
          "purpose": "useStoreRegistry is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreRegistry.",
          "advanced": "No additional advanced behavior is declared for useStoreRegistry.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "import(\"./provider-registry.js\").StoreRegistry",
          "errors": ["No documented errors are declared for useStoreRegistry."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useStoreRegistry.",
          "examples": [
            "function useStoreRegistry(): StoreRegistry; // 无 Provider 抛错\nfunction useStoreRuntime(): IRuntime; // = useStoreRegistry().runtime",
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport type { IReactiveStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\nimport type { IRuntime } from '@migaia/reactive';\n\ntype ISession = { userId: string; setUserId(id: string): void };\nconst sessionToken = createStoreToken<IReactiveStore<ISession>>('session');\n\nfunction restoreSession(): Promise<void> {\n  // 例如从 localStorage/远端恢复登录态；调用方负责把它 memo 住。\n  return Promise.resolve();\n}\n\nfunction App() {\n  // ready 数组的引用必须稳定，否则每次渲染都会命中 §2.3 的身份检查警告。\n  const ready = useMemo(() => [restoreSession], []);\n  const sessionStore = useMemo(\n    () =>\n      createStore<ISession>({\n        userId: '',\n        setUserId(id) {\n          this.userId = id;\n        }\n      }),\n    []\n  );\n\n  return (\n    <StoreProvider config={{ ready, fallback: <FullPageSpinner /> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<FullPageSpinner />}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\n// 用 register 而非全局单例：不同的 Provider 子树可以注入不同的 session 实例（比如测试）。\nfunction SessionInjector({\n  store,\n  children\n}: {\n  store: IReactiveStore<ISession>;\n  children: React.ReactNode;\n}) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return <>{children}</>;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const experimentalCharts = useStoreFeature('experimental.charts');\n  return (\n    <div>\n      <p>当前用户：{userId || '未登录'}</p>\n      {experimentalCharts ? <ChartsPanel userId={userId} /> : null}\n    </div>\n  );\n}\n\nconst profileResources = new Map<string, Resource<{ name: string }>>();\nfunction profileResource(userId: string, runtime: IRuntime): Resource<{ name: string }> {\n  let resource = profileResources.get(userId);\n  if (!resource) {\n    resource = new Resource(\n      async ({ signal }) => {\n        const res = await fetch(`/api/profile/${userId}`, { signal });\n        return res.json() as Promise<{ name: string }>;\n      },\n      runtime,\n      { debugName: `profile:${userId}` }\n    );\n    profileResources.set(userId, resource);\n  }\n  return resource;\n}\n\nfunction ChartsPanel({ userId }: { userId: string }) {\n  const runtime = useStoreRuntime();\n  // profileResource 按 userId 缓存同一个 Resource 实例，避免每次渲染都创建新对象（见 §7.6）。\n  const profile = useResourceValue(profileResource(userId, runtime)); // 未就绪时 throw promise，交给外层 Suspense\n  return <div>欢迎，{profile.name}</div>;\n}\n\nfunction FullPageSpinner() {\n  return <div role=\"status\">加载中…</div>;\n}",
            "const registry = useStoreRegistry();\nconst runtime = useStoreRuntime(); // = useStoreRegistry().runtime",
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\n\nconst sessionToken = createStoreToken('session');\n\nfunction App() {\n  const ready = useMemo(() => [() => Promise.resolve()], []); // 引用必须稳定\n  const sessionStore = useMemo(() => createStore({ userId: '' }), []);\n\n  return (\n    <StoreProvider config={{ ready, fallback: <div>加载中…</div> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<div>加载中…</div>}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\nfunction SessionInjector({ store, children }) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return children;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const runtime = useStoreRuntime();\n  const showCharts = useStoreFeature('experimental.charts');\n  const profile = useResourceValue(new Resource(async () => ({ name: userId }), runtime));\n  return (\n    <p>\n      {profile.name}\n      {showCharts ? ' · 图表已开启' : ''}\n    </p>\n  );\n}"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useStoreRegistry(): import(\"./provider-registry.js\").StoreRegistry;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["import(\"./provider-registry.js\").StoreRegistry"],
          "whenToUse": "Use useStoreRegistry when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useStoreRegistry does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useStoreRegistry is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useStoreRegistry from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useStoreRegistry when its function contract and declared inputs match the result you need. Choose a different public symbol when useStoreRegistry does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreRegistry."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useStoreRegistry."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider.d.ts:useStoreRegistry:export declare function useStoreRegistry(): import(\"./provider-registry.js\").StoreRegistry;",
          "guidance": [
            {
              "id": "3-12-usestoreconfig-usestorefeature-path-useassertstorefeature-path-apiname",
              "heading": "3.12 useStoreConfig() / useStoreFeature(path) / useAssertStoreFeature(path, apiName?)",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function useStoreConfig(): IStoreConfigValue | null;\nfunction useStoreFeature(path: 'wasm' | `experimental.${string}`): boolean;\nfunction useAssertStoreFeature(path: 'wasm' | `experimental.${string}`, apiName?: string): void;"
                },
                {
                  "type": "list",
                  "items": [
                    "`useStoreConfig`：拿完整归一化配置快照；无 Provider 时返回 `null`（这一点与 `useStoreRegistry` 不同——配置是可选的，Registry 不是）。",
                    "`useStoreFeature`：查询单个特性是否开启；无 Provider 一律返回 `false`，不抛，适合写\"有就用、没有就降级\"的可选增强代码。",
                    "`useAssertStoreFeature`：断言版本，供实验性/前置依赖初始化的 API 在入口处调用。未开启（含\"根本没有 Provider\"这种情况）都会抛 `Error`，因为实验特性不允许\"默默在树外半开着生效\"。"
                  ]
                }
              ]
            },
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 84,
          "configuration": [],
          "fragment": "store-react-index--useStoreRegistry"
        },
        {
          "name": "useStoreResource",
          "kind": "function",
          "signature": "export declare function useStoreResource<T>(resource: IStoreResource<T>): T;",
          "declarationLine": 4,
          "source": "packages/store-react/dist/useStore.d.ts",
          "purpose": "useStoreResource is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreResource.",
          "advanced": "No additional advanced behavior is declared for useStoreResource.",
          "parameters": ["resource: IStoreResource<T>"],
          "parameterDetails": [
            {
              "name": "resource",
              "type": "IStoreResource<T>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useStoreResource."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useStoreResource.",
          "examples": ["const value = useStoreResource(store.someAsyncField);"],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useStoreResource<T>(resource: IStoreResource<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "resource",
                "type": "IStoreResource<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useStoreResource when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useStoreResource does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useStoreResource is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useStoreResource from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useStoreResource when its function contract and declared inputs match the result you need. Choose a different public symbol when useStoreResource does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreResource."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useStoreResource."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useStore.d.ts:useStoreResource:export declare function useStoreResource<T>(resource: IStoreResource<T>): T;",
          "guidance": [],
          "usageScore": 44,
          "configuration": [],
          "fragment": "store-react-index--useStoreResource"
        },
        {
          "name": "useStoreRuntime",
          "kind": "function",
          "signature": "export declare function useStoreRuntime(): IRuntime;",
          "declarationLine": 11,
          "source": "packages/store-react/dist/provider.d.ts",
          "purpose": "useStoreRuntime is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreRuntime.",
          "advanced": "No additional advanced behavior is declared for useStoreRuntime.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IRuntime",
          "errors": ["No documented errors are declared for useStoreRuntime."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useStoreRuntime.",
          "examples": [
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport type { IReactiveStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\nimport type { IRuntime } from '@migaia/reactive';\n\ntype ISession = { userId: string; setUserId(id: string): void };\nconst sessionToken = createStoreToken<IReactiveStore<ISession>>('session');\n\nfunction restoreSession(): Promise<void> {\n  // 例如从 localStorage/远端恢复登录态；调用方负责把它 memo 住。\n  return Promise.resolve();\n}\n\nfunction App() {\n  // ready 数组的引用必须稳定，否则每次渲染都会命中 §2.3 的身份检查警告。\n  const ready = useMemo(() => [restoreSession], []);\n  const sessionStore = useMemo(\n    () =>\n      createStore<ISession>({\n        userId: '',\n        setUserId(id) {\n          this.userId = id;\n        }\n      }),\n    []\n  );\n\n  return (\n    <StoreProvider config={{ ready, fallback: <FullPageSpinner /> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<FullPageSpinner />}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\n// 用 register 而非全局单例：不同的 Provider 子树可以注入不同的 session 实例（比如测试）。\nfunction SessionInjector({\n  store,\n  children\n}: {\n  store: IReactiveStore<ISession>;\n  children: React.ReactNode;\n}) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return <>{children}</>;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const experimentalCharts = useStoreFeature('experimental.charts');\n  return (\n    <div>\n      <p>当前用户：{userId || '未登录'}</p>\n      {experimentalCharts ? <ChartsPanel userId={userId} /> : null}\n    </div>\n  );\n}\n\nconst profileResources = new Map<string, Resource<{ name: string }>>();\nfunction profileResource(userId: string, runtime: IRuntime): Resource<{ name: string }> {\n  let resource = profileResources.get(userId);\n  if (!resource) {\n    resource = new Resource(\n      async ({ signal }) => {\n        const res = await fetch(`/api/profile/${userId}`, { signal });\n        return res.json() as Promise<{ name: string }>;\n      },\n      runtime,\n      { debugName: `profile:${userId}` }\n    );\n    profileResources.set(userId, resource);\n  }\n  return resource;\n}\n\nfunction ChartsPanel({ userId }: { userId: string }) {\n  const runtime = useStoreRuntime();\n  // profileResource 按 userId 缓存同一个 Resource 实例，避免每次渲染都创建新对象（见 §7.6）。\n  const profile = useResourceValue(profileResource(userId, runtime)); // 未就绪时 throw promise，交给外层 Suspense\n  return <div>欢迎，{profile.name}</div>;\n}\n\nfunction FullPageSpinner() {\n  return <div role=\"status\">加载中…</div>;\n}",
            "const registry = useStoreRegistry();\nconst runtime = useStoreRuntime(); // = useStoreRegistry().runtime",
            "import { Suspense, useEffect, useMemo } from 'react';\nimport {\n  StoreProvider,\n  createStoreToken,\n  useProvidedStore,\n  useResourceValue,\n  useStoreFeature,\n  useStoreRegistry,\n  useStoreRuntime\n} from '@migaia/store-react';\nimport { createStore } from '@migaia/store-light';\nimport { Resource } from '@migaia/resource';\n\nconst sessionToken = createStoreToken('session');\n\nfunction App() {\n  const ready = useMemo(() => [() => Promise.resolve()], []); // 引用必须稳定\n  const sessionStore = useMemo(() => createStore({ userId: '' }), []);\n\n  return (\n    <StoreProvider config={{ ready, fallback: <div>加载中…</div> }}>\n      <SessionInjector store={sessionStore}>\n        <Suspense fallback={<div>加载中…</div>}>\n          <Dashboard />\n        </Suspense>\n      </SessionInjector>\n    </StoreProvider>\n  );\n}\n\nfunction SessionInjector({ store, children }) {\n  const registry = useStoreRegistry();\n  useEffect(() => registry.register(sessionToken, store, { owned: false }), [registry, store]);\n  return children;\n}\n\nfunction Dashboard() {\n  const userId = useProvidedStore(sessionToken, (s) => s.userId);\n  const runtime = useStoreRuntime();\n  const showCharts = useStoreFeature('experimental.charts');\n  const profile = useResourceValue(new Resource(async () => ({ name: userId }), runtime));\n  return (\n    <p>\n      {profile.name}\n      {showCharts ? ' · 图表已开启' : ''}\n    </p>\n  );\n}"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": ["export declare function useStoreRuntime(): IRuntime;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IRuntime"],
          "whenToUse": "Use useStoreRuntime when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useStoreRuntime does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useStoreRuntime is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useStoreRuntime from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useStoreRuntime when its function contract and declared inputs match the result you need. Choose a different public symbol when useStoreRuntime does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useStoreRuntime."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useStoreRuntime."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/provider.d.ts:useStoreRuntime:export declare function useStoreRuntime(): IRuntime;",
          "guidance": [
            {
              "id": "5-错误处理完整参考",
              "heading": "5. 错误处理完整参考",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "所有本包错误都带 `source: '@migaia/store-react'` 与稳定 `code`："
                },
                {
                  "type": "paragraph",
                  "text": "根入口同时导出 `STORE_REACT_SOURCE`、`createStoreReactError(code, message, options?)` 与 `createStoreReactAggregateError(code, errors, message)`，供与本包契约集成的 adapter 保留一致错误身份。普通组件只需捕获 hook/Registry 抛出的错误；不要为了改写 message 再包装一次。工厂保留传入的 native error/aggregate 语义和 cause 链，错误分支应比较 `STORE_REACT_SOURCE` 与 `StoreReactErrorCode`。"
                },
                {
                  "type": "table",
                  "headers": ["`StoreReactErrorCode`", "码值", "触发条件 / 处理"],
                  "rows": [
                    [
                      "`registryDisposed`",
                      "`REGISTRY_DISPOSED`",
                      "Registry 终结后继续读写；创建新 Registry"
                    ],
                    [
                      "`registryDisposalFailed`",
                      "`REGISTRY_DISPOSAL_FAILED`",
                      "owned 实体释放失败；展开 `AggregateError.errors`"
                    ],
                    [
                      "`providerRequired`",
                      "`PROVIDER_REQUIRED`",
                      "Provider-only hook 缺少 `StoreProvider`；补 Provider"
                    ],
                    [
                      "`featureDisabled`",
                      "`FEATURE_DISABLED`",
                      "所需 feature 未显式开启；修正 `config.features`"
                    ],
                    ["`storeMissing`", "`STORE_MISSING`", "token 尚未注册；修正注册顺序或 token"],
                    [
                      "`storeDuplicate`",
                      "`STORE_DUPLICATE`",
                      "同 token 重复注册；改用 `replace()` 或拆分 Registry"
                    ],
                    [
                      "`crossRuntime`",
                      "`CROSS_RUNTIME`",
                      "Store 与 Registry 不属于同一 Runtime；统一所有权"
                    ],
                    [
                      "`invalidConfig`",
                      "`INVALID_CONFIG`",
                      "token/Provider/features 配置非法；按 message 修正"
                    ],
                    [
                      "`readyRejected`",
                      "`READY_REJECTED`",
                      "readiness barrier reject；检查保留在 `cause` 的底层失败"
                    ]
                  ]
                },
                {
                  "type": "table",
                  "headers": ["消息", "触发条件", "处理方式"],
                  "rows": [
                    [
                      "`[store] StoreProvider registry/runtime ownership mismatch`",
                      "同时传 `registry` 与 `runtime` 且二者不属于同一 Runtime",
                      "只传其中一个，或保证 `registry.runtime === runtime`"
                    ],
                    [
                      "`[store] StoreProvider ready barriers changed identity; ...`（console.warn/console.error，不抛）",
                      "`config.ready` 数组在挂载后按元素比较发生了变化",
                      "用 `useMemo`/模块级常量稳住 `ready` 数组与元素引用"
                    ],
                    [
                      "`[store] features.wasm is true but config.ready is empty; pass ensureWasm from @migaia/store-wasm (e.g. ready: [ensureWasm])`",
                      "`features.wasm: true` 但没提供 `ready`",
                      "传入 `ready: [ensureWasm]`（来自 `@migaia/store-wasm`）或其它就绪 Promise"
                    ],
                    [
                      "`[store] ready barrier rejected: ...`",
                      "某个 `ready` 屏障 reject 且原因不是 `Error` 实例",
                      "检查具体屏障的失败原因；这个 Error 会从 `ReadyBoundary` 的 render 抛出，需要外层 Error Boundary 捕获"
                    ],
                    [
                      "`[store] hook requires a StoreProvider`",
                      "`useStoreRegistry`/`useStoreRuntime`/`useStoreFromProvider`/`useProvidedStore`/`useAtomDefinition`/`useSetAtomDefinition` 在没有 `StoreProvider` 的树里调用",
                      "在组件树更靠上的位置套一层 `<StoreProvider>`"
                    ],
                    [
                      "`[store] ${apiName} requires a StoreProvider (feature \"X\")`",
                      "`useAssertStoreFeature`/`assertStoreFeature` 在没有 Provider 的树里调用",
                      "套 `StoreProvider`"
                    ],
                    [
                      "`[store] ${apiName} requires feature \"X\" to be explicitly enabled on StoreProvider config`",
                      "有 Provider，但对应 feature 未在 `config.features` 里显式置 `true`",
                      "在 `StoreProvider` 的 `config.features` 里显式开启"
                    ],
                    [
                      "`[store] provider registry is disposed`",
                      "Registry 已 `dispose()`/`disposeAsync()` 后，仍调用 `register`/`replace`/`get`/`require`/`has`/`remove`",
                      "检查是否有代码在组件卸载后仍持有并调用 registry 引用"
                    ],
                    [
                      "`[store] duplicate provider store token: X`",
                      "同一个 `StoreToken` 被 `register` 两次",
                      "改用 `replace`，或确认没有重复挂载注册逻辑"
                    ],
                    [
                      "`[store] missing provider store: X`",
                      "`require(token)`/`useStoreFromProvider(token)` 在对应实例注册之前调用",
                      "确认注册逻辑先于消费者渲染执行；或先用 `has`/`get` 判断"
                    ],
                    [
                      "`[store] provider store \"X\" belongs to a different Runtime`",
                      "注册进 Registry 的值所属 Runtime 与 `registry.runtime` 不同",
                      "用同一个 Runtime 创建要注册的实例，或换一个 Registry"
                    ],
                    [
                      "`[store] StoreToken requires a debug name`",
                      "`createStoreToken('')`",
                      "传一个非空的调试名"
                    ],
                    [
                      "`AggregateError('[store] provider registry disposal failed')`",
                      "`dispose()`/`disposeAsync()` 时多个 owned 实体的释放各自抛错",
                      "展开 `error.errors` 逐个排查；单个实体报错时会直接抛出那个原始 error，不包一层"
                    ]
                  ]
                }
              ]
            }
          ],
          "usageScore": 66,
          "configuration": [],
          "fragment": "store-react-index--useStoreRuntime"
        },
        {
          "name": "useTracked",
          "kind": "function",
          "signature": "export declare function useTracked<T>(read: () => T, isEqual?: (a: T, b: T) => boolean, runtime?: IRuntime): T;",
          "declarationLine": 5,
          "source": "packages/store-react/dist/useStore.d.ts",
          "purpose": "useTracked is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useTracked.",
          "advanced": "No additional advanced behavior is declared for useTracked.",
          "parameters": [
            "read: () => T",
            "isEqual: (a: T, b: T) => boolean (optional)",
            "runtime: IRuntime (optional)"
          ],
          "parameterDetails": [
            {
              "name": "read",
              "type": "() => T",
              "optional": false
            },
            {
              "name": "isEqual",
              "type": "(a: T, b: T) => boolean",
              "optional": true
            },
            {
              "name": "runtime",
              "type": "IRuntime",
              "optional": true
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for useTracked."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for useTracked.",
          "examples": [
            "const total = useTracked(() => priceSignal.value + taxSignal.value);",
            "import { useTracked } from '@migaia/store-react';\nimport { Signal } from '@migaia/reactive';\n\nconst priceSignal = new Signal(10);\nconst qtySignal = new Signal(2);\n\nfunction Total() {\n  const total = useTracked(() => priceSignal.value * qtySignal.value);\n  return <span>合计：{total}</span>;\n}"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function useTracked<T>(read: () => T, isEqual?: (a: T, b: T) => boolean, runtime?: IRuntime): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "read",
                "type": "() => T",
                "optional": false
              },
              {
                "name": "isEqual",
                "type": "(a: T, b: T) => boolean",
                "optional": true
              },
              {
                "name": "runtime",
                "type": "IRuntime",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use useTracked when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when useTracked does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "useTracked is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import useTracked from @migaia/store-react, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use useTracked when its function contract and declared inputs match the result you need. Choose a different public symbol when useTracked does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for useTracked."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for useTracked."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/store-react/dist/useStore.d.ts:useTracked:export declare function useTracked<T>(read: () => T, isEqual?: (a: T, b: T) => boolean, runtime?: IRuntime): T;",
          "guidance": [],
          "usageScore": 98,
          "configuration": [],
          "fragment": "store-react-index--useTracked"
        }
      ],
      "aliases": []
    }
  ]
}
