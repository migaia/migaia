{
  "version": 1,
  "library": {
    "slug": "utils",
    "description": "Runtime-neutral reusable utility primitives.",
    "version": "0.0.4",
    "exports": [
      ".",
      "./bytes",
      "./config",
      "./error",
      "./function",
      "./number",
      "./object",
      "./object-path",
      "./promise",
      "./string",
      "./typing",
      "./value"
    ],
    "documentation": {
      "readme": {
        "sections": [
          {
            "id": "migaia-utils",
            "heading": "@migaia/utils",
            "blocks": [
              {
                "type": "paragraph",
                "text": "面向库代码的、与运行时无关的基础工具集：截止时间控制（deadline）、协作式中止（abort）、错误身份标注、字节/文本编解码、不可变对象路径读写，以及带所有权语义的配置对象。零运行时依赖，`sideEffects: false`。"
              }
            ]
          },
          {
            "id": "适用与不适用场景",
            "heading": "适用与不适用场景",
            "blocks": [
              {
                "type": "paragraph",
                "text": "适用：某个包需要一个体量小、宿主无关、并且明确暴露失败/调度/所有权语义的基础原语。典型场景：在截止时间内等待、协议边界上的规范 Base64、只读配置快照、不可变的嵌套字段写入、跨值/跨错误的因果链遍历。"
              },
              {
                "type": "paragraph",
                "text": "不适用：不要把它当作应用框架、事件总线、生命周期宿主、存储层，或强制取消 Promise 的机制。`withTimeout`/`raceWithAbort`/`retry` 只能通过 `signal` 通知协作式代码；如果被中止的代码从不检查 `signal.aborted`，本包无法真正打断它。"
              }
            ]
          },
          {
            "id": "安装",
            "heading": "安装",
            "blocks": [
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm add @migaia/utils"
              }
            ]
          },
          {
            "id": "目录",
            "heading": "目录",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "`/promise`：截止时间、中止、重试、并发限流",
                  "`/error`：错误身份与因果链",
                  "`/bytes`：Base64 与 UTF-8",
                  "`/object`：快照与不可变对象路径",
                  "`/typing`：跨项目复用类型",
                  "`/config`：带所有权语义的配置对象",
                  "`/function`：一次性调用",
                  "Collector：惰性字段筛选流水线",
                  "高阶组合示例",
                  "构建门禁"
                ]
              },
              {
                "type": "paragraph",
                "text": "完整签名、边界行为与错误码，见 USEGUIDE.md。"
              }
            ]
          },
          {
            "id": "promise-模块",
            "heading": "/promise 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  sleep,\n  createAbortTimeoutSignal,\n  withTimeout,\n  raceWithAbort,\n  retry,\n  createConcurrencyLimiter,\n  deferred,\n  toPromise,\n  createManualScheduler,\n  systemScheduler,\n  hostRethrowReporter\n} from '@migaia/utils/promise';"
              },
              {
                "type": "paragraph",
                "text": "`sleep`｜5 秒上手 —— 可中止的延时："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "await sleep(1000); // 1 秒后 resolve\nawait sleep(1000, { signal: controller.signal }); // 中止时 reject UtilsAbortError"
              },
              {
                "type": "paragraph",
                "text": "全部选项（第二参数 `IAsyncControls`）："
              },
              {
                "type": "list",
                "items": [
                  "`signal?: IAbortSignal` —— 与 `signals` 互斥，同时传抛 `TypeError`",
                  "`signals?: readonly IAbortSignal[]` —— 任意一个中止即中止",
                  "`scheduler?: IUtilsScheduler` —— 默认 `systemScheduler`",
                  "`unref?: boolean` —— 定时器不阻塞进程退出（Node）"
                ]
              },
              {
                "type": "paragraph",
                "text": "`createAbortTimeoutSignal`｜5 秒上手 —— 只合并外部 abort 与 deadline signal，不接管任何 Promise 的结算策略："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const merged = createAbortTimeoutSignal({\n  signal: controller.signal,\n  timeoutMs: 500,\n  timeoutReason: () => new Error('deadline')\n});\ntry {\n  await Promise.all([requestA(merged.signal), requestB(merged.signal)]);\n} finally {\n  merged.dispose();\n}"
              },
              {
                "type": "paragraph",
                "text": "返回 `{ signal, dispose }`；`dispose()` 幂等。无 `timeoutMs` 时原样返回外部 signal，不分配 controller。它与 `withTimeout` / `raceWithAbort` 的区别是：后两者拥有最终 Promise 的超时 reject、迟到 rejection 与 cleanup-error 政策，本 API 只拥有 signal composition，适合结算事实必须由底层事务决定的适配器。"
              },
              {
                "type": "paragraph",
                "text": "`withTimeout`｜10 秒上手 —— 给一段惰性操作套上截止时间，操作函数必须读 `signal` 才能提前退出："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "await withTimeout(\n  async ({ signal }) => {\n    if (signal.aborted) throw signal.reason;\n    return fetchData();\n  },\n  { timeoutMs: 500 }\n);"
              },
              {
                "type": "paragraph",
                "text": "全部选项：`IAsyncControls`（同上：`signal`/`signals`/`scheduler`/`unref`）叠加："
              },
              {
                "type": "list",
                "items": [
                  "`timeoutMs: number`（必填）—— 有限非负数",
                  "`report?: IUtilsReporter` —— 结算后才到达的迟到结果如何上报，默认 `hostRethrowReporter`",
                  "`zeroTimeoutBehavior?: 'skip' | 'start'` —— `timeoutMs: 0` 时默认 `'skip'`（立即以超时错误 reject）；`'start'` 改为立即执行、不设超时",
                  "`cooperativeCancellation?: boolean` —— 默认 `true`；`false` 时不分配内部 `AbortController`，操作拿到的是恒不中止的惰性信号"
                ]
              },
              {
                "type": "paragraph",
                "text": "`raceWithAbort`｜5 秒上手 —— 只响应外部中止，不设截止时间："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "await raceWithAbort(({ signal }) => fetch(url, { signal }), { signal: controller.signal });"
              },
              {
                "type": "paragraph",
                "text": "全部选项：`signal?`/`signals?`（互斥，语义同上）叠加："
              },
              {
                "type": "list",
                "items": [
                  "`report?: IUtilsReporter` —— 默认 `hostRethrowReporter`",
                  "`cleanupPolicy?: 'reject' | 'report'` —— 默认 `'reject'`；`'report'` 时监听器清理失败改为上报而不是让结果 reject"
                ]
              },
              {
                "type": "paragraph",
                "text": "`retry`｜10 秒上手 —— 带最大次数、可选退避、可选总/单次超时的串行重试："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "await retry(async () => mayFail(), {\n  maxAttempts: 3,\n  shouldRetry: () => true,\n  delay: (_, { attempt }) => attempt * 100\n});"
              },
              {
                "type": "paragraph",
                "text": "全部选项："
              },
              {
                "type": "list",
                "items": [
                  "`maxAttempts: number`（必填）—— 正安全整数",
                  "`shouldRetry: (error, { attempt, maxAttempts, signal }) => boolean | Promise<boolean>`（必填）",
                  "`delay?: number | ((error, context) => number)` —— 两次尝试间的退避毫秒数，默认 0",
                  "`totalTimeoutMs?: number` —— 整个重试过程（含退避等待）的总截止时间",
                  "`attemptTimeoutMs?: number` —— 每次尝试各自的超时",
                  "`signal?` / `signals?` —— 互斥，随时中止整个重试",
                  "`zeroTimeoutBehavior?: 'skip' | 'start'` —— 透传给内部 `withTimeout`",
                  "`report?: IUtilsReporter`",
                  "`unref?: boolean`",
                  "`scheduler?: IUtilsScheduler`"
                ]
              },
              {
                "type": "paragraph",
                "text": "`createConcurrencyLimiter`｜10 秒上手 —— 限制同时执行的任务数："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const limiter = createConcurrencyLimiter({ concurrency: 2 });\nawait Promise.all(urls.map((u) => limiter.run(() => fetch(u))));\nawait limiter.dispose(); // 等待在途任务结束后关闭"
              },
              {
                "type": "paragraph",
                "text": "构造选项：`concurrency: number`（必填，正安全整数）、`report?: IUtilsReporter`。 `run(task, options?)` 的 `options.signal?: IAbortSignal` —— 排队中的任务可被单独中止（已开始执行的任务需自行读取 `context.signal`）。 返回对象上的方法：`activeCount`/`pendingCount`（只读）、`whenIdle()`、`close(reason?)`、`dispose(reason?)`。"
              },
              {
                "type": "paragraph",
                "text": "`deferred`｜兼容性工具 —— 拿到可从外部结算的 Promise："
              },
              {
                "type": "paragraph",
                "text": "如果目标运行环境支持 `Promise.withResolvers()`，优先使用原生 API：语义相同、认知成本更低，也不需要额外引入 `deferred`。只有需要兼容尚未提供 `Promise.withResolvers()` 的运行时，或项目必须统一使用本包返回类型时，才选择 `deferred()`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const { promise, resolve, reject } = Promise.withResolvers<number>();\nresolve(42);"
              },
              {
                "type": "paragraph",
                "text": "兼容旧运行时："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const { promise, resolve } = deferred<number>();\nresolve(42);"
              },
              {
                "type": "paragraph",
                "text": "无选项；返回 `{ promise, resolve, reject }`。"
              },
              {
                "type": "paragraph",
                "text": "`toPromise`｜3 秒上手 —— 立即执行同步计算，并把返回值或同步异常统一成 Promise settlement："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const value = await toPromise(() => cache.get('key'));\nawait toPromise(() => {\n  throw new Error('failed'); // 不会从 toPromise 调用点同步抛出，而是返回 rejected Promise\n});"
              },
              {
                "type": "paragraph",
                "text": "`run` 同步且恰好执行一次；普通值和 thenable 交给原生 `Promise.resolve` 同化，原生 Promise 保持 identity。它不提供 timeout、retry 或 cancellation。"
              },
              {
                "type": "paragraph",
                "text": "它与 `Promise.resolve().then(() => run())` 的差别在执行时机和 Promise identity："
              },
              {
                "type": "list",
                "items": [
                  "`toPromise(run)` 在当前调用栈立即执行 `run`，函数返回前副作用已经发生；如果 `run` 返回原生 Promise，会直接返回同一个 Promise。",
                  "`Promise.resolve().then(run)` 等到下一次 microtask 才执行 `run`，可用于主动打断当前调用栈或避免同步重入；它始终创建一个新的链式 Promise。",
                  "两者都把 `run` 抛出的异常表现为 rejection。`toPromise` 在当前调用过程中捕获同步异常并返回已拒绝的 Promise；`.then(run)` 则在后续 microtask 执行 `run` 时产生 rejection。"
                ]
              },
              {
                "type": "paragraph",
                "text": "需要“现在执行，但统一以 Promise 返回”时选 `toPromise`；需要“稍后执行，让出当前调用栈”时选 `Promise.resolve().then(run)`。"
              },
              {
                "type": "paragraph",
                "text": "`createManualScheduler`｜本仓测试专用 —— 单测里把时间变成确定性的："
              },
              {
                "type": "paragraph",
                "text": "该工具为本仓测试与适配器验证提供确定性时钟，不面向外部业务代码。外部项目应优先使用所属测试框架提供的 fake timers；不要在生产流程中依赖 `advance()` 或 `pendingCount`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scheduler = createManualScheduler();\nconst p = withTimeout(op, { timeoutMs: 100, scheduler });\nscheduler.advance(100); // 手动触发超时，无需真实等待"
              },
              {
                "type": "paragraph",
                "text": "无入参；返回对象额外暴露 `advance(ms)`、只读 `pendingCount`（标准 `IUtilsScheduler` 的 `now()`/`schedule()` 也都有）。"
              },
              {
                "type": "paragraph",
                "text": "`systemScheduler`｜3 秒上手 —— 基于原生定时器的默认调度器，一般不用手动传，除非要替换成 `createManualScheduler()`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const now = systemScheduler.now();"
              },
              {
                "type": "paragraph",
                "text": "`systemScheduler.now()` 当前直接读取 `Date.now()`，所以两者返回的都是 Unix 时间戳，数值与精度没有区别。区别在调用边界：`systemScheduler` 把 `now()` 与 `schedule()` 放在同一个 `IUtilsScheduler` 中，使用方可以在测试时把它们一起替换成虚拟时钟。"
              },
              {
                "type": "paragraph",
                "text": "只需要读取真实墙上时间的普通业务代码，直接使用 `Date.now()` 即可。正在实现接受 `IUtilsScheduler` 的超时、重试或调度逻辑时，应始终配对使用 `scheduler.now()` 与 `scheduler.schedule()`；如果其中一处改用 `Date.now()`，就会绕过注入的调度器，导致测试同时混用真实时间和虚拟定时器，无法再确定性推进。"
              },
              {
                "type": "paragraph",
                "text": "无配置，是一个现成的 `IUtilsScheduler` 常量。"
              },
              {
                "type": "paragraph",
                "text": "`hostRethrowReporter`｜3 秒上手 —— 默认诊断上报器，把迟到的错误重新抛给宿主而不是吞掉；一般无需调用，作为 `report` 选项的默认值："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "withTimeout(op, { timeoutMs: 100, report: myLogger }); // 用自定义 reporter 替换默认行为"
              },
              {
                "type": "paragraph",
                "text": "签名固定：`(error: unknown, context: { operation, phase, attempt? }) => void`，自定义 reporter 需匹配同一签名。"
              }
            ]
          },
          {
            "id": "error-模块",
            "heading": "/error 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
              },
              {
                "type": "paragraph",
                "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
              },
              {
                "type": "paragraph",
                "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
              },
              {
                "type": "paragraph",
                "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "catch (value) { const error = toError(value); }"
              },
              {
                "type": "paragraph",
                "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
              },
              {
                "type": "paragraph",
                "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
              },
              {
                "type": "paragraph",
                "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
              },
              {
                "type": "paragraph",
                "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
              },
              {
                "type": "paragraph",
                "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
              },
              {
                "type": "paragraph",
                "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
              },
              {
                "type": "paragraph",
                "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
              },
              {
                "type": "paragraph",
                "text": "全部取值及其含义："
              },
              {
                "type": "list",
                "items": [
                  "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                  "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                  "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                  "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                  "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                  "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                  "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                  "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                  "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                  "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                  "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                  "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                  "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                  "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                  "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                  "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                  "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                  "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                  "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                  "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                ]
              },
              {
                "type": "paragraph",
                "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
              }
            ]
          },
          {
            "id": "bytes-模块",
            "heading": "/bytes 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  isArrayBuffer,\n  isUint8Array,\n  bytesToBase64,\n  base64ToBytes,\n  streamBase64Chunks,\n  utf8ByteLength,\n  encodeUtf8,\n  decodeUtf8,\n  splitUtf8\n} from '@migaia/utils/bytes';"
              },
              {
                "type": "paragraph",
                "text": "在使用本模块前，先区分底层存储与字节视图："
              },
              {
                "type": "list",
                "items": [
                  "`ArrayBuffer` 是一段固定长度的原始连续内存，只表示“这些字节存在哪里”。它本身不能按下标读写具体字节，通常来自 `fetch(...).arrayBuffer()`、文件读取、Web Crypto、WebAssembly 或 iframe/Worker 消息传输。",
                  "`Uint8Array` 是覆盖在 `ArrayBuffer` 上的 8 位无符号整数视图，表示“怎样把这段内存按 0～255 的单字节读写”。编码、Base64、网络协议帧和二进制序列化通常使用它，因为这些操作需要逐字节访问。",
                  "`new Uint8Array(buffer, byteOffset, length)` 默认不会复制数据；它可以只查看 buffer 的一个窗口，写入视图也会改变同一底层 `ArrayBuffer`。需要独立副本时使用 `slice()`，只需共享窗口时使用 `subarray()`。",
                  "`ArrayBuffer` 通过 Worker 等边界转移后可能被 detached，原发送方不再拥有可读字节；`SharedArrayBuffer` 则用于多线程共享内存，生命周期和并发语义不同，因此本模块不会把它当作普通 `ArrayBuffer`。"
                ]
              },
              {
                "type": "paragraph",
                "text": "`isUint8Array` / `isArrayBuffer`｜3 秒上手 —— 基于 ECMAScript 内部槽做跨 realm 品牌检测，不信任可篡改的 `constructor.name`、原型或 `Symbol.toStringTag`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "isUint8Array(new Uint8Array()); // true\nisArrayBuffer(new ArrayBuffer(1)); // true\nisUint8Array(new Uint8ClampedArray(1)); // false\nisArrayBuffer(new SharedArrayBuffer(1)); // false"
              },
              {
                "type": "paragraph",
                "text": "两者接受合法的跨 iframe/Worker 值和子类，拒绝 Proxy 与外形伪造对象。detached `Uint8Array` / `ArrayBuffer` 仍保留自身品牌；调用方必须在读取字节前另行判断可用性。"
              },
              {
                "type": "paragraph",
                "text": "`bytesToBase64` / `base64ToBytes`｜3 秒上手（单参数，无选项）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "bytesToBase64(new Uint8Array([1, 2, 3])); // 'AQID'\nbase64ToBytes('AQID'); // Uint8Array [1, 2, 3]（只接受规范 RFC 4648 形式，非规范输入抛 TypeError）"
              },
              {
                "type": "paragraph",
                "text": "`streamBase64Chunks`｜5 秒上手 —— 大字节数组切成多段、每段各自独立合法的 Base64："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "for (const chunk of streamBase64Chunks(bigBytes, 4096)) send(chunk);"
              },
              {
                "type": "paragraph",
                "text": "第二参数 `maxChunkBytes?: number`（默认 `32763`）—— 必须是 `>= 3` 的安全整数，实际切分宽度向下取整到 3 的倍数。"
              },
              {
                "type": "paragraph",
                "text": "`utf8ByteLength`｜3 秒上手 —— 不分配缓冲区地算 UTF-8 字节数（单参数，无选项）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "utf8ByteLength('你好'); // 6"
              },
              {
                "type": "paragraph",
                "text": "`encodeUtf8`｜3 秒上手（单参数，无选项）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const bytes = encodeUtf8('你好');"
              },
              {
                "type": "paragraph",
                "text": "`decodeUtf8`｜3 秒上手："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "decodeUtf8(bytes); // '你好'\ndecodeUtf8(badBytes, { fatal: true }); // 非法字节序列时抛错，而不是替换成 U+FFFD"
              },
              {
                "type": "paragraph",
                "text": "第二参数选项：`{ fatal?: boolean }`（默认 `false`）。"
              },
              {
                "type": "paragraph",
                "text": "`splitUtf8`｜5 秒上手 —— 按字节上限切分字符串，绝不切断码点："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "splitUtf8('你好世界', 6); // ['你好', '世界']"
              },
              {
                "type": "paragraph",
                "text": "第二参数 `maxBytes: number`（必填）—— 必须是 `>= 4` 的安全整数（单码点最大 UTF-8 宽度为 4）。"
              }
            ]
          },
          {
            "id": "object-模块",
            "heading": "/object 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  isPlainObject,\n  probeProperty,\n  immutableSnapshot,\n  diagnosticSnapshot,\n  structuredDiagnosticSnapshot,\n  identitySnapshot,\n  get,\n  set,\n  parseObjectPath,\n  probeObjectPath,\n  createPathAccessor\n} from '@migaia/utils/object';"
              },
              {
                "type": "paragraph",
                "text": "`isPlainObject`｜3 秒上手（单参数，无选项）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "isPlainObject({}); // true\nisPlainObject([]); // false\nisPlainObject(new Date()); // false"
              },
              {
                "type": "paragraph",
                "text": "`probeProperty`｜5 秒上手 —— 读一次属性，getter 抛错时不吞掉："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const profileUnavailable = new Error('profile unavailable');\nlet profileReads = 0;\nconst user = {\n  name: 'Ada',\n  get profile(): never {\n    profileReads += 1;\n    throw profileUnavailable;\n  }\n};\n\nfunction readLabel(value: object, key: PropertyKey): string {\n  const result = probeProperty<string>(value, key);\n  switch (result.kind) {\n    case 'value':\n      return result.value;\n    case 'missing':\n      return 'Anonymous';\n    case 'failed':\n      console.error('property read failed', result.error);\n      return 'Unavailable';\n  }\n}\n\nreadLabel(user, 'name'); // 'Ada'：读取成功，消费 value\nreadLabel(user, 'email'); // 'Anonymous'：字段不存在，走业务默认值\nreadLabel(user, 'profile'); // 'Unavailable'：getter 异常被显式上报\nprofileReads; // 1：probeProperty 没有为了判断结果而重复触发 getter"
              },
              {
                "type": "paragraph",
                "text": "这里的 `user` 就是第一个参数 `value`：可以是普通对象、class 实例或其他可读取属性的对象；第二参数是要读取的字符串、数字或 Symbol key。`probeProperty()` 把“字段不存在”和“读取字段失败”分开，调用方不会把 getter 异常误当成缺省值；它也只读取属性一次，避免先判断再读取导致 getter 重复执行。参数：`value: object`（必填）、`key: PropertyKey`（必填），无可选项。"
              },
              {
                "type": "paragraph",
                "text": "在使用 `immutableSnapshot()` 前，先理解它所说的“快照”："
              },
              {
                "type": "list",
                "items": [
                  "它直接调用当前宿主的原生 `globalThis.structuredClone()`，按结构化克隆算法一次复制整张对象图，不经过 JSON 字符串。因此循环引用和“多个字段指向同一对象”的共享关系可以保留，`Date`、`Map`、`Set`、`ArrayBuffer`、TypedArray 等常见结构也能保留对应数据类型。",
                  "它不会保留自定义 class 的原型方法、getter/setter 或属性描述符；函数、WeakMap 等不可结构化克隆的值会让整个操作失败。这里没有传 transfer list，输入中的 `ArrayBuffer` 会复制，不会因调用而 detached。",
                  "“快照”表示返回值与源对象断开引用，源对象之后的修改不会回写到副本；它并不调用 `Object.freeze()`，所以返回对象本身仍可修改。需要只读约束时，应在类型或调用边界另行施加。",
                  "适合隔离配置、消息负载或测试夹具的某一时刻状态；需要保留 class 行为、函数或精确属性描述符时不要使用。"
                ]
              },
              {
                "type": "paragraph",
                "text": "`immutableSnapshot`｜3 秒上手 —— 基于宿主原生结构化克隆算法的深拷贝（单参数，无选项；不支持时抛 `ENV_UNSUPPORTED`/`CLONE_UNSUPPORTED`）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const shared = { count: 1 };\nconst source = {\n  primary: shared,\n  alias: shared,\n  createdAt: new Date('2026-01-01T00:00:00Z'),\n  labels: new Map([['lang', 'zh']])\n};\nconst copy = immutableSnapshot(source);\n\nsource.primary.count = 2;\ncopy.primary.count; // 1：源对象修改不会回写快照\ncopy.primary === copy.alias; // true：共享引用关系被保留\ncopy.createdAt instanceof Date; // true：不是 JSON 字符串\nObject.isFrozen(copy); // false：独立副本不等于冻结对象"
              },
              {
                "type": "paragraph",
                "text": "三种复制入口的选择规则："
              },
              {
                "type": "list",
                "items": [
                  "原生 `structuredClone(value)`：全有或全无。整张对象图都受支持才返回副本；任一成员不可克隆就抛宿主原生异常，不提供失败路径。",
                  "`immutableSnapshot(value)`：同样全有或全无，内部直接调用原生 `structuredClone`；区别是把环境缺失和克隆失败转换为稳定的 `ENV_UNSUPPORTED` / `CLONE_UNSUPPORTED` 错误契约。",
                  "`diagnosticSnapshot(value)`：不先克隆整张对象图，而是递归处理普通对象和数组；可克隆的内建子树独立复制，函数、class 实例等不支持的叶子保留原引用，并在 `diagnostics` 中记录具体 `path`。它优先保证“仍返回一个可检查结果”，不保证所有层都与源对象隔离。",
                  "`structuredDiagnosticSnapshot(value)`：先尝试原生 `structuredClone`；成功时得到完整隔离副本且 `diagnostics` 为空，失败时再退回 `diagnosticSnapshot` 的逐节点策略，并额外记录根级克隆失败。适合既希望完整克隆、又不能因单个坏字段丢失整份遥测数据的边界。"
                ]
              },
              {
                "type": "paragraph",
                "text": "`diagnosticSnapshot`｜5 秒上手 —— 尽力而为深拷贝，同时报告哪里没拷成功（单参数，无选项）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const callback = () => 1;\nconst source = {\n  safe: { count: 1 },\n  callback,\n  get status() {\n    return 'ready';\n  }\n};\nconst { value, diagnostics } = diagnosticSnapshot(source);\n\nvalue.safe !== source.safe; // true：普通对象已复制\nvalue.callback === callback; // true：不支持的函数保留原引用\nvalue.status; // 'ready'：accessor 被读取并投影为普通值\ndiagnostics;\n// [\n//   { path: ['callback'], reason: 'unsupported', cause: callback },\n//   { path: ['status'], reason: 'accessor', cause: 'ready' }\n// ]"
              },
              {
                "type": "paragraph",
                "text": "`reason` 取值为 `accessor`、`read-failed` 或 `unsupported`。只要 `diagnostics` 非空，就不能把返回值宣称为完全隔离快照；调用方应根据路径决定删除、替换还是接受相应引用。"
              },
              {
                "type": "paragraph",
                "text": "`identitySnapshot`｜何时才有用 —— 单独调用它没有运行时收益；它只适合作为可配置 snapshot policy 的“零拷贝”分支，明确表示调用方接受共享引用："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type ISnapshotPolicy<T> = (value: T) => T;\n\nfunction capture<T>(value: T, snapshot: ISnapshotPolicy<T>): T {\n  return snapshot(value);\n}\n\nconst liveConfig = { retry: 2 };\nconst retained = capture(liveConfig, identitySnapshot);\n\nliveConfig.retry = 3;\nretained.retry; // 3：两者是同一对象，后续修改彼此可见"
              },
              {
                "type": "paragraph",
                "text": "这个策略适合可信进程内、身份敏感或不可克隆对象，并且调用方明确接受共享所有权的场景。若需要隔离外部输入、保留历史状态或跨边界传输，应使用 `immutableSnapshot`；不要为了“看起来调用过 snapshot”而单独套一层 `identitySnapshot(value)`。"
              },
              {
                "type": "paragraph",
                "text": "`get` / `set`｜10 秒上手 —— 以 immutable update 方式进行路径读写："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const data = {\n  user: { name: 'Ada' },\n  settings: { theme: 'dark' }\n};\nget(data, 'user.name'); // 'Ada'：只读，不修改 data\n\nconst next = set(data, 'user.name', 'Grace');\nnext !== data; // true：值发生变化时，每次 set 都返回新根对象\nnext.user !== data.user; // true：路径上的对象被浅拷贝\nnext.settings === data.settings; // true：未修改分支继续共享引用\ndata.user.name; // 'Ada'：原对象保持不变\nObject.isFrozen(next); // false：immutable update 不等于 freeze"
              },
              {
                "type": "paragraph",
                "text": "这里的 “immutable” 描述的是更新方式，不是对象状态：`set()` 不会修改传入对象，而是返回可继续修改的新对象；它不会深拷贝整棵树，也不会调用 `Object.freeze()`。唯一例外是新旧值经 `Object.is()` 相等时没有实际变化，此时直接返回原根对象。"
              },
              {
                "type": "paragraph",
                "text": "参数：`object: T`（必填）、`path: 字符串路径 | 元组路径`（必填，`get`/`set` 均支持两种形式）；`set` 额外要求 `nextValue`（必填，写入类型经字面量放宽）。均无可选项。"
              },
              {
                "type": "paragraph",
                "text": "`parseObjectPath` / `probeObjectPath`｜5 秒上手 —— 需要精确诊断时用探测版："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "parseObjectPath('user.tags.[0]'); // ['user', 'tags', 0]\nprobeObjectPath(data, 'user.x.y'); // { kind: 'missing' | 'blocked' | 'failed' | 'value', ... }"
              },
              {
                "type": "paragraph",
                "text": "均无可选项。限制：单段最长 512 字符、路径最多 256 段、字符串路径总长上限 4096 字符，`proto`/`prototype`/`constructor` 一律拒绝。"
              },
              {
                "type": "paragraph",
                "text": "`createPathAccessor`｜10 秒上手 —— 有状态访问器，写后自动前进到新根："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const accessor = createPathAccessor({ count: 0 });\naccessor.set('count', 5);\naccessor.value; // { count: 5 }"
              },
              {
                "type": "paragraph",
                "text": "第二参数 `IPathAccessorOptions<T>` 全部字段："
              },
              {
                "type": "list",
                "items": [
                  "`ifMissing?: (probe) => void` —— 路径某段不存在时触发",
                  "`ifBlocked?: (probe) => void` —— 路径中途遇到非对象值时触发",
                  "`ifFailed?: (probe) => void` —— 某段读取本身抛错时触发",
                  "`onGet?: (event) => void` —— 每次成功 `get()` 时触发，`event.replace(value)` 可改写返回值",
                  "`onSet?: (event) => void` —— 每次成功 `set()` 时触发，`event.replace(value)` 可改写实际写入值"
                ]
              },
              {
                "type": "paragraph",
                "text": "返回的访问器方法：`get(path)`、`set(path, value)`、`probeValue(path)`、`parsePath(path)`、只读 `value`。"
              }
            ]
          },
          {
            "id": "typing-模块",
            "heading": "/typing 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
              },
              {
                "type": "paragraph",
                "text": "`/typing` 是纯 type-only 子入口（不含任何运行时导出，`import type` 即可，打包后不会产生任何 JS 代码），且只能通过这个子路径导入——不经过根入口 `@migaia/utils` 重新导出。"
              },
              {
                "type": "paragraph",
                "text": "它做两件事：把散落在 `/object`（`IObjectPath`/`IObjectPathInput`/`IObjectPathSegment`/`IObjectPathTuple`/`IObjectPathTupleFor`/`IObjectPathValue`/`IObjectPathWriteValue`/`IProbePropertyResult`）与 `/promise`（`IAbortSignal`/`IDeferred`）里的类型汇总到一个跨项目复用的稳定入口；再提供两个 `/object` 里没有的全新类型工具，用来把带判别字段的联合类型（discriminated union）转成按判别值分组的映射类型。"
              },
              {
                "type": "paragraph",
                "text": "`IDiscriminatedByField`｜5 秒上手 —— 按顶层判别字段分组："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IEvent =\n  | { readonly type: 'created'; readonly payload: { readonly userId: string } }\n  | { readonly type: 'deleted'; readonly payload: { readonly reason: string } };\n\ntype IByType = IDiscriminatedByField<'type', IEvent>;\n// { created: Extract<IEvent, { type: 'created' }>; deleted: Extract<IEvent, { type: 'deleted' }> }"
              },
              {
                "type": "paragraph",
                "text": "类型参数：`F extends PropertyKey`（必填，判别字段名）、`T extends Record<F, PropertyKey>`（必填，联合类型本身；`F` 对应的字段值必须是 `PropertyKey`，即 `string`/`number`/`symbol`，否则编译期报错）。"
              },
              {
                "type": "paragraph",
                "text": "`IDiscriminatedByPath`｜5 秒上手 —— 按深层路径分组，路径写法与 `/object` 模块的对象路径完全一致（字符串路径或元组路径）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IEvent =\n  | { readonly meta: { readonly category: 'write' }; readonly payload: { readonly userId: string } }\n  | { readonly meta: { readonly category: 'read' }; readonly payload: { readonly cache: boolean } };\n\ntype IByCategory = IDiscriminatedByPath<IEvent, 'meta.category'>;\n// 等价于 IDiscriminatedByPath<IEvent, readonly ['meta', 'category']>"
              },
              {
                "type": "paragraph",
                "text": "类型参数：`T`（必填，联合类型本身）、`P extends IObjectPathInput<T>`（必填，字符串或元组路径；路径必须在联合的每个分支上都存在且合法，否则该分支被排除；路径值解析不出 `PropertyKey` 时整体报编译错误）。"
              },
              {
                "type": "paragraph",
                "text": "其余重导出的类型（`IObjectPath`、`IObjectPathInput`、`IObjectPathSegment`、`IObjectPathTuple`、`IObjectPathTupleFor`、`IObjectPathValue`、`IObjectPathWriteValue`、`IProbePropertyResult`、`IAbortSignal`、`IDeferred`）语义与 `/object`、`/promise` 模块中完全一致，仅为了方便跨包复用类型而在这里再导出一份，不是新的类型定义。"
              }
            ]
          },
          {
            "id": "config-模块",
            "heading": "/config 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
              },
              {
                "type": "paragraph",
                "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
              },
              {
                "type": "paragraph",
                "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
              },
              {
                "type": "paragraph",
                "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
              },
              {
                "type": "paragraph",
                "text": "第二参数选项："
              },
              {
                "type": "list",
                "items": [
                  "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                  "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                ]
              },
              {
                "type": "paragraph",
                "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
              },
              {
                "type": "paragraph",
                "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
              },
              {
                "type": "paragraph",
                "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
              },
              {
                "type": "paragraph",
                "text": "第三参数选项："
              },
              {
                "type": "list",
                "items": [
                  "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                  "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                  "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                ]
              },
              {
                "type": "paragraph",
                "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
              },
              {
                "type": "paragraph",
                "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
              },
              {
                "type": "paragraph",
                "text": "`parseConfigPath`｜3 秒上手："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
              },
              {
                "type": "paragraph",
                "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
              },
              {
                "type": "paragraph",
                "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
              },
              {
                "type": "paragraph",
                "text": "第二参数选项："
              },
              {
                "type": "list",
                "items": [
                  "`profile?` / `limits?` —— 同 `ownConfig`",
                  "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                  "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                  "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                ]
              },
              {
                "type": "paragraph",
                "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
              }
            ]
          },
          {
            "id": "function-模块",
            "heading": "/function 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { noop, once, onceAsync } from '@migaia/utils/function';"
              },
              {
                "type": "paragraph",
                "text": "`noop`｜3 秒上手（无参数，无选项）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const handler = options.onChange ?? noop;"
              },
              {
                "type": "paragraph",
                "text": "`once`｜10 秒理解边界 —— 让同一个包装函数实例只执行一次，结果（含异常）被缓存："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "// runtime.ts：整个模块只创建并导出这一份包装函数。\nexport const getRuntime = once(() => {\n  const registry = buildCommandRegistry();\n  return { registry, startedAt: Date.now() };\n});\n\nconst first = getRuntime(); // 创建 registry\nconst second = getRuntime(); // 直接返回 first\nfirst === second; // true"
              },
              {
                "type": "paragraph",
                "text": "单参数 `functionValue`（必填），无其他选项；首次执行未完成时重入会抛 `REENTRANT_CALL`。"
              },
              {
                "type": "paragraph",
                "text": "适合：模块内昂贵且确定的同步初始化，例如构建只读 registry、编译 schema 或探测一次运行时能力。第一次调用抛出的异常也会永久缓存，因此不适合需要重试、重置、热更新或按请求隔离的初始化。"
              },
              {
                "type": "paragraph",
                "text": "它不是全局单例管理器。每调用一次 `once(fn)` 都会创建一份独立缓存；同一包被重复打包，或代码运行在 iframe、Worker、Node `vm` 等不同 realm 时，也会各自初始化。把唯一的包装函数放在模块顶层并导出，只能得到“每个模块实例一份”。若确实需要同一 realm / 进程共享，应由应用入口或依赖注入容器拥有实例；必须挂到全局时，可用 `globalThis[Symbol.for('your-app/runtime')]`，并自行处理版本冲突、测试清理和生命周期。"
              },
              {
                "type": "paragraph",
                "text": "`Proxy` 可以把首次属性访问转成惰性初始化，或限制对象如何被访问，但它不会自动保证全局唯一；唯一性仍取决于 Proxy 背后那份实例存在哪里、是否只有一个 owner。不要为了“更像单例”而给 `once` 的结果额外套 Proxy。"
              },
              {
                "type": "paragraph",
                "text": "`onceAsync`｜10 秒理解场景 —— 同一个包装函数实例只启动一次异步任务，所有调用者共享同一个 Promise："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "// app-config.ts：多个组件启动时都可能读取配置，但网络请求只能发起一次。\nexport const loadAppConfig = onceAsync(async () => {\n  const response = await fetch('/config');\n  if (!response.ok) throw new Error(`config request failed: ${response.status}`);\n  return response.json() as Promise<{ apiBaseUrl: string }>;\n});\n\nconst [routerConfig, telemetryConfig] = await Promise.all([\n  loadAppConfig(),\n  loadAppConfig()\n]); // 两处拿到同一次请求的结果"
              },
              {
                "type": "paragraph",
                "text": "适合进程或页面生命周期内只应成功或失败一次的异步初始化，例如加载不会刷新的启动配置、初始化一个共享 SDK 客户端、动态导入并编译同一份 WASM 模块。并发调用不会制造竞态：任务尚未完成时共享 pending Promise，完成后继续复用相同的 fulfilled 或 rejected Promise。"
              },
              {
                "type": "paragraph",
                "text": "不适合带 key 的请求去重、定时刷新、失败重试、登出后重建、按租户或请求隔离等场景；这些需求需要带失效策略的缓存或由生命周期容器管理。首次失败会一直缓存，`onceAsync` 不会自动重试。所有调用者还共享同一底层任务，因此不要让某个调用者用自己的 `AbortSignal` 随意取消它，否则其他等待者也会受影响。"
              },
              {
                "type": "paragraph",
                "text": "作用域与 `once` 相同：每次调用 `onceAsync(fn)` 都会创建独立缓存，模块重复实例化或跨 iframe、Worker、Node `vm` 时不会共享。单参数 `functionValue: () => Promise<T>`（必填），无其他选项；返回值可以是原生 `Promise` 或合法 PromiseLike，否则以 `INVALID_ARGUMENT` reject。"
              }
            ]
          },
          {
            "id": "collector-惰性字段筛选流水线",
            "heading": "Collector：惰性字段筛选流水线",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`collect` 只从包根入口导出。它借用一个 `readonly` 数组，链式动作立即改变查询语义，首次读取 `result` 时才按调用顺序扫描一次并缓存："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { collect } from '@migaia/utils';\n\nconst collector = collect(users)\n  .fieldBy('profile.name-zh', 'profile.name-en')\n  .like(' world ')\n  .where((user) => user.active)\n  .take(20);\n\nconst result = collector.result;"
              },
              {
                "type": "paragraph",
                "text": "`fieldBy` 本身会过滤掉所有候选字段都为 `undefined` 的 source，并启用 `like`、`equals`、`oneOf` 的 typestate；未调用它时，TypeScript 不暴露这些方法。`like` 只匹配字符串，trim 后为空时为 no-op。另有 `distinctBy`、`skip`、`take`，所有动作保留原 source 项的身份与顺序。"
              },
              {
                "type": "paragraph",
                "text": "Collector 不复制或追踪外部修改：调用方必须在 collector 生命周期内遵守传入数组及对象的只读约定。相同查询 revision 重复读取 `result` 返回同一缓存引用；新增动作后会从原 source 重新求值，但不会修改旧结果快照。"
              }
            ]
          },
          {
            "id": "高阶组合示例-1-截止时间-重试-只读配置",
            "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
            "blocks": [
              {
                "type": "paragraph",
                "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
              }
            ]
          },
          {
            "id": "高阶组合示例-2-并发限流-重试-错误合并-批量拉取-容忍部分失败",
            "heading": "高阶组合示例 · 2. 并发限流 + 重试 + 错误合并：批量拉取，容忍部分失败",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
              }
            ]
          },
          {
            "id": "高阶组合示例-3-对象路径-配置-把校验后的嵌套结构整体写回配置根",
            "heading": "高阶组合示例 · 3. 对象路径 + 配置：把校验后的嵌套结构整体写回配置根",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
              }
            ]
          },
          {
            "id": "高阶组合示例-4-一次性初始化-错误身份-确保初始化异常带上可识别的-code",
            "heading": "高阶组合示例 · 4. 一次性初始化 + 错误身份：确保初始化异常带上可识别的 code",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { once } from '@migaia/utils/function';\nimport { attachErrorIdentity } from '@migaia/utils/error';\n\nconst initOnce = once(() => {\n  try {\n    return dangerousInit();\n  } catch (error) {\n    throw attachErrorIdentity(error as Error, { source: 'my-module', code: 'INIT_FAILED' });\n  }\n});"
              }
            ]
          },
          {
            "id": "高频值-模板与数字工具",
            "heading": "高频值、模板与数字工具",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { format, formatCurrency, isEmptyValue, isPrimitive } from '@migaia/utils';\n\nisEmptyValue('  '); // true；0、false、0n 均为 false\nisPrimitive(Symbol('id')); // true\nformat('库存 {stock}，金额 {money}', { stock: 0, money: '¥12.00' });\nformatCurrency(1234.5, 'CNY', {\n  locales: 'zh',\n  format: { minimumFractionDigits: 2, maximumFractionDigits: 2 }\n});"
              },
              {
                "type": "paragraph",
                "text": "`format` 默认使用 `{`/`}`，可通过 `placeholder.open/close` 改成 `${`/`}` 或 `[[`/`]]`。数字工具复用有界 `Intl.NumberFormat` 缓存；热循环优先调用 `createNumberFormatter()` 一次并复用返回函数。金额、库存和量化精度均由业务显式配置，utils 不内置领域默认值。"
              },
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm run fmt && pnpm run lint && pnpm run typecheck && pnpm run typecheck:test && pnpm run test"
              }
            ]
          }
        ]
      },
      "guide": {
        "sections": [
          {
            "id": "migaia-utils-使用指南",
            "heading": "@migaia/utils 使用指南",
            "blocks": [
              {
                "type": "paragraph",
                "text": "本指南逐个模块覆盖公开运行时 API、直接配置所需的关键类型、语义与用法示例。仅用于泛型推导的 type-only helper 以发布的 `.d.ts` 和编辑器提示为准；包的定位与安装方式见 README。"
              }
            ]
          },
          {
            "id": "目录",
            "heading": "目录",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "`/promise` 模块",
                  "`/error` 模块",
                  "`/bytes` 模块",
                  "`/object` 模块（含对象路径）",
                  "`/typing` 模块",
                  "`/config` 模块",
                  "`/function` 模块",
                  "Collector",
                  "组合工作流示例",
                  "排查与构建门禁"
                ]
              }
            ]
          },
          {
            "id": "promise-模块",
            "heading": "/promise 模块",
            "blocks": [
              {
                "type": "paragraph",
                "text": "处理截止时间、协作式中止、重试与并发限流。所有中止都是协作式的：被中止的操作只有在读取传入的 `signal` 并主动提前返回/抛错时才会真正停止；忽略 `signal` 的 I/O 不会被强制打断。"
              }
            ]
          },
          {
            "id": "调度器类型",
            "heading": "调度器类型",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IScheduledTask = { cancel(): void; unref?(): void };\ntype IUtilsScheduler = {\n  now(): number;\n  schedule(callback: () => void, delayMs: number): IScheduledTask;\n};\ntype IManualScheduler = IUtilsScheduler & {\n  advance(ms: number): void;\n  readonly pendingCount: number;\n};"
              },
              {
                "type": "list",
                "items": [
                  "`IUtilsScheduler`：可注入的时间源，`now()` 返回当前时刻，`schedule()` 安排一次性回调并返回可取消的句柄。",
                  "`IManualScheduler`：额外提供 `advance(ms)`（手动推进虚拟时间，触发到期回调）与 `pendingCount`（未取消的待执行任务数），用于确定性单测。"
                ]
              },
              {
                "type": "code",
                "language": "ts",
                "code": "export const systemScheduler: IUtilsScheduler;"
              },
              {
                "type": "paragraph",
                "text": "基于原生 `setTimeout`/`clearTimeout` 的默认调度器，`now()` 当前直接调用 `Date.now()`，因此两者返回的时间戳与精度相同。`systemScheduler.now()` 的价值不是提供另一种计时算法，而是让读取时间的 `now()` 和安排任务的 `schedule()` 共享同一个可注入边界；测试可将两者一起替换为虚拟时钟。"
              },
              {
                "type": "paragraph",
                "text": "普通业务只需要读取真实墙上时间时，直接使用 `Date.now()`。实现接受 `IUtilsScheduler` 的超时、重试或调度逻辑时，必须配对使用 `scheduler.now()` 和 `scheduler.schedule()`；直接调用 `Date.now()` 会绕过注入的调度器，造成真实时间与虚拟定时器混用。`schedule()` 返回的句柄在宿主支持时会转发 `unref()`（避免测试/进程因悬挂定时器无法退出）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function createManualScheduler(): IManualScheduler;"
              },
              {
                "type": "paragraph",
                "text": "本仓测试专用：用于本仓单元测试与适配器验证，不建议外部业务代码使用。外部项目应优先选择测试框架自带的 fake timers，生产代码使用 `systemScheduler` 或默认调度器。"
              },
              {
                "type": "paragraph",
                "text": "创建一个 FIFO 虚拟时钟调度器：`schedule()` 按 `到期时间 → 注册顺序` 排队；调用 `advance(ms)` 会一次性执行所有到期回调（回调内部再 `schedule` 的新任务，只要到期时间 `<= 目标时刻` 也会在同一次 `advance` 内继续触发）。`advance` 单次循环超过 10000 次会抛 `RangeError`（防止回调间互相递归调度导致死循环）。`delayMs`/`ms` 必须是有限的非负数，否则抛 `RangeError`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scheduler = createManualScheduler();\nconst task = scheduler.schedule(() => console.log('fired'), 100);\nscheduler.advance(50); // 未触发\nscheduler.advance(50); // 触发，打印 'fired'"
              }
            ]
          },
          {
            "id": "deferred",
            "heading": "deferred",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IDeferred<T> = {\n  readonly promise: Promise<T>;\n  readonly resolve: (value: T | PromiseLike<T>) => void;\n  readonly reject: (reason?: unknown) => void;\n};\nfunction deferred<T>(): IDeferred<T>;"
              },
              {
                "type": "paragraph",
                "text": "创建一个可从外部结算的原生 Promise。适合把回调式 API 转成 Promise，或在测试中手动控制某个异步操作何时完成/失败。"
              },
              {
                "type": "paragraph",
                "text": "新代码应先选择平台原生的 `Promise.withResolvers()`。`deferred()` 主要服务于尚未支持该 API 的旧运行时，或必须统一依赖 `IDeferred<T>` 的项目边界："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const { promise, resolve, reject } = Promise.withResolvers<number>();\nresolve(42);"
              },
              {
                "type": "paragraph",
                "text": "兼容旧运行时："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const { promise, resolve } = deferred<number>();\nsetTimeout(() => resolve(42), 0);\nawait promise; // 42"
              }
            ]
          },
          {
            "id": "topromise",
            "heading": "toPromise",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function toPromise<T>(run: () => T): Promise<Awaited<T>>;"
              },
              {
                "type": "paragraph",
                "text": "同步且恰好调用 `run` 一次，并用原生 Promise 规则同化返回值。若 `run` 同步抛出，调用点不会同步抛错，而是返回以 exact reason reject 的 Promise。它只负责同步计算到 Promise 的规范化，不创建 timer、scheduler、retry 或 cancellation 状态。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const found = await toPromise(() => cache.get('key'));\nconst pending = toPromise(() => {\n  throw new Error('read failed');\n});\nawait pending; // 异步观察 rejection"
              },
              {
                "type": "paragraph",
                "text": "与 `Promise.resolve().then(() => run())` 对比："
              },
              {
                "type": "list",
                "items": [
                  "`toPromise(run)` 立即调用 `run`；返回前已经发生读取、写入或其他同步副作用。`run` 返回原生 Promise 时，返回值保持同一个 Promise identity。",
                  "`Promise.resolve().then(run)` 把调用推迟到下一次 microtask，并返回新的链式 Promise。它适合主动让出当前调用栈、避免同步重入，不适合依赖“调用后立即生效”的逻辑。",
                  "两者都会把异常变成 rejection，但产生 rejection 的时点不同：前者在当前调用过程中捕获同步异常，后者等 microtask 真正执行 `run` 时才捕获。"
                ]
              },
              {
                "type": "paragraph",
                "text": "因此，需要同步启动工作并统一返回 Promise 时使用 `toPromise`；需要延迟启动工作时使用 `Promise.resolve().then(run)`。"
              }
            ]
          },
          {
            "id": "sleep",
            "heading": "sleep",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;"
              },
              {
                "type": "paragraph",
                "text": "在调度器延迟后 resolve；若传入的 `signal`/`signals` 在等待期间中止，则以 `UtilsAbortError` reject。`delayMs` 必须是有限非负数。会正确清理已注册的定时器与 abort 监听器，即使清理过程本身抛错也会通过 `hostRethrowReporter`（或调用方指定的 reporter）上报，而不是吞掉。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IAsyncControls = {\n  readonly signal?: IAbortSignal;\n  readonly signals?: readonly IAbortSignal[];\n  readonly scheduler?: IUtilsScheduler;\n  readonly unref?: boolean;\n};"
              },
              {
                "type": "paragraph",
                "text": "`signal` 与 `signals` 互斥——同时提供两者会 reject 一个 `TypeError`。`unref: true` 会尝试调用底层定时器句柄的 `unref()`（Node 环境下避免阻塞进程退出）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const controller = new AbortController();\nconst p = sleep(1000, { signal: controller.signal as any });\ncontroller.abort('cancelled');\nawait p; // 抛 UtilsAbortError，cause 为 'cancelled'"
              }
            ]
          },
          {
            "id": "createaborttimeoutsignal",
            "heading": "createAbortTimeoutSignal",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IAbortTimeoutSignalOptions = {\n  readonly signal?: IAbortSignal;\n  readonly timeoutMs?: number;\n  readonly scheduler?: IUtilsScheduler;\n  readonly timeoutReason?: () => unknown;\n  readonly report?: IUtilsReporter;\n};\ntype IAbortTimeoutSignal = {\n  readonly signal: IAbortSignal | undefined;\n  readonly dispose: () => void;\n};\nfunction createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;"
              },
              {
                "type": "paragraph",
                "text": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const merged = createAbortTimeoutSignal({\n  signal: controller.signal as any,\n  timeoutMs: 500,\n  timeoutReason: () => new Error('deadline')\n});\ntry {\n  await Promise.all([requestA(merged.signal), requestB(merged.signal)]);\n} finally {\n  merged.dispose();\n}"
              },
              {
                "type": "paragraph",
                "text": "`withTimeout` / `raceWithAbort` 的 callback 也能把同一 cooperative signal 交给多个请求，但它们同时拥有最终 Promise 的超时/中止 reject、late rejection 与 cleanup-error 政策。若事务 commit 才是唯一结算事实，应使用本 signal-only primitive，并由适配器自己决定 Promise 何时完成。"
              }
            ]
          },
          {
            "id": "withtimeout",
            "heading": "withTimeout",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal }) => T | PromiseLike<T>;\n\nfunction withTimeout<T>(\n  operation: ITimeoutOperation<T>,\n  options: IAsyncControls & {\n    readonly timeoutMs: number;\n    readonly report?: IUtilsReporter;\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly cooperativeCancellation?: boolean;\n  }\n): Promise<T>;"
              },
              {
                "type": "paragraph",
                "text": "用截止时间包裹一个惰性操作。`operation` 收到的 `signal` 是内部生成的合并信号：外部 `signal(s)` 中止、或截止时间到达，都会让这个 `signal` 变为 `aborted`；操作函数必须自行检查它才能提前退出。"
              },
              {
                "type": "list",
                "items": [
                  "`timeoutMs`：必须是有限非负数；`timeoutMs === 0` 时默认立即以 `UtilsTimeoutError('operation', 0)` reject（除非 `zeroTimeoutBehavior: 'start'`，此时改为立刻启动操作、不设超时）。",
                  "`cooperativeCancellation`（默认 `true`）：为 `false` 时不分配内部 `AbortController`，操作函数只会拿到一个惰性的、恒不中止的信号——用于纯粹的\"限时等待结果，但不需要通知操作提前退出\"的场景。",
                  "`report`：当操作在已经结算（超时/被外部中止）之后才 resolve/reject，这个迟到的结果不会再影响最终返回值，但会经由 `report`（默认 `hostRethrowReporter`）上报，避免静默丢弃未处理的错误。",
                  "清理阶段（取消定时器、移除监听器）失败时，会把清理错误与原始错误一起包装成 `AggregateError` 一并 reject。"
                ]
              },
              {
                "type": "code",
                "language": "ts",
                "code": "await withTimeout(\n  async ({ signal }) => {\n    while (!signal.aborted) {\n      // 做一些可中断的工作\n    }\n    throw signal.reason;\n  },\n  { timeoutMs: 500 }\n); // 500ms 后以 UtilsTimeoutError('operation', 500) reject"
              }
            ]
          },
          {
            "id": "racewithabort",
            "heading": "raceWithAbort",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IAbortOperation<T> = ITimeoutOperation<T>;\n\nfunction raceWithAbort<T>(\n  operation: IAbortOperation<T>,\n  options?: Pick<IAsyncControls, 'signal' | 'signals'> & {\n    readonly report?: IUtilsReporter;\n    readonly cleanupPolicy?: 'reject' | 'report';\n  }\n): Promise<T>;"
              },
              {
                "type": "paragraph",
                "text": "与 `withTimeout` 语义相同，但不设置截止时间定时器，只在外部 `signal(s)` 中止时让操作提前失败。`cleanupPolicy: 'report'`（默认 `'reject'`）让监听器清理失败时改为上报诊断而不是让最终 Promise reject。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const controller = new AbortController();\nconst result = raceWithAbort(({ signal }) => fetchSomething({ signal }), {\n  signal: controller.signal as any\n});"
              }
            ]
          },
          {
            "id": "retry",
            "heading": "retry",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IRetryContext = {\n  readonly attempt: number;\n  readonly signal: IAbortSignal;\n  readonly remainingMs?: number;\n};\ntype IRetryFailureContext = IRetryContext & { readonly maxAttempts: number };\n\nfunction retry<T>(\n  operation: (context: IRetryContext) => T | PromiseLike<T>,\n  options: {\n    readonly maxAttempts: number;\n    readonly shouldRetry: (\n      error: unknown,\n      context: IRetryFailureContext\n    ) => boolean | PromiseLike<boolean>;\n    readonly delay?: number | ((error: unknown, context: IRetryFailureContext) => number);\n    readonly totalTimeoutMs?: number;\n    readonly attemptTimeoutMs?: number;\n    readonly signal?: IAbortSignal;\n    readonly signals?: readonly IAbortSignal[];\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly report?: IUtilsReporter;\n    readonly unref?: boolean;\n    readonly scheduler?: IUtilsScheduler;\n  }\n): Promise<T>;"
              },
              {
                "type": "paragraph",
                "text": "串行重试，带三层限制："
              },
              {
                "type": "list",
                "items": [
                  "`maxAttempts`：必须是正安全整数，第 `maxAttempts` 次仍失败则直接抛出最后一次的错误。",
                  "`attemptTimeoutMs`：每次尝试单独的超时（内部用 `withTimeout` 包裹）。",
                  "`totalTimeoutMs`：整个重试过程（包含所有尝试与退避等待）的总截止时间；超出会抛 `UtilsTimeoutError('total', totalTimeoutMs)`。",
                  "`shouldRetry(error, context)`：决定某次失败是否值得重试；返回 `false` 或 `maxAttempts` 已用尽都会终止重试并抛出该错误。",
                  "`delay`：固定毫秒数或 `(error, context) => 毫秒数` 的退避函数，作用于两次尝试之间的等待（通过内部 `sleep`，同样遵守 `signal`/`signals`）。",
                  "外部信号在重试过程中的任意时刻中止，都会以 `UtilsAbortError` 结束整个重试，不再进入下一次尝试。"
                ]
              },
              {
                "type": "code",
                "language": "ts",
                "code": "await retry(\n  async ({ attempt }) => {\n    if (attempt < 3) throw new Error('boom');\n    return 'ok';\n  },\n  { maxAttempts: 5, shouldRetry: () => true, delay: (_, { attempt }) => attempt * 100 }\n); // 'ok'，共尝试 3 次，两次退避分别约 100ms/200ms"
              }
            ]
          },
          {
            "id": "hostrethrowreporter-与-iutilsreporter",
            "heading": "hostRethrowReporter 与 IUtilsReporter",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IUtilsReporter = (\n  error: unknown,\n  context: {\n    readonly operation: 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter';\n    readonly phase: 'late-rejection' | 'cleanup' | 'reporter';\n    readonly attempt?: number;\n  }\n) => void;\nconst hostRethrowReporter: IUtilsReporter;"
              },
              {
                "type": "paragraph",
                "text": "`hostRethrowReporter` 是默认诊断上报器：把错误通过 `queueMicrotask` 重新抛出（在不支持 `queueMicrotask` 的环境下同步抛出），使其成为未捕获异常，交由宿主（进程/浏览器）的全局错误处理机制记录，而不是被静默吞掉。自定义 `report` 选项可以替换为日志上报、埋点等。"
              }
            ]
          },
          {
            "id": "createconcurrencylimiter",
            "heading": "createConcurrencyLimiter",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IConcurrencyLimiter = {\n  run<T>(\n    task: (context: { readonly signal?: IAbortSignal }) => T | PromiseLike<T>,\n    options?: { readonly signal?: IAbortSignal }\n  ): Promise<T>;\n  readonly activeCount: number;\n  readonly pendingCount: number;\n  whenIdle(): Promise<void>;\n  close(reason?: unknown): void;\n  dispose(reason?: unknown): Promise<void>;\n};\nfunction createConcurrencyLimiter(options: {\n  readonly concurrency: number;\n  readonly report?: IUtilsReporter;\n}): IConcurrencyLimiter;"
              },
              {
                "type": "paragraph",
                "text": "本包中唯一具有跨调用生命周期的原语——一个带并发上限的 FIFO 任务队列："
              },
              {
                "type": "list",
                "items": [
                  "`run(task, { signal })`：把任务加入队列，达到 `concurrency` 上限前的任务立即执行，之后排队等待空位；传入的 `signal` 中止会让排队中的任务立即以其中止原因 reject（已开始执行的任务不受影响，需任务自身读取 `context.signal`）。",
                  "`activeCount` / `pendingCount`：当前正在执行 / 排队等待（未取消）的任务数。",
                  "`whenIdle()`：返回一个在\"当前没有活跃任务且队列已清空\"时 resolve 的 Promise；多次调用共享同一个等待，不会重复注册。",
                  "`close(reason?)`：停止接纳新任务与排队任务——已排队但未执行的任务立即以 `reason`（默认一个 `LIMITER_CLOSED` 错误）reject；正在执行的任务不受影响。",
                  "`dispose(reason?)`：调用 `close()` 并返回一个在所有活跃任务结束后才 resolve 的 Promise，用于优雅关闭。"
                ]
              },
              {
                "type": "paragraph",
                "text": "`concurrency` 必须是正安全整数，否则构造时抛 `RangeError`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const limiter = createConcurrencyLimiter({ concurrency: 2 });\nconst results = await Promise.all([1, 2, 3, 4].map((n) => limiter.run(() => fetchPage(n))));\nawait limiter.dispose(); // 等待剩余任务完成后关闭"
              }
            ]
          },
          {
            "id": "error-模块",
            "heading": "/error 模块",
            "blocks": [
              {
                "type": "paragraph",
                "text": "统一的错误身份、包装与因果链遍历工具。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
              },
              {
                "type": "paragraph",
                "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
              },
              {
                "type": "paragraph",
                "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
              },
              {
                "type": "paragraph",
                "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
              },
              {
                "type": "paragraph",
                "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
              },
              {
                "type": "paragraph",
                "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
              },
              {
                "type": "paragraph",
                "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
              }
            ]
          },
          {
            "id": "utilserrorcode",
            "heading": "UtilsErrorCode",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "const UtilsErrorCode = {\n  invalidArgument: 'INVALID_ARGUMENT',\n  nonErrorValue: 'NON_ERROR_VALUE',\n  envUnsupported: 'ENV_UNSUPPORTED',\n  aborted: 'ABORTED',\n  deadlineExceeded: 'DEADLINE_EXCEEDED',\n  schedulerRunaway: 'SCHEDULER_RUNAWAY',\n  errorIdentityConflict: 'ERROR_IDENTITY_CONFLICT',\n  cloneUnsupported: 'CLONE_UNSUPPORTED',\n  invalidEncoding: 'INVALID_ENCODING',\n  limiterClosed: 'LIMITER_CLOSED',\n  reentrantCall: 'REENTRANT_CALL',\n  configUnsupported: 'CONFIG_UNSUPPORTED',\n  configReadonly: 'CONFIG_READONLY',\n  configConflict: 'CONFIG_CONFLICT',\n  configLimitExceeded: 'CONFIG_LIMIT_EXCEEDED',\n  configPathInvalid: 'CONFIG_PATH_INVALID',\n  objectPathInvalid: 'OBJECT_PATH_INVALID',\n  formatInvalid: 'FORMAT_INVALID',\n  formatValueMissing: 'FORMAT_VALUE_MISSING',\n  numberFormatInvalid: 'NUMBER_FORMAT_INVALID'\n} as const;"
              },
              {
                "type": "paragraph",
                "text": "全包统一使用的稳定语义码，`IUtilsErrorCode` 是其取值的联合类型。除 `UtilsError` 子类外，`base64ToBytes`/`object-path`/`config` 等模块抛出的原生 `TypeError`/`RangeError` 也会通过 `Object.defineProperty` 挂上对应的 `code`（与 `source: '@migaia/utils'`），可用同一套判断逻辑识别。"
              },
              {
                "type": "paragraph",
                "text": "按用途理解全部错误码："
              },
              {
                "type": "list",
                "items": [
                  "`INVALID_ARGUMENT`：参数类型、范围或互斥组合错误；修正调用参数。",
                  "`NON_ERROR_VALUE`：非 `Error` 异常值已被 `toError()` 转换；从 `cause` 读取原始值。",
                  "`ENV_UNSUPPORTED`：宿主缺少 `structuredClone` 等必要能力；更换环境或实现。",
                  "`ABORTED`：外部信号请求取消；停止协作式工作并传播取消原因。",
                  "`DEADLINE_EXCEEDED`：操作、尝试或总流程超时；结合 `scope`、`timeoutMs` 选择重试或降级。",
                  "`SCHEDULER_RUNAWAY`：虚拟时钟一次推进超过 10000 个任务；排查同刻递归调度。",
                  "`ERROR_IDENTITY_CONFLICT`：已有错误身份与新 `source`/`code` 冲突；保留原身份。",
                  "`CLONE_UNSUPPORTED`：值不能被 `structuredClone` 复制；先转换为可克隆数据。",
                  "`INVALID_ENCODING`：编码输入不是规范形式；按错误偏移修正输入。",
                  "`LIMITER_CLOSED`：并发限制器已关闭；停止提交或创建新实例。",
                  "`REENTRANT_CALL`：同一实例发生禁止的重入；等待当前回调结束。",
                  "`CONFIG_UNSUPPORTED`：配置含不支持的数据形状或属性；改用普通数据。",
                  "`CONFIG_READONLY`：尝试修改只读配置；从所有者侧创建新配置。",
                  "`CONFIG_CONFLICT`：配置来源的所有权、profile 或描述符冲突；统一契约再合并。",
                  "`CONFIG_LIMIT_EXCEEDED`：配置键数、深度等超过安全上限；缩小输入或调整明确限制。",
                  "`CONFIG_PATH_INVALID`：配置路径为空、危险或分段无效；改用安全路径。",
                  "`OBJECT_PATH_INVALID`：对象路径无法安全解析；修正字符串或分段元组。",
                  "`FORMAT_INVALID`：模板、占位符或值不能安全格式化；修正模板或输入。",
                  "`FORMAT_VALUE_MISSING`：严格格式化缺少占位符值；补齐字段或调整策略。",
                  "`NUMBER_FORMAT_INVALID`：`Intl.NumberFormat` 拒绝 locale、选项、货币或数值；修正格式配置。"
                ]
              }
            ]
          },
          {
            "id": "bytes-模块",
            "heading": "/bytes 模块",
            "blocks": [
              {
                "type": "paragraph",
                "text": "不依赖 Node `Buffer` 或 DOM 的字节/文本编解码，纯 ECMAScript 实现。"
              }
            ]
          },
          {
            "id": "arraybuffer-与-uint8array-的关系",
            "heading": "ArrayBuffer 与 Uint8Array 的关系",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "`ArrayBuffer` 是固定长度的原始连续内存，只负责保存字节；不能直接用下标读写。它常见于 `fetch(...).arrayBuffer()`、文件、Web Crypto、WebAssembly 和 Worker 传输边界。",
                  "`Uint8Array` 是该内存上的 8 位无符号整数视图，每个元素对应一个 0～255 的字节。Base64、UTF-8、网络协议与二进制序列化需要逐字节操作，因此本模块以它作为主要输入输出。",
                  "`new Uint8Array(buffer, byteOffset, length)` 创建共享底层内存的窗口，不复制数据；视图写入会反映到原 buffer。`subarray()` 继续共享内存，`slice()` 才创建独立副本。",
                  "普通 `ArrayBuffer` 跨 Worker 转移后可能 detached；`SharedArrayBuffer` 是并发共享内存，具有不同的所有权和同步要求，所以 `isArrayBuffer()` 明确拒绝它。"
                ]
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function isUint8Array(value: unknown): value is Uint8Array;\nfunction isArrayBuffer(value: unknown): value is ArrayBuffer;"
              },
              {
                "type": "paragraph",
                "text": "两个 guard 通过引擎的 `%TypedArray%.prototype[Symbol.toStringTag]` 与 `ArrayBuffer.prototype.byteLength` 内部槽 getter 判定真实品牌，因此能识别跨 iframe/Worker realm 的合法实例与子类，同时拒绝 `constructor.name` / 原型 / `Symbol.toStringTag` 伪造、其他 TypedArray、`DataView`、`SharedArrayBuffer` 和 Proxy。检测不读取候选值的用户属性。"
              },
              {
                "type": "paragraph",
                "text": "detached `Uint8Array` / `ArrayBuffer` 仍返回 `true`：detachment 不改变对象品牌，但会影响读取能力；需要消费字节的调用方仍须独立处理 detached 状态。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function bytesToBase64(value: Uint8Array): string;"
              },
              {
                "type": "paragraph",
                "text": "按 RFC 4648 规范字母表编码为标准（非 URL-safe）、带 `=` 补齐的 Base64 字符串。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function base64ToBytes(value: string): Uint8Array;"
              },
              {
                "type": "paragraph",
                "text": "只接受规范形式：标准字母表、必须 4 的倍数长度、正确的 `=` 补齐。非规范输入（URL-safe 变体、无补齐、多余空白等）一律拒绝，抛出携带 `code: 'INVALID_ENCODING'` 的 `TypeError`。解码后会反向编码校验往返一致性，进一步拒绝虽然长度/字符集正确但数值不对齐的输入。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "bytesToBase64(new Uint8Array([1, 2, 3])); // 'AQID'\nbase64ToBytes('AQID'); // Uint8Array [1, 2, 3]\nbase64ToBytes('AQID-_'); // 抛 TypeError（URL-safe 字符不被接受）"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function* streamBase64Chunks(value: Uint8Array, maxChunkBytes?: number): Iterable<string>;"
              },
              {
                "type": "paragraph",
                "text": "把大字节数组切分成多个各自独立合法的 Base64 片段（每片段都在 3 字节边界上切分，因此每片段自身都能正确解码，不需要拼接后才合法）。`maxChunkBytes` 默认 `32763`，必须是 `>= 3` 的安全整数；实际切分宽度会向下取整到 3 的倍数。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function utf8ByteLength(value: string): number;"
              },
              {
                "type": "paragraph",
                "text": "计算字符串编码为 UTF-8 后的字节数，不分配缓冲区（正确处理代理对/非法代理，用 U+FFFD 的宽度 3 计入非法代理）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function encodeUtf8(value: string): Uint8Array;\nfunction decodeUtf8(value: Uint8Array, options?: { readonly fatal?: boolean }): string;"
              },
              {
                "type": "paragraph",
                "text": "`encodeUtf8` 编码为新分配的 UTF-8 字节数组。`decodeUtf8` 解码：`fatal: false`（默认）遇到非法字节序列时用 U+FFFD 替换并跳过 1 字节继续；`fatal: true` 时直接抛出携带偏移量的编码错误。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function splitUtf8(value: string, maxBytes: number): readonly string[];"
              },
              {
                "type": "paragraph",
                "text": "把字符串按码点边界切分成若干子串，保证每个子串编码后的字节数不超过 `maxBytes`（绝不会在一个码点中间切断）。`maxBytes` 必须是 `>= 4` 的安全整数（4 是单个码点的最大 UTF-8 宽度）。空字符串返回 `['']`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "splitUtf8('你好世界', 6); // 每个汉字 3 字节 → ['你好', '世界']"
              }
            ]
          },
          {
            "id": "快照与探测",
            "heading": "快照与探测",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function isPlainObject(value: unknown): value is Record<PropertyKey, unknown>;"
              },
              {
                "type": "paragraph",
                "text": "仅当 `value` 是普通 `{}` 字面量或 `Object.create(null)` 创建的对象时返回 `true`；数组、`class` 实例、内置对象（`Date`/`Map`/.）一律 `false`。读取原型链失败（代理陷阱抛错）时同样返回 `false`，不会向上抛错。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IProbePropertyResult<T> =\n  | { readonly kind: 'missing' }\n  | { readonly kind: 'value'; readonly value: T }\n  | { readonly kind: 'failed'; readonly error: unknown };\nfunction probeProperty<T>(value: object, key: PropertyKey): IProbePropertyResult<T>;"
              },
              {
                "type": "paragraph",
                "text": "读取单个属性且只读取一次（避免 getter 被调用两次产生副作用）：属性不存在返回 `missing`；读取（包括触发 getter）抛错返回 `failed` 并携带原始错误；否则返回 `value`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const profileUnavailable = new Error('profile unavailable');\nlet profileReads = 0;\nconst user = {\n  name: 'Ada',\n  get profile(): never {\n    profileReads += 1;\n    throw profileUnavailable;\n  }\n};\n\nfunction readLabel(value: object, key: PropertyKey): string {\n  const result = probeProperty<string>(value, key);\n  switch (result.kind) {\n    case 'value':\n      return result.value;\n    case 'missing':\n      return 'Anonymous';\n    case 'failed':\n      console.error('property read failed', result.error);\n      return 'Unavailable';\n  }\n}\n\nreadLabel(user, 'name'); // 'Ada'\nreadLabel(user, 'email'); // 'Anonymous'\nreadLabel(user, 'profile'); // 'Unavailable'\nprofileReads; // 1：getter 只被读取一次"
              },
              {
                "type": "paragraph",
                "text": "第一个参数可以是普通对象、class 实例或其他可读取属性的对象；第二参数接受字符串、数字或 Symbol key。它把缺失字段与读取失败拆成不同分支，避免 `try/catch` 后错误地使用默认值；返回 `failed` 时不会重建错误，`error` 就是 getter 原样抛出的实例。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function immutableSnapshot<T>(value: T): T;"
              },
              {
                "type": "paragraph",
                "text": "实现直接调用宿主原生 `globalThis.structuredClone(value)`，不是 JSON 序列化或手写递归：它一次复制完整对象图，保留循环引用、共享引用关系以及 `Date`、`Map`、`Set`、`ArrayBuffer`、TypedArray 等结构化克隆支持的数据类型。本 API 不接受 transfer list，因此 `ArrayBuffer` 会复制而不会转移所有权。"
              },
              {
                "type": "paragraph",
                "text": "结构化克隆不会保留自定义 class 的原型方法、getter/setter 与属性描述符；函数、WeakMap 等不支持的成员会让整个克隆失败。“immutable”表示快照不再共享源对象引用，不表示返回值经过 `Object.freeze()`；需要运行时冻结或递归只读时由调用方另行处理。宿主不提供 `structuredClone` 时抛出 `code: 'ENV_UNSUPPORTED'` 的 `TypeError`；值本身不可结构化克隆时抛出 `code: 'CLONE_UNSUPPORTED'` 的 `TypeError`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const shared = { count: 1 };\nconst source = {\n  primary: shared,\n  alias: shared,\n  createdAt: new Date('2026-01-01T00:00:00Z')\n};\nconst copy = immutableSnapshot(source);\n\nsource.primary.count = 2;\ncopy.primary.count; // 1\ncopy.primary === copy.alias; // true\ncopy.createdAt instanceof Date; // true\nObject.isFrozen(copy); // false"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type ISnapshotDiagnostic = {\n  readonly path: readonly PropertyKey[];\n  readonly reason: 'accessor' | 'read-failed' | 'unsupported';\n  readonly cause: unknown;\n};\ntype IDiagnosticSnapshot<T> = {\n  readonly value: T;\n  readonly diagnostics: readonly ISnapshotDiagnostic[];\n};\nfunction diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;\nfunction structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;"
              },
              {
                "type": "paragraph",
                "text": "与原生 `structuredClone` / `immutableSnapshot` 的关键区别是失败粒度：前两者是整图“全有或全无”，任何不支持成员都会让调用抛错；`diagnosticSnapshot` 始终尝试返回 `{ value, diagnostics }`。它递归复制普通对象与数组，可克隆的内建子树独立调用 `structuredClone`；函数、class 实例等不支持类型则保留原引用并记录 `unsupported`。访问器会被读取一次、投影为普通值并记录 `accessor`；读取或反射失败记录 `read-failed`。循环引用通过 `WeakMap` 保持安全。"
              },
              {
                "type": "paragraph",
                "text": "因此，`diagnosticSnapshot` 的返回值可能只有部分隔离：`diagnostics` 非空时，调用方必须检查对应 `path`，不能把结果当作完整深拷贝。它适合日志、遥测、调试面板等“数据不能整份丢失，但降级必须可见”的场景；安全边界或配置隔离仍应优先用 `immutableSnapshot` 并让失败显式中止。"
              },
              {
                "type": "paragraph",
                "text": "`structuredDiagnosticSnapshot` 是两种策略的组合：优先尝试原生 `structuredClone`，成功时 diagnostics 为空；宿主不支持或克隆失败时自动退回上述尽力快照，并在根路径追加 `unsupported` 或携带原始 cause 的 `read-failed` 诊断。需要“能完整克隆就完整克隆，否则仍返回可观测降级结果”的遥测边界优先使用它。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function identitySnapshot<T>(value: T): T;"
              },
              {
                "type": "paragraph",
                "text": "原样返回入参。单独调用没有运行时收益；它用于 `snapshot` / `clone` 这类可配置策略槽的零拷贝分支，明确表示调用方接受共享引用、后续修改双方可见。本仓的 clone policy 正是用它实现 `opaque` 分支，服务于身份敏感或不可克隆、且允许共享所有权的可信进程内数据。需要外部输入隔离、历史快照或跨边界传输时不要使用它，应选择 `immutableSnapshot`。"
              }
            ]
          },
          {
            "id": "对象路径-parseobjectpath-probeobjectpath-get-set-createpathaccessor",
            "heading": "对象路径（parseObjectPath / probeObjectPath / get / set / createPathAccessor）",
            "blocks": [
              {
                "type": "paragraph",
                "text": "对象路径支持两种输入形式："
              },
              {
                "type": "list",
                "items": [
                  "字符串路径：`a.b.c`、数组用方括号下标 `list.[0].name`。",
                  "元组路径：`readonly ['a', 'b', 'c']` 或 `readonly ['list', 0, 'name']`，可包含 `symbol` 键（字符串路径不能表达 symbol）。"
                ]
              },
              {
                "type": "paragraph",
                "text": "两种形式共享同一套安全限制：单段最长 512 字符、路径最多 256 段、字符串路径总长不超过 4096 字符；`proto`/`prototype`/`constructor` 一律被拒绝（防止原型污染）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;"
              },
              {
                "type": "paragraph",
                "text": "把字符串路径解析成规范化的 `readonly` 段元组（数字下标解析成 `number`），或校验并冻结一个已给定的元组路径。任何越界/危险段都会抛出携带 `code: 'OBJECT_PATH_INVALID'` 的 `TypeError`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;"
              },
              {
                "type": "paragraph",
                "text": "沿路径逐段探测，每段只读取一次。四种结果分别对应：成功取到值（`value`，携带 `value`）、某一段属性不存在（`missing`）、某一段的父值不是对象/函数因而无法继续深入（`blocked`，例如路径指向 `a.b.c` 但 `a.b` 是 `null` 或字符串）、某一段读取本身抛出异常（`failed`，例如触发了会抛错的 getter，携带 `error`）。"
              },
              {
                "type": "paragraph",
                "text": "四种结果共有字段 `originKey`（原始传入的 `path`，未加工）与 `segments`（`parseObjectPath` 解析后的完整段元组）；`missing`/`blocked`/`failed` 额外共有 `failedAt`（第几段失败，0-based）、`failedKey`（失败的那一段键）、`resolvedPath`（成功解析到的前缀路径，即 `segments` 中 `failedAt` 之前的部分）、`parent`（失败发生时的父级值——`missing`/`failed` 时是该属性所属的对象，`blocked` 时是那个非对象的原始值本身）。`failed` 唯一多出 `error` 字段，携带原始抛出值。"
              },
              {
                "type": "paragraph",
                "text": "`probeObjectPathSegments` 接受已经由调用方校验/缓存的段元组，跳过再次解析；结果语义与 `probeObjectPath` 完全相同。只有能保证 segments 已通过同一安全约束的高频内部路径才应使用它，外部字符串输入继续交给 `probeObjectPath`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function get<T, P extends IObjectPathInput<T>>(\n  object: T,\n  path: P\n): IObjectPathValue<T, P> | undefined;"
              },
              {
                "type": "paragraph",
                "text": "基于 `probeObjectPath` 的简化读取：`missing`/`blocked` 统一返回 `undefined`；`failed`（读取本身抛错）会原样重新抛出该错误，不会被静默吞掉——这一点区别于\"路径不存在\"，调用方需要分别处理。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const data = { user: { name: 'Ada' } };\nget(data, 'user.name'); // 'Ada'\nget(data, 'user.missing'); // undefined（missing）\nget(data, 'user.name.x'); // undefined（blocked：'Ada' 是字符串，无法继续深入 .x）"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function set<T, P extends IObjectPathInput<T>>(\n  object: T,\n  path: P,\n  nextValue: IObjectPathWriteValue<T, P>\n): T;"
              },
              {
                "type": "paragraph",
                "text": "不可变路径写入：每次值真正变化的 `set()` 都返回一个新根对象，传入对象保持不变。只有路径上的节点被浅拷贝（结构共享，未涉及的兄弟节点保持同一引用）。这里的 immutable 描述更新方式，不表示返回对象经过深拷贝或 `Object.freeze()`；返回值本身仍可修改。路径中间缺失的容器会按下一段键的类型自动创建（数字键 → `[]`，其他 → `{}`）。若路径中途遇到非空的原始值（字符串、数字等，无法继续深入）会抛出 `code: 'OBJECT_PATH_INVALID'` 的 `TypeError`。若新值与旧值经 `Object.is` 相等，则没有发生更新，直接复用原对象，不做任何拷贝。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const before = { user: { name: 'Ada', tags: ['a'] } };\nconst after = set(before, 'user.name', 'Grace');\nafter.user.name; // 'Grace'\nafter !== before; // true（发生更新时返回新根对象）\nafter.user !== before.user; // true（更新路径上的节点被复制）\nafter.user.tags === before.user.tags; // true（未涉及的分支保持引用不变）\nbefore.user.name; // 'Ada'（原对象未被修改）\nObject.isFrozen(after); // false（immutable update 不等于冻结）"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
              },
              {
                "type": "paragraph",
                "text": "封装一个内部持有当前根对象的有状态访问器：每次 `set()` 成功后，`accessor.value` 自动前进到写入后的新根（后续 `get`/`set` 都基于最新根）。`ifMissing`/`ifBlocked`/`ifFailed` 在对应探测结果出现时被调用，用作集中式诊断钩子。"
              },
              {
                "type": "paragraph",
                "text": "`onGet`/`onSet` 在每次成功读/写时被调用，事件对象字段：`originKey`（原始传入路径）、`segments`（解析后的段元组）、`probe`（本次读/写命中的 `IPathValueProbe`，即成功探测结果）、`value`（本次实际读到/将要写入的值）、`replace(value)`（调用后就地改写本次读取/写入的实际返回值/写入值，例如做统一的默认值填充或写入前校验/转换）。`IPathGetEvent`/`IPathSetEvent` 唯一的区别是 `value`/`replace` 的类型：读事件用 `IObjectPathValue`（读到的原值类型），写事件用 `IObjectPathWriteValue`（字面量放宽后的可写类型）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const accessor = createPathAccessor(\n  { count: 0 },\n  { onSet: (event) => console.log('write', event.originKey, event.value) }\n);\naccessor.set('count', 5); // 打印 write count 5\naccessor.value; // { count: 5 }"
              },
              {
                "type": "paragraph",
                "text": "对应的类型工具（用于给调用方提供编译期路径补全与值类型推断）：`IObjectPathSegment`、`IObjectPathTuple`、`IObjectPath<T>`（字符串路径，最深 8 层）、`IObjectPathTupleFor<T>`（元组路径）、`IObjectPathInput<T>`（两者联合）、`IObjectPathValue<T, P>`（路径指向的值类型）、`IObjectPathWriteValue<T, P>`（写入时接受的类型，字面量类型会被适度放宽为其基础类型，例如字面量联合放宽为 `string`/`number`/`boolean`/`bigint`）。"
              }
            ]
          },
          {
            "id": "typing-模块",
            "heading": "/typing 模块",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`@migaia/utils/typing`（`src/typing.ts`）是纯 type-only 的子入口：文件里全部是 `export type`，没有任何运行时值，构建产物 `dist/typing.js` 不含可执行代码。它不经由根入口 `@migaia/utils` 重新导出——只能写 `import type { . } from '@migaia/utils/typing'`，这是刻意的：把类型出口和运行时出口分开，消费方按类型引用它时不会因为模块解析牵连进任何运行时代码。"
              },
              {
                "type": "paragraph",
                "text": "它做两件事："
              },
              {
                "type": "paragraph",
                "text": "1. 重导出已在别处定义的类型，给跨包类型复用一个稳定、单一的入口，避免消费方为了拿一个类型而要记住它散落在 `/object` 还是 `/promise`："
              },
              {
                "type": "list",
                "items": [
                  "来自 `/object`（即 `object-path.ts`）：`IObjectPath`、`IObjectPathInput`、`IObjectPathSegment`、`IObjectPathTuple`、`IObjectPathTupleFor`、`IObjectPathValue`、`IObjectPathWriteValue`",
                  "来自 `/object`（即 `object.ts`）：`IProbePropertyResult`",
                  "来自 `/promise`：`IAbortSignal`、`IDeferred`"
                ]
              },
              {
                "type": "paragraph",
                "text": "这些类型的语义、字段、约束与它们各自源模块中的定义完全一致（同一个类型的两个入口，不是两份独立定义），完整说明见 `/object` 模块与 `/promise` 模块对应章节。"
              },
              {
                "type": "paragraph",
                "text": "2. 新增两个 `/object` 模块本身不提供的类型工具，用于把带判别字段的联合类型（discriminated union）转成\"判别值 → 对应联合分支\"的映射类型："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "/** 按一个顶层、值为 PropertyKey 的判别字段对联合类型分组。 */\ntype IDiscriminatedByField<F extends PropertyKey, T extends Record<F, PropertyKey>> = {\n  [K in T[F]]: Extract<T, Record<F, K>>;\n};"
              },
              {
                "type": "list",
                "items": [
                  "类型参数 `F extends PropertyKey`：判别字段名（`string`/`number`/`symbol`）。",
                  "类型参数 `T extends Record<F, PropertyKey>`：目标联合类型本身；约束要求 `T` 每个成员上 `F` 对应的字段值都必须是 `PropertyKey`——如果某个分支的该字段是对象等非 `PropertyKey` 类型，`T` 就不满足 `Record<F, PropertyKey>` 约束，直接编译报错（不是运行时报错，是这个类型工具压根用不起来）。",
                  "结果：一个以 `T[F]` 的每个字面量值为 key、value 是 `Extract<T, Record<F, K>>`（该判别值对应的联合分支，若多个分支共享同一判别值则保留为它们的联合）的映射类型。"
                ]
              },
              {
                "type": "code",
                "language": "ts",
                "code": "/** 按一个 string 或 tuple 对象路径对联合类型分组，路径解析规则与 /object 模块的 IObjectPath 完全一致。 */\ntype IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = {\n  [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>;\n};"
              },
              {
                "type": "list",
                "items": [
                  "类型参数 `T`：目标联合类型本身。",
                  "类型参数 `P extends IObjectPathInput<T>`：字符串路径（如 `'meta.category'`）或元组路径（如 `readonly ['meta', 'category']`），和 `get`/`set`（`/object` 模块）接受的路径写法完全相同。",
                  "路径必须在联合的每一个分支上都能解析出值，且该值必须收窄到 `PropertyKey`；哪个分支上路径不存在/类型不对，该分支在实现细节上通过条件类型被排除出结果（不是抛运行时错误——这纯粹是类型层行为，错误的路径在 TypeScript 编译期就直接报错，不会产出一个\"缺分支\"的类型让你在运行时才发现）。",
                  "结果：以路径解析出的每个字面量判别值为 key，value 是对应的联合分支（同判别值的多个分支合并为联合）。"
                ]
              },
              {
                "type": "paragraph",
                "text": "用法示例（摘自包自带测试 `test/typing.test.ts`，行为已用 `expectTypeOf` 验证）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IEvent =\n  | {\n      readonly type: 'created';\n      readonly meta: { readonly category: 'write' };\n      readonly payload: { readonly userId: string };\n    }\n  | {\n      readonly type: 'deleted';\n      readonly meta: { readonly category: 'write' };\n      readonly payload: { readonly reason: string };\n    }\n  | {\n      readonly type: 'read';\n      readonly meta: { readonly category: 'read' };\n      readonly payload: { readonly cache: boolean };\n    };\n\ntype IByType = IDiscriminatedByField<'type', IEvent>;\n// { created: Extract<IEvent, { type: 'created' }>; deleted: ...; read: ... }\n\ntype IByCategory = IDiscriminatedByPath<IEvent, 'meta.category'>;\n// { write: 'created' | 'deleted' 两个分支的联合; read: 'read' 分支 }\n\ntype IByTupleCategory = IDiscriminatedByPath<IEvent, readonly ['meta', 'category']>;\n// 与 IByCategory 完全等价——字符串路径和元组路径是同一套解析逻辑的两种写法"
              },
              {
                "type": "paragraph",
                "text": "编译期会拒绝的用法（同样摘自测试文件）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "// @ts-expect-error 路径在联合的某些/全部分支上不存在，判别提取前就被拒绝\ntype IInvalid1 = IDiscriminatedByPath<IEvent, 'meta.unknown'>;\n\ntype INotPropertyKey = { readonly discriminator: { readonly nested: true } };\n// @ts-expect-error 判别字段的值是对象，不满足 PropertyKey 约束，无法作为映射类型的 key\ntype IInvalid2 = IDiscriminatedByField<'discriminator', INotPropertyKey>;"
              }
            ]
          },
          {
            "id": "config-模块",
            "heading": "/config 模块",
            "blocks": [
              {
                "type": "paragraph",
                "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
              },
              {
                "type": "paragraph",
                "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const CONFIG_DELETE: unique symbol;"
              },
              {
                "type": "paragraph",
                "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
              },
              {
                "type": "paragraph",
                "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
              },
              {
                "type": "list",
                "items": [
                  "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                  "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                  "`proto`/`prototype`/`constructor` 键一律拒绝；",
                  "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                ]
              },
              {
                "type": "paragraph",
                "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
              },
              {
                "type": "paragraph",
                "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
              },
              {
                "type": "paragraph",
                "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
              },
              {
                "type": "paragraph",
                "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
              },
              {
                "type": "paragraph",
                "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
              },
              {
                "type": "paragraph",
                "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
              },
              {
                "type": "paragraph",
                "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
              },
              {
                "type": "paragraph",
                "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
              },
              {
                "type": "paragraph",
                "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
              },
              {
                "type": "paragraph",
                "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
              },
              {
                "type": "paragraph",
                "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
              }
            ]
          },
          {
            "id": "function-模块",
            "heading": "/function 模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "const noop: () => undefined;"
              },
              {
                "type": "paragraph",
                "text": "恒返回 `undefined` 的空函数，用作默认回调占位符。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function once<T extends (...args: never[]) => unknown>(functionValue: T): T;"
              },
              {
                "type": "paragraph",
                "text": "记忆化一个同步函数：只在首次调用时真正执行，之后的调用直接返回首次的结果（或重新抛出首次的异常——包括异常也被缓存，不会在第二次调用时重新执行函数体）。若在首次调用尚未完成时发生重入（函数体内部再次调用了被包装后的自身），会抛出 `code: 'REENTRANT_CALL'` 的 `TypeError`，防止意外的递归自调用绕过缓存语义。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "// runtime.ts：整个模块只创建并导出这一份包装函数。\nexport const getRuntime = once(() => {\n  const registry = buildCommandRegistry();\n  return { registry, startedAt: Date.now() };\n});\n\nconst first = getRuntime(); // 创建 registry\nconst second = getRuntime(); // 直接返回 first\nfirst === second; // true"
              },
              {
                "type": "paragraph",
                "text": "适合模块内昂贵且确定的同步初始化，例如构建只读 registry、编译 schema 或探测一次运行时能力。首次异常也会永久缓存，所以需要 retry、reset、热更新或按请求隔离时不要使用。"
              },
              {
                "type": "paragraph",
                "text": "边界必须明确：每次调用 `once(fn)` 都产生新的包装函数和独立缓存。模块顶层只创建、只导出一个包装函数，可以实现“每个模块实例一份”；但重复 bundle、iframe、Worker 与 Node `vm` 会拥有不同模块实例，因此它不是跨 realm 的全局单例工具。真正需要同一 realm / 进程共享时，让应用入口或依赖注入容器持有实例；必须使用全局表时，可选 `globalThis[Symbol.for('your-app/runtime')]`，同时处理版本冲突、生命周期与测试清理。"
              },
              {
                "type": "paragraph",
                "text": "`Proxy` 适合把首次属性访问变成惰性初始化，或限制访问方式；它本身不提供唯一性。是否是单例仍由 Proxy 后面的实例存储位置和 owner 数量决定，因此无需为了“真实单例”给 `once` 结果额外套 Proxy。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "function onceAsync<T>(functionValue: () => Promise<T>): () => Promise<T>;"
              },
              {
                "type": "paragraph",
                "text": "记忆化一次异步调用：首次调用时启动 `functionValue()`，包装函数会保留并返回同一个 Promise；后续调用无论发生在 pending、fulfilled 还是 rejected 阶段，都直接拿到这个 Promise。`functionValue()` 可以返回原生 `Promise` 或合法 PromiseLike；其他值会让共享 Promise 以 `code: 'INVALID_ARGUMENT'` 的 `TypeError` reject。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "// app-config.ts：路由和埋点会同时读取启动配置，但网络请求只能发起一次。\nexport const loadAppConfig = onceAsync(async () => {\n  const response = await fetch('/config');\n  if (!response.ok) throw new Error(`config request failed: ${response.status}`);\n  return response.json() as Promise<{ apiBaseUrl: string }>;\n});\n\nconst [routerConfig, telemetryConfig] = await Promise.all([\n  loadAppConfig(),\n  loadAppConfig()\n]); // 两处共享同一次请求"
              },
              {
                "type": "paragraph",
                "text": "适合页面或进程生命周期内只应执行一次的异步初始化，例如加载不会刷新的启动配置、初始化共享 SDK 客户端、动态导入并编译同一份 WASM 模块。它同时解决“并发时别重复启动”和“完成后继续复用结果”两个问题。"
              },
              {
                "type": "paragraph",
                "text": "不适合按 key 去重、定时刷新、失败重试、登出后重建、按租户或请求隔离。首次 rejection 会一直缓存，API 没有 reset；若底层任务使用某个调用者的 `AbortSignal`，该调用者取消任务也会影响所有共享 Promise 的等待者。此类需求应交给带失效策略的缓存或生命周期容器。"
              },
              {
                "type": "paragraph",
                "text": "每次调用 `onceAsync(fn)` 都会创建独立缓存；模块被重复实例化，或代码分布在 iframe、Worker、Node `vm` 时不会共享，因此它也不是跨 realm 的全局任务协调器。"
              }
            ]
          },
          {
            "id": "collector",
            "heading": "Collector",
            "blocks": [
              {
                "type": "paragraph",
                "text": "Collector 是包根入口提供的、数组专用的惰性查询流水线："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type ICollectorActions<T, Self> = {\n  readonly result: readonly T[];\n  fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(\n    ...paths: P\n  ): IFieldCollector<T, P[number]>;\n  where(predicate: (source: T) => boolean): Self;\n  distinctBy<P extends IObjectPathInput<T>>(path: P): Self;\n  skip(count: number): Self;\n  take(count: number): Self;\n};\n\ninterface ICollector<T> extends ICollectorActions<T, ICollector<T>> {}\n\ninterface IFieldCollector<T, P extends IObjectPathInput<T>>\n  extends ICollectorActions<T, IFieldCollector<T, P>> {\n  like(query: string): IFieldCollector<T, P>;\n  equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>;\n  oneOf(\n    value: IObjectPathValue<T, P>,\n    ...values: IObjectPathValue<T, P>[]\n  ): IFieldCollector<T, P>;\n}\n\nfunction collect<T>(dataSource: readonly T[]): ICollector<T>;"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const collector = collect(users)\n  .fieldBy('profile.name-zh', 'profile.name-en')\n  .like(' World ')\n  .distinctBy('id')\n  .skip(10)\n  .take(20);\n\nconst result = collector.result;"
              },
              {
                "type": "paragraph",
                "text": "执行与缓存："
              },
              {
                "type": "list",
                "items": [
                  "`collect` 借用原 `readonly` 数组且不复制；无动作或全部 source 通过时，`result` 直接复用原数组。",
                  "每个动作立即写入有序语义流水线，但不会扫描 source；首次读取 `result` 时单次遍历并缓存。同 revision 重复读取返回同一引用，新增有效动作后缓存失效。",
                  "动作顺序可观察：`take(2).where(predicate)` 与 `where(predicate).take(2)` 不等价。连续链式调用不会生成每步中间数组。",
                  "求值失败不缓存；getter、Proxy 与 `where` 抛出的错误保持原始身份。求值回调中重读或修改同一 collector 会抛 `REENTRANT_CALL`。"
                ]
              },
              {
                "type": "paragraph",
                "text": "字段动作："
              },
              {
                "type": "list",
                "items": [
                  "`fieldBy` 立即解析路径，并加入“任一字段值非 `undefined`”过滤；missing、blocked 和显式 `undefined` 统一视为无有效值。重复调用会保留前一个过滤动作，同时替换后续字段谓词的活动字段域。",
                  "`like` 对 query 执行 `trim().toLowerCase()`；trim 后为空是 no-op。只对字符串字段进行忽略大小写的连续子串匹配，多个字段使用 OR。",
                  "`equals` 与 `oneOf` 使用 `Object.is`；多个字段使用 OR，多个动作按顺序组成 AND。`oneOf` 固定线性扫描候选值，不按数据量升级为索引。",
                  "`distinctBy` 只去重已定义 key，保留第一次出现；missing、blocked、显式 `undefined` 均不参与去重。",
                  "`where` 只接收 source，不注入 index。`skip`/`take` 只接受非负安全整数，否则抛出带 `INVALID_ARGUMENT` 的原生 `RangeError`。"
                ]
              },
              {
                "type": "paragraph",
                "text": "输入数组和嵌套 source 由调用方维持只读。Collector 不监听外部 mutation；外部绕过类型修改数据后，已缓存 revision 不会自动失效。"
              }
            ]
          },
          {
            "id": "组合工作流示例-截止时间-配置",
            "heading": "组合工作流示例：截止时间 + 配置",
            "blocks": [
              {
                "type": "paragraph",
                "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
              },
              {
                "type": "paragraph",
                "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
              }
            ]
          },
          {
            "id": "value-string-number-高频工具",
            "heading": "/value、/string、/number 高频工具",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`isNullish` 只识别 `null | undefined`；`isBlankString` 只识别 trim 后为空的字符串。`isEmptyValue` 组合两者并额外把 `NaN` 判为空；`0`、`0n`、`false`、数组和对象保持有效。`isPrimitive` 覆盖 JavaScript 七类 primitive，不生成额外类型标签。"
              },
              {
                "type": "paragraph",
                "text": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
              },
              {
                "type": "paragraph",
                "text": "`formatNumber`、`formatCurrency`、`formatPercent`、`formatInteger`、`formatCompactNumber` 共享一个 64 项有界 `Intl.NumberFormat` 缓存。高频表格、行情或库存循环使用 `createNumberFormatter(options)`，避免每项重复解析配置。所有 locale、币种、精度和舍入规则由调用方显式拥有。"
              }
            ]
          },
          {
            "id": "排查与构建门禁",
            "heading": "排查与构建门禁",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "超时没能真正打断 I/O：符合预期——协作式取消要求操作函数自己读取并响应传入的 `signal`；把 `signal` 透传给底层 I/O（例如 `fetch(url, { signal })`）并让其遵守中止。",
                  "`base64ToBytes` 拒绝了输入：把产出方规范化为标准补齐的 RFC 4648 文本；URL-safe（`-`/`_`）或无补齐的变体不被接受，需要先转换成标准形式。",
                  "写只读配置抛错：这是刻意的行为——`readonlyConfig` 就是用来防止意外修改的；需要变更时对原来的 owned config 调用 `patchConfig`，而不是修改只读对象。",
                  "`once` 报重入错误：说明首次调用尚未返回时，函数体内部又调用了同一个被包装的函数；把递归逻辑挪到包装函数之外，或改用普通函数自身递归。",
                  "需要\"当前值订阅/重放\"或资源释放生命周期：`@migaia/utils` 不提供响应式状态或资源所有权模型，应使用专门的响应式/生命周期管理包，而不是在这里找替代品。",
                  "`ownConfig`/`patchConfig` 抛 `CONFIG_LIMIT_EXCEEDED`：说明配置图的深度/节点数/键数超过了限制；确认是否存在非预期的深层嵌套或循环膨胀，必要时通过 `limits` 选项显式收紧或（在确认安全的前提下）放宽默认值。"
                ]
              },
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm run fmt && pnpm run lint && pnpm run typecheck && pnpm run typecheck:test && pnpm run test"
              }
            ]
          }
        ]
      }
    }
  },
  "apis": [
    {
      "id": "utils:index",
      "library": "utils",
      "module": "index",
      "exportPath": ".",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "collect",
          "kind": "function",
          "signature": "export declare function collect<T>(dataSource: readonly T[]): ICollector<T>;",
          "declarationLine": 27,
          "source": "packages/utils/dist/collector.d.ts",
          "purpose": "Creates a lazy collector over a borrowed readonly array.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for collect.",
          "advanced": "No additional advanced behavior is declared for collect.",
          "parameters": ["dataSource: readonly T[]"],
          "parameterDetails": [
            {
              "name": "dataSource",
              "type": "readonly T[]",
              "optional": false
            }
          ],
          "returns": "ICollector<T>",
          "errors": ["No documented errors are declared for collect."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for collect.",
          "examples": [
            "const collector = collect(users)\n  .fieldBy('profile.name-zh', 'profile.name-en')\n  .like(' World ')\n  .distinctBy('id')\n  .skip(10)\n  .take(20);\n\nconst result = collector.result;",
            "import { collect } from '@migaia/utils';\n\nconst collector = collect(users)\n  .fieldBy('profile.name-zh', 'profile.name-en')\n  .like(' world ')\n  .where((user) => user.active)\n  .take(20);\n\nconst result = collector.result;"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function collect<T>(dataSource: readonly T[]): ICollector<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "dataSource",
                "type": "readonly T[]",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["ICollector<T>"],
          "whenToUse": "Use collect when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when collect does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a lazy collector over a borrowed readonly array."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import collect from @migaia/utils, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use collect when its function contract and declared inputs match the result you need. Choose a different public symbol when collect does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for collect."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for collect."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/collector.d.ts:collect:export declare function collect<T>(dataSource: readonly T[]): ICollector<T>;",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 30,
          "configuration": [],
          "fragment": "utils-index--collect"
        },
        {
          "name": "ICollector",
          "kind": "interface",
          "signature": "export interface ICollector<T> extends ICollectorActions<T, ICollector<T>> { }",
          "declarationLine": 15,
          "source": "packages/utils/dist/collector.d.ts",
          "purpose": "Collector state before a field scope enables field-dependent predicates. An interface is required because TypeScript rejects the equivalent directly recursive alias.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ICollector.",
          "advanced": "No additional advanced behavior is declared for ICollector.",
          "parameters": ["This interface declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this interface.",
          "errors": ["No documented errors are declared for ICollector."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ICollector.",
          "examples": [
            "type ICollectorActions<T, Self> = {\n  readonly result: readonly T[];\n  fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(\n    ...paths: P\n  ): IFieldCollector<T, P[number]>;\n  where(predicate: (source: T) => boolean): Self;\n  distinctBy<P extends IObjectPathInput<T>>(path: P): Self;\n  skip(count: number): Self;\n  take(count: number): Self;\n};\n\ninterface ICollector<T> extends ICollectorActions<T, ICollector<T>> {}\n\ninterface IFieldCollector<T, P extends IObjectPathInput<T>>\n  extends ICollectorActions<T, IFieldCollector<T, P>> {\n  like(query: string): IFieldCollector<T, P>;\n  equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>;\n  oneOf(\n    value: IObjectPathValue<T, P>,\n    ...values: IObjectPathValue<T, P>[]\n  ): IFieldCollector<T, P>;\n}\n\nfunction collect<T>(dataSource: readonly T[]): ICollector<T>;"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export interface ICollector<T> extends ICollectorActions<T, ICollector<T>> { }"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ICollector when its interface contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ICollector does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Collector state before a field scope enables field-dependent predicates. An interface is required because TypeScript rejects the equivalent directly recursive alias."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ICollector from @migaia/utils, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ICollector when its interface contract and declared inputs match the result you need. Choose a different public symbol when ICollector does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained interface example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ICollector."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ICollector."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/collector.d.ts:ICollector:export interface ICollector<T> extends ICollectorActions<T, ICollector<T>> { }",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-index--ICollector"
        },
        {
          "name": "ICollectorActions",
          "kind": "type",
          "signature": "export type ICollectorActions<T, Self> = { readonly result: readonly T[]; fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(...paths: P): IFieldCollector<T, P[number]>; where(predicate: (source: T) => boolean): Self; distinctBy<P extends IObjectPathInput<T>>(path: P): Self; skip(count: number): Self; take(count: number): Self; };",
          "declarationLine": 3,
          "source": "packages/utils/dist/collector.d.ts",
          "purpose": "Actions available before and after a field scope is selected.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ICollectorActions.",
          "advanced": "No additional advanced behavior is declared for ICollectorActions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ICollectorActions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ICollectorActions.",
          "examples": [
            "type ICollectorActions<T, Self> = {\n  readonly result: readonly T[];\n  fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(\n    ...paths: P\n  ): IFieldCollector<T, P[number]>;\n  where(predicate: (source: T) => boolean): Self;\n  distinctBy<P extends IObjectPathInput<T>>(path: P): Self;\n  skip(count: number): Self;\n  take(count: number): Self;\n};\n\ninterface ICollector<T> extends ICollectorActions<T, ICollector<T>> {}\n\ninterface IFieldCollector<T, P extends IObjectPathInput<T>>\n  extends ICollectorActions<T, IFieldCollector<T, P>> {\n  like(query: string): IFieldCollector<T, P>;\n  equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>;\n  oneOf(\n    value: IObjectPathValue<T, P>,\n    ...values: IObjectPathValue<T, P>[]\n  ): IFieldCollector<T, P>;\n}\n\nfunction collect<T>(dataSource: readonly T[]): ICollector<T>;"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export type ICollectorActions<T, Self> = { readonly result: readonly T[]; fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(...paths: P): IFieldCollector<T, P[number]>; where(predicate: (source: T) => boolean): Self; distinctBy<P extends IObjectPathInput<T>>(path: P): Self; skip(count: number): Self; take(count: number): Self; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ICollectorActions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ICollectorActions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Actions available before and after a field scope is selected."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ICollectorActions from @migaia/utils, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ICollectorActions when its type contract and declared inputs match the result you need. Choose a different public symbol when ICollectorActions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ICollectorActions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ICollectorActions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/collector.d.ts:ICollectorActions:export type ICollectorActions<T, Self> = { readonly result: readonly T[]; fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(...paths: P): IFieldCollector<T, P[number]>; where(predicate: (source: T) => boolean): Self; distinctBy<P extends IObjectPathInput<T>>(path: P): Self; skip(count: number): Self; take(count: number): Self; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-index--ICollectorActions"
        },
        {
          "name": "IFieldCollector",
          "kind": "interface",
          "signature": "export interface IFieldCollector<T, P extends IObjectPathInput<T>> extends ICollectorActions<T, IFieldCollector<T, P>> { like(query: string): IFieldCollector<T, P>; equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>; oneOf(value: IObjectPathValue<T, P>, ...values: IObjectPathValue<T, P>[]): IFieldCollector<T, P>; }",
          "declarationLine": 21,
          "source": "packages/utils/dist/collector.d.ts",
          "purpose": "Collector state whose values are inferred from the latest `fieldBy`. An interface is required to preserve its recursive fluent return type.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IFieldCollector.",
          "advanced": "No additional advanced behavior is declared for IFieldCollector.",
          "parameters": ["This interface declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this interface.",
          "errors": ["No documented errors are declared for IFieldCollector."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IFieldCollector.",
          "examples": [
            "type ICollectorActions<T, Self> = {\n  readonly result: readonly T[];\n  fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(\n    ...paths: P\n  ): IFieldCollector<T, P[number]>;\n  where(predicate: (source: T) => boolean): Self;\n  distinctBy<P extends IObjectPathInput<T>>(path: P): Self;\n  skip(count: number): Self;\n  take(count: number): Self;\n};\n\ninterface ICollector<T> extends ICollectorActions<T, ICollector<T>> {}\n\ninterface IFieldCollector<T, P extends IObjectPathInput<T>>\n  extends ICollectorActions<T, IFieldCollector<T, P>> {\n  like(query: string): IFieldCollector<T, P>;\n  equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>;\n  oneOf(\n    value: IObjectPathValue<T, P>,\n    ...values: IObjectPathValue<T, P>[]\n  ): IFieldCollector<T, P>;\n}\n\nfunction collect<T>(dataSource: readonly T[]): ICollector<T>;"
          ],
          "members": [],
          "exportPath": ".",
          "overloadSignatures": [
            "export interface IFieldCollector<T, P extends IObjectPathInput<T>> extends ICollectorActions<T, IFieldCollector<T, P>> { like(query: string): IFieldCollector<T, P>; equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>; oneOf(value: IObjectPathValue<T, P>, ...values: IObjectPathValue<T, P>[]): IFieldCollector<T, P>; }"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IFieldCollector when its interface contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IFieldCollector does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Collector state whose values are inferred from the latest `fieldBy`. An interface is required to preserve its recursive fluent return type."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IFieldCollector from @migaia/utils, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IFieldCollector when its interface contract and declared inputs match the result you need. Choose a different public symbol when IFieldCollector does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained interface example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IFieldCollector."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IFieldCollector."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/collector.d.ts:IFieldCollector:export interface IFieldCollector<T, P extends IObjectPathInput<T>> extends ICollectorActions<T, IFieldCollector<T, P>> { like(query: string): IFieldCollector<T, P>; equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>; oneOf(value: IObjectPathValue<T, P>, ...values: IObjectPathValue<T, P>[]): IFieldCollector<T, P>; }",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "utils-index--IFieldCollector"
        }
      ],
      "aliases": [
        {
          "identity": "packages/utils/dist/promise.d.ts:admitAbortSignal:export declare function admitAbortSignal(candidate: unknown): IAbortSignalAdmission;",
          "name": "admitAbortSignal",
          "signature": "export declare function admitAbortSignal(candidate: unknown): IAbortSignalAdmission;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--admitAbortSignal"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:assimilateCapturedThen:export declare function assimilateCapturedThen<T>(thenFn: (resolve: unknown, reject: unknown) => void, thenable: unknown): Promise<T>;",
          "name": "assimilateCapturedThen",
          "signature": "export declare function assimilateCapturedThen<T>(thenFn: (resolve: unknown, reject: unknown) => void, thenable: unknown): Promise<T>;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--assimilateCapturedThen"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:attachErrorIdentity:export declare function attachErrorIdentity<T extends object>(error: T, identity: IErrorIdentity, hooks?: IErrorIdentityHooks<T>): T;",
          "name": "attachErrorIdentity",
          "signature": "export declare function attachErrorIdentity<T extends object>(error: T, identity: IErrorIdentity, hooks?: IErrorIdentityHooks<T>): T;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--attachErrorIdentity"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:attachSecondaryErrors:export declare function attachSecondaryErrors(primary: unknown, secondaryErrors: readonly unknown[]): unknown;",
          "name": "attachSecondaryErrors",
          "signature": "export declare function attachSecondaryErrors(primary: unknown, secondaryErrors: readonly unknown[]): unknown;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--attachSecondaryErrors"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:base64ToBytes:export declare function base64ToBytes(value: string): Uint8Array;",
          "name": "base64ToBytes",
          "signature": "export declare function base64ToBytes(value: string): Uint8Array;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--base64ToBytes"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:bytesToBase64:export declare function bytesToBase64(value: Uint8Array): string;",
          "name": "bytesToBase64",
          "signature": "export declare function bytesToBase64(value: Uint8Array): string;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--bytesToBase64"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:combineConfig:export declare function combineConfig(sources: readonly IOwnedConfig<IConfigRecord>[], options?: IConfigCombineOptions): IOwnedConfig<IConfigRecord>;",
          "name": "combineConfig",
          "signature": "export declare function combineConfig(sources: readonly IOwnedConfig<IConfigRecord>[], options?: IConfigCombineOptions): IOwnedConfig<IConfigRecord>;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--combineConfig"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:combineErrors:export declare function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;",
          "name": "combineErrors",
          "signature": "export declare function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--combineErrors"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:CONFIG_DELETE:export declare const CONFIG_DELETE: unique symbol;",
          "name": "CONFIG_DELETE",
          "signature": "export declare const CONFIG_DELETE: unique symbol;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--CONFIG_DELETE"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:ConfigProfile:export declare const ConfigProfile: { readonly data: \"data\"; readonly richRuntime: \"richRuntime\"; };",
          "name": "ConfigProfile",
          "signature": "export declare const ConfigProfile: { readonly data: \"data\"; readonly richRuntime: \"richRuntime\"; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--ConfigProfile"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:createAbortTimeoutSignal:export declare function createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;",
          "name": "createAbortTimeoutSignal",
          "signature": "export declare function createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--createAbortTimeoutSignal"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:createConcurrencyLimiter:export declare function createConcurrencyLimiter(options: { readonly concurrency: number; readonly report?: IUtilsReporter; }): IConcurrencyLimiter;",
          "name": "createConcurrencyLimiter",
          "signature": "export declare function createConcurrencyLimiter(options: { readonly concurrency: number; readonly report?: IUtilsReporter; }): IConcurrencyLimiter;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--createConcurrencyLimiter"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:createManualScheduler:export declare function createManualScheduler(): IManualScheduler;",
          "name": "createManualScheduler",
          "signature": "export declare function createManualScheduler(): IManualScheduler;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--createManualScheduler"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:createNumberFormatter:export declare function createNumberFormatter(options?: INumberFormatOptions): INumberFormatter;",
          "name": "createNumberFormatter",
          "signature": "export declare function createNumberFormatter(options?: INumberFormatOptions): INumberFormatter;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--createNumberFormatter"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:createPathAccessor:export declare function createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
          "name": "createPathAccessor",
          "signature": "export declare function createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--createPathAccessor"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:decodeUtf8:export declare function decodeUtf8(value: Uint8Array, options?: { readonly fatal?: boolean; }): string;",
          "name": "decodeUtf8",
          "signature": "export declare function decodeUtf8(value: Uint8Array, options?: { readonly fatal?: boolean; }): string;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--decodeUtf8"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:deferred:export declare function deferred<T>(): IDeferred<T>;",
          "name": "deferred",
          "signature": "export declare function deferred<T>(): IDeferred<T>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--deferred"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:diagnosticSnapshot:export declare function diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "name": "diagnosticSnapshot",
          "signature": "export declare function diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--diagnosticSnapshot"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:encodeUtf8:export declare function encodeUtf8(value: string): Uint8Array;",
          "name": "encodeUtf8",
          "signature": "export declare function encodeUtf8(value: string): Uint8Array;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--encodeUtf8"
        },
        {
          "identity": "packages/utils/dist/string.d.ts:format:export declare function format(source: string, values: Readonly<Record<string, unknown>>, options?: IFormatOptions): string;",
          "name": "format",
          "signature": "export declare function format(source: string, values: Readonly<Record<string, unknown>>, options?: IFormatOptions): string;",
          "source": "packages/utils/dist/string.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./string",
          "ownerModule": "string",
          "ownerFragment": "utils-string--format"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:formatCompactNumber:export declare function formatCompactNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "name": "formatCompactNumber",
          "signature": "export declare function formatCompactNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--formatCompactNumber"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:formatCurrency:export declare function formatCurrency(value: INumericValue, currency: string, options?: INumberFormatOptions): string;",
          "name": "formatCurrency",
          "signature": "export declare function formatCurrency(value: INumericValue, currency: string, options?: INumberFormatOptions): string;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--formatCurrency"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:formatInteger:export declare function formatInteger(value: INumericValue, options?: INumberFormatOptions): string;",
          "name": "formatInteger",
          "signature": "export declare function formatInteger(value: INumericValue, options?: INumberFormatOptions): string;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--formatInteger"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:formatNumber:export declare function formatNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "name": "formatNumber",
          "signature": "export declare function formatNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--formatNumber"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:formatPercent:export declare function formatPercent(value: INumericValue, options?: INumberFormatOptions): string;",
          "name": "formatPercent",
          "signature": "export declare function formatPercent(value: INumericValue, options?: INumberFormatOptions): string;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--formatPercent"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:get:export declare function get<T, P extends IObjectPathInput<T>>(object: T, path: P): IObjectPathValue<T, P> | undefined;",
          "name": "get",
          "signature": "export declare function get<T, P extends IObjectPathInput<T>>(object: T, path: P): IObjectPathValue<T, P> | undefined;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--get"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:hostRethrowReporter:export declare const hostRethrowReporter: IUtilsReporter;",
          "name": "hostRethrowReporter",
          "signature": "export declare const hostRethrowReporter: IUtilsReporter;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--hostRethrowReporter"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortControllerLike:export type IAbortControllerLike = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "name": "IAbortControllerLike",
          "signature": "export type IAbortControllerLike = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortControllerLike"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortOperation:export type IAbortOperation<T> = ITimeoutOperation<T>;",
          "name": "IAbortOperation",
          "signature": "export type IAbortOperation<T> = ITimeoutOperation<T>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortOperation"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortSignal:export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "name": "IAbortSignal",
          "signature": "export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortSignal"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortSignalAdmission:export type IAbortSignalAdmission = { readonly kind: 'valid'; readonly signal: IAbortSignal; readonly aborted: boolean; } | { readonly kind: 'invalid'; readonly reason: IAbortSignalRejection; readonly cause?: unknown; };",
          "name": "IAbortSignalAdmission",
          "signature": "export type IAbortSignalAdmission = { readonly kind: 'valid'; readonly signal: IAbortSignal; readonly aborted: boolean; } | { readonly kind: 'invalid'; readonly reason: IAbortSignalRejection; readonly cause?: unknown; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortSignalAdmission"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortSignalRejection:export type IAbortSignalRejection = 'not-object' | 'aborted-not-boolean' | 'aborted-threw' | 'listener-not-function';",
          "name": "IAbortSignalRejection",
          "signature": "export type IAbortSignalRejection = 'not-object' | 'aborted-not-boolean' | 'aborted-threw' | 'listener-not-function';",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortSignalRejection"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortTimeoutSignal:export type IAbortTimeoutSignal = { readonly signal: IAbortSignal | undefined; readonly dispose: () => void; };",
          "name": "IAbortTimeoutSignal",
          "signature": "export type IAbortTimeoutSignal = { readonly signal: IAbortSignal | undefined; readonly dispose: () => void; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortTimeoutSignal"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortTimeoutSignalOptions:export type IAbortTimeoutSignalOptions = { readonly signal?: IAbortSignal; readonly timeoutMs?: number; readonly scheduler?: IUtilsScheduler; readonly timeoutReason?: () => unknown; readonly report?: IUtilsReporter; };",
          "name": "IAbortTimeoutSignalOptions",
          "signature": "export type IAbortTimeoutSignalOptions = { readonly signal?: IAbortSignal; readonly timeoutMs?: number; readonly scheduler?: IUtilsScheduler; readonly timeoutReason?: () => unknown; readonly report?: IUtilsReporter; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortTimeoutSignalOptions"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IAsyncControls:export type IAsyncControls = { readonly signal?: IAbortSignal; readonly signals?: readonly IAbortSignal[]; readonly scheduler?: IUtilsScheduler; readonly unref?: boolean; };",
          "name": "IAsyncControls",
          "signature": "export type IAsyncControls = { readonly signal?: IAbortSignal; readonly signals?: readonly IAbortSignal[]; readonly scheduler?: IUtilsScheduler; readonly unref?: boolean; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAsyncControls"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IConcurrencyLimiter:export type IConcurrencyLimiter = { run<T>(task: (context: { readonly signal?: IAbortSignal; }) => T | PromiseLike<T>, options?: { readonly signal?: IAbortSignal; }): Promise<T>; readonly activeCount: number; readonly pendingCount: number; whenIdle(): Promise<void>; close(reason?: unknown): void; dispose(reason?: unknown): Promise<void>; };",
          "name": "IConcurrencyLimiter",
          "signature": "export type IConcurrencyLimiter = { run<T>(task: (context: { readonly signal?: IAbortSignal; }) => T | PromiseLike<T>, options?: { readonly signal?: IAbortSignal; }): Promise<T>; readonly activeCount: number; readonly pendingCount: number; whenIdle(): Promise<void>; close(reason?: unknown): void; dispose(reason?: unknown): Promise<void>; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IConcurrencyLimiter"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigCombineOptions:export type IConfigCombineOptions = IOwnConfigOptions & { readonly strategies?: Partial<IConfigMergeStrategies>; readonly pathRules?: readonly IConfigPathRule[]; readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision; };",
          "name": "IConfigCombineOptions",
          "signature": "export type IConfigCombineOptions = IOwnConfigOptions & { readonly strategies?: Partial<IConfigMergeStrategies>; readonly pathRules?: readonly IConfigPathRule[]; readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigCombineOptions"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigConflictContext:export type IConfigConflictContext = { readonly path: readonly PropertyKey[]; readonly left: unknown; readonly right: unknown; };",
          "name": "IConfigConflictContext",
          "signature": "export type IConfigConflictContext = { readonly path: readonly PropertyKey[]; readonly left: unknown; readonly right: unknown; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigConflictContext"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigConflictDecision:export type IConfigConflictDecision = { readonly kind: 'left'; } | { readonly kind: 'right'; } | { readonly kind: 'delete'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "name": "IConfigConflictDecision",
          "signature": "export type IConfigConflictDecision = { readonly kind: 'left'; } | { readonly kind: 'right'; } | { readonly kind: 'delete'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigConflictDecision"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigLimits:export type IConfigLimits = { readonly maxDepth: number; readonly maxNodes: number; readonly maxKeys: number; readonly maxPathLength: number; readonly maxSegmentLength: number; };",
          "name": "IConfigLimits",
          "signature": "export type IConfigLimits = { readonly maxDepth: number; readonly maxNodes: number; readonly maxKeys: number; readonly maxPathLength: number; readonly maxSegmentLength: number; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigLimits"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigMergeStrategies:export type IConfigMergeStrategies = { readonly record: 'merge' | 'replace'; readonly array: 'replace' | 'concat' | 'mergeByIndex'; readonly map: 'replace' | 'merge'; readonly set: 'replace' | 'union'; readonly undefined: 'ignore' | 'assign'; };",
          "name": "IConfigMergeStrategies",
          "signature": "export type IConfigMergeStrategies = { readonly record: 'merge' | 'replace'; readonly array: 'replace' | 'concat' | 'mergeByIndex'; readonly map: 'replace' | 'merge'; readonly set: 'replace' | 'union'; readonly undefined: 'ignore' | 'assign'; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigMergeStrategies"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigPatch:export type IConfigPatch<T extends IConfigRecord> = { readonly [K in keyof T]?: T[K] | typeof CONFIG_DELETE; } & Readonly<Record<PropertyKey, unknown>>;",
          "name": "IConfigPatch",
          "signature": "export type IConfigPatch<T extends IConfigRecord> = { readonly [K in keyof T]?: T[K] | typeof CONFIG_DELETE; } & Readonly<Record<PropertyKey, unknown>>;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigPatch"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigPatchOptions:export type IConfigPatchOptions = IOwnConfigOptions & { readonly reuseUnchangedRoot?: boolean; };",
          "name": "IConfigPatchOptions",
          "signature": "export type IConfigPatchOptions = IOwnConfigOptions & { readonly reuseUnchangedRoot?: boolean; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigPatchOptions"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigPathRule:export type IConfigPathRule = { readonly prefix: readonly PropertyKey[]; readonly strategies: Partial<IConfigMergeStrategies>; };",
          "name": "IConfigPathRule",
          "signature": "export type IConfigPathRule = { readonly prefix: readonly PropertyKey[]; readonly strategies: Partial<IConfigMergeStrategies>; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigPathRule"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigProfile:export type IConfigProfile = (typeof ConfigProfile)[keyof typeof ConfigProfile];",
          "name": "IConfigProfile",
          "signature": "export type IConfigProfile = (typeof ConfigProfile)[keyof typeof ConfigProfile];",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigProfile"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigReadResult:export type IConfigReadResult = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "name": "IConfigReadResult",
          "signature": "export type IConfigReadResult = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigReadResult"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IConfigRecord:export type IConfigRecord = Record<PropertyKey, unknown>;",
          "name": "IConfigRecord",
          "signature": "export type IConfigRecord = Record<PropertyKey, unknown>;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IConfigRecord"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IDeferred:export type IDeferred<T> = { readonly promise: Promise<T>; readonly resolve: (value: T | PromiseLike<T>) => void; readonly reject: (reason?: unknown) => void; };",
          "name": "IDeferred",
          "signature": "export type IDeferred<T> = { readonly promise: Promise<T>; readonly resolve: (value: T | PromiseLike<T>) => void; readonly reject: (reason?: unknown) => void; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IDeferred"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:identitySnapshot:export declare function identitySnapshot<T>(value: T): T;",
          "name": "identitySnapshot",
          "signature": "export declare function identitySnapshot<T>(value: T): T;",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--identitySnapshot"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:IDiagnosticSnapshot:export type IDiagnosticSnapshot<T> = { readonly value: T; readonly diagnostics: readonly ISnapshotDiagnostic[]; };",
          "name": "IDiagnosticSnapshot",
          "signature": "export type IDiagnosticSnapshot<T> = { readonly value: T; readonly diagnostics: readonly ISnapshotDiagnostic[]; };",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--IDiagnosticSnapshot"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:IErrorIdentity:export type IErrorIdentity = { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; };",
          "name": "IErrorIdentity",
          "signature": "export type IErrorIdentity = { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; };",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--IErrorIdentity"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:IErrorIdentityHooks:export type IErrorIdentityHooks<T extends object> = { readonly onConflict?: (context: { readonly target: T; readonly key: string; readonly existing: unknown; readonly incoming: unknown; }) => T; readonly onFailure?: (context: { readonly target: T; readonly key: string; readonly incoming: unknown; readonly cause: unknown; }) => T; };",
          "name": "IErrorIdentityHooks",
          "signature": "export type IErrorIdentityHooks<T extends object> = { readonly onConflict?: (context: { readonly target: T; readonly key: string; readonly existing: unknown; readonly incoming: unknown; }) => T; readonly onFailure?: (context: { readonly target: T; readonly key: string; readonly incoming: unknown; readonly cause: unknown; }) => T; };",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--IErrorIdentityHooks"
        },
        {
          "identity": "packages/utils/dist/string.d.ts:IFormatOptions:export type IFormatOptions = { readonly placeholder?: IFormatPlaceholder; readonly missing?: 'preserve' | 'empty' | 'throw'; readonly nullish?: 'empty' | 'stringify'; };",
          "name": "IFormatOptions",
          "signature": "export type IFormatOptions = { readonly placeholder?: IFormatPlaceholder; readonly missing?: 'preserve' | 'empty' | 'throw'; readonly nullish?: 'empty' | 'stringify'; };",
          "source": "packages/utils/dist/string.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./string",
          "ownerModule": "string",
          "ownerFragment": "utils-string--IFormatOptions"
        },
        {
          "identity": "packages/utils/dist/string.d.ts:IFormatPlaceholder:export type IFormatPlaceholder = { readonly open: string; readonly close: string; };",
          "name": "IFormatPlaceholder",
          "signature": "export type IFormatPlaceholder = { readonly open: string; readonly close: string; };",
          "source": "packages/utils/dist/string.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./string",
          "ownerModule": "string",
          "ownerFragment": "utils-string--IFormatPlaceholder"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IManualScheduler:export type IManualScheduler = IUtilsScheduler & { advance(ms: number): void; readonly pendingCount: number; };",
          "name": "IManualScheduler",
          "signature": "export type IManualScheduler = IUtilsScheduler & { advance(ms: number): void; readonly pendingCount: number; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IManualScheduler"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:immutableSnapshot:export declare function immutableSnapshot<T>(value: T): T;",
          "name": "immutableSnapshot",
          "signature": "export declare function immutableSnapshot<T>(value: T): T;",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--immutableSnapshot"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:inspectThenable:export declare function inspectThenable(value: unknown): IThenableInspection;",
          "name": "inspectThenable",
          "signature": "export declare function inspectThenable(value: unknown): IThenableInspection;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--inspectThenable"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:INumberFormatOptions:export type INumberFormatOptions = { /** Locale preference passed through canonical `Intl.getCanonicalLocales` normalization. */ readonly locales?: Intl.LocalesArgument; /** Native `Intl.NumberFormat` policy merged with the helper-specific defaults. */ readonly format?: Intl.NumberFormatOptions; };",
          "name": "INumberFormatOptions",
          "signature": "export type INumberFormatOptions = { /** Locale preference passed through canonical `Intl.getCanonicalLocales` normalization. */ readonly locales?: Intl.LocalesArgument; /** Native `Intl.NumberFormat` policy merged with the helper-specific defaults. */ readonly format?: Intl.NumberFormatOptions; };",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--INumberFormatOptions"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:INumberFormatter:export type INumberFormatter = (value: INumericValue) => string;",
          "name": "INumberFormatter",
          "signature": "export type INumberFormatter = (value: INumericValue) => string;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--INumberFormatter"
        },
        {
          "identity": "packages/utils/dist/number.d.ts:INumericValue:export type INumericValue = number | bigint;",
          "name": "INumericValue",
          "signature": "export type INumericValue = number | bigint;",
          "source": "packages/utils/dist/number.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./number",
          "ownerModule": "number",
          "ownerFragment": "utils-number--INumericValue"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPath:export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "name": "IObjectPath",
          "signature": "export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPath"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathInput:export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "name": "IObjectPathInput",
          "signature": "export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathInput"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathSegment:export type IObjectPathSegment = string | number | symbol;",
          "name": "IObjectPathSegment",
          "signature": "export type IObjectPathSegment = string | number | symbol;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathSegment"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTuple:export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "name": "IObjectPathTuple",
          "signature": "export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathTuple"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTupleFor:export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "name": "IObjectPathTupleFor",
          "signature": "export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathTupleFor"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathValue:export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "name": "IObjectPathValue",
          "signature": "export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathValue"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathWriteValue:export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "name": "IObjectPathWriteValue",
          "signature": "export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathWriteValue"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IOwnConfigOptions:export type IOwnConfigOptions = { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits>; };",
          "name": "IOwnConfigOptions",
          "signature": "export type IOwnConfigOptions = { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits>; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IOwnConfigOptions"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:IOwnDescriptorsSnapshot:export type IOwnDescriptorsSnapshot = { readonly kind: 'captured'; readonly ok: true; readonly descriptors: Readonly<Record<PropertyKey, PropertyDescriptor>>; } | { readonly kind: 'failed'; readonly ok: false; readonly error: unknown; };",
          "name": "IOwnDescriptorsSnapshot",
          "signature": "export type IOwnDescriptorsSnapshot = { readonly kind: 'captured'; readonly ok: true; readonly descriptors: Readonly<Record<PropertyKey, PropertyDescriptor>>; } | { readonly kind: 'failed'; readonly ok: false; readonly error: unknown; };",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--IOwnDescriptorsSnapshot"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:IOwnedConfig:export type IOwnedConfig<T extends IConfigRecord> = T & { readonly __utilsOwnedConfigBrand?: unique symbol; };",
          "name": "IOwnedConfig",
          "signature": "export type IOwnedConfig<T extends IConfigRecord> = T & { readonly __utilsOwnedConfigBrand?: unique symbol; };",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--IOwnedConfig"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathAccessor:export type IPathAccessor<T> = { readonly value: T; get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined; set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T; probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>; parsePath(path: string | IObjectPathTuple): IObjectPathTuple; };",
          "name": "IPathAccessor",
          "signature": "export type IPathAccessor<T> = { readonly value: T; get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined; set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T; probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>; parsePath(path: string | IObjectPathTuple): IObjectPathTuple; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathAccessor"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathAccessorOptions:export type IPathAccessorOptions<T> = { readonly ifMissing?: (probe: IPathMissingProbe) => void; readonly ifBlocked?: (probe: IPathBlockedProbe) => void; readonly ifFailed?: (probe: IPathFailedProbe) => void; readonly onGet?: (event: IStringGetEvent<T> | ITupleGetEvent<T>) => void; readonly onSet?: (event: IStringSetEvent<T> | ITupleSetEvent<T>) => void; };",
          "name": "IPathAccessorOptions",
          "signature": "export type IPathAccessorOptions<T> = { readonly ifMissing?: (probe: IPathMissingProbe) => void; readonly ifBlocked?: (probe: IPathBlockedProbe) => void; readonly ifFailed?: (probe: IPathFailedProbe) => void; readonly onGet?: (event: IStringGetEvent<T> | ITupleGetEvent<T>) => void; readonly onSet?: (event: IStringSetEvent<T> | ITupleSetEvent<T>) => void; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathAccessorOptions"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathBlockedProbe:export type IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'blocked'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "name": "IPathBlockedProbe",
          "signature": "export type IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'blocked'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathBlockedProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathFailedProbe:export type IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'failed'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; readonly error: unknown; };",
          "name": "IPathFailedProbe",
          "signature": "export type IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'failed'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; readonly error: unknown; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathFailedProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathGetEvent:export type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathValue<T, P>; replace(value: IObjectPathValue<T, P>): void; };",
          "name": "IPathGetEvent",
          "signature": "export type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathValue<T, P>; replace(value: IObjectPathValue<T, P>): void; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathGetEvent"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathMissingProbe:export type IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'missing'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "name": "IPathMissingProbe",
          "signature": "export type IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'missing'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathMissingProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathProbe:export type IPathProbe<T, P extends IObjectPathInput<T>> = IPathValueProbe<T, P> | IPathMissingProbe<P> | IPathBlockedProbe<P> | IPathFailedProbe<P>;",
          "name": "IPathProbe",
          "signature": "export type IPathProbe<T, P extends IObjectPathInput<T>> = IPathValueProbe<T, P> | IPathMissingProbe<P> | IPathBlockedProbe<P> | IPathFailedProbe<P>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathSetEvent:export type IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathWriteValue<T, P>; replace(value: IObjectPathWriteValue<T, P>): void; };",
          "name": "IPathSetEvent",
          "signature": "export type IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathWriteValue<T, P>; replace(value: IObjectPathWriteValue<T, P>): void; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathSetEvent"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathValueProbe:export type IPathValueProbe<T, P extends IObjectPathInput<T>> = { readonly kind: 'value'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly value: IObjectPathValue<T, P>; };",
          "name": "IPathValueProbe",
          "signature": "export type IPathValueProbe<T, P extends IObjectPathInput<T>> = { readonly kind: 'value'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly value: IObjectPathValue<T, P>; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathValueProbe"
        },
        {
          "identity": "packages/utils/dist/value.d.ts:IPrimitive:export type IPrimitive = undefined | null | string | number | bigint | boolean | symbol;",
          "name": "IPrimitive",
          "signature": "export type IPrimitive = undefined | null | string | number | bigint | boolean | symbol;",
          "source": "packages/utils/dist/value.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./value",
          "ownerModule": "value",
          "ownerFragment": "utils-value--IPrimitive"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:IProbePropertyResult:export type IProbePropertyResult<T> = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: T; } | { readonly kind: 'failed'; readonly error: unknown; };",
          "name": "IProbePropertyResult",
          "signature": "export type IProbePropertyResult<T> = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: T; } | { readonly kind: 'failed'; readonly error: unknown; };",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--IProbePropertyResult"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IRetryContext:export type IRetryContext = { readonly attempt: number; readonly signal: IAbortSignal; readonly remainingMs?: number; };",
          "name": "IRetryContext",
          "signature": "export type IRetryContext = { readonly attempt: number; readonly signal: IAbortSignal; readonly remainingMs?: number; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IRetryContext"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IRetryFailureContext:export type IRetryFailureContext = IRetryContext & { readonly maxAttempts: number; };",
          "name": "IRetryFailureContext",
          "signature": "export type IRetryFailureContext = IRetryContext & { readonly maxAttempts: number; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IRetryFailureContext"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:isArrayBuffer:export declare const isArrayBuffer: (value: unknown) => value is ArrayBuffer;",
          "name": "isArrayBuffer",
          "signature": "export declare const isArrayBuffer: (value: unknown) => value is ArrayBuffer;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--isArrayBuffer"
        },
        {
          "identity": "packages/utils/dist/value.d.ts:isBlankString:export declare const isBlankString: (value: unknown) => value is string;",
          "name": "isBlankString",
          "signature": "export declare const isBlankString: (value: unknown) => value is string;",
          "source": "packages/utils/dist/value.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./value",
          "ownerModule": "value",
          "ownerFragment": "utils-value--isBlankString"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IScheduledTask:export type IScheduledTask = { cancel(): void; unref?(): void; };",
          "name": "IScheduledTask",
          "signature": "export type IScheduledTask = { cancel(): void; unref?(): void; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IScheduledTask"
        },
        {
          "identity": "packages/utils/dist/value.d.ts:isEmptyValue:export declare const isEmptyValue: (value: unknown) => value is null | undefined | string | number;",
          "name": "isEmptyValue",
          "signature": "export declare const isEmptyValue: (value: unknown) => value is null | undefined | string | number;",
          "source": "packages/utils/dist/value.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./value",
          "ownerModule": "value",
          "ownerFragment": "utils-value--isEmptyValue"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:ISnapshotDiagnostic:export type ISnapshotDiagnostic = { readonly path: readonly PropertyKey[]; readonly reason: 'accessor' | 'read-failed' | 'unsupported'; readonly cause: unknown; };",
          "name": "ISnapshotDiagnostic",
          "signature": "export type ISnapshotDiagnostic = { readonly path: readonly PropertyKey[]; readonly reason: 'accessor' | 'read-failed' | 'unsupported'; readonly cause: unknown; };",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--ISnapshotDiagnostic"
        },
        {
          "identity": "packages/utils/dist/value.d.ts:isNullish:export declare const isNullish: (value: unknown) => value is null | undefined;",
          "name": "isNullish",
          "signature": "export declare const isNullish: (value: unknown) => value is null | undefined;",
          "source": "packages/utils/dist/value.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./value",
          "ownerModule": "value",
          "ownerFragment": "utils-value--isNullish"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:isPlainObject:export declare function isPlainObject(value: unknown): value is Record<PropertyKey, unknown>;",
          "name": "isPlainObject",
          "signature": "export declare function isPlainObject(value: unknown): value is Record<PropertyKey, unknown>;",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--isPlainObject"
        },
        {
          "identity": "packages/utils/dist/value.d.ts:isPrimitive:export declare const isPrimitive: (value: unknown) => value is IPrimitive;",
          "name": "isPrimitive",
          "signature": "export declare const isPrimitive: (value: unknown) => value is IPrimitive;",
          "source": "packages/utils/dist/value.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./value",
          "ownerModule": "value",
          "ownerFragment": "utils-value--isPrimitive"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:isUint8Array:export declare const isUint8Array: (value: unknown) => value is Uint8Array;",
          "name": "isUint8Array",
          "signature": "export declare const isUint8Array: (value: unknown) => value is Uint8Array;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--isUint8Array"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:isUtilsAbortError:export declare function isUtilsAbortError(value: unknown): value is UtilsAbortError;",
          "name": "isUtilsAbortError",
          "signature": "export declare function isUtilsAbortError(value: unknown): value is UtilsAbortError;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--isUtilsAbortError"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:isUtilsError:export declare function isUtilsError(value: unknown): value is UtilsError;",
          "name": "isUtilsError",
          "signature": "export declare function isUtilsError(value: unknown): value is UtilsError;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--isUtilsError"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:isUtilsTimeoutError:export declare function isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;",
          "name": "isUtilsTimeoutError",
          "signature": "export declare function isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--isUtilsTimeoutError"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:IThenableInspection:export type IThenableInspection = { readonly then: undefined; } | { readonly then: (resolve: unknown, reject: unknown) => void; } | { readonly error: unknown; };",
          "name": "IThenableInspection",
          "signature": "export type IThenableInspection = { readonly then: undefined; } | { readonly then: (resolve: unknown, reject: unknown) => void; } | { readonly error: unknown; };",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--IThenableInspection"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:IThenableProbe:export type IThenableProbe = { readonly kind: typeof ThenableProbeKind.notThenable; } | { readonly kind: typeof ThenableProbeKind.thenable; readonly thenFn: (resolve: unknown, reject: unknown) => void; } | { readonly kind: typeof ThenableProbeKind.failed; readonly error: unknown; };",
          "name": "IThenableProbe",
          "signature": "export type IThenableProbe = { readonly kind: typeof ThenableProbeKind.notThenable; } | { readonly kind: typeof ThenableProbeKind.thenable; readonly thenFn: (resolve: unknown, reject: unknown) => void; } | { readonly kind: typeof ThenableProbeKind.failed; readonly error: unknown; };",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--IThenableProbe"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:IThenableProbeKind:export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];",
          "name": "IThenableProbeKind",
          "signature": "export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--IThenableProbeKind"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:ITimeoutOperation:export type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal; }) => T | PromiseLike<T>;",
          "name": "ITimeoutOperation",
          "signature": "export type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal; }) => T | PromiseLike<T>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--ITimeoutOperation"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IUtilsReporter:export type IUtilsReporter = (error: unknown, context: { readonly operation: 'abort-timeout-signal' | 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter'; readonly phase: 'late-rejection' | 'cleanup' | 'reporter'; readonly attempt?: number; }) => void;",
          "name": "IUtilsReporter",
          "signature": "export type IUtilsReporter = (error: unknown, context: { readonly operation: 'abort-timeout-signal' | 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter'; readonly phase: 'late-rejection' | 'cleanup' | 'reporter'; readonly attempt?: number; }) => void;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IUtilsReporter"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IUtilsScheduler:export type IUtilsScheduler = { now(): number; schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "name": "IUtilsScheduler",
          "signature": "export type IUtilsScheduler = { now(): number; schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IUtilsScheduler"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:MAX_NATIVE_RECURSION_DEPTH:export declare const MAX_NATIVE_RECURSION_DEPTH = 256;",
          "name": "MAX_NATIVE_RECURSION_DEPTH",
          "signature": "export declare const MAX_NATIVE_RECURSION_DEPTH = 256;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--MAX_NATIVE_RECURSION_DEPTH"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:noop:export declare const noop: () => undefined;",
          "name": "noop",
          "signature": "export declare const noop: () => undefined;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--noop"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:observeThenableRejection:export declare function observeThenableRejection(value: unknown, inspection: IThenableInspection, onRejected: (error: unknown) => void): void;",
          "name": "observeThenableRejection",
          "signature": "export declare function observeThenableRejection(value: unknown, inspection: IThenableInspection, onRejected: (error: unknown) => void): void;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--observeThenableRejection"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:once:export declare function once<T extends (...args: never[]) => unknown>(functionValue: T): T;",
          "name": "once",
          "signature": "export declare function once<T extends (...args: never[]) => unknown>(functionValue: T): T;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--once"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:onceAsync:export declare function onceAsync<T>(functionValue: () => Promise<T>): () => Promise<T>;",
          "name": "onceAsync",
          "signature": "export declare function onceAsync<T>(functionValue: () => Promise<T>): () => Promise<T>;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--onceAsync"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:ownConfig:export declare function ownConfig<T extends IConfigRecord>(value: T, options?: IOwnConfigOptions): IOwnedConfig<T>;",
          "name": "ownConfig",
          "signature": "export declare function ownConfig<T extends IConfigRecord>(value: T, options?: IOwnConfigOptions): IOwnedConfig<T>;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--ownConfig"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:parseConfigPath:export declare function parseConfigPath(path: string, limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>): readonly string[];",
          "name": "parseConfigPath",
          "signature": "export declare function parseConfigPath(path: string, limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>): readonly string[];",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--parseConfigPath"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:parseObjectPath:export declare function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;",
          "name": "parseObjectPath",
          "signature": "export declare function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--parseObjectPath"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:patchConfig:export declare function patchConfig<T extends IConfigRecord>(base: IOwnedConfig<T>, patch: IConfigPatch<T>, options?: IConfigPatchOptions): IOwnedConfig<T>;",
          "name": "patchConfig",
          "signature": "export declare function patchConfig<T extends IConfigRecord>(base: IOwnedConfig<T>, patch: IConfigPatch<T>, options?: IConfigPatchOptions): IOwnedConfig<T>;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--patchConfig"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:probeObjectPath:export declare function probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;",
          "name": "probeObjectPath",
          "signature": "export declare function probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--probeObjectPath"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:probeObjectPathSegments:export declare function probeObjectPathSegments<T, P extends IObjectPathInput<T>>(object: T, segments: P): IPathProbe<T, P>;",
          "name": "probeObjectPathSegments",
          "signature": "export declare function probeObjectPathSegments<T, P extends IObjectPathInput<T>>(object: T, segments: P): IPathProbe<T, P>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--probeObjectPathSegments"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:probeProperty:export declare function probeProperty<T>(value: object, key: PropertyKey): IProbePropertyResult<T>;",
          "name": "probeProperty",
          "signature": "export declare function probeProperty<T>(value: object, key: PropertyKey): IProbePropertyResult<T>;",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--probeProperty"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:probeThenable:export declare function probeThenable(value: unknown): IThenableProbe;",
          "name": "probeThenable",
          "signature": "export declare function probeThenable(value: unknown): IThenableProbe;",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--probeThenable"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:raceWithAbort:export declare function raceWithAbort<T>(operation: IAbortOperation<T>, options?: Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; }): Promise<T>;",
          "name": "raceWithAbort",
          "signature": "export declare function raceWithAbort<T>(operation: IAbortOperation<T>, options?: Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; }): Promise<T>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--raceWithAbort"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:readConfigPath:export declare function readConfigPath(config: IOwnedConfig<IConfigRecord>, path: string | readonly string[]): IConfigReadResult;",
          "name": "readConfigPath",
          "signature": "export declare function readConfigPath(config: IOwnedConfig<IConfigRecord>, path: string | readonly string[]): IConfigReadResult;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--readConfigPath"
        },
        {
          "identity": "packages/utils/dist/config.d.ts:readonlyConfig:export declare function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;",
          "name": "readonlyConfig",
          "signature": "export declare function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;",
          "source": "packages/utils/dist/config.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./config",
          "ownerModule": "config",
          "ownerFragment": "utils-config--readonlyConfig"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:retry:export declare function retry<T>(operation: (context: IRetryContext) => T | PromiseLike<T>, options: Parameters<typeof retryCore<T>>[1]): Promise<T>;",
          "name": "retry",
          "signature": "export declare function retry<T>(operation: (context: IRetryContext) => T | PromiseLike<T>, options: Parameters<typeof retryCore<T>>[1]): Promise<T>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--retry"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:safeErrorReason:export declare function safeErrorReason(error: unknown, fallback: string): string;",
          "name": "safeErrorReason",
          "signature": "export declare function safeErrorReason(error: unknown, fallback: string): string;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--safeErrorReason"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:set:export declare function set<T, P extends IObjectPathInput<T>>(object: T, path: P, nextValue: IObjectPathWriteValue<T, P>): T;",
          "name": "set",
          "signature": "export declare function set<T, P extends IObjectPathInput<T>>(object: T, path: P, nextValue: IObjectPathWriteValue<T, P>): T;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--set"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:sleep:export declare function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;",
          "name": "sleep",
          "signature": "export declare function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--sleep"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:snapshotOwnDescriptors:export declare function snapshotOwnDescriptors(value: unknown): IOwnDescriptorsSnapshot;",
          "name": "snapshotOwnDescriptors",
          "signature": "export declare function snapshotOwnDescriptors(value: unknown): IOwnDescriptorsSnapshot;",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--snapshotOwnDescriptors"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:splitUtf8:export declare function splitUtf8(value: string, maxBytes: number): readonly string[];",
          "name": "splitUtf8",
          "signature": "export declare function splitUtf8(value: string, maxBytes: number): readonly string[];",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--splitUtf8"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:streamBase64Chunks:export declare function streamBase64Chunks(value: Uint8Array, maxChunkBytes?: number): Iterable<string>;",
          "name": "streamBase64Chunks",
          "signature": "export declare function streamBase64Chunks(value: Uint8Array, maxChunkBytes?: number): Iterable<string>;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--streamBase64Chunks"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:structuredDiagnosticSnapshot:export declare function structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "name": "structuredDiagnosticSnapshot",
          "signature": "export declare function structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--structuredDiagnosticSnapshot"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:systemScheduler:export declare const systemScheduler: IUtilsScheduler;",
          "name": "systemScheduler",
          "signature": "export declare const systemScheduler: IUtilsScheduler;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--systemScheduler"
        },
        {
          "identity": "packages/utils/dist/function.d.ts:ThenableProbeKind:export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; };",
          "name": "ThenableProbeKind",
          "signature": "export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; };",
          "source": "packages/utils/dist/function.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./function",
          "ownerModule": "function",
          "ownerFragment": "utils-function--ThenableProbeKind"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:toError:export declare function toError(value: unknown, options?: { readonly message?: string; }): Error;",
          "name": "toError",
          "signature": "export declare function toError(value: unknown, options?: { readonly message?: string; }): Error;",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--toError"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:toPromise:export declare function toPromise<T>(run: () => T): Promise<Awaited<T>>;",
          "name": "toPromise",
          "signature": "export declare function toPromise<T>(run: () => T): Promise<Awaited<T>>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--toPromise"
        },
        {
          "identity": "packages/utils/dist/bytes.d.ts:utf8ByteLength:export declare function utf8ByteLength(value: string): number;",
          "name": "utf8ByteLength",
          "signature": "export declare function utf8ByteLength(value: string): number;",
          "source": "packages/utils/dist/bytes.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./bytes",
          "ownerModule": "bytes",
          "ownerFragment": "utils-bytes--utf8ByteLength"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:UtilsAbortError:export declare class UtilsAbortError extends UtilsError { readonly name: \"AbortError\"; constructor(reason?: unknown); }",
          "name": "UtilsAbortError",
          "signature": "export declare class UtilsAbortError extends UtilsError { readonly name: \"AbortError\"; constructor(reason?: unknown); }",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--UtilsAbortError"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:UtilsError:export declare abstract class UtilsError extends Error { readonly source: \"@migaia/utils\"; readonly code: IUtilsErrorCode; protected constructor(code: IUtilsErrorCode, message: string, options?: { readonly cause?: unknown; }); }",
          "name": "UtilsError",
          "signature": "export declare abstract class UtilsError extends Error { readonly source: \"@migaia/utils\"; readonly code: IUtilsErrorCode; protected constructor(code: IUtilsErrorCode, message: string, options?: { readonly cause?: unknown; }); }",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--UtilsError"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:UtilsTimeoutError:export declare class UtilsTimeoutError extends UtilsError { readonly scope: 'operation' | 'attempt' | 'total'; readonly timeoutMs: number; readonly name: \"TimeoutError\"; constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number); }",
          "name": "UtilsTimeoutError",
          "signature": "export declare class UtilsTimeoutError extends UtilsError { readonly scope: 'operation' | 'attempt' | 'total'; readonly timeoutMs: number; readonly name: \"TimeoutError\"; constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number); }",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--UtilsTimeoutError"
        },
        {
          "identity": "packages/utils/dist/error.d.ts:walkErrorCauses:export declare function walkErrorCauses(error: unknown, options?: { readonly maxDepth?: number; }): readonly unknown[];",
          "name": "walkErrorCauses",
          "signature": "export declare function walkErrorCauses(error: unknown, options?: { readonly maxDepth?: number; }): readonly unknown[];",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--walkErrorCauses"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:withTimeout:export declare function withTimeout<T>(operation: ITimeoutOperation<T>, options: IWithTimeoutOptions): Promise<T>;",
          "name": "withTimeout",
          "signature": "export declare function withTimeout<T>(operation: ITimeoutOperation<T>, options: IWithTimeoutOptions): Promise<T>;",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--withTimeout"
        }
      ]
    },
    {
      "id": "utils:bytes",
      "library": "utils",
      "module": "bytes",
      "exportPath": "./bytes",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "base64ToBytes",
          "kind": "function",
          "signature": "export declare function base64ToBytes(value: string): Uint8Array;",
          "declarationLine": 14,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Decodes only canonical RFC 4648 Base64 text.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for base64ToBytes.",
          "advanced": "No additional advanced behavior is declared for base64ToBytes.",
          "parameters": ["value: string"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "Uint8Array",
          "errors": ["No documented errors are declared for base64ToBytes."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for base64ToBytes.",
          "examples": [
            "bytesToBase64(new Uint8Array([1, 2, 3])); // 'AQID'\nbase64ToBytes('AQID'); // Uint8Array [1, 2, 3]\nbase64ToBytes('AQID-_'); // 抛 TypeError（URL-safe 字符不被接受）",
            "bytesToBase64(new Uint8Array([1, 2, 3])); // 'AQID'\nbase64ToBytes('AQID'); // Uint8Array [1, 2, 3]（只接受规范 RFC 4648 形式，非规范输入抛 TypeError）"
          ],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": [
            "export declare function base64ToBytes(value: string): Uint8Array;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Uint8Array"],
          "whenToUse": "Use base64ToBytes when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when base64ToBytes does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Decodes only canonical RFC 4648 Base64 text."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import base64ToBytes from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use base64ToBytes when its function contract and declared inputs match the result you need. Choose a different public symbol when base64ToBytes does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for base64ToBytes."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for base64ToBytes."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:base64ToBytes:export declare function base64ToBytes(value: string): Uint8Array;",
          "guidance": [
            {
              "id": "utilserrorcode",
              "heading": "UtilsErrorCode",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const UtilsErrorCode = {\n  invalidArgument: 'INVALID_ARGUMENT',\n  nonErrorValue: 'NON_ERROR_VALUE',\n  envUnsupported: 'ENV_UNSUPPORTED',\n  aborted: 'ABORTED',\n  deadlineExceeded: 'DEADLINE_EXCEEDED',\n  schedulerRunaway: 'SCHEDULER_RUNAWAY',\n  errorIdentityConflict: 'ERROR_IDENTITY_CONFLICT',\n  cloneUnsupported: 'CLONE_UNSUPPORTED',\n  invalidEncoding: 'INVALID_ENCODING',\n  limiterClosed: 'LIMITER_CLOSED',\n  reentrantCall: 'REENTRANT_CALL',\n  configUnsupported: 'CONFIG_UNSUPPORTED',\n  configReadonly: 'CONFIG_READONLY',\n  configConflict: 'CONFIG_CONFLICT',\n  configLimitExceeded: 'CONFIG_LIMIT_EXCEEDED',\n  configPathInvalid: 'CONFIG_PATH_INVALID',\n  objectPathInvalid: 'OBJECT_PATH_INVALID',\n  formatInvalid: 'FORMAT_INVALID',\n  formatValueMissing: 'FORMAT_VALUE_MISSING',\n  numberFormatInvalid: 'NUMBER_FORMAT_INVALID'\n} as const;"
                },
                {
                  "type": "paragraph",
                  "text": "全包统一使用的稳定语义码，`IUtilsErrorCode` 是其取值的联合类型。除 `UtilsError` 子类外，`base64ToBytes`/`object-path`/`config` 等模块抛出的原生 `TypeError`/`RangeError` 也会通过 `Object.defineProperty` 挂上对应的 `code`（与 `source: '@migaia/utils'`），可用同一套判断逻辑识别。"
                },
                {
                  "type": "paragraph",
                  "text": "按用途理解全部错误码："
                },
                {
                  "type": "list",
                  "items": [
                    "`INVALID_ARGUMENT`：参数类型、范围或互斥组合错误；修正调用参数。",
                    "`NON_ERROR_VALUE`：非 `Error` 异常值已被 `toError()` 转换；从 `cause` 读取原始值。",
                    "`ENV_UNSUPPORTED`：宿主缺少 `structuredClone` 等必要能力；更换环境或实现。",
                    "`ABORTED`：外部信号请求取消；停止协作式工作并传播取消原因。",
                    "`DEADLINE_EXCEEDED`：操作、尝试或总流程超时；结合 `scope`、`timeoutMs` 选择重试或降级。",
                    "`SCHEDULER_RUNAWAY`：虚拟时钟一次推进超过 10000 个任务；排查同刻递归调度。",
                    "`ERROR_IDENTITY_CONFLICT`：已有错误身份与新 `source`/`code` 冲突；保留原身份。",
                    "`CLONE_UNSUPPORTED`：值不能被 `structuredClone` 复制；先转换为可克隆数据。",
                    "`INVALID_ENCODING`：编码输入不是规范形式；按错误偏移修正输入。",
                    "`LIMITER_CLOSED`：并发限制器已关闭；停止提交或创建新实例。",
                    "`REENTRANT_CALL`：同一实例发生禁止的重入；等待当前回调结束。",
                    "`CONFIG_UNSUPPORTED`：配置含不支持的数据形状或属性；改用普通数据。",
                    "`CONFIG_READONLY`：尝试修改只读配置；从所有者侧创建新配置。",
                    "`CONFIG_CONFLICT`：配置来源的所有权、profile 或描述符冲突；统一契约再合并。",
                    "`CONFIG_LIMIT_EXCEEDED`：配置键数、深度等超过安全上限；缩小输入或调整明确限制。",
                    "`CONFIG_PATH_INVALID`：配置路径为空、危险或分段无效；改用安全路径。",
                    "`OBJECT_PATH_INVALID`：对象路径无法安全解析；修正字符串或分段元组。",
                    "`FORMAT_INVALID`：模板、占位符或值不能安全格式化；修正模板或输入。",
                    "`FORMAT_VALUE_MISSING`：严格格式化缺少占位符值；补齐字段或调整策略。",
                    "`NUMBER_FORMAT_INVALID`：`Intl.NumberFormat` 拒绝 locale、选项、货币或数值；修正格式配置。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 38,
          "configuration": [],
          "fragment": "utils-bytes--base64ToBytes"
        },
        {
          "name": "bytesToBase64",
          "kind": "function",
          "signature": "export declare function bytesToBase64(value: Uint8Array): string;",
          "declarationLine": 12,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Encodes bytes using the canonical RFC 4648 alphabet.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for bytesToBase64.",
          "advanced": "No additional advanced behavior is declared for bytesToBase64.",
          "parameters": ["value: Uint8Array"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "Uint8Array",
              "optional": false
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for bytesToBase64."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for bytesToBase64.",
          "examples": [
            "bytesToBase64(new Uint8Array([1, 2, 3])); // 'AQID'\nbase64ToBytes('AQID'); // Uint8Array [1, 2, 3]\nbase64ToBytes('AQID-_'); // 抛 TypeError（URL-safe 字符不被接受）",
            "bytesToBase64(new Uint8Array([1, 2, 3])); // 'AQID'\nbase64ToBytes('AQID'); // Uint8Array [1, 2, 3]（只接受规范 RFC 4648 形式，非规范输入抛 TypeError）"
          ],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": [
            "export declare function bytesToBase64(value: Uint8Array): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "Uint8Array",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use bytesToBase64 when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when bytesToBase64 does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Encodes bytes using the canonical RFC 4648 alphabet."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import bytesToBase64 from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use bytesToBase64 when its function contract and declared inputs match the result you need. Choose a different public symbol when bytesToBase64 does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for bytesToBase64."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for bytesToBase64."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:bytesToBase64:export declare function bytesToBase64(value: Uint8Array): string;",
          "guidance": [],
          "usageScore": 26,
          "configuration": [],
          "fragment": "utils-bytes--bytesToBase64"
        },
        {
          "name": "decodeUtf8",
          "kind": "function",
          "signature": "export declare function decodeUtf8(value: Uint8Array, options?: { readonly fatal?: boolean; }): string;",
          "declarationLine": 22,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Decodes UTF-8 bytes using replacement or fatal mode.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for decodeUtf8.",
          "advanced": "No additional advanced behavior is declared for decodeUtf8.",
          "parameters": ["value: Uint8Array", "options: { readonly fatal?: boolean; } (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "Uint8Array",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly fatal?: boolean; }",
              "optional": true
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for decodeUtf8."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for decodeUtf8.",
          "examples": [
            "decodeUtf8(bytes); // '你好'\ndecodeUtf8(badBytes, { fatal: true }); // 非法字节序列时抛错，而不是替换成 U+FFFD"
          ],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": [
            "export declare function decodeUtf8(value: Uint8Array, options?: { readonly fatal?: boolean; }): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "Uint8Array",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly fatal?: boolean; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use decodeUtf8 when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when decodeUtf8 does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Decodes UTF-8 bytes using replacement or fatal mode."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import decodeUtf8 from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use decodeUtf8 when its function contract and declared inputs match the result you need. Choose a different public symbol when decodeUtf8 does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for decodeUtf8."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for decodeUtf8."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:decodeUtf8:export declare function decodeUtf8(value: Uint8Array, options?: { readonly fatal?: boolean; }): string;",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "utils-bytes--decodeUtf8"
        },
        {
          "name": "encodeUtf8",
          "kind": "function",
          "signature": "export declare function encodeUtf8(value: string): Uint8Array;",
          "declarationLine": 20,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Encodes text as a newly allocated UTF-8 byte array.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for encodeUtf8.",
          "advanced": "No additional advanced behavior is declared for encodeUtf8.",
          "parameters": ["value: string"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "Uint8Array",
          "errors": ["No documented errors are declared for encodeUtf8."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for encodeUtf8.",
          "examples": ["const bytes = encodeUtf8('你好');"],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": ["export declare function encodeUtf8(value: string): Uint8Array;"],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Uint8Array"],
          "whenToUse": "Use encodeUtf8 when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when encodeUtf8 does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Encodes text as a newly allocated UTF-8 byte array."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import encodeUtf8 from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use encodeUtf8 when its function contract and declared inputs match the result you need. Choose a different public symbol when encodeUtf8 does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for encodeUtf8."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for encodeUtf8."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:encodeUtf8:export declare function encodeUtf8(value: string): Uint8Array;",
          "guidance": [],
          "usageScore": 26,
          "configuration": [],
          "fragment": "utils-bytes--encodeUtf8"
        },
        {
          "name": "isArrayBuffer",
          "kind": "const",
          "signature": "export declare const isArrayBuffer: (value: unknown) => value is ArrayBuffer;",
          "declarationLine": 10,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Recognizes ArrayBuffer internal slots across realms, including detached buffers, without accepting SharedArrayBuffer, proxies, or objects that imitate the public constructor shape.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isArrayBuffer.",
          "advanced": "No additional advanced behavior is declared for isArrayBuffer.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for isArrayBuffer."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isArrayBuffer.",
          "examples": [
            "function isUint8Array(value: unknown): value is Uint8Array;\nfunction isArrayBuffer(value: unknown): value is ArrayBuffer;",
            "import {\n  isArrayBuffer,\n  isUint8Array,\n  bytesToBase64,\n  base64ToBytes,\n  streamBase64Chunks,\n  utf8ByteLength,\n  encodeUtf8,\n  decodeUtf8,\n  splitUtf8\n} from '@migaia/utils/bytes';",
            "isUint8Array(new Uint8Array()); // true\nisArrayBuffer(new ArrayBuffer(1)); // true\nisUint8Array(new Uint8ClampedArray(1)); // false\nisArrayBuffer(new SharedArrayBuffer(1)); // false"
          ],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": [
            "export declare const isArrayBuffer: (value: unknown) => value is ArrayBuffer;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use isArrayBuffer when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isArrayBuffer does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Recognizes ArrayBuffer internal slots across realms, including detached buffers, without accepting SharedArrayBuffer, proxies, or objects that imitate the public constructor shape."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isArrayBuffer from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isArrayBuffer when its const contract and declared inputs match the result you need. Choose a different public symbol when isArrayBuffer does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isArrayBuffer."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isArrayBuffer."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:isArrayBuffer:export declare const isArrayBuffer: (value: unknown) => value is ArrayBuffer;",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "utils-bytes--isArrayBuffer"
        },
        {
          "name": "isUint8Array",
          "kind": "const",
          "signature": "export declare const isUint8Array: (value: unknown) => value is Uint8Array;",
          "declarationLine": 5,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Recognizes Uint8Array internal slots across realms and subclasses without trusting mutable prototypes, constructor names, or Symbol.toStringTag properties supplied by the value.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUint8Array.",
          "advanced": "No additional advanced behavior is declared for isUint8Array.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for isUint8Array."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isUint8Array.",
          "examples": [
            "function isUint8Array(value: unknown): value is Uint8Array;\nfunction isArrayBuffer(value: unknown): value is ArrayBuffer;",
            "import {\n  isArrayBuffer,\n  isUint8Array,\n  bytesToBase64,\n  base64ToBytes,\n  streamBase64Chunks,\n  utf8ByteLength,\n  encodeUtf8,\n  decodeUtf8,\n  splitUtf8\n} from '@migaia/utils/bytes';",
            "isUint8Array(new Uint8Array()); // true\nisArrayBuffer(new ArrayBuffer(1)); // true\nisUint8Array(new Uint8ClampedArray(1)); // false\nisArrayBuffer(new SharedArrayBuffer(1)); // false"
          ],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": [
            "export declare const isUint8Array: (value: unknown) => value is Uint8Array;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use isUint8Array when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isUint8Array does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Recognizes Uint8Array internal slots across realms and subclasses without trusting mutable prototypes, constructor names, or Symbol.toStringTag properties supplied by the value."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isUint8Array from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isUint8Array when its const contract and declared inputs match the result you need. Choose a different public symbol when isUint8Array does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUint8Array."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isUint8Array."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:isUint8Array:export declare const isUint8Array: (value: unknown) => value is Uint8Array;",
          "guidance": [],
          "usageScore": 24,
          "configuration": [],
          "fragment": "utils-bytes--isUint8Array"
        },
        {
          "name": "splitUtf8",
          "kind": "function",
          "signature": "export declare function splitUtf8(value: string, maxBytes: number): readonly string[];",
          "declarationLine": 26,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Splits UTF-8 text into encoded chunks without exceeding maxBytes.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for splitUtf8.",
          "advanced": "No additional advanced behavior is declared for splitUtf8.",
          "parameters": ["value: string", "maxBytes: number"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "string",
              "optional": false
            },
            {
              "name": "maxBytes",
              "type": "number",
              "optional": false
            }
          ],
          "returns": "readonly string[]",
          "errors": ["No documented errors are declared for splitUtf8."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for splitUtf8.",
          "examples": [
            "splitUtf8('你好世界', 6); // 每个汉字 3 字节 → ['你好', '世界']",
            "splitUtf8('你好世界', 6); // ['你好', '世界']"
          ],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": [
            "export declare function splitUtf8(value: string, maxBytes: number): readonly string[];"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "string",
                "optional": false
              },
              {
                "name": "maxBytes",
                "type": "number",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["readonly string[]"],
          "whenToUse": "Use splitUtf8 when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when splitUtf8 does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Splits UTF-8 text into encoded chunks without exceeding maxBytes."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import splitUtf8 from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use splitUtf8 when its function contract and declared inputs match the result you need. Choose a different public symbol when splitUtf8 does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for splitUtf8."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for splitUtf8."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:splitUtf8:export declare function splitUtf8(value: string, maxBytes: number): readonly string[];",
          "guidance": [],
          "usageScore": 26,
          "configuration": [],
          "fragment": "utils-bytes--splitUtf8"
        },
        {
          "name": "streamBase64Chunks",
          "kind": "function",
          "signature": "export declare function streamBase64Chunks(value: Uint8Array, maxChunkBytes?: number): Iterable<string>;",
          "declarationLine": 16,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Encodes a byte sequence as canonical chunks with a bounded chunk size.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for streamBase64Chunks.",
          "advanced": "No additional advanced behavior is declared for streamBase64Chunks.",
          "parameters": ["value: Uint8Array", "maxChunkBytes: number (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "Uint8Array",
              "optional": false
            },
            {
              "name": "maxChunkBytes",
              "type": "number",
              "optional": true
            }
          ],
          "returns": "Iterable<string>",
          "errors": ["No documented errors are declared for streamBase64Chunks."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for streamBase64Chunks.",
          "examples": [
            "function* streamBase64Chunks(value: Uint8Array, maxChunkBytes?: number): Iterable<string>;",
            "for (const chunk of streamBase64Chunks(bigBytes, 4096)) send(chunk);"
          ],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": [
            "export declare function streamBase64Chunks(value: Uint8Array, maxChunkBytes?: number): Iterable<string>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "Uint8Array",
                "optional": false
              },
              {
                "name": "maxChunkBytes",
                "type": "number",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["Iterable<string>"],
          "whenToUse": "Use streamBase64Chunks when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when streamBase64Chunks does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Encodes a byte sequence as canonical chunks with a bounded chunk size."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import streamBase64Chunks from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use streamBase64Chunks when its function contract and declared inputs match the result you need. Choose a different public symbol when streamBase64Chunks does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for streamBase64Chunks."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for streamBase64Chunks."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:streamBase64Chunks:export declare function streamBase64Chunks(value: Uint8Array, maxChunkBytes?: number): Iterable<string>;",
          "guidance": [],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-bytes--streamBase64Chunks"
        },
        {
          "name": "utf8ByteLength",
          "kind": "function",
          "signature": "export declare function utf8ByteLength(value: string): number;",
          "declarationLine": 18,
          "source": "packages/utils/dist/bytes.d.ts",
          "purpose": "Returns the UTF-8 byte length without allocating an encoded buffer.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for utf8ByteLength.",
          "advanced": "No additional advanced behavior is declared for utf8ByteLength.",
          "parameters": ["value: string"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "number",
          "errors": ["No documented errors are declared for utf8ByteLength."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for utf8ByteLength.",
          "examples": ["utf8ByteLength('你好'); // 6"],
          "members": [],
          "exportPath": "./bytes",
          "overloadSignatures": ["export declare function utf8ByteLength(value: string): number;"],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["number"],
          "whenToUse": "Use utf8ByteLength when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when utf8ByteLength does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Returns the UTF-8 byte length without allocating an encoded buffer."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import utf8ByteLength from @migaia/utils/bytes, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use utf8ByteLength when its function contract and declared inputs match the result you need. Choose a different public symbol when utf8ByteLength does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for utf8ByteLength."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for utf8ByteLength."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/bytes.d.ts:utf8ByteLength:export declare function utf8ByteLength(value: string): number;",
          "guidance": [],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-bytes--utf8ByteLength"
        }
      ],
      "aliases": []
    },
    {
      "id": "utils:config",
      "library": "utils",
      "module": "config",
      "exportPath": "./config",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "combineConfig",
          "kind": "function",
          "signature": "export declare function combineConfig(sources: readonly IOwnedConfig<IConfigRecord>[], options?: IConfigCombineOptions): IOwnedConfig<IConfigRecord>;",
          "declarationLine": 76,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "Combines owned roots in source order using explicit record conflict policy.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for combineConfig.",
          "advanced": "No additional advanced behavior is declared for combineConfig.",
          "parameters": [
            "sources: readonly IOwnedConfig<IConfigRecord>[]",
            "options: IConfigCombineOptions (optional)"
          ],
          "parameterDetails": [
            {
              "name": "sources",
              "type": "readonly IOwnedConfig<IConfigRecord>[]",
              "optional": false
            },
            {
              "name": "options",
              "type": "IConfigCombineOptions",
              "optional": true
            }
          ],
          "returns": "IOwnedConfig<IConfigRecord>",
          "errors": ["No documented errors are declared for combineConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for combineConfig.",
          "examples": [
            "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export declare function combineConfig(sources: readonly IOwnedConfig<IConfigRecord>[], options?: IConfigCombineOptions): IOwnedConfig<IConfigRecord>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "sources",
                "type": "readonly IOwnedConfig<IConfigRecord>[]",
                "optional": false
              },
              {
                "name": "options",
                "type": "IConfigCombineOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IOwnedConfig<IConfigRecord>"],
          "whenToUse": "Use combineConfig when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when combineConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Combines owned roots in source order using explicit record conflict policy."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import combineConfig from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use combineConfig when its function contract and declared inputs match the result you need. Choose a different public symbol when combineConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for combineConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for combineConfig."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "__utilsOwnedConfigBrand",
              "type": "unique symbol",
              "optional": true,
              "description": ""
            },
            {
              "name": "strategies",
              "type": "Partial<IConfigMergeStrategies>",
              "optional": true,
              "description": ""
            },
            {
              "name": "pathRules",
              "type": "readonly IConfigPathRule[]",
              "optional": true,
              "description": ""
            },
            {
              "name": "onConflict",
              "type": "(context: IConfigConflictContext) => IConfigConflictDecision",
              "optional": true,
              "description": ""
            },
            {
              "name": "profile",
              "type": "IConfigProfile",
              "optional": true,
              "description": ""
            },
            {
              "name": "limits",
              "type": "Partial<IConfigLimits>",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/config.d.ts:combineConfig:export declare function combineConfig(sources: readonly IOwnedConfig<IConfigRecord>[], options?: IConfigCombineOptions): IOwnedConfig<IConfigRecord>;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            }
          ],
          "usageScore": 50,
          "configuration": [
            {
              "name": "strategies",
              "type": "Partial<IConfigMergeStrategies>",
              "optional": true,
              "description": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。",
              "descriptionEn": "",
              "descriptionZh": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
            },
            {
              "name": "pathRules",
              "type": "readonly IConfigPathRule[]",
              "optional": true,
              "description": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。",
              "descriptionEn": "",
              "descriptionZh": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
            },
            {
              "name": "onConflict",
              "type": "(context: IConfigConflictContext) => IConfigConflictDecision",
              "optional": true,
              "description": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。",
              "descriptionEn": "",
              "descriptionZh": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
            },
            {
              "name": "profile",
              "type": "IConfigProfile",
              "optional": true,
              "description": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。",
              "descriptionEn": "",
              "descriptionZh": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
            },
            {
              "name": "limits",
              "type": "Partial<IConfigLimits>",
              "optional": true,
              "description": "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。",
              "descriptionEn": "",
              "descriptionZh": "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
            }
          ],
          "fragment": "utils-config--combineConfig"
        },
        {
          "name": "CONFIG_DELETE",
          "kind": "const",
          "signature": "export declare const CONFIG_DELETE: unique symbol;",
          "declarationLine": 6,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "CONFIG_DELETE is part of this module's public const contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for CONFIG_DELETE.",
          "advanced": "No additional advanced behavior is declared for CONFIG_DELETE.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for CONFIG_DELETE."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for CONFIG_DELETE.",
          "examples": [
            "const CONFIG_DELETE: unique symbol;",
            "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';",
            "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": ["export declare const CONFIG_DELETE: unique symbol;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use CONFIG_DELETE when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when CONFIG_DELETE does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "CONFIG_DELETE is part of this module's public const contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import CONFIG_DELETE from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use CONFIG_DELETE when its const contract and declared inputs match the result you need. Choose a different public symbol when CONFIG_DELETE does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for CONFIG_DELETE."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for CONFIG_DELETE."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:CONFIG_DELETE:export declare const CONFIG_DELETE: unique symbol;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "组合工作流示例-截止时间-配置",
              "heading": "组合工作流示例：截止时间 + 配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                },
                {
                  "type": "paragraph",
                  "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            },
            {
              "id": "高阶组合示例-1-截止时间-重试-只读配置",
              "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                }
              ]
            }
          ],
          "usageScore": 62,
          "configuration": [],
          "fragment": "utils-config--CONFIG_DELETE"
        },
        {
          "name": "ConfigProfile",
          "kind": "const",
          "signature": "export declare const ConfigProfile: { readonly data: \"data\"; readonly richRuntime: \"richRuntime\"; };",
          "declarationLine": 1,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "ConfigProfile is part of this module's public const contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ConfigProfile.",
          "advanced": "No additional advanced behavior is declared for ConfigProfile.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for ConfigProfile."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ConfigProfile.",
          "examples": [
            "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';",
            "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';",
            "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export declare const ConfigProfile: { readonly data: \"data\"; readonly richRuntime: \"richRuntime\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ConfigProfile when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ConfigProfile does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ConfigProfile is part of this module's public const contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ConfigProfile from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ConfigProfile when its const contract and declared inputs match the result you need. Choose a different public symbol when ConfigProfile does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ConfigProfile."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ConfigProfile."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:ConfigProfile:export declare const ConfigProfile: { readonly data: \"data\"; readonly richRuntime: \"richRuntime\"; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            }
          ],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-config--ConfigProfile"
        },
        {
          "name": "IConfigCombineOptions",
          "kind": "type",
          "signature": "export type IConfigCombineOptions = IOwnConfigOptions & { readonly strategies?: Partial<IConfigMergeStrategies>; readonly pathRules?: readonly IConfigPathRule[]; readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision; };",
          "declarationLine": 60,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigCombineOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigCombineOptions.",
          "advanced": "No additional advanced behavior is declared for IConfigCombineOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigCombineOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigCombineOptions.",
          "examples": [],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigCombineOptions = IOwnConfigOptions & { readonly strategies?: Partial<IConfigMergeStrategies>; readonly pathRules?: readonly IConfigPathRule[]; readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigCombineOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigCombineOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigCombineOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigCombineOptions from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigCombineOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigCombineOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigCombineOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigCombineOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigCombineOptions:export type IConfigCombineOptions = IOwnConfigOptions & { readonly strategies?: Partial<IConfigMergeStrategies>; readonly pathRules?: readonly IConfigPathRule[]; readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-config--IConfigCombineOptions"
        },
        {
          "name": "IConfigConflictContext",
          "kind": "type",
          "signature": "export type IConfigConflictContext = { readonly path: readonly PropertyKey[]; readonly left: unknown; readonly right: unknown; };",
          "declarationLine": 51,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigConflictContext is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigConflictContext.",
          "advanced": "No additional advanced behavior is declared for IConfigConflictContext.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigConflictContext."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigConflictContext.",
          "examples": [
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;",
            "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigConflictContext = { readonly path: readonly PropertyKey[]; readonly left: unknown; readonly right: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigConflictContext when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigConflictContext does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigConflictContext is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigConflictContext from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigConflictContext when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigConflictContext does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigConflictContext."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigConflictContext."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigConflictContext:export type IConfigConflictContext = { readonly path: readonly PropertyKey[]; readonly left: unknown; readonly right: unknown; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-config--IConfigConflictContext"
        },
        {
          "name": "IConfigConflictDecision",
          "kind": "type",
          "signature": "export type IConfigConflictDecision = { readonly kind: 'left'; } | { readonly kind: 'right'; } | { readonly kind: 'delete'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "declarationLine": 41,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigConflictDecision is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigConflictDecision.",
          "advanced": "No additional advanced behavior is declared for IConfigConflictDecision.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigConflictDecision."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigConflictDecision.",
          "examples": [
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;",
            "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigConflictDecision = { readonly kind: 'left'; } | { readonly kind: 'right'; } | { readonly kind: 'delete'; } | { readonly kind: 'value'; readonly value: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigConflictDecision when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigConflictDecision does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigConflictDecision is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigConflictDecision from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigConflictDecision when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigConflictDecision does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigConflictDecision."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigConflictDecision."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigConflictDecision:export type IConfigConflictDecision = { readonly kind: 'left'; } | { readonly kind: 'right'; } | { readonly kind: 'delete'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-config--IConfigConflictDecision"
        },
        {
          "name": "IConfigLimits",
          "kind": "type",
          "signature": "export type IConfigLimits = { readonly maxDepth: number; readonly maxNodes: number; readonly maxKeys: number; readonly maxPathLength: number; readonly maxSegmentLength: number; };",
          "declarationLine": 11,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigLimits is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigLimits.",
          "advanced": "No additional advanced behavior is declared for IConfigLimits.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigLimits."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigLimits.",
          "examples": [
            "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;",
            "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};",
            "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;",
            "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];",
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigLimits = { readonly maxDepth: number; readonly maxNodes: number; readonly maxKeys: number; readonly maxPathLength: number; readonly maxSegmentLength: number; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigLimits when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigLimits does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigLimits is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigLimits from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigLimits when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigLimits does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigLimits."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigLimits."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigLimits:export type IConfigLimits = { readonly maxDepth: number; readonly maxNodes: number; readonly maxKeys: number; readonly maxPathLength: number; readonly maxSegmentLength: number; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            }
          ],
          "usageScore": 32,
          "configuration": [],
          "fragment": "utils-config--IConfigLimits"
        },
        {
          "name": "IConfigMergeStrategies",
          "kind": "type",
          "signature": "export type IConfigMergeStrategies = { readonly record: 'merge' | 'replace'; readonly array: 'replace' | 'concat' | 'mergeByIndex'; readonly map: 'replace' | 'merge'; readonly set: 'replace' | 'union'; readonly undefined: 'ignore' | 'assign'; };",
          "declarationLine": 34,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigMergeStrategies is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigMergeStrategies.",
          "advanced": "No additional advanced behavior is declared for IConfigMergeStrategies.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigMergeStrategies."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigMergeStrategies.",
          "examples": [
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;",
            "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigMergeStrategies = { readonly record: 'merge' | 'replace'; readonly array: 'replace' | 'concat' | 'mergeByIndex'; readonly map: 'replace' | 'merge'; readonly set: 'replace' | 'union'; readonly undefined: 'ignore' | 'assign'; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigMergeStrategies when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigMergeStrategies does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigMergeStrategies is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigMergeStrategies from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigMergeStrategies when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigMergeStrategies does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigMergeStrategies."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigMergeStrategies."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigMergeStrategies:export type IConfigMergeStrategies = { readonly record: 'merge' | 'replace'; readonly array: 'replace' | 'concat' | 'mergeByIndex'; readonly map: 'replace' | 'merge'; readonly set: 'replace' | 'union'; readonly undefined: 'ignore' | 'assign'; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            }
          ],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-config--IConfigMergeStrategies"
        },
        {
          "name": "IConfigPatch",
          "kind": "type",
          "signature": "export type IConfigPatch<T extends IConfigRecord> = { readonly [K in keyof T]?: T[K] | typeof CONFIG_DELETE; } & Readonly<Record<PropertyKey, unknown>>;",
          "declarationLine": 22,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigPatch is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigPatch.",
          "advanced": "No additional advanced behavior is declared for IConfigPatch.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigPatch."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigPatch.",
          "examples": [
            "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigPatch<T extends IConfigRecord> = { readonly [K in keyof T]?: T[K] | typeof CONFIG_DELETE; } & Readonly<Record<PropertyKey, unknown>>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigPatch when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigPatch does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigPatch is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigPatch from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigPatch when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigPatch does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigPatch."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigPatch."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigPatch:export type IConfigPatch<T extends IConfigRecord> = { readonly [K in keyof T]?: T[K] | typeof CONFIG_DELETE; } & Readonly<Record<PropertyKey, unknown>>;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 10,
          "configuration": [],
          "fragment": "utils-config--IConfigPatch"
        },
        {
          "name": "IConfigPatchOptions",
          "kind": "type",
          "signature": "export type IConfigPatchOptions = IOwnConfigOptions & { readonly reuseUnchangedRoot?: boolean; };",
          "declarationLine": 25,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigPatchOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigPatchOptions.",
          "advanced": "No additional advanced behavior is declared for IConfigPatchOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigPatchOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigPatchOptions.",
          "examples": [],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigPatchOptions = IOwnConfigOptions & { readonly reuseUnchangedRoot?: boolean; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigPatchOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigPatchOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigPatchOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigPatchOptions from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigPatchOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigPatchOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigPatchOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigPatchOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigPatchOptions:export type IConfigPatchOptions = IOwnConfigOptions & { readonly reuseUnchangedRoot?: boolean; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-config--IConfigPatchOptions"
        },
        {
          "name": "IConfigPathRule",
          "kind": "type",
          "signature": "export type IConfigPathRule = { readonly prefix: readonly PropertyKey[]; readonly strategies: Partial<IConfigMergeStrategies>; };",
          "declarationLine": 56,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigPathRule is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigPathRule.",
          "advanced": "No additional advanced behavior is declared for IConfigPathRule.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigPathRule."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigPathRule.",
          "examples": [
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;",
            "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigPathRule = { readonly prefix: readonly PropertyKey[]; readonly strategies: Partial<IConfigMergeStrategies>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigPathRule when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigPathRule does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigPathRule is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigPathRule from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigPathRule when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigPathRule does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigPathRule."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigPathRule."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigPathRule:export type IConfigPathRule = { readonly prefix: readonly PropertyKey[]; readonly strategies: Partial<IConfigMergeStrategies>; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-config--IConfigPathRule"
        },
        {
          "name": "IConfigProfile",
          "kind": "type",
          "signature": "export type IConfigProfile = (typeof ConfigProfile)[keyof typeof ConfigProfile];",
          "declarationLine": 5,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigProfile is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigProfile.",
          "advanced": "No additional advanced behavior is declared for IConfigProfile.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigProfile."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigProfile.",
          "examples": [
            "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';",
            "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;",
            "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;",
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigProfile = (typeof ConfigProfile)[keyof typeof ConfigProfile];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigProfile when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigProfile does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigProfile is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigProfile from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigProfile when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigProfile does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigProfile."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigProfile."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigProfile:export type IConfigProfile = (typeof ConfigProfile)[keyof typeof ConfigProfile];",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-config--IConfigProfile"
        },
        {
          "name": "IConfigReadResult",
          "kind": "type",
          "signature": "export type IConfigReadResult = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "declarationLine": 28,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigReadResult is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigReadResult.",
          "advanced": "No additional advanced behavior is declared for IConfigReadResult.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigReadResult."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigReadResult.",
          "examples": [
            "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IConfigReadResult = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigReadResult when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigReadResult does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigReadResult is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigReadResult from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigReadResult when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigReadResult does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigReadResult."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigReadResult."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigReadResult:export type IConfigReadResult = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: unknown; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-config--IConfigReadResult"
        },
        {
          "name": "IConfigRecord",
          "kind": "type",
          "signature": "export type IConfigRecord = Record<PropertyKey, unknown>;",
          "declarationLine": 7,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IConfigRecord is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigRecord.",
          "advanced": "No additional advanced behavior is declared for IConfigRecord.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConfigRecord."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConfigRecord.",
          "examples": [
            "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;",
            "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;",
            "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;",
            "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;",
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": ["export type IConfigRecord = Record<PropertyKey, unknown>;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConfigRecord when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConfigRecord does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConfigRecord is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConfigRecord from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConfigRecord when its type contract and declared inputs match the result you need. Choose a different public symbol when IConfigRecord does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConfigRecord."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConfigRecord."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IConfigRecord:export type IConfigRecord = Record<PropertyKey, unknown>;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 30,
          "configuration": [],
          "fragment": "utils-config--IConfigRecord"
        },
        {
          "name": "IOwnConfigOptions",
          "kind": "type",
          "signature": "export type IOwnConfigOptions = { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits>; };",
          "declarationLine": 18,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IOwnConfigOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IOwnConfigOptions.",
          "advanced": "No additional advanced behavior is declared for IOwnConfigOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IOwnConfigOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IOwnConfigOptions.",
          "examples": [],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IOwnConfigOptions = { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IOwnConfigOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IOwnConfigOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IOwnConfigOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IOwnConfigOptions from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IOwnConfigOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IOwnConfigOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IOwnConfigOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IOwnConfigOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IOwnConfigOptions:export type IOwnConfigOptions = { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits>; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-config--IOwnConfigOptions"
        },
        {
          "name": "IOwnedConfig",
          "kind": "type",
          "signature": "export type IOwnedConfig<T extends IConfigRecord> = T & { readonly __utilsOwnedConfigBrand?: unique symbol; };",
          "declarationLine": 8,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "IOwnedConfig is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IOwnedConfig.",
          "advanced": "No additional advanced behavior is declared for IOwnedConfig.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IOwnedConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IOwnedConfig.",
          "examples": [
            "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;",
            "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;",
            "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;",
            "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;",
            "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export type IOwnedConfig<T extends IConfigRecord> = T & { readonly __utilsOwnedConfigBrand?: unique symbol; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IOwnedConfig when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IOwnedConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IOwnedConfig is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IOwnedConfig from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IOwnedConfig when its type contract and declared inputs match the result you need. Choose a different public symbol when IOwnedConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IOwnedConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IOwnedConfig."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:IOwnedConfig:export type IOwnedConfig<T extends IConfigRecord> = T & { readonly __utilsOwnedConfigBrand?: unique symbol; };",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 34,
          "configuration": [],
          "fragment": "utils-config--IOwnedConfig"
        },
        {
          "name": "ownConfig",
          "kind": "function",
          "signature": "export declare function ownConfig<T extends IConfigRecord>(value: T, options?: IOwnConfigOptions): IOwnedConfig<T>;",
          "declarationLine": 66,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "Creates a validated isolated graph with a runtime ownership brand.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ownConfig.",
          "advanced": "No additional advanced behavior is declared for ownConfig.",
          "parameters": ["value: T", "options: IOwnConfigOptions (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "T",
              "optional": false
            },
            {
              "name": "options",
              "type": "IOwnConfigOptions",
              "optional": true
            }
          ],
          "returns": "IOwnedConfig<T>",
          "errors": ["No documented errors are declared for ownConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ownConfig.",
          "examples": [
            "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false",
            "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }",
            "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export declare function ownConfig<T extends IConfigRecord>(value: T, options?: IOwnConfigOptions): IOwnedConfig<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "T",
                "optional": false
              },
              {
                "name": "options",
                "type": "IOwnConfigOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IOwnedConfig<T>"],
          "whenToUse": "Use ownConfig when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ownConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a validated isolated graph with a runtime ownership brand."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ownConfig from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ownConfig when its function contract and declared inputs match the result you need. Choose a different public symbol when ownConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ownConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ownConfig."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "profile",
              "type": "IConfigProfile",
              "optional": true,
              "description": ""
            },
            {
              "name": "limits",
              "type": "Partial<IConfigLimits>",
              "optional": true,
              "description": ""
            },
            {
              "name": "__utilsOwnedConfigBrand",
              "type": "unique symbol",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/config.d.ts:ownConfig:export declare function ownConfig<T extends IConfigRecord>(value: T, options?: IOwnConfigOptions): IOwnedConfig<T>;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "组合工作流示例-截止时间-配置",
              "heading": "组合工作流示例：截止时间 + 配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                },
                {
                  "type": "paragraph",
                  "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            },
            {
              "id": "高阶组合示例-1-截止时间-重试-只读配置",
              "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                }
              ]
            },
            {
              "id": "高阶组合示例-3-对象路径-配置-把校验后的嵌套结构整体写回配置根",
              "heading": "高阶组合示例 · 3. 对象路径 + 配置：把校验后的嵌套结构整体写回配置根",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
                }
              ]
            }
          ],
          "usageScore": 110,
          "configuration": [
            {
              "name": "profile",
              "type": "IConfigProfile",
              "optional": true,
              "description": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。",
              "descriptionEn": "",
              "descriptionZh": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
            },
            {
              "name": "limits",
              "type": "Partial<IConfigLimits>",
              "optional": true,
              "description": "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。",
              "descriptionEn": "",
              "descriptionZh": "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
            }
          ],
          "fragment": "utils-config--ownConfig"
        },
        {
          "name": "parseConfigPath",
          "kind": "function",
          "signature": "export declare function parseConfigPath(path: string, limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>): readonly string[];",
          "declarationLine": 72,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "Applies config limits and string-key policy over the canonical object-path parser.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for parseConfigPath.",
          "advanced": "No additional advanced behavior is declared for parseConfigPath.",
          "parameters": [
            "path: string",
            "limits: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'> (optional)"
          ],
          "parameterDetails": [
            {
              "name": "path",
              "type": "string",
              "optional": false
            },
            {
              "name": "limits",
              "type": "Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>",
              "optional": true
            }
          ],
          "returns": "readonly string[]",
          "errors": ["No documented errors are declared for parseConfigPath."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for parseConfigPath.",
          "examples": ["parseConfigPath('a.b.c'); // ['a', 'b', 'c']"],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export declare function parseConfigPath(path: string, limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>): readonly string[];"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "path",
                "type": "string",
                "optional": false
              },
              {
                "name": "limits",
                "type": "Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["readonly string[]"],
          "whenToUse": "Use parseConfigPath when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when parseConfigPath does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Applies config limits and string-key policy over the canonical object-path parser."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import parseConfigPath from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use parseConfigPath when its function contract and declared inputs match the result you need. Choose a different public symbol when parseConfigPath does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for parseConfigPath."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for parseConfigPath."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/config.d.ts:parseConfigPath:export declare function parseConfigPath(path: string, limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>): readonly string[];",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            }
          ],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-config--parseConfigPath"
        },
        {
          "name": "patchConfig",
          "kind": "function",
          "signature": "export declare function patchConfig<T extends IConfigRecord>(base: IOwnedConfig<T>, patch: IConfigPatch<T>, options?: IConfigPatchOptions): IOwnedConfig<T>;",
          "declarationLine": 70,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "Applies a root-level copy-on-write overlay.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for patchConfig.",
          "advanced": "No additional advanced behavior is declared for patchConfig.",
          "parameters": [
            "base: IOwnedConfig<T>",
            "patch: IConfigPatch<T>",
            "options: IConfigPatchOptions (optional)"
          ],
          "parameterDetails": [
            {
              "name": "base",
              "type": "IOwnedConfig<T>",
              "optional": false
            },
            {
              "name": "patch",
              "type": "IConfigPatch<T>",
              "optional": false
            },
            {
              "name": "options",
              "type": "IConfigPatchOptions",
              "optional": true
            }
          ],
          "returns": "IOwnedConfig<T>",
          "errors": ["No documented errors are declared for patchConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for patchConfig.",
          "examples": [
            "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }",
            "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export declare function patchConfig<T extends IConfigRecord>(base: IOwnedConfig<T>, patch: IConfigPatch<T>, options?: IConfigPatchOptions): IOwnedConfig<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "base",
                "type": "IOwnedConfig<T>",
                "optional": false
              },
              {
                "name": "patch",
                "type": "IConfigPatch<T>",
                "optional": false
              },
              {
                "name": "options",
                "type": "IConfigPatchOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IOwnedConfig<T>"],
          "whenToUse": "Use patchConfig when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when patchConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Applies a root-level copy-on-write overlay."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import patchConfig from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use patchConfig when its function contract and declared inputs match the result you need. Choose a different public symbol when patchConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for patchConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for patchConfig."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "__utilsOwnedConfigBrand",
              "type": "unique symbol",
              "optional": true,
              "description": ""
            },
            {
              "name": "reuseUnchangedRoot",
              "type": "boolean",
              "optional": true,
              "description": ""
            },
            {
              "name": "profile",
              "type": "IConfigProfile",
              "optional": true,
              "description": ""
            },
            {
              "name": "limits",
              "type": "Partial<IConfigLimits>",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/config.d.ts:patchConfig:export declare function patchConfig<T extends IConfigRecord>(base: IOwnedConfig<T>, patch: IConfigPatch<T>, options?: IConfigPatchOptions): IOwnedConfig<T>;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "组合工作流示例-截止时间-配置",
              "heading": "组合工作流示例：截止时间 + 配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                },
                {
                  "type": "paragraph",
                  "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            },
            {
              "id": "高阶组合示例-1-截止时间-重试-只读配置",
              "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                }
              ]
            },
            {
              "id": "高阶组合示例-3-对象路径-配置-把校验后的嵌套结构整体写回配置根",
              "heading": "高阶组合示例 · 3. 对象路径 + 配置：把校验后的嵌套结构整体写回配置根",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
                }
              ]
            }
          ],
          "usageScore": 102,
          "configuration": [
            {
              "name": "reuseUnchangedRoot",
              "type": "boolean",
              "optional": true,
              "description": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。",
              "descriptionEn": "",
              "descriptionZh": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
            },
            {
              "name": "profile",
              "type": "IConfigProfile",
              "optional": true,
              "description": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。",
              "descriptionEn": "",
              "descriptionZh": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
            },
            {
              "name": "limits",
              "type": "Partial<IConfigLimits>",
              "optional": true,
              "description": "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。",
              "descriptionEn": "",
              "descriptionZh": "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
            }
          ],
          "fragment": "utils-config--patchConfig"
        },
        {
          "name": "readConfigPath",
          "kind": "function",
          "signature": "export declare function readConfigPath(config: IOwnedConfig<IConfigRecord>, path: string | readonly string[]): IConfigReadResult;",
          "declarationLine": 74,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "Reads a config path and distinguishes missing from an undefined value.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for readConfigPath.",
          "advanced": "No additional advanced behavior is declared for readConfigPath.",
          "parameters": ["config: IOwnedConfig<IConfigRecord>", "path: string | readonly string[]"],
          "parameterDetails": [
            {
              "name": "config",
              "type": "IOwnedConfig<IConfigRecord>",
              "optional": false
            },
            {
              "name": "path",
              "type": "string | readonly string[]",
              "optional": false
            }
          ],
          "returns": "IConfigReadResult",
          "errors": ["No documented errors are declared for readConfigPath."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for readConfigPath.",
          "examples": [
            "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }",
            "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export declare function readConfigPath(config: IOwnedConfig<IConfigRecord>, path: string | readonly string[]): IConfigReadResult;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "config",
                "type": "IOwnedConfig<IConfigRecord>",
                "optional": false
              },
              {
                "name": "path",
                "type": "string | readonly string[]",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IConfigReadResult"],
          "whenToUse": "Use readConfigPath when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when readConfigPath does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reads a config path and distinguishes missing from an undefined value."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import readConfigPath from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use readConfigPath when its function contract and declared inputs match the result you need. Choose a different public symbol when readConfigPath does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for readConfigPath."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for readConfigPath."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "__utilsOwnedConfigBrand",
              "type": "unique symbol",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/config.d.ts:readConfigPath:export declare function readConfigPath(config: IOwnedConfig<IConfigRecord>, path: string | readonly string[]): IConfigReadResult;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            }
          ],
          "usageScore": 30,
          "configuration": [],
          "fragment": "utils-config--readConfigPath"
        },
        {
          "name": "readonlyConfig",
          "kind": "function",
          "signature": "export declare function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;",
          "declarationLine": 68,
          "source": "packages/utils/dist/config.d.ts",
          "purpose": "Creates a cached readonly facade that rejects structural mutation.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for readonlyConfig.",
          "advanced": "No additional advanced behavior is declared for readonlyConfig.",
          "parameters": ["value: IOwnedConfig<T>"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "IOwnedConfig<T>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for readonlyConfig."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for readonlyConfig.",
          "examples": [
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }",
            "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
          ],
          "members": [],
          "exportPath": "./config",
          "overloadSignatures": [
            "export declare function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "IOwnedConfig<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use readonlyConfig when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when readonlyConfig does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a cached readonly facade that rejects structural mutation."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import readonlyConfig from @migaia/utils/config, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use readonlyConfig when its function contract and declared inputs match the result you need. Choose a different public symbol when readonlyConfig does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for readonlyConfig."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for readonlyConfig."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "__utilsOwnedConfigBrand",
              "type": "unique symbol",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/config.d.ts:readonlyConfig:export declare function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "组合工作流示例-截止时间-配置",
              "heading": "组合工作流示例：截止时间 + 配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                },
                {
                  "type": "paragraph",
                  "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            },
            {
              "id": "高阶组合示例-1-截止时间-重试-只读配置",
              "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                }
              ]
            }
          ],
          "usageScore": 76,
          "configuration": [],
          "fragment": "utils-config--readonlyConfig"
        }
      ],
      "aliases": []
    },
    {
      "id": "utils:error",
      "library": "utils",
      "module": "error",
      "exportPath": "./error",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "attachErrorIdentity",
          "kind": "function",
          "signature": "export declare function attachErrorIdentity<T extends object>(error: T, identity: IErrorIdentity, hooks?: IErrorIdentityHooks<T>): T;",
          "declarationLine": 51,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Attaches stable identity without replacing the original object. Optional hooks let a module preserve its own fallback semantics for conflicts and non-extensible targets.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for attachErrorIdentity.",
          "advanced": "No additional advanced behavior is declared for attachErrorIdentity.",
          "parameters": [
            "error: T",
            "identity: IErrorIdentity",
            "hooks: IErrorIdentityHooks<T> (optional)"
          ],
          "parameterDetails": [
            {
              "name": "error",
              "type": "T",
              "optional": false
            },
            {
              "name": "identity",
              "type": "IErrorIdentity",
              "optional": false
            },
            {
              "name": "hooks",
              "type": "IErrorIdentityHooks<T>",
              "optional": true
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for attachErrorIdentity."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for attachErrorIdentity.",
          "examples": [
            "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'",
            "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });",
            "import { once } from '@migaia/utils/function';\nimport { attachErrorIdentity } from '@migaia/utils/error';\n\nconst initOnce = once(() => {\n  try {\n    return dangerousInit();\n  } catch (error) {\n    throw attachErrorIdentity(error as Error, { source: 'my-module', code: 'INIT_FAILED' });\n  }\n});"
          ],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function attachErrorIdentity<T extends object>(error: T, identity: IErrorIdentity, hooks?: IErrorIdentityHooks<T>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "error",
                "type": "T",
                "optional": false
              },
              {
                "name": "identity",
                "type": "IErrorIdentity",
                "optional": false
              },
              {
                "name": "hooks",
                "type": "IErrorIdentityHooks<T>",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use attachErrorIdentity when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when attachErrorIdentity does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Attaches stable identity without replacing the original object. Optional hooks let a module preserve its own fallback semantics for conflicts and non-extensible targets."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import attachErrorIdentity from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use attachErrorIdentity when its function contract and declared inputs match the result you need. Choose a different public symbol when attachErrorIdentity does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for attachErrorIdentity."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for attachErrorIdentity."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:attachErrorIdentity:export declare function attachErrorIdentity<T extends object>(error: T, identity: IErrorIdentity, hooks?: IErrorIdentityHooks<T>): T;",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            },
            {
              "id": "高阶组合示例-4-一次性初始化-错误身份-确保初始化异常带上可识别的-code",
              "heading": "高阶组合示例 · 4. 一次性初始化 + 错误身份：确保初始化异常带上可识别的 code",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { once } from '@migaia/utils/function';\nimport { attachErrorIdentity } from '@migaia/utils/error';\n\nconst initOnce = once(() => {\n  try {\n    return dangerousInit();\n  } catch (error) {\n    throw attachErrorIdentity(error as Error, { source: 'my-module', code: 'INIT_FAILED' });\n  }\n});"
                }
              ]
            }
          ],
          "usageScore": 36,
          "configuration": [],
          "fragment": "utils-error--attachErrorIdentity"
        },
        {
          "name": "attachSecondaryErrors",
          "kind": "function",
          "signature": "export declare function attachSecondaryErrors(primary: unknown, secondaryErrors: readonly unknown[]): unknown;",
          "declarationLine": 56,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Appends secondary failures to a primary without changing primary identity. A hostile or non-extensible primary receives an `AggregateError` fallback whose first entry is the primary.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for attachSecondaryErrors.",
          "advanced": "No additional advanced behavior is declared for attachSecondaryErrors.",
          "parameters": ["primary: unknown", "secondaryErrors: readonly unknown[]"],
          "parameterDetails": [
            {
              "name": "primary",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "secondaryErrors",
              "type": "readonly unknown[]",
              "optional": false
            }
          ],
          "returns": "unknown",
          "errors": ["No documented errors are declared for attachSecondaryErrors."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for attachSecondaryErrors.",
          "examples": [],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function attachSecondaryErrors(primary: unknown, secondaryErrors: readonly unknown[]): unknown;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "primary",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "secondaryErrors",
                "type": "readonly unknown[]",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["unknown"],
          "whenToUse": "Use attachSecondaryErrors when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when attachSecondaryErrors does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Appends secondary failures to a primary without changing primary identity. A hostile or non-extensible primary receives an `AggregateError` fallback whose first entry is the primary."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import attachSecondaryErrors from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use attachSecondaryErrors when its function contract and declared inputs match the result you need. Choose a different public symbol when attachSecondaryErrors does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for attachSecondaryErrors."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for attachSecondaryErrors."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:attachSecondaryErrors:export declare function attachSecondaryErrors(primary: unknown, secondaryErrors: readonly unknown[]): unknown;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-error--attachSecondaryErrors"
        },
        {
          "name": "combineErrors",
          "kind": "function",
          "signature": "export declare function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;",
          "declarationLine": 68,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Combines zero, one, or many errors while preserving iteration failures.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for combineErrors.",
          "advanced": "No additional advanced behavior is declared for combineErrors.",
          "parameters": ["errors: Iterable<unknown>", "message: string"],
          "parameterDetails": [
            {
              "name": "errors",
              "type": "Iterable<unknown>",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "Error | undefined",
          "errors": ["No documented errors are declared for combineErrors."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for combineErrors.",
          "examples": [
            "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError",
            "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
          ],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "errors",
                "type": "Iterable<unknown>",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Error | undefined"],
          "whenToUse": "Use combineErrors when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when combineErrors does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Combines zero, one, or many errors while preserving iteration failures."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import combineErrors from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use combineErrors when its function contract and declared inputs match the result you need. Choose a different public symbol when combineErrors does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for combineErrors."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for combineErrors."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:combineErrors:export declare function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            },
            {
              "id": "高阶组合示例-2-并发限流-重试-错误合并-批量拉取-容忍部分失败",
              "heading": "高阶组合示例 · 2. 并发限流 + 重试 + 错误合并：批量拉取，容忍部分失败",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
                }
              ]
            }
          ],
          "usageScore": 32,
          "configuration": [],
          "fragment": "utils-error--combineErrors"
        },
        {
          "name": "IErrorIdentity",
          "kind": "type",
          "signature": "export type IErrorIdentity = { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; };",
          "declarationLine": 23,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Stable identity fields shared by module-boundary native errors.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorIdentity.",
          "advanced": "No additional advanced behavior is declared for IErrorIdentity.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IErrorIdentity."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IErrorIdentity.",
          "examples": [],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export type IErrorIdentity = { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IErrorIdentity when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IErrorIdentity does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Stable identity fields shared by module-boundary native errors."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IErrorIdentity from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IErrorIdentity when its type contract and declared inputs match the result you need. Choose a different public symbol when IErrorIdentity does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorIdentity."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IErrorIdentity."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:IErrorIdentity:export type IErrorIdentity = { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-error--IErrorIdentity"
        },
        {
          "name": "IErrorIdentityHooks",
          "kind": "type",
          "signature": "export type IErrorIdentityHooks<T extends object> = { readonly onConflict?: (context: { readonly target: T; readonly key: string; readonly existing: unknown; readonly incoming: unknown; }) => T; readonly onFailure?: (context: { readonly target: T; readonly key: string; readonly incoming: unknown; readonly cause: unknown; }) => T; };",
          "declarationLine": 33,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Caller-owned recovery for an identity attachment conflict or descriptor write failure. Each hook returns the target that receives any remaining identity fields.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorIdentityHooks.",
          "advanced": "No additional advanced behavior is declared for IErrorIdentityHooks.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IErrorIdentityHooks."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IErrorIdentityHooks.",
          "examples": [],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export type IErrorIdentityHooks<T extends object> = { readonly onConflict?: (context: { readonly target: T; readonly key: string; readonly existing: unknown; readonly incoming: unknown; }) => T; readonly onFailure?: (context: { readonly target: T; readonly key: string; readonly incoming: unknown; readonly cause: unknown; }) => T; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IErrorIdentityHooks when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IErrorIdentityHooks does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Caller-owned recovery for an identity attachment conflict or descriptor write failure. Each hook returns the target that receives any remaining identity fields."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IErrorIdentityHooks from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IErrorIdentityHooks when its type contract and declared inputs match the result you need. Choose a different public symbol when IErrorIdentityHooks does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorIdentityHooks."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IErrorIdentityHooks."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:IErrorIdentityHooks:export type IErrorIdentityHooks<T extends object> = { readonly onConflict?: (context: { readonly target: T; readonly key: string; readonly existing: unknown; readonly incoming: unknown; }) => T; readonly onFailure?: (context: { readonly target: T; readonly key: string; readonly incoming: unknown; readonly cause: unknown; }) => T; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-error--IErrorIdentityHooks"
        },
        {
          "name": "isUtilsAbortError",
          "kind": "function",
          "signature": "export declare function isUtilsAbortError(value: unknown): value is UtilsAbortError;",
          "declarationLine": 70,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "isUtilsAbortError is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUtilsAbortError.",
          "advanced": "No additional advanced behavior is declared for isUtilsAbortError.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "value is UtilsAbortError",
          "errors": ["No documented errors are declared for isUtilsAbortError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isUtilsAbortError.",
          "examples": [],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function isUtilsAbortError(value: unknown): value is UtilsAbortError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["value is UtilsAbortError"],
          "whenToUse": "Use isUtilsAbortError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isUtilsAbortError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "isUtilsAbortError is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isUtilsAbortError from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isUtilsAbortError when its function contract and declared inputs match the result you need. Choose a different public symbol when isUtilsAbortError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUtilsAbortError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isUtilsAbortError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:isUtilsAbortError:export declare function isUtilsAbortError(value: unknown): value is UtilsAbortError;",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 20,
          "configuration": [],
          "fragment": "utils-error--isUtilsAbortError"
        },
        {
          "name": "isUtilsError",
          "kind": "function",
          "signature": "export declare function isUtilsError(value: unknown): value is UtilsError;",
          "declarationLine": 69,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "isUtilsError is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUtilsError.",
          "advanced": "No additional advanced behavior is declared for isUtilsError.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "value is UtilsError",
          "errors": ["No documented errors are declared for isUtilsError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isUtilsError.",
          "examples": [],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function isUtilsError(value: unknown): value is UtilsError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["value is UtilsError"],
          "whenToUse": "Use isUtilsError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isUtilsError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "isUtilsError is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isUtilsError from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isUtilsError when its function contract and declared inputs match the result you need. Choose a different public symbol when isUtilsError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUtilsError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isUtilsError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:isUtilsError:export declare function isUtilsError(value: unknown): value is UtilsError;",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 20,
          "configuration": [],
          "fragment": "utils-error--isUtilsError"
        },
        {
          "name": "isUtilsTimeoutError",
          "kind": "function",
          "signature": "export declare function isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;",
          "declarationLine": 71,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "isUtilsTimeoutError is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUtilsTimeoutError.",
          "advanced": "No additional advanced behavior is declared for isUtilsTimeoutError.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "value is UtilsTimeoutError",
          "errors": ["No documented errors are declared for isUtilsTimeoutError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isUtilsTimeoutError.",
          "examples": [
            "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
          ],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["value is UtilsTimeoutError"],
          "whenToUse": "Use isUtilsTimeoutError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isUtilsTimeoutError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "isUtilsTimeoutError is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isUtilsTimeoutError from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isUtilsTimeoutError when its function contract and declared inputs match the result you need. Choose a different public symbol when isUtilsTimeoutError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isUtilsTimeoutError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isUtilsTimeoutError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:isUtilsTimeoutError:export declare function isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-error--isUtilsTimeoutError"
        },
        {
          "name": "safeErrorReason",
          "kind": "function",
          "signature": "export declare function safeErrorReason(error: unknown, fallback: string): string;",
          "declarationLine": 58,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Reads an arbitrary failure's diagnostic reason once, returning a caller-owned safe fallback.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for safeErrorReason.",
          "advanced": "No additional advanced behavior is declared for safeErrorReason.",
          "parameters": ["error: unknown", "fallback: string"],
          "parameterDetails": [
            {
              "name": "error",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "fallback",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for safeErrorReason."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for safeErrorReason.",
          "examples": [],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function safeErrorReason(error: unknown, fallback: string): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "error",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "fallback",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use safeErrorReason when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when safeErrorReason does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reads an arbitrary failure's diagnostic reason once, returning a caller-owned safe fallback."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import safeErrorReason from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use safeErrorReason when its function contract and declared inputs match the result you need. Choose a different public symbol when safeErrorReason does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for safeErrorReason."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for safeErrorReason."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:safeErrorReason:export declare function safeErrorReason(error: unknown, fallback: string): string;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-error--safeErrorReason"
        },
        {
          "name": "toError",
          "kind": "function",
          "signature": "export declare function toError(value: unknown, options?: { readonly message?: string; }): Error;",
          "declarationLine": 60,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Converts arbitrary thrown values while preserving the original as cause.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for toError.",
          "advanced": "No additional advanced behavior is declared for toError.",
          "parameters": ["value: unknown", "options: { readonly message?: string; } (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly message?: string; }",
              "optional": true
            }
          ],
          "returns": "Error",
          "errors": ["No documented errors are declared for toError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for toError.",
          "examples": ["catch (value) { const error = toError(value); }"],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function toError(value: unknown, options?: { readonly message?: string; }): Error;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly message?: string; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["Error"],
          "whenToUse": "Use toError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when toError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Converts arbitrary thrown values while preserving the original as cause."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import toError from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use toError when its function contract and declared inputs match the result you need. Choose a different public symbol when toError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for toError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for toError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:toError:export declare function toError(value: unknown, options?: { readonly message?: string; }): Error;",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "utilserrorcode",
              "heading": "UtilsErrorCode",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const UtilsErrorCode = {\n  invalidArgument: 'INVALID_ARGUMENT',\n  nonErrorValue: 'NON_ERROR_VALUE',\n  envUnsupported: 'ENV_UNSUPPORTED',\n  aborted: 'ABORTED',\n  deadlineExceeded: 'DEADLINE_EXCEEDED',\n  schedulerRunaway: 'SCHEDULER_RUNAWAY',\n  errorIdentityConflict: 'ERROR_IDENTITY_CONFLICT',\n  cloneUnsupported: 'CLONE_UNSUPPORTED',\n  invalidEncoding: 'INVALID_ENCODING',\n  limiterClosed: 'LIMITER_CLOSED',\n  reentrantCall: 'REENTRANT_CALL',\n  configUnsupported: 'CONFIG_UNSUPPORTED',\n  configReadonly: 'CONFIG_READONLY',\n  configConflict: 'CONFIG_CONFLICT',\n  configLimitExceeded: 'CONFIG_LIMIT_EXCEEDED',\n  configPathInvalid: 'CONFIG_PATH_INVALID',\n  objectPathInvalid: 'OBJECT_PATH_INVALID',\n  formatInvalid: 'FORMAT_INVALID',\n  formatValueMissing: 'FORMAT_VALUE_MISSING',\n  numberFormatInvalid: 'NUMBER_FORMAT_INVALID'\n} as const;"
                },
                {
                  "type": "paragraph",
                  "text": "全包统一使用的稳定语义码，`IUtilsErrorCode` 是其取值的联合类型。除 `UtilsError` 子类外，`base64ToBytes`/`object-path`/`config` 等模块抛出的原生 `TypeError`/`RangeError` 也会通过 `Object.defineProperty` 挂上对应的 `code`（与 `source: '@migaia/utils'`），可用同一套判断逻辑识别。"
                },
                {
                  "type": "paragraph",
                  "text": "按用途理解全部错误码："
                },
                {
                  "type": "list",
                  "items": [
                    "`INVALID_ARGUMENT`：参数类型、范围或互斥组合错误；修正调用参数。",
                    "`NON_ERROR_VALUE`：非 `Error` 异常值已被 `toError()` 转换；从 `cause` 读取原始值。",
                    "`ENV_UNSUPPORTED`：宿主缺少 `structuredClone` 等必要能力；更换环境或实现。",
                    "`ABORTED`：外部信号请求取消；停止协作式工作并传播取消原因。",
                    "`DEADLINE_EXCEEDED`：操作、尝试或总流程超时；结合 `scope`、`timeoutMs` 选择重试或降级。",
                    "`SCHEDULER_RUNAWAY`：虚拟时钟一次推进超过 10000 个任务；排查同刻递归调度。",
                    "`ERROR_IDENTITY_CONFLICT`：已有错误身份与新 `source`/`code` 冲突；保留原身份。",
                    "`CLONE_UNSUPPORTED`：值不能被 `structuredClone` 复制；先转换为可克隆数据。",
                    "`INVALID_ENCODING`：编码输入不是规范形式；按错误偏移修正输入。",
                    "`LIMITER_CLOSED`：并发限制器已关闭；停止提交或创建新实例。",
                    "`REENTRANT_CALL`：同一实例发生禁止的重入；等待当前回调结束。",
                    "`CONFIG_UNSUPPORTED`：配置含不支持的数据形状或属性；改用普通数据。",
                    "`CONFIG_READONLY`：尝试修改只读配置；从所有者侧创建新配置。",
                    "`CONFIG_CONFLICT`：配置来源的所有权、profile 或描述符冲突；统一契约再合并。",
                    "`CONFIG_LIMIT_EXCEEDED`：配置键数、深度等超过安全上限；缩小输入或调整明确限制。",
                    "`CONFIG_PATH_INVALID`：配置路径为空、危险或分段无效；改用安全路径。",
                    "`OBJECT_PATH_INVALID`：对象路径无法安全解析；修正字符串或分段元组。",
                    "`FORMAT_INVALID`：模板、占位符或值不能安全格式化；修正模板或输入。",
                    "`FORMAT_VALUE_MISSING`：严格格式化缺少占位符值；补齐字段或调整策略。",
                    "`NUMBER_FORMAT_INVALID`：`Intl.NumberFormat` 拒绝 locale、选项、货币或数值；修正格式配置。"
                  ]
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            },
            {
              "id": "高阶组合示例-2-并发限流-重试-错误合并-批量拉取-容忍部分失败",
              "heading": "高阶组合示例 · 2. 并发限流 + 重试 + 错误合并：批量拉取，容忍部分失败",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
                }
              ]
            }
          ],
          "usageScore": 40,
          "configuration": [],
          "fragment": "utils-error--toError"
        },
        {
          "name": "UtilsAbortError",
          "kind": "class",
          "signature": "export declare class UtilsAbortError extends UtilsError { readonly name: \"AbortError\"; constructor(reason?: unknown); }",
          "declarationLine": 11,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Error representing cooperative cancellation.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for UtilsAbortError.",
          "advanced": "No additional advanced behavior is declared for UtilsAbortError.",
          "parameters": ["reason: unknown (optional)"],
          "parameterDetails": [
            {
              "name": "reason",
              "type": "unknown",
              "optional": true
            }
          ],
          "returns": "No explicit return annotation is declared for this class.",
          "errors": ["No documented errors are declared for UtilsAbortError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for UtilsAbortError.",
          "examples": [
            "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
          ],
          "members": [
            {
              "name": "name",
              "kind": "property",
              "signature": "readonly name: \"AbortError\";",
              "description": null,
              "parameterDetails": [],
              "returns": "\"AbortError\""
            }
          ],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare class UtilsAbortError extends UtilsError { readonly name: \"AbortError\"; constructor(reason?: unknown); }"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "reason",
                "type": "unknown",
                "optional": true
              }
            ]
          ],
          "overloadReturns": [],
          "whenToUse": "Use UtilsAbortError when its class contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when UtilsAbortError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Error representing cooperative cancellation."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import UtilsAbortError from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use UtilsAbortError when its class contract and declared inputs match the result you need. Choose a different public symbol when UtilsAbortError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained class example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for UtilsAbortError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for UtilsAbortError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:UtilsAbortError:export declare class UtilsAbortError extends UtilsError { readonly name: \"AbortError\"; constructor(reason?: unknown); }",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 56,
          "configuration": [],
          "fragment": "utils-error--UtilsAbortError"
        },
        {
          "name": "UtilsError",
          "kind": "class",
          "signature": "export declare abstract class UtilsError extends Error { readonly source: \"@migaia/utils\"; readonly code: IUtilsErrorCode; protected constructor(code: IUtilsErrorCode, message: string, options?: { readonly cause?: unknown; }); }",
          "declarationLine": 3,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Base error retaining the utils source and stable semantic code.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for UtilsError.",
          "advanced": "No additional advanced behavior is declared for UtilsError.",
          "parameters": [
            "code: IUtilsErrorCode",
            "message: string",
            "options: { readonly cause?: unknown; } (optional)"
          ],
          "parameterDetails": [
            {
              "name": "code",
              "type": "IUtilsErrorCode",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly cause?: unknown; }",
              "optional": true
            }
          ],
          "returns": "No explicit return annotation is declared for this class.",
          "errors": ["No documented errors are declared for UtilsError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for UtilsError.",
          "examples": [],
          "members": [
            {
              "name": "source",
              "kind": "property",
              "signature": "readonly source: \"@migaia/utils\";",
              "description": null,
              "parameterDetails": [],
              "returns": "\"@migaia/utils\""
            },
            {
              "name": "code",
              "kind": "property",
              "signature": "readonly code: IUtilsErrorCode;",
              "description": null,
              "parameterDetails": [],
              "returns": "IUtilsErrorCode"
            }
          ],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare abstract class UtilsError extends Error { readonly source: \"@migaia/utils\"; readonly code: IUtilsErrorCode; protected constructor(code: IUtilsErrorCode, message: string, options?: { readonly cause?: unknown; }); }"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "code",
                "type": "IUtilsErrorCode",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly cause?: unknown; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": [],
          "whenToUse": "Use UtilsError when its class contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when UtilsError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Base error retaining the utils source and stable semantic code."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import UtilsError from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use UtilsError when its class contract and declared inputs match the result you need. Choose a different public symbol when UtilsError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained class example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for UtilsError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for UtilsError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:UtilsError:export declare abstract class UtilsError extends Error { readonly source: \"@migaia/utils\"; readonly code: IUtilsErrorCode; protected constructor(code: IUtilsErrorCode, message: string, options?: { readonly cause?: unknown; }); }",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "utilserrorcode",
              "heading": "UtilsErrorCode",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const UtilsErrorCode = {\n  invalidArgument: 'INVALID_ARGUMENT',\n  nonErrorValue: 'NON_ERROR_VALUE',\n  envUnsupported: 'ENV_UNSUPPORTED',\n  aborted: 'ABORTED',\n  deadlineExceeded: 'DEADLINE_EXCEEDED',\n  schedulerRunaway: 'SCHEDULER_RUNAWAY',\n  errorIdentityConflict: 'ERROR_IDENTITY_CONFLICT',\n  cloneUnsupported: 'CLONE_UNSUPPORTED',\n  invalidEncoding: 'INVALID_ENCODING',\n  limiterClosed: 'LIMITER_CLOSED',\n  reentrantCall: 'REENTRANT_CALL',\n  configUnsupported: 'CONFIG_UNSUPPORTED',\n  configReadonly: 'CONFIG_READONLY',\n  configConflict: 'CONFIG_CONFLICT',\n  configLimitExceeded: 'CONFIG_LIMIT_EXCEEDED',\n  configPathInvalid: 'CONFIG_PATH_INVALID',\n  objectPathInvalid: 'OBJECT_PATH_INVALID',\n  formatInvalid: 'FORMAT_INVALID',\n  formatValueMissing: 'FORMAT_VALUE_MISSING',\n  numberFormatInvalid: 'NUMBER_FORMAT_INVALID'\n} as const;"
                },
                {
                  "type": "paragraph",
                  "text": "全包统一使用的稳定语义码，`IUtilsErrorCode` 是其取值的联合类型。除 `UtilsError` 子类外，`base64ToBytes`/`object-path`/`config` 等模块抛出的原生 `TypeError`/`RangeError` 也会通过 `Object.defineProperty` 挂上对应的 `code`（与 `source: '@migaia/utils'`），可用同一套判断逻辑识别。"
                },
                {
                  "type": "paragraph",
                  "text": "按用途理解全部错误码："
                },
                {
                  "type": "list",
                  "items": [
                    "`INVALID_ARGUMENT`：参数类型、范围或互斥组合错误；修正调用参数。",
                    "`NON_ERROR_VALUE`：非 `Error` 异常值已被 `toError()` 转换；从 `cause` 读取原始值。",
                    "`ENV_UNSUPPORTED`：宿主缺少 `structuredClone` 等必要能力；更换环境或实现。",
                    "`ABORTED`：外部信号请求取消；停止协作式工作并传播取消原因。",
                    "`DEADLINE_EXCEEDED`：操作、尝试或总流程超时；结合 `scope`、`timeoutMs` 选择重试或降级。",
                    "`SCHEDULER_RUNAWAY`：虚拟时钟一次推进超过 10000 个任务；排查同刻递归调度。",
                    "`ERROR_IDENTITY_CONFLICT`：已有错误身份与新 `source`/`code` 冲突；保留原身份。",
                    "`CLONE_UNSUPPORTED`：值不能被 `structuredClone` 复制；先转换为可克隆数据。",
                    "`INVALID_ENCODING`：编码输入不是规范形式；按错误偏移修正输入。",
                    "`LIMITER_CLOSED`：并发限制器已关闭；停止提交或创建新实例。",
                    "`REENTRANT_CALL`：同一实例发生禁止的重入；等待当前回调结束。",
                    "`CONFIG_UNSUPPORTED`：配置含不支持的数据形状或属性；改用普通数据。",
                    "`CONFIG_READONLY`：尝试修改只读配置；从所有者侧创建新配置。",
                    "`CONFIG_CONFLICT`：配置来源的所有权、profile 或描述符冲突；统一契约再合并。",
                    "`CONFIG_LIMIT_EXCEEDED`：配置键数、深度等超过安全上限；缩小输入或调整明确限制。",
                    "`CONFIG_PATH_INVALID`：配置路径为空、危险或分段无效；改用安全路径。",
                    "`OBJECT_PATH_INVALID`：对象路径无法安全解析；修正字符串或分段元组。",
                    "`FORMAT_INVALID`：模板、占位符或值不能安全格式化；修正模板或输入。",
                    "`FORMAT_VALUE_MISSING`：严格格式化缺少占位符值；补齐字段或调整策略。",
                    "`NUMBER_FORMAT_INVALID`：`Intl.NumberFormat` 拒绝 locale、选项、货币或数值；修正格式配置。"
                  ]
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 52,
          "configuration": [],
          "fragment": "utils-error--UtilsError"
        },
        {
          "name": "UtilsTimeoutError",
          "kind": "class",
          "signature": "export declare class UtilsTimeoutError extends UtilsError { readonly scope: 'operation' | 'attempt' | 'total'; readonly timeoutMs: number; readonly name: \"TimeoutError\"; constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number); }",
          "declarationLine": 16,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Error representing an operation, attempt, or total deadline.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for UtilsTimeoutError.",
          "advanced": "No additional advanced behavior is declared for UtilsTimeoutError.",
          "parameters": ["scope: 'operation' | 'attempt' | 'total'", "timeoutMs: number"],
          "parameterDetails": [
            {
              "name": "scope",
              "type": "'operation' | 'attempt' | 'total'",
              "optional": false
            },
            {
              "name": "timeoutMs",
              "type": "number",
              "optional": false
            }
          ],
          "returns": "No explicit return annotation is declared for this class.",
          "errors": ["No documented errors are declared for UtilsTimeoutError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for UtilsTimeoutError.",
          "examples": [
            "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
          ],
          "members": [
            {
              "name": "scope",
              "kind": "property",
              "signature": "readonly scope: 'operation' | 'attempt' | 'total';",
              "description": null,
              "parameterDetails": [],
              "returns": "'operation' | 'attempt' | 'total'"
            },
            {
              "name": "timeoutMs",
              "kind": "property",
              "signature": "readonly timeoutMs: number;",
              "description": null,
              "parameterDetails": [],
              "returns": "number"
            },
            {
              "name": "name",
              "kind": "property",
              "signature": "readonly name: \"TimeoutError\";",
              "description": null,
              "parameterDetails": [],
              "returns": "\"TimeoutError\""
            }
          ],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare class UtilsTimeoutError extends UtilsError { readonly scope: 'operation' | 'attempt' | 'total'; readonly timeoutMs: number; readonly name: \"TimeoutError\"; constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number); }"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "scope",
                "type": "'operation' | 'attempt' | 'total'",
                "optional": false
              },
              {
                "name": "timeoutMs",
                "type": "number",
                "optional": false
              }
            ]
          ],
          "overloadReturns": [],
          "whenToUse": "Use UtilsTimeoutError when its class contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when UtilsTimeoutError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Error representing an operation, attempt, or total deadline."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import UtilsTimeoutError from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use UtilsTimeoutError when its class contract and declared inputs match the result you need. Choose a different public symbol when UtilsTimeoutError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained class example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for UtilsTimeoutError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for UtilsTimeoutError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:UtilsTimeoutError:export declare class UtilsTimeoutError extends UtilsError { readonly scope: 'operation' | 'attempt' | 'total'; readonly timeoutMs: number; readonly name: \"TimeoutError\"; constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number); }",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 48,
          "configuration": [],
          "fragment": "utils-error--UtilsTimeoutError"
        },
        {
          "name": "walkErrorCauses",
          "kind": "function",
          "signature": "export declare function walkErrorCauses(error: unknown, options?: { readonly maxDepth?: number; }): readonly unknown[];",
          "declarationLine": 64,
          "source": "packages/utils/dist/error.d.ts",
          "purpose": "Walks cause and AggregateError chains with bounded identity deduplication.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for walkErrorCauses.",
          "advanced": "No additional advanced behavior is declared for walkErrorCauses.",
          "parameters": ["error: unknown", "options: { readonly maxDepth?: number; } (optional)"],
          "parameterDetails": [
            {
              "name": "error",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly maxDepth?: number; }",
              "optional": true
            }
          ],
          "returns": "readonly unknown[]",
          "errors": ["No documented errors are declared for walkErrorCauses."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for walkErrorCauses.",
          "examples": [
            "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}",
            "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
          ],
          "members": [],
          "exportPath": "./error",
          "overloadSignatures": [
            "export declare function walkErrorCauses(error: unknown, options?: { readonly maxDepth?: number; }): readonly unknown[];"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "error",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly maxDepth?: number; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["readonly unknown[]"],
          "whenToUse": "Use walkErrorCauses when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when walkErrorCauses does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Walks cause and AggregateError chains with bounded identity deduplication."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import walkErrorCauses from @migaia/utils/error, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use walkErrorCauses when its function contract and declared inputs match the result you need. Choose a different public symbol when walkErrorCauses does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for walkErrorCauses."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for walkErrorCauses."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/error.d.ts:walkErrorCauses:export declare function walkErrorCauses(error: unknown, options?: { readonly maxDepth?: number; }): readonly unknown[];",
          "guidance": [
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "统一的错误身份、包装与因果链遍历工具。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "abstract class UtilsError extends Error {\n  readonly source = '@migaia/utils';\n  readonly code: IUtilsErrorCode;\n}\nclass UtilsAbortError extends UtilsError {\n  readonly name = 'AbortError';\n  constructor(reason?: unknown);\n}\nclass UtilsTimeoutError extends UtilsError {\n  readonly name = 'TimeoutError';\n  readonly scope: 'operation' | 'attempt' | 'total';\n  readonly timeoutMs: number;\n  constructor(scope: 'operation' | 'attempt' | 'total', timeoutMs: number);\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsError` 是抽象基类，所有本包抛出的语义化错误都携带 `source: '@migaia/utils'` 与稳定的 `code`。`UtilsAbortError` 通过 `cause` 保留原始中止原因；`UtilsTimeoutError` 额外携带 `scope`（超时发生在单次操作 / 单次尝试 / 总截止时间的哪一层）与 `timeoutMs`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function attachErrorIdentity<T extends Error>(\n  error: T,\n  identity: {\n    readonly source: string;\n    readonly code: string;\n    readonly phase?: string;\n    readonly detail?: Readonly<Record<string, unknown>>;\n  }\n): T;"
                },
                {
                  "type": "paragraph",
                  "text": "就地给一个已存在的错误对象追加不可写的身份字段（`source`/`code`/`phase`/`detail`），不会替换或包装该错误。如果同一字段已存在且值不同，会抛出 `TypeError`（避免悄悄覆盖其他代码写入的身份信息）；值相同则视为幂等，直接返回。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const error = new Error('boom');\nattachErrorIdentity(error, { source: 'my-module', code: 'FETCH_FAILED' });\nerror.code; // 'FETCH_FAILED'"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toError(value: unknown, options?: { readonly message?: string }): Error;"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 `throw` 出来的值规整为 `Error` 实例：已经是 `Error` 的原样返回；否则包装成携带 `cause: value` 的内部错误类型，消息优先取 `options.message`，其次（当抛出值本身是字符串时）取该字符串，否则用固定的兜底文案。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function walkErrorCauses(\n  error: unknown,\n  options?: { readonly maxDepth?: number }\n): readonly unknown[];"
                },
                {
                  "type": "paragraph",
                  "text": "按 `cause` 链与 `AggregateError.errors` 展开遍历，返回访问到的全部节点（含起始 `error` 自身），用 `Set` 做同一值去重防止环路。`maxDepth`（默认 32）限制最大深度，超出后停止继续下探那一支。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "try {\n  throw new Error('outer', { cause: new Error('inner') });\n} catch (error) {\n  walkErrorCauses(error); // [outerError, innerError]\n}"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineErrors(errors: Iterable<unknown>, message: string): Error | undefined;"
                },
                {
                  "type": "paragraph",
                  "text": "把 0 个、1 个或多个错误合并：0 个返回 `undefined`；1 个原样返回（经 `toError` 规整）；多个包装成 `AggregateError(errors, message)`。如果遍历 `errors` 这个 `Iterable` 本身抛错，会把已收集的错误连同该次遍历失败一起打包进 `AggregateError` 再抛出。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function isUtilsError(value: unknown): value is UtilsError;\nfunction isUtilsAbortError(value: unknown): value is UtilsAbortError;\nfunction isUtilsTimeoutError(value: unknown): value is UtilsTimeoutError;"
                },
                {
                  "type": "paragraph",
                  "text": "标准的 `instanceof` 类型守卫，便于在 `catch` 块中按类型分支处理。"
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 26,
          "configuration": [],
          "fragment": "utils-error--walkErrorCauses"
        }
      ],
      "aliases": []
    },
    {
      "id": "utils:function",
      "library": "utils",
      "module": "function",
      "exportPath": "./function",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "assimilateCapturedThen",
          "kind": "function",
          "signature": "export declare function assimilateCapturedThen<T>(thenFn: (resolve: unknown, reject: unknown) => void, thenable: unknown): Promise<T>;",
          "declarationLine": 42,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Invokes an already-captured then method once with its original thenable receiver.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for assimilateCapturedThen.",
          "advanced": "No additional advanced behavior is declared for assimilateCapturedThen.",
          "parameters": [
            "thenFn: (resolve: unknown, reject: unknown) => void",
            "thenable: unknown"
          ],
          "parameterDetails": [
            {
              "name": "thenFn",
              "type": "(resolve: unknown, reject: unknown) => void",
              "optional": false
            },
            {
              "name": "thenable",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "Promise<T>",
          "errors": ["No documented errors are declared for assimilateCapturedThen."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for assimilateCapturedThen.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export declare function assimilateCapturedThen<T>(thenFn: (resolve: unknown, reject: unknown) => void, thenable: unknown): Promise<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "thenFn",
                "type": "(resolve: unknown, reject: unknown) => void",
                "optional": false
              },
              {
                "name": "thenable",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Promise<T>"],
          "whenToUse": "Use assimilateCapturedThen when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when assimilateCapturedThen does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Invokes an already-captured then method once with its original thenable receiver."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import assimilateCapturedThen from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use assimilateCapturedThen when its function contract and declared inputs match the result you need. Choose a different public symbol when assimilateCapturedThen does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for assimilateCapturedThen."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for assimilateCapturedThen."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:assimilateCapturedThen:export declare function assimilateCapturedThen<T>(thenFn: (resolve: unknown, reject: unknown) => void, thenable: unknown): Promise<T>;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--assimilateCapturedThen"
        },
        {
          "name": "inspectThenable",
          "kind": "function",
          "signature": "export declare function inspectThenable(value: unknown): IThenableInspection;",
          "declarationLine": 44,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Reads a callback result's then property once for synchronous-contract enforcement.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for inspectThenable.",
          "advanced": "No additional advanced behavior is declared for inspectThenable.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "IThenableInspection",
          "errors": ["No documented errors are declared for inspectThenable."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for inspectThenable.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export declare function inspectThenable(value: unknown): IThenableInspection;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IThenableInspection"],
          "whenToUse": "Use inspectThenable when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when inspectThenable does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reads a callback result's then property once for synchronous-contract enforcement."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import inspectThenable from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use inspectThenable when its function contract and declared inputs match the result you need. Choose a different public symbol when inspectThenable does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for inspectThenable."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for inspectThenable."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:inspectThenable:export declare function inspectThenable(value: unknown): IThenableInspection;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--inspectThenable"
        },
        {
          "name": "IThenableInspection",
          "kind": "type",
          "signature": "export type IThenableInspection = { readonly then: undefined; } | { readonly then: (resolve: unknown, reject: unknown) => void; } | { readonly error: unknown; };",
          "declarationLine": 27,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Result shape used when synchronous callers need to reject or observe a captured thenable.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableInspection.",
          "advanced": "No additional advanced behavior is declared for IThenableInspection.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IThenableInspection."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IThenableInspection.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export type IThenableInspection = { readonly then: undefined; } | { readonly then: (resolve: unknown, reject: unknown) => void; } | { readonly error: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IThenableInspection when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IThenableInspection does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Result shape used when synchronous callers need to reject or observe a captured thenable."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IThenableInspection from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IThenableInspection when its type contract and declared inputs match the result you need. Choose a different public symbol when IThenableInspection does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableInspection."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IThenableInspection."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:IThenableInspection:export type IThenableInspection = { readonly then: undefined; } | { readonly then: (resolve: unknown, reject: unknown) => void; } | { readonly error: unknown; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--IThenableInspection"
        },
        {
          "name": "IThenableProbe",
          "kind": "type",
          "signature": "export type IThenableProbe = { readonly kind: typeof ThenableProbeKind.notThenable; } | { readonly kind: typeof ThenableProbeKind.thenable; readonly thenFn: (resolve: unknown, reject: unknown) => void; } | { readonly kind: typeof ThenableProbeKind.failed; readonly error: unknown; };",
          "declarationLine": 17,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Result of a single, failure-contained thenable inspection.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableProbe.",
          "advanced": "No additional advanced behavior is declared for IThenableProbe.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IThenableProbe."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IThenableProbe.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export type IThenableProbe = { readonly kind: typeof ThenableProbeKind.notThenable; } | { readonly kind: typeof ThenableProbeKind.thenable; readonly thenFn: (resolve: unknown, reject: unknown) => void; } | { readonly kind: typeof ThenableProbeKind.failed; readonly error: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IThenableProbe when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IThenableProbe does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Result of a single, failure-contained thenable inspection."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IThenableProbe from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IThenableProbe when its type contract and declared inputs match the result you need. Choose a different public symbol when IThenableProbe does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableProbe."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IThenableProbe."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:IThenableProbe:export type IThenableProbe = { readonly kind: typeof ThenableProbeKind.notThenable; } | { readonly kind: typeof ThenableProbeKind.thenable; readonly thenFn: (resolve: unknown, reject: unknown) => void; } | { readonly kind: typeof ThenableProbeKind.failed; readonly error: unknown; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--IThenableProbe"
        },
        {
          "name": "IThenableProbeKind",
          "kind": "type",
          "signature": "export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];",
          "declarationLine": 15,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "IThenableProbeKind is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableProbeKind.",
          "advanced": "No additional advanced behavior is declared for IThenableProbeKind.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IThenableProbeKind."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IThenableProbeKind.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IThenableProbeKind when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IThenableProbeKind does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IThenableProbeKind is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IThenableProbeKind from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IThenableProbeKind when its type contract and declared inputs match the result you need. Choose a different public symbol when IThenableProbeKind does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableProbeKind."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IThenableProbeKind."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:IThenableProbeKind:export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--IThenableProbeKind"
        },
        {
          "name": "MAX_NATIVE_RECURSION_DEPTH",
          "kind": "const",
          "signature": "export declare const MAX_NATIVE_RECURSION_DEPTH = 256;",
          "declarationLine": 5,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Maximum synchronous reentrancy before consumers spill work from the native stack. Event-subscriber dispatch and middleware-pipeline invocation share this stable boundary.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for MAX_NATIVE_RECURSION_DEPTH.",
          "advanced": "No additional advanced behavior is declared for MAX_NATIVE_RECURSION_DEPTH.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for MAX_NATIVE_RECURSION_DEPTH."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for MAX_NATIVE_RECURSION_DEPTH.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": ["export declare const MAX_NATIVE_RECURSION_DEPTH = 256;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use MAX_NATIVE_RECURSION_DEPTH when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when MAX_NATIVE_RECURSION_DEPTH does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Maximum synchronous reentrancy before consumers spill work from the native stack. Event-subscriber dispatch and middleware-pipeline invocation share this stable boundary."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import MAX_NATIVE_RECURSION_DEPTH from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use MAX_NATIVE_RECURSION_DEPTH when its const contract and declared inputs match the result you need. Choose a different public symbol when MAX_NATIVE_RECURSION_DEPTH does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for MAX_NATIVE_RECURSION_DEPTH."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for MAX_NATIVE_RECURSION_DEPTH."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:MAX_NATIVE_RECURSION_DEPTH:export declare const MAX_NATIVE_RECURSION_DEPTH = 256;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--MAX_NATIVE_RECURSION_DEPTH"
        },
        {
          "name": "noop",
          "kind": "const",
          "signature": "export declare const noop: () => undefined;",
          "declarationLine": 35,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Returns a function that always returns the same stable value.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for noop.",
          "advanced": "No additional advanced behavior is declared for noop.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for noop."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for noop.",
          "examples": [
            "const noop: () => undefined;",
            "import { noop, once, onceAsync } from '@migaia/utils/function';",
            "const handler = options.onChange ?? noop;"
          ],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": ["export declare const noop: () => undefined;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use noop when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when noop does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Returns a function that always returns the same stable value."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import noop from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use noop when its const contract and declared inputs match the result you need. Choose a different public symbol when noop does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for noop."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for noop."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:noop:export declare const noop: () => undefined;",
          "guidance": [],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-function--noop"
        },
        {
          "name": "observeThenableRejection",
          "kind": "function",
          "signature": "export declare function observeThenableRejection(value: unknown, inspection: IThenableInspection, onRejected: (error: unknown) => void): void;",
          "declarationLine": 46,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Observes a captured thenable rejection while containing reporter failures at the boundary.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for observeThenableRejection.",
          "advanced": "No additional advanced behavior is declared for observeThenableRejection.",
          "parameters": [
            "value: unknown",
            "inspection: IThenableInspection",
            "onRejected: (error: unknown) => void"
          ],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "inspection",
              "type": "IThenableInspection",
              "optional": false
            },
            {
              "name": "onRejected",
              "type": "(error: unknown) => void",
              "optional": false
            }
          ],
          "returns": "void",
          "errors": ["No documented errors are declared for observeThenableRejection."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for observeThenableRejection.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export declare function observeThenableRejection(value: unknown, inspection: IThenableInspection, onRejected: (error: unknown) => void): void;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "inspection",
                "type": "IThenableInspection",
                "optional": false
              },
              {
                "name": "onRejected",
                "type": "(error: unknown) => void",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["void"],
          "whenToUse": "Use observeThenableRejection when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when observeThenableRejection does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Observes a captured thenable rejection while containing reporter failures at the boundary."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import observeThenableRejection from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use observeThenableRejection when its function contract and declared inputs match the result you need. Choose a different public symbol when observeThenableRejection does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for observeThenableRejection."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for observeThenableRejection."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:observeThenableRejection:export declare function observeThenableRejection(value: unknown, inspection: IThenableInspection, onRejected: (error: unknown) => void): void;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--observeThenableRejection"
        },
        {
          "name": "once",
          "kind": "function",
          "signature": "export declare function once<T extends (...args: never[]) => unknown>(functionValue: T): T;",
          "declarationLine": 48,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Memoizes a synchronous function, including its first thrown result.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for once.",
          "advanced": "No additional advanced behavior is declared for once.",
          "parameters": ["...args: never[]: unknown"],
          "parameterDetails": [
            {
              "name": "...args: never[]",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "=> unknown>(functionValue: T): T",
          "errors": ["No documented errors are declared for once."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for once.",
          "examples": [
            "// runtime.ts：整个模块只创建并导出这一份包装函数。\nexport const getRuntime = once(() => {\n  const registry = buildCommandRegistry();\n  return { registry, startedAt: Date.now() };\n});\n\nconst first = getRuntime(); // 创建 registry\nconst second = getRuntime(); // 直接返回 first\nfirst === second; // true",
            "import { once } from '@migaia/utils/function';\nimport { attachErrorIdentity } from '@migaia/utils/error';\n\nconst initOnce = once(() => {\n  try {\n    return dangerousInit();\n  } catch (error) {\n    throw attachErrorIdentity(error as Error, { source: 'my-module', code: 'INIT_FAILED' });\n  }\n});"
          ],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export declare function once<T extends (...args: never[]) => unknown>(functionValue: T): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "...args: never[]",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["=> unknown>(functionValue: T): T"],
          "whenToUse": "Use once when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when once does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Memoizes a synchronous function, including its first thrown result."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import once from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use once when its function contract and declared inputs match the result you need. Choose a different public symbol when once does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for once."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for once."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:once:export declare function once<T extends (...args: never[]) => unknown>(functionValue: T): T;",
          "guidance": [
            {
              "id": "高阶组合示例-4-一次性初始化-错误身份-确保初始化异常带上可识别的-code",
              "heading": "高阶组合示例 · 4. 一次性初始化 + 错误身份：确保初始化异常带上可识别的 code",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { once } from '@migaia/utils/function';\nimport { attachErrorIdentity } from '@migaia/utils/error';\n\nconst initOnce = once(() => {\n  try {\n    return dangerousInit();\n  } catch (error) {\n    throw attachErrorIdentity(error as Error, { source: 'my-module', code: 'INIT_FAILED' });\n  }\n});"
                }
              ]
            }
          ],
          "usageScore": 54,
          "configuration": [],
          "fragment": "utils-function--once"
        },
        {
          "name": "onceAsync",
          "kind": "function",
          "signature": "export declare function onceAsync<T>(functionValue: () => Promise<T>): () => Promise<T>;",
          "declarationLine": 50,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Memoizes one asynchronous invocation and preserves exact Promise identity.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for onceAsync.",
          "advanced": "No additional advanced behavior is declared for onceAsync.",
          "parameters": ["functionValue: () => Promise<T>"],
          "parameterDetails": [
            {
              "name": "functionValue",
              "type": "() => Promise<T>",
              "optional": false
            }
          ],
          "returns": "() => Promise<T>",
          "errors": ["No documented errors are declared for onceAsync."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for onceAsync.",
          "examples": [
            "// app-config.ts：路由和埋点会同时读取启动配置，但网络请求只能发起一次。\nexport const loadAppConfig = onceAsync(async () => {\n  const response = await fetch('/config');\n  if (!response.ok) throw new Error(`config request failed: ${response.status}`);\n  return response.json() as Promise<{ apiBaseUrl: string }>;\n});\n\nconst [routerConfig, telemetryConfig] = await Promise.all([\n  loadAppConfig(),\n  loadAppConfig()\n]); // 两处共享同一次请求",
            "// app-config.ts：多个组件启动时都可能读取配置，但网络请求只能发起一次。\nexport const loadAppConfig = onceAsync(async () => {\n  const response = await fetch('/config');\n  if (!response.ok) throw new Error(`config request failed: ${response.status}`);\n  return response.json() as Promise<{ apiBaseUrl: string }>;\n});\n\nconst [routerConfig, telemetryConfig] = await Promise.all([\n  loadAppConfig(),\n  loadAppConfig()\n]); // 两处拿到同一次请求的结果"
          ],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export declare function onceAsync<T>(functionValue: () => Promise<T>): () => Promise<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "functionValue",
                "type": "() => Promise<T>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["() => Promise<T>"],
          "whenToUse": "Use onceAsync when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when onceAsync does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Memoizes one asynchronous invocation and preserves exact Promise identity."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import onceAsync from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use onceAsync when its function contract and declared inputs match the result you need. Choose a different public symbol when onceAsync does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for onceAsync."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for onceAsync."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:onceAsync:export declare function onceAsync<T>(functionValue: () => Promise<T>): () => Promise<T>;",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "utils-function--onceAsync"
        },
        {
          "name": "probeThenable",
          "kind": "function",
          "signature": "export declare function probeThenable(value: unknown): IThenableProbe;",
          "declarationLine": 40,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Reads `value.then` once and returns an explicit discriminant. Getter failures stay in the result, allowing callers to apply their own module error policy without a second read.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeThenable.",
          "advanced": "No additional advanced behavior is declared for probeThenable.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "IThenableProbe",
          "errors": ["No documented errors are declared for probeThenable."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for probeThenable.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export declare function probeThenable(value: unknown): IThenableProbe;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IThenableProbe"],
          "whenToUse": "Use probeThenable when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when probeThenable does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reads `value.then` once and returns an explicit discriminant. Getter failures stay in the result, allowing callers to apply their own module error policy without a second read."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import probeThenable from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use probeThenable when its function contract and declared inputs match the result you need. Choose a different public symbol when probeThenable does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeThenable."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for probeThenable."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:probeThenable:export declare function probeThenable(value: unknown): IThenableProbe;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--probeThenable"
        },
        {
          "name": "ThenableProbeKind",
          "kind": "const",
          "signature": "export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; };",
          "declarationLine": 10,
          "source": "packages/utils/dist/function.d.ts",
          "purpose": "Stable outcomes for one `.then` admission read. Consumers must branch on this value instead of probing a thenable again, which preserves hostile getter and receiver semantics.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ThenableProbeKind.",
          "advanced": "No additional advanced behavior is declared for ThenableProbeKind.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for ThenableProbeKind."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ThenableProbeKind.",
          "examples": [],
          "members": [],
          "exportPath": "./function",
          "overloadSignatures": [
            "export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ThenableProbeKind when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ThenableProbeKind does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Stable outcomes for one `.then` admission read. Consumers must branch on this value instead of probing a thenable again, which preserves hostile getter and receiver semantics."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ThenableProbeKind from @migaia/utils/function, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ThenableProbeKind when its const contract and declared inputs match the result you need. Choose a different public symbol when ThenableProbeKind does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ThenableProbeKind."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ThenableProbeKind."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/function.d.ts:ThenableProbeKind:export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-function--ThenableProbeKind"
        }
      ],
      "aliases": []
    },
    {
      "id": "utils:number",
      "library": "utils",
      "module": "number",
      "exportPath": "./number",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "createNumberFormatter",
          "kind": "function",
          "signature": "export declare function createNumberFormatter(options?: INumberFormatOptions): INumberFormatter;",
          "declarationLine": 13,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Creates a reusable formatter so hot loops pay option parsing and cache lookup only once.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createNumberFormatter.",
          "advanced": "No additional advanced behavior is declared for createNumberFormatter.",
          "parameters": ["options: INumberFormatOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "INumberFormatOptions",
              "optional": true
            }
          ],
          "returns": "INumberFormatter",
          "errors": ["No documented errors are declared for createNumberFormatter."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createNumberFormatter.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export declare function createNumberFormatter(options?: INumberFormatOptions): INumberFormatter;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "INumberFormatOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["INumberFormatter"],
          "whenToUse": "Use createNumberFormatter when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createNumberFormatter does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a reusable formatter so hot loops pay option parsing and cache lookup only once."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createNumberFormatter from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createNumberFormatter when its function contract and declared inputs match the result you need. Choose a different public symbol when createNumberFormatter does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createNumberFormatter."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createNumberFormatter."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization."
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults."
            }
          ],
          "identity": "packages/utils/dist/number.d.ts:createNumberFormatter:export declare function createNumberFormatter(options?: INumberFormatOptions): INumberFormatter;",
          "guidance": [],
          "usageScore": 6,
          "configuration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionEn": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionZh": ""
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults.",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            }
          ],
          "fragment": "utils-number--createNumberFormatter"
        },
        {
          "name": "formatCompactNumber",
          "kind": "function",
          "signature": "export declare function formatCompactNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "declarationLine": 23,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Formats large quantitative values with compact notation and caller-owned precision.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatCompactNumber.",
          "advanced": "No additional advanced behavior is declared for formatCompactNumber.",
          "parameters": ["value: INumericValue", "options: INumberFormatOptions (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "INumericValue",
              "optional": false
            },
            {
              "name": "options",
              "type": "INumberFormatOptions",
              "optional": true
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for formatCompactNumber."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for formatCompactNumber.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export declare function formatCompactNumber(value: INumericValue, options?: INumberFormatOptions): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "INumericValue",
                "optional": false
              },
              {
                "name": "options",
                "type": "INumberFormatOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use formatCompactNumber when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when formatCompactNumber does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Formats large quantitative values with compact notation and caller-owned precision."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import formatCompactNumber from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use formatCompactNumber when its function contract and declared inputs match the result you need. Choose a different public symbol when formatCompactNumber does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatCompactNumber."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for formatCompactNumber."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization."
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults."
            }
          ],
          "identity": "packages/utils/dist/number.d.ts:formatCompactNumber:export declare function formatCompactNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "guidance": [],
          "usageScore": 4,
          "configuration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionEn": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionZh": ""
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults.",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            }
          ],
          "fragment": "utils-number--formatCompactNumber"
        },
        {
          "name": "formatCurrency",
          "kind": "function",
          "signature": "export declare function formatCurrency(value: INumericValue, currency: string, options?: INumberFormatOptions): string;",
          "declarationLine": 17,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Formats money while requiring the caller to own currency and rounding policy.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatCurrency.",
          "advanced": "No additional advanced behavior is declared for formatCurrency.",
          "parameters": [
            "value: INumericValue",
            "currency: string",
            "options: INumberFormatOptions (optional)"
          ],
          "parameterDetails": [
            {
              "name": "value",
              "type": "INumericValue",
              "optional": false
            },
            {
              "name": "currency",
              "type": "string",
              "optional": false
            },
            {
              "name": "options",
              "type": "INumberFormatOptions",
              "optional": true
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for formatCurrency."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for formatCurrency.",
          "examples": [
            "import { format, formatCurrency, isEmptyValue, isPrimitive } from '@migaia/utils';\n\nisEmptyValue('  '); // true；0、false、0n 均为 false\nisPrimitive(Symbol('id')); // true\nformat('库存 {stock}，金额 {money}', { stock: 0, money: '¥12.00' });\nformatCurrency(1234.5, 'CNY', {\n  locales: 'zh',\n  format: { minimumFractionDigits: 2, maximumFractionDigits: 2 }\n});"
          ],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export declare function formatCurrency(value: INumericValue, currency: string, options?: INumberFormatOptions): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "INumericValue",
                "optional": false
              },
              {
                "name": "currency",
                "type": "string",
                "optional": false
              },
              {
                "name": "options",
                "type": "INumberFormatOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use formatCurrency when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when formatCurrency does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Formats money while requiring the caller to own currency and rounding policy."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import formatCurrency from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use formatCurrency when its function contract and declared inputs match the result you need. Choose a different public symbol when formatCurrency does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatCurrency."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for formatCurrency."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization."
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults."
            }
          ],
          "identity": "packages/utils/dist/number.d.ts:formatCurrency:export declare function formatCurrency(value: INumericValue, currency: string, options?: INumberFormatOptions): string;",
          "guidance": [],
          "usageScore": 14,
          "configuration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionEn": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionZh": ""
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults.",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            }
          ],
          "fragment": "utils-number--formatCurrency"
        },
        {
          "name": "formatInteger",
          "kind": "function",
          "signature": "export declare function formatInteger(value: INumericValue, options?: INumberFormatOptions): string;",
          "declarationLine": 21,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Formats inventory-style whole numbers; callers may override fraction policy explicitly.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatInteger.",
          "advanced": "No additional advanced behavior is declared for formatInteger.",
          "parameters": ["value: INumericValue", "options: INumberFormatOptions (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "INumericValue",
              "optional": false
            },
            {
              "name": "options",
              "type": "INumberFormatOptions",
              "optional": true
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for formatInteger."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for formatInteger.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export declare function formatInteger(value: INumericValue, options?: INumberFormatOptions): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "INumericValue",
                "optional": false
              },
              {
                "name": "options",
                "type": "INumberFormatOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use formatInteger when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when formatInteger does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Formats inventory-style whole numbers; callers may override fraction policy explicitly."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import formatInteger from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use formatInteger when its function contract and declared inputs match the result you need. Choose a different public symbol when formatInteger does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatInteger."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for formatInteger."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization."
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults."
            }
          ],
          "identity": "packages/utils/dist/number.d.ts:formatInteger:export declare function formatInteger(value: INumericValue, options?: INumberFormatOptions): string;",
          "guidance": [],
          "usageScore": 4,
          "configuration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionEn": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionZh": ""
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults.",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            }
          ],
          "fragment": "utils-number--formatInteger"
        },
        {
          "name": "formatNumber",
          "kind": "function",
          "signature": "export declare function formatNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "declarationLine": 15,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Formats one number or bigint with explicit Intl policy and cached formatter reuse.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatNumber.",
          "advanced": "No additional advanced behavior is declared for formatNumber.",
          "parameters": ["value: INumericValue", "options: INumberFormatOptions (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "INumericValue",
              "optional": false
            },
            {
              "name": "options",
              "type": "INumberFormatOptions",
              "optional": true
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for formatNumber."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for formatNumber.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export declare function formatNumber(value: INumericValue, options?: INumberFormatOptions): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "INumericValue",
                "optional": false
              },
              {
                "name": "options",
                "type": "INumberFormatOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use formatNumber when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when formatNumber does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Formats one number or bigint with explicit Intl policy and cached formatter reuse."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import formatNumber from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use formatNumber when its function contract and declared inputs match the result you need. Choose a different public symbol when formatNumber does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatNumber."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for formatNumber."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization."
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults."
            }
          ],
          "identity": "packages/utils/dist/number.d.ts:formatNumber:export declare function formatNumber(value: INumericValue, options?: INumberFormatOptions): string;",
          "guidance": [],
          "usageScore": 4,
          "configuration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionEn": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionZh": ""
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults.",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            }
          ],
          "fragment": "utils-number--formatNumber"
        },
        {
          "name": "formatPercent",
          "kind": "function",
          "signature": "export declare function formatPercent(value: INumericValue, options?: INumberFormatOptions): string;",
          "declarationLine": 19,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Formats a ratio as a percentage while leaving precision policy configurable.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatPercent.",
          "advanced": "No additional advanced behavior is declared for formatPercent.",
          "parameters": ["value: INumericValue", "options: INumberFormatOptions (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "INumericValue",
              "optional": false
            },
            {
              "name": "options",
              "type": "INumberFormatOptions",
              "optional": true
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for formatPercent."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for formatPercent.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export declare function formatPercent(value: INumericValue, options?: INumberFormatOptions): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "INumericValue",
                "optional": false
              },
              {
                "name": "options",
                "type": "INumberFormatOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use formatPercent when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when formatPercent does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Formats a ratio as a percentage while leaving precision policy configurable."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import formatPercent from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use formatPercent when its function contract and declared inputs match the result you need. Choose a different public symbol when formatPercent does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for formatPercent."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for formatPercent."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization."
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults."
            }
          ],
          "identity": "packages/utils/dist/number.d.ts:formatPercent:export declare function formatPercent(value: INumericValue, options?: INumberFormatOptions): string;",
          "guidance": [],
          "usageScore": 4,
          "configuration": [
            {
              "name": "locales",
              "type": "Intl.LocalesArgument",
              "optional": true,
              "description": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionEn": "Locale preference passed through canonical `Intl.getCanonicalLocales` normalization.",
              "descriptionZh": ""
            },
            {
              "name": "format",
              "type": "Intl.NumberFormatOptions",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "Native `Intl.NumberFormat` policy merged with the helper-specific defaults.",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            }
          ],
          "fragment": "utils-number--formatPercent"
        },
        {
          "name": "INumberFormatOptions",
          "kind": "type",
          "signature": "export type INumberFormatOptions = { /** Locale preference passed through canonical `Intl.getCanonicalLocales` normalization. */ readonly locales?: Intl.LocalesArgument; /** Native `Intl.NumberFormat` policy merged with the helper-specific defaults. */ readonly format?: Intl.NumberFormatOptions; };",
          "declarationLine": 4,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Locale and Intl policy used by all numeric format helpers.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for INumberFormatOptions.",
          "advanced": "No additional advanced behavior is declared for INumberFormatOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for INumberFormatOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for INumberFormatOptions.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export type INumberFormatOptions = { /** Locale preference passed through canonical `Intl.getCanonicalLocales` normalization. */ readonly locales?: Intl.LocalesArgument; /** Native `Intl.NumberFormat` policy merged with the helper-specific defaults. */ readonly format?: Intl.NumberFormatOptions; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use INumberFormatOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when INumberFormatOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Locale and Intl policy used by all numeric format helpers."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import INumberFormatOptions from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use INumberFormatOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when INumberFormatOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for INumberFormatOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for INumberFormatOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/number.d.ts:INumberFormatOptions:export type INumberFormatOptions = { /** Locale preference passed through canonical `Intl.getCanonicalLocales` normalization. */ readonly locales?: Intl.LocalesArgument; /** Native `Intl.NumberFormat` policy merged with the helper-specific defaults. */ readonly format?: Intl.NumberFormatOptions; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-number--INumberFormatOptions"
        },
        {
          "name": "INumberFormatter",
          "kind": "type",
          "signature": "export type INumberFormatter = (value: INumericValue) => string;",
          "declarationLine": 11,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Reusable formatter for hot loops such as inventory tables and market data.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for INumberFormatter.",
          "advanced": "No additional advanced behavior is declared for INumberFormatter.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for INumberFormatter."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for INumberFormatter.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": [
            "export type INumberFormatter = (value: INumericValue) => string;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use INumberFormatter when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when INumberFormatter does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reusable formatter for hot loops such as inventory tables and market data."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import INumberFormatter from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use INumberFormatter when its type contract and declared inputs match the result you need. Choose a different public symbol when INumberFormatter does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for INumberFormatter."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for INumberFormatter."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/number.d.ts:INumberFormatter:export type INumberFormatter = (value: INumericValue) => string;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-number--INumberFormatter"
        },
        {
          "name": "INumericValue",
          "kind": "type",
          "signature": "export type INumericValue = number | bigint;",
          "declarationLine": 2,
          "source": "packages/utils/dist/number.d.ts",
          "purpose": "Public numeric values accepted without lossy coercion from strings.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for INumericValue.",
          "advanced": "No additional advanced behavior is declared for INumericValue.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for INumericValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for INumericValue.",
          "examples": [],
          "members": [],
          "exportPath": "./number",
          "overloadSignatures": ["export type INumericValue = number | bigint;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use INumericValue when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when INumericValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Public numeric values accepted without lossy coercion from strings."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import INumericValue from @migaia/utils/number, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use INumericValue when its type contract and declared inputs match the result you need. Choose a different public symbol when INumericValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for INumericValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for INumericValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/number.d.ts:INumericValue:export type INumericValue = number | bigint;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-number--INumericValue"
        }
      ],
      "aliases": []
    },
    {
      "id": "utils:object",
      "library": "utils",
      "module": "object",
      "exportPath": "./object",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "diagnosticSnapshot",
          "kind": "function",
          "signature": "export declare function diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "declarationLine": 42,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Produces a best-effort value snapshot plus explicit diagnostics.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for diagnosticSnapshot.",
          "advanced": "No additional advanced behavior is declared for diagnosticSnapshot.",
          "parameters": ["value: T"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "T",
              "optional": false
            }
          ],
          "returns": "IDiagnosticSnapshot<T>",
          "errors": ["No documented errors are declared for diagnosticSnapshot."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for diagnosticSnapshot.",
          "examples": [
            "const callback = () => 1;\nconst source = {\n  safe: { count: 1 },\n  callback,\n  get status() {\n    return 'ready';\n  }\n};\nconst { value, diagnostics } = diagnosticSnapshot(source);\n\nvalue.safe !== source.safe; // true：普通对象已复制\nvalue.callback === callback; // true：不支持的函数保留原引用\nvalue.status; // 'ready'：accessor 被读取并投影为普通值\ndiagnostics;\n// [\n//   { path: ['callback'], reason: 'unsupported', cause: callback },\n//   { path: ['status'], reason: 'accessor', cause: 'ready' }\n// ]"
          ],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export declare function diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "T",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IDiagnosticSnapshot<T>"],
          "whenToUse": "Use diagnosticSnapshot when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when diagnosticSnapshot does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Produces a best-effort value snapshot plus explicit diagnostics."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import diagnosticSnapshot from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use diagnosticSnapshot when its function contract and declared inputs match the result you need. Choose a different public symbol when diagnosticSnapshot does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for diagnosticSnapshot."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for diagnosticSnapshot."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:diagnosticSnapshot:export declare function diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "utils-object--diagnosticSnapshot"
        },
        {
          "name": "identitySnapshot",
          "kind": "function",
          "signature": "export declare function identitySnapshot<T>(value: T): T;",
          "declarationLine": 46,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Returns the original value while making identity preservation explicit.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for identitySnapshot.",
          "advanced": "No additional advanced behavior is declared for identitySnapshot.",
          "parameters": ["value: T"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "T",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for identitySnapshot."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for identitySnapshot.",
          "examples": [],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": ["export declare function identitySnapshot<T>(value: T): T;"],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "T",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use identitySnapshot when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when identitySnapshot does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Returns the original value while making identity preservation explicit."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import identitySnapshot from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use identitySnapshot when its function contract and declared inputs match the result you need. Choose a different public symbol when identitySnapshot does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for identitySnapshot."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for identitySnapshot."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:identitySnapshot:export declare function identitySnapshot<T>(value: T): T;",
          "guidance": [],
          "usageScore": 24,
          "configuration": [],
          "fragment": "utils-object--identitySnapshot"
        },
        {
          "name": "IDiagnosticSnapshot",
          "kind": "type",
          "signature": "export type IDiagnosticSnapshot<T> = { readonly value: T; readonly diagnostics: readonly ISnapshotDiagnostic[]; };",
          "declarationLine": 16,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "IDiagnosticSnapshot is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDiagnosticSnapshot.",
          "advanced": "No additional advanced behavior is declared for IDiagnosticSnapshot.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDiagnosticSnapshot."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDiagnosticSnapshot.",
          "examples": [
            "type ISnapshotDiagnostic = {\n  readonly path: readonly PropertyKey[];\n  readonly reason: 'accessor' | 'read-failed' | 'unsupported';\n  readonly cause: unknown;\n};\ntype IDiagnosticSnapshot<T> = {\n  readonly value: T;\n  readonly diagnostics: readonly ISnapshotDiagnostic[];\n};\nfunction diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;\nfunction structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;"
          ],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export type IDiagnosticSnapshot<T> = { readonly value: T; readonly diagnostics: readonly ISnapshotDiagnostic[]; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDiagnosticSnapshot when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDiagnosticSnapshot does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IDiagnosticSnapshot is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDiagnosticSnapshot from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDiagnosticSnapshot when its type contract and declared inputs match the result you need. Choose a different public symbol when IDiagnosticSnapshot does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDiagnosticSnapshot."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDiagnosticSnapshot."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:IDiagnosticSnapshot:export type IDiagnosticSnapshot<T> = { readonly value: T; readonly diagnostics: readonly ISnapshotDiagnostic[]; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-object--IDiagnosticSnapshot"
        },
        {
          "name": "immutableSnapshot",
          "kind": "function",
          "signature": "export declare function immutableSnapshot<T>(value: T): T;",
          "declarationLine": 40,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Produces an independent immutable data snapshot for supported plain values.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for immutableSnapshot.",
          "advanced": "No additional advanced behavior is declared for immutableSnapshot.",
          "parameters": ["value: T"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "T",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for immutableSnapshot."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for immutableSnapshot.",
          "examples": [
            "const shared = { count: 1 };\nconst source = {\n  primary: shared,\n  alias: shared,\n  createdAt: new Date('2026-01-01T00:00:00Z')\n};\nconst copy = immutableSnapshot(source);\n\nsource.primary.count = 2;\ncopy.primary.count; // 1\ncopy.primary === copy.alias; // true\ncopy.createdAt instanceof Date; // true\nObject.isFrozen(copy); // false",
            "const shared = { count: 1 };\nconst source = {\n  primary: shared,\n  alias: shared,\n  createdAt: new Date('2026-01-01T00:00:00Z'),\n  labels: new Map([['lang', 'zh']])\n};\nconst copy = immutableSnapshot(source);\n\nsource.primary.count = 2;\ncopy.primary.count; // 1：源对象修改不会回写快照\ncopy.primary === copy.alias; // true：共享引用关系被保留\ncopy.createdAt instanceof Date; // true：不是 JSON 字符串\nObject.isFrozen(copy); // false：独立副本不等于冻结对象"
          ],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": ["export declare function immutableSnapshot<T>(value: T): T;"],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "T",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use immutableSnapshot when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when immutableSnapshot does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Produces an independent immutable data snapshot for supported plain values."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import immutableSnapshot from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use immutableSnapshot when its function contract and declared inputs match the result you need. Choose a different public symbol when immutableSnapshot does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for immutableSnapshot."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for immutableSnapshot."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:immutableSnapshot:export declare function immutableSnapshot<T>(value: T): T;",
          "guidance": [],
          "usageScore": 44,
          "configuration": [],
          "fragment": "utils-object--immutableSnapshot"
        },
        {
          "name": "IOwnDescriptorsSnapshot",
          "kind": "type",
          "signature": "export type IOwnDescriptorsSnapshot = { readonly kind: 'captured'; readonly ok: true; readonly descriptors: Readonly<Record<PropertyKey, PropertyDescriptor>>; } | { readonly kind: 'failed'; readonly ok: false; readonly error: unknown; };",
          "declarationLine": 21,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Result of one non-throwing own-descriptor capture at an object boundary.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IOwnDescriptorsSnapshot.",
          "advanced": "No additional advanced behavior is declared for IOwnDescriptorsSnapshot.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IOwnDescriptorsSnapshot."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IOwnDescriptorsSnapshot.",
          "examples": [],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export type IOwnDescriptorsSnapshot = { readonly kind: 'captured'; readonly ok: true; readonly descriptors: Readonly<Record<PropertyKey, PropertyDescriptor>>; } | { readonly kind: 'failed'; readonly ok: false; readonly error: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IOwnDescriptorsSnapshot when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IOwnDescriptorsSnapshot does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Result of one non-throwing own-descriptor capture at an object boundary."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IOwnDescriptorsSnapshot from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IOwnDescriptorsSnapshot when its type contract and declared inputs match the result you need. Choose a different public symbol when IOwnDescriptorsSnapshot does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IOwnDescriptorsSnapshot."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IOwnDescriptorsSnapshot."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:IOwnDescriptorsSnapshot:export type IOwnDescriptorsSnapshot = { readonly kind: 'captured'; readonly ok: true; readonly descriptors: Readonly<Record<PropertyKey, PropertyDescriptor>>; } | { readonly kind: 'failed'; readonly ok: false; readonly error: unknown; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-object--IOwnDescriptorsSnapshot"
        },
        {
          "name": "IProbePropertyResult",
          "kind": "type",
          "signature": "export type IProbePropertyResult<T> = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: T; } | { readonly kind: 'failed'; readonly error: unknown; };",
          "declarationLine": 2,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "IProbePropertyResult is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IProbePropertyResult.",
          "advanced": "No additional advanced behavior is declared for IProbePropertyResult.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IProbePropertyResult."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IProbePropertyResult.",
          "examples": [
            "type IProbePropertyResult<T> =\n  | { readonly kind: 'missing' }\n  | { readonly kind: 'value'; readonly value: T }\n  | { readonly kind: 'failed'; readonly error: unknown };\nfunction probeProperty<T>(value: object, key: PropertyKey): IProbePropertyResult<T>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export type IProbePropertyResult<T> = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: T; } | { readonly kind: 'failed'; readonly error: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IProbePropertyResult when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IProbePropertyResult does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IProbePropertyResult is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IProbePropertyResult from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IProbePropertyResult when its type contract and declared inputs match the result you need. Choose a different public symbol when IProbePropertyResult does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IProbePropertyResult."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IProbePropertyResult."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:IProbePropertyResult:export type IProbePropertyResult<T> = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: T; } | { readonly kind: 'failed'; readonly error: unknown; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "utils-object--IProbePropertyResult"
        },
        {
          "name": "ISnapshotDiagnostic",
          "kind": "type",
          "signature": "export type ISnapshotDiagnostic = { readonly path: readonly PropertyKey[]; readonly reason: 'accessor' | 'read-failed' | 'unsupported'; readonly cause: unknown; };",
          "declarationLine": 11,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "ISnapshotDiagnostic is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISnapshotDiagnostic.",
          "advanced": "No additional advanced behavior is declared for ISnapshotDiagnostic.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ISnapshotDiagnostic."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ISnapshotDiagnostic.",
          "examples": [
            "type ISnapshotDiagnostic = {\n  readonly path: readonly PropertyKey[];\n  readonly reason: 'accessor' | 'read-failed' | 'unsupported';\n  readonly cause: unknown;\n};\ntype IDiagnosticSnapshot<T> = {\n  readonly value: T;\n  readonly diagnostics: readonly ISnapshotDiagnostic[];\n};\nfunction diagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;\nfunction structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;"
          ],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export type ISnapshotDiagnostic = { readonly path: readonly PropertyKey[]; readonly reason: 'accessor' | 'read-failed' | 'unsupported'; readonly cause: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ISnapshotDiagnostic when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ISnapshotDiagnostic does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ISnapshotDiagnostic is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ISnapshotDiagnostic from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ISnapshotDiagnostic when its type contract and declared inputs match the result you need. Choose a different public symbol when ISnapshotDiagnostic does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISnapshotDiagnostic."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ISnapshotDiagnostic."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:ISnapshotDiagnostic:export type ISnapshotDiagnostic = { readonly path: readonly PropertyKey[]; readonly reason: 'accessor' | 'read-failed' | 'unsupported'; readonly cause: unknown; };",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-object--ISnapshotDiagnostic"
        },
        {
          "name": "isPlainObject",
          "kind": "function",
          "signature": "export declare function isPlainObject(value: unknown): value is Record<PropertyKey, unknown>;",
          "declarationLine": 31,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Accepts only ordinary record objects or null-prototype records, never arrays.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isPlainObject.",
          "advanced": "No additional advanced behavior is declared for isPlainObject.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "value is Record<PropertyKey, unknown>",
          "errors": ["No documented errors are declared for isPlainObject."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isPlainObject.",
          "examples": [
            "isPlainObject({}); // true\nisPlainObject([]); // false\nisPlainObject(new Date()); // false"
          ],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export declare function isPlainObject(value: unknown): value is Record<PropertyKey, unknown>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["value is Record<PropertyKey, unknown>"],
          "whenToUse": "Use isPlainObject when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isPlainObject does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Accepts only ordinary record objects or null-prototype records, never arrays."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isPlainObject from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isPlainObject when its function contract and declared inputs match the result you need. Choose a different public symbol when isPlainObject does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isPlainObject."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isPlainObject."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:isPlainObject:export declare function isPlainObject(value: unknown): value is Record<PropertyKey, unknown>;",
          "guidance": [],
          "usageScore": 26,
          "configuration": [],
          "fragment": "utils-object--isPlainObject"
        },
        {
          "name": "probeProperty",
          "kind": "function",
          "signature": "export declare function probeProperty<T>(value: object, key: PropertyKey): IProbePropertyResult<T>;",
          "declarationLine": 33,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Reads one property exactly once and exposes getter failures explicitly.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeProperty.",
          "advanced": "No additional advanced behavior is declared for probeProperty.",
          "parameters": ["value: object", "key: PropertyKey"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "object",
              "optional": false
            },
            {
              "name": "key",
              "type": "PropertyKey",
              "optional": false
            }
          ],
          "returns": "IProbePropertyResult<T>",
          "errors": ["No documented errors are declared for probeProperty."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for probeProperty.",
          "examples": [
            "const profileUnavailable = new Error('profile unavailable');\nlet profileReads = 0;\nconst user = {\n  name: 'Ada',\n  get profile(): never {\n    profileReads += 1;\n    throw profileUnavailable;\n  }\n};\n\nfunction readLabel(value: object, key: PropertyKey): string {\n  const result = probeProperty<string>(value, key);\n  switch (result.kind) {\n    case 'value':\n      return result.value;\n    case 'missing':\n      return 'Anonymous';\n    case 'failed':\n      console.error('property read failed', result.error);\n      return 'Unavailable';\n  }\n}\n\nreadLabel(user, 'name'); // 'Ada'\nreadLabel(user, 'email'); // 'Anonymous'\nreadLabel(user, 'profile'); // 'Unavailable'\nprofileReads; // 1：getter 只被读取一次",
            "const profileUnavailable = new Error('profile unavailable');\nlet profileReads = 0;\nconst user = {\n  name: 'Ada',\n  get profile(): never {\n    profileReads += 1;\n    throw profileUnavailable;\n  }\n};\n\nfunction readLabel(value: object, key: PropertyKey): string {\n  const result = probeProperty<string>(value, key);\n  switch (result.kind) {\n    case 'value':\n      return result.value;\n    case 'missing':\n      return 'Anonymous';\n    case 'failed':\n      console.error('property read failed', result.error);\n      return 'Unavailable';\n  }\n}\n\nreadLabel(user, 'name'); // 'Ada'：读取成功，消费 value\nreadLabel(user, 'email'); // 'Anonymous'：字段不存在，走业务默认值\nreadLabel(user, 'profile'); // 'Unavailable'：getter 异常被显式上报\nprofileReads; // 1：probeProperty 没有为了判断结果而重复触发 getter"
          ],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export declare function probeProperty<T>(value: object, key: PropertyKey): IProbePropertyResult<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "object",
                "optional": false
              },
              {
                "name": "key",
                "type": "PropertyKey",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IProbePropertyResult<T>"],
          "whenToUse": "Use probeProperty when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when probeProperty does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reads one property exactly once and exposes getter failures explicitly."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import probeProperty from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use probeProperty when its function contract and declared inputs match the result you need. Choose a different public symbol when probeProperty does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeProperty."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for probeProperty."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:probeProperty:export declare function probeProperty<T>(value: object, key: PropertyKey): IProbePropertyResult<T>;",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "utils-object--probeProperty"
        },
        {
          "name": "snapshotOwnDescriptors",
          "kind": "function",
          "signature": "export declare function snapshotOwnDescriptors(value: unknown): IOwnDescriptorsSnapshot;",
          "declarationLine": 38,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Captures own descriptors at most once and contains revoked-proxy or descriptor-trap failures. Primitive inputs are valid empty snapshots; callers decide their module-specific value policy.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for snapshotOwnDescriptors.",
          "advanced": "No additional advanced behavior is declared for snapshotOwnDescriptors.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "IOwnDescriptorsSnapshot",
          "errors": ["No documented errors are declared for snapshotOwnDescriptors."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for snapshotOwnDescriptors.",
          "examples": [],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export declare function snapshotOwnDescriptors(value: unknown): IOwnDescriptorsSnapshot;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IOwnDescriptorsSnapshot"],
          "whenToUse": "Use snapshotOwnDescriptors when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when snapshotOwnDescriptors does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Captures own descriptors at most once and contains revoked-proxy or descriptor-trap failures. Primitive inputs are valid empty snapshots; callers decide their module-specific value policy."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import snapshotOwnDescriptors from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use snapshotOwnDescriptors when its function contract and declared inputs match the result you need. Choose a different public symbol when snapshotOwnDescriptors does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for snapshotOwnDescriptors."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for snapshotOwnDescriptors."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:snapshotOwnDescriptors:export declare function snapshotOwnDescriptors(value: unknown): IOwnDescriptorsSnapshot;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-object--snapshotOwnDescriptors"
        },
        {
          "name": "structuredDiagnosticSnapshot",
          "kind": "function",
          "signature": "export declare function structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "declarationLine": 44,
          "source": "packages/utils/dist/object.d.ts",
          "purpose": "Produces one structuredClone-first diagnostic projection for tolerant consumers.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for structuredDiagnosticSnapshot.",
          "advanced": "No additional advanced behavior is declared for structuredDiagnosticSnapshot.",
          "parameters": ["value: T"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "T",
              "optional": false
            }
          ],
          "returns": "IDiagnosticSnapshot<T>",
          "errors": ["No documented errors are declared for structuredDiagnosticSnapshot."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for structuredDiagnosticSnapshot.",
          "examples": [],
          "members": [],
          "exportPath": "./object",
          "overloadSignatures": [
            "export declare function structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "T",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IDiagnosticSnapshot<T>"],
          "whenToUse": "Use structuredDiagnosticSnapshot when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when structuredDiagnosticSnapshot does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Produces one structuredClone-first diagnostic projection for tolerant consumers."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import structuredDiagnosticSnapshot from @migaia/utils/object, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use structuredDiagnosticSnapshot when its function contract and declared inputs match the result you need. Choose a different public symbol when structuredDiagnosticSnapshot does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for structuredDiagnosticSnapshot."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for structuredDiagnosticSnapshot."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object.d.ts:structuredDiagnosticSnapshot:export declare function structuredDiagnosticSnapshot<T>(value: T): IDiagnosticSnapshot<T>;",
          "guidance": [],
          "usageScore": 24,
          "configuration": [],
          "fragment": "utils-object--structuredDiagnosticSnapshot"
        }
      ],
      "aliases": [
        {
          "identity": "packages/utils/dist/object-path.d.ts:createPathAccessor:export declare function createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
          "name": "createPathAccessor",
          "signature": "export declare function createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--createPathAccessor"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:get:export declare function get<T, P extends IObjectPathInput<T>>(object: T, path: P): IObjectPathValue<T, P> | undefined;",
          "name": "get",
          "signature": "export declare function get<T, P extends IObjectPathInput<T>>(object: T, path: P): IObjectPathValue<T, P> | undefined;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--get"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPath:export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "name": "IObjectPath",
          "signature": "export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPath"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathInput:export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "name": "IObjectPathInput",
          "signature": "export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathInput"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathSegment:export type IObjectPathSegment = string | number | symbol;",
          "name": "IObjectPathSegment",
          "signature": "export type IObjectPathSegment = string | number | symbol;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathSegment"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTuple:export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "name": "IObjectPathTuple",
          "signature": "export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathTuple"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTupleFor:export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "name": "IObjectPathTupleFor",
          "signature": "export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathTupleFor"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathValue:export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "name": "IObjectPathValue",
          "signature": "export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathValue"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathWriteValue:export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "name": "IObjectPathWriteValue",
          "signature": "export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathWriteValue"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathAccessor:export type IPathAccessor<T> = { readonly value: T; get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined; set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T; probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>; parsePath(path: string | IObjectPathTuple): IObjectPathTuple; };",
          "name": "IPathAccessor",
          "signature": "export type IPathAccessor<T> = { readonly value: T; get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined; set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T; probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>; parsePath(path: string | IObjectPathTuple): IObjectPathTuple; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathAccessor"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathAccessorOptions:export type IPathAccessorOptions<T> = { readonly ifMissing?: (probe: IPathMissingProbe) => void; readonly ifBlocked?: (probe: IPathBlockedProbe) => void; readonly ifFailed?: (probe: IPathFailedProbe) => void; readonly onGet?: (event: IStringGetEvent<T> | ITupleGetEvent<T>) => void; readonly onSet?: (event: IStringSetEvent<T> | ITupleSetEvent<T>) => void; };",
          "name": "IPathAccessorOptions",
          "signature": "export type IPathAccessorOptions<T> = { readonly ifMissing?: (probe: IPathMissingProbe) => void; readonly ifBlocked?: (probe: IPathBlockedProbe) => void; readonly ifFailed?: (probe: IPathFailedProbe) => void; readonly onGet?: (event: IStringGetEvent<T> | ITupleGetEvent<T>) => void; readonly onSet?: (event: IStringSetEvent<T> | ITupleSetEvent<T>) => void; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathAccessorOptions"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathBlockedProbe:export type IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'blocked'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "name": "IPathBlockedProbe",
          "signature": "export type IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'blocked'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathBlockedProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathFailedProbe:export type IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'failed'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; readonly error: unknown; };",
          "name": "IPathFailedProbe",
          "signature": "export type IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'failed'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; readonly error: unknown; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathFailedProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathGetEvent:export type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathValue<T, P>; replace(value: IObjectPathValue<T, P>): void; };",
          "name": "IPathGetEvent",
          "signature": "export type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathValue<T, P>; replace(value: IObjectPathValue<T, P>): void; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathGetEvent"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathMissingProbe:export type IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'missing'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "name": "IPathMissingProbe",
          "signature": "export type IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'missing'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathMissingProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathProbe:export type IPathProbe<T, P extends IObjectPathInput<T>> = IPathValueProbe<T, P> | IPathMissingProbe<P> | IPathBlockedProbe<P> | IPathFailedProbe<P>;",
          "name": "IPathProbe",
          "signature": "export type IPathProbe<T, P extends IObjectPathInput<T>> = IPathValueProbe<T, P> | IPathMissingProbe<P> | IPathBlockedProbe<P> | IPathFailedProbe<P>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathSetEvent:export type IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathWriteValue<T, P>; replace(value: IObjectPathWriteValue<T, P>): void; };",
          "name": "IPathSetEvent",
          "signature": "export type IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathWriteValue<T, P>; replace(value: IObjectPathWriteValue<T, P>): void; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathSetEvent"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IPathValueProbe:export type IPathValueProbe<T, P extends IObjectPathInput<T>> = { readonly kind: 'value'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly value: IObjectPathValue<T, P>; };",
          "name": "IPathValueProbe",
          "signature": "export type IPathValueProbe<T, P extends IObjectPathInput<T>> = { readonly kind: 'value'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly value: IObjectPathValue<T, P>; };",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IPathValueProbe"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:parseObjectPath:export declare function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;",
          "name": "parseObjectPath",
          "signature": "export declare function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--parseObjectPath"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:probeObjectPath:export declare function probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;",
          "name": "probeObjectPath",
          "signature": "export declare function probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--probeObjectPath"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:probeObjectPathSegments:export declare function probeObjectPathSegments<T, P extends IObjectPathInput<T>>(object: T, segments: P): IPathProbe<T, P>;",
          "name": "probeObjectPathSegments",
          "signature": "export declare function probeObjectPathSegments<T, P extends IObjectPathInput<T>>(object: T, segments: P): IPathProbe<T, P>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--probeObjectPathSegments"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:set:export declare function set<T, P extends IObjectPathInput<T>>(object: T, path: P, nextValue: IObjectPathWriteValue<T, P>): T;",
          "name": "set",
          "signature": "export declare function set<T, P extends IObjectPathInput<T>>(object: T, path: P, nextValue: IObjectPathWriteValue<T, P>): T;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./object",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--set"
        }
      ]
    },
    {
      "id": "utils:object-path",
      "library": "utils",
      "module": "object-path",
      "exportPath": "./object-path",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "createPathAccessor",
          "kind": "function",
          "signature": "export declare function createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
          "declarationLine": 116,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Creates a reusable accessor whose immutable root advances after each successful set.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createPathAccessor.",
          "advanced": "No additional advanced behavior is declared for createPathAccessor.",
          "parameters": ["object: T", "options: IPathAccessorOptions<T> (optional)"],
          "parameterDetails": [
            {
              "name": "object",
              "type": "T",
              "optional": false
            },
            {
              "name": "options",
              "type": "IPathAccessorOptions<T>",
              "optional": true
            }
          ],
          "returns": "IPathAccessor<T>",
          "errors": ["No documented errors are declared for createPathAccessor."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createPathAccessor.",
          "examples": [
            "const accessor = createPathAccessor(\n  { count: 0 },\n  { onSet: (event) => console.log('write', event.originKey, event.value) }\n);\naccessor.set('count', 5); // 打印 write count 5\naccessor.value; // { count: 5 }",
            "const accessor = createPathAccessor({ count: 0 });\naccessor.set('count', 5);\naccessor.value; // { count: 5 }"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export declare function createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "object",
                "type": "T",
                "optional": false
              },
              {
                "name": "options",
                "type": "IPathAccessorOptions<T>",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IPathAccessor<T>"],
          "whenToUse": "Use createPathAccessor when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createPathAccessor does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a reusable accessor whose immutable root advances after each successful set."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createPathAccessor from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createPathAccessor when its function contract and declared inputs match the result you need. Choose a different public symbol when createPathAccessor does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createPathAccessor."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createPathAccessor."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "ifMissing",
              "type": "(probe: IPathMissingProbe) => void",
              "optional": true,
              "description": ""
            },
            {
              "name": "ifBlocked",
              "type": "(probe: IPathBlockedProbe) => void",
              "optional": true,
              "description": ""
            },
            {
              "name": "ifFailed",
              "type": "(probe: IPathFailedProbe) => void",
              "optional": true,
              "description": ""
            },
            {
              "name": "onGet",
              "type": "(event: IStringGetEvent<T> | ITupleGetEvent<T>) => void",
              "optional": true,
              "description": ""
            },
            {
              "name": "onSet",
              "type": "(event: IStringSetEvent<T> | ITupleSetEvent<T>) => void",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/object-path.d.ts:createPathAccessor:export declare function createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
          "guidance": [],
          "usageScore": 38,
          "configuration": [
            {
              "name": "ifMissing",
              "type": "(probe: IPathMissingProbe) => void",
              "optional": true,
              "description": "封装一个内部持有当前根对象的有状态访问器：每次 `set()` 成功后，`accessor.value` 自动前进到写入后的新根（后续 `get`/`set` 都基于最新根）。`ifMissing`/`ifBlocked`/`ifFailed` 在对应探测结果出现时被调用，用作集中式诊断钩子。",
              "descriptionEn": "",
              "descriptionZh": "封装一个内部持有当前根对象的有状态访问器：每次 `set()` 成功后，`accessor.value` 自动前进到写入后的新根（后续 `get`/`set` 都基于最新根）。`ifMissing`/`ifBlocked`/`ifFailed` 在对应探测结果出现时被调用，用作集中式诊断钩子。"
            },
            {
              "name": "ifBlocked",
              "type": "(probe: IPathBlockedProbe) => void",
              "optional": true,
              "description": "封装一个内部持有当前根对象的有状态访问器：每次 `set()` 成功后，`accessor.value` 自动前进到写入后的新根（后续 `get`/`set` 都基于最新根）。`ifMissing`/`ifBlocked`/`ifFailed` 在对应探测结果出现时被调用，用作集中式诊断钩子。",
              "descriptionEn": "",
              "descriptionZh": "封装一个内部持有当前根对象的有状态访问器：每次 `set()` 成功后，`accessor.value` 自动前进到写入后的新根（后续 `get`/`set` 都基于最新根）。`ifMissing`/`ifBlocked`/`ifFailed` 在对应探测结果出现时被调用，用作集中式诊断钩子。"
            },
            {
              "name": "ifFailed",
              "type": "(probe: IPathFailedProbe) => void",
              "optional": true,
              "description": "封装一个内部持有当前根对象的有状态访问器：每次 `set()` 成功后，`accessor.value` 自动前进到写入后的新根（后续 `get`/`set` 都基于最新根）。`ifMissing`/`ifBlocked`/`ifFailed` 在对应探测结果出现时被调用，用作集中式诊断钩子。",
              "descriptionEn": "",
              "descriptionZh": "封装一个内部持有当前根对象的有状态访问器：每次 `set()` 成功后，`accessor.value` 自动前进到写入后的新根（后续 `get`/`set` 都基于最新根）。`ifMissing`/`ifBlocked`/`ifFailed` 在对应探测结果出现时被调用，用作集中式诊断钩子。"
            },
            {
              "name": "onGet",
              "type": "(event: IStringGetEvent<T> | ITupleGetEvent<T>) => void",
              "optional": true,
              "description": "`onGet`/`onSet` 在每次成功读/写时被调用，事件对象字段：`originKey`（原始传入路径）、`segments`（解析后的段元组）、`probe`（本次读/写命中的 `IPathValueProbe`，即成功探测结果）、`value`（本次实际读到/将要写入的值）、`replace(value)`（调用后就地改写本次读取/写入的实际返回值/写入值，例如做统一的默认值填充或写入前校验/转换）。`IPathGetEvent`/`IPathSetEvent` 唯一的区别是 `value`/`replace` 的类型：读事件用 `IObjectPathValue`（读到的原值类型），写事件用 `IObjectPathWriteValue`（字面量放宽后的可写类型）。",
              "descriptionEn": "",
              "descriptionZh": "`onGet`/`onSet` 在每次成功读/写时被调用，事件对象字段：`originKey`（原始传入路径）、`segments`（解析后的段元组）、`probe`（本次读/写命中的 `IPathValueProbe`，即成功探测结果）、`value`（本次实际读到/将要写入的值）、`replace(value)`（调用后就地改写本次读取/写入的实际返回值/写入值，例如做统一的默认值填充或写入前校验/转换）。`IPathGetEvent`/`IPathSetEvent` 唯一的区别是 `value`/`replace` 的类型：读事件用 `IObjectPathValue`（读到的原值类型），写事件用 `IObjectPathWriteValue`（字面量放宽后的可写类型）。"
            },
            {
              "name": "onSet",
              "type": "(event: IStringSetEvent<T> | ITupleSetEvent<T>) => void",
              "optional": true,
              "description": "`onGet`/`onSet` 在每次成功读/写时被调用，事件对象字段：`originKey`（原始传入路径）、`segments`（解析后的段元组）、`probe`（本次读/写命中的 `IPathValueProbe`，即成功探测结果）、`value`（本次实际读到/将要写入的值）、`replace(value)`（调用后就地改写本次读取/写入的实际返回值/写入值，例如做统一的默认值填充或写入前校验/转换）。`IPathGetEvent`/`IPathSetEvent` 唯一的区别是 `value`/`replace` 的类型：读事件用 `IObjectPathValue`（读到的原值类型），写事件用 `IObjectPathWriteValue`（字面量放宽后的可写类型）。",
              "descriptionEn": "",
              "descriptionZh": "`onGet`/`onSet` 在每次成功读/写时被调用，事件对象字段：`originKey`（原始传入路径）、`segments`（解析后的段元组）、`probe`（本次读/写命中的 `IPathValueProbe`，即成功探测结果）、`value`（本次实际读到/将要写入的值）、`replace(value)`（调用后就地改写本次读取/写入的实际返回值/写入值，例如做统一的默认值填充或写入前校验/转换）。`IPathGetEvent`/`IPathSetEvent` 唯一的区别是 `value`/`replace` 的类型：读事件用 `IObjectPathValue`（读到的原值类型），写事件用 `IObjectPathWriteValue`（字面量放宽后的可写类型）。"
            }
          ],
          "fragment": "utils-object-path--createPathAccessor"
        },
        {
          "name": "get",
          "kind": "function",
          "signature": "export declare function get<T, P extends IObjectPathInput<T>>(object: T, path: P): IObjectPathValue<T, P> | undefined;",
          "declarationLine": 112,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Reads one path; missing/blocked paths yield undefined while getter failures remain observable.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for get.",
          "advanced": "No additional advanced behavior is declared for get.",
          "parameters": ["object: T", "path: P"],
          "parameterDetails": [
            {
              "name": "object",
              "type": "T",
              "optional": false
            },
            {
              "name": "path",
              "type": "P",
              "optional": false
            }
          ],
          "returns": "IObjectPathValue<T, P> | undefined",
          "errors": ["No documented errors are declared for get."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for get.",
          "examples": [
            "const found = await toPromise(() => cache.get('key'));\nconst pending = toPromise(() => {\n  throw new Error('read failed');\n});\nawait pending; // 异步观察 rejection",
            "const data = { user: { name: 'Ada' } };\nget(data, 'user.name'); // 'Ada'\nget(data, 'user.missing'); // undefined（missing）\nget(data, 'user.name.x'); // undefined（blocked：'Ada' 是字符串，无法继续深入 .x）",
            "const value = await toPromise(() => cache.get('key'));\nawait toPromise(() => {\n  throw new Error('failed'); // 不会从 toPromise 调用点同步抛出，而是返回 rejected Promise\n});",
            "const data = {\n  user: { name: 'Ada' },\n  settings: { theme: 'dark' }\n};\nget(data, 'user.name'); // 'Ada'：只读，不修改 data\n\nconst next = set(data, 'user.name', 'Grace');\nnext !== data; // true：值发生变化时，每次 set 都返回新根对象\nnext.user !== data.user; // true：路径上的对象被浅拷贝\nnext.settings === data.settings; // true：未修改分支继续共享引用\ndata.user.name; // 'Ada'：原对象保持不变\nObject.isFrozen(next); // false：immutable update 不等于 freeze",
            "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export declare function get<T, P extends IObjectPathInput<T>>(object: T, path: P): IObjectPathValue<T, P> | undefined;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "object",
                "type": "T",
                "optional": false
              },
              {
                "name": "path",
                "type": "P",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IObjectPathValue<T, P> | undefined"],
          "whenToUse": "Use get when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when get does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reads one path; missing/blocked paths yield undefined while getter failures remain observable."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import get from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use get when its function contract and declared inputs match the result you need. Choose a different public symbol when get does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for get."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for get."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:get:export declare function get<T, P extends IObjectPathInput<T>>(object: T, path: P): IObjectPathValue<T, P> | undefined;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "高阶组合示例-3-对象路径-配置-把校验后的嵌套结构整体写回配置根",
              "heading": "高阶组合示例 · 3. 对象路径 + 配置：把校验后的嵌套结构整体写回配置根",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
                }
              ]
            }
          ],
          "usageScore": 110,
          "configuration": [],
          "fragment": "utils-object-path--get"
        },
        {
          "name": "IObjectPath",
          "kind": "type",
          "signature": "export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "declarationLine": 11,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Typed dot/bracket paths, capped at eight levels to bound compiler work.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPath.",
          "advanced": "No additional advanced behavior is declared for IObjectPath.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObjectPath."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObjectPath.",
          "examples": [
            "/** 按一个 string 或 tuple 对象路径对联合类型分组，路径解析规则与 /object 模块的 IObjectPath 完全一致。 */\ntype IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = {\n  [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>;\n};",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObjectPath when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObjectPath does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Typed dot/bracket paths, capped at eight levels to bound compiler work."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObjectPath from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObjectPath when its type contract and declared inputs match the result you need. Choose a different public symbol when IObjectPath does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPath."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObjectPath."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPath:export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "guidance": [],
          "usageScore": 36,
          "configuration": [],
          "fragment": "utils-object-path--IObjectPath"
        },
        {
          "name": "IObjectPathInput",
          "kind": "type",
          "signature": "export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "declarationLine": 19,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "All statically valid string and tuple paths for one root type.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathInput.",
          "advanced": "No additional advanced behavior is declared for IObjectPathInput.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObjectPathInput."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObjectPathInput.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "function get<T, P extends IObjectPathInput<T>>(\n  object: T,\n  path: P\n): IObjectPathValue<T, P> | undefined;",
            "function set<T, P extends IObjectPathInput<T>>(\n  object: T,\n  path: P,\n  nextValue: IObjectPathWriteValue<T, P>\n): T;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
            "/** 按一个 string 或 tuple 对象路径对联合类型分组，路径解析规则与 /object 模块的 IObjectPath 完全一致。 */\ntype IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = {\n  [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>;\n};",
            "type ICollectorActions<T, Self> = {\n  readonly result: readonly T[];\n  fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(\n    ...paths: P\n  ): IFieldCollector<T, P[number]>;\n  where(predicate: (source: T) => boolean): Self;\n  distinctBy<P extends IObjectPathInput<T>>(path: P): Self;\n  skip(count: number): Self;\n  take(count: number): Self;\n};\n\ninterface ICollector<T> extends ICollectorActions<T, ICollector<T>> {}\n\ninterface IFieldCollector<T, P extends IObjectPathInput<T>>\n  extends ICollectorActions<T, IFieldCollector<T, P>> {\n  like(query: string): IFieldCollector<T, P>;\n  equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>;\n  oneOf(\n    value: IObjectPathValue<T, P>,\n    ...values: IObjectPathValue<T, P>[]\n  ): IFieldCollector<T, P>;\n}\n\nfunction collect<T>(dataSource: readonly T[]): ICollector<T>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObjectPathInput when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObjectPathInput does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "All statically valid string and tuple paths for one root type."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObjectPathInput from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObjectPathInput when its type contract and declared inputs match the result you need. Choose a different public symbol when IObjectPathInput does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathInput."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObjectPathInput."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathInput:export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "guidance": [],
          "usageScore": 116,
          "configuration": [],
          "fragment": "utils-object-path--IObjectPathInput"
        },
        {
          "name": "IObjectPathSegment",
          "kind": "type",
          "signature": "export type IObjectPathSegment = string | number | symbol;",
          "declarationLine": 2,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Property keys accepted by tuple paths.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathSegment.",
          "advanced": "No additional advanced behavior is declared for IObjectPathSegment.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObjectPathSegment."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObjectPathSegment.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": ["export type IObjectPathSegment = string | number | symbol;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObjectPathSegment when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObjectPathSegment does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Property keys accepted by tuple paths."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObjectPathSegment from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObjectPathSegment when its type contract and declared inputs match the result you need. Choose a different public symbol when IObjectPathSegment does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathSegment."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObjectPathSegment."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathSegment:export type IObjectPathSegment = string | number | symbol;",
          "guidance": [],
          "usageScore": 38,
          "configuration": [],
          "fragment": "utils-object-path--IObjectPathSegment"
        },
        {
          "name": "IObjectPathTuple",
          "kind": "type",
          "signature": "export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "declarationLine": 3,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IObjectPathTuple is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathTuple.",
          "advanced": "No additional advanced behavior is declared for IObjectPathTuple.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObjectPathTuple."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObjectPathTuple.",
          "examples": [
            "function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;",
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": ["export type IObjectPathTuple = readonly IObjectPathSegment[];"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObjectPathTuple when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObjectPathTuple does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IObjectPathTuple is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObjectPathTuple from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObjectPathTuple when its type contract and declared inputs match the result you need. Choose a different public symbol when IObjectPathTuple does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathTuple."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObjectPathTuple."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTuple:export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "guidance": [],
          "usageScore": 102,
          "configuration": [],
          "fragment": "utils-object-path--IObjectPathTuple"
        },
        {
          "name": "IObjectPathTupleFor",
          "kind": "type",
          "signature": "export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "declarationLine": 15,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Typed readonly tuple paths, including number indexes and symbol object keys.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathTupleFor.",
          "advanced": "No additional advanced behavior is declared for IObjectPathTupleFor.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObjectPathTupleFor."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObjectPathTupleFor.",
          "examples": [
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObjectPathTupleFor when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObjectPathTupleFor does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Typed readonly tuple paths, including number indexes and symbol object keys."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObjectPathTupleFor from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObjectPathTupleFor when its type contract and declared inputs match the result you need. Choose a different public symbol when IObjectPathTupleFor does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathTupleFor."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObjectPathTupleFor."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTupleFor:export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "guidance": [],
          "usageScore": 20,
          "configuration": [],
          "fragment": "utils-object-path--IObjectPathTupleFor"
        },
        {
          "name": "IObjectPathValue",
          "kind": "type",
          "signature": "export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "declarationLine": 27,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Resolves a string or tuple path to its value type.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathValue.",
          "advanced": "No additional advanced behavior is declared for IObjectPathValue.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObjectPathValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObjectPathValue.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "function get<T, P extends IObjectPathInput<T>>(\n  object: T,\n  path: P\n): IObjectPathValue<T, P> | undefined;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
            "/** 按一个 string 或 tuple 对象路径对联合类型分组，路径解析规则与 /object 模块的 IObjectPath 完全一致。 */\ntype IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = {\n  [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>;\n};",
            "type ICollectorActions<T, Self> = {\n  readonly result: readonly T[];\n  fieldBy<const P extends readonly [IObjectPathInput<T>, ...IObjectPathInput<T>[]]>(\n    ...paths: P\n  ): IFieldCollector<T, P[number]>;\n  where(predicate: (source: T) => boolean): Self;\n  distinctBy<P extends IObjectPathInput<T>>(path: P): Self;\n  skip(count: number): Self;\n  take(count: number): Self;\n};\n\ninterface ICollector<T> extends ICollectorActions<T, ICollector<T>> {}\n\ninterface IFieldCollector<T, P extends IObjectPathInput<T>>\n  extends ICollectorActions<T, IFieldCollector<T, P>> {\n  like(query: string): IFieldCollector<T, P>;\n  equals(value: IObjectPathValue<T, P>): IFieldCollector<T, P>;\n  oneOf(\n    value: IObjectPathValue<T, P>,\n    ...values: IObjectPathValue<T, P>[]\n  ): IFieldCollector<T, P>;\n}\n\nfunction collect<T>(dataSource: readonly T[]): ICollector<T>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObjectPathValue when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObjectPathValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Resolves a string or tuple path to its value type."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObjectPathValue from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObjectPathValue when its type contract and declared inputs match the result you need. Choose a different public symbol when IObjectPathValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObjectPathValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathValue:export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "guidance": [],
          "usageScore": 78,
          "configuration": [],
          "fragment": "utils-object-path--IObjectPathValue"
        },
        {
          "name": "IObjectPathWriteValue",
          "kind": "type",
          "signature": "export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "declarationLine": 31,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Resolves the accepted replacement type for a string or tuple path.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathWriteValue.",
          "advanced": "No additional advanced behavior is declared for IObjectPathWriteValue.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObjectPathWriteValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObjectPathWriteValue.",
          "examples": [
            "function set<T, P extends IObjectPathInput<T>>(\n  object: T,\n  path: P,\n  nextValue: IObjectPathWriteValue<T, P>\n): T;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObjectPathWriteValue when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObjectPathWriteValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Resolves the accepted replacement type for a string or tuple path."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObjectPathWriteValue from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObjectPathWriteValue when its type contract and declared inputs match the result you need. Choose a different public symbol when IObjectPathWriteValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObjectPathWriteValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObjectPathWriteValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathWriteValue:export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "guidance": [],
          "usageScore": 46,
          "configuration": [],
          "fragment": "utils-object-path--IObjectPathWriteValue"
        },
        {
          "name": "IPathAccessor",
          "kind": "type",
          "signature": "export type IPathAccessor<T> = { readonly value: T; get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined; set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T; probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>; parsePath(path: string | IObjectPathTuple): IObjectPathTuple; };",
          "declarationLine": 98,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IPathAccessor is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathAccessor.",
          "advanced": "No additional advanced behavior is declared for IPathAccessor.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathAccessor."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathAccessor.",
          "examples": [
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathAccessor<T> = { readonly value: T; get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined; set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T; probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>; parsePath(path: string | IObjectPathTuple): IObjectPathTuple; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathAccessor when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathAccessor does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IPathAccessor is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathAccessor from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathAccessor when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathAccessor does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathAccessor."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathAccessor."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathAccessor:export type IPathAccessor<T> = { readonly value: T; get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined; set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T; probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>; parsePath(path: string | IObjectPathTuple): IObjectPathTuple; };",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-object-path--IPathAccessor"
        },
        {
          "name": "IPathAccessorOptions",
          "kind": "type",
          "signature": "export type IPathAccessorOptions<T> = { readonly ifMissing?: (probe: IPathMissingProbe) => void; readonly ifBlocked?: (probe: IPathBlockedProbe) => void; readonly ifFailed?: (probe: IPathFailedProbe) => void; readonly onGet?: (event: IStringGetEvent<T> | ITupleGetEvent<T>) => void; readonly onSet?: (event: IStringSetEvent<T> | ITupleSetEvent<T>) => void; };",
          "declarationLine": 91,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IPathAccessorOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathAccessorOptions.",
          "advanced": "No additional advanced behavior is declared for IPathAccessorOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathAccessorOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathAccessorOptions.",
          "examples": [
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathAccessorOptions<T> = { readonly ifMissing?: (probe: IPathMissingProbe) => void; readonly ifBlocked?: (probe: IPathBlockedProbe) => void; readonly ifFailed?: (probe: IPathFailedProbe) => void; readonly onGet?: (event: IStringGetEvent<T> | ITupleGetEvent<T>) => void; readonly onSet?: (event: IStringSetEvent<T> | ITupleSetEvent<T>) => void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathAccessorOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathAccessorOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IPathAccessorOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathAccessorOptions from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathAccessorOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathAccessorOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathAccessorOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathAccessorOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathAccessorOptions:export type IPathAccessorOptions<T> = { readonly ifMissing?: (probe: IPathMissingProbe) => void; readonly ifBlocked?: (probe: IPathBlockedProbe) => void; readonly ifFailed?: (probe: IPathFailedProbe) => void; readonly onGet?: (event: IStringGetEvent<T> | ITupleGetEvent<T>) => void; readonly onSet?: (event: IStringSetEvent<T> | ITupleSetEvent<T>) => void; };",
          "guidance": [],
          "usageScore": 16,
          "configuration": [],
          "fragment": "utils-object-path--IPathAccessorOptions"
        },
        {
          "name": "IPathBlockedProbe",
          "kind": "type",
          "signature": "export type IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'blocked'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "declarationLine": 47,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IPathBlockedProbe is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathBlockedProbe.",
          "advanced": "No additional advanced behavior is declared for IPathBlockedProbe.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathBlockedProbe."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathBlockedProbe.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'blocked'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathBlockedProbe when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathBlockedProbe does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IPathBlockedProbe is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathBlockedProbe from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathBlockedProbe when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathBlockedProbe does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathBlockedProbe."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathBlockedProbe."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathBlockedProbe:export type IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'blocked'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-object-path--IPathBlockedProbe"
        },
        {
          "name": "IPathFailedProbe",
          "kind": "type",
          "signature": "export type IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'failed'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; readonly error: unknown; };",
          "declarationLine": 56,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IPathFailedProbe is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathFailedProbe.",
          "advanced": "No additional advanced behavior is declared for IPathFailedProbe.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathFailedProbe."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathFailedProbe.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'failed'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; readonly error: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathFailedProbe when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathFailedProbe does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IPathFailedProbe is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathFailedProbe from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathFailedProbe when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathFailedProbe does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathFailedProbe."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathFailedProbe."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathFailedProbe:export type IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'failed'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; readonly error: unknown; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-object-path--IPathFailedProbe"
        },
        {
          "name": "IPathGetEvent",
          "kind": "type",
          "signature": "export type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathValue<T, P>; replace(value: IObjectPathValue<T, P>): void; };",
          "declarationLine": 76,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Correlated read hook; replacement remains the exact narrowed path value type.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathGetEvent.",
          "advanced": "No additional advanced behavior is declared for IPathGetEvent.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathGetEvent."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathGetEvent.",
          "examples": [
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathValue<T, P>; replace(value: IObjectPathValue<T, P>): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathGetEvent when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathGetEvent does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Correlated read hook; replacement remains the exact narrowed path value type."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathGetEvent from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathGetEvent when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathGetEvent does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathGetEvent."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathGetEvent."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathGetEvent:export type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathValue<T, P>; replace(value: IObjectPathValue<T, P>): void; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-object-path--IPathGetEvent"
        },
        {
          "name": "IPathMissingProbe",
          "kind": "type",
          "signature": "export type IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'missing'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "declarationLine": 38,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IPathMissingProbe is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathMissingProbe.",
          "advanced": "No additional advanced behavior is declared for IPathMissingProbe.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathMissingProbe."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathMissingProbe.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'missing'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathMissingProbe when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathMissingProbe does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IPathMissingProbe is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathMissingProbe from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathMissingProbe when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathMissingProbe does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathMissingProbe."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathMissingProbe."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathMissingProbe:export type IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = { readonly kind: 'missing'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly failedAt: number; readonly failedKey: IObjectPathSegment; readonly resolvedPath: IObjectPathTuple; readonly parent: unknown; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-object-path--IPathMissingProbe"
        },
        {
          "name": "IPathProbe",
          "kind": "type",
          "signature": "export type IPathProbe<T, P extends IObjectPathInput<T>> = IPathValueProbe<T, P> | IPathMissingProbe<P> | IPathBlockedProbe<P> | IPathFailedProbe<P>;",
          "declarationLine": 66,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IPathProbe is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathProbe.",
          "advanced": "No additional advanced behavior is declared for IPathProbe.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathProbe."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathProbe.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathProbe<T, P extends IObjectPathInput<T>> = IPathValueProbe<T, P> | IPathMissingProbe<P> | IPathBlockedProbe<P> | IPathFailedProbe<P>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathProbe when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathProbe does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IPathProbe is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathProbe from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathProbe when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathProbe does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathProbe."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathProbe."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathProbe:export type IPathProbe<T, P extends IObjectPathInput<T>> = IPathValueProbe<T, P> | IPathMissingProbe<P> | IPathBlockedProbe<P> | IPathFailedProbe<P>;",
          "guidance": [],
          "usageScore": 22,
          "configuration": [],
          "fragment": "utils-object-path--IPathProbe"
        },
        {
          "name": "IPathSetEvent",
          "kind": "type",
          "signature": "export type IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathWriteValue<T, P>; replace(value: IObjectPathWriteValue<T, P>): void; };",
          "declarationLine": 84,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Correlated write hook; literal primitive leaves accept their widened write type.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathSetEvent.",
          "advanced": "No additional advanced behavior is declared for IPathSetEvent.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathSetEvent."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathSetEvent.",
          "examples": [
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathWriteValue<T, P>; replace(value: IObjectPathWriteValue<T, P>): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathSetEvent when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathSetEvent does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Correlated write hook; literal primitive leaves accept their widened write type."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathSetEvent from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathSetEvent when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathSetEvent does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathSetEvent."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathSetEvent."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathSetEvent:export type IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = { readonly originKey: P; readonly segments: IObjectPathTuple; readonly probe: IPathValueProbe<T, P>; readonly value: IObjectPathWriteValue<T, P>; replace(value: IObjectPathWriteValue<T, P>): void; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-object-path--IPathSetEvent"
        },
        {
          "name": "IPathValueProbe",
          "kind": "type",
          "signature": "export type IPathValueProbe<T, P extends IObjectPathInput<T>> = { readonly kind: 'value'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly value: IObjectPathValue<T, P>; };",
          "declarationLine": 32,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "IPathValueProbe is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathValueProbe.",
          "advanced": "No additional advanced behavior is declared for IPathValueProbe.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPathValueProbe."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPathValueProbe.",
          "examples": [
            "type IPathValueProbe<T, P extends IObjectPathInput<T>> = {\n  readonly kind: 'value';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly value: IObjectPathValue<T, P>;\n};\ntype IPathMissingProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'missing';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathBlockedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'blocked';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n};\ntype IPathFailedProbe<P extends string | IObjectPathTuple = string | IObjectPathTuple> = {\n  readonly kind: 'failed';\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly failedAt: number;\n  readonly failedKey: IObjectPathSegment;\n  readonly resolvedPath: IObjectPathTuple;\n  readonly parent: unknown;\n  readonly error: unknown;\n};\ntype IPathProbe<T, P extends IObjectPathInput<T>> =\n  | IPathValueProbe<T, P>\n  | IPathMissingProbe<P>\n  | IPathBlockedProbe<P>\n  | IPathFailedProbe<P>;\n\nfunction probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;\nfunction probeObjectPathSegments<T, P extends IObjectPathInput<T>>(\n  object: T,\n  segments: P\n): IPathProbe<T, P>;",
            "type IPathGetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathValue<T, P>;\n  replace(value: IObjectPathValue<T, P>): void;\n};\ntype IPathSetEvent<T, P extends IObjectPathInput<T> = IObjectPathInput<T>> = {\n  readonly originKey: P;\n  readonly segments: IObjectPathTuple;\n  readonly probe: IPathValueProbe<T, P>;\n  readonly value: IObjectPathWriteValue<T, P>;\n  replace(value: IObjectPathWriteValue<T, P>): void;\n};\ntype IPathAccessorOptions<T> = {\n  readonly ifMissing?: (probe: IPathMissingProbe) => void;\n  readonly ifBlocked?: (probe: IPathBlockedProbe) => void;\n  readonly ifFailed?: (probe: IPathFailedProbe) => void;\n  readonly onGet?: (event: IPathGetEvent<T>) => void;\n  readonly onSet?: (event: IPathSetEvent<T>) => void;\n};\ntype IPathAccessor<T> = {\n  readonly value: T;\n  get<P extends IObjectPathInput<T>>(path: P): IObjectPathValue<T, P> | undefined;\n  set<P extends IObjectPathInput<T>>(path: P, value: IObjectPathWriteValue<T, P>): T;\n  probeValue<P extends IObjectPathInput<T>>(path: P): IPathProbe<T, P>;\n  parsePath(path: string | IObjectPathTuple): IObjectPathTuple;\n};\nfunction createPathAccessor<T>(object: T, options?: IPathAccessorOptions<T>): IPathAccessor<T>;"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export type IPathValueProbe<T, P extends IObjectPathInput<T>> = { readonly kind: 'value'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly value: IObjectPathValue<T, P>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPathValueProbe when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPathValueProbe does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IPathValueProbe is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPathValueProbe from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPathValueProbe when its type contract and declared inputs match the result you need. Choose a different public symbol when IPathValueProbe does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPathValueProbe."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPathValueProbe."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:IPathValueProbe:export type IPathValueProbe<T, P extends IObjectPathInput<T>> = { readonly kind: 'value'; readonly originKey: P; readonly segments: IObjectPathTuple; readonly value: IObjectPathValue<T, P>; };",
          "guidance": [],
          "usageScore": 26,
          "configuration": [],
          "fragment": "utils-object-path--IPathValueProbe"
        },
        {
          "name": "parseObjectPath",
          "kind": "function",
          "signature": "export declare function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;",
          "declarationLine": 106,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Parses strict `a.b.[0].c` syntax or snapshots a tuple path.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for parseObjectPath.",
          "advanced": "No additional advanced behavior is declared for parseObjectPath.",
          "parameters": ["path: string | IObjectPathTuple"],
          "parameterDetails": [
            {
              "name": "path",
              "type": "string | IObjectPathTuple",
              "optional": false
            }
          ],
          "returns": "IObjectPathTuple",
          "errors": ["No documented errors are declared for parseObjectPath."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for parseObjectPath.",
          "examples": [
            "parseObjectPath('user.tags.[0]'); // ['user', 'tags', 0]\nprobeObjectPath(data, 'user.x.y'); // { kind: 'missing' | 'blocked' | 'failed' | 'value', ... }"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export declare function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "path",
                "type": "string | IObjectPathTuple",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IObjectPathTuple"],
          "whenToUse": "Use parseObjectPath when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when parseObjectPath does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Parses strict `a.b.[0].c` syntax or snapshots a tuple path."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import parseObjectPath from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use parseObjectPath when its function contract and declared inputs match the result you need. Choose a different public symbol when parseObjectPath does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for parseObjectPath."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for parseObjectPath."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:parseObjectPath:export declare function parseObjectPath(path: string | IObjectPathTuple): IObjectPathTuple;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            }
          ],
          "usageScore": 42,
          "configuration": [],
          "fragment": "utils-object-path--parseObjectPath"
        },
        {
          "name": "probeObjectPath",
          "kind": "function",
          "signature": "export declare function probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;",
          "declarationLine": 108,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Parses and probes one path in a single operation.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeObjectPath.",
          "advanced": "No additional advanced behavior is declared for probeObjectPath.",
          "parameters": ["object: T", "path: P"],
          "parameterDetails": [
            {
              "name": "object",
              "type": "T",
              "optional": false
            },
            {
              "name": "path",
              "type": "P",
              "optional": false
            }
          ],
          "returns": "IPathProbe<T, P>",
          "errors": ["No documented errors are declared for probeObjectPath."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for probeObjectPath.",
          "examples": [
            "parseObjectPath('user.tags.[0]'); // ['user', 'tags', 0]\nprobeObjectPath(data, 'user.x.y'); // { kind: 'missing' | 'blocked' | 'failed' | 'value', ... }"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export declare function probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "object",
                "type": "T",
                "optional": false
              },
              {
                "name": "path",
                "type": "P",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IPathProbe<T, P>"],
          "whenToUse": "Use probeObjectPath when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when probeObjectPath does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Parses and probes one path in a single operation."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import probeObjectPath from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use probeObjectPath when its function contract and declared inputs match the result you need. Choose a different public symbol when probeObjectPath does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeObjectPath."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for probeObjectPath."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:probeObjectPath:export declare function probeObjectPath<T, P extends IObjectPathInput<T>>(object: T, path: P): IPathProbe<T, P>;",
          "guidance": [],
          "usageScore": 46,
          "configuration": [],
          "fragment": "utils-object-path--probeObjectPath"
        },
        {
          "name": "probeObjectPathSegments",
          "kind": "function",
          "signature": "export declare function probeObjectPathSegments<T, P extends IObjectPathInput<T>>(object: T, segments: P): IPathProbe<T, P>;",
          "declarationLine": 110,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Probes an already parsed tuple without reparsing it during delivery.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeObjectPathSegments.",
          "advanced": "No additional advanced behavior is declared for probeObjectPathSegments.",
          "parameters": ["object: T", "segments: P"],
          "parameterDetails": [
            {
              "name": "object",
              "type": "T",
              "optional": false
            },
            {
              "name": "segments",
              "type": "P",
              "optional": false
            }
          ],
          "returns": "IPathProbe<T, P>",
          "errors": ["No documented errors are declared for probeObjectPathSegments."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for probeObjectPathSegments.",
          "examples": [],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export declare function probeObjectPathSegments<T, P extends IObjectPathInput<T>>(object: T, segments: P): IPathProbe<T, P>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "object",
                "type": "T",
                "optional": false
              },
              {
                "name": "segments",
                "type": "P",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IPathProbe<T, P>"],
          "whenToUse": "Use probeObjectPathSegments when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when probeObjectPathSegments does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Probes an already parsed tuple without reparsing it during delivery."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import probeObjectPathSegments from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use probeObjectPathSegments when its function contract and declared inputs match the result you need. Choose a different public symbol when probeObjectPathSegments does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for probeObjectPathSegments."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for probeObjectPathSegments."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:probeObjectPathSegments:export declare function probeObjectPathSegments<T, P extends IObjectPathInput<T>>(object: T, segments: P): IPathProbe<T, P>;",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-object-path--probeObjectPathSegments"
        },
        {
          "name": "set",
          "kind": "function",
          "signature": "export declare function set<T, P extends IObjectPathInput<T>>(object: T, path: P, nextValue: IObjectPathWriteValue<T, P>): T;",
          "declarationLine": 114,
          "source": "packages/utils/dist/object-path.d.ts",
          "purpose": "Performs an immutable path update with structural sharing and missing-container creation.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for set.",
          "advanced": "No additional advanced behavior is declared for set.",
          "parameters": ["object: T", "path: P", "nextValue: IObjectPathWriteValue<T, P>"],
          "parameterDetails": [
            {
              "name": "object",
              "type": "T",
              "optional": false
            },
            {
              "name": "path",
              "type": "P",
              "optional": false
            },
            {
              "name": "nextValue",
              "type": "IObjectPathWriteValue<T, P>",
              "optional": false
            }
          ],
          "returns": "T",
          "errors": ["No documented errors are declared for set."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for set.",
          "examples": [
            "const before = { user: { name: 'Ada', tags: ['a'] } };\nconst after = set(before, 'user.name', 'Grace');\nafter.user.name; // 'Grace'\nafter !== before; // true（发生更新时返回新根对象）\nafter.user !== before.user; // true（更新路径上的节点被复制）\nafter.user.tags === before.user.tags; // true（未涉及的分支保持引用不变）\nbefore.user.name; // 'Ada'（原对象未被修改）\nObject.isFrozen(after); // false（immutable update 不等于冻结）",
            "const accessor = createPathAccessor(\n  { count: 0 },\n  { onSet: (event) => console.log('write', event.originKey, event.value) }\n);\naccessor.set('count', 5); // 打印 write count 5\naccessor.value; // { count: 5 }",
            "const data = {\n  user: { name: 'Ada' },\n  settings: { theme: 'dark' }\n};\nget(data, 'user.name'); // 'Ada'：只读，不修改 data\n\nconst next = set(data, 'user.name', 'Grace');\nnext !== data; // true：值发生变化时，每次 set 都返回新根对象\nnext.user !== data.user; // true：路径上的对象被浅拷贝\nnext.settings === data.settings; // true：未修改分支继续共享引用\ndata.user.name; // 'Ada'：原对象保持不变\nObject.isFrozen(next); // false：immutable update 不等于 freeze",
            "const accessor = createPathAccessor({ count: 0 });\naccessor.set('count', 5);\naccessor.value; // { count: 5 }",
            "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
          ],
          "members": [],
          "exportPath": "./object-path",
          "overloadSignatures": [
            "export declare function set<T, P extends IObjectPathInput<T>>(object: T, path: P, nextValue: IObjectPathWriteValue<T, P>): T;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "object",
                "type": "T",
                "optional": false
              },
              {
                "name": "path",
                "type": "P",
                "optional": false
              },
              {
                "name": "nextValue",
                "type": "IObjectPathWriteValue<T, P>",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["T"],
          "whenToUse": "Use set when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when set does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Performs an immutable path update with structural sharing and missing-container creation."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import set from @migaia/utils/object-path, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use set when its function contract and declared inputs match the result you need. Choose a different public symbol when set does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for set."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for set."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/object-path.d.ts:set:export declare function set<T, P extends IObjectPathInput<T>>(object: T, path: P, nextValue: IObjectPathWriteValue<T, P>): T;",
          "guidance": [
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "带所有权语义的配置对象：只有经 `ownConfig` 接纳的对象才能被 `readonlyConfig`/`patchConfig`/`readConfigPath`/`combineConfig` 处理，用运行时 `WeakMap` 元数据（而非可伪造的字段）标记\"这是一个受本包管理的配置根\"。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const ConfigProfile = { data: 'data', richRuntime: 'richRuntime' } as const;\ntype IConfigProfile = 'data' | 'richRuntime';"
                },
                {
                  "type": "paragraph",
                  "text": "`data`（默认）：只接受可移植的纯数据——普通对象、数组、`Date`/`RegExp`/`Map`/`Set` 等内置类型；拒绝函数。`richRuntime`：额外接受函数/可构造类（连同其自身属性与 `prototype` 一起深拷贝），用于确实需要在配置里携带回调或工厂的场景。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const CONFIG_DELETE: unique symbol;"
                },
                {
                  "type": "paragraph",
                  "text": "在 `patchConfig`/`combineConfig` 的补丁对象中，把某个键的值设为这个符号，表示\"删除该键\"（而不是设为 `undefined`）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function ownConfig<T extends IConfigRecord>(\n  value: T,\n  options?: { readonly profile?: IConfigProfile; readonly limits?: Partial<IConfigLimits> }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "深拷贝 `value` 得到一个独立的、被本包元数据标记为\"已拥有\"的图。要求根必须是普通对象（不能是数组）且不含 `symbol` 键。深拷贝过程中："
                },
                {
                  "type": "list",
                  "items": [
                    "`Date`/`RegExp`/`Map`/`Set` 会被识别并对应构造新实例（而不是被当成普通对象递归展开字段）；",
                    "循环引用通过内部 `WeakMap` 保留同一图结构（不会无限递归，也不会拆散共享节点）；",
                    "`proto`/`prototype`/`constructor` 键一律拒绝；",
                    "超过 `limits`（见下）中的深度/节点数/键数会抛出 `code: 'CONFIG_LIMIT_EXCEEDED'` 的 `RangeError`。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "若 `value` 已经是某次 `ownConfig` 的返回值（元数据匹配同一 `profile`/`limits`），直接原样返回（幂等）；若元数据不匹配（例如同一对象先后用不同 `profile` 调用），抛出 `code: 'CONFIG_CONFLICT'` 的 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "调用方拿到结果后有三种明确路径：内部继续把它作为稳定配置根；通过 `readonlyConfig()` 暴露可读但不可写的递归 Proxy；通过 `patchConfig()` 或 `combineConfig()` 派生下一份已拥有配置。不要直接把原始外部对象交给这些 API，也不要把“已拥有”误解为冻结——所有权保证来自独立图与内部登记，运行时写保护由 `readonlyConfig()` 单独提供。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigLimits = {\n  readonly maxDepth: number; // 默认 256\n  readonly maxNodes: number; // 默认 100_000\n  readonly maxKeys: number; // 默认 1_000_000\n  readonly maxPathLength: number; // 默认 4096\n  readonly maxSegmentLength: number; // 默认 512\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`limits` 只能收紧（小于等于）默认值，不能放宽——`patchConfig`/`combineConfig` 里若传入的 `limits` 试图超过基准限制会抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function readonlyConfig<T extends IConfigRecord>(value: IOwnedConfig<T>): T;"
                },
                {
                  "type": "paragraph",
                  "text": "返回一个带缓存的只读对象，内部通过 `Proxy` 拦截修改；同一输入对象重复调用会返回同一个只读对象。读取时仍然访问原配置，没有再复制一份数据。任何写操作（`set`/`defineProperty`/`deleteProperty`/`setPrototypeOf`/`preventExtensions`，以及 `Map`/`Set`/`Date` 上的变更方法如 `.set()`/`.add()`/`.setFullYear()`）都会抛出 `code: 'CONFIG_READONLY'` 的 `TypeError`。嵌套对象、`Map`、`Set` 和函数字段也会受到同样的写入保护。`value` 必须是 `ownConfig` 的产物，否则抛 `code: 'CONFIG_UNSUPPORTED'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function patchConfig<T extends IConfigRecord>(\n  base: IOwnedConfig<T>,\n  patch: IConfigPatch<T>,\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly reuseUnchangedRoot?: boolean;\n  }\n): IOwnedConfig<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "对根做写时复制式覆盖：`patch` 中每个键要么设为新值（会被递归深拷贝并纳入所有权），要么设为 `CONFIG_DELETE` 表示删除。只有被改动路径上的节点被复制，未涉及的子树按引用共享。`patch` 为空对象且 `reuseUnchangedRoot`（默认 `true`）时直接返回 `base` 本身，不做任何拷贝。`patch` 必须是普通对象、键不能是 `symbol`、不能是访问器属性，否则抛 `code: 'CONFIG_UNSUPPORTED'`；键名撞上 `proto`/`prototype`/`constructor` 抛 `code: 'CONFIG_PATH_INVALID'`。注意：`patchConfig` 只在根一层应用补丁——要修改嵌套字段，先用 `get`/`set`（`/object` 模块）在应用层算出新的嵌套子树，再作为整体值放进 `patch`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const base = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(base, { retries: 3, debug: CONFIG_DELETE });\nnext.retries; // 3\n'debug' in next; // false"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function parseConfigPath(\n  path: string,\n  limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>\n): readonly string[];"
                },
                {
                  "type": "paragraph",
                  "text": "在 `/object` 模块的 `parseObjectPath` 基础上叠加 config 专属限制（长度上限可自定义，默认取包级默认值）并强制所有段都是字符串（不支持数字下标简写形式在 config 路径里独立出现，统一按字符串段处理）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigReadResult =\n  { readonly kind: 'missing' } | { readonly kind: 'value'; readonly value: unknown };\nfunction readConfigPath(\n  config: IOwnedConfig<IConfigRecord>,\n  path: string | readonly string[]\n): IConfigReadResult;"
                },
                {
                  "type": "paragraph",
                  "text": "读取配置内某条路径，区分\"键不存在\"（`missing`）与\"值就是 `undefined`\"（`value: undefined`）。返回的对象/函数类型的值会自动包一层只读 Proxy（与 `readonlyConfig` 相同的包装逻辑），保证读到的嵌套值也不可被外部修改。`config` 必须是 `ownConfig` 的产物。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function combineConfig(\n  sources: readonly IOwnedConfig<IConfigRecord>[],\n  options?: {\n    readonly profile?: IConfigProfile;\n    readonly limits?: Partial<IConfigLimits>;\n    readonly strategies?: Partial<IConfigMergeStrategies>;\n    readonly pathRules?: readonly IConfigPathRule[];\n    readonly onConflict?: (context: IConfigConflictContext) => IConfigConflictDecision;\n  }\n): IOwnedConfig<IConfigRecord>;"
                },
                {
                  "type": "paragraph",
                  "text": "按 `sources` 数组的顺序（后者覆盖前者）合并多个已拥有的配置根，产出一个新的 `ownConfig` 结果。全部来源必须共享同一个 `profile`，否则抛 `code: 'CONFIG_CONFLICT'`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConfigMergeStrategies = {\n  readonly record: 'merge' | 'replace'; // 默认 'merge'：普通对象递归合并；'replace'：整体覆盖\n  readonly array: 'replace' | 'concat' | 'mergeByIndex'; // 默认 'replace'\n  readonly map: 'replace' | 'merge'; // 默认 'replace'\n  readonly set: 'replace' | 'union'; // 默认 'replace'\n  readonly undefined: 'ignore' | 'assign'; // 默认 'ignore'：右侧显式 undefined 不覆盖左侧\n};\n\ntype IConfigPathRule = {\n  readonly prefix: readonly PropertyKey[];\n  readonly strategies: Partial<IConfigMergeStrategies>;\n};\n\ntype IConfigConflictContext = {\n  readonly path: readonly PropertyKey[];\n  readonly left: unknown;\n  readonly right: unknown;\n};\ntype IConfigConflictDecision =\n  | { readonly kind: 'left' }\n  | { readonly kind: 'right' }\n  | { readonly kind: 'delete' }\n  | { readonly kind: 'value'; readonly value: unknown };"
                },
                {
                  "type": "paragraph",
                  "text": "`strategies` 是全局默认策略；`pathRules` 是一组 `{ prefix, strategies }`，按 `prefix`（从根开始的键路径）匹配当前正在合并的键路径，`prefix` 更长（更具体）的规则优先生效，命中的规则会与全局 `strategies` 合并（规则内声明的字段覆盖全局同名字段，规则未声明的字段沿用全局值）。"
                },
                {
                  "type": "paragraph",
                  "text": "当某个键在双方都存在、且不属于以上任何自动合并分支（例如两侧都是不可合并的原始值）时，若提供了 `onConflict`，会调用它并传入 `IConfigConflictContext`（`path`：当前键的完整路径；`left`：左侧/已有值；`right`：右侧/新值），其返回值 `IConfigConflictDecision` 决定最终结果：`{ kind: 'left' }` 保留左值、`{ kind: 'right' }` 采用右值、`{ kind: 'delete' }` 删除该键、`{ kind: 'value', value }` 采用自定义值。`onConflict` 必须同步返回（返回 Promise 会抛 `code: 'CONFIG_CONFLICT'`；返回值不是上述四种 `kind` 之一同样抛 `CONFIG_CONFLICT`）。"
                },
                {
                  "type": "paragraph",
                  "text": "未提供 `onConflict` 时右侧直接覆盖左侧（标准合并语义）。`right === CONFIG_DELETE` 会删除目标键（可用于合并阶段裁剪掉某个来源写入的默认值）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            },
            {
              "id": "高阶组合示例-3-对象路径-配置-把校验后的嵌套结构整体写回配置根",
              "heading": "高阶组合示例 · 3. 对象路径 + 配置：把校验后的嵌套结构整体写回配置根",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { get, set } from '@migaia/utils/object';\nimport { ownConfig, patchConfig } from '@migaia/utils/config';\n\nconst config = ownConfig({ server: { host: 'localhost', port: 8080 } });\n// patchConfig 只在根一层生效，嵌套修改先在应用层用 set() 算好整棵子树\nconst nextServer = set(get(config, 'server')!, 'port', 9090);\nconst next = patchConfig(config, { server: nextServer });"
                }
              ]
            }
          ],
          "usageScore": 114,
          "configuration": [],
          "fragment": "utils-object-path--set"
        }
      ],
      "aliases": []
    },
    {
      "id": "utils:promise",
      "library": "utils",
      "module": "promise",
      "exportPath": "./promise",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "admitAbortSignal",
          "kind": "function",
          "signature": "export declare function admitAbortSignal(candidate: unknown): IAbortSignalAdmission;",
          "declarationLine": 39,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Admits an abort signal shape through one `aborted` read without probing `reason` or subscribing. Getter and proxy failures remain data so the owning caller can apply its own error policy.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for admitAbortSignal.",
          "advanced": "No additional advanced behavior is declared for admitAbortSignal.",
          "parameters": ["candidate: unknown"],
          "parameterDetails": [
            {
              "name": "candidate",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "IAbortSignalAdmission",
          "errors": ["No documented errors are declared for admitAbortSignal."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for admitAbortSignal.",
          "examples": [],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function admitAbortSignal(candidate: unknown): IAbortSignalAdmission;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "candidate",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IAbortSignalAdmission"],
          "whenToUse": "Use admitAbortSignal when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when admitAbortSignal does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Admits an abort signal shape through one `aborted` read without probing `reason` or subscribing. Getter and proxy failures remain data so the owning caller can apply its own error policy."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import admitAbortSignal from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use admitAbortSignal when its function contract and declared inputs match the result you need. Choose a different public symbol when admitAbortSignal does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for admitAbortSignal."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for admitAbortSignal."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:admitAbortSignal:export declare function admitAbortSignal(candidate: unknown): IAbortSignalAdmission;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-promise--admitAbortSignal"
        },
        {
          "name": "createAbortTimeoutSignal",
          "kind": "function",
          "signature": "export declare function createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;",
          "declarationLine": 95,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Creates the canonical merged signal used by adapters that need timeout plus external abort.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createAbortTimeoutSignal.",
          "advanced": "No additional advanced behavior is declared for createAbortTimeoutSignal.",
          "parameters": ["options: IAbortTimeoutSignalOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "IAbortTimeoutSignalOptions",
              "optional": true
            }
          ],
          "returns": "IAbortTimeoutSignal",
          "errors": ["No documented errors are declared for createAbortTimeoutSignal."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createAbortTimeoutSignal.",
          "examples": [
            "const merged = createAbortTimeoutSignal({\n  signal: controller.signal as any,\n  timeoutMs: 500,\n  timeoutReason: () => new Error('deadline')\n});\ntry {\n  await Promise.all([requestA(merged.signal), requestB(merged.signal)]);\n} finally {\n  merged.dispose();\n}",
            "const merged = createAbortTimeoutSignal({\n  signal: controller.signal,\n  timeoutMs: 500,\n  timeoutReason: () => new Error('deadline')\n});\ntry {\n  await Promise.all([requestA(merged.signal), requestB(merged.signal)]);\n} finally {\n  merged.dispose();\n}"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "IAbortTimeoutSignalOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IAbortTimeoutSignal"],
          "whenToUse": "Use createAbortTimeoutSignal when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createAbortTimeoutSignal does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates the canonical merged signal used by adapters that need timeout plus external abort."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createAbortTimeoutSignal from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createAbortTimeoutSignal when its function contract and declared inputs match the result you need. Choose a different public symbol when createAbortTimeoutSignal does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createAbortTimeoutSignal."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createAbortTimeoutSignal."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "signal",
              "type": "IAbortSignal",
              "optional": true,
              "description": ""
            },
            {
              "name": "timeoutMs",
              "type": "number",
              "optional": true,
              "description": ""
            },
            {
              "name": "scheduler",
              "type": "IUtilsScheduler",
              "optional": true,
              "description": ""
            },
            {
              "name": "timeoutReason",
              "type": "() => unknown",
              "optional": true,
              "description": ""
            },
            {
              "name": "report",
              "type": "IUtilsReporter",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/promise.d.ts:createAbortTimeoutSignal:export declare function createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;",
          "guidance": [
            {
              "id": "createaborttimeoutsignal",
              "heading": "createAbortTimeoutSignal",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IAbortTimeoutSignalOptions = {\n  readonly signal?: IAbortSignal;\n  readonly timeoutMs?: number;\n  readonly scheduler?: IUtilsScheduler;\n  readonly timeoutReason?: () => unknown;\n  readonly report?: IUtilsReporter;\n};\ntype IAbortTimeoutSignal = {\n  readonly signal: IAbortSignal | undefined;\n  readonly dispose: () => void;\n};\nfunction createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;"
                },
                {
                  "type": "paragraph",
                  "text": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const merged = createAbortTimeoutSignal({\n  signal: controller.signal as any,\n  timeoutMs: 500,\n  timeoutReason: () => new Error('deadline')\n});\ntry {\n  await Promise.all([requestA(merged.signal), requestB(merged.signal)]);\n} finally {\n  merged.dispose();\n}"
                },
                {
                  "type": "paragraph",
                  "text": "`withTimeout` / `raceWithAbort` 的 callback 也能把同一 cooperative signal 交给多个请求，但它们同时拥有最终 Promise 的超时/中止 reject、late rejection 与 cleanup-error 政策。若事务 commit 才是唯一结算事实，应使用本 signal-only primitive，并由适配器自己决定 Promise 何时完成。"
                }
              ]
            }
          ],
          "usageScore": 38,
          "configuration": [
            {
              "name": "signal",
              "type": "IAbortSignal",
              "optional": true,
              "description": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。",
              "descriptionEn": "",
              "descriptionZh": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。"
            },
            {
              "name": "timeoutMs",
              "type": "number",
              "optional": true,
              "description": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。",
              "descriptionEn": "",
              "descriptionZh": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。"
            },
            {
              "name": "scheduler",
              "type": "IUtilsScheduler",
              "optional": true,
              "description": "`scheduler?: IUtilsScheduler` —— 默认 `systemScheduler`",
              "descriptionEn": "",
              "descriptionZh": "`scheduler?: IUtilsScheduler` —— 默认 `systemScheduler`"
            },
            {
              "name": "timeoutReason",
              "type": "() => unknown",
              "optional": true,
              "description": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。",
              "descriptionEn": "",
              "descriptionZh": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。"
            },
            {
              "name": "report",
              "type": "IUtilsReporter",
              "optional": true,
              "description": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。",
              "descriptionEn": "",
              "descriptionZh": "创建一个可交给多个底层请求共享的 operation signal，但不创建、race 或结算业务 Promise。外部 signal 先中止时保留原始 reason；deadline 先到时使用 `timeoutReason()` 的返回值。reason factory 抛出的原始错误会成为 signal reason，不会从 timer callback 逃逸。无 `timeoutMs` 时透传外部 signal identity；`dispose()` 幂等并负责取消 timer、移除 listener，cleanup 失败通过 `report` 的 `operation: 'abort-timeout-signal'` 上报。"
            }
          ],
          "fragment": "utils-promise--createAbortTimeoutSignal"
        },
        {
          "name": "createConcurrencyLimiter",
          "kind": "function",
          "signature": "export declare function createConcurrencyLimiter(options: { readonly concurrency: number; readonly report?: IUtilsReporter; }): IConcurrencyLimiter;",
          "declarationLine": 155,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Limits active tasks with FIFO admission and cooperative queued cancellation.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createConcurrencyLimiter.",
          "advanced": "No additional advanced behavior is declared for createConcurrencyLimiter.",
          "parameters": [
            "options: { readonly concurrency: number; readonly report?: IUtilsReporter; }"
          ],
          "parameterDetails": [
            {
              "name": "options",
              "type": "{ readonly concurrency: number; readonly report?: IUtilsReporter; }",
              "optional": false
            }
          ],
          "returns": "IConcurrencyLimiter",
          "errors": ["No documented errors are declared for createConcurrencyLimiter."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createConcurrencyLimiter.",
          "examples": [
            "const limiter = createConcurrencyLimiter({ concurrency: 2 });\nconst results = await Promise.all([1, 2, 3, 4].map((n) => limiter.run(() => fetchPage(n))));\nawait limiter.dispose(); // 等待剩余任务完成后关闭",
            "const limiter = createConcurrencyLimiter({ concurrency: 2 });\nawait Promise.all(urls.map((u) => limiter.run(() => fetch(u))));\nawait limiter.dispose(); // 等待在途任务结束后关闭",
            "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function createConcurrencyLimiter(options: { readonly concurrency: number; readonly report?: IUtilsReporter; }): IConcurrencyLimiter;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "{ readonly concurrency: number; readonly report?: IUtilsReporter; }",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IConcurrencyLimiter"],
          "whenToUse": "Use createConcurrencyLimiter when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createConcurrencyLimiter does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Limits active tasks with FIFO admission and cooperative queued cancellation."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createConcurrencyLimiter from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createConcurrencyLimiter when its function contract and declared inputs match the result you need. Choose a different public symbol when createConcurrencyLimiter does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createConcurrencyLimiter."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createConcurrencyLimiter."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:createConcurrencyLimiter:export declare function createConcurrencyLimiter(options: { readonly concurrency: number; readonly report?: IUtilsReporter; }): IConcurrencyLimiter;",
          "guidance": [
            {
              "id": "createconcurrencylimiter",
              "heading": "createConcurrencyLimiter",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IConcurrencyLimiter = {\n  run<T>(\n    task: (context: { readonly signal?: IAbortSignal }) => T | PromiseLike<T>,\n    options?: { readonly signal?: IAbortSignal }\n  ): Promise<T>;\n  readonly activeCount: number;\n  readonly pendingCount: number;\n  whenIdle(): Promise<void>;\n  close(reason?: unknown): void;\n  dispose(reason?: unknown): Promise<void>;\n};\nfunction createConcurrencyLimiter(options: {\n  readonly concurrency: number;\n  readonly report?: IUtilsReporter;\n}): IConcurrencyLimiter;"
                },
                {
                  "type": "paragraph",
                  "text": "本包中唯一具有跨调用生命周期的原语——一个带并发上限的 FIFO 任务队列："
                },
                {
                  "type": "list",
                  "items": [
                    "`run(task, { signal })`：把任务加入队列，达到 `concurrency` 上限前的任务立即执行，之后排队等待空位；传入的 `signal` 中止会让排队中的任务立即以其中止原因 reject（已开始执行的任务不受影响，需任务自身读取 `context.signal`）。",
                    "`activeCount` / `pendingCount`：当前正在执行 / 排队等待（未取消）的任务数。",
                    "`whenIdle()`：返回一个在\"当前没有活跃任务且队列已清空\"时 resolve 的 Promise；多次调用共享同一个等待，不会重复注册。",
                    "`close(reason?)`：停止接纳新任务与排队任务——已排队但未执行的任务立即以 `reason`（默认一个 `LIMITER_CLOSED` 错误）reject；正在执行的任务不受影响。",
                    "`dispose(reason?)`：调用 `close()` 并返回一个在所有活跃任务结束后才 resolve 的 Promise，用于优雅关闭。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`concurrency` 必须是正安全整数，否则构造时抛 `RangeError`。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const limiter = createConcurrencyLimiter({ concurrency: 2 });\nconst results = await Promise.all([1, 2, 3, 4].map((n) => limiter.run(() => fetchPage(n))));\nawait limiter.dispose(); // 等待剩余任务完成后关闭"
                }
              ]
            },
            {
              "id": "高阶组合示例-2-并发限流-重试-错误合并-批量拉取-容忍部分失败",
              "heading": "高阶组合示例 · 2. 并发限流 + 重试 + 错误合并：批量拉取，容忍部分失败",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
                }
              ]
            }
          ],
          "usageScore": 48,
          "configuration": [],
          "fragment": "utils-promise--createConcurrencyLimiter"
        },
        {
          "name": "createManualScheduler",
          "kind": "function",
          "signature": "export declare function createManualScheduler(): IManualScheduler;",
          "declarationLine": 120,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Creates a deterministic FIFO scheduler for unit tests and adapters.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createManualScheduler.",
          "advanced": "No additional advanced behavior is declared for createManualScheduler.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IManualScheduler",
          "errors": ["No documented errors are declared for createManualScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createManualScheduler.",
          "examples": [
            "const scheduler = createManualScheduler();\nconst task = scheduler.schedule(() => console.log('fired'), 100);\nscheduler.advance(50); // 未触发\nscheduler.advance(50); // 触发，打印 'fired'",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "const scheduler = createManualScheduler();\nconst p = withTimeout(op, { timeoutMs: 100, scheduler });\nscheduler.advance(100); // 手动触发超时，无需真实等待",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function createManualScheduler(): IManualScheduler;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IManualScheduler"],
          "whenToUse": "Use createManualScheduler when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createManualScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a deterministic FIFO scheduler for unit tests and adapters."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createManualScheduler from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createManualScheduler when its function contract and declared inputs match the result you need. Choose a different public symbol when createManualScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createManualScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createManualScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:createManualScheduler:export declare function createManualScheduler(): IManualScheduler;",
          "guidance": [
            {
              "id": "组合工作流示例-截止时间-配置",
              "heading": "组合工作流示例：截止时间 + 配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                },
                {
                  "type": "paragraph",
                  "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
                }
              ]
            },
            {
              "id": "高阶组合示例-1-截止时间-重试-只读配置",
              "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                }
              ]
            }
          ],
          "usageScore": 56,
          "configuration": [],
          "fragment": "utils-promise--createManualScheduler"
        },
        {
          "name": "deferred",
          "kind": "function",
          "signature": "export declare function deferred<T>(): IDeferred<T>;",
          "declarationLine": 113,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Creates a native Promise deferred without exposing mutable settlement state.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for deferred.",
          "advanced": "No additional advanced behavior is declared for deferred.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IDeferred<T>",
          "errors": ["No documented errors are declared for deferred."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for deferred.",
          "examples": [
            "const { promise, resolve } = deferred<number>();\nsetTimeout(() => resolve(42), 0);\nawait promise; // 42",
            "const { promise, resolve } = deferred<number>();\nresolve(42);"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": ["export declare function deferred<T>(): IDeferred<T>;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IDeferred<T>"],
          "whenToUse": "Use deferred when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when deferred does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a native Promise deferred without exposing mutable settlement state."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import deferred from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use deferred when its function contract and declared inputs match the result you need. Choose a different public symbol when deferred does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for deferred."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for deferred."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:deferred:export declare function deferred<T>(): IDeferred<T>;",
          "guidance": [
            {
              "id": "deferred",
              "heading": "deferred",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IDeferred<T> = {\n  readonly promise: Promise<T>;\n  readonly resolve: (value: T | PromiseLike<T>) => void;\n  readonly reject: (reason?: unknown) => void;\n};\nfunction deferred<T>(): IDeferred<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "创建一个可从外部结算的原生 Promise。适合把回调式 API 转成 Promise，或在测试中手动控制某个异步操作何时完成/失败。"
                },
                {
                  "type": "paragraph",
                  "text": "新代码应先选择平台原生的 `Promise.withResolvers()`。`deferred()` 主要服务于尚未支持该 API 的旧运行时，或必须统一依赖 `IDeferred<T>` 的项目边界："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const { promise, resolve, reject } = Promise.withResolvers<number>();\nresolve(42);"
                },
                {
                  "type": "paragraph",
                  "text": "兼容旧运行时："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const { promise, resolve } = deferred<number>();\nsetTimeout(() => resolve(42), 0);\nawait promise; // 42"
                }
              ]
            }
          ],
          "usageScore": 46,
          "configuration": [],
          "fragment": "utils-promise--deferred"
        },
        {
          "name": "hostRethrowReporter",
          "kind": "const",
          "signature": "export declare const hostRethrowReporter: IUtilsReporter;",
          "declarationLine": 77,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Reports a diagnostic to the host without creating an unhandled rejected Promise.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for hostRethrowReporter.",
          "advanced": "No additional advanced behavior is declared for hostRethrowReporter.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for hostRethrowReporter."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for hostRethrowReporter.",
          "examples": [
            "type IUtilsReporter = (\n  error: unknown,\n  context: {\n    readonly operation: 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter';\n    readonly phase: 'late-rejection' | 'cleanup' | 'reporter';\n    readonly attempt?: number;\n  }\n) => void;\nconst hostRethrowReporter: IUtilsReporter;",
            "import {\n  sleep,\n  createAbortTimeoutSignal,\n  withTimeout,\n  raceWithAbort,\n  retry,\n  createConcurrencyLimiter,\n  deferred,\n  toPromise,\n  createManualScheduler,\n  systemScheduler,\n  hostRethrowReporter\n} from '@migaia/utils/promise';"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": ["export declare const hostRethrowReporter: IUtilsReporter;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use hostRethrowReporter when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when hostRethrowReporter does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reports a diagnostic to the host without creating an unhandled rejected Promise."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import hostRethrowReporter from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use hostRethrowReporter when its const contract and declared inputs match the result you need. Choose a different public symbol when hostRethrowReporter does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for hostRethrowReporter."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for hostRethrowReporter."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:hostRethrowReporter:export declare const hostRethrowReporter: IUtilsReporter;",
          "guidance": [],
          "usageScore": 48,
          "configuration": [],
          "fragment": "utils-promise--hostRethrowReporter"
        },
        {
          "name": "IAbortControllerLike",
          "kind": "type",
          "signature": "export type IAbortControllerLike = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "declarationLine": 19,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IAbortControllerLike is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortControllerLike.",
          "advanced": "No additional advanced behavior is declared for IAbortControllerLike.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortControllerLike."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortControllerLike.",
          "examples": [],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IAbortControllerLike = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortControllerLike when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortControllerLike does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IAbortControllerLike is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortControllerLike from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortControllerLike when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortControllerLike does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortControllerLike."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortControllerLike."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAbortControllerLike:export type IAbortControllerLike = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-promise--IAbortControllerLike"
        },
        {
          "name": "IAbortOperation",
          "kind": "type",
          "signature": "export type IAbortOperation<T> = ITimeoutOperation<T>;",
          "declarationLine": 97,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Describes a lazy operation whose cooperative cancellation observes the supplied signal.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortOperation.",
          "advanced": "No additional advanced behavior is declared for IAbortOperation.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortOperation."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortOperation.",
          "examples": [
            "type IAbortOperation<T> = ITimeoutOperation<T>;\n\nfunction raceWithAbort<T>(\n  operation: IAbortOperation<T>,\n  options?: Pick<IAsyncControls, 'signal' | 'signals'> & {\n    readonly report?: IUtilsReporter;\n    readonly cleanupPolicy?: 'reject' | 'report';\n  }\n): Promise<T>;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": ["export type IAbortOperation<T> = ITimeoutOperation<T>;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortOperation when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortOperation does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Describes a lazy operation whose cooperative cancellation observes the supplied signal."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortOperation from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortOperation when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortOperation does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortOperation."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortOperation."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAbortOperation:export type IAbortOperation<T> = ITimeoutOperation<T>;",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-promise--IAbortOperation"
        },
        {
          "name": "IAbortSignal",
          "kind": "type",
          "signature": "export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "declarationLine": 2,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IAbortSignal is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignal.",
          "advanced": "No additional advanced behavior is declared for IAbortSignal.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortSignal."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortSignal.",
          "examples": [
            "type IAsyncControls = {\n  readonly signal?: IAbortSignal;\n  readonly signals?: readonly IAbortSignal[];\n  readonly scheduler?: IUtilsScheduler;\n  readonly unref?: boolean;\n};",
            "type IAbortTimeoutSignalOptions = {\n  readonly signal?: IAbortSignal;\n  readonly timeoutMs?: number;\n  readonly scheduler?: IUtilsScheduler;\n  readonly timeoutReason?: () => unknown;\n  readonly report?: IUtilsReporter;\n};\ntype IAbortTimeoutSignal = {\n  readonly signal: IAbortSignal | undefined;\n  readonly dispose: () => void;\n};\nfunction createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;",
            "type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal }) => T | PromiseLike<T>;\n\nfunction withTimeout<T>(\n  operation: ITimeoutOperation<T>,\n  options: IAsyncControls & {\n    readonly timeoutMs: number;\n    readonly report?: IUtilsReporter;\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly cooperativeCancellation?: boolean;\n  }\n): Promise<T>;",
            "type IRetryContext = {\n  readonly attempt: number;\n  readonly signal: IAbortSignal;\n  readonly remainingMs?: number;\n};\ntype IRetryFailureContext = IRetryContext & { readonly maxAttempts: number };\n\nfunction retry<T>(\n  operation: (context: IRetryContext) => T | PromiseLike<T>,\n  options: {\n    readonly maxAttempts: number;\n    readonly shouldRetry: (\n      error: unknown,\n      context: IRetryFailureContext\n    ) => boolean | PromiseLike<boolean>;\n    readonly delay?: number | ((error: unknown, context: IRetryFailureContext) => number);\n    readonly totalTimeoutMs?: number;\n    readonly attemptTimeoutMs?: number;\n    readonly signal?: IAbortSignal;\n    readonly signals?: readonly IAbortSignal[];\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly report?: IUtilsReporter;\n    readonly unref?: boolean;\n    readonly scheduler?: IUtilsScheduler;\n  }\n): Promise<T>;",
            "type IConcurrencyLimiter = {\n  run<T>(\n    task: (context: { readonly signal?: IAbortSignal }) => T | PromiseLike<T>,\n    options?: { readonly signal?: IAbortSignal }\n  ): Promise<T>;\n  readonly activeCount: number;\n  readonly pendingCount: number;\n  whenIdle(): Promise<void>;\n  close(reason?: unknown): void;\n  dispose(reason?: unknown): Promise<void>;\n};\nfunction createConcurrencyLimiter(options: {\n  readonly concurrency: number;\n  readonly report?: IUtilsReporter;\n}): IConcurrencyLimiter;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortSignal when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortSignal does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IAbortSignal is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortSignal from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortSignal when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortSignal does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignal."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortSignal."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAbortSignal:export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "guidance": [],
          "usageScore": 92,
          "configuration": [],
          "fragment": "utils-promise--IAbortSignal"
        },
        {
          "name": "IAbortSignalAdmission",
          "kind": "type",
          "signature": "export type IAbortSignalAdmission = { readonly kind: 'valid'; readonly signal: IAbortSignal; readonly aborted: boolean; } | { readonly kind: 'invalid'; readonly reason: IAbortSignalRejection; readonly cause?: unknown; };",
          "declarationLine": 26,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Result of one structural signal admission read; callers retain all error and abort policy.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignalAdmission.",
          "advanced": "No additional advanced behavior is declared for IAbortSignalAdmission.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortSignalAdmission."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortSignalAdmission.",
          "examples": [],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IAbortSignalAdmission = { readonly kind: 'valid'; readonly signal: IAbortSignal; readonly aborted: boolean; } | { readonly kind: 'invalid'; readonly reason: IAbortSignalRejection; readonly cause?: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortSignalAdmission when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortSignalAdmission does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Result of one structural signal admission read; callers retain all error and abort policy."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortSignalAdmission from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortSignalAdmission when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortSignalAdmission does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignalAdmission."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortSignalAdmission."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAbortSignalAdmission:export type IAbortSignalAdmission = { readonly kind: 'valid'; readonly signal: IAbortSignal; readonly aborted: boolean; } | { readonly kind: 'invalid'; readonly reason: IAbortSignalRejection; readonly cause?: unknown; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-promise--IAbortSignalAdmission"
        },
        {
          "name": "IAbortSignalRejection",
          "kind": "type",
          "signature": "export type IAbortSignalRejection = 'not-object' | 'aborted-not-boolean' | 'aborted-threw' | 'listener-not-function';",
          "declarationLine": 24,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Stable reasons for rejecting an untrusted abort-signal-shaped value without throwing.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignalRejection.",
          "advanced": "No additional advanced behavior is declared for IAbortSignalRejection.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortSignalRejection."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortSignalRejection.",
          "examples": [],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IAbortSignalRejection = 'not-object' | 'aborted-not-boolean' | 'aborted-threw' | 'listener-not-function';"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortSignalRejection when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortSignalRejection does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Stable reasons for rejecting an untrusted abort-signal-shaped value without throwing."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortSignalRejection from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortSignalRejection when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortSignalRejection does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignalRejection."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortSignalRejection."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAbortSignalRejection:export type IAbortSignalRejection = 'not-object' | 'aborted-not-boolean' | 'aborted-threw' | 'listener-not-function';",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-promise--IAbortSignalRejection"
        },
        {
          "name": "IAbortTimeoutSignal",
          "kind": "type",
          "signature": "export type IAbortTimeoutSignal = { readonly signal: IAbortSignal | undefined; readonly dispose: () => void; };",
          "declarationLine": 90,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IAbortTimeoutSignal is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortTimeoutSignal.",
          "advanced": "No additional advanced behavior is declared for IAbortTimeoutSignal.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortTimeoutSignal."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortTimeoutSignal.",
          "examples": [
            "type IAbortTimeoutSignalOptions = {\n  readonly signal?: IAbortSignal;\n  readonly timeoutMs?: number;\n  readonly scheduler?: IUtilsScheduler;\n  readonly timeoutReason?: () => unknown;\n  readonly report?: IUtilsReporter;\n};\ntype IAbortTimeoutSignal = {\n  readonly signal: IAbortSignal | undefined;\n  readonly dispose: () => void;\n};\nfunction createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IAbortTimeoutSignal = { readonly signal: IAbortSignal | undefined; readonly dispose: () => void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortTimeoutSignal when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortTimeoutSignal does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IAbortTimeoutSignal is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortTimeoutSignal from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortTimeoutSignal when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortTimeoutSignal does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortTimeoutSignal."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortTimeoutSignal."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAbortTimeoutSignal:export type IAbortTimeoutSignal = { readonly signal: IAbortSignal | undefined; readonly dispose: () => void; };",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-promise--IAbortTimeoutSignal"
        },
        {
          "name": "IAbortTimeoutSignalOptions",
          "kind": "type",
          "signature": "export type IAbortTimeoutSignalOptions = { readonly signal?: IAbortSignal; readonly timeoutMs?: number; readonly scheduler?: IUtilsScheduler; readonly timeoutReason?: () => unknown; readonly report?: IUtilsReporter; };",
          "declarationLine": 83,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IAbortTimeoutSignalOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortTimeoutSignalOptions.",
          "advanced": "No additional advanced behavior is declared for IAbortTimeoutSignalOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortTimeoutSignalOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortTimeoutSignalOptions.",
          "examples": [
            "type IAbortTimeoutSignalOptions = {\n  readonly signal?: IAbortSignal;\n  readonly timeoutMs?: number;\n  readonly scheduler?: IUtilsScheduler;\n  readonly timeoutReason?: () => unknown;\n  readonly report?: IUtilsReporter;\n};\ntype IAbortTimeoutSignal = {\n  readonly signal: IAbortSignal | undefined;\n  readonly dispose: () => void;\n};\nfunction createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IAbortTimeoutSignalOptions = { readonly signal?: IAbortSignal; readonly timeoutMs?: number; readonly scheduler?: IUtilsScheduler; readonly timeoutReason?: () => unknown; readonly report?: IUtilsReporter; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortTimeoutSignalOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortTimeoutSignalOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IAbortTimeoutSignalOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortTimeoutSignalOptions from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortTimeoutSignalOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortTimeoutSignalOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortTimeoutSignalOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortTimeoutSignalOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAbortTimeoutSignalOptions:export type IAbortTimeoutSignalOptions = { readonly signal?: IAbortSignal; readonly timeoutMs?: number; readonly scheduler?: IUtilsScheduler; readonly timeoutReason?: () => unknown; readonly report?: IUtilsReporter; };",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-promise--IAbortTimeoutSignalOptions"
        },
        {
          "name": "IAsyncControls",
          "kind": "type",
          "signature": "export type IAsyncControls = { readonly signal?: IAbortSignal; readonly signals?: readonly IAbortSignal[]; readonly scheduler?: IUtilsScheduler; readonly unref?: boolean; };",
          "declarationLine": 57,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IAsyncControls is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAsyncControls.",
          "advanced": "No additional advanced behavior is declared for IAsyncControls.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAsyncControls."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAsyncControls.",
          "examples": [
            "function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;",
            "type IAsyncControls = {\n  readonly signal?: IAbortSignal;\n  readonly signals?: readonly IAbortSignal[];\n  readonly scheduler?: IUtilsScheduler;\n  readonly unref?: boolean;\n};",
            "type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal }) => T | PromiseLike<T>;\n\nfunction withTimeout<T>(\n  operation: ITimeoutOperation<T>,\n  options: IAsyncControls & {\n    readonly timeoutMs: number;\n    readonly report?: IUtilsReporter;\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly cooperativeCancellation?: boolean;\n  }\n): Promise<T>;",
            "type IAbortOperation<T> = ITimeoutOperation<T>;\n\nfunction raceWithAbort<T>(\n  operation: IAbortOperation<T>,\n  options?: Pick<IAsyncControls, 'signal' | 'signals'> & {\n    readonly report?: IUtilsReporter;\n    readonly cleanupPolicy?: 'reject' | 'report';\n  }\n): Promise<T>;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IAsyncControls = { readonly signal?: IAbortSignal; readonly signals?: readonly IAbortSignal[]; readonly scheduler?: IUtilsScheduler; readonly unref?: boolean; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAsyncControls when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAsyncControls does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IAsyncControls is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAsyncControls from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAsyncControls when its type contract and declared inputs match the result you need. Choose a different public symbol when IAsyncControls does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAsyncControls."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAsyncControls."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IAsyncControls:export type IAsyncControls = { readonly signal?: IAbortSignal; readonly signals?: readonly IAbortSignal[]; readonly scheduler?: IUtilsScheduler; readonly unref?: boolean; };",
          "guidance": [],
          "usageScore": 38,
          "configuration": [],
          "fragment": "utils-promise--IAsyncControls"
        },
        {
          "name": "IConcurrencyLimiter",
          "kind": "type",
          "signature": "export type IConcurrencyLimiter = { run<T>(task: (context: { readonly signal?: IAbortSignal; }) => T | PromiseLike<T>, options?: { readonly signal?: IAbortSignal; }): Promise<T>; readonly activeCount: number; readonly pendingCount: number; whenIdle(): Promise<void>; close(reason?: unknown): void; dispose(reason?: unknown): Promise<void>; };",
          "declarationLine": 98,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IConcurrencyLimiter is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConcurrencyLimiter.",
          "advanced": "No additional advanced behavior is declared for IConcurrencyLimiter.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IConcurrencyLimiter."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IConcurrencyLimiter.",
          "examples": [
            "type IConcurrencyLimiter = {\n  run<T>(\n    task: (context: { readonly signal?: IAbortSignal }) => T | PromiseLike<T>,\n    options?: { readonly signal?: IAbortSignal }\n  ): Promise<T>;\n  readonly activeCount: number;\n  readonly pendingCount: number;\n  whenIdle(): Promise<void>;\n  close(reason?: unknown): void;\n  dispose(reason?: unknown): Promise<void>;\n};\nfunction createConcurrencyLimiter(options: {\n  readonly concurrency: number;\n  readonly report?: IUtilsReporter;\n}): IConcurrencyLimiter;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IConcurrencyLimiter = { run<T>(task: (context: { readonly signal?: IAbortSignal; }) => T | PromiseLike<T>, options?: { readonly signal?: IAbortSignal; }): Promise<T>; readonly activeCount: number; readonly pendingCount: number; whenIdle(): Promise<void>; close(reason?: unknown): void; dispose(reason?: unknown): Promise<void>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IConcurrencyLimiter when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IConcurrencyLimiter does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IConcurrencyLimiter is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IConcurrencyLimiter from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IConcurrencyLimiter when its type contract and declared inputs match the result you need. Choose a different public symbol when IConcurrencyLimiter does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IConcurrencyLimiter."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IConcurrencyLimiter."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IConcurrencyLimiter:export type IConcurrencyLimiter = { run<T>(task: (context: { readonly signal?: IAbortSignal; }) => T | PromiseLike<T>, options?: { readonly signal?: IAbortSignal; }): Promise<T>; readonly activeCount: number; readonly pendingCount: number; whenIdle(): Promise<void>; close(reason?: unknown): void; dispose(reason?: unknown): Promise<void>; };",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-promise--IConcurrencyLimiter"
        },
        {
          "name": "IDeferred",
          "kind": "type",
          "signature": "export type IDeferred<T> = { readonly promise: Promise<T>; readonly resolve: (value: T | PromiseLike<T>) => void; readonly reject: (reason?: unknown) => void; };",
          "declarationLine": 52,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IDeferred is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDeferred.",
          "advanced": "No additional advanced behavior is declared for IDeferred.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDeferred."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDeferred.",
          "examples": [
            "type IDeferred<T> = {\n  readonly promise: Promise<T>;\n  readonly resolve: (value: T | PromiseLike<T>) => void;\n  readonly reject: (reason?: unknown) => void;\n};\nfunction deferred<T>(): IDeferred<T>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IDeferred<T> = { readonly promise: Promise<T>; readonly resolve: (value: T | PromiseLike<T>) => void; readonly reject: (reason?: unknown) => void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDeferred when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDeferred does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IDeferred is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDeferred from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDeferred when its type contract and declared inputs match the result you need. Choose a different public symbol when IDeferred does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDeferred."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDeferred."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IDeferred:export type IDeferred<T> = { readonly promise: Promise<T>; readonly resolve: (value: T | PromiseLike<T>) => void; readonly reject: (reason?: unknown) => void; };",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "utils-promise--IDeferred"
        },
        {
          "name": "IManualScheduler",
          "kind": "type",
          "signature": "export type IManualScheduler = IUtilsScheduler & { advance(ms: number): void; readonly pendingCount: number; };",
          "declarationLine": 48,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IManualScheduler is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IManualScheduler.",
          "advanced": "No additional advanced behavior is declared for IManualScheduler.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IManualScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IManualScheduler.",
          "examples": [
            "type IScheduledTask = { cancel(): void; unref?(): void };\ntype IUtilsScheduler = {\n  now(): number;\n  schedule(callback: () => void, delayMs: number): IScheduledTask;\n};\ntype IManualScheduler = IUtilsScheduler & {\n  advance(ms: number): void;\n  readonly pendingCount: number;\n};",
            "function createManualScheduler(): IManualScheduler;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IManualScheduler = IUtilsScheduler & { advance(ms: number): void; readonly pendingCount: number; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IManualScheduler when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IManualScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IManualScheduler is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IManualScheduler from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IManualScheduler when its type contract and declared inputs match the result you need. Choose a different public symbol when IManualScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IManualScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IManualScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IManualScheduler:export type IManualScheduler = IUtilsScheduler & { advance(ms: number): void; readonly pendingCount: number; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-promise--IManualScheduler"
        },
        {
          "name": "IRetryContext",
          "kind": "type",
          "signature": "export type IRetryContext = { readonly attempt: number; readonly signal: IAbortSignal; readonly remainingMs?: number; };",
          "declarationLine": 63,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IRetryContext is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IRetryContext.",
          "advanced": "No additional advanced behavior is declared for IRetryContext.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IRetryContext."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IRetryContext.",
          "examples": [
            "type IRetryContext = {\n  readonly attempt: number;\n  readonly signal: IAbortSignal;\n  readonly remainingMs?: number;\n};\ntype IRetryFailureContext = IRetryContext & { readonly maxAttempts: number };\n\nfunction retry<T>(\n  operation: (context: IRetryContext) => T | PromiseLike<T>,\n  options: {\n    readonly maxAttempts: number;\n    readonly shouldRetry: (\n      error: unknown,\n      context: IRetryFailureContext\n    ) => boolean | PromiseLike<boolean>;\n    readonly delay?: number | ((error: unknown, context: IRetryFailureContext) => number);\n    readonly totalTimeoutMs?: number;\n    readonly attemptTimeoutMs?: number;\n    readonly signal?: IAbortSignal;\n    readonly signals?: readonly IAbortSignal[];\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly report?: IUtilsReporter;\n    readonly unref?: boolean;\n    readonly scheduler?: IUtilsScheduler;\n  }\n): Promise<T>;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IRetryContext = { readonly attempt: number; readonly signal: IAbortSignal; readonly remainingMs?: number; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IRetryContext when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IRetryContext does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IRetryContext is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IRetryContext from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IRetryContext when its type contract and declared inputs match the result you need. Choose a different public symbol when IRetryContext does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IRetryContext."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IRetryContext."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IRetryContext:export type IRetryContext = { readonly attempt: number; readonly signal: IAbortSignal; readonly remainingMs?: number; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-promise--IRetryContext"
        },
        {
          "name": "IRetryFailureContext",
          "kind": "type",
          "signature": "export type IRetryFailureContext = IRetryContext & { readonly maxAttempts: number; };",
          "declarationLine": 68,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IRetryFailureContext is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IRetryFailureContext.",
          "advanced": "No additional advanced behavior is declared for IRetryFailureContext.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IRetryFailureContext."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IRetryFailureContext.",
          "examples": [
            "type IRetryContext = {\n  readonly attempt: number;\n  readonly signal: IAbortSignal;\n  readonly remainingMs?: number;\n};\ntype IRetryFailureContext = IRetryContext & { readonly maxAttempts: number };\n\nfunction retry<T>(\n  operation: (context: IRetryContext) => T | PromiseLike<T>,\n  options: {\n    readonly maxAttempts: number;\n    readonly shouldRetry: (\n      error: unknown,\n      context: IRetryFailureContext\n    ) => boolean | PromiseLike<boolean>;\n    readonly delay?: number | ((error: unknown, context: IRetryFailureContext) => number);\n    readonly totalTimeoutMs?: number;\n    readonly attemptTimeoutMs?: number;\n    readonly signal?: IAbortSignal;\n    readonly signals?: readonly IAbortSignal[];\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly report?: IUtilsReporter;\n    readonly unref?: boolean;\n    readonly scheduler?: IUtilsScheduler;\n  }\n): Promise<T>;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IRetryFailureContext = IRetryContext & { readonly maxAttempts: number; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IRetryFailureContext when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IRetryFailureContext does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IRetryFailureContext is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IRetryFailureContext from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IRetryFailureContext when its type contract and declared inputs match the result you need. Choose a different public symbol when IRetryFailureContext does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IRetryFailureContext."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IRetryFailureContext."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IRetryFailureContext:export type IRetryFailureContext = IRetryContext & { readonly maxAttempts: number; };",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "utils-promise--IRetryFailureContext"
        },
        {
          "name": "IScheduledTask",
          "kind": "type",
          "signature": "export type IScheduledTask = { cancel(): void; unref?(): void; };",
          "declarationLine": 40,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IScheduledTask is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IScheduledTask.",
          "advanced": "No additional advanced behavior is declared for IScheduledTask.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IScheduledTask."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IScheduledTask.",
          "examples": [
            "type IScheduledTask = { cancel(): void; unref?(): void };\ntype IUtilsScheduler = {\n  now(): number;\n  schedule(callback: () => void, delayMs: number): IScheduledTask;\n};\ntype IManualScheduler = IUtilsScheduler & {\n  advance(ms: number): void;\n  readonly pendingCount: number;\n};"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IScheduledTask = { cancel(): void; unref?(): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IScheduledTask when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IScheduledTask does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IScheduledTask is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IScheduledTask from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IScheduledTask when its type contract and declared inputs match the result you need. Choose a different public symbol when IScheduledTask does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IScheduledTask."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IScheduledTask."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IScheduledTask:export type IScheduledTask = { cancel(): void; unref?(): void; };",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-promise--IScheduledTask"
        },
        {
          "name": "ITimeoutOperation",
          "kind": "type",
          "signature": "export type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal; }) => T | PromiseLike<T>;",
          "declarationLine": 78,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "ITimeoutOperation is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ITimeoutOperation.",
          "advanced": "No additional advanced behavior is declared for ITimeoutOperation.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ITimeoutOperation."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ITimeoutOperation.",
          "examples": [
            "type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal }) => T | PromiseLike<T>;\n\nfunction withTimeout<T>(\n  operation: ITimeoutOperation<T>,\n  options: IAsyncControls & {\n    readonly timeoutMs: number;\n    readonly report?: IUtilsReporter;\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly cooperativeCancellation?: boolean;\n  }\n): Promise<T>;",
            "type IAbortOperation<T> = ITimeoutOperation<T>;\n\nfunction raceWithAbort<T>(\n  operation: IAbortOperation<T>,\n  options?: Pick<IAsyncControls, 'signal' | 'signals'> & {\n    readonly report?: IUtilsReporter;\n    readonly cleanupPolicy?: 'reject' | 'report';\n  }\n): Promise<T>;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal; }) => T | PromiseLike<T>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ITimeoutOperation when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ITimeoutOperation does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ITimeoutOperation is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ITimeoutOperation from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ITimeoutOperation when its type contract and declared inputs match the result you need. Choose a different public symbol when ITimeoutOperation does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ITimeoutOperation."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ITimeoutOperation."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:ITimeoutOperation:export type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal; }) => T | PromiseLike<T>;",
          "guidance": [],
          "usageScore": 24,
          "configuration": [],
          "fragment": "utils-promise--ITimeoutOperation"
        },
        {
          "name": "IUtilsReporter",
          "kind": "type",
          "signature": "export type IUtilsReporter = (error: unknown, context: { readonly operation: 'abort-timeout-signal' | 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter'; readonly phase: 'late-rejection' | 'cleanup' | 'reporter'; readonly attempt?: number; }) => void;",
          "declarationLine": 71,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IUtilsReporter is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IUtilsReporter.",
          "advanced": "No additional advanced behavior is declared for IUtilsReporter.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IUtilsReporter."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IUtilsReporter.",
          "examples": [
            "type IAbortTimeoutSignalOptions = {\n  readonly signal?: IAbortSignal;\n  readonly timeoutMs?: number;\n  readonly scheduler?: IUtilsScheduler;\n  readonly timeoutReason?: () => unknown;\n  readonly report?: IUtilsReporter;\n};\ntype IAbortTimeoutSignal = {\n  readonly signal: IAbortSignal | undefined;\n  readonly dispose: () => void;\n};\nfunction createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;",
            "type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal }) => T | PromiseLike<T>;\n\nfunction withTimeout<T>(\n  operation: ITimeoutOperation<T>,\n  options: IAsyncControls & {\n    readonly timeoutMs: number;\n    readonly report?: IUtilsReporter;\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly cooperativeCancellation?: boolean;\n  }\n): Promise<T>;",
            "type IAbortOperation<T> = ITimeoutOperation<T>;\n\nfunction raceWithAbort<T>(\n  operation: IAbortOperation<T>,\n  options?: Pick<IAsyncControls, 'signal' | 'signals'> & {\n    readonly report?: IUtilsReporter;\n    readonly cleanupPolicy?: 'reject' | 'report';\n  }\n): Promise<T>;",
            "type IRetryContext = {\n  readonly attempt: number;\n  readonly signal: IAbortSignal;\n  readonly remainingMs?: number;\n};\ntype IRetryFailureContext = IRetryContext & { readonly maxAttempts: number };\n\nfunction retry<T>(\n  operation: (context: IRetryContext) => T | PromiseLike<T>,\n  options: {\n    readonly maxAttempts: number;\n    readonly shouldRetry: (\n      error: unknown,\n      context: IRetryFailureContext\n    ) => boolean | PromiseLike<boolean>;\n    readonly delay?: number | ((error: unknown, context: IRetryFailureContext) => number);\n    readonly totalTimeoutMs?: number;\n    readonly attemptTimeoutMs?: number;\n    readonly signal?: IAbortSignal;\n    readonly signals?: readonly IAbortSignal[];\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly report?: IUtilsReporter;\n    readonly unref?: boolean;\n    readonly scheduler?: IUtilsScheduler;\n  }\n): Promise<T>;",
            "type IUtilsReporter = (\n  error: unknown,\n  context: {\n    readonly operation: 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter';\n    readonly phase: 'late-rejection' | 'cleanup' | 'reporter';\n    readonly attempt?: number;\n  }\n) => void;\nconst hostRethrowReporter: IUtilsReporter;",
            "type IConcurrencyLimiter = {\n  run<T>(\n    task: (context: { readonly signal?: IAbortSignal }) => T | PromiseLike<T>,\n    options?: { readonly signal?: IAbortSignal }\n  ): Promise<T>;\n  readonly activeCount: number;\n  readonly pendingCount: number;\n  whenIdle(): Promise<void>;\n  close(reason?: unknown): void;\n  dispose(reason?: unknown): Promise<void>;\n};\nfunction createConcurrencyLimiter(options: {\n  readonly concurrency: number;\n  readonly report?: IUtilsReporter;\n}): IConcurrencyLimiter;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IUtilsReporter = (error: unknown, context: { readonly operation: 'abort-timeout-signal' | 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter'; readonly phase: 'late-rejection' | 'cleanup' | 'reporter'; readonly attempt?: number; }) => void;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IUtilsReporter when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IUtilsReporter does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IUtilsReporter is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IUtilsReporter from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IUtilsReporter when its type contract and declared inputs match the result you need. Choose a different public symbol when IUtilsReporter does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IUtilsReporter."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IUtilsReporter."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IUtilsReporter:export type IUtilsReporter = (error: unknown, context: { readonly operation: 'abort-timeout-signal' | 'withTimeout' | 'raceWithAbort' | 'retry' | 'sleep' | 'limiter'; readonly phase: 'late-rejection' | 'cleanup' | 'reporter'; readonly attempt?: number; }) => void;",
          "guidance": [],
          "usageScore": 84,
          "configuration": [],
          "fragment": "utils-promise--IUtilsReporter"
        },
        {
          "name": "IUtilsScheduler",
          "kind": "type",
          "signature": "export type IUtilsScheduler = { now(): number; schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "declarationLine": 44,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "IUtilsScheduler is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IUtilsScheduler.",
          "advanced": "No additional advanced behavior is declared for IUtilsScheduler.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IUtilsScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IUtilsScheduler.",
          "examples": [
            "type IScheduledTask = { cancel(): void; unref?(): void };\ntype IUtilsScheduler = {\n  now(): number;\n  schedule(callback: () => void, delayMs: number): IScheduledTask;\n};\ntype IManualScheduler = IUtilsScheduler & {\n  advance(ms: number): void;\n  readonly pendingCount: number;\n};",
            "export const systemScheduler: IUtilsScheduler;",
            "type IAsyncControls = {\n  readonly signal?: IAbortSignal;\n  readonly signals?: readonly IAbortSignal[];\n  readonly scheduler?: IUtilsScheduler;\n  readonly unref?: boolean;\n};",
            "type IAbortTimeoutSignalOptions = {\n  readonly signal?: IAbortSignal;\n  readonly timeoutMs?: number;\n  readonly scheduler?: IUtilsScheduler;\n  readonly timeoutReason?: () => unknown;\n  readonly report?: IUtilsReporter;\n};\ntype IAbortTimeoutSignal = {\n  readonly signal: IAbortSignal | undefined;\n  readonly dispose: () => void;\n};\nfunction createAbortTimeoutSignal(options?: IAbortTimeoutSignalOptions): IAbortTimeoutSignal;",
            "type IRetryContext = {\n  readonly attempt: number;\n  readonly signal: IAbortSignal;\n  readonly remainingMs?: number;\n};\ntype IRetryFailureContext = IRetryContext & { readonly maxAttempts: number };\n\nfunction retry<T>(\n  operation: (context: IRetryContext) => T | PromiseLike<T>,\n  options: {\n    readonly maxAttempts: number;\n    readonly shouldRetry: (\n      error: unknown,\n      context: IRetryFailureContext\n    ) => boolean | PromiseLike<boolean>;\n    readonly delay?: number | ((error: unknown, context: IRetryFailureContext) => number);\n    readonly totalTimeoutMs?: number;\n    readonly attemptTimeoutMs?: number;\n    readonly signal?: IAbortSignal;\n    readonly signals?: readonly IAbortSignal[];\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly report?: IUtilsReporter;\n    readonly unref?: boolean;\n    readonly scheduler?: IUtilsScheduler;\n  }\n): Promise<T>;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export type IUtilsScheduler = { now(): number; schedule(callback: () => void, delayMs: number): IScheduledTask; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IUtilsScheduler when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IUtilsScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IUtilsScheduler is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IUtilsScheduler from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IUtilsScheduler when its type contract and declared inputs match the result you need. Choose a different public symbol when IUtilsScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IUtilsScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IUtilsScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:IUtilsScheduler:export type IUtilsScheduler = { now(): number; schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "guidance": [],
          "usageScore": 68,
          "configuration": [],
          "fragment": "utils-promise--IUtilsScheduler"
        },
        {
          "name": "raceWithAbort",
          "kind": "function",
          "signature": "export declare function raceWithAbort<T>(operation: IAbortOperation<T>, options?: Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; }): Promise<T>;",
          "declarationLine": 134,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Races a lazy operation against one or more external abort signals without creating a timer.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for raceWithAbort.",
          "advanced": "No additional advanced behavior is declared for raceWithAbort.",
          "parameters": [
            "operation: IAbortOperation<T>",
            "options: Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; } (optional)"
          ],
          "parameterDetails": [
            {
              "name": "operation",
              "type": "IAbortOperation<T>",
              "optional": false
            },
            {
              "name": "options",
              "type": "Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; }",
              "optional": true
            }
          ],
          "returns": "Promise<T>",
          "errors": ["No documented errors are declared for raceWithAbort."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for raceWithAbort.",
          "examples": [
            "const controller = new AbortController();\nconst result = raceWithAbort(({ signal }) => fetchSomething({ signal }), {\n  signal: controller.signal as any\n});",
            "await raceWithAbort(({ signal }) => fetch(url, { signal }), { signal: controller.signal });"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function raceWithAbort<T>(operation: IAbortOperation<T>, options?: Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; }): Promise<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "operation",
                "type": "IAbortOperation<T>",
                "optional": false
              },
              {
                "name": "options",
                "type": "Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["Promise<T>"],
          "whenToUse": "Use raceWithAbort when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when raceWithAbort does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Races a lazy operation against one or more external abort signals without creating a timer."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import raceWithAbort from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use raceWithAbort when its function contract and declared inputs match the result you need. Choose a different public symbol when raceWithAbort does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for raceWithAbort."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for raceWithAbort."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:raceWithAbort:export declare function raceWithAbort<T>(operation: IAbortOperation<T>, options?: Pick<IAsyncControls, 'signal' | 'signals'> & { readonly report?: IUtilsReporter; readonly cleanupPolicy?: 'reject' | 'report'; }): Promise<T>;",
          "guidance": [
            {
              "id": "racewithabort",
              "heading": "raceWithAbort",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IAbortOperation<T> = ITimeoutOperation<T>;\n\nfunction raceWithAbort<T>(\n  operation: IAbortOperation<T>,\n  options?: Pick<IAsyncControls, 'signal' | 'signals'> & {\n    readonly report?: IUtilsReporter;\n    readonly cleanupPolicy?: 'reject' | 'report';\n  }\n): Promise<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "与 `withTimeout` 语义相同，但不设置截止时间定时器，只在外部 `signal(s)` 中止时让操作提前失败。`cleanupPolicy: 'report'`（默认 `'reject'`）让监听器清理失败时改为上报诊断而不是让最终 Promise reject。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const controller = new AbortController();\nconst result = raceWithAbort(({ signal }) => fetchSomething({ signal }), {\n  signal: controller.signal as any\n});"
                }
              ]
            }
          ],
          "usageScore": 56,
          "configuration": [],
          "fragment": "utils-promise--raceWithAbort"
        },
        {
          "name": "retry",
          "kind": "function",
          "signature": "export declare function retry<T>(operation: (context: IRetryContext) => T | PromiseLike<T>, options: Parameters<typeof retryCore<T>>[1]): Promise<T>;",
          "declarationLine": 153,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Enforces the retry task's outer deadline, including policy and backoff admission work.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for retry.",
          "advanced": "No additional advanced behavior is declared for retry.",
          "parameters": [
            "operation: (context: IRetryContext) => T | PromiseLike<T>",
            "options: Parameters<typeof retryCore<T>>[1]"
          ],
          "parameterDetails": [
            {
              "name": "operation",
              "type": "(context: IRetryContext) => T | PromiseLike<T>",
              "optional": false
            },
            {
              "name": "options",
              "type": "Parameters<typeof retryCore<T>>[1]",
              "optional": false
            }
          ],
          "returns": "Promise<T>",
          "errors": ["No documented errors are declared for retry."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for retry.",
          "examples": [
            "await retry(\n  async ({ attempt }) => {\n    if (attempt < 3) throw new Error('boom');\n    return 'ok';\n  },\n  { maxAttempts: 5, shouldRetry: () => true, delay: (_, { attempt }) => attempt * 100 }\n); // 'ok'，共尝试 3 次，两次退避分别约 100ms/200ms",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "await retry(async () => mayFail(), {\n  maxAttempts: 3,\n  shouldRetry: () => true,\n  delay: (_, { attempt }) => attempt * 100\n});",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function retry<T>(operation: (context: IRetryContext) => T | PromiseLike<T>, options: Parameters<typeof retryCore<T>>[1]): Promise<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "operation",
                "type": "(context: IRetryContext) => T | PromiseLike<T>",
                "optional": false
              },
              {
                "name": "options",
                "type": "Parameters<typeof retryCore<T>>[1]",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Promise<T>"],
          "whenToUse": "Use retry when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when retry does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Enforces the retry task's outer deadline, including policy and backoff admission work."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import retry from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use retry when its function contract and declared inputs match the result you need. Choose a different public symbol when retry does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for retry."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for retry."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:retry:export declare function retry<T>(operation: (context: IRetryContext) => T | PromiseLike<T>, options: Parameters<typeof retryCore<T>>[1]): Promise<T>;",
          "guidance": [
            {
              "id": "retry",
              "heading": "retry",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IRetryContext = {\n  readonly attempt: number;\n  readonly signal: IAbortSignal;\n  readonly remainingMs?: number;\n};\ntype IRetryFailureContext = IRetryContext & { readonly maxAttempts: number };\n\nfunction retry<T>(\n  operation: (context: IRetryContext) => T | PromiseLike<T>,\n  options: {\n    readonly maxAttempts: number;\n    readonly shouldRetry: (\n      error: unknown,\n      context: IRetryFailureContext\n    ) => boolean | PromiseLike<boolean>;\n    readonly delay?: number | ((error: unknown, context: IRetryFailureContext) => number);\n    readonly totalTimeoutMs?: number;\n    readonly attemptTimeoutMs?: number;\n    readonly signal?: IAbortSignal;\n    readonly signals?: readonly IAbortSignal[];\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly report?: IUtilsReporter;\n    readonly unref?: boolean;\n    readonly scheduler?: IUtilsScheduler;\n  }\n): Promise<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "串行重试，带三层限制："
                },
                {
                  "type": "list",
                  "items": [
                    "`maxAttempts`：必须是正安全整数，第 `maxAttempts` 次仍失败则直接抛出最后一次的错误。",
                    "`attemptTimeoutMs`：每次尝试单独的超时（内部用 `withTimeout` 包裹）。",
                    "`totalTimeoutMs`：整个重试过程（包含所有尝试与退避等待）的总截止时间；超出会抛 `UtilsTimeoutError('total', totalTimeoutMs)`。",
                    "`shouldRetry(error, context)`：决定某次失败是否值得重试；返回 `false` 或 `maxAttempts` 已用尽都会终止重试并抛出该错误。",
                    "`delay`：固定毫秒数或 `(error, context) => 毫秒数` 的退避函数，作用于两次尝试之间的等待（通过内部 `sleep`，同样遵守 `signal`/`signals`）。",
                    "外部信号在重试过程中的任意时刻中止，都会以 `UtilsAbortError` 结束整个重试，不再进入下一次尝试。"
                  ]
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "await retry(\n  async ({ attempt }) => {\n    if (attempt < 3) throw new Error('boom');\n    return 'ok';\n  },\n  { maxAttempts: 5, shouldRetry: () => true, delay: (_, { attempt }) => attempt * 100 }\n); // 'ok'，共尝试 3 次，两次退避分别约 100ms/200ms"
                }
              ]
            },
            {
              "id": "组合工作流示例-截止时间-配置",
              "heading": "组合工作流示例：截止时间 + 配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                },
                {
                  "type": "paragraph",
                  "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
                }
              ]
            },
            {
              "id": "config-模块",
              "heading": "/config 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  ConfigProfile,\n  CONFIG_DELETE,\n  ownConfig,\n  readonlyConfig,\n  patchConfig,\n  parseConfigPath,\n  readConfigPath,\n  combineConfig\n} from '@migaia/utils/config';"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig`｜15 秒理解能力 —— 在接收外部配置的边界创建独立、可验证所有权的配置根："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const callerOptions = {\n  endpoint: '/v1',\n  retry: { attempts: 2 },\n  tags: new Set(['stable'])\n};\nconst config = ownConfig(callerOptions);\n\ncallerOptions.retry.attempts = 99;\ncallerOptions.tags.add('caller-mutated');\nconfig.retry.attempts; // 2：接纳后不再受调用方修改影响\nconfig.tags.has('caller-mutated'); // false：Set 也已复制\n\nconst publicView = readonlyConfig(config); // 可公开读取，任何层级的写入都会明确报错\nconst next = patchConfig(config, { retry: { attempts: 3 } }); // 从已拥有配置派生新根\nreadConfigPath(next, 'retry.attempts'); // { kind: 'value', value: 3 }"
                },
                {
                  "type": "paragraph",
                  "text": "`ownConfig()` 不只是普通 deep clone。它会验证整张配置图并复制循环引用、共享节点、`Date`、`RegExp`、`Map`、`Set`，再用不可伪造的内部 `WeakMap` 元数据登记 profile 与 limits。后续 `readonlyConfig()`、`patchConfig()`、`readConfigPath()` 和 `combineConfig()` 只接收这种已登记的配置根，因此不会把未经校验的普通对象误当成受管理配置。返回对象没有被冻结；需要向外暴露只读能力时使用 `readonlyConfig()`，需要更新时使用 `patchConfig()` 派生下一份配置。"
                },
                {
                  "type": "paragraph",
                  "text": "默认 `data` profile 面向可移植配置，遇到函数、危险键、Symbol key、非普通根对象或超限图会明确报错。只有配置确实需要携带回调或构造器时才选择 `richRuntime`；它是进程内运行时配置，不应当作可序列化数据。"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?: 'data' | 'richRuntime'` —— 默认 `'data'`（拒绝函数）；`'richRuntime'` 允许函数/可构造类",
                    "`limits?: Partial<IConfigLimits>` —— 收紧默认限制，字段：`maxDepth`(默认256)、`maxNodes`(默认100000)、`maxKeys`(默认1000000)、`maxPathLength`(默认4096)、`maxSegmentLength`(默认512)；只能收紧不能放宽"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readonlyConfig`｜5 秒上手 —— 给已有配置套上只读保护（单参数，无选项；入参必须是 `ownConfig` 产物）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const view = readonlyConfig(config);\nview.endpoint; // 正常读取；没有复制第二份配置\nview.retries = 5; // 抛 TypeError（CONFIG_READONLY）"
                },
                {
                  "type": "paragraph",
                  "text": "大白话：`view` 仍然读取 `config` 里的数据，但不允许调用方从 `view` 修改任何层级。普通对象赋值/删除、数组修改以及 `Map.set()`、`Set.add()`、`Date.setFullYear()` 都会报错。重复对同一份 `config` 调用会返回同一个只读对象；要修改配置，应对原来的 owned config 调用 `patchConfig()` 生成下一份，而不是写 `view`。"
                },
                {
                  "type": "paragraph",
                  "text": "`patchConfig`｜10 秒上手 —— 根级写时复制覆盖，`CONFIG_DELETE` 删除某键："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const next = patchConfig(config, { retries: 3, debug: CONFIG_DELETE });"
                },
                {
                  "type": "paragraph",
                  "text": "第三参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` —— 必须与 `base` 一致，否则抛 `CONFIG_CONFLICT`",
                    "`limits?: Partial<IConfigLimits>` —— 不能比 `base` 的限制更宽，否则抛 `CONFIG_CONFLICT`",
                    "`reuseUnchangedRoot?: boolean` —— 默认 `true`；`patch` 为空对象时直接复用 `base`，不做拷贝"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`readConfigPath`｜5 秒上手 —— 读嵌套路径，区分\"不存在\"与\"值是 undefined\"："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "readConfigPath(config, 'nested.value'); // { kind: 'missing' } | { kind: 'value', value }"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`config`（必填，须为 `ownConfig` 产物）、`path: string | readonly string[]`（必填），无可选项；返回的对象/函数类型值会自动包只读代理。"
                },
                {
                  "type": "paragraph",
                  "text": "`parseConfigPath`｜3 秒上手："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "parseConfigPath('a.b.c'); // ['a', 'b', 'c']"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数 `limits?: Pick<IConfigLimits, 'maxPathLength' | 'maxSegmentLength'>` —— 默认取包级默认值。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineConfig`｜10 秒上手 —— 按顺序合并多个来源，后者覆盖前者："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const defaults = ownConfig({\n  retries: 1,\n  tags: ['default'],\n  transport: { timeoutMs: 1_000, keepAlive: true },\n});\nconst override = ownConfig({\n  retries: 3,\n  tags: ['custom'],\n  transport: { timeoutMs: 2_500 },\n});\n\nconst merged = combineConfig([defaults, override], {\n  strategies: { record: 'merge', array: 'concat' },\n});\n\n// merged 是新配置；defaults 和 override 都不会被修改。\nmerged;\n// {\n//   retries: 3,\n//   tags: ['default', 'custom'],\n//   transport: { timeoutMs: 2500, keepAlive: true },\n// }\n\n// 这里的 diff 只是把结果变化写明，不是 combineConfig 的额外返回值。\nconst diff = {\n  retries: { before: defaults.retries, after: merged.retries },\n  tags: { before: defaults.tags, after: merged.tags },\n  timeoutMs: { before: defaults.transport.timeoutMs, after: merged.transport.timeoutMs },\n};"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项："
                },
                {
                  "type": "list",
                  "items": [
                    "`profile?` / `limits?` —— 同 `ownConfig`",
                    "`strategies?: Partial<IConfigMergeStrategies>` —— `record?: 'merge'|'replace'`(默认merge)、`array?: 'replace'|'concat'|'mergeByIndex'`(默认replace)、`map?: 'replace'|'merge'`(默认replace)、`set?: 'replace'|'union'`(默认replace)、`undefined?: 'ignore'|'assign'`(默认ignore)",
                    "`pathRules?: readonly { prefix: readonly PropertyKey[]; strategies: Partial<IConfigMergeStrategies> }[]` —— 按路径前缀覆盖策略，最长前缀优先",
                    "`onConflict?: (context: { path, left, right }) => { kind: 'left'|'right'|'delete'|'value'; value? }` —— 不可合并的双侧冲突值如何裁决，必须同步返回"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`ConfigProfile`｜3 秒上手 —— `data`（默认，纯数据）或 `richRuntime`（允许函数），常量对象，无调用参数："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "ownConfig({ onReady: () => {} }, { profile: ConfigProfile.richRuntime });"
                }
              ]
            },
            {
              "id": "高阶组合示例-1-截止时间-重试-只读配置",
              "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                }
              ]
            },
            {
              "id": "高阶组合示例-2-并发限流-重试-错误合并-批量拉取-容忍部分失败",
              "heading": "高阶组合示例 · 2. 并发限流 + 重试 + 错误合并：批量拉取，容忍部分失败",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createConcurrencyLimiter, retry } from '@migaia/utils/promise';\nimport { combineErrors, toError } from '@migaia/utils/error';\n\nconst limiter = createConcurrencyLimiter({ concurrency: 4 });\nconst outcomes = await Promise.allSettled(\n  urls.map((url) =>\n    limiter.run(() =>\n      retry(() => fetch(url).then((r) => r.json()), { maxAttempts: 3, shouldRetry: () => true })\n    )\n  )\n);\nawait limiter.dispose();\n\nconst failed = combineErrors(\n  outcomes.filter((o) => o.status === 'rejected').map((o) => (o as PromiseRejectedResult).reason),\n  'some URLs failed'\n);\nif (failed) throw failed;"
                }
              ]
            }
          ],
          "usageScore": 126,
          "configuration": [],
          "fragment": "utils-promise--retry"
        },
        {
          "name": "sleep",
          "kind": "function",
          "signature": "export declare function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;",
          "declarationLine": 122,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Resolves after a scheduler delay or rejects on cooperative abort.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for sleep.",
          "advanced": "No additional advanced behavior is declared for sleep.",
          "parameters": ["delayMs: number", "options: IAsyncControls (optional)"],
          "parameterDetails": [
            {
              "name": "delayMs",
              "type": "number",
              "optional": false
            },
            {
              "name": "options",
              "type": "IAsyncControls",
              "optional": true
            }
          ],
          "returns": "Promise<void>",
          "errors": ["No documented errors are declared for sleep."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for sleep.",
          "examples": [
            "const controller = new AbortController();\nconst p = sleep(1000, { signal: controller.signal as any });\ncontroller.abort('cancelled');\nawait p; // 抛 UtilsAbortError，cause 为 'cancelled'",
            "await sleep(1000); // 1 秒后 resolve\nawait sleep(1000, { signal: controller.signal }); // 中止时 reject UtilsAbortError"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "delayMs",
                "type": "number",
                "optional": false
              },
              {
                "name": "options",
                "type": "IAsyncControls",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["Promise<void>"],
          "whenToUse": "Use sleep when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when sleep does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Resolves after a scheduler delay or rejects on cooperative abort."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import sleep from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use sleep when its function contract and declared inputs match the result you need. Choose a different public symbol when sleep does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for sleep."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for sleep."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:sleep:export declare function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;",
          "guidance": [
            {
              "id": "sleep",
              "heading": "sleep",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function sleep(delayMs: number, options?: IAsyncControls): Promise<void>;"
                },
                {
                  "type": "paragraph",
                  "text": "在调度器延迟后 resolve；若传入的 `signal`/`signals` 在等待期间中止，则以 `UtilsAbortError` reject。`delayMs` 必须是有限非负数。会正确清理已注册的定时器与 abort 监听器，即使清理过程本身抛错也会通过 `hostRethrowReporter`（或调用方指定的 reporter）上报，而不是吞掉。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type IAsyncControls = {\n  readonly signal?: IAbortSignal;\n  readonly signals?: readonly IAbortSignal[];\n  readonly scheduler?: IUtilsScheduler;\n  readonly unref?: boolean;\n};"
                },
                {
                  "type": "paragraph",
                  "text": "`signal` 与 `signals` 互斥——同时提供两者会 reject 一个 `TypeError`。`unref: true` 会尝试调用底层定时器句柄的 `unref()`（Node 环境下避免阻塞进程退出）。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const controller = new AbortController();\nconst p = sleep(1000, { signal: controller.signal as any });\ncontroller.abort('cancelled');\nawait p; // 抛 UtilsAbortError，cause 为 'cancelled'"
                }
              ]
            }
          ],
          "usageScore": 54,
          "configuration": [],
          "fragment": "utils-promise--sleep"
        },
        {
          "name": "systemScheduler",
          "kind": "const",
          "signature": "export declare const systemScheduler: IUtilsScheduler;",
          "declarationLine": 111,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Schedules callbacks with native timers while forwarding optional unref hints.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for systemScheduler.",
          "advanced": "No additional advanced behavior is declared for systemScheduler.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for systemScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for systemScheduler.",
          "examples": [
            "export const systemScheduler: IUtilsScheduler;",
            "import {\n  sleep,\n  createAbortTimeoutSignal,\n  withTimeout,\n  raceWithAbort,\n  retry,\n  createConcurrencyLimiter,\n  deferred,\n  toPromise,\n  createManualScheduler,\n  systemScheduler,\n  hostRethrowReporter\n} from '@migaia/utils/promise';",
            "const now = systemScheduler.now();"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": ["export declare const systemScheduler: IUtilsScheduler;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use systemScheduler when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when systemScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Schedules callbacks with native timers while forwarding optional unref hints."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import systemScheduler from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use systemScheduler when its const contract and declared inputs match the result you need. Choose a different public symbol when systemScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for systemScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for systemScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:systemScheduler:export declare const systemScheduler: IUtilsScheduler;",
          "guidance": [],
          "usageScore": 36,
          "configuration": [],
          "fragment": "utils-promise--systemScheduler"
        },
        {
          "name": "toPromise",
          "kind": "function",
          "signature": "export declare function toPromise<T>(run: () => T): Promise<Awaited<T>>;",
          "declarationLine": 118,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Runs one synchronous computation immediately while normalizing its value or thrown reason to a native Promise settlement.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for toPromise.",
          "advanced": "No additional advanced behavior is declared for toPromise.",
          "parameters": ["run: () => T"],
          "parameterDetails": [
            {
              "name": "run",
              "type": "() => T",
              "optional": false
            }
          ],
          "returns": "Promise<Awaited<T>>",
          "errors": ["No documented errors are declared for toPromise."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for toPromise.",
          "examples": [
            "const found = await toPromise(() => cache.get('key'));\nconst pending = toPromise(() => {\n  throw new Error('read failed');\n});\nawait pending; // 异步观察 rejection",
            "const value = await toPromise(() => cache.get('key'));\nawait toPromise(() => {\n  throw new Error('failed'); // 不会从 toPromise 调用点同步抛出，而是返回 rejected Promise\n});"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function toPromise<T>(run: () => T): Promise<Awaited<T>>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "run",
                "type": "() => T",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Promise<Awaited<T>>"],
          "whenToUse": "Use toPromise when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when toPromise does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Runs one synchronous computation immediately while normalizing its value or thrown reason to a native Promise settlement."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import toPromise from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use toPromise when its function contract and declared inputs match the result you need. Choose a different public symbol when toPromise does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for toPromise."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for toPromise."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:toPromise:export declare function toPromise<T>(run: () => T): Promise<Awaited<T>>;",
          "guidance": [
            {
              "id": "topromise",
              "heading": "toPromise",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function toPromise<T>(run: () => T): Promise<Awaited<T>>;"
                },
                {
                  "type": "paragraph",
                  "text": "同步且恰好调用 `run` 一次，并用原生 Promise 规则同化返回值。若 `run` 同步抛出，调用点不会同步抛错，而是返回以 exact reason reject 的 Promise。它只负责同步计算到 Promise 的规范化，不创建 timer、scheduler、retry 或 cancellation 状态。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const found = await toPromise(() => cache.get('key'));\nconst pending = toPromise(() => {\n  throw new Error('read failed');\n});\nawait pending; // 异步观察 rejection"
                },
                {
                  "type": "paragraph",
                  "text": "与 `Promise.resolve().then(() => run())` 对比："
                },
                {
                  "type": "list",
                  "items": [
                    "`toPromise(run)` 立即调用 `run`；返回前已经发生读取、写入或其他同步副作用。`run` 返回原生 Promise 时，返回值保持同一个 Promise identity。",
                    "`Promise.resolve().then(run)` 把调用推迟到下一次 microtask，并返回新的链式 Promise。它适合主动让出当前调用栈、避免同步重入，不适合依赖“调用后立即生效”的逻辑。",
                    "两者都会把异常变成 rejection，但产生 rejection 的时点不同：前者在当前调用过程中捕获同步异常，后者等 microtask 真正执行 `run` 时才捕获。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "因此，需要同步启动工作并统一返回 Promise 时使用 `toPromise`；需要延迟启动工作时使用 `Promise.resolve().then(run)`。"
                }
              ]
            }
          ],
          "usageScore": 60,
          "configuration": [],
          "fragment": "utils-promise--toPromise"
        },
        {
          "name": "withTimeout",
          "kind": "function",
          "signature": "export declare function withTimeout<T>(operation: ITimeoutOperation<T>, options: IWithTimeoutOptions): Promise<T>;",
          "declarationLine": 132,
          "source": "packages/utils/dist/promise.d.ts",
          "purpose": "Races a lazy operation against an abort-aware deadline.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for withTimeout.",
          "advanced": "No additional advanced behavior is declared for withTimeout.",
          "parameters": ["operation: ITimeoutOperation<T>", "options: IWithTimeoutOptions"],
          "parameterDetails": [
            {
              "name": "operation",
              "type": "ITimeoutOperation<T>",
              "optional": false
            },
            {
              "name": "options",
              "type": "IWithTimeoutOptions",
              "optional": false
            }
          ],
          "returns": "Promise<T>",
          "errors": ["No documented errors are declared for withTimeout."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for withTimeout.",
          "examples": [
            "await withTimeout(\n  async ({ signal }) => {\n    while (!signal.aborted) {\n      // 做一些可中断的工作\n    }\n    throw signal.reason;\n  },\n  { timeoutMs: 500 }\n); // 500ms 后以 UtilsTimeoutError('operation', 500) reject",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);",
            "await withTimeout(\n  async ({ signal }) => {\n    if (signal.aborted) throw signal.reason;\n    return fetchData();\n  },\n  { timeoutMs: 500 }\n);",
            "const scheduler = createManualScheduler();\nconst p = withTimeout(op, { timeoutMs: 100, scheduler });\nscheduler.advance(100); // 手动触发超时，无需真实等待",
            "withTimeout(op, { timeoutMs: 100, report: myLogger }); // 用自定义 reporter 替换默认行为",
            "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
          ],
          "members": [],
          "exportPath": "./promise",
          "overloadSignatures": [
            "export declare function withTimeout<T>(operation: ITimeoutOperation<T>, options: IWithTimeoutOptions): Promise<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "operation",
                "type": "ITimeoutOperation<T>",
                "optional": false
              },
              {
                "name": "options",
                "type": "IWithTimeoutOptions",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Promise<T>"],
          "whenToUse": "Use withTimeout when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when withTimeout does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Races a lazy operation against an abort-aware deadline."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import withTimeout from @migaia/utils/promise, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use withTimeout when its function contract and declared inputs match the result you need. Choose a different public symbol when withTimeout does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for withTimeout."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for withTimeout."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/promise.d.ts:withTimeout:export declare function withTimeout<T>(operation: ITimeoutOperation<T>, options: IWithTimeoutOptions): Promise<T>;",
          "guidance": [
            {
              "id": "withtimeout",
              "heading": "withTimeout",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "type ITimeoutOperation<T> = (context: { readonly signal: IAbortSignal }) => T | PromiseLike<T>;\n\nfunction withTimeout<T>(\n  operation: ITimeoutOperation<T>,\n  options: IAsyncControls & {\n    readonly timeoutMs: number;\n    readonly report?: IUtilsReporter;\n    readonly zeroTimeoutBehavior?: 'skip' | 'start';\n    readonly cooperativeCancellation?: boolean;\n  }\n): Promise<T>;"
                },
                {
                  "type": "paragraph",
                  "text": "用截止时间包裹一个惰性操作。`operation` 收到的 `signal` 是内部生成的合并信号：外部 `signal(s)` 中止、或截止时间到达，都会让这个 `signal` 变为 `aborted`；操作函数必须自行检查它才能提前退出。"
                },
                {
                  "type": "list",
                  "items": [
                    "`timeoutMs`：必须是有限非负数；`timeoutMs === 0` 时默认立即以 `UtilsTimeoutError('operation', 0)` reject（除非 `zeroTimeoutBehavior: 'start'`，此时改为立刻启动操作、不设超时）。",
                    "`cooperativeCancellation`（默认 `true`）：为 `false` 时不分配内部 `AbortController`，操作函数只会拿到一个惰性的、恒不中止的信号——用于纯粹的\"限时等待结果，但不需要通知操作提前退出\"的场景。",
                    "`report`：当操作在已经结算（超时/被外部中止）之后才 resolve/reject，这个迟到的结果不会再影响最终返回值，但会经由 `report`（默认 `hostRethrowReporter`）上报，避免静默丢弃未处理的错误。",
                    "清理阶段（取消定时器、移除监听器）失败时，会把清理错误与原始错误一起包装成 `AggregateError` 一并 reject。"
                  ]
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "await withTimeout(\n  async ({ signal }) => {\n    while (!signal.aborted) {\n      // 做一些可中断的工作\n    }\n    throw signal.reason;\n  },\n  { timeoutMs: 500 }\n); // 500ms 后以 UtilsTimeoutError('operation', 500) reject"
                }
              ]
            },
            {
              "id": "组合工作流示例-截止时间-配置",
              "heading": "组合工作流示例：截止时间 + 配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "该组合用于本仓测试验证，不是外部生产代码示例。外部测试应优先使用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst initial = ownConfig({ endpoint: '/v1', retries: 2, debug: true });\nconst next = patchConfig(initial, { debug: CONFIG_DELETE });\nconst publicConfig = readonlyConfig(next);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return publicConfig.endpoint;\n      },\n      { maxAttempts: publicConfig.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\n\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                },
                {
                  "type": "paragraph",
                  "text": "`createManualScheduler()` 让时间变为确定性，便于在单测中精确断言超时/重试行为。超时中止是协作式的：`retry`/业务代码必须读取传入的 `signal` 才能提前退出。`ownConfig` 拷贝并登记入参；`readonlyConfig` 返回带缓存、只能读取不能修改的对象；`patchConfig` 返回一份新的受管理配置，`CONFIG_DELETE` 只在根一层的补丁里生效。"
                }
              ]
            },
            {
              "id": "高阶组合示例-1-截止时间-重试-只读配置",
              "heading": "高阶组合示例 · 1. 截止时间 + 重试 + 只读配置",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "以下示例用于本仓测试组合验证，不是外部生产代码的推荐写法；外部测试应优先复用测试框架的 fake timers。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createManualScheduler, retry, withTimeout } from '@migaia/utils/promise';\nimport { CONFIG_DELETE, ownConfig, patchConfig, readonlyConfig } from '@migaia/utils/config';\n\nconst scheduler = createManualScheduler();\nconst config = readonlyConfig(\n  patchConfig(ownConfig({ endpoint: '/v1', retries: 2, debug: true }), { debug: CONFIG_DELETE })\n);\n\nconst request = withTimeout(\n  ({ signal }) =>\n    retry(\n      async () => {\n        if (signal.aborted) throw signal.reason;\n        return config.endpoint;\n      },\n      { maxAttempts: config.retries, scheduler }\n    ),\n  { timeoutMs: 100, scheduler }\n);\nscheduler.advance(100);\nawait request.catch(() => undefined);"
                }
              ]
            }
          ],
          "usageScore": 94,
          "configuration": [],
          "fragment": "utils-promise--withTimeout"
        }
      ],
      "aliases": [
        {
          "identity": "packages/utils/dist/error.d.ts:UtilsAbortError:export declare class UtilsAbortError extends UtilsError { readonly name: \"AbortError\"; constructor(reason?: unknown); }",
          "name": "UtilsAbortError",
          "signature": "export declare class UtilsAbortError extends UtilsError { readonly name: \"AbortError\"; constructor(reason?: unknown); }",
          "source": "packages/utils/dist/error.d.ts",
          "exportPath": "./promise",
          "ownerExportPath": "./error",
          "ownerModule": "error",
          "ownerFragment": "utils-error--UtilsAbortError"
        }
      ]
    },
    {
      "id": "utils:string",
      "library": "utils",
      "module": "string",
      "exportPath": "./string",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "format",
          "kind": "function",
          "signature": "export declare function format(source: string, values: Readonly<Record<string, unknown>>, options?: IFormatOptions): string;",
          "declarationLine": 16,
          "source": "packages/utils/dist/string.d.ts",
          "purpose": "Replaces placeholder paths in one linear scan. Doubled delimiters emit literal delimiters. Missing paths are preserved by default so malformed user-visible templates fail visibly.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for format.",
          "advanced": "No additional advanced behavior is declared for format.",
          "parameters": [
            "source: string",
            "values: Readonly<Record<string, unknown>>",
            "options: IFormatOptions (optional)"
          ],
          "parameterDetails": [
            {
              "name": "source",
              "type": "string",
              "optional": false
            },
            {
              "name": "values",
              "type": "Readonly<Record<string, unknown>>",
              "optional": false
            },
            {
              "name": "options",
              "type": "IFormatOptions",
              "optional": true
            }
          ],
          "returns": "string",
          "errors": ["No documented errors are declared for format."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for format.",
          "examples": [
            "import { format, formatCurrency, isEmptyValue, isPrimitive } from '@migaia/utils';\n\nisEmptyValue('  '); // true；0、false、0n 均为 false\nisPrimitive(Symbol('id')); // true\nformat('库存 {stock}，金额 {money}', { stock: 0, money: '¥12.00' });\nformatCurrency(1234.5, 'CNY', {\n  locales: 'zh',\n  format: { minimumFractionDigits: 2, maximumFractionDigits: 2 }\n});"
          ],
          "members": [],
          "exportPath": "./string",
          "overloadSignatures": [
            "export declare function format(source: string, values: Readonly<Record<string, unknown>>, options?: IFormatOptions): string;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "source",
                "type": "string",
                "optional": false
              },
              {
                "name": "values",
                "type": "Readonly<Record<string, unknown>>",
                "optional": false
              },
              {
                "name": "options",
                "type": "IFormatOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["string"],
          "whenToUse": "Use format when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when format does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Replaces placeholder paths in one linear scan. Doubled delimiters emit literal delimiters. Missing paths are preserved by default so malformed user-visible templates fail visibly."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import format from @migaia/utils/string, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use format when its function contract and declared inputs match the result you need. Choose a different public symbol when format does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for format."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for format."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "placeholder",
              "type": "IFormatPlaceholder",
              "optional": true,
              "description": ""
            },
            {
              "name": "missing",
              "type": "'preserve' | 'empty' | 'throw'",
              "optional": true,
              "description": ""
            },
            {
              "name": "nullish",
              "type": "'empty' | 'stringify'",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/utils/dist/string.d.ts:format:export declare function format(source: string, values: Readonly<Record<string, unknown>>, options?: IFormatOptions): string;",
          "guidance": [
            {
              "id": "utilserrorcode",
              "heading": "UtilsErrorCode",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const UtilsErrorCode = {\n  invalidArgument: 'INVALID_ARGUMENT',\n  nonErrorValue: 'NON_ERROR_VALUE',\n  envUnsupported: 'ENV_UNSUPPORTED',\n  aborted: 'ABORTED',\n  deadlineExceeded: 'DEADLINE_EXCEEDED',\n  schedulerRunaway: 'SCHEDULER_RUNAWAY',\n  errorIdentityConflict: 'ERROR_IDENTITY_CONFLICT',\n  cloneUnsupported: 'CLONE_UNSUPPORTED',\n  invalidEncoding: 'INVALID_ENCODING',\n  limiterClosed: 'LIMITER_CLOSED',\n  reentrantCall: 'REENTRANT_CALL',\n  configUnsupported: 'CONFIG_UNSUPPORTED',\n  configReadonly: 'CONFIG_READONLY',\n  configConflict: 'CONFIG_CONFLICT',\n  configLimitExceeded: 'CONFIG_LIMIT_EXCEEDED',\n  configPathInvalid: 'CONFIG_PATH_INVALID',\n  objectPathInvalid: 'OBJECT_PATH_INVALID',\n  formatInvalid: 'FORMAT_INVALID',\n  formatValueMissing: 'FORMAT_VALUE_MISSING',\n  numberFormatInvalid: 'NUMBER_FORMAT_INVALID'\n} as const;"
                },
                {
                  "type": "paragraph",
                  "text": "全包统一使用的稳定语义码，`IUtilsErrorCode` 是其取值的联合类型。除 `UtilsError` 子类外，`base64ToBytes`/`object-path`/`config` 等模块抛出的原生 `TypeError`/`RangeError` 也会通过 `Object.defineProperty` 挂上对应的 `code`（与 `source: '@migaia/utils'`），可用同一套判断逻辑识别。"
                },
                {
                  "type": "paragraph",
                  "text": "按用途理解全部错误码："
                },
                {
                  "type": "list",
                  "items": [
                    "`INVALID_ARGUMENT`：参数类型、范围或互斥组合错误；修正调用参数。",
                    "`NON_ERROR_VALUE`：非 `Error` 异常值已被 `toError()` 转换；从 `cause` 读取原始值。",
                    "`ENV_UNSUPPORTED`：宿主缺少 `structuredClone` 等必要能力；更换环境或实现。",
                    "`ABORTED`：外部信号请求取消；停止协作式工作并传播取消原因。",
                    "`DEADLINE_EXCEEDED`：操作、尝试或总流程超时；结合 `scope`、`timeoutMs` 选择重试或降级。",
                    "`SCHEDULER_RUNAWAY`：虚拟时钟一次推进超过 10000 个任务；排查同刻递归调度。",
                    "`ERROR_IDENTITY_CONFLICT`：已有错误身份与新 `source`/`code` 冲突；保留原身份。",
                    "`CLONE_UNSUPPORTED`：值不能被 `structuredClone` 复制；先转换为可克隆数据。",
                    "`INVALID_ENCODING`：编码输入不是规范形式；按错误偏移修正输入。",
                    "`LIMITER_CLOSED`：并发限制器已关闭；停止提交或创建新实例。",
                    "`REENTRANT_CALL`：同一实例发生禁止的重入；等待当前回调结束。",
                    "`CONFIG_UNSUPPORTED`：配置含不支持的数据形状或属性；改用普通数据。",
                    "`CONFIG_READONLY`：尝试修改只读配置；从所有者侧创建新配置。",
                    "`CONFIG_CONFLICT`：配置来源的所有权、profile 或描述符冲突；统一契约再合并。",
                    "`CONFIG_LIMIT_EXCEEDED`：配置键数、深度等超过安全上限；缩小输入或调整明确限制。",
                    "`CONFIG_PATH_INVALID`：配置路径为空、危险或分段无效；改用安全路径。",
                    "`OBJECT_PATH_INVALID`：对象路径无法安全解析；修正字符串或分段元组。",
                    "`FORMAT_INVALID`：模板、占位符或值不能安全格式化；修正模板或输入。",
                    "`FORMAT_VALUE_MISSING`：严格格式化缺少占位符值；补齐字段或调整策略。",
                    "`NUMBER_FORMAT_INVALID`：`Intl.NumberFormat` 拒绝 locale、选项、货币或数值；修正格式配置。"
                  ]
                }
              ]
            },
            {
              "id": "error-模块",
              "heading": "/error 模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  UtilsError,\n  UtilsAbortError,\n  UtilsTimeoutError,\n  attachErrorIdentity,\n  toError,\n  walkErrorCauses,\n  combineErrors,\n  isUtilsError,\n  isUtilsAbortError,\n  isUtilsTimeoutError,\n  UtilsErrorCode\n} from '@migaia/utils/error';"
                },
                {
                  "type": "paragraph",
                  "text": "`attachErrorIdentity`｜5 秒上手 —— 给已有错误对象贴上稳定的 `source`/`code`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "attachErrorIdentity(new Error('boom'), { source: 'my-module', code: 'FETCH_FAILED' });"
                },
                {
                  "type": "paragraph",
                  "text": "`identity` 全部字段：`source: string`（必填）、`code: string`（必填）、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`。同名字段已存在且值不同会抛 `TypeError`。"
                },
                {
                  "type": "paragraph",
                  "text": "`toError`｜3 秒上手 —— 把任意 `throw` 值规整为 `Error`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "catch (value) { const error = toError(value); }"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ message?: string }` —— 指定后覆盖默认消息推导（原本：字符串抛出值直接作为消息，否则用固定兜底文案）。"
                },
                {
                  "type": "paragraph",
                  "text": "`walkErrorCauses`｜5 秒上手 —— 展开 `cause`/`AggregateError` 链："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "walkErrorCauses(error); // [error, ...每一层 cause / AggregateError.errors]"
                },
                {
                  "type": "paragraph",
                  "text": "第二参数选项：`{ maxDepth?: number }`（默认 32）—— 超过深度停止继续下探。"
                },
                {
                  "type": "paragraph",
                  "text": "`combineErrors`｜5 秒上手 —— 把多个错误合并成一个："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "combineErrors([err1, err2], 'batch failed'); // 0 个→undefined，1 个→原样，多个→AggregateError"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`errors: Iterable<unknown>`（必填）、`message: string`（必填，仅在合并成 `AggregateError` 时用作其 `message`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`isUtilsError` / `isUtilsAbortError` / `isUtilsTimeoutError`｜3 秒上手 —— 类型守卫，单参数 `value: unknown`，无其他选项："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (isUtilsTimeoutError(error)) console.log(error.scope, error.timeoutMs);"
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsErrorCode`｜3 秒上手 —— 稳定错误码表，用于 `switch`/比较："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === UtilsErrorCode.aborted) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值及其含义："
                },
                {
                  "type": "list",
                  "items": [
                    "`invalidArgument`（`INVALID_ARGUMENT`）—— 参数类型、范围或组合不符合 API 契约；修正调用参数后重试。",
                    "`nonErrorValue`（`NON_ERROR_VALUE`）—— `toError()` 收到了字符串等非 `Error` 异常值，并已将它转换为可追踪的 `Error`。",
                    "`envUnsupported`（`ENV_UNSUPPORTED`）—— 当前宿主缺少所需原生能力，例如 `structuredClone`；改用支持该能力的运行环境或其他实现。",
                    "`aborted`（`ABORTED`）—— 外部 `AbortSignal` 已请求取消协作式操作；停止后续工作并向上传递取消原因。",
                    "`deadlineExceeded`（`DEADLINE_EXCEEDED`）—— 操作、单次尝试或整个重试流程超过约定时限；检查 `scope` 与 `timeoutMs` 决定重试或降级。",
                    "`schedulerRunaway`（`SCHEDULER_RUNAWAY`）—— 手动调度器一次推进执行超过 10000 个任务，通常表示回调在同一时刻递归调度；修复循环，而不是提高阈值。",
                    "`errorIdentityConflict`（`ERROR_IDENTITY_CONFLICT`）—— 错误已有的 `source` 或 `code` 与准备附加的身份冲突；保留原错误身份，不要重复改写。",
                    "`cloneUnsupported`（`CLONE_UNSUPPORTED`）—— `structuredClone` 无法复制该值，例如包含不可克隆成员；先转换为可克隆数据再创建不可变快照。",
                    "`invalidEncoding`（`INVALID_ENCODING`）—— Base64 等输入不符合本包要求的规范编码；根据错误偏移修正输入，不会宽松解码。",
                    "`limiterClosed`（`LIMITER_CLOSED`）—— 并发限制器已经关闭，不能再接收或继续排队任务；创建新限制器或停止提交任务。",
                    "`reentrantCall`（`REENTRANT_CALL`）—— 回调尚未结束时又进入禁止重入的函数或 collector；等待当前调用结束，避免从回调内部再次调用同一实例。",
                    "`configUnsupported`（`CONFIG_UNSUPPORTED`）—— 配置包含不支持的形状、访问器、Symbol key 或函数位置；改成契约允许的普通数据。",
                    "`configReadonly`（`CONFIG_READONLY`）—— 尝试修改只读配置视图；从可写所有者生成新配置，不要修改只读快照。",
                    "`configConflict`（`CONFIG_CONFLICT`）—— 合并配置时所有权、profile 或属性描述符互不兼容；统一来源契约后再合并。",
                    "`configLimitExceeded`（`CONFIG_LIMIT_EXCEEDED`）—— 配置图的键数、深度或其他安全上限被超过；缩小输入或显式调整允许的限制。",
                    "`configPathInvalid`（`CONFIG_PATH_INVALID`）—— 配置路径为空、过长、含危险键或无效分段；使用经过解析且安全的配置路径。",
                    "`objectPathInvalid`（`OBJECT_PATH_INVALID`）—— 对象路径字符串或分段元组无法安全定位属性；修正路径语法并移除危险分段。",
                    "`formatInvalid`（`FORMAT_INVALID`）—— 模板语法、占位符边界或待格式化值不合法；修正模板或值后重新格式化。",
                    "`formatValueMissing`（`FORMAT_VALUE_MISSING`）—— 严格格式化模式找不到指定占位符路径；补齐数据字段或改用允许缺失值的策略。",
                    "`numberFormatInvalid`（`NUMBER_FORMAT_INVALID`）—— `Intl.NumberFormat` 拒绝 locale、格式选项、货币代码或数值；修正国际化配置或输入值。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "`UtilsAbortError` / `UtilsTimeoutError`｜3 秒上手 —— 一般由包内抛出，也可直接构造："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw new UtilsAbortError('user cancelled'); // 构造参数：reason?: unknown（存入 cause）\nthrow new UtilsTimeoutError('operation', 500); // 构造参数：scope: 'operation' | 'attempt' | 'total', timeoutMs: number（两者均必填）"
                }
              ]
            }
          ],
          "usageScore": 18,
          "configuration": [
            {
              "name": "placeholder",
              "type": "IFormatPlaceholder",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            },
            {
              "name": "missing",
              "type": "'preserve' | 'empty' | 'throw'",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            },
            {
              "name": "nullish",
              "type": "'empty' | 'stringify'",
              "optional": true,
              "description": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。",
              "descriptionEn": "",
              "descriptionZh": "`format(source, values, options?)` 使用线性扫描替换 `{path}`，嵌套路径只读取 own property；缺失值默认保留占位符。`options.placeholder` 可配置 `{ open, close }`，`missing` 可选 `preserve | empty | throw`，`nullish` 可选 `empty | stringify`。双写边界字符用于输出字面量边界。"
            }
          ],
          "fragment": "utils-string--format"
        },
        {
          "name": "IFormatOptions",
          "kind": "type",
          "signature": "export type IFormatOptions = { readonly placeholder?: IFormatPlaceholder; readonly missing?: 'preserve' | 'empty' | 'throw'; readonly nullish?: 'empty' | 'stringify'; };",
          "declarationLine": 7,
          "source": "packages/utils/dist/string.d.ts",
          "purpose": "String-template policies; defaults preserve missing placeholders and blank nullish values.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IFormatOptions.",
          "advanced": "No additional advanced behavior is declared for IFormatOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IFormatOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IFormatOptions.",
          "examples": [],
          "members": [],
          "exportPath": "./string",
          "overloadSignatures": [
            "export type IFormatOptions = { readonly placeholder?: IFormatPlaceholder; readonly missing?: 'preserve' | 'empty' | 'throw'; readonly nullish?: 'empty' | 'stringify'; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IFormatOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IFormatOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "String-template policies; defaults preserve missing placeholders and blank nullish values."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IFormatOptions from @migaia/utils/string, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IFormatOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IFormatOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IFormatOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IFormatOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/string.d.ts:IFormatOptions:export type IFormatOptions = { readonly placeholder?: IFormatPlaceholder; readonly missing?: 'preserve' | 'empty' | 'throw'; readonly nullish?: 'empty' | 'stringify'; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-string--IFormatOptions"
        },
        {
          "name": "IFormatPlaceholder",
          "kind": "type",
          "signature": "export type IFormatPlaceholder = { readonly open: string; readonly close: string; };",
          "declarationLine": 2,
          "source": "packages/utils/dist/string.d.ts",
          "purpose": "Placeholder delimiters used by the linear template scanner.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IFormatPlaceholder.",
          "advanced": "No additional advanced behavior is declared for IFormatPlaceholder.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IFormatPlaceholder."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IFormatPlaceholder.",
          "examples": [],
          "members": [],
          "exportPath": "./string",
          "overloadSignatures": [
            "export type IFormatPlaceholder = { readonly open: string; readonly close: string; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IFormatPlaceholder when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IFormatPlaceholder does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Placeholder delimiters used by the linear template scanner."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IFormatPlaceholder from @migaia/utils/string, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IFormatPlaceholder when its type contract and declared inputs match the result you need. Choose a different public symbol when IFormatPlaceholder does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IFormatPlaceholder."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IFormatPlaceholder."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/string.d.ts:IFormatPlaceholder:export type IFormatPlaceholder = { readonly open: string; readonly close: string; };",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-string--IFormatPlaceholder"
        }
      ],
      "aliases": []
    },
    {
      "id": "utils:typing",
      "library": "utils",
      "module": "typing",
      "exportPath": "./typing",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "IDiscriminatedByField",
          "kind": "type",
          "signature": "export type IDiscriminatedByField<F extends PropertyKey, T extends Record<F, PropertyKey>> = { [K in T[F]]: Extract<T, Record<F, K>>; };",
          "declarationLine": 6,
          "source": "packages/utils/dist/typing.d.ts",
          "purpose": "Groups a discriminated union by one top-level property-key field.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDiscriminatedByField.",
          "advanced": "No additional advanced behavior is declared for IDiscriminatedByField.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDiscriminatedByField."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDiscriminatedByField.",
          "examples": [
            "/** 按一个顶层、值为 PropertyKey 的判别字段对联合类型分组。 */\ntype IDiscriminatedByField<F extends PropertyKey, T extends Record<F, PropertyKey>> = {\n  [K in T[F]]: Extract<T, Record<F, K>>;\n};",
            "type IEvent =\n  | {\n      readonly type: 'created';\n      readonly meta: { readonly category: 'write' };\n      readonly payload: { readonly userId: string };\n    }\n  | {\n      readonly type: 'deleted';\n      readonly meta: { readonly category: 'write' };\n      readonly payload: { readonly reason: string };\n    }\n  | {\n      readonly type: 'read';\n      readonly meta: { readonly category: 'read' };\n      readonly payload: { readonly cache: boolean };\n    };\n\ntype IByType = IDiscriminatedByField<'type', IEvent>;\n// { created: Extract<IEvent, { type: 'created' }>; deleted: ...; read: ... }\n\ntype IByCategory = IDiscriminatedByPath<IEvent, 'meta.category'>;\n// { write: 'created' | 'deleted' 两个分支的联合; read: 'read' 分支 }\n\ntype IByTupleCategory = IDiscriminatedByPath<IEvent, readonly ['meta', 'category']>;\n// 与 IByCategory 完全等价——字符串路径和元组路径是同一套解析逻辑的两种写法",
            "// @ts-expect-error 路径在联合的某些/全部分支上不存在，判别提取前就被拒绝\ntype IInvalid1 = IDiscriminatedByPath<IEvent, 'meta.unknown'>;\n\ntype INotPropertyKey = { readonly discriminator: { readonly nested: true } };\n// @ts-expect-error 判别字段的值是对象，不满足 PropertyKey 约束，无法作为映射类型的 key\ntype IInvalid2 = IDiscriminatedByField<'discriminator', INotPropertyKey>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';",
            "type IEvent =\n  | { readonly type: 'created'; readonly payload: { readonly userId: string } }\n  | { readonly type: 'deleted'; readonly payload: { readonly reason: string } };\n\ntype IByType = IDiscriminatedByField<'type', IEvent>;\n// { created: Extract<IEvent, { type: 'created' }>; deleted: Extract<IEvent, { type: 'deleted' }> }"
          ],
          "members": [],
          "exportPath": "./typing",
          "overloadSignatures": [
            "export type IDiscriminatedByField<F extends PropertyKey, T extends Record<F, PropertyKey>> = { [K in T[F]]: Extract<T, Record<F, K>>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDiscriminatedByField when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDiscriminatedByField does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Groups a discriminated union by one top-level property-key field."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDiscriminatedByField from @migaia/utils/typing, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDiscriminatedByField when its type contract and declared inputs match the result you need. Choose a different public symbol when IDiscriminatedByField does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDiscriminatedByField."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDiscriminatedByField."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/typing.d.ts:IDiscriminatedByField:export type IDiscriminatedByField<F extends PropertyKey, T extends Record<F, PropertyKey>> = { [K in T[F]]: Extract<T, Record<F, K>>; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "utils-typing--IDiscriminatedByField"
        },
        {
          "name": "IDiscriminatedByPath",
          "kind": "type",
          "signature": "export type IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = { [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>; };",
          "declarationLine": 12,
          "source": "packages/utils/dist/typing.d.ts",
          "purpose": "Groups a discriminated union by a typed string or tuple object path.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDiscriminatedByPath.",
          "advanced": "No additional advanced behavior is declared for IDiscriminatedByPath.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDiscriminatedByPath."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDiscriminatedByPath.",
          "examples": [
            "/** 按一个 string 或 tuple 对象路径对联合类型分组，路径解析规则与 /object 模块的 IObjectPath 完全一致。 */\ntype IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = {\n  [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>;\n};",
            "type IEvent =\n  | {\n      readonly type: 'created';\n      readonly meta: { readonly category: 'write' };\n      readonly payload: { readonly userId: string };\n    }\n  | {\n      readonly type: 'deleted';\n      readonly meta: { readonly category: 'write' };\n      readonly payload: { readonly reason: string };\n    }\n  | {\n      readonly type: 'read';\n      readonly meta: { readonly category: 'read' };\n      readonly payload: { readonly cache: boolean };\n    };\n\ntype IByType = IDiscriminatedByField<'type', IEvent>;\n// { created: Extract<IEvent, { type: 'created' }>; deleted: ...; read: ... }\n\ntype IByCategory = IDiscriminatedByPath<IEvent, 'meta.category'>;\n// { write: 'created' | 'deleted' 两个分支的联合; read: 'read' 分支 }\n\ntype IByTupleCategory = IDiscriminatedByPath<IEvent, readonly ['meta', 'category']>;\n// 与 IByCategory 完全等价——字符串路径和元组路径是同一套解析逻辑的两种写法",
            "// @ts-expect-error 路径在联合的某些/全部分支上不存在，判别提取前就被拒绝\ntype IInvalid1 = IDiscriminatedByPath<IEvent, 'meta.unknown'>;\n\ntype INotPropertyKey = { readonly discriminator: { readonly nested: true } };\n// @ts-expect-error 判别字段的值是对象，不满足 PropertyKey 约束，无法作为映射类型的 key\ntype IInvalid2 = IDiscriminatedByField<'discriminator', INotPropertyKey>;",
            "import type {\n  IDiscriminatedByField,\n  IDiscriminatedByPath,\n  IObjectPath,\n  IObjectPathInput,\n  IObjectPathSegment,\n  IObjectPathTuple,\n  IObjectPathTupleFor,\n  IObjectPathValue,\n  IObjectPathWriteValue,\n  IProbePropertyResult,\n  IAbortSignal,\n  IDeferred\n} from '@migaia/utils/typing';",
            "type IEvent =\n  | { readonly meta: { readonly category: 'write' }; readonly payload: { readonly userId: string } }\n  | { readonly meta: { readonly category: 'read' }; readonly payload: { readonly cache: boolean } };\n\ntype IByCategory = IDiscriminatedByPath<IEvent, 'meta.category'>;\n// 等价于 IDiscriminatedByPath<IEvent, readonly ['meta', 'category']>"
          ],
          "members": [],
          "exportPath": "./typing",
          "overloadSignatures": [
            "export type IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = { [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDiscriminatedByPath when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDiscriminatedByPath does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Groups a discriminated union by a typed string or tuple object path."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDiscriminatedByPath from @migaia/utils/typing, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDiscriminatedByPath when its type contract and declared inputs match the result you need. Choose a different public symbol when IDiscriminatedByPath does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDiscriminatedByPath."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDiscriminatedByPath."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/typing.d.ts:IDiscriminatedByPath:export type IDiscriminatedByPath<T, P extends IObjectPathInput<T>> = { [K in Extract<IObjectPathValue<T, P>, PropertyKey>]: IExtractDiscriminatedByPath<T, P, K>; };",
          "guidance": [],
          "usageScore": 36,
          "configuration": [],
          "fragment": "utils-typing--IDiscriminatedByPath"
        }
      ],
      "aliases": [
        {
          "identity": "packages/utils/dist/promise.d.ts:IAbortSignal:export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "name": "IAbortSignal",
          "signature": "export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IAbortSignal"
        },
        {
          "identity": "packages/utils/dist/promise.d.ts:IDeferred:export type IDeferred<T> = { readonly promise: Promise<T>; readonly resolve: (value: T | PromiseLike<T>) => void; readonly reject: (reason?: unknown) => void; };",
          "name": "IDeferred",
          "signature": "export type IDeferred<T> = { readonly promise: Promise<T>; readonly resolve: (value: T | PromiseLike<T>) => void; readonly reject: (reason?: unknown) => void; };",
          "source": "packages/utils/dist/promise.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./promise",
          "ownerModule": "promise",
          "ownerFragment": "utils-promise--IDeferred"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPath:export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "name": "IObjectPath",
          "signature": "export type IObjectPath<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly unknown[] ? `[${number}]` | `[${number}].${IObjectPath<IArrayValue<T>, IShift<D>>}` : T extends object ? { [K in IStringKey<T>]: K | (IObjectPath<T[K], IShift<D>> extends infer P extends string ? IJoinPath<K, P> : never); }[IStringKey<T>] : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPath"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathInput:export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "name": "IObjectPathInput",
          "signature": "export type IObjectPathInput<T> = IObjectPath<T> | IObjectPathTupleFor<T>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathInput"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathSegment:export type IObjectPathSegment = string | number | symbol;",
          "name": "IObjectPathSegment",
          "signature": "export type IObjectPathSegment = string | number | symbol;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathSegment"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTuple:export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "name": "IObjectPathTuple",
          "signature": "export type IObjectPathTuple = readonly IObjectPathSegment[];",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathTuple"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathTupleFor:export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "name": "IObjectPathTupleFor",
          "signature": "export type IObjectPathTupleFor<T, D extends readonly unknown[] = IPathDepth> = D extends readonly [] ? never : T extends readonly (infer V)[] ? readonly [number] | IPrefixTuple<number, IObjectPathTupleFor<NonNullable<V>, IShift<D>>> : T extends object ? { [K in keyof T]: readonly [K] | IPrefixTuple<K, IObjectPathTupleFor<NonNullable<T[K]>, IShift<D>>>; }[keyof T] : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathTupleFor"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathValue:export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "name": "IObjectPathValue",
          "signature": "export type IObjectPathValue<T, P extends IObjectPath<T> | IObjectPathTuple> = P extends string ? IStringPathValue<T, P> : P extends IObjectPathTuple ? ITuplePathValue<T, P> : never;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathValue"
        },
        {
          "identity": "packages/utils/dist/object-path.d.ts:IObjectPathWriteValue:export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "name": "IObjectPathWriteValue",
          "signature": "export type IObjectPathWriteValue<T, P extends IObjectPath<T> | IObjectPathTuple> = IObjectPathWritableLeaf<IObjectPathValue<T, P>>;",
          "source": "packages/utils/dist/object-path.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object-path",
          "ownerModule": "object-path",
          "ownerFragment": "utils-object-path--IObjectPathWriteValue"
        },
        {
          "identity": "packages/utils/dist/object.d.ts:IProbePropertyResult:export type IProbePropertyResult<T> = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: T; } | { readonly kind: 'failed'; readonly error: unknown; };",
          "name": "IProbePropertyResult",
          "signature": "export type IProbePropertyResult<T> = { readonly kind: 'missing'; } | { readonly kind: 'value'; readonly value: T; } | { readonly kind: 'failed'; readonly error: unknown; };",
          "source": "packages/utils/dist/object.d.ts",
          "exportPath": "./typing",
          "ownerExportPath": "./object",
          "ownerModule": "object",
          "ownerFragment": "utils-object--IProbePropertyResult"
        }
      ]
    },
    {
      "id": "utils:value",
      "library": "utils",
      "module": "value",
      "exportPath": "./value",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/utils/getting-started",
      "architecturePath": "/en/architecture/utils",
      "symbols": [
        {
          "name": "IPrimitive",
          "kind": "type",
          "signature": "export type IPrimitive = undefined | null | string | number | bigint | boolean | symbol;",
          "declarationLine": 2,
          "source": "packages/utils/dist/value.d.ts",
          "purpose": "JavaScript primitive values; functions remain callable objects and are intentionally excluded.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPrimitive.",
          "advanced": "No additional advanced behavior is declared for IPrimitive.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPrimitive."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPrimitive.",
          "examples": [],
          "members": [],
          "exportPath": "./value",
          "overloadSignatures": [
            "export type IPrimitive = undefined | null | string | number | bigint | boolean | symbol;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPrimitive when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPrimitive does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "JavaScript primitive values; functions remain callable objects and are intentionally excluded."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPrimitive from @migaia/utils/value, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPrimitive when its type contract and declared inputs match the result you need. Choose a different public symbol when IPrimitive does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPrimitive."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPrimitive."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/value.d.ts:IPrimitive:export type IPrimitive = undefined | null | string | number | bigint | boolean | symbol;",
          "guidance": [],
          "usageScore": 0,
          "configuration": [],
          "fragment": "utils-value--IPrimitive"
        },
        {
          "name": "isBlankString",
          "kind": "const",
          "signature": "export declare const isBlankString: (value: unknown) => value is string;",
          "declarationLine": 6,
          "source": "packages/utils/dist/value.d.ts",
          "purpose": "Detects empty or whitespace-only strings without coercing non-string values.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isBlankString.",
          "advanced": "No additional advanced behavior is declared for isBlankString.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for isBlankString."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isBlankString.",
          "examples": [],
          "members": [],
          "exportPath": "./value",
          "overloadSignatures": [
            "export declare const isBlankString: (value: unknown) => value is string;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use isBlankString when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isBlankString does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Detects empty or whitespace-only strings without coercing non-string values."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isBlankString from @migaia/utils/value, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isBlankString when its const contract and declared inputs match the result you need. Choose a different public symbol when isBlankString does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isBlankString."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isBlankString."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/value.d.ts:isBlankString:export declare const isBlankString: (value: unknown) => value is string;",
          "guidance": [],
          "usageScore": 4,
          "configuration": [],
          "fragment": "utils-value--isBlankString"
        },
        {
          "name": "isEmptyValue",
          "kind": "const",
          "signature": "export declare const isEmptyValue: (value: unknown) => value is null | undefined | string | number;",
          "declarationLine": 11,
          "source": "packages/utils/dist/value.d.ts",
          "purpose": "Detects common business-form emptiness while preserving `0`, `0n`, and `false` as real values. Arrays and objects are never inferred empty because collection policy belongs to their owners.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isEmptyValue.",
          "advanced": "No additional advanced behavior is declared for isEmptyValue.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for isEmptyValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isEmptyValue.",
          "examples": [
            "import { format, formatCurrency, isEmptyValue, isPrimitive } from '@migaia/utils';\n\nisEmptyValue('  '); // true；0、false、0n 均为 false\nisPrimitive(Symbol('id')); // true\nformat('库存 {stock}，金额 {money}', { stock: 0, money: '¥12.00' });\nformatCurrency(1234.5, 'CNY', {\n  locales: 'zh',\n  format: { minimumFractionDigits: 2, maximumFractionDigits: 2 }\n});"
          ],
          "members": [],
          "exportPath": "./value",
          "overloadSignatures": [
            "export declare const isEmptyValue: (value: unknown) => value is null | undefined | string | number;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use isEmptyValue when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isEmptyValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Detects common business-form emptiness while preserving `0`, `0n`, and `false` as real values. Arrays and objects are never inferred empty because collection policy belongs to their owners."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isEmptyValue from @migaia/utils/value, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isEmptyValue when its const contract and declared inputs match the result you need. Choose a different public symbol when isEmptyValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isEmptyValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isEmptyValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/value.d.ts:isEmptyValue:export declare const isEmptyValue: (value: unknown) => value is null | undefined | string | number;",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-value--isEmptyValue"
        },
        {
          "name": "isNullish",
          "kind": "const",
          "signature": "export declare const isNullish: (value: unknown) => value is null | undefined;",
          "declarationLine": 4,
          "source": "packages/utils/dist/value.d.ts",
          "purpose": "Narrows null and undefined without treating other falsy business values as absent.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isNullish.",
          "advanced": "No additional advanced behavior is declared for isNullish.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for isNullish."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isNullish.",
          "examples": [],
          "members": [],
          "exportPath": "./value",
          "overloadSignatures": [
            "export declare const isNullish: (value: unknown) => value is null | undefined;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use isNullish when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isNullish does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Narrows null and undefined without treating other falsy business values as absent."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isNullish from @migaia/utils/value, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isNullish when its const contract and declared inputs match the result you need. Choose a different public symbol when isNullish does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isNullish."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isNullish."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/value.d.ts:isNullish:export declare const isNullish: (value: unknown) => value is null | undefined;",
          "guidance": [],
          "usageScore": 4,
          "configuration": [],
          "fragment": "utils-value--isNullish"
        },
        {
          "name": "isPrimitive",
          "kind": "const",
          "signature": "export declare const isPrimitive: (value: unknown) => value is IPrimitive;",
          "declarationLine": 13,
          "source": "packages/utils/dist/value.d.ts",
          "purpose": "Narrows all JavaScript primitives without allocating a runtime type tag.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isPrimitive.",
          "advanced": "No additional advanced behavior is declared for isPrimitive.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for isPrimitive."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for isPrimitive.",
          "examples": [
            "import { format, formatCurrency, isEmptyValue, isPrimitive } from '@migaia/utils';\n\nisEmptyValue('  '); // true；0、false、0n 均为 false\nisPrimitive(Symbol('id')); // true\nformat('库存 {stock}，金额 {money}', { stock: 0, money: '¥12.00' });\nformatCurrency(1234.5, 'CNY', {\n  locales: 'zh',\n  format: { minimumFractionDigits: 2, maximumFractionDigits: 2 }\n});"
          ],
          "members": [],
          "exportPath": "./value",
          "overloadSignatures": [
            "export declare const isPrimitive: (value: unknown) => value is IPrimitive;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use isPrimitive when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when isPrimitive does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Narrows all JavaScript primitives without allocating a runtime type tag."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import isPrimitive from @migaia/utils/value, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use isPrimitive when its const contract and declared inputs match the result you need. Choose a different public symbol when isPrimitive does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for isPrimitive."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for isPrimitive."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/utils/dist/value.d.ts:isPrimitive:export declare const isPrimitive: (value: unknown) => value is IPrimitive;",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "utils-value--isPrimitive"
        }
      ],
      "aliases": []
    }
  ]
}
