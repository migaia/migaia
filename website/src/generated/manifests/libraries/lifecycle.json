{
  "version": 1,
  "library": {
    "slug": "lifecycle",
    "description": "Runtime-neutral lifecycle primitives: scopes, generations, quiescence tracking and dispose transactions.",
    "version": "0.0.2",
    "exports": [
      ".",
      "./abort",
      "./disposal",
      "./errors",
      "./generation",
      "./quiescence",
      "./scheduler",
      "./scope"
    ],
    "documentation": {
      "readme": {
        "sections": [
          {
            "id": "migaia-lifecycle",
            "heading": "@migaia/lifecycle",
            "blocks": [
              {
                "type": "paragraph",
                "text": "运行时中立的生命周期原语：作用域所有权、代数（generation）、静默追踪（quiescence）与释放事务。零领域知识——公开 API 里不出现 store / plugin / capability / service / rpc / storage 等词汇，只知道「资源」和「怎么释放资源」。"
              },
              {
                "type": "paragraph",
                "text": "设计文档：`docs/lifecycle/lifecycle-extraction.sdd.md`（SDD 1/3）；错误码契约：`docs/contracts/error-codes.md`。"
              }
            ]
          },
          {
            "id": "适用与不适用场景",
            "heading": "适用与不适用场景",
            "blocks": [
              {
                "type": "paragraph",
                "text": "适用：需要明确资源所有权、关闭边界、代数失效（用新结果取代旧结果）、静默/排空、或多资源释放事务（含超时降级与错误聚合）的 runtime-neutral 库或运行时宿主。"
              },
              {
                "type": "paragraph",
                "text": "不适用：不要把它当作事件总线、当前值存储/订阅、能力依赖图、拓扑排序/环检测（那些属于 `@migaia/capability/graph`）、业务队列策略，或跨进程传输。`ready`/`blocked` 之类需要依赖关系才能回答的问题，本包刻意不回答。"
              }
            ]
          },
          {
            "id": "场景怎么选",
            "heading": "场景怎么选",
            "blocks": [
              {
                "type": "table",
                "headers": ["业务问题", "首选 API", "典型场景"],
                "rows": [
                  [
                    "一组资源随 owner 统一关闭",
                    "`createLifecycleScope`",
                    "页面/插件/服务实例拥有 socket、timer、subscription、DB handle"
                  ],
                  [
                    "资源必须在当前 stack 释放",
                    "`createSyncLifecycleScope`",
                    "DOM listener、observer、纯同步 native handle"
                  ],
                  [
                    "同步 `dispose()` 启动异步 disposer，另提供 awaitable completion",
                    "`createSyncStartedDisposalLedger`",
                    "Store/registry 的 sync + async 双销毁 API"
                  ],
                  [
                    "只接受最新一次异步请求结果",
                    "`createGenerationController`",
                    "搜索联想、路由加载、刷新覆盖、重连"
                  ],
                  [
                    "管理 start/restart 的 idle/loading/loaded/failed",
                    "`createLifecycleUnit`",
                    "服务初始化、懒加载模块、配置加载"
                  ],
                  [
                    "等所有在途 Promise 真正排空",
                    "`createPendingTracker`",
                    "flush、close、批处理 drain、SSR request completion"
                  ],
                  [
                    "按 key/object 统计占用并封存",
                    "`create*QuiescenceTracker` / `create*LeaseRegistry`",
                    "session、channel、cache entry、共享资源引用"
                  ],
                  [
                    "构造成功才转交资源，失败自动回滚",
                    "`createProvisionalScope`",
                    "插件安装、模块装配、连接组初始化"
                  ],
                  [
                    "多个变更必须 FIFO 串行",
                    "`createMutationQueue`",
                    "config CRUD、拓扑变更、schema/register mutation"
                  ],
                  [
                    "多资源 graceful→force、deadline、错误聚合",
                    "`createDisposeTransaction`",
                    "服务停机、连接池关闭、插件卸载"
                  ],
                  [
                    "真实平台取消信号",
                    "`createAbortController`",
                    "fetch、stream、timer、Worker/RPC operation"
                  ],
                  [
                    "可重复、无真实等待的时间测试",
                    "`createManualScheduler`",
                    "timeout、deadline、重试和队列 SLA 单测"
                  ]
                ]
              },
              {
                "type": "paragraph",
                "text": "选择原则：Scope 管“谁拥有资源”；Generation 管“哪个异步结果仍有效”；Pending/Lease 管“还有多少工作”；MutationQueue 管“谁先修改”；DisposeTransaction/Ledger 管“如何结束”。这些轴可以组合，但不要让两个原语同时成为同一状态的 owner。"
              }
            ]
          },
          {
            "id": "安装",
            "heading": "安装",
            "blocks": [
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm add @migaia/lifecycle"
              },
              {
                "type": "paragraph",
                "text": "唯一依赖是同层的 `@migaia/utils`（时间/错误原语）；不引入 DOM、Node 或任何领域包类型。"
              }
            ]
          },
          {
            "id": "目录",
            "heading": "目录",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "作用域与释放：`createLifecycleScope`、`createSyncLifecycleScope`、`executeReleaseDescriptor`、`createDisposeTransaction`、`createSyncStartedDisposalLedger`",
                  "单元与代数：`createLifecycleUnit`、`createGenerationController`",
                  "静默追踪与租约：`createQuiescenceTracker`、`createStringQuiescenceTracker`、`createObjectLeaseRegistry`、`createStringLeaseRegistry`、`createPendingTracker`",
                  "事务性所有权：`createProvisionalScope`",
                  "变更队列：`createMutationQueue`",
                  "调度器、中止与有界等待：`systemScheduler`、`createManualScheduler`、`snapshotScheduler`、`validateSchedulerDelay`、`validateSchedulerTime`、`createAbortController`、`boundedWait`、`createTerminalController`",
                  "错误基础设施：`LIFECYCLE_SOURCE`、`createLifecycleError`、`createLifecycleRangeError`、`tagLifecycleError`、`containAsyncRejection`、`probeThenable`、`assimilateCapturedThen`、`createErrorCollector`、`LifecycleErrorCode`、`LifecycleErrorText`",
                  "状态常量：`LifecycleState`、`LifecycleUnitState`、`LifecycleErrorPolicy`、`ThenableProbeKind`、`DisposeTransactionKind`",
                  "按需导入与 tree-shaking",
                  "高阶组合示例",
                  "构建门禁"
                ]
              },
              {
                "type": "paragraph",
                "text": "完整签名、边界行为与错误码，见 USEGUIDE.md。"
              }
            ]
          },
          {
            "id": "作用域与释放模块",
            "heading": "作用域与释放模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  type IDisposerContext,\n  type IReleaseDescriptor,\n  type IReleaseContext\n} from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "两条正交轴：容器存活轴（`open → closing → terminal`，由 `close()`/`dispose()` 驱动）与资源释放本身。`close()` 永远同步、幂等、不调用用户代码；`dispose()` 永远异步（唯一形态，不提供同步版本）。"
              },
              {
                "type": "paragraph",
                "text": "`createLifecycleScope`｜10 秒上手 —— 通用异步所有权容器："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
              },
              {
                "type": "paragraph",
                "text": "全部选项（`ILifecycleScopeOptions`）："
              },
              {
                "type": "list",
                "items": [
                  "`errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError'` —— 默认 `'throw'`",
                  "`report?: (error: unknown) => void` —— `report`/`firstError` 策略下的诊断通道；异常被自身吞掉（最后一道错误边界）",
                  "`deadlineAt?: number` —— 本次 `dispose()` 内所有资源共享的绝对释放截止时间",
                  "`scheduler?: ILifecycleScheduler` —— 默认 `systemScheduler`；决定 `deadlineAt`/`gracefulTimeoutMs` 的时间域"
                ]
              },
              {
                "type": "paragraph",
                "text": "`own(resource, descriptor)` 返回的方法：`own`、`release(resource)`（反注册但不释放，用于调用方已自行释放的场景）、`close()`、`dispose()`。`dispose()` 并发多次调用复用同一个 Promise；disposer 内部重入 `own()`/`dispose()` 分别抛 `SCOPE_REENTRANT_OWN`/`SCOPE_REENTRANT_DISPOSE`。环境支持时自动挂载 `[Symbol.asyncDispose]`。"
              },
              {
                "type": "paragraph",
                "text": "`createSyncLifecycleScope`｜10 秒上手 —— 纯同步容器，`dispose()` 不返回 Promise："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回，不可能有异步资源"
              },
              {
                "type": "paragraph",
                "text": "全部选项（`ISyncLifecycleScopeOptions`）：`errorPolicy?`（同上，默认 `'throw'`）、`report?`（同上）。约束：每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`；某个 callback 若返回 thenable，同样在释放时抛 `SCOPE_SYNC_VIOLATION`。不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例。环境支持时挂载 `[Symbol.dispose]`。"
              },
              {
                "type": "paragraph",
                "text": "`IReleaseDescriptor`｜10 秒上手 —— 领域表达释放意图的唯一词汇（类型，非函数，随 `own()` 一起使用）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "scope.own(handle, {\n  syncSafe: false,\n  order: 10, // 大的先释放；只在 order 模式事务里生效\n  graceful: (ctx) => flush(), // 优雅释放；超时只放弃等待，仍会跑 force\n  gracefulTimeoutMs: 200,\n  force: (ctx) => handle.close(), // 必填，必须无条件完成\n  gcFallback: true // 显式释放时自动 unregister 的 FinalizationRegistry 兜底\n})"
              },
              {
                "type": "paragraph",
                "text": "全部字段：`force: (context: IReleaseContext) => void | PromiseLike<void>`（必填）；`syncSafe?: boolean`；`order?: number`（缺省按 `0`）；`graceful?: (context) => void | PromiseLike<void>`；`gracefulTimeoutMs?: number`；`gcFallback?: boolean`；`custom?: (context) => void | PromiseLike<void>`（逃生舱，设置后完全接管释放，`graceful`/`force` 被忽略）。回调收到的 `IReleaseContext`：`signal`（容器进入 closing 时中止）、`deadlineAt`、`scheduler?"
              },
              {
                "type": "paragraph",
                "text": "`、`report(error)`，以及由 `LifecycleScope` 提供的 `disposer.join()`。`disposer.join()` 是当前 owner-disposal 的显式 self-join guard：无论同步调用或 await/microtask/timer/nested helper 后调用，都会立即抛 `SCOPE_REENTRANT_DISPOSE`；它不返回 Promise。通用 `createDisposeTransaction()` 没有 scope owner 时不提供 `disposer`。"
              },
              {
                "type": "paragraph",
                "text": "`executeReleaseDescriptor`｜5 秒上手 —— 独立运行单个 descriptor 的完整降级链（`custom` → 否则 `graceful` 带超时 → `force`），不经过完整事务，常用于测试或自定义编排："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const errors = await executeReleaseDescriptor(descriptor, {\n  signal: controller.signal,\n  deadlineAt: undefined,\n  report: (e) => console.error(e)\n})"
              },
              {
                "type": "paragraph",
                "text": "参数：`descriptor: IReleaseDescriptor`（必填）、`context: IReleaseContext`（必填，无可选项——直接构造完整 context 对象）。返回全部失败错误的数组（成功为空数组），从不抛出。"
              },
              {
                "type": "paragraph",
                "text": "`createDisposeTransaction`｜10 秒上手 —— 编排多个资源的释放，`order` 弱排序或 `plan` 强序列二选一："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const transaction = createDisposeTransaction(\n  { kind: 'plan' },\n  { errorPolicy: 'collect', signal: controller.signal }\n)\nconst failures = await transaction.run([\n  { source: 'db', descriptor: dbDescriptor },\n  { source: 'socket', descriptor: socketDescriptor }\n])"
              },
              {
                "type": "paragraph",
                "text": "第一参数 `mode`（必填）：`{ kind: 'order' }`（按 `descriptor.order` 降序分组，稳定排序保留同序调用方给定的相对顺序；只读 order 模式的 items）或 `{ kind: 'plan' }`（严格按 `items` 给定顺序执行，从不读取 `order`）——同一事务不能混用。"
              },
              {
                "type": "paragraph",
                "text": "全部选项（`IDisposeTransactionOptions`）："
              },
              {
                "type": "list",
                "items": [
                  "`errorPolicy?` —— 默认 `'throw'`",
                  "`report?: (error: unknown) => void`",
                  "`deadlineAt?: number` —— 本次 `run()` 内所有 item 共享的绝对截止时间",
                  "`scheduler?: ILifecycleScheduler` —— 默认 `systemScheduler`",
                  "`signal?: IAbortSignal` —— 转发进每个 item 的 `context.signal`；`run()` 开始时自动镜像中止状态",
                  "`pending?: { drain(): Promise<void> }` —— 每个 item 释放完后等待其触发的在途工作排空（通常传 `createPendingTracker()`）"
                ]
              },
              {
                "type": "paragraph",
                "text": "`createSyncStartedDisposalLedger`｜10 秒上手 —— 为同步 `dispose()` 启动的异步 disposer 提供唯一 pending/error/completion 账本："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\n\nconst ledger = createSyncStartedDisposalLedger()\nledger.start('socket', () => socket.close()) // callback 在当前 stack 立即执行\nledger.start('cache', () => cache.flush())\n\nconst outcome = ledger.seal() // open → closing；之后不再接受 start()\nif (outcome.synchronousErrors.length > 0) {\n  // 领域层决定 throw、report、firstError 或 AggregateError 投影。\n}\nconst allErrors = await outcome.completion // 永远 resolve raw error ledger，不因 item failure reject\nawait ledger.whenTerminal()"
              },
              {
                "type": "paragraph",
                "text": "Ledger 不决定 disposer 顺序、不拥有领域资源、也不制造领域错误。`start()` 捕获 callback 同步失败与 hostile then getter；thenable 只读一次并保持 receiver。`seal()`/`completion` identity 稳定；pending 全部 settle 后才进入 `terminal`。永不 settle 的合法 thenable 会让 ledger 保持 `closing`，本原语不自造 timeout 或 cancellation。"
              }
            ]
          },
          {
            "id": "单元与代数模块",
            "heading": "单元与代数模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleUnit, createGenerationController } from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "`createLifecycleUnit`｜10 秒上手 —— 单元装载轴的状态机（`idle`/`loading`/`loaded`/`failed`），内部用一个 `GenerationController` 丢弃过期结果："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const unit = createLifecycleUnit<Config>()\nunit.start(() => fetchConfig()) // thenable → 先进 'loading'，settle 后进 'loaded'/'failed'\nunit.state // 'loading'\nunit.value // undefined（settle 前）\nunit.restart(() => fetchConfig()) // 新 generation，旧一次的迟到结果被静默丢弃"
              },
              {
                "type": "paragraph",
                "text": "全部选项（`ILifecycleUnitOptions`）：`report?: (error: unknown) => void` —— `start()` 的 factory 同步抛出或 thenable reject 时，除了把原始错误存进 `unit.error`（原样、不打标签），还会额外把一份打上 `UNIT_START_FAILED` 标签的错误发给 `report`。`start`/`restart` 是同一函数的两个别名；同步非 thenable 返回直接落 `loaded`（跳过 `loading`）。`close()` 同步；`dispose()` 同步（单元本身不持有需要等待的资源，只是终结 generation controller）。"
              },
              {
                "type": "paragraph",
                "text": "`createGenerationController`｜10 秒上手 —— 代数 + 每代一个 `AbortSignal`，新一代自动使旧一代失效："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const shutdown = new AbortController()\nconst generations = createGenerationController({ parentSignal: shutdown.signal })\nconst request = generations.begin({ timeoutMs: 5000 }) // 超过 5s 自动 abort 当前代\nconst result = await fetchWithSignal(request.signal)\nif (generations.adopt(request.token, result, (v) => v.close())) {\n  use(result) // 仍是当前代，安全采用\n} // 否则内部已调用 release() 回收 result，返回 false"
              },
              {
                "type": "paragraph",
                "text": "全部选项（`IGenerationControllerOptions`）："
              },
              {
                "type": "list",
                "items": [
                  "`parentSignal?: IAbortSignal` —— 该信号中止时，当前活跃代同步中止",
                  "`onSuperseded?: (info: ILifecycleError) => void` —— `adopt()` 发现 token 已过期时的诊断钩子（`GENERATION_SUPERSEDED`），不是失败信号",
                  "`scheduler?: ILifecycleScheduler` —— 默认 `systemScheduler`；驱动 `begin({ timeoutMs })` 的计时"
                ]
              },
              {
                "type": "paragraph",
                "text": "返回方法：`generation`（只读当前代号）、`disposed`（只读）、`begin(options?)` → `{ generation, token, signal }`（`options.timeoutMs?: number`，超时/父中止/新 `begin()` 都会使这个 token 的 `signal` 中止）、`isCurrent(token)`、`supersede(reason?)`（作废当前代但控制器仍可用）、`adopt(token, value, release, onReleaseError?)`、`dispose(reason?)`（终态，之后 `begin()` 抛 `GENERATION_DISPOSED`）。"
              }
            ]
          },
          {
            "id": "静默追踪与租约模块",
            "heading": "静默追踪与租约模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createQuiescenceTracker,\n  createStringQuiescenceTracker,\n  createObjectLeaseRegistry,\n  createStringLeaseRegistry,\n  createPendingTracker\n} from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "`createQuiescenceTracker` / `createStringQuiescenceTracker`｜10 秒上手 —— 按 key 计数的租约追踪，`WeakMap`（对象键，自动 GC）或 `Map`（字符串键，归零后自动清理未 seal 的条目）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const tracker = createQuiescenceTracker<object>() // 或 createStringQuiescenceTracker()\nconst release = tracker.retain(key)\ntracker.count(key) // 1\ntracker.seal(key) // 之后 retain(key) 抛 QUIESCENCE_SEALED\nrelease()\nawait tracker.whenZero(key) // 必须先 seal，否则同步抛 QUIESCENCE_UNSEALED_WAIT"
              },
              {
                "type": "paragraph",
                "text": "两者均无构造参数。返回方法：`retain(key)` → 返回释放函数 `IDisposer`（重复调用幂等）；`count(key)`；`whenZero(key)`（严格独占等待，必须先 `seal(key)`，否则同步抛错，不是 rejected Promise）；`whenZeroOnce(key)`（非独占，不要求 seal，随时可等待下一次归零）；`seal(key)`（幂等）；`isSealed(key)`。"
              },
              {
                "type": "paragraph",
                "text": "`createObjectLeaseRegistry` / `createStringLeaseRegistry`｜3 秒上手 —— 与上面完全同一引擎的别名，供领域友好命名的调用点使用："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const leases = createObjectLeaseRegistry<object>() // 内部就是 createQuiescenceTracker()"
              },
              {
                "type": "paragraph",
                "text": "无构造参数；`ILeaseRegistry<TKey>` 类型等价于 `IQuiescenceTracker<TKey>`。"
              },
              {
                "type": "paragraph",
                "text": "`createPendingTracker`｜5 秒上手 —— 只暴露 `track`/`drain`/`size` 的窄接口，用于追踪并排空在途 Promise："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const pending = createPendingTracker()\npending.track(doWork()) // 原样返回传入的 promise\nawait pending.drain() // 等到 size 归零；排空期间新增的 track() 不会被漏计（内部循环 whenZeroOnce）\npending.size // 当前在途数量"
              },
              {
                "type": "paragraph",
                "text": "无构造参数。`track(promise)` 结算后自动释放内部租约，无论 resolve 还是 reject；`drain()` 永不抛出（内部忽略释放函数自身的异常，release 本身也不会抛）。"
              }
            ]
          },
          {
            "id": "事务性所有权模块",
            "heading": "事务性所有权模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createProvisionalScope, type ILifecycleOwner } from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "`createProvisionalScope`｜10 秒上手 —— 构造期两阶段所有权：资源先暂存这里，最终要么 `commitTo(parent)` 转移给真正的 owner，要么 `rollback()` 释放，二选一、只能选一次："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const provisional = createProvisionalScope()\nconst db = provisional.own(await connect(), { force: (ctx) => db.close() })\ntry {\n  await provisional.commitTo(outerScope) // 按注册顺序逐个 own() 到 parent\n} catch (error) {\n  // parent 中途拒绝（如已在 closing）：已转移部分留在 parent 名下，剩余部分已被自动释放并 await 完成\n}"
              },
              {
                "type": "paragraph",
                "text": "全部选项（`IProvisionalScopeOptions`）：`parentSignal?: IAbortSignal` —— 转发到 `provisional.signal`，令其也随父信号中止。"
              },
              {
                "type": "paragraph",
                "text": "返回方法：`signal`（只读）、`own(resource, descriptor)`、`commitTo(parent: ILifecycleOwner)`（逐个转移；某项失败时，已转移的留给 parent，未转移的由本作用域逆序释放并 await 完成后才重新抛出原错误；若失败原因是 parent 已 `closing`/`terminal`，额外包一层 `PROVISIONAL_PARENT_CLOSED`，原错误仍在 `cause`）、`rollback()`（逆序释放全部已注册资源，聚合失败为 `AggregateError`，幂等——并发/重复调用复用同一个 settle Promise）。`commitTo`/`rollback` 二次调用（或 commit 后 rollback / rollback 后 commit）抛 `PROVISIONAL_SETTLED`。"
              }
            ]
          },
          {
            "id": "变更队列模块",
            "heading": "变更队列模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createMutationQueue } from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "`createMutationQueue`｜10 秒上手 —— 严格 FIFO 串行队列，无并发池，带可选的入队 SLA 看门狗："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const queue = createMutationQueue({ queueAdmissionTimeoutMs: 5000 })\nconst result = await queue.enqueue(() => applyMutation(), { owner: 'sync-loop' })\nqueue.size // 排队中 + 正在执行的任务数"
              },
              {
                "type": "paragraph",
                "text": "全部选项（`IMutationQueueOptions`）："
              },
              {
                "type": "list",
                "items": [
                  "`queueAdmissionTimeoutMs?: number | false` —— 默认 `undefined`（只诊断、从不因排队超时而拒绝）；数字表示任务排队超过这个时长即被移出队列并以 `QUEUE_ADMISSION_TIMEOUT` reject；`false` 连诊断计时器也不开",
                  "`admissionDiagnosticMs?: number | false` —— 默认 `1000`；仅在 `queueAdmissionTimeoutMs` 为 `undefined` 时生效的诊断阈值，`false` 关闭诊断计时器",
                  "`onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void`",
                  "`scheduler?: ILifecycleScheduler` —— 默认 `systemScheduler`"
                ]
              },
              {
                "type": "paragraph",
                "text": "`enqueue(task, options?)` 的 `options`（`IEnqueueOptions`）：`owner?: string`（用于自依赖检测——同一 owner 在自己正在运行时又提交新任务会立即以 `QUEUE_SELF_DEPENDENCY` reject，因为 FIFO 队列不可能在当前任务完成前跑新任务）、`queueAdmissionTimeoutMs?: number | false`（覆盖本次任务的队列默认值）。`size` 只读，等于排队数加运行中的 0/1。"
              }
            ]
          },
          {
            "id": "调度器-中止与有界等待模块",
            "heading": "调度器、中止与有界等待模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController\n} from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "`systemScheduler`｜3 秒上手 —— 默认调度器，`now()` 用 `performance.now()`、`schedule()` 用 `setTimeout`/`clearTimeout`，一般不用手动传，除非要换成 `createManualScheduler()`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "systemScheduler.now() // 单调递增毫秒数"
              },
              {
                "type": "paragraph",
                "text": "无配置，纯常量对象；宿主缺 `performance.now`/`setTimeout`/`clearTimeout` 时，首次调用对应方法才 fail-fast 抛 `ENV_UNSUPPORTED`。"
              },
              {
                "type": "paragraph",
                "text": "`createManualScheduler`｜10 秒上手 —— 单测里把时间变成确定性的虚拟时钟："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scheduler = createManualScheduler()\nconst task = scheduler.schedule(() => console.log('fired'), 100)\nscheduler.advance(100) // 一次性 flush 所有到期回调（含到期回调内部再排的到期任务）"
              },
              {
                "type": "paragraph",
                "text": "无入参；返回 `IManualScheduler`（在标准 `ILifecycleScheduler` 的 `now()`/`schedule()` 基础上加 `advance(ms)`）。`advance` 单次循环超过 10000 个 flush 任务会抛 `INVALID_OPTION`（runaway guard）；`ms`/`delayMs` 必须是有限非负数，否则抛 `INVALID_OPTION`。"
              },
              {
                "type": "paragraph",
                "text": "`snapshotScheduler`｜5 秒上手 —— 把任意 duck-typed 值快照成标准 `ILifecycleScheduler`（校验并锁定 accessor，防止 hostile getter 二次读取）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const snap = snapshotScheduler(candidate) // 不是合法 scheduler 时返回 undefined，而不是抛错"
              },
              {
                "type": "paragraph",
                "text": "单参数 `value: unknown`，无选项；`now`/`schedule` 都不是函数时返回 `undefined`；读取 `now`/`schedule` 属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
              },
              {
                "type": "paragraph",
                "text": "`validateSchedulerDelay` / `validateSchedulerTime` / `addSchedulerTime`｜3 秒上手 —— 本包内部用来校验时间值和安全相加的公开工具，自定义 scheduler 实现也可复用："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 两个有限数相加；溢出 Infinity 时抛 RangeError"
              },
              {
                "type": "paragraph",
                "text": "`validateSchedulerTime(value, label)`：`label: string` 必填，仅用于错误信息里标注字段名。`validateSchedulerDelay(value, label?)`：`label` 可选，默认 `'delayMs'`。`addSchedulerTime(base, delta, label)` 只从 `/scheduler` leaf 导出，验证两端及结果都是有限数。三者返回校验后的数值。"
              },
              {
                "type": "paragraph",
                "text": "`createAbortController`｜5 秒上手 —— 委托当前宿主的原生 `AbortController`，返回真实 host instance："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const controller = createAbortController()\ncontroller.signal.addEventListener('abort', () => console.log('aborted'))\ncontroller.abort('reason')"
              },
              {
                "type": "paragraph",
                "text": "无构造参数。`abort(reason?)` 保持宿主 native brand、reason 与 listener dispatch 语义；宿主能力缺失时抛出带 `ENV_UNSUPPORTED` 的 lifecycle error。"
              },
              {
                "type": "paragraph",
                "text": "这是 V2 breaking change：`abort()` 或 `abort(undefined)` 的 reason 是宿主 `DOMException`/`AbortError`，不再是 `undefined`；non-`undefined` reason 保持对象 identity。外部 listener 抛错走宿主 EventTarget 错误通道，不会从 `controller.abort()` 同步抛出 lifecycle `ABORT_LISTENER_FAILED`。Lifecycle 自己登记的 parent/cancellation callback 由内部 observed-subscription 边界捕获并交给对应 operation 的 collect/report 通道，不会拦截同一 signal 上的外部 listener。"
              },
              {
                "type": "paragraph",
                "text": "`boundedWait`｜5 秒上手 —— 等到绝对截止时间为止，从不取消被等待的任务："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const won = await boundedWait(task, deadlineAt) // true=task 先完成；false=截止时间先到（task 仍在跑）"
              },
              {
                "type": "paragraph",
                "text": "参数：`task: PromiseLike<unknown>`（必填）、`deadlineAt: number`（必填，绝对时刻）、第三参数选项 `{ scheduler?: ILifecycleScheduler }`（默认 `systemScheduler`）。无论超时与否都会挂一个 `.catch()` 观察 `task`，避免它日后 reject 变成未处理拒绝；`deadlineAt` 已经过去时立即返回 `false`（仍会先观察 `task`）。"
              },
              {
                "type": "paragraph",
                "text": "`createTerminalController`｜5 秒上手 —— 独立复用的容器存活轴状态机，`createLifecycleScope`/`createSyncLifecycleScope`/`createLifecycleUnit` 内部都基于它："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const terminal = createTerminalController()\nterminal.close() // open → closing，幂等\nterminal.forceTerminal() // 直接进 terminal，resolve whenTerminal()\nawait terminal.whenTerminal()"
              },
              {
                "type": "paragraph",
                "text": "无构造参数。`lifecycle`（只读，`'open' | 'closing' | 'terminal'`）、`close()`（同步幂等，只在 `open` 时生效）、`forceTerminal()`（同步幂等，直接到 `terminal`）、`whenTerminal()`（返回同一个 Promise，只在到达 `terminal` 时 resolve 一次）。"
              }
            ]
          },
          {
            "id": "按需导入与-tree-shaking",
            "heading": "按需导入与 tree-shaking",
            "blocks": [
              {
                "type": "paragraph",
                "text": "根入口保持兼容；需要控制 retained graph 时使用稳定 leaf："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createAbortController,\n  observeAbortSubscription,\n  type IObservedAbortFailureSink,\n  type IObservedAbortSubscription\n} from '@migaia/lifecycle/abort'\nimport { snapshotScheduler } from '@migaia/lifecycle/scheduler'\nimport { createPendingTracker } from '@migaia/lifecycle/quiescence'\nimport { createLifecycleScope } from '@migaia/lifecycle/scope'\nimport { createGenerationController } from '@migaia/lifecycle/generation'\nimport { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\nimport { probeThenable } from '@migaia/lifecycle/errors'"
              },
              {
                "type": "paragraph",
                "text": "`/abort` 还公开 canonical hostile-signal-safe `observeAbortSubscription` 及其 `I`-prefixed handle/sink types；Resource 等消费者应静态从该 leaf 委托 signal registration，不复制 registration ledger。可用 subpath：`/abort`、`/scheduler`、`/quiescence`、`/scope`、`/generation`、`/disposal`、`/errors`。Root 和 subpath 都 re-export 同一 preserve-modules 实现，同名 runtime export 保持 `===`；包不开放 `dist/*` wildcard。"
              },
              {
                "type": "paragraph",
                "text": "`sideEffects: false` 表示未导入 leaf 可被 bundler 删除；Node/Bun 直接 ESM import 本身不执行 tree-shaking，体积验收应看 bundler retained modules，而不是只看 gzip 总字节。"
              }
            ]
          },
          {
            "id": "错误基础设施模块",
            "heading": "错误基础设施模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "字符串常量，无调用。"
              },
              {
                "type": "paragraph",
                "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
              },
              {
                "type": "paragraph",
                "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
              },
              {
                "type": "paragraph",
                "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
              },
              {
                "type": "paragraph",
                "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
              },
              {
                "type": "paragraph",
                "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
              },
              {
                "type": "paragraph",
                "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
              },
              {
                "type": "paragraph",
                "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
              },
              {
                "type": "paragraph",
                "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
              },
              {
                "type": "paragraph",
                "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
              },
              {
                "type": "paragraph",
                "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
              },
              {
                "type": "paragraph",
                "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
              },
              {
                "type": "paragraph",
                "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
              },
              {
                "type": "paragraph",
                "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
              },
              {
                "type": "paragraph",
                "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
              },
              {
                "type": "paragraph",
                "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
              },
              {
                "type": "paragraph",
                "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
              },
              {
                "type": "paragraph",
                "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
              },
              {
                "type": "paragraph",
                "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
              },
              {
                "type": "paragraph",
                "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
              },
              {
                "type": "paragraph",
                "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
              },
              {
                "type": "paragraph",
                "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
              },
              {
                "type": "paragraph",
                "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
              }
            ]
          },
          {
            "id": "状态常量模块",
            "heading": "状态常量模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  LifecycleState,\n  LifecycleUnitState,\n  LifecycleErrorPolicy,\n  ThenableProbeKind,\n  DisposeTransactionKind\n} from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "均为纯常量对象（`as const`），无调用参数，用于替代裸字符串字面量做比较/`switch`。"
              },
              {
                "type": "paragraph",
                "text": "`LifecycleState`｜3 秒上手 —— 容器存活轴：`{ open: 'open', closing: 'closing', terminal: 'terminal' }`。"
              },
              {
                "type": "paragraph",
                "text": "`LifecycleUnitState`｜3 秒上手 —— 单元装载轴：`{ idle: 'idle', loading: 'loading', loaded: 'loaded', failed: 'failed' }`。"
              },
              {
                "type": "paragraph",
                "text": "`LifecycleErrorPolicy`｜3 秒上手 —— 释放错误策略：`{ throw: 'throw', collect: 'collect', report: 'report', firstError: 'firstError' }`；`firstError` 不是通用默认值，只为兼容 `web-rpc` discovery-registry 的迁移语义存在。"
              },
              {
                "type": "paragraph",
                "text": "`ThenableProbeKind`｜3 秒上手 —— `probeThenable`/内部归化逻辑的判别结果：`{ notThenable: 'not-thenable', thenable: 'thenable', failed: 'failed', promise: 'promise' }`。"
              },
              {
                "type": "paragraph",
                "text": "`DisposeTransactionKind`｜3 秒上手 —— `createDisposeTransaction` 的模式：`{ order: 'order', plan: 'plan' }`。"
              }
            ]
          },
          {
            "id": "1-作用域-代数-请求场景下取代过期结果",
            "heading": "1. 作用域 + 代数：请求场景下取代过期结果",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleScope, createGenerationController } from '@migaia/lifecycle'\n\nconst scope = createLifecycleScope({ errorPolicy: 'collect' })\nconst generations = createGenerationController()\n\nasync function loadUser(id: string) {\n  const request = generations.begin({ timeoutMs: 5000 })\n  const user = await fetchUser(id, { signal: request.signal })\n  if (!generations.adopt(request.token, user, (u) => u.dispose?.())) return // 已过期，静默丢弃\n  scope.own(user, { force: () => user.dispose?.() })\n}"
              }
            ]
          },
          {
            "id": "2-事务性所有权-构造期失败自动回滚-成功后转移给长期-scope",
            "heading": "2. 事务性所有权：构造期失败自动回滚，成功后转移给长期 scope",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleScope, createProvisionalScope } from '@migaia/lifecycle'\n\nconst rootScope = createLifecycleScope()\n\nasync function setupModule() {\n  const provisional = createProvisionalScope()\n  try {\n    const db = provisional.own(await connectDb(), { force: (ctx) => closeDb(db) })\n    const cache = provisional.own(await connectCache(), { force: (ctx) => closeCache(cache) })\n    await provisional.commitTo(rootScope) // 全部转移给 rootScope，之后随 rootScope.dispose() 释放\n  } catch (error) {\n    await provisional.rollback() // 任一步失败：已注册的资源逆序释放，冒泡原错误\n    throw error\n  }\n}"
              }
            ]
          },
          {
            "id": "3-静默追踪-变更队列-确保排空后才提交下一批变更",
            "heading": "3. 静默追踪 + 变更队列：确保排空后才提交下一批变更",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createPendingTracker, createMutationQueue } from '@migaia/lifecycle'\n\nconst pending = createPendingTracker()\nconst queue = createMutationQueue({ queueAdmissionTimeoutMs: 3000 })\n\nasync function applyBatch(mutations: Array<() => Promise<void>>) {\n  for (const mutation of mutations) {\n    pending.track(queue.enqueue(mutation, { owner: 'batch' }))\n  }\n  await pending.drain() // 等到本批（含批内动态追加的）全部落地\n}"
              }
            ]
          },
          {
            "id": "4-释放事务-有界等待-手动编排一组资源的优雅关闭",
            "heading": "4. 释放事务 + 有界等待：手动编排一组资源的优雅关闭",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createDisposeTransaction,\n  createManualScheduler,\n  type IDisposeItem\n} from '@migaia/lifecycle'\n\nconst scheduler = createManualScheduler()\nconst items: IDisposeItem[] = [\n  {\n    source: 'socket',\n    descriptor: {\n      order: 10,\n      graceful: (ctx) => flushSocket(),\n      gracefulTimeoutMs: 100,\n      force: () => forceCloseSocket()\n    }\n  },\n  { source: 'db', descriptor: { order: 5, force: () => closeDb() } }\n]\nconst transaction = createDisposeTransaction(\n  { kind: 'order' },\n  { errorPolicy: 'collect', scheduler }\n)\nconst runPromise = transaction.run(items)\nscheduler.advance(100) // 驱动 socket 的 graceful 超时降级到 force\nconst failures = await runPromise"
              }
            ]
          },
          {
            "id": "5-同步作用域-可暴露-using-语法的纯同步资源",
            "heading": "5. 同步作用域：可暴露 using 语法的纯同步资源",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createSyncLifecycleScope } from '@migaia/lifecycle'\n\nfunction withListeners() {\n  using scope = createSyncLifecycleScope() // 环境支持 `[Symbol.dispose]` 时可用 `using`\n  const handler = () => console.log('tick')\n  window.addEventListener('tick', handler)\n  scope.own(handler, { syncSafe: true, force: () => window.removeEventListener('tick', handler) })\n  // 作用域结束时自动 dispose()，移除监听器\n}"
              }
            ]
          },
          {
            "id": "构建门禁",
            "heading": "构建门禁",
            "blocks": [
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm run fmt && pnpm run lint && pnpm run typecheck && pnpm run typecheck:test && pnpm run test"
              },
              {
                "type": "paragraph",
                "text": "验收矩阵（L-T1 ~ L-T44）见 `docs/lifecycle/lifecycle-extraction.sdd.md` §5.4；每个 `L-T` 编号都能在 `test/` 下检索到对应用例。"
              }
            ]
          }
        ]
      },
      "guide": {
        "sections": [
          {
            "id": "migaia-lifecycle-使用指南",
            "heading": "@migaia/lifecycle 使用指南",
            "blocks": [
              {
                "type": "paragraph",
                "text": "本指南逐个模块列出全部导出 API 的签名、边界行为与错误码。包的定位、适用/不适用场景与安装方式见 README。"
              }
            ]
          },
          {
            "id": "目录",
            "heading": "目录",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "公共基础类型：owner/state/disposer/error 的共享结构",
                  "作用域与释放模块：`createLifecycleScope`、`createSyncLifecycleScope`、`executeReleaseDescriptor`、`createDisposeTransaction`、`createSyncStartedDisposalLedger`、`IReleaseDescriptor`、`IReleaseContext`",
                  "单元与代数模块：`createLifecycleUnit`、`createGenerationController`",
                  "静默追踪与租约模块：`createQuiescenceTracker`、`createStringQuiescenceTracker`、`createObjectLeaseRegistry`、`createStringLeaseRegistry`、`createPendingTracker`",
                  "事务性所有权模块：`createProvisionalScope`",
                  "变更队列模块：`createMutationQueue`",
                  "调度器、中止与有界等待模块：`systemScheduler`、`createManualScheduler`、`snapshotScheduler`、`resolveScheduler`、`resolveSchedulerOption`、`validateSchedulerDelay`、`validateSchedulerTime`、`addSchedulerTime`、`createAbortController`、`boundedWait`、`createTerminalController`",
                  "错误基础设施模块：`LIFECYCLE_SOURCE`、`createLifecycleError`、`createLifecycleRangeError`、`createLifecycleTypeError`、`createLifecycleFailure`、`tagLifecycleError`、`containAsyncRejection`、`probeThenable`、`assimilateCapturedThen`、`createErrorCollector`",
                  "状态常量模块：`LifecycleState`、`LifecycleUnitState`、`LifecycleErrorPolicy`、`ThenableProbeKind`、`DisposeTransactionKind`",
                  "按需导入与 tree-shaking",
                  "错误码：`LifecycleErrorCode`（20 个码）逐条语义",
                  "诊断消息：`LifecycleErrorText`",
                  "高阶组合示例",
                  "全部公开 API 索引",
                  "排查与构建门禁"
                ]
              }
            ]
          },
          {
            "id": "公共基础类型",
            "heading": "公共基础类型",
            "blocks": [
              {
                "type": "paragraph",
                "text": "这些类型从根入口导出，不创建runtime state："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'"
              },
              {
                "type": "list",
                "items": [
                  "`IDisposer`：调用方持有的同步、应幂等释放函数形状。",
                  "`ILifecycleOwner`：只表达可接收显式 `IReleaseDescriptor` 的 ownership handoff；`ProvisionalScope.commitTo()` 依赖它，因此同步/异步 scope 都能作为 parent，而无需共享各自不同的 dispose 形状。",
                  "`ILifecycleState`：资源容器存活轴；不要与 `IUnitState` 的加载轴混用。",
                  "`ICollectedError`：raw failure 加调用方提供的 source 标签；`error` 保持原始 identity。",
                  "`IErrorPolicy`：四种释放错误投影。新调用方通常选 `throw`、`collect` 或 `report`；`firstError` 主要服务已有 first-error-wins 迁移。",
                  "`IReleaseContext`、`IReleaseDescriptor` 在下一节完整说明；其他命名类型紧邻其构造函数章节。"
                ]
              }
            ]
          },
          {
            "id": "作用域与释放模块",
            "heading": "作用域与释放模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "两条正交轴：容器存活轴（`open → closing → terminal`，由 `close()`/`dispose()` 驱动）与资源释放本身。`close()` 永远同步、幂等、不调用用户代码；`dispose()` 永远异步。"
              }
            ]
          },
          {
            "id": "ireleasedescriptor-领域表达释放意图的唯一词汇",
            "heading": "IReleaseDescriptor —— 领域表达释放意图的唯一词汇",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "type IReleaseDescriptor = {\n  readonly syncSafe?: boolean\n  readonly order?: number\n  readonly graceful?: (context: IReleaseContext) => void | PromiseLike<void>\n  readonly gracefulTimeoutMs?: number\n  readonly force: (context: IReleaseContext) => void | PromiseLike<void>\n  readonly gcFallback?: boolean\n  readonly custom?: (context: IReleaseContext) => void | PromiseLike<void>\n}"
              },
              {
                "type": "paragraph",
                "text": "字段语义："
              },
              {
                "type": "list",
                "items": [
                  "`syncSafe?: boolean` —— 声明该 descriptor 可被 `SyncLifecycleScope` 接受，不改变 `LifecycleScope` 本身的静态类型（同/异步由构造的具体类型决定，不由 descriptor 推断）。",
                  "`order?: number` —— 释放排序键，缺省按 `0`；只在 `DisposeTransaction` 的 `order` 模式下生效，数值大者先释放（往「释放顺序的下方」沉降，与分配顺序相反）。",
                  "`graceful?` —— 优雅释放；超时只放弃等待（不取消它），随即降级到 `force`；`graceful` 本身抛错（非超时）不会阻止 `force` 继续跑。",
                  "`gracefulTimeoutMs?: number` —— 优雅阶段自己的预算，会被事务共享的绝对 `deadlineAt` 进一步封顶（取两者较小值）。",
                  "`force: (context) => void | PromiseLike<void>`（必填）—— 必须无条件完成，不得依赖任何前置条件。",
                  "`gcFallback?: boolean` —— 置为 `true` 时向内部 `FinalizationRegistry` 注册一个不强引用 `resource`/闭包 target 的兜底；显式释放（`dispose()`/`release()`）会自动 unregister 它。",
                  "`custom?` —— 逃生舱：一旦提供，完全接管释放，`graceful`/`force` 被忽略。"
                ]
              },
              {
                "type": "paragraph",
                "text": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer."
              },
              {
                "type": "paragraph",
                "text": "join()`；该显式 owner self-join operation 在同步调用及任意 await、microtask、Promise chain、nested helper 或 timer 后都立即抛 `SCOPE_REENTRANT_DISPOSE`，不返回 outer disposal Promise。没有 scope owner 的 generic `createDisposeTransaction()` 不伪造此 capability；不要捕获 scope 后在 disposer 中直接调用 `scope.dispose()` 绕过 context。"
              }
            ]
          },
          {
            "id": "createlifecyclescope",
            "heading": "createLifecycleScope",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
              },
              {
                "type": "paragraph",
                "text": "通用异步所有权容器："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
              },
              {
                "type": "paragraph",
                "text": "边界行为："
              },
              {
                "type": "list",
                "items": [
                  "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                  "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                  "`close()`：同步、幂等；`open → closing`。",
                  "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                  "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                ]
              }
            ]
          },
          {
            "id": "createsynclifecyclescope",
            "heading": "createSyncLifecycleScope",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
              },
              {
                "type": "paragraph",
                "text": "纯同步容器："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回"
              },
              {
                "type": "paragraph",
                "text": "约束与边界："
              },
              {
                "type": "list",
                "items": [
                  "每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`。",
                  "不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例（内部品牌检测），命中同样抛 `SCOPE_SYNC_VIOLATION`。",
                  "某个 callback（`custom`/`graceful`/`force`）若返回 thenable，在释放当次同步探测到并抛 `SCOPE_SYNC_VIOLATION`（`graceful` 违规时会继续跑 `force`，`force`/`custom` 违规则该项失败计入当前错误策略）。",
                  "降级链与异步版本相同（`custom` → 否则 `graceful` → `force`），但没有超时竞速——没有可等待的对象。",
                  "`release()`/`close()` 语义与异步版本一致；`dispose()` 幂等，二次调用返回 `[]`；在 disposer 内部重入抛 `SCOPE_REENTRANT_DISPOSE`。",
                  "环境支持时挂载 `[Symbol.dispose]`，可配合 `using` 语法。"
                ]
              }
            ]
          },
          {
            "id": "executereleasedescriptor",
            "heading": "executeReleaseDescriptor",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function executeReleaseDescriptor(\n  descriptor: IReleaseDescriptor,\n  context: IReleaseContext\n): Promise<readonly unknown[]>"
              },
              {
                "type": "paragraph",
                "text": "独立运行单个 descriptor 的完整降级链（`custom` → 否则 `graceful`（带超时竞速）→ `force`），不经过完整事务，常用于测试或自定义编排："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const errors = await executeReleaseDescriptor(descriptor, {\n  signal: controller.signal,\n  deadlineAt: undefined,\n  report: (e) => console.error(e)\n})"
              },
              {
                "type": "paragraph",
                "text": "`descriptor`、`context` 均必填，`context` 无可选项——必须直接构造完整对象。返回全部失败错误的数组（成功为空数组），从不抛出；`graceful` 超时时会通过 `context.report` 上报一条诊断（`DEADLINE_EXCEEDED` 或空转到 `force`），`force` 失败时额外通过 `report` 上报一条 `RELEASE_FORCE_FAILED` 标签的错误（原始错误仍原样进入返回数组）。"
              }
            ]
          },
          {
            "id": "createdisposetransaction",
            "heading": "createDisposeTransaction",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}"
              },
              {
                "type": "paragraph",
                "text": "编排多个资源的释放，`order` 弱排序或 `plan` 强序列二选一，同一实例不能混用："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const transaction = createDisposeTransaction(\n  { kind: 'plan' },\n  { errorPolicy: 'collect', signal: controller.signal }\n)\nconst failures = await transaction.run([\n  { source: 'db', descriptor: dbDescriptor },\n  { source: 'socket', descriptor: socketDescriptor }\n])"
              },
              {
                "type": "paragraph",
                "text": "边界行为："
              },
              {
                "type": "list",
                "items": [
                  "`{ kind: 'order' }`：按每个 item 的 `descriptor.order`（缺省 `0`）降序分组释放，稳定排序保留同序调用方给定的相对顺序；只在此模式读取 `order`。",
                  "`{ kind: 'plan' }`：严格按 `items` 给定顺序执行，从不读取 `order` 字段。",
                  "`signal?`：转发进每个 item 的 `context.signal`；`run()` 开始时自动镜像调用方传入信号的中止状态；无 `signal` 时 item 收到一个永不中止的惰性信号。",
                  "`deadlineAt`/`scheduler`：本次 `run()` 内所有 item 共享，不逐 item 重算。",
                  "`pending?`：每个 item 释放完后调用 `pending.drain()` 等待其触发的在途工作排空（典型传 `createPendingTracker()`）。",
                  "descriptor 校验（`order`/`custom`/`graceful`/`gracefulTimeoutMs`/`force` 的类型与访问）逐项独立进行——一个 item 的非法 descriptor（贴 `INVALID_OPTION` 码）不阻断其余 item 的释放，会作为该 item 的失败计入错误策略。",
                  "`errorPolicy` 语义同 `createErrorCollector`；`throw` 策略下单错原样抛出，多错聚合为携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`。"
                ]
              }
            ]
          },
          {
            "id": "createsyncstarteddisposalledger",
            "heading": "createSyncStartedDisposalLedger",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger\n\ntype ISyncStartedDisposalLedger = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly pending: number\n  start(source: string, callback: () => unknown): void\n  seal(): ISyncStartedDisposalOutcome\n  whenTerminal(): Promise<void>\n}\n\ntype ISyncStartedDisposalOutcome = Readonly<{\n  synchronousErrors: readonly ICollectedError[]\n  completion: Promise<readonly ICollectedError[]>\n}>"
              },
              {
                "type": "paragraph",
                "text": "用于“同步启动全部 disposer，但异步统一等待”的领域容器。Ledger 是 pending、raw error 与 terminal 的唯一事实源；调用方仍负责生成顺序计划、清空领域 registry，以及把 raw errors 投影成自己的 throw/report/aggregate 契约。"
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\n\nconst ledger = createSyncStartedDisposalLedger()\n\n// 调用顺序由领域层决定；每个 callback 在 start() 的当前 stack 执行。\nledger.start('connection', () => connection.close())\nledger.start('cache', () => cache.flush())\n\nconst outcome = ledger.seal()\nif (outcome.synchronousErrors.length === 1) {\n  throw outcome.synchronousErrors[0].error\n}\n\nconst finalErrors = await outcome.completion\n// completion 永远 resolve；领域层在这里决定是否 reject/aggregate/report。"
              },
              {
                "type": "paragraph",
                "text": "状态和 identity："
              },
              {
                "type": "list",
                "items": [
                  "初始 `open`；`start()` 不改变 lifecycle。",
                  "首次 `seal()` 同步进入 `closing`，冻结当时全部 synchronous errors，并返回唯一 outcome；重复 `seal()` 返回同一对象，`outcome.completion` 也保持同一 Promise。",
                  "已 seal 且 `pending === 0` 时进入 `terminal`；否则最后一个 thenable settle 后进入 `terminal`。`whenTerminal()` 只表达 terminal 等待，不承诺 Promise 对象 identity。",
                  "合法 thenable 永不 settle 时保持 `closing`；Ledger 不自造 timeout、abort、retry 或强制 terminal。"
                ]
              },
              {
                "type": "paragraph",
                "text": "Callback 与 thenable："
              },
              {
                "type": "list",
                "items": [
                  "`start()` 先校验 `source` 为 string、`callback` 为 function；非法输入同步抛 `INVALID_OPTION`，且不调用 callback。",
                  "callback 同步 throw 或读取 `.then` getter throw：记录进 `synchronousErrors` 与最终 ledger；`start()` 不重抛 item failure，因此调用方仍能启动后续 item。",
                  "thenable 的 `.then` 只读取一次，并以原 thenable 为 receiver 调用；resolve/reject first-settlement-wins。Rejection按 settlement observation order进入最终 ledger。",
                  "每条错误是冻结的 `{ source, error }`；原始 `error` identity不被 Ledger替换。`completion` 因 item failure也不会 reject，避免 Ledger抢走领域错误政策。"
                ]
              },
              {
                "type": "paragraph",
                "text": "重入和关闭边界："
              },
              {
                "type": "list",
                "items": [
                  "当前 item callback/thenable probe 尚未结束时重入 `start()` 或 `seal()`，同步抛 `SCOPE_REENTRANT_OWN`（`disposalLedgerReentrant` 文案）；外层 `start()` 把它当作当前 item 的同步 failure收集。",
                  "seal 后再 `start()` 同步抛 `SCOPE_CLOSED`（`disposalLedgerClosed` 文案），不调用新 callback。",
                  "Ledger 没有 rollback：进入 closing 后只能等待 admitted thenables settle。"
                ]
              },
              {
                "type": "paragraph",
                "text": "错误政策边界：Ledger 固定采用 raw `collect`。`firstError` 是最终 ledger 的第一条 observation；`throw`、`report`、`AggregateError` message/source/code和reporter failure containment全部属于调用 adapter，不应反向写入 Ledger。"
              }
            ]
          },
          {
            "id": "单元与代数模块",
            "heading": "单元与代数模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createLifecycleUnit,\n  type ILifecycleUnit,\n  type ILifecycleUnitOptions,\n  createGenerationController,\n  type IGenerationController,\n  type IGenerationControllerOptions,\n  type IGenerationRequest,\n  type IGenerationToken\n} from '@migaia/lifecycle'"
              }
            ]
          },
          {
            "id": "createlifecycleunit",
            "heading": "createLifecycleUnit",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>\n\ntype ILifecycleUnitOptions = { readonly report?: (error: unknown) => void }\n\ntype ILifecycleUnit<T> = {\n  readonly state: 'idle' | 'loading' | 'loaded' | 'failed'\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly value: T | undefined\n  readonly error: unknown\n  start(factory: () => T | PromiseLike<T>): void\n  restart(factory: () => T | PromiseLike<T>): void // start 的别名\n  close(): void\n  dispose(): void // 永远同步\n}"
              },
              {
                "type": "paragraph",
                "text": "单元装载轴的状态机，内部用一个 `GenerationController` 丢弃过期结果："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const unit = createLifecycleUnit<Config>()\nunit.start(() => fetchConfig()) // thenable → 先进 'loading'，settle 后进 'loaded'/'failed'\nunit.state // 'loading'\nunit.value // undefined（settle 前）\nunit.restart(() => fetchConfig()) // 新 generation，旧一次的迟到结果被静默丢弃"
              },
              {
                "type": "paragraph",
                "text": "边界行为："
              },
              {
                "type": "list",
                "items": [
                  "`factory()` 只读一次其返回值的 `.then`（探测语义同 `probeThenable`）：同步非 thenable 返回直接落 `loaded`（跳过 `loading`）；`factory()` 同步抛出或 thenable reject 时进入 `failed`，`unit.error` 存原始错误（不打标签），同时额外把一份打上 `UNIT_START_FAILED` 标签的错误发给 `options.report`（若提供）。",
                  "每次 `start()`/`restart()` 隐式开启新 generation；旧 generation 的迟到 settle 结果被静默丢弃（不写入 `state`/`value`/`error`）。",
                  "`lifecycle` 是容器存活轴（`close()`/`dispose()` 驱动），与 `state`（装载轴）是两个独立读值。",
                  "`dispose()` 永远同步——单元本身不持有需要等待的资源，只是终结内部 generation controller（超过一次调用幂等）。"
                ]
              }
            ]
          },
          {
            "id": "creategenerationcontroller",
            "heading": "createGenerationController",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}"
              },
              {
                "type": "paragraph",
                "text": "代数 + 每代一个 `AbortSignal`，新一代自动使旧一代失效："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const shutdown = new AbortController()\nconst generations = createGenerationController({ parentSignal: shutdown.signal })\nconst request = generations.begin({ timeoutMs: 5000 }) // 超过 5s 自动 abort 当前代\nconst result = await fetchWithSignal(request.signal)\nif (generations.adopt(request.token, result, (v) => v.close())) {\n  use(result) // 仍是当前代，安全采用\n} // 否则内部已调用 release() 回收 result，返回 false"
              },
              {
                "type": "paragraph",
                "text": "边界行为："
              },
              {
                "type": "list",
                "items": [
                  "`begin(options?)`：使当前活跃代（若存在）立即失效（触发其 `signal` 中止），`generation` 自增，返回新的 `{ generation, token, signal }`。`options.timeoutMs?` 非法（非有限/负数）抛 `INVALID_OPTION`；超过该毫秒数后当前代的 `signal` 自动中止。控制器已 `dispose()` 后调用抛 `GENERATION_DISPOSED`。",
                  "`isCurrent(token)`：`token` 是否仍是当前代；`dispose()` 之后恒为 `false`。",
                  "`supersede(reason?)`：作废当前代（其 `signal` 中止）但控制器本身仍可用，之后 `begin()` 仍能开新代；对已 `dispose()` 的控制器调用是空操作。",
                  "`adopt(token, value, release, onReleaseError?)`：`token` 仍是当前代时返回 `true`，`value` 留给调用方；否则调用 `release(value)` 回收并返回 `false`——`release` 本身抛出/reject 只会经 `onReleaseError` 上报，绝不反过来污染\"当前是哪一代\"这一状态（不会抛出、不会重新计入错误策略）。若提供了 `onSuperseded`，在过期路径上先额外触发一次携带 `GENERATION_SUPERSEDED` 码的诊断信息（非失败信号）。",
                  "`dispose(reason?)`：终态；之后 `begin()` 恒抛 `GENERATION_DISPOSED`，`isCurrent()` 恒 `false`。",
                  "`parentSignal` 中止时，当前活跃代同步中止（清理 timer、移除 parent listener、abort 自身 controller）；清理过程中任一步失败会聚合为 `GENERATION_CANCELLATION_FAILED`。"
                ]
              }
            ]
          },
          {
            "id": "静默追踪与租约模块",
            "heading": "静默追踪与租约模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createQuiescenceTracker,\n  createStringQuiescenceTracker,\n  createObjectLeaseRegistry,\n  createStringLeaseRegistry,\n  createPendingTracker,\n  type IQuiescenceTracker,\n  type ILeaseRegistry,\n  type IPendingTracker\n} from '@migaia/lifecycle'"
              }
            ]
          },
          {
            "id": "createquiescencetracker-createstringquiescencetracker",
            "heading": "createQuiescenceTracker / createStringQuiescenceTracker",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>\nfunction createStringQuiescenceTracker(): IQuiescenceTracker<string>\n\ntype IQuiescenceTracker<TKey> = {\n  retain(key: TKey): IDisposer // IDisposer = () => void\n  count(key: TKey): number\n  whenZero(key: TKey): Promise<void> // 严格独占等待\n  whenZeroOnce(key: TKey): Promise<void> // 非独占等待\n  seal(key: TKey): void\n  isSealed(key: TKey): boolean\n  forget(key: TKey): boolean\n}"
              },
              {
                "type": "paragraph",
                "text": "按 key 计数的租约追踪：对象键用 `WeakMap`（自动 GC），字符串键用 `Map`（归零后自动清理未 seal 的条目）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const tracker = createQuiescenceTracker<object>() // 或 createStringQuiescenceTracker()\nconst release = tracker.retain(key)\ntracker.count(key) // 1\ntracker.seal(key) // 之后 retain(key) 抛 QUIESCENCE_SEALED\nrelease()\nawait tracker.whenZero(key) // 必须先 seal，否则同步抛 QUIESCENCE_UNSEALED_WAIT"
              },
              {
                "type": "paragraph",
                "text": "两者均无构造参数。边界行为："
              },
              {
                "type": "list",
                "items": [
                  "`retain(key)`：已 `seal(key)` 的 key 上调用抛 `QUIESCENCE_SEALED`；返回的释放函数重复调用幂等（只在首次调用时递减计数）。",
                  "`whenZero(key)`：必须先 `seal(key)`，否则同步抛 `QUIESCENCE_UNSEALED_WAIT`（不是返回 rejected Promise）；已归零时立即 resolve。",
                  "`whenZeroOnce(key)`：不要求 seal，随时可等待\"下一次归零\"，不阻塞新 `retain()`。",
                  "`seal(key)`：幂等，标记该 key 之后拒绝新租约。",
                  "`forget(key)`：仅当计数为 0 且没有任何等待者时才移除该 key 的状态并返回 `true`；否则返回 `false`。",
                  "一个未 seal 且归零的 key（字符串键）会被自动从底层 `Map` 中删除，避免长期驻留；已 `seal` 的 key 的\"已 seal\"事实会保留。"
                ]
              }
            ]
          },
          {
            "id": "createobjectleaseregistry-createstringleaseregistry",
            "heading": "createObjectLeaseRegistry / createStringLeaseRegistry",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>\nfunction createStringLeaseRegistry(): ILeaseRegistry<string>\n// type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>"
              },
              {
                "type": "paragraph",
                "text": "与上面完全同一引擎的别名（内部就是分别调用 `createQuiescenceTracker()`/`createStringQuiescenceTracker()`），供领域友好命名的调用点使用；无构造参数。"
              }
            ]
          },
          {
            "id": "creatependingtracker",
            "heading": "createPendingTracker",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createPendingTracker(): IPendingTracker\n\ntype IPendingTracker = {\n  track<T>(promise: Promise<T>): Promise<T>\n  drain(): Promise<void>\n  readonly size: number\n}"
              },
              {
                "type": "paragraph",
                "text": "只暴露 `track`/`drain`/`size` 的窄接口，用于追踪并排空在途 Promise（内部基于一个字符串键 `createStringQuiescenceTracker()`）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const pending = createPendingTracker()\npending.track(doWork()) // 原样返回传入的 promise\nawait pending.drain() // 等到 size 归零；排空期间新增的 track() 不会被漏计（内部循环 whenZeroOnce）\npending.size // 当前在途数量"
              },
              {
                "type": "paragraph",
                "text": "无构造参数。`track(promise)` 结算后（无论 resolve 还是 reject）自动释放内部租约；`drain()` 永不抛出（内部忽略释放函数自身的异常，`release` 本身也不会抛）；`drain()` 内部循环调用 `whenZeroOnce()`，因此排空期间新 `track()` 进来的 promise 也会被等到。"
              }
            ]
          },
          {
            "id": "事务性所有权模块",
            "heading": "事务性所有权模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createProvisionalScope,\n  type IProvisionalScope,\n  type IProvisionalScopeOptions,\n  type ILifecycleOwner\n} from '@migaia/lifecycle'"
              }
            ]
          },
          {
            "id": "createprovisionalscope",
            "heading": "createProvisionalScope",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope\n\ntype IProvisionalScopeOptions = { readonly parentSignal?: IAbortSignal }\n\ntype IProvisionalScope = {\n  readonly signal: IAbortSignal\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  commitTo(parent: ILifecycleOwner): Promise<void>\n  rollback(): Promise<void>\n}\n\ntype ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T }"
              },
              {
                "type": "paragraph",
                "text": "构造期两阶段所有权：资源先暂存这里，最终要么 `commitTo(parent)` 转移给真正的 owner，要么 `rollback()` 释放，二选一、只能选一次："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const shutdown = new AbortController()\nconst provisional = createProvisionalScope({ parentSignal: shutdown.signal })\nconst db = provisional.own(await connect(), { force: (ctx) => db.close() })\ntry {\n  await provisional.commitTo(outerScope) // 按注册顺序逐个 own() 到 parent\n} catch (error) {\n  // parent 中途拒绝（如已在 closing）：已转移部分留在 parent 名下，剩余部分已被自动释放并 await 完成\n}"
              },
              {
                "type": "paragraph",
                "text": "边界行为："
              },
              {
                "type": "list",
                "items": [
                  "`own(resource, descriptor)`：已 commit/rollback 后调用抛 `PROVISIONAL_SETTLED`。",
                  "`commitTo(parent)`：按注册顺序逐个调用 `parent.own(resource, descriptor)`；某项失败时，已转移给 parent 的部分保留在 parent 名下，尚未转移的部分由本作用域逆序释放并 await 完成后才重新抛出原错误——不会有资源被两边都不持有，也不会有资源留给\"none\"；若失败原因是 `parent` 已处于 `closing`/`terminal`（错误码为 `SCOPE_CLOSED`/`SCOPE_TERMINAL`），额外包一层 `PROVISIONAL_PARENT_CLOSED`，原错误仍在 `cause`，遗留资源的释放失败列表挂在 `errors`。",
                  "`rollback()`：逆序释放全部已注册资源，聚合失败为携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；幂等——并发/重复调用复用同一个 settle Promise。",
                  "`commitTo`/`rollback` 二次调用（或 commit 后 rollback / rollback 后 commit）一律抛 `PROVISIONAL_SETTLED`（`rollback()` 以 rejected Promise 形式返回，不是同步抛出）。",
                  "`parentSignal` 中止时（构造时已中止，或之后中止），`signal` 同步跟随中止，但不会自动触发 rollback/commit——仍需调用方显式二选一。"
                ]
              }
            ]
          },
          {
            "id": "变更队列模块",
            "heading": "变更队列模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createMutationQueue,\n  type IMutationQueue,\n  type IMutationQueueOptions,\n  type IEnqueueOptions\n} from '@migaia/lifecycle'"
              }
            ]
          },
          {
            "id": "createmutationqueue",
            "heading": "createMutationQueue",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue\n\ntype IMutationQueueOptions = {\n  readonly queueAdmissionTimeoutMs?: number | false // 默认 undefined（只诊断，从不因排队超时而拒绝）\n  readonly admissionDiagnosticMs?: number | false // 默认 1000\n  readonly onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IEnqueueOptions = {\n  readonly owner?: string\n  readonly queueAdmissionTimeoutMs?: number | false // 覆盖本次任务的队列默认值\n}\n\ntype IMutationQueue = {\n  enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>\n  readonly size: number\n}"
              },
              {
                "type": "paragraph",
                "text": "严格 FIFO 串行队列，无并发池，带可选的入队 SLA 看门狗："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const queue = createMutationQueue({ queueAdmissionTimeoutMs: 5000 })\nconst result = await queue.enqueue(() => applyMutation(), { owner: 'sync-loop' })\nqueue.size // 排队中 + 正在执行的任务数（0 或 1）"
              },
              {
                "type": "paragraph",
                "text": "边界行为："
              },
              {
                "type": "list",
                "items": [
                  "`queueAdmissionTimeoutMs`：数字表示任务在队列中等待超过这个时长即被移出队列并以 `QUEUE_ADMISSION_TIMEOUT` reject；`undefined`（默认）只诊断、从不因排队超时而拒绝；`false` 连诊断计时器也不开。",
                  "`admissionDiagnosticMs`：仅在 `queueAdmissionTimeoutMs` 为 `undefined` 且提供了 `onAdmissionDiagnostic` 时生效的诊断阈值；`false` 关闭诊断计时器。",
                  "`owner`：用于自依赖检测——同一 `owner` 在自己正在运行时又向同一队列提交新任务，会立即以 `QUEUE_SELF_DEPENDENCY` reject（因为 FIFO 队列不可能在当前任务完成前跑新任务）。这是部分防护：正在运行的任务提交一个不同/无 `owner` 的任务并原地 `await` 它同样会死锁，但没有可靠信号能检测这种情况，因此不拦截；安全模式是提交后续任务而不在当前任务内 `await` 它。",
                  "`size`：只读，等于排队数加运行中的 0/1。"
                ]
              }
            ]
          },
          {
            "id": "调度器-中止与有界等待模块",
            "heading": "调度器、中止与有界等待模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'"
              }
            ]
          },
          {
            "id": "systemscheduler",
            "heading": "systemScheduler",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "const systemScheduler: ILifecycleScheduler // { now(): number; schedule(cb, delayMs): IScheduledTask }"
              },
              {
                "type": "paragraph",
                "text": "默认调度器，`now()` 用 `performance.now()`、`schedule()` 用 `setTimeout`/`clearTimeout`，一般不用手动传，除非要换成 `createManualScheduler()`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "systemScheduler.now() // 单调递增毫秒数"
              },
              {
                "type": "paragraph",
                "text": "无配置，纯常量对象；宿主缺 `performance.now`/`setTimeout`/`clearTimeout` 时，首次调用对应方法才 fail-fast 抛 `ENV_UNSUPPORTED`。"
              }
            ]
          },
          {
            "id": "createmanualscheduler",
            "heading": "createManualScheduler",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createManualScheduler(): IManualScheduler\n// type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void };"
              },
              {
                "type": "paragraph",
                "text": "单测里把时间变成确定性的虚拟时钟："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const scheduler = createManualScheduler()\nconst task = scheduler.schedule(() => console.log('fired'), 100)\nscheduler.advance(100) // 一次性 flush 所有到期回调（含到期回调内部再排的到期任务）"
              },
              {
                "type": "paragraph",
                "text": "无入参。`advance(ms)` 按到期时刻升序（同刻按登记顺序）依次执行所有到期回调，单次循环超过 10000 个 flush 任务会抛 `INVALID_OPTION`（runaway guard）；`ms`/`schedule()` 的 `delayMs` 必须是有限非负数，否则抛 `INVALID_OPTION`（`RangeError`/`TypeError` 原生类型，贴该码）。"
              }
            ]
          },
          {
            "id": "snapshotscheduler",
            "heading": "snapshotScheduler",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined"
              },
              {
                "type": "paragraph",
                "text": "把任意 duck-typed 值快照成标准 `ILifecycleScheduler`（校验并锁定 accessor，防止 hostile getter 二次读取）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const snap = snapshotScheduler(candidate) // 不是合法 scheduler 时返回 undefined，而不是抛错"
              },
              {
                "type": "paragraph",
                "text": "单参数 `value: unknown`，无选项；`now`/`schedule` 都不是函数时返回 `undefined`；读取 `now`/`schedule` 属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
              }
            ]
          },
          {
            "id": "resolvescheduler-resolvescheduleroption",
            "heading": "resolveScheduler / resolveSchedulerOption",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function resolveScheduler(value: unknown): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined,\n  fallback: ISchedulerSnapshot\n): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined\n): ISchedulerSnapshot | undefined"
              },
              {
                "type": "paragraph",
                "text": "内部/自定义 scheduler 实现复用的边界解析函数：`resolveScheduler(value)` 对非法值抛 `SCHEDULER_INVALID` 文案的 `INVALID_OPTION` 错误（而不是像 `snapshotScheduler` 那样返回 `undefined`）；`resolveSchedulerOption(options, fallback?)` 读取 `options.scheduler`，未提供时返回 `fallback`（若调用形态未传 `fallback` 则返回 `undefined`），读取该属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
              }
            ]
          },
          {
            "id": "validateschedulerdelay-validateschedulertime-addschedulertime",
            "heading": "validateSchedulerDelay / validateSchedulerTime / addSchedulerTime",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function validateSchedulerTime(value: unknown, label: string): number\nfunction validateSchedulerDelay(value: unknown, label?: string): number // label 默认 'delayMs'\nfunction addSchedulerTime(base: number, delta: number, label: string): number"
              },
              {
                "type": "paragraph",
                "text": "本包内部用来校验时间值的公开工具，自定义 scheduler 实现也可复用："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 结果溢出为 Infinity 时抛 RangeError"
              },
              {
                "type": "paragraph",
                "text": "`validateSchedulerTime`：`label` 必填，仅用于错误信息里标注字段名；非 `number` 抛贴 `INVALID_OPTION` 码的 `TypeError`，非有限（`NaN`/`Infinity`）抛贴同码的 `RangeError`。`validateSchedulerDelay`：`label` 可选；在上面基础上额外要求 `>= 0`，否则抛 `RangeError`。`addSchedulerTime` 校验 `base`、`delta` 和相加结果均为有限数；它只从 `/scheduler` leaf 导出。三者校验通过时返回数值。"
              }
            ]
          },
          {
            "id": "createabortcontroller",
            "heading": "createAbortController",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createAbortController(): IAbortController\n// type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(...); removeEventListener(...) };\n// type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void };"
              },
              {
                "type": "paragraph",
                "text": "本包委托当前宿主的原生 `AbortController`，返回真实 host instance（结构上与 DOM/Node 的 `AbortSignal` 兼容，可互相传递）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const controller = createAbortController()\ncontroller.signal.addEventListener('abort', () => console.log('aborted'))\ncontroller.abort('reason')"
              },
              {
                "type": "paragraph",
                "text": "无构造参数，也不接受 custom factory。实际对象由当前 realm 的 host-owned `globalThis.AbortController` 构造；lifecycle 不返回 facade、Proxy 或自建 EventTarget。宿主 constructor 缺失、getter throw、不可构造、构造失败或结果不满足最小 controller/signal shape时，抛保留原始 cause 的 `ENV_UNSUPPORTED`。"
              },
              {
                "type": "paragraph",
                "text": "V2 intentional breaking behavior："
              },
              {
                "type": "list",
                "items": [
                  "`abort()` 与 `abort(undefined)` 使用宿主默认 reason，通常是原生 `DOMException`（name 为 `AbortError`），不再保留 V1 的 `undefined`。",
                  "non-`undefined` explicit reason保持 `===` identity；把 structural parent signal 的 `undefined` reason转发给native child时，同样规范化为宿主 `AbortError`。",
                  "重复 `abort()` 幂等，不覆盖第一次固化的 reason。",
                  "外部 listener throw走宿主 EventTarget error channel；`try/catch controller.abort()` 不能同步捕获它，lifecycle也不再提供 `ABORT_LISTENER_FAILED`。",
                  "Lifecycle 自己登记的 parent/cancellation callback 经过 module-private observed-subscription：处理 already-aborted、registration-time abort、post-registration recheck和幂等remove，并把自身 callback/read/cleanup failure交给对应 operation的collect/report边界。它不代理、枚举或捕获同一 signal 上的外部 listener。"
                ]
              },
              {
                "type": "paragraph",
                "text": "如果业务需要旧式“listener错误由调用 abort 的栈聚合抛出”，应使用业务自己拥有的回调编排器；不要包裹或 monkey-patch native `AbortSignal`。"
              }
            ]
          },
          {
            "id": "boundedwait",
            "heading": "boundedWait",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function boundedWait(\n  task: PromiseLike<unknown>,\n  deadlineAt: number,\n  options?: { readonly scheduler?: ILifecycleScheduler } // 默认 systemScheduler\n): Promise<boolean>"
              },
              {
                "type": "paragraph",
                "text": "等到绝对截止时间为止，从不取消被等待的任务："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const won = await boundedWait(task, deadlineAt) // true=task 先完成；false=截止时间先到（task 仍在跑）"
              },
              {
                "type": "paragraph",
                "text": "`task`、`deadlineAt` 均必填（`deadlineAt` 是绝对时刻，经 `validateSchedulerTime` 校验）。无论超时与否都会挂一个 `.catch()` 观察 `task`，避免它日后 reject 变成未处理拒绝；`deadlineAt` 已经过去时立即返回 `false`（仍会先观察 `task`）。"
              }
            ]
          },
          {
            "id": "createterminalcontroller",
            "heading": "createTerminalController",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createTerminalController(): ITerminalController\n\ntype ITerminalController = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  close(): void\n  forceTerminal(): void\n  whenTerminal(): Promise<void>\n}"
              },
              {
                "type": "paragraph",
                "text": "独立复用的容器存活轴状态机，`createLifecycleScope`/`createSyncLifecycleScope`/`createLifecycleUnit` 内部都基于它："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const terminal = createTerminalController()\nterminal.close() // open → closing，幂等\nterminal.forceTerminal() // 直接进 terminal，resolve whenTerminal()\nawait terminal.whenTerminal()"
              },
              {
                "type": "paragraph",
                "text": "无构造参数。`close()`：同步幂等，只在 `open` 时生效（`closing`/`terminal` 时空操作）。`forceTerminal()`：同步幂等，直接到 `terminal`（跳过 `closing`）。`whenTerminal()`：返回同一个 Promise，只在到达 `terminal` 时 resolve 一次；`terminal` 之前调用不会立即 resolve。"
              }
            ]
          },
          {
            "id": "按需导入与-tree-shaking",
            "heading": "按需导入与 tree-shaking",
            "blocks": [
              {
                "type": "paragraph",
                "text": "根入口 `@migaia/lifecycle` 保持完整兼容；对 bundle retained graph敏感的消费者可使用以下稳定 subpath："
              },
              {
                "type": "table",
                "headers": ["Subpath", "主要导出"],
                "rows": [
                  [
                    "`@migaia/lifecycle/abort`",
                    "`createAbortController`、`observeAbortSubscription`、`IAbortSignal`、`IAbortController`、`IObservedAbortSubscription`、`IObservedAbortFailureSink`"
                  ],
                  ["`@migaia/lifecycle/scheduler`", "scheduler、snapshot与时间校验"],
                  ["`@migaia/lifecycle/quiescence`", "quiescence/lease/pending trackers"],
                  ["`@migaia/lifecycle/scope`", "`createLifecycleScope`"],
                  ["`@migaia/lifecycle/generation`", "`createGenerationController`"],
                  ["`@migaia/lifecycle/disposal`", "release transaction与sync-start ledger"],
                  ["`@migaia/lifecycle/errors`", "thenable/error helpers与error identity"]
                ]
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createAbortController } from '@migaia/lifecycle/abort'\nimport { createPendingTracker } from '@migaia/lifecycle/quiescence'\nimport { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'"
              },
              {
                "type": "paragraph",
                "text": "Root 与 subpath 都re-export preserve-modules产物中的同一实现，同名runtime function/object保持 `===`；没有第二份module-scoped state。module exports不开放 `dist/*` wildcard，内部文件不是公共API。"
              },
              {
                "type": "paragraph",
                "text": "`module.json` 声明 `sideEffects: false`，各模块import不会启动timer、登记listener或修改global。Tree-shaking是bundler行为：Node/Bun直接ESM import会解析依赖图，但不会替你删除未使用代码。验证按需效果时应检查Rollup/Vite retained modules或chunk内容；单独比较gzip总字节容易被压缩噪声误导。"
              },
              {
                "type": "paragraph",
                "text": "Leaf依赖仍会正常保留。例如 `/disposal` 必须保留thenable/error helper；“按需”只保证不把无关Scope、Generation或scheduler实现拉入，不保证单文件零依赖。"
              }
            ]
          },
          {
            "id": "错误基础设施模块",
            "heading": "错误基础设施模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
              }
            ]
          },
          {
            "id": "lifecycle-source",
            "heading": "LIFECYCLE_SOURCE",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "const LIFECYCLE_SOURCE: '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "本包所有错误的 `source` 字段固定值，字符串常量，无调用。"
              }
            ]
          },
          {
            "id": "createlifecycleerror",
            "heading": "createLifecycleError",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createLifecycleError(\n  code: string,\n  message: string,\n  options?: {\n    readonly cause?: unknown\n    readonly phase?: string\n    readonly detail?: Readonly<Record<string, unknown>>\n    readonly errors?: readonly unknown[]\n  }\n): ILifecycleError // Error & { source, code, phase?, detail?, cause?, errors? }"
              },
              {
                "type": "paragraph",
                "text": "构造一个携带 `(source, code)` 身份的原生 `Error`，从不改写 `stack`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
              },
              {
                "type": "paragraph",
                "text": "`code`、`message` 均必填（建议 `code` 取自 `LifecycleErrorCode`）。`options.errors` 非空时会冻结后挂到 `.errors`。"
              }
            ]
          },
          {
            "id": "createlifecyclerangeerror",
            "heading": "createLifecycleRangeError",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createLifecycleRangeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): RangeError"
              },
              {
                "type": "paragraph",
                "text": "同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
              },
              {
                "type": "paragraph",
                "text": "`code`、`message` 均必填。"
              }
            ]
          },
          {
            "id": "createlifecycletypeerror-createlifecyclefailure",
            "heading": "createLifecycleTypeError / createLifecycleFailure",
            "blocks": [
              {
                "type": "paragraph",
                "text": "二者只从 `@migaia/lifecycle/errors` leaf 导出："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nfunction createLifecycleTypeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): TypeError\n\nfunction createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError"
              },
              {
                "type": "paragraph",
                "text": "`createLifecycleTypeError` 创建保留原生 `TypeError` runtime type 的附码错误，并可携带 `cause`/`detail`。`createLifecycleFailure` 用在捕获边界：可扩展的原生 `Error` 会尽量原位附码并保持 `===` identity；primitive、冻结对象或不可重标记错误会被包装，原始抛出物仍可从 `cause` 到达。两者都不改写原始 `stack`。"
              }
            ]
          },
          {
            "id": "taglifecycleerror",
            "heading": "tagLifecycleError",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function tagLifecycleError<E extends Error>(error: E, code: string): E"
              },
              {
                "type": "paragraph",
                "text": "给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
              },
              {
                "type": "paragraph",
                "text": "`error`、`code` 均必填。返回同一个对象（原地修改，保留原生类型如 `AggregateError`）。"
              }
            ]
          },
          {
            "id": "containasyncrejection",
            "heading": "containAsyncRejection",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function containAsyncRejection(value: unknown, onRejected: (error: unknown) => void): void"
              },
              {
                "type": "paragraph",
                "text": "保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
              },
              {
                "type": "paragraph",
                "text": "`value`、`onRejected` 均必填。非 thenable 时直接返回，不调用 `onRejected`；`onRejected` 自身抛错被吞掉（最后一道错误边界）。"
              }
            ]
          },
          {
            "id": "probethenable",
            "heading": "probeThenable",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function probeThenable(value: unknown): IThenableProbe\n\ntype IThenableProbe =\n  | { readonly kind: 'not-thenable' }\n  | { readonly kind: 'thenable'; readonly thenFn: (resolve: unknown, reject: unknown) => void }\n  | { readonly kind: 'failed'; readonly error: unknown }"
              },
              {
                "type": "paragraph",
                "text": "只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
              },
              {
                "type": "paragraph",
                "text": "单参数 `value: unknown`，无选项。三种结果：`'not-thenable'`（`null`/非对象非函数，或 `.then` 不是函数）、`'thenable'`（捕获到 `thenFn`）、`'failed'`（读 `.then` 本身抛错，`error` 是原始抛出物）。"
              }
            ]
          },
          {
            "id": "assimilatecapturedthen",
            "heading": "assimilateCapturedThen",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function assimilateCapturedThen<T>(\n  thenFn: (resolve: unknown, reject: unknown) => void,\n  thenable: unknown\n): Promise<T>"
              },
              {
                "type": "paragraph",
                "text": "把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次（内部用 `Reflect.apply` 保持 `this === thenable`）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
              },
              {
                "type": "paragraph",
                "text": "`thenFn`（必填，须来自 `probeThenable` 的捕获结果）、`thenable`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
              }
            ]
          },
          {
            "id": "createerrorcollector",
            "heading": "createErrorCollector",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
              },
              {
                "type": "paragraph",
                "text": "按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
              },
              {
                "type": "paragraph",
                "text": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
              }
            ]
          },
          {
            "id": "状态常量模块",
            "heading": "状态常量模块",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  LifecycleState,\n  LifecycleUnitState,\n  LifecycleErrorPolicy,\n  ThenableProbeKind,\n  DisposeTransactionKind\n} from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "均为纯常量对象（`as const`），无调用参数，用于替代裸字符串字面量做比较/`switch`。"
              },
              {
                "type": "table",
                "headers": ["常量", "取值", "语义"],
                "rows": [
                  [
                    "`LifecycleState`",
                    "`{ open: 'open', closing: 'closing', terminal: 'terminal' }`",
                    "容器存活轴"
                  ],
                  [
                    "`LifecycleUnitState`",
                    "`{ idle: 'idle', loading: 'loading', loaded: 'loaded', failed: 'failed' }`",
                    "单元装载轴"
                  ],
                  [
                    "`LifecycleErrorPolicy`",
                    "`{ throw: 'throw', collect: 'collect', report: 'report', firstError: 'firstError' }`",
                    "释放错误策略；`firstError` 只为兼容 `web-rpc` discovery-registry 的迁移语义存在，不是通用默认值"
                  ],
                  [
                    "`ThenableProbeKind`",
                    "`{ notThenable: 'not-thenable', thenable: 'thenable', failed: 'failed', promise: 'promise' }`",
                    "`probeThenable`/内部归化逻辑的判别结果"
                  ],
                  [
                    "`DisposeTransactionKind`",
                    "`{ order: 'order', plan: 'plan' }`",
                    "`createDisposeTransaction` 的模式"
                  ]
                ]
              }
            ]
          },
          {
            "id": "错误码",
            "heading": "错误码",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { LifecycleErrorCode, type ILifecycleErrorCode } from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "稳定错误码表，20 个码，唯一声明处 `src/error-code.ts`，`source` 恒为 `'@migaia/lifecycle'`："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
              },
              {
                "type": "table",
                "headers": ["`LifecycleErrorCode` 键", "码值", "触发条件"],
                "rows": [
                  [
                    "`scopeClosed`",
                    "`SCOPE_CLOSED`",
                    "`close()` 之后调用 `own()`（异步/同步 scope）或某个 registry 的 `retain()`"
                  ],
                  [
                    "`scopeTerminal`",
                    "`SCOPE_TERMINAL`",
                    "容器已到达 `terminal` 之后，任何登记/激活操作"
                  ],
                  [
                    "`scopeReentrantDispose`",
                    "`SCOPE_REENTRANT_DISPOSE`",
                    "disposer 内部重入本 scope 的 `dispose()`"
                  ],
                  [
                    "`scopeReentrantOwn`",
                    "`SCOPE_REENTRANT_OWN`",
                    "disposer 内部调用本 scope 的 `own()`"
                  ],
                  [
                    "`scopeSyncViolation`",
                    "`SCOPE_SYNC_VIOLATION`",
                    "`SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor、收到一个异步 scope 实例作为资源、或某回调返回了 thenable"
                  ],
                  [
                    "`scopeDisposalFailed`",
                    "`SCOPE_DISPOSAL_FAILED`",
                    "`throw` 错误策略在多个资源释放失败时的 `AggregateError` 聚合出口"
                  ],
                  [
                    "`unitStartFailed`",
                    "`UNIT_START_FAILED`",
                    "`LifecycleUnit.start()` 返回的 thenable 被 reject（单元进入 `failed`）"
                  ],
                  [
                    "`generationSuperseded`",
                    "`GENERATION_SUPERSEDED`",
                    "一次异步操作的结果在 `adopt()` 提交点已被更晚的 generation 取代（非失败信号，仅诊断）"
                  ],
                  [
                    "`generationCancellationFailed`",
                    "`GENERATION_CANCELLATION_FAILED`",
                    "generation 取消期间的 timer、parent listener 或 signal cleanup 失败"
                  ],
                  [
                    "`generationDisposed`",
                    "`GENERATION_DISPOSED`",
                    "在已 `dispose()` 的 `GenerationController` 上调用 `begin()`"
                  ],
                  ["`quiescenceSealed`", "`QUIESCENCE_SEALED`", "`seal()` 之后又调用 `retain()`"],
                  [
                    "`quiescenceUnsealedWait`",
                    "`QUIESCENCE_UNSEALED_WAIT`",
                    "未先 `seal(key)` 就调用严格 `whenZero(key)`（同步抛出，非 rejected Promise）"
                  ],
                  [
                    "`provisionalSettled`",
                    "`PROVISIONAL_SETTLED`",
                    "同一个 `ProvisionalScope` 二次 `commitTo()`/`rollback()`，或交叉调用"
                  ],
                  [
                    "`provisionalParentClosed`",
                    "`PROVISIONAL_PARENT_CLOSED`",
                    "`commitTo()` 的目标 parent 已 `closing`/`terminal`，拒绝接收资源"
                  ],
                  [
                    "`queueAdmissionTimeout`",
                    "`QUEUE_ADMISSION_TIMEOUT`",
                    "mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`"
                  ],
                  [
                    "`queueSelfDependency`",
                    "`QUEUE_SELF_DEPENDENCY`",
                    "同一 `owner` 在自己尚未完成时又向同一队列提交新任务且被等待，会死锁"
                  ],
                  [
                    "`releaseForceFailed`",
                    "`RELEASE_FORCE_FAILED`",
                    "descriptor 的 `force` 抛出/reject（契约要求 `force` 必须无条件成功）"
                  ],
                  [
                    "`deadlineExceeded`",
                    "`DEADLINE_EXCEEDED`",
                    "共享绝对 deadline 已过去，但仍有资源被要求进入新的 graceful 阶段（诊断码，正常降级路径不抛这个码）"
                  ],
                  [
                    "`envUnsupported`",
                    "`ENV_UNSUPPORTED`",
                    "`systemScheduler` 依赖的宿主能力（`performance.now`/`setTimeout`/`clearTimeout`）缺失"
                  ],
                  [
                    "`invalidOption`",
                    "`INVALID_OPTION`",
                    "scheduler 收到非法时间/延迟参数，或 descriptor 字段类型非法"
                  ]
                ]
              },
              {
                "type": "paragraph",
                "text": "逐条设计动机见源码 `src/error-code.ts` 的 JSDoc；调用方应始终以 `error.code === LifecycleErrorCode.xxx` 判别，不要硬编码码值字符串。"
              }
            ]
          },
          {
            "id": "诊断消息",
            "heading": "诊断消息",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { LifecycleErrorText, type ILifecycleErrorText } from '@migaia/lifecycle'"
              },
              {
                "type": "paragraph",
                "text": "稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
              },
              {
                "type": "paragraph",
                "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
              },
              {
                "type": "paragraph",
                "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。每个键对应的文本值与语义见源码 `src/error-text.ts`。"
              }
            ]
          },
          {
            "id": "1-作用域-代数-请求场景下取代过期结果",
            "heading": "1. 作用域 + 代数：请求场景下取代过期结果",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleScope, createGenerationController } from '@migaia/lifecycle'\n\nconst scope = createLifecycleScope({ errorPolicy: 'collect' })\nconst generations = createGenerationController()\n\nasync function loadUser(id: string) {\n  const request = generations.begin({ timeoutMs: 5000 })\n  const user = await fetchUser(id, { signal: request.signal })\n  if (!generations.adopt(request.token, user, (u) => u.dispose?.())) return // 已过期，静默丢弃\n  scope.own(user, { force: () => user.dispose?.() })\n}"
              }
            ]
          },
          {
            "id": "2-事务性所有权-构造期失败自动回滚-成功后转移给长期-scope",
            "heading": "2. 事务性所有权：构造期失败自动回滚，成功后转移给长期 scope",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleScope, createProvisionalScope } from '@migaia/lifecycle'\n\nconst rootScope = createLifecycleScope()\n\nasync function setupModule() {\n  const provisional = createProvisionalScope()\n  try {\n    const db = provisional.own(await connectDb(), { force: (ctx) => closeDb(db) })\n    const cache = provisional.own(await connectCache(), { force: (ctx) => closeCache(cache) })\n    await provisional.commitTo(rootScope) // 全部转移给 rootScope，之后随 rootScope.dispose() 释放\n  } catch (error) {\n    await provisional.rollback() // 任一步失败：已注册的资源逆序释放，冒泡原错误\n    throw error\n  }\n}"
              }
            ]
          },
          {
            "id": "3-静默追踪-变更队列-确保排空后才提交下一批变更",
            "heading": "3. 静默追踪 + 变更队列：确保排空后才提交下一批变更",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createPendingTracker, createMutationQueue } from '@migaia/lifecycle'\n\nconst pending = createPendingTracker()\nconst queue = createMutationQueue({ queueAdmissionTimeoutMs: 3000 })\n\nasync function applyBatch(mutations: Array<() => Promise<void>>) {\n  for (const mutation of mutations) {\n    pending.track(queue.enqueue(mutation, { owner: 'batch' }))\n  }\n  await pending.drain() // 等到本批（含批内动态追加的）全部落地\n}"
              }
            ]
          },
          {
            "id": "4-释放事务-有界等待-手动编排一组资源的优雅关闭",
            "heading": "4. 释放事务 + 有界等待：手动编排一组资源的优雅关闭",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import {\n  createDisposeTransaction,\n  createManualScheduler,\n  type IDisposeItem\n} from '@migaia/lifecycle'\n\nconst scheduler = createManualScheduler()\nconst items: IDisposeItem[] = [\n  {\n    source: 'socket',\n    descriptor: {\n      order: 10,\n      graceful: (ctx) => flushSocket(),\n      gracefulTimeoutMs: 100,\n      force: () => forceCloseSocket()\n    }\n  },\n  { source: 'db', descriptor: { order: 5, force: () => closeDb() } }\n]\nconst transaction = createDisposeTransaction(\n  { kind: 'order' },\n  { errorPolicy: 'collect', scheduler }\n)\nconst runPromise = transaction.run(items)\nscheduler.advance(100) // 驱动 socket 的 graceful 超时降级到 force\nconst failures = await runPromise"
              }
            ]
          },
          {
            "id": "5-同步作用域-可暴露-using-语法的纯同步资源",
            "heading": "5. 同步作用域：可暴露 using 语法的纯同步资源",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createSyncLifecycleScope } from '@migaia/lifecycle'\n\nfunction withListeners() {\n  using scope = createSyncLifecycleScope() // 环境支持 `[Symbol.dispose]` 时可用 `using`\n  const handler = () => console.log('tick')\n  window.addEventListener('tick', handler)\n  scope.own(handler, { syncSafe: true, force: () => window.removeEventListener('tick', handler) })\n  // 作用域结束时自动 dispose()，移除监听器\n}"
              }
            ]
          },
          {
            "id": "6-同步入口-异步清理-registry-的-residual-disposer",
            "heading": "6. 同步入口 + 异步清理：Registry 的 residual disposer",
            "blocks": [
              {
                "type": "paragraph",
                "text": "`dispose()` 必须同步返回、但内部 disposer 可能返回 Promise 时，用 ledger 启动并统一观测；不要让每个 Registry 自造 pending/error 状态机："
              },
              {
                "type": "code",
                "language": "ts",
                "code": "import { createSyncStartedDisposalLedger } from '@migaia/lifecycle'\n\nconst ledger = createSyncStartedDisposalLedger()\n\nfunction disposeRegistry(): void {\n  ledger.start('cache', () => cache.dispose())\n  ledger.start('worker', () => worker.dispose())\n  ledger.seal()\n}\n\ndisposeRegistry()\nconst { synchronousErrors, completion } = ledger.seal()\nconst allErrors = await completion"
              },
              {
                "type": "paragraph",
                "text": "`synchronousErrors` 只含启动阶段立即失败；`completion` 归并同步与异步失败，并且永远 resolve raw errors，错误投影留给 Registry 的公开契约。"
              }
            ]
          },
          {
            "id": "7-服务退出-停止接单-等待在途任务-再释放资源",
            "heading": "7. 服务退出：停止接单，等待在途任务，再释放资源",
            "blocks": [
              {
                "type": "code",
                "language": "ts",
                "code": "import { createLifecycleScope, createPendingTracker } from '@migaia/lifecycle'\n\nconst scope = createLifecycleScope({ errorPolicy: 'collect' })\nconst pending = createPendingTracker()\n\nfunction handle(request: Request): Promise<Response> {\n  return pending.track(route(request))\n}\n\nasync function shutdown() {\n  await pending.drain()\n  return scope.dispose()\n}"
              },
              {
                "type": "paragraph",
                "text": "这里 `PendingTracker` 只负责“还有多少工作”，`LifecycleScope` 只负责“资源由谁释放”；职责不互相替代。"
              }
            ]
          },
          {
            "id": "全部公开-api-索引",
            "heading": "全部公开 API 索引",
            "blocks": [
              {
                "type": "paragraph",
                "text": "根入口 `@migaia/lifecycle` 可直接导入以下全部符号："
              },
              {
                "type": "list",
                "items": [
                  "构造/执行：`createAbortController`、`createSyncStartedDisposalLedger`、`systemScheduler`、`snapshotScheduler`、`createManualScheduler`、`validateSchedulerDelay`、`validateSchedulerTime`、`createLifecycleError`、`createLifecycleRangeError`、`tagLifecycleError`、`containAsyncRejection`、`probeThenable`、`assimilateCapturedThen`、`createErrorCollector`、`boundedWait`、`createTerminalController`、`createLifecycleScope`、`createSyncLifecycleScope`、`createLifecycleUnit`、`createGenerationController`、`createQuiescenceTracker`、`createStringQuiescenceTracker`、`createObjectLeaseRegistry`、`createStringLeaseRegistry`、`createPendingTracker`、`createProvisionalScope`、`createMutationQueue`、`executeReleaseDescriptor`、`createDisposeTransaction`。",
                  "常量：`LifecycleState`、`LifecycleUnitState`、`LifecycleErrorPolicy`、`ThenableProbeKind`、`DisposeTransactionKind`、`LifecycleErrorCode`、`LifecycleErrorText`、`LIFECYCLE_SOURCE`。",
                  "类型：`IDisposer`、`ILifecycleOwner`、`ILifecycleState`、`IUnitState`、`IDisposerContext`、`IReleaseContext`、`IReleaseDescriptor`、`ICollectedError`、`IErrorPolicy`、`ILifecycleStateValue`、`ILifecycleUnitStateValue`、`ILifecycleErrorPolicy`、`IThenableProbeKind`、`IDisposeTransactionKind`、`IAbortSignal`、`IAbortController`、`ISyncStartedDisposalLedger`、`ISyncStartedDisposalOutcome`、`ILifecycleErrorCode`、`ILifecycleErrorText`、`IScheduledTask`、`ILifecycleScheduler`、`ISchedulerSnapshot`、`IManualScheduler`、`ILifecycleError`、`IThenableProbe`、`IErrorCollector`、`ITerminalController`、`ILifecycleScope`、`ILifecycleScopeOptions`、`ISyncLifecycleScope`、`ISyncLifecycleScopeOptions`、`ISyncReleaseDescriptor`、`ILifecycleUnit`、`ILifecycleUnitOptions`、`IGenerationController`、`IGenerationControllerOptions`、`IGenerationRequest`、`IGenerationToken`、`IQuiescenceTracker`、`ILeaseRegistry`、`IPendingTracker`、`IProvisionalScope`、`IProvisionalScopeOptions`、`IMutationQueue`、`IMutationQueueOptions`、`IEnqueueOptions`、`IDisposeItem`、`IDisposeTransaction`、`IDisposeTransactionMode`、`IDisposeTransactionOptions`。"
                ]
              },
              {
                "type": "paragraph",
                "text": "Leaf entry 提供更小的按需边界；其中只有五项不在根入口：`@migaia/lifecycle/scheduler` 的 `addSchedulerTime`、`resolveScheduler`、`resolveSchedulerOption`，以及 `@migaia/lifecycle/errors` 的 `createLifecycleFailure`、`createLifecycleTypeError`。其余 leaf 符号都是根入口子集；完整入口映射见“按需导入与 tree-shaking”。"
              }
            ]
          },
          {
            "id": "排查与构建门禁",
            "heading": "排查与构建门禁",
            "blocks": [
              {
                "type": "list",
                "items": [
                  "`own()` 抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`：容器已进入 `closing`/`terminal`，不接受新登记；应新建 scope，而不是复用已关闭的实例。",
                  "`own()`/`dispose()` 抛 `SCOPE_REENTRANT_OWN`/`SCOPE_REENTRANT_DISPOSE`：disposer 回调内部直接调用了正在释放它的那个 scope 的 `own()`/`dispose()`；把这类逻辑挪到 disposer 之外，或改由外层协调。",
                  "`SyncLifecycleScope.own()` 抛 `SCOPE_SYNC_VIOLATION`：descriptor 缺少 `syncSafe: true`，或某回调返回了 thenable，或试图 own 一个异步 `LifecycleScope`/`ProvisionalScope` 实例；同步作用域绝不 await，把异步资源迁到 `createLifecycleScope()`。",
                  "`whenZero(key)` 同步抛 `QUIESCENCE_UNSEALED_WAIT`：忘记先 `seal(key)`；严格归零等待要求先切断新租约来源，改用 `whenZeroOnce()` 可跳过这一要求（但语义变为非独占）。",
                  "`GenerationController.begin()` 抛 `GENERATION_DISPOSED`：控制器已 `dispose()`，不可复用，需新建一个。",
                  "`commitTo()` 抛 `PROVISIONAL_PARENT_CLOSED`：目标 parent 已进入 `closing`/`terminal`；已转移的资源留在 parent 名下，未转移的部分已被自动释放，不需要再手动 `rollback()`。",
                  "`enqueue()` 抛 `QUEUE_SELF_DEPENDENCY`：同一 `owner` 在自身运行期间又提交并同步等待了新任务，FIFO 队列必然死锁；拆分成两次独立的顶层调用。",
                  "`systemScheduler` 抛 `ENV_UNSUPPORTED`：宿主缺 `performance.now`/`setTimeout`/`clearTimeout`；注入自实现的 `ILifecycleScheduler`，或改在具备这些能力的运行时里运行。",
                  "需要事件总线/当前值订阅/依赖图拓扑：`@migaia/lifecycle` 刻意不提供，应分别参考 `@migaia/event-subscriber`、`@migaia/reactive`、`@migaia/capability/graph`。"
                ]
              },
              {
                "type": "code",
                "language": "bash",
                "code": "pnpm run fmt && pnpm run lint && pnpm run typecheck && pnpm run typecheck:test && pnpm run test"
              },
              {
                "type": "paragraph",
                "text": "验收矩阵（L-T1 ~ L-T44）见 `docs/lifecycle/lifecycle-extraction.sdd.md` §5.4；每个 `L-T` 编号都能在 `test/` 下检索到对应用例。"
              }
            ]
          }
        ]
      }
    }
  },
  "apis": [
    {
      "id": "lifecycle:index",
      "library": "lifecycle",
      "module": "index",
      "exportPath": ".",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "boundedWait",
          "kind": "const",
          "signature": "export declare const boundedWait: (task: PromiseLike<unknown>, deadlineAt: number, options?: { scheduler?: ILifecycleScheduler; }) => Promise<boolean>;",
          "declarationLine": 11,
          "source": "packages/lifecycle/dist/bounded-wait.d.ts",
          "purpose": "boundedWait is part of this module's public const contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for boundedWait.",
          "advanced": "No additional advanced behavior is declared for boundedWait.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for boundedWait."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for boundedWait.",
          "examples": [
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "function boundedWait(\n  task: PromiseLike<unknown>,\n  deadlineAt: number,\n  options?: { readonly scheduler?: ILifecycleScheduler } // 默认 systemScheduler\n): Promise<boolean>",
            "const won = await boundedWait(task, deadlineAt) // true=task 先完成；false=截止时间先到（task 仍在跑）",
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "boundedWait",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const boundedWait: (task: PromiseLike<unknown>, deadlineAt: number, options?: { scheduler?: ILifecycleScheduler; }) => Promise<boolean>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use boundedWait when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when boundedWait does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "boundedWait is part of this module's public const contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import boundedWait from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use boundedWait when its const contract and declared inputs match the result you need. Choose a different public symbol when boundedWait does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for boundedWait."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for boundedWait."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/bounded-wait.d.ts:boundedWait:export declare const boundedWait: (task: PromiseLike<unknown>, deadlineAt: number, options?: { scheduler?: ILifecycleScheduler; }) => Promise<boolean>;",
          "guidance": [
            {
              "id": "boundedwait",
              "heading": "boundedWait",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function boundedWait(\n  task: PromiseLike<unknown>,\n  deadlineAt: number,\n  options?: { readonly scheduler?: ILifecycleScheduler } // 默认 systemScheduler\n): Promise<boolean>"
                },
                {
                  "type": "paragraph",
                  "text": "等到绝对截止时间为止，从不取消被等待的任务："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const won = await boundedWait(task, deadlineAt) // true=task 先完成；false=截止时间先到（task 仍在跑）"
                },
                {
                  "type": "paragraph",
                  "text": "`task`、`deadlineAt` 均必填（`deadlineAt` 是绝对时刻，经 `validateSchedulerTime` 校验）。无论超时与否都会挂一个 `.catch()` 观察 `task`，避免它日后 reject 变成未处理拒绝；`deadlineAt` 已经过去时立即返回 `false`（仍会先观察 `task`）。"
                }
              ]
            }
          ],
          "usageScore": 58,
          "configuration": [],
          "fragment": "lifecycle-index--boundedWait"
        },
        {
          "name": "createLifecycleUnit",
          "kind": "function",
          "signature": "export declare function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>;",
          "declarationLine": 32,
          "source": "packages/lifecycle/dist/lifecycle-unit.d.ts",
          "purpose": "createLifecycleUnit is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleUnit.",
          "advanced": "No additional advanced behavior is declared for createLifecycleUnit.",
          "parameters": ["options: ILifecycleUnitOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "ILifecycleUnitOptions",
              "optional": true
            }
          ],
          "returns": "ILifecycleUnit<T>",
          "errors": ["No documented errors are declared for createLifecycleUnit."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createLifecycleUnit.",
          "examples": [
            "const unit = createLifecycleUnit<Config>()\nunit.start(() => fetchConfig()) // thenable → 先进 'loading'，settle 后进 'loaded'/'failed'\nunit.state // 'loading'\nunit.value // undefined（settle 前）\nunit.restart(() => fetchConfig()) // 新 generation，旧一次的迟到结果被静默丢弃"
          ],
          "aliasOf": "createLifecycleUnit",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "ILifecycleUnitOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["ILifecycleUnit<T>"],
          "whenToUse": "Use createLifecycleUnit when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createLifecycleUnit does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createLifecycleUnit is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createLifecycleUnit from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createLifecycleUnit when its function contract and declared inputs match the result you need. Choose a different public symbol when createLifecycleUnit does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleUnit."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createLifecycleUnit."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": "Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity."
            }
          ],
          "identity": "packages/lifecycle/dist/lifecycle-unit.d.ts:createLifecycleUnit:export declare function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>;",
          "guidance": [
            {
              "id": "createlifecycleunit",
              "heading": "createLifecycleUnit",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>\n\ntype ILifecycleUnitOptions = { readonly report?: (error: unknown) => void }\n\ntype ILifecycleUnit<T> = {\n  readonly state: 'idle' | 'loading' | 'loaded' | 'failed'\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly value: T | undefined\n  readonly error: unknown\n  start(factory: () => T | PromiseLike<T>): void\n  restart(factory: () => T | PromiseLike<T>): void // start 的别名\n  close(): void\n  dispose(): void // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单元装载轴的状态机，内部用一个 `GenerationController` 丢弃过期结果："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const unit = createLifecycleUnit<Config>()\nunit.start(() => fetchConfig()) // thenable → 先进 'loading'，settle 后进 'loaded'/'failed'\nunit.state // 'loading'\nunit.value // undefined（settle 前）\nunit.restart(() => fetchConfig()) // 新 generation，旧一次的迟到结果被静默丢弃"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`factory()` 只读一次其返回值的 `.then`（探测语义同 `probeThenable`）：同步非 thenable 返回直接落 `loaded`（跳过 `loading`）；`factory()` 同步抛出或 thenable reject 时进入 `failed`，`unit.error` 存原始错误（不打标签），同时额外把一份打上 `UNIT_START_FAILED` 标签的错误发给 `options.report`（若提供）。",
                    "每次 `start()`/`restart()` 隐式开启新 generation；旧 generation 的迟到 settle 结果被静默丢弃（不写入 `state`/`value`/`error`）。",
                    "`lifecycle` 是容器存活轴（`close()`/`dispose()` 驱动），与 `state`（装载轴）是两个独立读值。",
                    "`dispose()` 永远同步——单元本身不持有需要等待的资源，只是终结内部 generation controller（超过一次调用幂等）。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 66,
          "configuration": [
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": "`factory()` 只读一次其返回值的 `.then`（探测语义同 `probeThenable`）：同步非 thenable 返回直接落 `loaded`（跳过 `loading`）；`factory()` 同步抛出或 thenable reject 时进入 `failed`，`unit.error` 存原始错误（不打标签），同时额外把一份打上 `UNIT_START_FAILED` 标签的错误发给 `options.report`（若提供）。",
              "descriptionEn": "Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity.",
              "descriptionZh": "`factory()` 只读一次其返回值的 `.then`（探测语义同 `probeThenable`）：同步非 thenable 返回直接落 `loaded`（跳过 `loading`）；`factory()` 同步抛出或 thenable reject 时进入 `failed`，`unit.error` 存原始错误（不打标签），同时额外把一份打上 `UNIT_START_FAILED` 标签的错误发给 `options.report`（若提供）。"
            }
          ],
          "fragment": "lifecycle-index--createLifecycleUnit"
        },
        {
          "name": "createMutationQueue",
          "kind": "function",
          "signature": "export declare function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue;",
          "declarationLine": 37,
          "source": "packages/lifecycle/dist/mutation-queue.d.ts",
          "purpose": "createMutationQueue is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createMutationQueue.",
          "advanced": "No additional advanced behavior is declared for createMutationQueue.",
          "parameters": ["options: IMutationQueueOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "IMutationQueueOptions",
              "optional": true
            }
          ],
          "returns": "IMutationQueue",
          "errors": ["No documented errors are declared for createMutationQueue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createMutationQueue.",
          "examples": [
            "const queue = createMutationQueue({ queueAdmissionTimeoutMs: 5000 })\nconst result = await queue.enqueue(() => applyMutation(), { owner: 'sync-loop' })\nqueue.size // 排队中 + 正在执行的任务数（0 或 1）",
            "import { createPendingTracker, createMutationQueue } from '@migaia/lifecycle'\n\nconst pending = createPendingTracker()\nconst queue = createMutationQueue({ queueAdmissionTimeoutMs: 3000 })\n\nasync function applyBatch(mutations: Array<() => Promise<void>>) {\n  for (const mutation of mutations) {\n    pending.track(queue.enqueue(mutation, { owner: 'batch' }))\n  }\n  await pending.drain() // 等到本批（含批内动态追加的）全部落地\n}",
            "const queue = createMutationQueue({ queueAdmissionTimeoutMs: 5000 })\nconst result = await queue.enqueue(() => applyMutation(), { owner: 'sync-loop' })\nqueue.size // 排队中 + 正在执行的任务数"
          ],
          "aliasOf": "createMutationQueue",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "IMutationQueueOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IMutationQueue"],
          "whenToUse": "Use createMutationQueue when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createMutationQueue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createMutationQueue is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createMutationQueue from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createMutationQueue when its function contract and declared inputs match the result you need. Choose a different public symbol when createMutationQueue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createMutationQueue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createMutationQueue."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "queueAdmissionTimeoutMs",
              "type": "number | false",
              "optional": true,
              "description": "Default per-task admission budget. `undefined` (default) means \"diagnose only, never reject\" — * the core never invents a domain timeout. `false` disables even diagnostics. A number is the * `plugin-host`-equivalent behavior: reject once a queued task has waited this long."
            },
            {
              "name": "admissionDiagnosticMs",
              "type": "number | false",
              "optional": true,
              "description": "Diagnostic-only threshold used while `queueAdmissionTimeoutMs` is `undefined`. `false` disables * the diagnostic timer."
            },
            {
              "name": "onAdmissionDiagnostic",
              "type": "(info: { readonly owner: string | undefined; readonly waitedMs: number; }) => void",
              "optional": true,
              "description": ""
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "Runtime-neutral scheduler（默认 `systemScheduler`）；watchdog 计时与时钟都经它，不直接用宿主 timer。"
            }
          ],
          "identity": "packages/lifecycle/dist/mutation-queue.d.ts:createMutationQueue:export declare function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue;",
          "guidance": [
            {
              "id": "createmutationqueue",
              "heading": "createMutationQueue",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue\n\ntype IMutationQueueOptions = {\n  readonly queueAdmissionTimeoutMs?: number | false // 默认 undefined（只诊断，从不因排队超时而拒绝）\n  readonly admissionDiagnosticMs?: number | false // 默认 1000\n  readonly onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IEnqueueOptions = {\n  readonly owner?: string\n  readonly queueAdmissionTimeoutMs?: number | false // 覆盖本次任务的队列默认值\n}\n\ntype IMutationQueue = {\n  enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>\n  readonly size: number\n}"
                },
                {
                  "type": "paragraph",
                  "text": "严格 FIFO 串行队列，无并发池，带可选的入队 SLA 看门狗："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const queue = createMutationQueue({ queueAdmissionTimeoutMs: 5000 })\nconst result = await queue.enqueue(() => applyMutation(), { owner: 'sync-loop' })\nqueue.size // 排队中 + 正在执行的任务数（0 或 1）"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`queueAdmissionTimeoutMs`：数字表示任务在队列中等待超过这个时长即被移出队列并以 `QUEUE_ADMISSION_TIMEOUT` reject；`undefined`（默认）只诊断、从不因排队超时而拒绝；`false` 连诊断计时器也不开。",
                    "`admissionDiagnosticMs`：仅在 `queueAdmissionTimeoutMs` 为 `undefined` 且提供了 `onAdmissionDiagnostic` 时生效的诊断阈值；`false` 关闭诊断计时器。",
                    "`owner`：用于自依赖检测——同一 `owner` 在自己正在运行时又向同一队列提交新任务，会立即以 `QUEUE_SELF_DEPENDENCY` reject（因为 FIFO 队列不可能在当前任务完成前跑新任务）。这是部分防护：正在运行的任务提交一个不同/无 `owner` 的任务并原地 `await` 它同样会死锁，但没有可靠信号能检测这种情况，因此不拦截；安全模式是提交后续任务而不在当前任务内 `await` 它。",
                    "`size`：只读，等于排队数加运行中的 0/1。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 84,
          "configuration": [
            {
              "name": "queueAdmissionTimeoutMs",
              "type": "number | false",
              "optional": true,
              "description": "`queueAdmissionTimeoutMs`：数字表示任务在队列中等待超过这个时长即被移出队列并以 `QUEUE_ADMISSION_TIMEOUT` reject；`undefined`（默认）只诊断、从不因排队超时而拒绝；`false` 连诊断计时器也不开。",
              "descriptionEn": "Default per-task admission budget. `undefined` (default) means \"diagnose only, never reject\" — * the core never invents a domain timeout. `false` disables even diagnostics. A number is the * `plugin-host`-equivalent behavior: reject once a queued task has waited this long.",
              "descriptionZh": "`queueAdmissionTimeoutMs`：数字表示任务在队列中等待超过这个时长即被移出队列并以 `QUEUE_ADMISSION_TIMEOUT` reject；`undefined`（默认）只诊断、从不因排队超时而拒绝；`false` 连诊断计时器也不开。"
            },
            {
              "name": "admissionDiagnosticMs",
              "type": "number | false",
              "optional": true,
              "description": "`admissionDiagnosticMs`：仅在 `queueAdmissionTimeoutMs` 为 `undefined` 且提供了 `onAdmissionDiagnostic` 时生效的诊断阈值；`false` 关闭诊断计时器。",
              "descriptionEn": "Diagnostic-only threshold used while `queueAdmissionTimeoutMs` is `undefined`. `false` disables * the diagnostic timer.",
              "descriptionZh": "`admissionDiagnosticMs`：仅在 `queueAdmissionTimeoutMs` 为 `undefined` 且提供了 `onAdmissionDiagnostic` 时生效的诊断阈值；`false` 关闭诊断计时器。"
            },
            {
              "name": "onAdmissionDiagnostic",
              "type": "(info: { readonly owner: string | undefined; readonly waitedMs: number; }) => void",
              "optional": true,
              "description": "`admissionDiagnosticMs`：仅在 `queueAdmissionTimeoutMs` 为 `undefined` 且提供了 `onAdmissionDiagnostic` 时生效的诊断阈值；`false` 关闭诊断计时器。",
              "descriptionEn": "",
              "descriptionZh": "`admissionDiagnosticMs`：仅在 `queueAdmissionTimeoutMs` 为 `undefined` 且提供了 `onAdmissionDiagnostic` 时生效的诊断阈值；`false` 关闭诊断计时器。"
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "Leaf entry 提供更小的按需边界；其中只有五项不在根入口：`@migaia/lifecycle/scheduler` 的 `addSchedulerTime`、`resolveScheduler`、`resolveSchedulerOption`，以及 `@migaia/lifecycle/errors` 的 `createLifecycleFailure`、`createLifecycleTypeError`。其余 leaf 符号都是根入口子集；完整入口映射见“按需导入与 tree-shaking”。",
              "descriptionEn": "",
              "descriptionZh": "Leaf entry 提供更小的按需边界；其中只有五项不在根入口：`@migaia/lifecycle/scheduler` 的 `addSchedulerTime`、`resolveScheduler`、`resolveSchedulerOption`，以及 `@migaia/lifecycle/errors` 的 `createLifecycleFailure`、`createLifecycleTypeError`。其余 leaf 符号都是根入口子集；完整入口映射见“按需导入与 tree-shaking”。"
            }
          ],
          "fragment": "lifecycle-index--createMutationQueue"
        },
        {
          "name": "createProvisionalScope",
          "kind": "function",
          "signature": "export declare function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope;",
          "declarationLine": 29,
          "source": "packages/lifecycle/dist/provisional-scope.d.ts",
          "purpose": "A two-phase ownership transaction for async `setup()` work: resources accumulate here first, then either move to a real owner via prefix transfer + awaited compensation (`commitTo`) or get torn down (`rollback`) — never both, never neither (§4.9).",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createProvisionalScope.",
          "advanced": "No additional advanced behavior is declared for createProvisionalScope.",
          "parameters": ["options: IProvisionalScopeOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "IProvisionalScopeOptions",
              "optional": true
            }
          ],
          "returns": "IProvisionalScope",
          "errors": ["No documented errors are declared for createProvisionalScope."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createProvisionalScope.",
          "examples": [
            "const shutdown = new AbortController()\nconst provisional = createProvisionalScope({ parentSignal: shutdown.signal })\nconst db = provisional.own(await connect(), { force: (ctx) => db.close() })\ntry {\n  await provisional.commitTo(outerScope) // 按注册顺序逐个 own() 到 parent\n} catch (error) {\n  // parent 中途拒绝（如已在 closing）：已转移部分留在 parent 名下，剩余部分已被自动释放并 await 完成\n}",
            "import { createLifecycleScope, createProvisionalScope } from '@migaia/lifecycle'\n\nconst rootScope = createLifecycleScope()\n\nasync function setupModule() {\n  const provisional = createProvisionalScope()\n  try {\n    const db = provisional.own(await connectDb(), { force: (ctx) => closeDb(db) })\n    const cache = provisional.own(await connectCache(), { force: (ctx) => closeCache(cache) })\n    await provisional.commitTo(rootScope) // 全部转移给 rootScope，之后随 rootScope.dispose() 释放\n  } catch (error) {\n    await provisional.rollback() // 任一步失败：已注册的资源逆序释放，冒泡原错误\n    throw error\n  }\n}",
            "const provisional = createProvisionalScope()\nconst db = provisional.own(await connect(), { force: (ctx) => db.close() })\ntry {\n  await provisional.commitTo(outerScope) // 按注册顺序逐个 own() 到 parent\n} catch (error) {\n  // parent 中途拒绝（如已在 closing）：已转移部分留在 parent 名下，剩余部分已被自动释放并 await 完成\n}"
          ],
          "aliasOf": "createProvisionalScope",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "IProvisionalScopeOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IProvisionalScope"],
          "whenToUse": "Use createProvisionalScope when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createProvisionalScope does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "A two-phase ownership transaction for async `setup()` work: resources accumulate here first, then either move to a real owner via prefix transfer + awaited compensation (`commitTo`) or get torn down (`rollback`) — never both, never neither (§4.9)."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createProvisionalScope from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createProvisionalScope when its function contract and declared inputs match the result you need. Choose a different public symbol when createProvisionalScope does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createProvisionalScope."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createProvisionalScope."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "parentSignal",
              "type": "IAbortSignal",
              "optional": true,
              "description": "Forwarded so `signal` also reflects a parent's abort (e.g. a superseded generation)."
            }
          ],
          "identity": "packages/lifecycle/dist/provisional-scope.d.ts:createProvisionalScope:export declare function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope;",
          "guidance": [
            {
              "id": "createprovisionalscope",
              "heading": "createProvisionalScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope\n\ntype IProvisionalScopeOptions = { readonly parentSignal?: IAbortSignal }\n\ntype IProvisionalScope = {\n  readonly signal: IAbortSignal\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  commitTo(parent: ILifecycleOwner): Promise<void>\n  rollback(): Promise<void>\n}\n\ntype ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T }"
                },
                {
                  "type": "paragraph",
                  "text": "构造期两阶段所有权：资源先暂存这里，最终要么 `commitTo(parent)` 转移给真正的 owner，要么 `rollback()` 释放，二选一、只能选一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const shutdown = new AbortController()\nconst provisional = createProvisionalScope({ parentSignal: shutdown.signal })\nconst db = provisional.own(await connect(), { force: (ctx) => db.close() })\ntry {\n  await provisional.commitTo(outerScope) // 按注册顺序逐个 own() 到 parent\n} catch (error) {\n  // parent 中途拒绝（如已在 closing）：已转移部分留在 parent 名下，剩余部分已被自动释放并 await 完成\n}"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：已 commit/rollback 后调用抛 `PROVISIONAL_SETTLED`。",
                    "`commitTo(parent)`：按注册顺序逐个调用 `parent.own(resource, descriptor)`；某项失败时，已转移给 parent 的部分保留在 parent 名下，尚未转移的部分由本作用域逆序释放并 await 完成后才重新抛出原错误——不会有资源被两边都不持有，也不会有资源留给\"none\"；若失败原因是 `parent` 已处于 `closing`/`terminal`（错误码为 `SCOPE_CLOSED`/`SCOPE_TERMINAL`），额外包一层 `PROVISIONAL_PARENT_CLOSED`，原错误仍在 `cause`，遗留资源的释放失败列表挂在 `errors`。",
                    "`rollback()`：逆序释放全部已注册资源，聚合失败为携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；幂等——并发/重复调用复用同一个 settle Promise。",
                    "`commitTo`/`rollback` 二次调用（或 commit 后 rollback / rollback 后 commit）一律抛 `PROVISIONAL_SETTLED`（`rollback()` 以 rejected Promise 形式返回，不是同步抛出）。",
                    "`parentSignal` 中止时（构造时已中止，或之后中止），`signal` 同步跟随中止，但不会自动触发 rollback/commit——仍需调用方显式二选一。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 84,
          "configuration": [
            {
              "name": "parentSignal",
              "type": "IAbortSignal",
              "optional": true,
              "description": "`parentSignal` 中止时（构造时已中止，或之后中止），`signal` 同步跟随中止，但不会自动触发 rollback/commit——仍需调用方显式二选一。",
              "descriptionEn": "Forwarded so `signal` also reflects a parent's abort (e.g. a superseded generation).",
              "descriptionZh": "`parentSignal` 中止时（构造时已中止，或之后中止），`signal` 同步跟随中止，但不会自动触发 rollback/commit——仍需调用方显式二选一。"
            }
          ],
          "fragment": "lifecycle-index--createProvisionalScope"
        },
        {
          "name": "createSyncLifecycleScope",
          "kind": "function",
          "signature": "export declare function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope;",
          "declarationLine": 23,
          "source": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts",
          "purpose": "The `syncSafe`-only scope. Exists so a scope that owns nothing but reactive-graph-style nodes can offer a real, deterministic `[Symbol.dispose]` instead of the general `LifecycleScope`'s always-async `[Symbol.asyncDispose]` (D-6).",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createSyncLifecycleScope.",
          "advanced": "No additional advanced behavior is declared for createSyncLifecycleScope.",
          "parameters": ["options: ISyncLifecycleScopeOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "ISyncLifecycleScopeOptions",
              "optional": true
            }
          ],
          "returns": "ISyncLifecycleScope",
          "errors": ["No documented errors are declared for createSyncLifecycleScope."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createSyncLifecycleScope.",
          "examples": [
            "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回",
            "import { createSyncLifecycleScope } from '@migaia/lifecycle'\n\nfunction withListeners() {\n  using scope = createSyncLifecycleScope() // 环境支持 `[Symbol.dispose]` 时可用 `using`\n  const handler = () => console.log('tick')\n  window.addEventListener('tick', handler)\n  scope.own(handler, { syncSafe: true, force: () => window.removeEventListener('tick', handler) })\n  // 作用域结束时自动 dispose()，移除监听器\n}",
            "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回，不可能有异步资源"
          ],
          "aliasOf": "createSyncLifecycleScope",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "ISyncLifecycleScopeOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["ISyncLifecycleScope"],
          "whenToUse": "Use createSyncLifecycleScope when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createSyncLifecycleScope does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "The `syncSafe`-only scope. Exists so a scope that owns nothing but reactive-graph-style nodes can offer a real, deterministic `[Symbol.dispose]` instead of the general `LifecycleScope`'s always-async `[Symbol.asyncDispose]` (D-6)."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createSyncLifecycleScope from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createSyncLifecycleScope when its function contract and declared inputs match the result you need. Choose a different public symbol when createSyncLifecycleScope does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createSyncLifecycleScope."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createSyncLifecycleScope."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "errorPolicy",
              "type": "IErrorPolicy",
              "optional": true,
              "description": ""
            },
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": ""
            }
          ],
          "identity": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts:createSyncLifecycleScope:export declare function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope;",
          "guidance": [
            {
              "id": "createsynclifecyclescope",
              "heading": "createSyncLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "纯同步容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回"
                },
                {
                  "type": "paragraph",
                  "text": "约束与边界："
                },
                {
                  "type": "list",
                  "items": [
                    "每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`。",
                    "不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例（内部品牌检测），命中同样抛 `SCOPE_SYNC_VIOLATION`。",
                    "某个 callback（`custom`/`graceful`/`force`）若返回 thenable，在释放当次同步探测到并抛 `SCOPE_SYNC_VIOLATION`（`graceful` 违规时会继续跑 `force`，`force`/`custom` 违规则该项失败计入当前错误策略）。",
                    "降级链与异步版本相同（`custom` → 否则 `graceful` → `force`），但没有超时竞速——没有可等待的对象。",
                    "`release()`/`close()` 语义与异步版本一致；`dispose()` 幂等，二次调用返回 `[]`；在 disposer 内部重入抛 `SCOPE_REENTRANT_DISPOSE`。",
                    "环境支持时挂载 `[Symbol.dispose]`，可配合 `using` 语法。"
                  ]
                }
              ]
            },
            {
              "id": "createerrorcollector",
              "heading": "createErrorCollector",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
                },
                {
                  "type": "paragraph",
                  "text": "按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 96,
          "configuration": [
            {
              "name": "errorPolicy",
              "type": "IErrorPolicy",
              "optional": true,
              "description": "`errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError'` —— 默认 `'throw'`",
              "descriptionEn": "",
              "descriptionZh": "`errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError'` —— 默认 `'throw'`"
            },
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。",
              "descriptionEn": "Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity.",
              "descriptionZh": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
            }
          ],
          "fragment": "lifecycle-index--createSyncLifecycleScope"
        },
        {
          "name": "createTerminalController",
          "kind": "function",
          "signature": "export declare function createTerminalController(): ITerminalController;",
          "declarationLine": 16,
          "source": "packages/lifecycle/dist/terminal-controller.d.ts",
          "purpose": "createTerminalController is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createTerminalController.",
          "advanced": "No additional advanced behavior is declared for createTerminalController.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "ITerminalController",
          "errors": ["No documented errors are declared for createTerminalController."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createTerminalController.",
          "examples": [
            "const terminal = createTerminalController()\nterminal.close() // open → closing，幂等\nterminal.forceTerminal() // 直接进 terminal，resolve whenTerminal()\nawait terminal.whenTerminal()"
          ],
          "aliasOf": "createTerminalController",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare function createTerminalController(): ITerminalController;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["ITerminalController"],
          "whenToUse": "Use createTerminalController when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createTerminalController does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createTerminalController is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createTerminalController from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createTerminalController when its function contract and declared inputs match the result you need. Choose a different public symbol when createTerminalController does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createTerminalController."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createTerminalController."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/terminal-controller.d.ts:createTerminalController:export declare function createTerminalController(): ITerminalController;",
          "guidance": [
            {
              "id": "createterminalcontroller",
              "heading": "createTerminalController",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createTerminalController(): ITerminalController\n\ntype ITerminalController = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  close(): void\n  forceTerminal(): void\n  whenTerminal(): Promise<void>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "独立复用的容器存活轴状态机，`createLifecycleScope`/`createSyncLifecycleScope`/`createLifecycleUnit` 内部都基于它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const terminal = createTerminalController()\nterminal.close() // open → closing，幂等\nterminal.forceTerminal() // 直接进 terminal，resolve whenTerminal()\nawait terminal.whenTerminal()"
                },
                {
                  "type": "paragraph",
                  "text": "无构造参数。`close()`：同步幂等，只在 `open` 时生效（`closing`/`terminal` 时空操作）。`forceTerminal()`：同步幂等，直接到 `terminal`（跳过 `closing`）。`whenTerminal()`：返回同一个 Promise，只在到达 `terminal` 时 resolve 一次；`terminal` 之前调用不会立即 resolve。"
                }
              ]
            }
          ],
          "usageScore": 58,
          "configuration": [],
          "fragment": "lifecycle-index--createTerminalController"
        },
        {
          "name": "DisposeTransactionKind",
          "kind": "const",
          "signature": "export declare const DisposeTransactionKind: { readonly order: \"order\"; readonly plan: \"plan\"; };",
          "declarationLine": 29,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "Disposal transaction planning modes.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for DisposeTransactionKind.",
          "advanced": "No additional advanced behavior is declared for DisposeTransactionKind.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for DisposeTransactionKind."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for DisposeTransactionKind.",
          "examples": [
            "import {\n  LifecycleState,\n  LifecycleUnitState,\n  LifecycleErrorPolicy,\n  ThenableProbeKind,\n  DisposeTransactionKind\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "DisposeTransactionKind",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const DisposeTransactionKind: { readonly order: \"order\"; readonly plan: \"plan\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use DisposeTransactionKind when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when DisposeTransactionKind does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Disposal transaction planning modes."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import DisposeTransactionKind from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use DisposeTransactionKind when its const contract and declared inputs match the result you need. Choose a different public symbol when DisposeTransactionKind does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for DisposeTransactionKind."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for DisposeTransactionKind."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:DisposeTransactionKind:export declare const DisposeTransactionKind: { readonly order: \"order\"; readonly plan: \"plan\"; };",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "lifecycle-index--DisposeTransactionKind"
        },
        {
          "name": "ICollectedError",
          "kind": "type",
          "signature": "export type ICollectedError = { readonly source: string; readonly error: unknown; };",
          "declarationLine": 100,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "One release failure, labeled by the caller-supplied `source` identity for that item.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ICollectedError.",
          "advanced": "No additional advanced behavior is declared for ICollectedError.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ICollectedError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ICollectedError.",
          "examples": [
            "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'",
            "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}",
            "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}",
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}",
            "function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger\n\ntype ISyncStartedDisposalLedger = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly pending: number\n  start(source: string, callback: () => unknown): void\n  seal(): ISyncStartedDisposalOutcome\n  whenTerminal(): Promise<void>\n}\n\ntype ISyncStartedDisposalOutcome = Readonly<{\n  synchronousErrors: readonly ICollectedError[]\n  completion: Promise<readonly ICollectedError[]>\n}>",
            "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}",
            "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
          ],
          "aliasOf": "ICollectedError",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ICollectedError = { readonly source: string; readonly error: unknown; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ICollectedError when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ICollectedError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "One release failure, labeled by the caller-supplied `source` identity for that item."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ICollectedError from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ICollectedError when its type contract and declared inputs match the result you need. Choose a different public symbol when ICollectedError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ICollectedError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ICollectedError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:ICollectedError:export type ICollectedError = { readonly source: string; readonly error: unknown; };",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            },
            {
              "id": "createsynclifecyclescope",
              "heading": "createSyncLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "纯同步容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回"
                },
                {
                  "type": "paragraph",
                  "text": "约束与边界："
                },
                {
                  "type": "list",
                  "items": [
                    "每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`。",
                    "不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例（内部品牌检测），命中同样抛 `SCOPE_SYNC_VIOLATION`。",
                    "某个 callback（`custom`/`graceful`/`force`）若返回 thenable，在释放当次同步探测到并抛 `SCOPE_SYNC_VIOLATION`（`graceful` 违规时会继续跑 `force`，`force`/`custom` 违规则该项失败计入当前错误策略）。",
                    "降级链与异步版本相同（`custom` → 否则 `graceful` → `force`），但没有超时竞速——没有可等待的对象。",
                    "`release()`/`close()` 语义与异步版本一致；`dispose()` 幂等，二次调用返回 `[]`；在 disposer 内部重入抛 `SCOPE_REENTRANT_DISPOSE`。",
                    "环境支持时挂载 `[Symbol.dispose]`，可配合 `using` 语法。"
                  ]
                }
              ]
            },
            {
              "id": "createerrorcollector",
              "heading": "createErrorCollector",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
                },
                {
                  "type": "paragraph",
                  "text": "按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 88,
          "configuration": [],
          "fragment": "lifecycle-index--ICollectedError"
        },
        {
          "name": "IDisposer",
          "kind": "type",
          "signature": "export type IDisposer = () => void;",
          "declarationLine": 12,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "Synchronous, idempotent teardown/unregister function. Declared locally rather than imported from `@migaia/reactive` — this module is a zero-dependency leaf (`docs/lifecycle/lifecycle-extraction.sdd.md` §1.1) and must not pull in any other workspace module just to share a structural type.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposer.",
          "advanced": "No additional advanced behavior is declared for IDisposer.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDisposer."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDisposer.",
          "examples": [
            "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'",
            "function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>\nfunction createStringQuiescenceTracker(): IQuiescenceTracker<string>\n\ntype IQuiescenceTracker<TKey> = {\n  retain(key: TKey): IDisposer // IDisposer = () => void\n  count(key: TKey): number\n  whenZero(key: TKey): Promise<void> // 严格独占等待\n  whenZeroOnce(key: TKey): Promise<void> // 非独占等待\n  seal(key: TKey): void\n  isSealed(key: TKey): boolean\n  forget(key: TKey): boolean\n}"
          ],
          "aliasOf": "IDisposer",
          "exportPath": ".",
          "overloadSignatures": ["export type IDisposer = () => void;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDisposer when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDisposer does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Synchronous, idempotent teardown/unregister function. Declared locally rather than imported from `@migaia/reactive` — this module is a zero-dependency leaf (`docs/lifecycle/lifecycle-extraction.sdd.md` §1.1) and must not pull in any other workspace module just to share a structural type."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDisposer from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDisposer when its type contract and declared inputs match the result you need. Choose a different public symbol when IDisposer does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposer."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDisposer."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:IDisposer:export type IDisposer = () => void;",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "lifecycle-index--IDisposer"
        },
        {
          "name": "IDisposerContext",
          "kind": "type",
          "signature": "export type IDisposerContext = { /** Rejects an attempt to join the disposal currently waiting for this callback. */ readonly join: () => never; };",
          "declarationLine": 6,
          "source": "packages/lifecycle/dist/disposer-context.d.ts",
          "purpose": "Explicit owner-disposal capability delivered only to callbacks driven by a LifecycleScope. Generic DisposeTransaction callers omit it because they do not own a scope whose disposal can be joined; `join()` is intentionally a fail-fast operation rather than a Promise-producing API.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposerContext.",
          "advanced": "No additional advanced behavior is declared for IDisposerContext.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDisposerContext."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDisposerContext.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  type IDisposerContext,\n  type IReleaseDescriptor,\n  type IReleaseContext\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "IDisposerContext",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IDisposerContext = { /** Rejects an attempt to join the disposal currently waiting for this callback. */ readonly join: () => never; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDisposerContext when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDisposerContext does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Explicit owner-disposal capability delivered only to callbacks driven by a LifecycleScope. Generic DisposeTransaction callers omit it because they do not own a scope whose disposal can be joined; `join()` is intentionally a fail-fast operation rather than a Promise-producing API."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDisposerContext from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDisposerContext when its type contract and declared inputs match the result you need. Choose a different public symbol when IDisposerContext does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposerContext."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDisposerContext."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/disposer-context.d.ts:IDisposerContext:export type IDisposerContext = { /** Rejects an attempt to join the disposal currently waiting for this callback. */ readonly join: () => never; };",
          "guidance": [],
          "usageScore": 26,
          "configuration": [],
          "fragment": "lifecycle-index--IDisposerContext"
        },
        {
          "name": "IDisposeTransactionKind",
          "kind": "type",
          "signature": "export type IDisposeTransactionKind = (typeof DisposeTransactionKind)[keyof typeof DisposeTransactionKind];",
          "declarationLine": 43,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "IDisposeTransactionKind is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransactionKind.",
          "advanced": "No additional advanced behavior is declared for IDisposeTransactionKind.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDisposeTransactionKind."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDisposeTransactionKind.",
          "examples": [],
          "aliasOf": "IDisposeTransactionKind",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IDisposeTransactionKind = (typeof DisposeTransactionKind)[keyof typeof DisposeTransactionKind];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDisposeTransactionKind when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDisposeTransactionKind does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IDisposeTransactionKind is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDisposeTransactionKind from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDisposeTransactionKind when its type contract and declared inputs match the result you need. Choose a different public symbol when IDisposeTransactionKind does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransactionKind."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDisposeTransactionKind."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:IDisposeTransactionKind:export type IDisposeTransactionKind = (typeof DisposeTransactionKind)[keyof typeof DisposeTransactionKind];",
          "guidance": [],
          "usageScore": 4,
          "configuration": [],
          "fragment": "lifecycle-index--IDisposeTransactionKind"
        },
        {
          "name": "IEnqueueOptions",
          "kind": "type",
          "signature": "export type IEnqueueOptions = { readonly owner?: string; /** Overrides the queue's default for this one task. */ readonly queueAdmissionTimeoutMs?: number | false; };",
          "declarationLine": 21,
          "source": "packages/lifecycle/dist/mutation-queue.d.ts",
          "purpose": "IEnqueueOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IEnqueueOptions.",
          "advanced": "No additional advanced behavior is declared for IEnqueueOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IEnqueueOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IEnqueueOptions.",
          "examples": [
            "import {\n  createMutationQueue,\n  type IMutationQueue,\n  type IMutationQueueOptions,\n  type IEnqueueOptions\n} from '@migaia/lifecycle'",
            "function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue\n\ntype IMutationQueueOptions = {\n  readonly queueAdmissionTimeoutMs?: number | false // 默认 undefined（只诊断，从不因排队超时而拒绝）\n  readonly admissionDiagnosticMs?: number | false // 默认 1000\n  readonly onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IEnqueueOptions = {\n  readonly owner?: string\n  readonly queueAdmissionTimeoutMs?: number | false // 覆盖本次任务的队列默认值\n}\n\ntype IMutationQueue = {\n  enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>\n  readonly size: number\n}"
          ],
          "aliasOf": "IEnqueueOptions",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IEnqueueOptions = { readonly owner?: string; /** Overrides the queue's default for this one task. */ readonly queueAdmissionTimeoutMs?: number | false; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IEnqueueOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IEnqueueOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IEnqueueOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IEnqueueOptions from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IEnqueueOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IEnqueueOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IEnqueueOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IEnqueueOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/mutation-queue.d.ts:IEnqueueOptions:export type IEnqueueOptions = { readonly owner?: string; /** Overrides the queue's default for this one task. */ readonly queueAdmissionTimeoutMs?: number | false; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-index--IEnqueueOptions"
        },
        {
          "name": "IErrorPolicy",
          "kind": "type",
          "signature": "export type IErrorPolicy = (typeof LifecycleErrorPolicy)[keyof typeof LifecycleErrorPolicy];",
          "declarationLine": 111,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "The four release error outcomes (`lifecycle-extraction.sdd.md` §4.5, D-4). `firstError` is not a general-purpose default — it exists only to give a caller migrating `web-rpc`'s `discovery-registry` first-error-wins semantics an exact equivalent; new call sites should reach for `throw`, `collect`, or `report` instead.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorPolicy.",
          "advanced": "No additional advanced behavior is declared for IErrorPolicy.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IErrorPolicy."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IErrorPolicy.",
          "examples": [
            "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'",
            "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
          ],
          "aliasOf": "IErrorPolicy",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IErrorPolicy = (typeof LifecycleErrorPolicy)[keyof typeof LifecycleErrorPolicy];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IErrorPolicy when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IErrorPolicy does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "The four release error outcomes (`lifecycle-extraction.sdd.md` §4.5, D-4). `firstError` is not a general-purpose default — it exists only to give a caller migrating `web-rpc`'s `discovery-registry` first-error-wins semantics an exact equivalent; new call sites should reach for `throw`, `collect`, or `report` instead."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IErrorPolicy from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IErrorPolicy when its type contract and declared inputs match the result you need. Choose a different public symbol when IErrorPolicy does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorPolicy."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IErrorPolicy."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:IErrorPolicy:export type IErrorPolicy = (typeof LifecycleErrorPolicy)[keyof typeof LifecycleErrorPolicy];",
          "guidance": [
            {
              "id": "createerrorcollector",
              "heading": "createErrorCollector",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
                },
                {
                  "type": "paragraph",
                  "text": "按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
                }
              ]
            }
          ],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-index--IErrorPolicy"
        },
        {
          "name": "ILifecycleErrorCode",
          "kind": "type",
          "signature": "export type ILifecycleErrorCode = (typeof LifecycleErrorCode)[keyof typeof LifecycleErrorCode];",
          "declarationLine": 136,
          "source": "packages/lifecycle/dist/error-code.d.ts",
          "purpose": "ILifecycleErrorCode is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleErrorCode.",
          "advanced": "No additional advanced behavior is declared for ILifecycleErrorCode.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleErrorCode."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleErrorCode.",
          "examples": [
            "import { LifecycleErrorCode, type ILifecycleErrorCode } from '@migaia/lifecycle'"
          ],
          "aliasOf": "ILifecycleErrorCode",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleErrorCode = (typeof LifecycleErrorCode)[keyof typeof LifecycleErrorCode];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleErrorCode when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleErrorCode does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleErrorCode is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleErrorCode from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleErrorCode when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleErrorCode does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleErrorCode."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleErrorCode."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/error-code.d.ts:ILifecycleErrorCode:export type ILifecycleErrorCode = (typeof LifecycleErrorCode)[keyof typeof LifecycleErrorCode];",
          "guidance": [
            {
              "id": "错误码",
              "heading": "错误码",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { LifecycleErrorCode, type ILifecycleErrorCode } from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "稳定错误码表，20 个码，唯一声明处 `src/error-code.ts`，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "table",
                  "headers": ["`LifecycleErrorCode` 键", "码值", "触发条件"],
                  "rows": [
                    [
                      "`scopeClosed`",
                      "`SCOPE_CLOSED`",
                      "`close()` 之后调用 `own()`（异步/同步 scope）或某个 registry 的 `retain()`"
                    ],
                    [
                      "`scopeTerminal`",
                      "`SCOPE_TERMINAL`",
                      "容器已到达 `terminal` 之后，任何登记/激活操作"
                    ],
                    [
                      "`scopeReentrantDispose`",
                      "`SCOPE_REENTRANT_DISPOSE`",
                      "disposer 内部重入本 scope 的 `dispose()`"
                    ],
                    [
                      "`scopeReentrantOwn`",
                      "`SCOPE_REENTRANT_OWN`",
                      "disposer 内部调用本 scope 的 `own()`"
                    ],
                    [
                      "`scopeSyncViolation`",
                      "`SCOPE_SYNC_VIOLATION`",
                      "`SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor、收到一个异步 scope 实例作为资源、或某回调返回了 thenable"
                    ],
                    [
                      "`scopeDisposalFailed`",
                      "`SCOPE_DISPOSAL_FAILED`",
                      "`throw` 错误策略在多个资源释放失败时的 `AggregateError` 聚合出口"
                    ],
                    [
                      "`unitStartFailed`",
                      "`UNIT_START_FAILED`",
                      "`LifecycleUnit.start()` 返回的 thenable 被 reject（单元进入 `failed`）"
                    ],
                    [
                      "`generationSuperseded`",
                      "`GENERATION_SUPERSEDED`",
                      "一次异步操作的结果在 `adopt()` 提交点已被更晚的 generation 取代（非失败信号，仅诊断）"
                    ],
                    [
                      "`generationCancellationFailed`",
                      "`GENERATION_CANCELLATION_FAILED`",
                      "generation 取消期间的 timer、parent listener 或 signal cleanup 失败"
                    ],
                    [
                      "`generationDisposed`",
                      "`GENERATION_DISPOSED`",
                      "在已 `dispose()` 的 `GenerationController` 上调用 `begin()`"
                    ],
                    ["`quiescenceSealed`", "`QUIESCENCE_SEALED`", "`seal()` 之后又调用 `retain()`"],
                    [
                      "`quiescenceUnsealedWait`",
                      "`QUIESCENCE_UNSEALED_WAIT`",
                      "未先 `seal(key)` 就调用严格 `whenZero(key)`（同步抛出，非 rejected Promise）"
                    ],
                    [
                      "`provisionalSettled`",
                      "`PROVISIONAL_SETTLED`",
                      "同一个 `ProvisionalScope` 二次 `commitTo()`/`rollback()`，或交叉调用"
                    ],
                    [
                      "`provisionalParentClosed`",
                      "`PROVISIONAL_PARENT_CLOSED`",
                      "`commitTo()` 的目标 parent 已 `closing`/`terminal`，拒绝接收资源"
                    ],
                    [
                      "`queueAdmissionTimeout`",
                      "`QUEUE_ADMISSION_TIMEOUT`",
                      "mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`"
                    ],
                    [
                      "`queueSelfDependency`",
                      "`QUEUE_SELF_DEPENDENCY`",
                      "同一 `owner` 在自己尚未完成时又向同一队列提交新任务且被等待，会死锁"
                    ],
                    [
                      "`releaseForceFailed`",
                      "`RELEASE_FORCE_FAILED`",
                      "descriptor 的 `force` 抛出/reject（契约要求 `force` 必须无条件成功）"
                    ],
                    [
                      "`deadlineExceeded`",
                      "`DEADLINE_EXCEEDED`",
                      "共享绝对 deadline 已过去，但仍有资源被要求进入新的 graceful 阶段（诊断码，正常降级路径不抛这个码）"
                    ],
                    [
                      "`envUnsupported`",
                      "`ENV_UNSUPPORTED`",
                      "`systemScheduler` 依赖的宿主能力（`performance.now`/`setTimeout`/`clearTimeout`）缺失"
                    ],
                    [
                      "`invalidOption`",
                      "`INVALID_OPTION`",
                      "scheduler 收到非法时间/延迟参数，或 descriptor 字段类型非法"
                    ]
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "逐条设计动机见源码 `src/error-code.ts` 的 JSDoc；调用方应始终以 `error.code === LifecycleErrorCode.xxx` 判别，不要硬编码码值字符串。"
                }
              ]
            }
          ],
          "usageScore": 14,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleErrorCode"
        },
        {
          "name": "ILifecycleErrorPolicy",
          "kind": "type",
          "signature": "export type ILifecycleErrorPolicy = (typeof LifecycleErrorPolicy)[keyof typeof LifecycleErrorPolicy];",
          "declarationLine": 41,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "ILifecycleErrorPolicy is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleErrorPolicy.",
          "advanced": "No additional advanced behavior is declared for ILifecycleErrorPolicy.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleErrorPolicy."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleErrorPolicy.",
          "examples": [],
          "aliasOf": "ILifecycleErrorPolicy",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleErrorPolicy = (typeof LifecycleErrorPolicy)[keyof typeof LifecycleErrorPolicy];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleErrorPolicy when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleErrorPolicy does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleErrorPolicy is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleErrorPolicy from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleErrorPolicy when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleErrorPolicy does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleErrorPolicy."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleErrorPolicy."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:ILifecycleErrorPolicy:export type ILifecycleErrorPolicy = (typeof LifecycleErrorPolicy)[keyof typeof LifecycleErrorPolicy];",
          "guidance": [],
          "usageScore": 4,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleErrorPolicy"
        },
        {
          "name": "ILifecycleErrorText",
          "kind": "type",
          "signature": "export type ILifecycleErrorText = (typeof LifecycleErrorText)[keyof typeof LifecycleErrorText];",
          "declarationLine": 42,
          "source": "packages/lifecycle/dist/error-text.d.ts",
          "purpose": "ILifecycleErrorText is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleErrorText.",
          "advanced": "No additional advanced behavior is declared for ILifecycleErrorText.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleErrorText."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleErrorText.",
          "examples": [
            "import { LifecycleErrorText, type ILifecycleErrorText } from '@migaia/lifecycle'"
          ],
          "aliasOf": "ILifecycleErrorText",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleErrorText = (typeof LifecycleErrorText)[keyof typeof LifecycleErrorText];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleErrorText when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleErrorText does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleErrorText is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleErrorText from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleErrorText when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleErrorText does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleErrorText."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleErrorText."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/error-text.d.ts:ILifecycleErrorText:export type ILifecycleErrorText = (typeof LifecycleErrorText)[keyof typeof LifecycleErrorText];",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleErrorText"
        },
        {
          "name": "ILifecycleOwner",
          "kind": "type",
          "signature": "export type ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T; };",
          "declarationLine": 24,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "ILifecycleOwner is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleOwner.",
          "advanced": "No additional advanced behavior is declared for ILifecycleOwner.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleOwner."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleOwner.",
          "examples": [
            "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'",
            "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}",
            "import {\n  createProvisionalScope,\n  type IProvisionalScope,\n  type IProvisionalScopeOptions,\n  type ILifecycleOwner\n} from '@migaia/lifecycle'",
            "function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope\n\ntype IProvisionalScopeOptions = { readonly parentSignal?: IAbortSignal }\n\ntype IProvisionalScope = {\n  readonly signal: IAbortSignal\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  commitTo(parent: ILifecycleOwner): Promise<void>\n  rollback(): Promise<void>\n}\n\ntype ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T }",
            "import { createProvisionalScope, type ILifecycleOwner } from '@migaia/lifecycle'"
          ],
          "aliasOf": "ILifecycleOwner",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleOwner when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleOwner does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleOwner is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleOwner from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleOwner when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleOwner does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleOwner."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleOwner."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:ILifecycleOwner:export type ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T; };",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 62,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleOwner"
        },
        {
          "name": "ILifecycleState",
          "kind": "type",
          "signature": "export type ILifecycleState = (typeof LifecycleState)[keyof typeof LifecycleState];",
          "declarationLine": 35,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "Container survival axis (`lifecycle-extraction.sdd.md` §4.2, axis 1).",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleState.",
          "advanced": "No additional advanced behavior is declared for ILifecycleState.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleState."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleState.",
          "examples": [
            "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'"
          ],
          "aliasOf": "ILifecycleState",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleState = (typeof LifecycleState)[keyof typeof LifecycleState];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleState when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleState does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Container survival axis (`lifecycle-extraction.sdd.md` §4.2, axis 1)."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleState from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleState when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleState does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleState."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleState."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:ILifecycleState:export type ILifecycleState = (typeof LifecycleState)[keyof typeof LifecycleState];",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleState"
        },
        {
          "name": "ILifecycleStateValue",
          "kind": "type",
          "signature": "export type ILifecycleStateValue = (typeof LifecycleState)[keyof typeof LifecycleState];",
          "declarationLine": 39,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "ILifecycleStateValue is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleStateValue.",
          "advanced": "No additional advanced behavior is declared for ILifecycleStateValue.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleStateValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleStateValue.",
          "examples": [],
          "aliasOf": "ILifecycleStateValue",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleStateValue = (typeof LifecycleState)[keyof typeof LifecycleState];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleStateValue when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleStateValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleStateValue is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleStateValue from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleStateValue when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleStateValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleStateValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleStateValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:ILifecycleStateValue:export type ILifecycleStateValue = (typeof LifecycleState)[keyof typeof LifecycleState];",
          "guidance": [],
          "usageScore": 4,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleStateValue"
        },
        {
          "name": "ILifecycleUnit",
          "kind": "type",
          "signature": "export type ILifecycleUnit<T> = { readonly state: IUnitState; /** * The container-survival axis for the unit itself (separate from `state`) — `close()`/`dispose()` * move this one. */ readonly lifecycle: ILifecycleState; readonly value: T | undefined; readonly error: unknown; /** * Runtime-probes `factory()`'s return: a synchronous non-thenable value commits to `loaded` * immediately, without ever passing through `loading` (L-T5, D-2). A thenable commits to * `loading` and transitions on settle — but only if this call's generation is still current; a * superseded generation's late result is discarded silently (§4.2, L-T34). */ start(factory: () => T | PromiseLike<T>): void; /** Alias for `start()` — same behavior, named for the \"retry after `failed`\" call site (L-T34). */ restart(factory: () => T | PromiseLike<T>): void; close(): void; /** Synchronous — a unit owns no user resource to await releasing, only its own bookkeeping. */ dispose(): void; };",
          "declarationLine": 10,
          "source": "packages/lifecycle/dist/lifecycle-unit.d.ts",
          "purpose": "ILifecycleUnit is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleUnit.",
          "advanced": "No additional advanced behavior is declared for ILifecycleUnit.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleUnit."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleUnit.",
          "examples": [
            "import {\n  createLifecycleUnit,\n  type ILifecycleUnit,\n  type ILifecycleUnitOptions,\n  createGenerationController,\n  type IGenerationController,\n  type IGenerationControllerOptions,\n  type IGenerationRequest,\n  type IGenerationToken\n} from '@migaia/lifecycle'",
            "function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>\n\ntype ILifecycleUnitOptions = { readonly report?: (error: unknown) => void }\n\ntype ILifecycleUnit<T> = {\n  readonly state: 'idle' | 'loading' | 'loaded' | 'failed'\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly value: T | undefined\n  readonly error: unknown\n  start(factory: () => T | PromiseLike<T>): void\n  restart(factory: () => T | PromiseLike<T>): void // start 的别名\n  close(): void\n  dispose(): void // 永远同步\n}"
          ],
          "aliasOf": "ILifecycleUnit",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleUnit<T> = { readonly state: IUnitState; /** * The container-survival axis for the unit itself (separate from `state`) — `close()`/`dispose()` * move this one. */ readonly lifecycle: ILifecycleState; readonly value: T | undefined; readonly error: unknown; /** * Runtime-probes `factory()`'s return: a synchronous non-thenable value commits to `loaded` * immediately, without ever passing through `loading` (L-T5, D-2). A thenable commits to * `loading` and transitions on settle — but only if this call's generation is still current; a * superseded generation's late result is discarded silently (§4.2, L-T34). */ start(factory: () => T | PromiseLike<T>): void; /** Alias for `start()` — same behavior, named for the \"retry after `failed`\" call site (L-T34). */ restart(factory: () => T | PromiseLike<T>): void; close(): void; /** Synchronous — a unit owns no user resource to await releasing, only its own bookkeeping. */ dispose(): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleUnit when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleUnit does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleUnit is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleUnit from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleUnit when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleUnit does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleUnit."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleUnit."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/lifecycle-unit.d.ts:ILifecycleUnit:export type ILifecycleUnit<T> = { readonly state: IUnitState; /** * The container-survival axis for the unit itself (separate from `state`) — `close()`/`dispose()` * move this one. */ readonly lifecycle: ILifecycleState; readonly value: T | undefined; readonly error: unknown; /** * Runtime-probes `factory()`'s return: a synchronous non-thenable value commits to `loaded` * immediately, without ever passing through `loading` (L-T5, D-2). A thenable commits to * `loading` and transitions on settle — but only if this call's generation is still current; a * superseded generation's late result is discarded silently (§4.2, L-T34). */ start(factory: () => T | PromiseLike<T>): void; /** Alias for `start()` — same behavior, named for the \"retry after `failed`\" call site (L-T34). */ restart(factory: () => T | PromiseLike<T>): void; close(): void; /** Synchronous — a unit owns no user resource to await releasing, only its own bookkeeping. */ dispose(): void; };",
          "guidance": [
            {
              "id": "createlifecycleunit",
              "heading": "createLifecycleUnit",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>\n\ntype ILifecycleUnitOptions = { readonly report?: (error: unknown) => void }\n\ntype ILifecycleUnit<T> = {\n  readonly state: 'idle' | 'loading' | 'loaded' | 'failed'\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly value: T | undefined\n  readonly error: unknown\n  start(factory: () => T | PromiseLike<T>): void\n  restart(factory: () => T | PromiseLike<T>): void // start 的别名\n  close(): void\n  dispose(): void // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单元装载轴的状态机，内部用一个 `GenerationController` 丢弃过期结果："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const unit = createLifecycleUnit<Config>()\nunit.start(() => fetchConfig()) // thenable → 先进 'loading'，settle 后进 'loaded'/'failed'\nunit.state // 'loading'\nunit.value // undefined（settle 前）\nunit.restart(() => fetchConfig()) // 新 generation，旧一次的迟到结果被静默丢弃"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`factory()` 只读一次其返回值的 `.then`（探测语义同 `probeThenable`）：同步非 thenable 返回直接落 `loaded`（跳过 `loading`）；`factory()` 同步抛出或 thenable reject 时进入 `failed`，`unit.error` 存原始错误（不打标签），同时额外把一份打上 `UNIT_START_FAILED` 标签的错误发给 `options.report`（若提供）。",
                    "每次 `start()`/`restart()` 隐式开启新 generation；旧 generation 的迟到 settle 结果被静默丢弃（不写入 `state`/`value`/`error`）。",
                    "`lifecycle` 是容器存活轴（`close()`/`dispose()` 驱动），与 `state`（装载轴）是两个独立读值。",
                    "`dispose()` 永远同步——单元本身不持有需要等待的资源，只是终结内部 generation controller（超过一次调用幂等）。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleUnit"
        },
        {
          "name": "ILifecycleUnitOptions",
          "kind": "type",
          "signature": "export type ILifecycleUnitOptions = { /** * Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity. */ readonly report?: (error: unknown) => void; };",
          "declarationLine": 2,
          "source": "packages/lifecycle/dist/lifecycle-unit.d.ts",
          "purpose": "ILifecycleUnitOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleUnitOptions.",
          "advanced": "No additional advanced behavior is declared for ILifecycleUnitOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleUnitOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleUnitOptions.",
          "examples": [
            "import {\n  createLifecycleUnit,\n  type ILifecycleUnit,\n  type ILifecycleUnitOptions,\n  createGenerationController,\n  type IGenerationController,\n  type IGenerationControllerOptions,\n  type IGenerationRequest,\n  type IGenerationToken\n} from '@migaia/lifecycle'",
            "function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>\n\ntype ILifecycleUnitOptions = { readonly report?: (error: unknown) => void }\n\ntype ILifecycleUnit<T> = {\n  readonly state: 'idle' | 'loading' | 'loaded' | 'failed'\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly value: T | undefined\n  readonly error: unknown\n  start(factory: () => T | PromiseLike<T>): void\n  restart(factory: () => T | PromiseLike<T>): void // start 的别名\n  close(): void\n  dispose(): void // 永远同步\n}"
          ],
          "aliasOf": "ILifecycleUnitOptions",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleUnitOptions = { /** * Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity. */ readonly report?: (error: unknown) => void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleUnitOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleUnitOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleUnitOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleUnitOptions from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleUnitOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleUnitOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleUnitOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleUnitOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/lifecycle-unit.d.ts:ILifecycleUnitOptions:export type ILifecycleUnitOptions = { /** * Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity. */ readonly report?: (error: unknown) => void; };",
          "guidance": [
            {
              "id": "createlifecycleunit",
              "heading": "createLifecycleUnit",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleUnit<T>(options?: ILifecycleUnitOptions): ILifecycleUnit<T>\n\ntype ILifecycleUnitOptions = { readonly report?: (error: unknown) => void }\n\ntype ILifecycleUnit<T> = {\n  readonly state: 'idle' | 'loading' | 'loaded' | 'failed'\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly value: T | undefined\n  readonly error: unknown\n  start(factory: () => T | PromiseLike<T>): void\n  restart(factory: () => T | PromiseLike<T>): void // start 的别名\n  close(): void\n  dispose(): void // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单元装载轴的状态机，内部用一个 `GenerationController` 丢弃过期结果："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const unit = createLifecycleUnit<Config>()\nunit.start(() => fetchConfig()) // thenable → 先进 'loading'，settle 后进 'loaded'/'failed'\nunit.state // 'loading'\nunit.value // undefined（settle 前）\nunit.restart(() => fetchConfig()) // 新 generation，旧一次的迟到结果被静默丢弃"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`factory()` 只读一次其返回值的 `.then`（探测语义同 `probeThenable`）：同步非 thenable 返回直接落 `loaded`（跳过 `loading`）；`factory()` 同步抛出或 thenable reject 时进入 `failed`，`unit.error` 存原始错误（不打标签），同时额外把一份打上 `UNIT_START_FAILED` 标签的错误发给 `options.report`（若提供）。",
                    "每次 `start()`/`restart()` 隐式开启新 generation；旧 generation 的迟到 settle 结果被静默丢弃（不写入 `state`/`value`/`error`）。",
                    "`lifecycle` 是容器存活轴（`close()`/`dispose()` 驱动），与 `state`（装载轴）是两个独立读值。",
                    "`dispose()` 永远同步——单元本身不持有需要等待的资源，只是终结内部 generation controller（超过一次调用幂等）。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleUnitOptions"
        },
        {
          "name": "ILifecycleUnitStateValue",
          "kind": "type",
          "signature": "export type ILifecycleUnitStateValue = (typeof LifecycleUnitState)[keyof typeof LifecycleUnitState];",
          "declarationLine": 40,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "ILifecycleUnitStateValue is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleUnitStateValue.",
          "advanced": "No additional advanced behavior is declared for ILifecycleUnitStateValue.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleUnitStateValue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleUnitStateValue.",
          "examples": [],
          "aliasOf": "ILifecycleUnitStateValue",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ILifecycleUnitStateValue = (typeof LifecycleUnitState)[keyof typeof LifecycleUnitState];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleUnitStateValue when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleUnitStateValue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleUnitStateValue is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleUnitStateValue from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleUnitStateValue when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleUnitStateValue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleUnitStateValue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleUnitStateValue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:ILifecycleUnitStateValue:export type ILifecycleUnitStateValue = (typeof LifecycleUnitState)[keyof typeof LifecycleUnitState];",
          "guidance": [],
          "usageScore": 4,
          "configuration": [],
          "fragment": "lifecycle-index--ILifecycleUnitStateValue"
        },
        {
          "name": "IMutationQueue",
          "kind": "type",
          "signature": "export type IMutationQueue = { enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>; readonly size: number; };",
          "declarationLine": 26,
          "source": "packages/lifecycle/dist/mutation-queue.d.ts",
          "purpose": "IMutationQueue is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IMutationQueue.",
          "advanced": "No additional advanced behavior is declared for IMutationQueue.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IMutationQueue."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IMutationQueue.",
          "examples": [
            "import {\n  createMutationQueue,\n  type IMutationQueue,\n  type IMutationQueueOptions,\n  type IEnqueueOptions\n} from '@migaia/lifecycle'",
            "function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue\n\ntype IMutationQueueOptions = {\n  readonly queueAdmissionTimeoutMs?: number | false // 默认 undefined（只诊断，从不因排队超时而拒绝）\n  readonly admissionDiagnosticMs?: number | false // 默认 1000\n  readonly onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IEnqueueOptions = {\n  readonly owner?: string\n  readonly queueAdmissionTimeoutMs?: number | false // 覆盖本次任务的队列默认值\n}\n\ntype IMutationQueue = {\n  enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>\n  readonly size: number\n}"
          ],
          "aliasOf": "IMutationQueue",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IMutationQueue = { enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>; readonly size: number; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IMutationQueue when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IMutationQueue does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IMutationQueue is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IMutationQueue from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IMutationQueue when its type contract and declared inputs match the result you need. Choose a different public symbol when IMutationQueue does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IMutationQueue."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IMutationQueue."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/mutation-queue.d.ts:IMutationQueue:export type IMutationQueue = { enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>; readonly size: number; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-index--IMutationQueue"
        },
        {
          "name": "IMutationQueueOptions",
          "kind": "type",
          "signature": "export type IMutationQueueOptions = { /** * Default per-task admission budget. `undefined` (default) means \"diagnose only, never reject\" — * the core never invents a domain timeout. `false` disables even diagnostics. A number is the * `plugin-host`-equivalent behavior: reject once a queued task has waited this long. */ readonly queueAdmissionTimeoutMs?: number | false; /** * Diagnostic-only threshold used while `queueAdmissionTimeoutMs` is `undefined`. `false` disables * the diagnostic timer. */ readonly admissionDiagnosticMs?: number | false; readonly onAdmissionDiagnostic?: (info: { readonly owner: string | undefined; readonly waitedMs: number; }) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；watchdog 计时与时钟都经它，不直接用宿主 timer。 */ readonly scheduler?: ILifecycleScheduler; };",
          "declarationLine": 2,
          "source": "packages/lifecycle/dist/mutation-queue.d.ts",
          "purpose": "IMutationQueueOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IMutationQueueOptions.",
          "advanced": "No additional advanced behavior is declared for IMutationQueueOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IMutationQueueOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IMutationQueueOptions.",
          "examples": [
            "import {\n  createMutationQueue,\n  type IMutationQueue,\n  type IMutationQueueOptions,\n  type IEnqueueOptions\n} from '@migaia/lifecycle'",
            "function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue\n\ntype IMutationQueueOptions = {\n  readonly queueAdmissionTimeoutMs?: number | false // 默认 undefined（只诊断，从不因排队超时而拒绝）\n  readonly admissionDiagnosticMs?: number | false // 默认 1000\n  readonly onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IEnqueueOptions = {\n  readonly owner?: string\n  readonly queueAdmissionTimeoutMs?: number | false // 覆盖本次任务的队列默认值\n}\n\ntype IMutationQueue = {\n  enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>\n  readonly size: number\n}"
          ],
          "aliasOf": "IMutationQueueOptions",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IMutationQueueOptions = { /** * Default per-task admission budget. `undefined` (default) means \"diagnose only, never reject\" — * the core never invents a domain timeout. `false` disables even diagnostics. A number is the * `plugin-host`-equivalent behavior: reject once a queued task has waited this long. */ readonly queueAdmissionTimeoutMs?: number | false; /** * Diagnostic-only threshold used while `queueAdmissionTimeoutMs` is `undefined`. `false` disables * the diagnostic timer. */ readonly admissionDiagnosticMs?: number | false; readonly onAdmissionDiagnostic?: (info: { readonly owner: string | undefined; readonly waitedMs: number; }) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；watchdog 计时与时钟都经它，不直接用宿主 timer。 */ readonly scheduler?: ILifecycleScheduler; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IMutationQueueOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IMutationQueueOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IMutationQueueOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IMutationQueueOptions from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IMutationQueueOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IMutationQueueOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IMutationQueueOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IMutationQueueOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/mutation-queue.d.ts:IMutationQueueOptions:export type IMutationQueueOptions = { /** * Default per-task admission budget. `undefined` (default) means \"diagnose only, never reject\" — * the core never invents a domain timeout. `false` disables even diagnostics. A number is the * `plugin-host`-equivalent behavior: reject once a queued task has waited this long. */ readonly queueAdmissionTimeoutMs?: number | false; /** * Diagnostic-only threshold used while `queueAdmissionTimeoutMs` is `undefined`. `false` disables * the diagnostic timer. */ readonly admissionDiagnosticMs?: number | false; readonly onAdmissionDiagnostic?: (info: { readonly owner: string | undefined; readonly waitedMs: number; }) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；watchdog 计时与时钟都经它，不直接用宿主 timer。 */ readonly scheduler?: ILifecycleScheduler; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-index--IMutationQueueOptions"
        },
        {
          "name": "IProvisionalScope",
          "kind": "type",
          "signature": "export type IProvisionalScope = { readonly signal: IAbortSignal; own<T>(resource: T, descriptor: IReleaseDescriptor): T; /** * Transfers every owned resource to `parent`, in registration order. If `parent.own()` rejects * partway (e.g. `parent` is already closing), everything already transferred stays with `parent`; * everything not yet transferred is released by this scope (in reverse order) and **awaited** * before the original error is rethrown — nothing is left owned by neither side, and the returned * Promise settles only after the compensation has completed. */ commitTo(parent: ILifecycleOwner): Promise<void>; /** * Releases every owned resource in reverse order, collecting every failure into one * `AggregateError` (idempotent). */ rollback(): Promise<void>; };",
          "declarationLine": 7,
          "source": "packages/lifecycle/dist/provisional-scope.d.ts",
          "purpose": "IProvisionalScope is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IProvisionalScope.",
          "advanced": "No additional advanced behavior is declared for IProvisionalScope.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IProvisionalScope."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IProvisionalScope.",
          "examples": [
            "import {\n  createProvisionalScope,\n  type IProvisionalScope,\n  type IProvisionalScopeOptions,\n  type ILifecycleOwner\n} from '@migaia/lifecycle'",
            "function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope\n\ntype IProvisionalScopeOptions = { readonly parentSignal?: IAbortSignal }\n\ntype IProvisionalScope = {\n  readonly signal: IAbortSignal\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  commitTo(parent: ILifecycleOwner): Promise<void>\n  rollback(): Promise<void>\n}\n\ntype ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T }"
          ],
          "aliasOf": "IProvisionalScope",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IProvisionalScope = { readonly signal: IAbortSignal; own<T>(resource: T, descriptor: IReleaseDescriptor): T; /** * Transfers every owned resource to `parent`, in registration order. If `parent.own()` rejects * partway (e.g. `parent` is already closing), everything already transferred stays with `parent`; * everything not yet transferred is released by this scope (in reverse order) and **awaited** * before the original error is rethrown — nothing is left owned by neither side, and the returned * Promise settles only after the compensation has completed. */ commitTo(parent: ILifecycleOwner): Promise<void>; /** * Releases every owned resource in reverse order, collecting every failure into one * `AggregateError` (idempotent). */ rollback(): Promise<void>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IProvisionalScope when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IProvisionalScope does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IProvisionalScope is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IProvisionalScope from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IProvisionalScope when its type contract and declared inputs match the result you need. Choose a different public symbol when IProvisionalScope does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IProvisionalScope."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IProvisionalScope."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/provisional-scope.d.ts:IProvisionalScope:export type IProvisionalScope = { readonly signal: IAbortSignal; own<T>(resource: T, descriptor: IReleaseDescriptor): T; /** * Transfers every owned resource to `parent`, in registration order. If `parent.own()` rejects * partway (e.g. `parent` is already closing), everything already transferred stays with `parent`; * everything not yet transferred is released by this scope (in reverse order) and **awaited** * before the original error is rethrown — nothing is left owned by neither side, and the returned * Promise settles only after the compensation has completed. */ commitTo(parent: ILifecycleOwner): Promise<void>; /** * Releases every owned resource in reverse order, collecting every failure into one * `AggregateError` (idempotent). */ rollback(): Promise<void>; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-index--IProvisionalScope"
        },
        {
          "name": "IProvisionalScopeOptions",
          "kind": "type",
          "signature": "export type IProvisionalScopeOptions = { /** Forwarded so `signal` also reflects a parent's abort (e.g. a superseded generation). */ readonly parentSignal?: IAbortSignal; };",
          "declarationLine": 3,
          "source": "packages/lifecycle/dist/provisional-scope.d.ts",
          "purpose": "IProvisionalScopeOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IProvisionalScopeOptions.",
          "advanced": "No additional advanced behavior is declared for IProvisionalScopeOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IProvisionalScopeOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IProvisionalScopeOptions.",
          "examples": [
            "import {\n  createProvisionalScope,\n  type IProvisionalScope,\n  type IProvisionalScopeOptions,\n  type ILifecycleOwner\n} from '@migaia/lifecycle'",
            "function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope\n\ntype IProvisionalScopeOptions = { readonly parentSignal?: IAbortSignal }\n\ntype IProvisionalScope = {\n  readonly signal: IAbortSignal\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  commitTo(parent: ILifecycleOwner): Promise<void>\n  rollback(): Promise<void>\n}\n\ntype ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T }"
          ],
          "aliasOf": "IProvisionalScopeOptions",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IProvisionalScopeOptions = { /** Forwarded so `signal` also reflects a parent's abort (e.g. a superseded generation). */ readonly parentSignal?: IAbortSignal; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IProvisionalScopeOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IProvisionalScopeOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IProvisionalScopeOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IProvisionalScopeOptions from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IProvisionalScopeOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IProvisionalScopeOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IProvisionalScopeOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IProvisionalScopeOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/provisional-scope.d.ts:IProvisionalScopeOptions:export type IProvisionalScopeOptions = { /** Forwarded so `signal` also reflects a parent's abort (e.g. a superseded generation). */ readonly parentSignal?: IAbortSignal; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-index--IProvisionalScopeOptions"
        },
        {
          "name": "IReleaseContext",
          "kind": "type",
          "signature": "export type IReleaseContext = { /** Aborts when the owning transaction/scope enters `closing` (L-T26). */ readonly signal: IAbortSignal; readonly deadlineAt: number | undefined; /** * The scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）. Omitted → `systemScheduler`. * Exposed so descriptor callbacks and the graceful-phase deadline share one clock source instead * of silently mixing domains. */ readonly scheduler?: ILifecycleScheduler; /** Side-channel diagnostic reporter; its own throws are contained (L-T26). */ readonly report: (error: unknown) => void; /** Owner-bound self-join guard; absent when a generic transaction has no scope owner. */ readonly disposer?: IDisposerContext; };",
          "declarationLine": 45,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "The context handed to a release descriptor's `graceful`/`force`/`custom` callback. `deadlineAt` is the transaction-wide shared absolute deadline (same value for every item in one `DisposeTransaction` run, per §4.6 \"全程共享同一份绝对 deadline\" / L-T32) — it is not recomputed per item.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IReleaseContext.",
          "advanced": "No additional advanced behavior is declared for IReleaseContext.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IReleaseContext."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IReleaseContext.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "type IReleaseDescriptor = {\n  readonly syncSafe?: boolean\n  readonly order?: number\n  readonly graceful?: (context: IReleaseContext) => void | PromiseLike<void>\n  readonly gracefulTimeoutMs?: number\n  readonly force: (context: IReleaseContext) => void | PromiseLike<void>\n  readonly gcFallback?: boolean\n  readonly custom?: (context: IReleaseContext) => void | PromiseLike<void>\n}",
            "function executeReleaseDescriptor(\n  descriptor: IReleaseDescriptor,\n  context: IReleaseContext\n): Promise<readonly unknown[]>",
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  type IDisposerContext,\n  type IReleaseDescriptor,\n  type IReleaseContext\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "IReleaseContext",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IReleaseContext = { /** Aborts when the owning transaction/scope enters `closing` (L-T26). */ readonly signal: IAbortSignal; readonly deadlineAt: number | undefined; /** * The scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）. Omitted → `systemScheduler`. * Exposed so descriptor callbacks and the graceful-phase deadline share one clock source instead * of silently mixing domains. */ readonly scheduler?: ILifecycleScheduler; /** Side-channel diagnostic reporter; its own throws are contained (L-T26). */ readonly report: (error: unknown) => void; /** Owner-bound self-join guard; absent when a generic transaction has no scope owner. */ readonly disposer?: IDisposerContext; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IReleaseContext when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IReleaseContext does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "The context handed to a release descriptor's `graceful`/`force`/`custom` callback. `deadlineAt` is the transaction-wide shared absolute deadline (same value for every item in one `DisposeTransaction` run, per §4.6 \"全程共享同一份绝对 deadline\" / L-T32) — it is not recomputed per item."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IReleaseContext from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IReleaseContext when its type contract and declared inputs match the result you need. Choose a different public symbol when IReleaseContext does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IReleaseContext."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IReleaseContext."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:IReleaseContext:export type IReleaseContext = { /** Aborts when the owning transaction/scope enters `closing` (L-T26). */ readonly signal: IAbortSignal; readonly deadlineAt: number | undefined; /** * The scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）. Omitted → `systemScheduler`. * Exposed so descriptor callbacks and the graceful-phase deadline share one clock source instead * of silently mixing domains. */ readonly scheduler?: ILifecycleScheduler; /** Side-channel diagnostic reporter; its own throws are contained (L-T26). */ readonly report: (error: unknown) => void; /** Owner-bound self-join guard; absent when a generic transaction has no scope owner. */ readonly disposer?: IDisposerContext; };",
          "guidance": [],
          "usageScore": 68,
          "configuration": [],
          "fragment": "lifecycle-index--IReleaseContext"
        },
        {
          "name": "IReleaseDescriptor",
          "kind": "type",
          "signature": "export type IReleaseDescriptor = { /** * Declares this descriptor may be accepted by `SyncLifecycleScope`. Does not change the static * type of the general-purpose `LifecycleScope` (D-6) — a scope's sync/async nature is determined * by which concrete type you construct, never inferred from descriptor content. */ readonly syncSafe?: boolean; /** * Release ordering key. Higher values release first, lower values release last (lower \"sinks to * the bottom\" of the release order — the opposite of allocation order). Omitted keys are treated * as `0`. Only meaningful in `DisposeTransaction`'s `order` mode (D-3: this module never * computes order, it only executes an order supplied by the caller). */ readonly order?: number; /** * Graceful release. A timeout abandons waiting for it (does not cancel it) and falls through to * `force`. */ readonly graceful?: (context: IReleaseContext) => void | PromiseLike<void>; /** Budget for the graceful phase, capped by the transaction's shared `deadlineAt` if one exists. */ readonly gracefulTimeoutMs?: number; /** Forced release. Must complete unconditionally; must not throw a recoverable error. */ readonly force: (context: IReleaseContext) => void | PromiseLike<void>; /** * GC fallback. When set, the core registers a `FinalizationRegistry` entry that unregisters on * explicit release. */ readonly gcFallback?: boolean; /** * Escape hatch: fully takes over this resource's release. The core only orders and collects * errors. */ readonly custom?: (context: IReleaseContext) => void | PromiseLike<void>; };",
          "declarationLine": 65,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "Declares how one resource is released. The core module has zero knowledge of what a resource _is_ — descriptors are the only vocabulary a domain module uses to express release intent (`lifecycle-extraction.sdd.md` §4.4).",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IReleaseDescriptor.",
          "advanced": "No additional advanced behavior is declared for IReleaseDescriptor.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IReleaseDescriptor."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IReleaseDescriptor.",
          "examples": [
            "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'",
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "type IReleaseDescriptor = {\n  readonly syncSafe?: boolean\n  readonly order?: number\n  readonly graceful?: (context: IReleaseContext) => void | PromiseLike<void>\n  readonly gracefulTimeoutMs?: number\n  readonly force: (context: IReleaseContext) => void | PromiseLike<void>\n  readonly gcFallback?: boolean\n  readonly custom?: (context: IReleaseContext) => void | PromiseLike<void>\n}",
            "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}",
            "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}",
            "function executeReleaseDescriptor(\n  descriptor: IReleaseDescriptor,\n  context: IReleaseContext\n): Promise<readonly unknown[]>",
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}",
            "function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope\n\ntype IProvisionalScopeOptions = { readonly parentSignal?: IAbortSignal }\n\ntype IProvisionalScope = {\n  readonly signal: IAbortSignal\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  commitTo(parent: ILifecycleOwner): Promise<void>\n  rollback(): Promise<void>\n}\n\ntype ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T }",
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  type IDisposerContext,\n  type IReleaseDescriptor,\n  type IReleaseContext\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "IReleaseDescriptor",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IReleaseDescriptor = { /** * Declares this descriptor may be accepted by `SyncLifecycleScope`. Does not change the static * type of the general-purpose `LifecycleScope` (D-6) — a scope's sync/async nature is determined * by which concrete type you construct, never inferred from descriptor content. */ readonly syncSafe?: boolean; /** * Release ordering key. Higher values release first, lower values release last (lower \"sinks to * the bottom\" of the release order — the opposite of allocation order). Omitted keys are treated * as `0`. Only meaningful in `DisposeTransaction`'s `order` mode (D-3: this module never * computes order, it only executes an order supplied by the caller). */ readonly order?: number; /** * Graceful release. A timeout abandons waiting for it (does not cancel it) and falls through to * `force`. */ readonly graceful?: (context: IReleaseContext) => void | PromiseLike<void>; /** Budget for the graceful phase, capped by the transaction's shared `deadlineAt` if one exists. */ readonly gracefulTimeoutMs?: number; /** Forced release. Must complete unconditionally; must not throw a recoverable error. */ readonly force: (context: IReleaseContext) => void | PromiseLike<void>; /** * GC fallback. When set, the core registers a `FinalizationRegistry` entry that unregisters on * explicit release. */ readonly gcFallback?: boolean; /** * Escape hatch: fully takes over this resource's release. The core only orders and collects * errors. */ readonly custom?: (context: IReleaseContext) => void | PromiseLike<void>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IReleaseDescriptor when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IReleaseDescriptor does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Declares how one resource is released. The core module has zero knowledge of what a resource _is_ — descriptors are the only vocabulary a domain module uses to express release intent (`lifecycle-extraction.sdd.md` §4.4)."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IReleaseDescriptor from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IReleaseDescriptor when its type contract and declared inputs match the result you need. Choose a different public symbol when IReleaseDescriptor does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IReleaseDescriptor."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IReleaseDescriptor."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:IReleaseDescriptor:export type IReleaseDescriptor = { /** * Declares this descriptor may be accepted by `SyncLifecycleScope`. Does not change the static * type of the general-purpose `LifecycleScope` (D-6) — a scope's sync/async nature is determined * by which concrete type you construct, never inferred from descriptor content. */ readonly syncSafe?: boolean; /** * Release ordering key. Higher values release first, lower values release last (lower \"sinks to * the bottom\" of the release order — the opposite of allocation order). Omitted keys are treated * as `0`. Only meaningful in `DisposeTransaction`'s `order` mode (D-3: this package never * computes order, it only executes an order supplied by the caller). */ readonly order?: number; /** * Graceful release. A timeout abandons waiting for it (does not cancel it) and falls through to * `force`. */ readonly graceful?: (context: IReleaseContext) => void | PromiseLike<void>; /** Budget for the graceful phase, capped by the transaction's shared `deadlineAt` if one exists. */ readonly gracefulTimeoutMs?: number; /** Forced release. Must complete unconditionally; must not throw a recoverable error. */ readonly force: (context: IReleaseContext) => void | PromiseLike<void>; /** * GC fallback. When set, the core registers a `FinalizationRegistry` entry that unregisters on * explicit release. */ readonly gcFallback?: boolean; /** * Escape hatch: fully takes over this resource's release. The core only orders and collects * errors. */ readonly custom?: (context: IReleaseContext) => void | PromiseLike<void>; };",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            },
            {
              "id": "createsynclifecyclescope",
              "heading": "createSyncLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "纯同步容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回"
                },
                {
                  "type": "paragraph",
                  "text": "约束与边界："
                },
                {
                  "type": "list",
                  "items": [
                    "每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`。",
                    "不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例（内部品牌检测），命中同样抛 `SCOPE_SYNC_VIOLATION`。",
                    "某个 callback（`custom`/`graceful`/`force`）若返回 thenable，在释放当次同步探测到并抛 `SCOPE_SYNC_VIOLATION`（`graceful` 违规时会继续跑 `force`，`force`/`custom` 违规则该项失败计入当前错误策略）。",
                    "降级链与异步版本相同（`custom` → 否则 `graceful` → `force`），但没有超时竞速——没有可等待的对象。",
                    "`release()`/`close()` 语义与异步版本一致；`dispose()` 幂等，二次调用返回 `[]`；在 disposer 内部重入抛 `SCOPE_REENTRANT_DISPOSE`。",
                    "环境支持时挂载 `[Symbol.dispose]`，可配合 `using` 语法。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 124,
          "configuration": [],
          "fragment": "lifecycle-index--IReleaseDescriptor"
        },
        {
          "name": "ISyncLifecycleScope",
          "kind": "type",
          "signature": "export type ISyncLifecycleScope = { readonly lifecycle: ILifecycleState; own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T; release(resource: unknown): boolean; close(): void; /** Always synchronous — this is the whole reason this type exists (D-6). */ dispose(): readonly ICollectedError[]; };",
          "declarationLine": 10,
          "source": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts",
          "purpose": "ISyncLifecycleScope is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncLifecycleScope.",
          "advanced": "No additional advanced behavior is declared for ISyncLifecycleScope.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ISyncLifecycleScope."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ISyncLifecycleScope.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
          ],
          "aliasOf": "ISyncLifecycleScope",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ISyncLifecycleScope = { readonly lifecycle: ILifecycleState; own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T; release(resource: unknown): boolean; close(): void; /** Always synchronous — this is the whole reason this type exists (D-6). */ dispose(): readonly ICollectedError[]; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ISyncLifecycleScope when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ISyncLifecycleScope does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ISyncLifecycleScope is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ISyncLifecycleScope from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ISyncLifecycleScope when its type contract and declared inputs match the result you need. Choose a different public symbol when ISyncLifecycleScope does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncLifecycleScope."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ISyncLifecycleScope."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts:ISyncLifecycleScope:export type ISyncLifecycleScope = { readonly lifecycle: ILifecycleState; own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T; release(resource: unknown): boolean; close(): void; /** Always synchronous — this is the whole reason this type exists (D-6). */ dispose(): readonly ICollectedError[]; };",
          "guidance": [
            {
              "id": "createsynclifecyclescope",
              "heading": "createSyncLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "纯同步容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回"
                },
                {
                  "type": "paragraph",
                  "text": "约束与边界："
                },
                {
                  "type": "list",
                  "items": [
                    "每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`。",
                    "不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例（内部品牌检测），命中同样抛 `SCOPE_SYNC_VIOLATION`。",
                    "某个 callback（`custom`/`graceful`/`force`）若返回 thenable，在释放当次同步探测到并抛 `SCOPE_SYNC_VIOLATION`（`graceful` 违规时会继续跑 `force`，`force`/`custom` 违规则该项失败计入当前错误策略）。",
                    "降级链与异步版本相同（`custom` → 否则 `graceful` → `force`），但没有超时竞速——没有可等待的对象。",
                    "`release()`/`close()` 语义与异步版本一致；`dispose()` 幂等，二次调用返回 `[]`；在 disposer 内部重入抛 `SCOPE_REENTRANT_DISPOSE`。",
                    "环境支持时挂载 `[Symbol.dispose]`，可配合 `using` 语法。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-index--ISyncLifecycleScope"
        },
        {
          "name": "ISyncLifecycleScopeOptions",
          "kind": "type",
          "signature": "export type ISyncLifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; };",
          "declarationLine": 6,
          "source": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts",
          "purpose": "ISyncLifecycleScopeOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncLifecycleScopeOptions.",
          "advanced": "No additional advanced behavior is declared for ISyncLifecycleScopeOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ISyncLifecycleScopeOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ISyncLifecycleScopeOptions.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
          ],
          "aliasOf": "ISyncLifecycleScopeOptions",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ISyncLifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ISyncLifecycleScopeOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ISyncLifecycleScopeOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ISyncLifecycleScopeOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ISyncLifecycleScopeOptions from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ISyncLifecycleScopeOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when ISyncLifecycleScopeOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncLifecycleScopeOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ISyncLifecycleScopeOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts:ISyncLifecycleScopeOptions:export type ISyncLifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; };",
          "guidance": [
            {
              "id": "createsynclifecyclescope",
              "heading": "createSyncLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "纯同步容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回"
                },
                {
                  "type": "paragraph",
                  "text": "约束与边界："
                },
                {
                  "type": "list",
                  "items": [
                    "每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`。",
                    "不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例（内部品牌检测），命中同样抛 `SCOPE_SYNC_VIOLATION`。",
                    "某个 callback（`custom`/`graceful`/`force`）若返回 thenable，在释放当次同步探测到并抛 `SCOPE_SYNC_VIOLATION`（`graceful` 违规时会继续跑 `force`，`force`/`custom` 违规则该项失败计入当前错误策略）。",
                    "降级链与异步版本相同（`custom` → 否则 `graceful` → `force`），但没有超时竞速——没有可等待的对象。",
                    "`release()`/`close()` 语义与异步版本一致；`dispose()` 幂等，二次调用返回 `[]`；在 disposer 内部重入抛 `SCOPE_REENTRANT_DISPOSE`。",
                    "环境支持时挂载 `[Symbol.dispose]`，可配合 `using` 语法。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-index--ISyncLifecycleScopeOptions"
        },
        {
          "name": "ISyncReleaseDescriptor",
          "kind": "type",
          "signature": "export type ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true; };",
          "declarationLine": 3,
          "source": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts",
          "purpose": "A descriptor accepted by `SyncLifecycleScope`: `syncSafe` is required and must be `true`.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncReleaseDescriptor.",
          "advanced": "No additional advanced behavior is declared for ISyncReleaseDescriptor.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ISyncReleaseDescriptor."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ISyncReleaseDescriptor.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
          ],
          "aliasOf": "ISyncReleaseDescriptor",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ISyncReleaseDescriptor when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ISyncReleaseDescriptor does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "A descriptor accepted by `SyncLifecycleScope`: `syncSafe` is required and must be `true`."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ISyncReleaseDescriptor from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ISyncReleaseDescriptor when its type contract and declared inputs match the result you need. Choose a different public symbol when ISyncReleaseDescriptor does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncReleaseDescriptor."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ISyncReleaseDescriptor."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/sync-lifecycle-scope.d.ts:ISyncReleaseDescriptor:export type ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true; };",
          "guidance": [
            {
              "id": "createsynclifecyclescope",
              "heading": "createSyncLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createSyncLifecycleScope(options?: ISyncLifecycleScopeOptions): ISyncLifecycleScope\n\ntype ISyncLifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n}\n\ntype ISyncReleaseDescriptor = IReleaseDescriptor & { readonly syncSafe: true }\n\ntype ISyncLifecycleScope = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: ISyncReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): readonly ICollectedError[] // 永远同步\n}"
                },
                {
                  "type": "paragraph",
                  "text": "纯同步容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createSyncLifecycleScope()\nscope.own(node, { syncSafe: true, force: () => node.detach() })\nscope.close()\nconst failures = scope.dispose() // 同步返回"
                },
                {
                  "type": "paragraph",
                  "text": "约束与边界："
                },
                {
                  "type": "list",
                  "items": [
                    "每个 descriptor 必须显式 `syncSafe: true`，否则 `own()` 在注册时（不是释放时）就抛 `SCOPE_SYNC_VIOLATION`。",
                    "不能 `own()` 一个 `LifecycleScope`/`ProvisionalScope` 实例（内部品牌检测），命中同样抛 `SCOPE_SYNC_VIOLATION`。",
                    "某个 callback（`custom`/`graceful`/`force`）若返回 thenable，在释放当次同步探测到并抛 `SCOPE_SYNC_VIOLATION`（`graceful` 违规时会继续跑 `force`，`force`/`custom` 违规则该项失败计入当前错误策略）。",
                    "降级链与异步版本相同（`custom` → 否则 `graceful` → `force`），但没有超时竞速——没有可等待的对象。",
                    "`release()`/`close()` 语义与异步版本一致；`dispose()` 幂等，二次调用返回 `[]`；在 disposer 内部重入抛 `SCOPE_REENTRANT_DISPOSE`。",
                    "环境支持时挂载 `[Symbol.dispose]`，可配合 `using` 语法。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-index--ISyncReleaseDescriptor"
        },
        {
          "name": "ITerminalController",
          "kind": "type",
          "signature": "export type ITerminalController = { readonly lifecycle: ILifecycleState; /** Idempotent. Moves `open` → `closing`; does nothing once past `open`. */ close(): void; /** Idempotent. Moves straight to `terminal`, resolving `whenTerminal()`. */ forceTerminal(): void; /** Resolves exactly once, when `terminal` is reached. */ whenTerminal(): Promise<void>; };",
          "declarationLine": 7,
          "source": "packages/lifecycle/dist/terminal-controller.d.ts",
          "purpose": "Owns the `open → closing → terminal` axis (§4.2, axis 1). Ported near-verbatim from `@migaia/reactive`'s `TerminalControllerImpl` — the state machine itself was already correct; only the domain-specific naming is gone.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ITerminalController.",
          "advanced": "No additional advanced behavior is declared for ITerminalController.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ITerminalController."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ITerminalController.",
          "examples": [
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "function createTerminalController(): ITerminalController\n\ntype ITerminalController = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  close(): void\n  forceTerminal(): void\n  whenTerminal(): Promise<void>\n}"
          ],
          "aliasOf": "ITerminalController",
          "exportPath": ".",
          "overloadSignatures": [
            "export type ITerminalController = { readonly lifecycle: ILifecycleState; /** Idempotent. Moves `open` → `closing`; does nothing once past `open`. */ close(): void; /** Idempotent. Moves straight to `terminal`, resolving `whenTerminal()`. */ forceTerminal(): void; /** Resolves exactly once, when `terminal` is reached. */ whenTerminal(): Promise<void>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ITerminalController when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ITerminalController does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Owns the `open → closing → terminal` axis (§4.2, axis 1). Ported near-verbatim from `@migaia/reactive`'s `TerminalControllerImpl` — the state machine itself was already correct; only the domain-specific naming is gone."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ITerminalController from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ITerminalController when its type contract and declared inputs match the result you need. Choose a different public symbol when ITerminalController does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ITerminalController."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ITerminalController."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/terminal-controller.d.ts:ITerminalController:export type ITerminalController = { readonly lifecycle: ILifecycleState; /** Idempotent. Moves `open` → `closing`; does nothing once past `open`. */ close(): void; /** Idempotent. Moves straight to `terminal`, resolving `whenTerminal()`. */ forceTerminal(): void; /** Resolves exactly once, when `terminal` is reached. */ whenTerminal(): Promise<void>; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-index--ITerminalController"
        },
        {
          "name": "IThenableProbeKind",
          "kind": "type",
          "signature": "export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];",
          "declarationLine": 42,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "IThenableProbeKind is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableProbeKind.",
          "advanced": "No additional advanced behavior is declared for IThenableProbeKind.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IThenableProbeKind."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IThenableProbeKind.",
          "examples": [],
          "aliasOf": "IThenableProbeKind",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IThenableProbeKind when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IThenableProbeKind does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IThenableProbeKind is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IThenableProbeKind from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IThenableProbeKind when its type contract and declared inputs match the result you need. Choose a different public symbol when IThenableProbeKind does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IThenableProbeKind."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IThenableProbeKind."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:IThenableProbeKind:export type IThenableProbeKind = (typeof ThenableProbeKind)[keyof typeof ThenableProbeKind];",
          "guidance": [],
          "usageScore": 4,
          "configuration": [],
          "fragment": "lifecycle-index--IThenableProbeKind"
        },
        {
          "name": "IUnitState",
          "kind": "type",
          "signature": "export type IUnitState = (typeof LifecycleUnitState)[keyof typeof LifecycleUnitState];",
          "declarationLine": 37,
          "source": "packages/lifecycle/dist/types.d.ts",
          "purpose": "Unit loading axis (`lifecycle-extraction.sdd.md` §4.2, axis 2).",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IUnitState.",
          "advanced": "No additional advanced behavior is declared for IUnitState.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IUnitState."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IUnitState.",
          "examples": [
            "type IDisposer = () => void\ntype ILifecycleState = 'open' | 'closing' | 'terminal'\ntype IUnitState = 'idle' | 'loading' | 'loaded' | 'failed'\n\ntype ILifecycleOwner = {\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n}\n\ntype ICollectedError = Readonly<{\n  source: string\n  error: unknown\n}>\n\ntype IErrorPolicy = 'throw' | 'collect' | 'report' | 'firstError'"
          ],
          "aliasOf": "IUnitState",
          "exportPath": ".",
          "overloadSignatures": [
            "export type IUnitState = (typeof LifecycleUnitState)[keyof typeof LifecycleUnitState];"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IUnitState when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IUnitState does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Unit loading axis (`lifecycle-extraction.sdd.md` §4.2, axis 2)."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IUnitState from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IUnitState when its type contract and declared inputs match the result you need. Choose a different public symbol when IUnitState does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IUnitState."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IUnitState."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/types.d.ts:IUnitState:export type IUnitState = (typeof LifecycleUnitState)[keyof typeof LifecycleUnitState];",
          "guidance": [],
          "usageScore": 18,
          "configuration": [],
          "fragment": "lifecycle-index--IUnitState"
        },
        {
          "name": "LifecycleErrorCode",
          "kind": "const",
          "signature": "export declare const LifecycleErrorCode: { /** * `close()` 之后调用 `own()` 或某个 registry 的 `retain()` 时抛出。 * * 容器已停止接受新工作；调用方应新建 scope，而不是复用这个。落实 §4.1 的两阶段契约：`close()` 是同步的、不可失败的、不调用用户代码的状态变更。 */ readonly scopeClosed: \"SCOPE_CLOSED\"; /** * 容器已到达 `terminal`（`dispose()` 已完成）之后，任何登记/激活操作都抛出。 * * 与 `SCOPE_CLOSED` 分开是因为它是更强的终态信号——`terminal` 不可逆，调用方不该重试，只能新建。 */ readonly scopeTerminal: \"SCOPE_TERMINAL\"; /** * Disposer 内部重入本 scope 的 `dispose()` 时抛出。 * * 错误归属到引发重入的那个 disposer（沿正常的 try/catch 传播），其余资源照常释放完毕。调用方应把 清理逻辑移出 disposer，不要让一个资源的释放去释放正在释放它的那个 * scope。 */ readonly scopeReentrantDispose: \"SCOPE_REENTRANT_DISPOSE\"; /** * Disposer 内部调用本 scope 的 `own()` 时抛出。 * * 同上错误归属规则。释放期间容器已进入 `closing`，再登记新资源没有人会负责释放它。 */ readonly scopeReentrantOwn: \"SCOPE_REENTRANT_OWN\"; /** * `SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor，或收到一个 `LifecycleScope` / * `ProvisionalScope` 实例作为待 own 的资源时，在注册时（而非释放时）立即抛出。 * * 调用方要么把该资源迁到异步 `LifecycleScope`，要么把它的 descriptor 显式标注 `syncSafe: true` 并确保其 force/graceful 从不返回 * thenable。 */ readonly scopeSyncViolation: \"SCOPE_SYNC_VIOLATION\"; /** * `throw` 错误策略在多个资源释放失败时的聚合出口（`AggregateError` 外壳）。 * * 调用方应展开 `error.errors` 逐条处理；单个资源失败时不会用到这个码，错误原样抛出。 */ readonly scopeDisposalFailed: \"SCOPE_DISPOSAL_FAILED\"; /** * `LifecycleUnit.start()` 返回的 thenable 被 reject，单元进入 `failed`。 * * 调用方可以显式调用 `start()`/`restart()` 开启新 generation 重试，或调用 `close()`/`dispose()` 让单元到达 * `terminal`；`failed` 不是无出口终态。 */ readonly unitStartFailed: \"UNIT_START_FAILED\"; /** * 一次异步操作的结果在提交点已被更晚的 generation 取代，结果被丢弃。 * * 这不是失败，是正常的竞态处理信号：调用方通常只需要静默忽略，除非在诊断路径里需要感知它。 */ readonly generationSuperseded: \"GENERATION_SUPERSEDED\"; /** * Generation 取消期间的 timer、parent listener 或 signal cleanup 失败。 generation 作废必须先完成状态收敛并按登记顺序保留所有 * cleanup 失败，落实 lifecycle-extraction.sdd.md §4.10.2；调用方应处理该错误并依赖新 generation，不得复活旧 generation。 */ readonly generationCancellationFailed: \"GENERATION_CANCELLATION_FAILED\"; /** * 在已经 `dispose()` 过的 `GenerationController` 上调用 `begin()`。 * * 该控制器不可复用；调用方应新建一个。 */ readonly generationDisposed: \"GENERATION_DISPOSED\"; /** * `seal()` 之后又调用 `retain()` 时抛出。 * * 封存表示「不再接受新的租约」；调用方要么在 seal 之前完成所有 retain，要么改走 `whenZeroOnce()` 的非独占路径。 */ readonly quiescenceSealed: \"QUIESCENCE_SEALED\"; /** * 未先调用 `seal(key)` 就调用严格 `whenZero(key)` 时，在调用的当次同步抛出（不是返回一个 rejected Promise）。 * * 严格归零等待必须先切断新租约的来源，否则「归零」的保证不成立；调用方应先 `seal()`，或改用 `whenZeroOnce()` 自行承担二次校验的责任。 */ readonly quiescenceUnsealedWait: \"QUIESCENCE_UNSEALED_WAIT\"; /** * 同一个 `ProvisionalScope` 二次 `commitTo()`，或在 `rollback()` 之后再次 `commitTo()`/`rollback()`。 * * Commit/rollback 是二选一的终态动作；调用方的状态机出现了重复调用，需要检查调用路径。 */ readonly provisionalSettled: \"PROVISIONAL_SETTLED\"; /** * `commitTo()` 的目标 parent 已经进入 `closing`/`terminal`，拒绝接收资源。 * * 已转移给 parent 的资源保持在 parent 名下；尚未转移的部分会被这次调用自动释放，调用方不需要 再手动 `rollback()`。 */ readonly provisionalParentClosed: \"PROVISIONAL_PARENT_CLOSED\"; /** * Mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`，被移出队列并拒绝。 * * 这是一条可配置的入队 SLA，不是硬编码行为——未配置时不会触发。调用方应检查是否有任务卡住了 队列前面的位置，或调大阈值。 */ readonly queueAdmissionTimeout: \"QUEUE_ADMISSION_TIMEOUT\"; /** * 同一个 owner 标签在自己尚未完成时又向同一队列提交了新任务，且该提交方正在（通过 `await`） 等待这个新任务——这会死锁，因为 FIFO 队列要求前一个任务先完成。 * * 调用方应避免在一个任务内部同步等待同 owner 的另一个排队任务；必要时拆分成两次独立的顶层调用。 */ readonly queueSelfDependency: \"QUEUE_SELF_DEPENDENCY\"; /** * Descriptor 的 `force` 抛出（或其 Promise 被 reject），而契约要求 `force` 必须无条件成功完成。 * * 错误仍会按当前错误策略收集/上报，释放流程仍会推进到 terminal；调用方应把这个错误当作 「不可恢复的资源泄漏风险」处理，而不是可重试的瞬时失败。 */ readonly releaseForceFailed: \"RELEASE_FORCE_FAILED\"; /** * 共享的绝对 deadline 已经过去，但仍有资源被要求进入一个新的 graceful 阶段。 * * 只在诊断/边界场景下作为显式错误码使用；正常的「graceful 超时降级到 force」走的是降级路径， 不抛出这个码（见下方「不是错误码」的说明）。 */ readonly deadlineExceeded: \"DEADLINE_EXCEEDED\"; /** * `systemScheduler` 依赖的宿主能力（`performance.now` / `setTimeout` / `clearTimeout`）缺失。 * * 首次调用 `now()`（缺 `performance.now`）或 `schedule()`（缺 `setTimeout`/`clearTimeout`）时 * fail-fast。调用方要么注入自实现 `ILifecycleScheduler`，要么在具备这些跨运行时公共 API 的环境里运行。 */ readonly envUnsupported: \"ENV_UNSUPPORTED\"; /** * Scheduler 收到非法的时间/延迟参数：`schedule()` 的 `delayMs` 或 manual `advance(ms)` 的 `ms` 非有限或为负数。 * * 落实 `docs/contracts/runtime-neutrality.sdd.md` R-9/T-16 的「delay 有限非负、now 单调不递减」； * 调用方应修正传入的延迟/推进量，不要重试同一份非法参数。 */ readonly invalidOption: \"INVALID_OPTION\"; };",
          "declarationLine": 9,
          "source": "packages/lifecycle/dist/error-code.d.ts",
          "purpose": "`@migaia/lifecycle` 错误码的唯一声明处。 契约见 `docs/contracts/error-codes.md`：错误由 `(source, code)` 二元组唯一定位，`source` 恒为 `'@migaia/lifecycle'`。抛出点必须引用本文件的常量，不得内联字面量。 码值是公开 API 的一部分，改名等同破坏性变更。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleErrorCode.",
          "advanced": "No additional advanced behavior is declared for LifecycleErrorCode.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for LifecycleErrorCode."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for LifecycleErrorCode.",
          "examples": [
            "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')",
            "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')",
            "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)",
            "import { LifecycleErrorCode, type ILifecycleErrorCode } from '@migaia/lifecycle'",
            "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}",
            "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'",
            "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
          ],
          "aliasOf": "LifecycleErrorCode",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const LifecycleErrorCode: { /** * `close()` 之后调用 `own()` 或某个 registry 的 `retain()` 时抛出。 * * 容器已停止接受新工作；调用方应新建 scope，而不是复用这个。落实 §4.1 的两阶段契约：`close()` 是同步的、不可失败的、不调用用户代码的状态变更。 */ readonly scopeClosed: \"SCOPE_CLOSED\"; /** * 容器已到达 `terminal`（`dispose()` 已完成）之后，任何登记/激活操作都抛出。 * * 与 `SCOPE_CLOSED` 分开是因为它是更强的终态信号——`terminal` 不可逆，调用方不该重试，只能新建。 */ readonly scopeTerminal: \"SCOPE_TERMINAL\"; /** * Disposer 内部重入本 scope 的 `dispose()` 时抛出。 * * 错误归属到引发重入的那个 disposer（沿正常的 try/catch 传播），其余资源照常释放完毕。调用方应把 清理逻辑移出 disposer，不要让一个资源的释放去释放正在释放它的那个 * scope。 */ readonly scopeReentrantDispose: \"SCOPE_REENTRANT_DISPOSE\"; /** * Disposer 内部调用本 scope 的 `own()` 时抛出。 * * 同上错误归属规则。释放期间容器已进入 `closing`，再登记新资源没有人会负责释放它。 */ readonly scopeReentrantOwn: \"SCOPE_REENTRANT_OWN\"; /** * `SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor，或收到一个 `LifecycleScope` / * `ProvisionalScope` 实例作为待 own 的资源时，在注册时（而非释放时）立即抛出。 * * 调用方要么把该资源迁到异步 `LifecycleScope`，要么把它的 descriptor 显式标注 `syncSafe: true` 并确保其 force/graceful 从不返回 * thenable。 */ readonly scopeSyncViolation: \"SCOPE_SYNC_VIOLATION\"; /** * `throw` 错误策略在多个资源释放失败时的聚合出口（`AggregateError` 外壳）。 * * 调用方应展开 `error.errors` 逐条处理；单个资源失败时不会用到这个码，错误原样抛出。 */ readonly scopeDisposalFailed: \"SCOPE_DISPOSAL_FAILED\"; /** * `LifecycleUnit.start()` 返回的 thenable 被 reject，单元进入 `failed`。 * * 调用方可以显式调用 `start()`/`restart()` 开启新 generation 重试，或调用 `close()`/`dispose()` 让单元到达 * `terminal`；`failed` 不是无出口终态。 */ readonly unitStartFailed: \"UNIT_START_FAILED\"; /** * 一次异步操作的结果在提交点已被更晚的 generation 取代，结果被丢弃。 * * 这不是失败，是正常的竞态处理信号：调用方通常只需要静默忽略，除非在诊断路径里需要感知它。 */ readonly generationSuperseded: \"GENERATION_SUPERSEDED\"; /** * Generation 取消期间的 timer、parent listener 或 signal cleanup 失败。 generation 作废必须先完成状态收敛并按登记顺序保留所有 * cleanup 失败，落实 lifecycle-extraction.sdd.md §4.10.2；调用方应处理该错误并依赖新 generation，不得复活旧 generation。 */ readonly generationCancellationFailed: \"GENERATION_CANCELLATION_FAILED\"; /** * 在已经 `dispose()` 过的 `GenerationController` 上调用 `begin()`。 * * 该控制器不可复用；调用方应新建一个。 */ readonly generationDisposed: \"GENERATION_DISPOSED\"; /** * `seal()` 之后又调用 `retain()` 时抛出。 * * 封存表示「不再接受新的租约」；调用方要么在 seal 之前完成所有 retain，要么改走 `whenZeroOnce()` 的非独占路径。 */ readonly quiescenceSealed: \"QUIESCENCE_SEALED\"; /** * 未先调用 `seal(key)` 就调用严格 `whenZero(key)` 时，在调用的当次同步抛出（不是返回一个 rejected Promise）。 * * 严格归零等待必须先切断新租约的来源，否则「归零」的保证不成立；调用方应先 `seal()`，或改用 `whenZeroOnce()` 自行承担二次校验的责任。 */ readonly quiescenceUnsealedWait: \"QUIESCENCE_UNSEALED_WAIT\"; /** * 同一个 `ProvisionalScope` 二次 `commitTo()`，或在 `rollback()` 之后再次 `commitTo()`/`rollback()`。 * * Commit/rollback 是二选一的终态动作；调用方的状态机出现了重复调用，需要检查调用路径。 */ readonly provisionalSettled: \"PROVISIONAL_SETTLED\"; /** * `commitTo()` 的目标 parent 已经进入 `closing`/`terminal`，拒绝接收资源。 * * 已转移给 parent 的资源保持在 parent 名下；尚未转移的部分会被这次调用自动释放，调用方不需要 再手动 `rollback()`。 */ readonly provisionalParentClosed: \"PROVISIONAL_PARENT_CLOSED\"; /** * Mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`，被移出队列并拒绝。 * * 这是一条可配置的入队 SLA，不是硬编码行为——未配置时不会触发。调用方应检查是否有任务卡住了 队列前面的位置，或调大阈值。 */ readonly queueAdmissionTimeout: \"QUEUE_ADMISSION_TIMEOUT\"; /** * 同一个 owner 标签在自己尚未完成时又向同一队列提交了新任务，且该提交方正在（通过 `await`） 等待这个新任务——这会死锁，因为 FIFO 队列要求前一个任务先完成。 * * 调用方应避免在一个任务内部同步等待同 owner 的另一个排队任务；必要时拆分成两次独立的顶层调用。 */ readonly queueSelfDependency: \"QUEUE_SELF_DEPENDENCY\"; /** * Descriptor 的 `force` 抛出（或其 Promise 被 reject），而契约要求 `force` 必须无条件成功完成。 * * 错误仍会按当前错误策略收集/上报，释放流程仍会推进到 terminal；调用方应把这个错误当作 「不可恢复的资源泄漏风险」处理，而不是可重试的瞬时失败。 */ readonly releaseForceFailed: \"RELEASE_FORCE_FAILED\"; /** * 共享的绝对 deadline 已经过去，但仍有资源被要求进入一个新的 graceful 阶段。 * * 只在诊断/边界场景下作为显式错误码使用；正常的「graceful 超时降级到 force」走的是降级路径， 不抛出这个码（见下方「不是错误码」的说明）。 */ readonly deadlineExceeded: \"DEADLINE_EXCEEDED\"; /** * `systemScheduler` 依赖的宿主能力（`performance.now` / `setTimeout` / `clearTimeout`）缺失。 * * 首次调用 `now()`（缺 `performance.now`）或 `schedule()`（缺 `setTimeout`/`clearTimeout`）时 * fail-fast。调用方要么注入自实现 `ILifecycleScheduler`，要么在具备这些跨运行时公共 API 的环境里运行。 */ readonly envUnsupported: \"ENV_UNSUPPORTED\"; /** * Scheduler 收到非法的时间/延迟参数：`schedule()` 的 `delayMs` 或 manual `advance(ms)` 的 `ms` 非有限或为负数。 * * 落实 `docs/contracts/runtime-neutrality.sdd.md` R-9/T-16 的「delay 有限非负、now 单调不递减」； * 调用方应修正传入的延迟/推进量，不要重试同一份非法参数。 */ readonly invalidOption: \"INVALID_OPTION\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use LifecycleErrorCode when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when LifecycleErrorCode does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "`@migaia/lifecycle` 错误码的唯一声明处。 契约见 `docs/contracts/error-codes.md`：错误由 `(source, code)` 二元组唯一定位，`source` 恒为 `'@migaia/lifecycle'`。抛出点必须引用本文件的常量，不得内联字面量。 码值是公开 API 的一部分，改名等同破坏性变更。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import LifecycleErrorCode from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use LifecycleErrorCode when its const contract and declared inputs match the result you need. Choose a different public symbol when LifecycleErrorCode does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleErrorCode."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for LifecycleErrorCode."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/error-code.d.ts:LifecycleErrorCode:export declare const LifecycleErrorCode: { /** * `close()` 之后调用 `own()` 或某个 registry 的 `retain()` 时抛出。 * * 容器已停止接受新工作；调用方应新建 scope，而不是复用这个。落实 §4.1 的两阶段契约：`close()` 是同步的、不可失败的、不调用用户代码的状态变更。 */ readonly scopeClosed: \"SCOPE_CLOSED\"; /** * 容器已到达 `terminal`（`dispose()` 已完成）之后，任何登记/激活操作都抛出。 * * 与 `SCOPE_CLOSED` 分开是因为它是更强的终态信号——`terminal` 不可逆，调用方不该重试，只能新建。 */ readonly scopeTerminal: \"SCOPE_TERMINAL\"; /** * Disposer 内部重入本 scope 的 `dispose()` 时抛出。 * * 错误归属到引发重入的那个 disposer（沿正常的 try/catch 传播），其余资源照常释放完毕。调用方应把 清理逻辑移出 disposer，不要让一个资源的释放去释放正在释放它的那个 * scope。 */ readonly scopeReentrantDispose: \"SCOPE_REENTRANT_DISPOSE\"; /** * Disposer 内部调用本 scope 的 `own()` 时抛出。 * * 同上错误归属规则。释放期间容器已进入 `closing`，再登记新资源没有人会负责释放它。 */ readonly scopeReentrantOwn: \"SCOPE_REENTRANT_OWN\"; /** * `SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor，或收到一个 `LifecycleScope` / * `ProvisionalScope` 实例作为待 own 的资源时，在注册时（而非释放时）立即抛出。 * * 调用方要么把该资源迁到异步 `LifecycleScope`，要么把它的 descriptor 显式标注 `syncSafe: true` 并确保其 force/graceful 从不返回 * thenable。 */ readonly scopeSyncViolation: \"SCOPE_SYNC_VIOLATION\"; /** * `throw` 错误策略在多个资源释放失败时的聚合出口（`AggregateError` 外壳）。 * * 调用方应展开 `error.errors` 逐条处理；单个资源失败时不会用到这个码，错误原样抛出。 */ readonly scopeDisposalFailed: \"SCOPE_DISPOSAL_FAILED\"; /** * `LifecycleUnit.start()` 返回的 thenable 被 reject，单元进入 `failed`。 * * 调用方可以显式调用 `start()`/`restart()` 开启新 generation 重试，或调用 `close()`/`dispose()` 让单元到达 * `terminal`；`failed` 不是无出口终态。 */ readonly unitStartFailed: \"UNIT_START_FAILED\"; /** * 一次异步操作的结果在提交点已被更晚的 generation 取代，结果被丢弃。 * * 这不是失败，是正常的竞态处理信号：调用方通常只需要静默忽略，除非在诊断路径里需要感知它。 */ readonly generationSuperseded: \"GENERATION_SUPERSEDED\"; /** * Generation 取消期间的 timer、parent listener 或 signal cleanup 失败。 generation 作废必须先完成状态收敛并按登记顺序保留所有 * cleanup 失败，落实 lifecycle-extraction.sdd.md §4.10.2；调用方应处理该错误并依赖新 generation，不得复活旧 generation。 */ readonly generationCancellationFailed: \"GENERATION_CANCELLATION_FAILED\"; /** * 在已经 `dispose()` 过的 `GenerationController` 上调用 `begin()`。 * * 该控制器不可复用；调用方应新建一个。 */ readonly generationDisposed: \"GENERATION_DISPOSED\"; /** * `seal()` 之后又调用 `retain()` 时抛出。 * * 封存表示「不再接受新的租约」；调用方要么在 seal 之前完成所有 retain，要么改走 `whenZeroOnce()` 的非独占路径。 */ readonly quiescenceSealed: \"QUIESCENCE_SEALED\"; /** * 未先调用 `seal(key)` 就调用严格 `whenZero(key)` 时，在调用的当次同步抛出（不是返回一个 rejected Promise）。 * * 严格归零等待必须先切断新租约的来源，否则「归零」的保证不成立；调用方应先 `seal()`，或改用 `whenZeroOnce()` 自行承担二次校验的责任。 */ readonly quiescenceUnsealedWait: \"QUIESCENCE_UNSEALED_WAIT\"; /** * 同一个 `ProvisionalScope` 二次 `commitTo()`，或在 `rollback()` 之后再次 `commitTo()`/`rollback()`。 * * Commit/rollback 是二选一的终态动作；调用方的状态机出现了重复调用，需要检查调用路径。 */ readonly provisionalSettled: \"PROVISIONAL_SETTLED\"; /** * `commitTo()` 的目标 parent 已经进入 `closing`/`terminal`，拒绝接收资源。 * * 已转移给 parent 的资源保持在 parent 名下；尚未转移的部分会被这次调用自动释放，调用方不需要 再手动 `rollback()`。 */ readonly provisionalParentClosed: \"PROVISIONAL_PARENT_CLOSED\"; /** * Mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`，被移出队列并拒绝。 * * 这是一条可配置的入队 SLA，不是硬编码行为——未配置时不会触发。调用方应检查是否有任务卡住了 队列前面的位置，或调大阈值。 */ readonly queueAdmissionTimeout: \"QUEUE_ADMISSION_TIMEOUT\"; /** * 同一个 owner 标签在自己尚未完成时又向同一队列提交了新任务，且该提交方正在（通过 `await`） 等待这个新任务——这会死锁，因为 FIFO 队列要求前一个任务先完成。 * * 调用方应避免在一个任务内部同步等待同 owner 的另一个排队任务；必要时拆分成两次独立的顶层调用。 */ readonly queueSelfDependency: \"QUEUE_SELF_DEPENDENCY\"; /** * Descriptor 的 `force` 抛出（或其 Promise 被 reject），而契约要求 `force` 必须无条件成功完成。 * * 错误仍会按当前错误策略收集/上报，释放流程仍会推进到 terminal；调用方应把这个错误当作 「不可恢复的资源泄漏风险」处理，而不是可重试的瞬时失败。 */ readonly releaseForceFailed: \"RELEASE_FORCE_FAILED\"; /** * 共享的绝对 deadline 已经过去，但仍有资源被要求进入一个新的 graceful 阶段。 * * 只在诊断/边界场景下作为显式错误码使用；正常的「graceful 超时降级到 force」走的是降级路径， 不抛出这个码（见下方「不是错误码」的说明）。 */ readonly deadlineExceeded: \"DEADLINE_EXCEEDED\"; /** * `systemScheduler` 依赖的宿主能力（`performance.now` / `setTimeout` / `clearTimeout`）缺失。 * * 首次调用 `now()`（缺 `performance.now`）或 `schedule()`（缺 `setTimeout`/`clearTimeout`）时 * fail-fast。调用方要么注入自实现 `ILifecycleScheduler`，要么在具备这些跨运行时公共 API 的环境里运行。 */ readonly envUnsupported: \"ENV_UNSUPPORTED\"; /** * Scheduler 收到非法的时间/延迟参数：`schedule()` 的 `delayMs` 或 manual `advance(ms)` 的 `ms` 非有限或为负数。 * * 落实 `docs/contracts/runtime-neutrality.sdd.md` R-9/T-16 的「delay 有限非负、now 单调不递减」； * 调用方应修正传入的延迟/推进量，不要重试同一份非法参数。 */ readonly invalidOption: \"INVALID_OPTION\"; };",
          "guidance": [
            {
              "id": "createlifecycleerror",
              "heading": "createLifecycleError",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleError(\n  code: string,\n  message: string,\n  options?: {\n    readonly cause?: unknown\n    readonly phase?: string\n    readonly detail?: Readonly<Record<string, unknown>>\n    readonly errors?: readonly unknown[]\n  }\n): ILifecycleError // Error & { source, code, phase?, detail?, cause?, errors? }"
                },
                {
                  "type": "paragraph",
                  "text": "构造一个携带 `(source, code)` 身份的原生 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "`code`、`message` 均必填（建议 `code` 取自 `LifecycleErrorCode`）。`options.errors` 非空时会冻结后挂到 `.errors`。"
                }
              ]
            },
            {
              "id": "createlifecyclerangeerror",
              "heading": "createLifecycleRangeError",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleRangeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): RangeError"
                },
                {
                  "type": "paragraph",
                  "text": "同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "`code`、`message` 均必填。"
                }
              ]
            },
            {
              "id": "taglifecycleerror",
              "heading": "tagLifecycleError",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function tagLifecycleError<E extends Error>(error: E, code: string): E"
                },
                {
                  "type": "paragraph",
                  "text": "给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "`error`、`code` 均必填。返回同一个对象（原地修改，保留原生类型如 `AggregateError`）。"
                }
              ]
            },
            {
              "id": "错误码",
              "heading": "错误码",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { LifecycleErrorCode, type ILifecycleErrorCode } from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "稳定错误码表，20 个码，唯一声明处 `src/error-code.ts`，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "table",
                  "headers": ["`LifecycleErrorCode` 键", "码值", "触发条件"],
                  "rows": [
                    [
                      "`scopeClosed`",
                      "`SCOPE_CLOSED`",
                      "`close()` 之后调用 `own()`（异步/同步 scope）或某个 registry 的 `retain()`"
                    ],
                    [
                      "`scopeTerminal`",
                      "`SCOPE_TERMINAL`",
                      "容器已到达 `terminal` 之后，任何登记/激活操作"
                    ],
                    [
                      "`scopeReentrantDispose`",
                      "`SCOPE_REENTRANT_DISPOSE`",
                      "disposer 内部重入本 scope 的 `dispose()`"
                    ],
                    [
                      "`scopeReentrantOwn`",
                      "`SCOPE_REENTRANT_OWN`",
                      "disposer 内部调用本 scope 的 `own()`"
                    ],
                    [
                      "`scopeSyncViolation`",
                      "`SCOPE_SYNC_VIOLATION`",
                      "`SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor、收到一个异步 scope 实例作为资源、或某回调返回了 thenable"
                    ],
                    [
                      "`scopeDisposalFailed`",
                      "`SCOPE_DISPOSAL_FAILED`",
                      "`throw` 错误策略在多个资源释放失败时的 `AggregateError` 聚合出口"
                    ],
                    [
                      "`unitStartFailed`",
                      "`UNIT_START_FAILED`",
                      "`LifecycleUnit.start()` 返回的 thenable 被 reject（单元进入 `failed`）"
                    ],
                    [
                      "`generationSuperseded`",
                      "`GENERATION_SUPERSEDED`",
                      "一次异步操作的结果在 `adopt()` 提交点已被更晚的 generation 取代（非失败信号，仅诊断）"
                    ],
                    [
                      "`generationCancellationFailed`",
                      "`GENERATION_CANCELLATION_FAILED`",
                      "generation 取消期间的 timer、parent listener 或 signal cleanup 失败"
                    ],
                    [
                      "`generationDisposed`",
                      "`GENERATION_DISPOSED`",
                      "在已 `dispose()` 的 `GenerationController` 上调用 `begin()`"
                    ],
                    ["`quiescenceSealed`", "`QUIESCENCE_SEALED`", "`seal()` 之后又调用 `retain()`"],
                    [
                      "`quiescenceUnsealedWait`",
                      "`QUIESCENCE_UNSEALED_WAIT`",
                      "未先 `seal(key)` 就调用严格 `whenZero(key)`（同步抛出，非 rejected Promise）"
                    ],
                    [
                      "`provisionalSettled`",
                      "`PROVISIONAL_SETTLED`",
                      "同一个 `ProvisionalScope` 二次 `commitTo()`/`rollback()`，或交叉调用"
                    ],
                    [
                      "`provisionalParentClosed`",
                      "`PROVISIONAL_PARENT_CLOSED`",
                      "`commitTo()` 的目标 parent 已 `closing`/`terminal`，拒绝接收资源"
                    ],
                    [
                      "`queueAdmissionTimeout`",
                      "`QUEUE_ADMISSION_TIMEOUT`",
                      "mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`"
                    ],
                    [
                      "`queueSelfDependency`",
                      "`QUEUE_SELF_DEPENDENCY`",
                      "同一 `owner` 在自己尚未完成时又向同一队列提交新任务且被等待，会死锁"
                    ],
                    [
                      "`releaseForceFailed`",
                      "`RELEASE_FORCE_FAILED`",
                      "descriptor 的 `force` 抛出/reject（契约要求 `force` 必须无条件成功）"
                    ],
                    [
                      "`deadlineExceeded`",
                      "`DEADLINE_EXCEEDED`",
                      "共享绝对 deadline 已过去，但仍有资源被要求进入新的 graceful 阶段（诊断码，正常降级路径不抛这个码）"
                    ],
                    [
                      "`envUnsupported`",
                      "`ENV_UNSUPPORTED`",
                      "`systemScheduler` 依赖的宿主能力（`performance.now`/`setTimeout`/`clearTimeout`）缺失"
                    ],
                    [
                      "`invalidOption`",
                      "`INVALID_OPTION`",
                      "scheduler 收到非法时间/延迟参数，或 descriptor 字段类型非法"
                    ]
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "逐条设计动机见源码 `src/error-code.ts` 的 JSDoc；调用方应始终以 `error.code === LifecycleErrorCode.xxx` 判别，不要硬编码码值字符串。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 88,
          "configuration": [],
          "fragment": "lifecycle-index--LifecycleErrorCode"
        },
        {
          "name": "LifecycleErrorPolicy",
          "kind": "const",
          "signature": "export declare const LifecycleErrorPolicy: { readonly throw: \"throw\"; readonly collect: \"collect\"; readonly report: \"report\"; readonly firstError: \"firstError\"; };",
          "declarationLine": 15,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "Error handling policies supported by disposal transactions.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleErrorPolicy.",
          "advanced": "No additional advanced behavior is declared for LifecycleErrorPolicy.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for LifecycleErrorPolicy."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for LifecycleErrorPolicy.",
          "examples": [
            "import {\n  LifecycleState,\n  LifecycleUnitState,\n  LifecycleErrorPolicy,\n  ThenableProbeKind,\n  DisposeTransactionKind\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "LifecycleErrorPolicy",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const LifecycleErrorPolicy: { readonly throw: \"throw\"; readonly collect: \"collect\"; readonly report: \"report\"; readonly firstError: \"firstError\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use LifecycleErrorPolicy when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when LifecycleErrorPolicy does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Error handling policies supported by disposal transactions."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import LifecycleErrorPolicy from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use LifecycleErrorPolicy when its const contract and declared inputs match the result you need. Choose a different public symbol when LifecycleErrorPolicy does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleErrorPolicy."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for LifecycleErrorPolicy."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:LifecycleErrorPolicy:export declare const LifecycleErrorPolicy: { readonly throw: \"throw\"; readonly collect: \"collect\"; readonly report: \"report\"; readonly firstError: \"firstError\"; };",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "lifecycle-index--LifecycleErrorPolicy"
        },
        {
          "name": "LifecycleErrorText",
          "kind": "const",
          "signature": "export declare const LifecycleErrorText: { /** Fallback message for a provisional registration/cleanup wrapper around a non-Error throw. */ readonly provisionalCleanupFailed: \"[lifecycle] provisional cleanup failed\"; /** Stable finalization message for transaction-level failures. */ readonly disposeTransactionFailed: \"[lifecycle] dispose transaction failed\"; /** Explains that a disposer attempted to join the scope disposal that is waiting for it. */ readonly scopeReentrantDispose: \"[lifecycle] cannot join the same scope disposal from its disposer\"; /** Stable message for a descriptor that cannot be admitted into a release transaction. */ readonly disposeDescriptorInvalid: \"[lifecycle] release descriptor admission failed\"; /** Fallback message for a queue timeout wrapper around a non-Error primary. */ readonly mutationAdmissionTimedOut: \"[lifecycle] mutation admission timed out\"; /** Explains that generation cancellation had multiple cleanup failures. */ readonly generationCancellationFailed: \"[lifecycle] generation cancellation failed\"; /** Explains that a disposal ledger no longer accepts new item callbacks. */ readonly disposalLedgerClosed: \"[lifecycle] disposal ledger is sealed\"; /** Explains that a disposal ledger operation re-entered its active item boundary. */ readonly disposalLedgerReentrant: \"[lifecycle] disposal ledger operation is reentrant\"; /** Explains that the host lacks a usable native AbortController capability. */ readonly envUnsupported: \"[lifecycle] required host capability is unavailable\"; /** Explains that an observed cancellation source lacks its required listener methods. */ readonly abortSignalInvalid: \"[lifecycle] abort signal is invalid\"; /** Explains that a scheduled task's cancel accessor threw during admission. */ readonly schedulerTaskCancelGetterFailed: \"[lifecycle] scheduler task cancel getter failed\"; /** Explains that a scheduler returned a task without the required cancel function. */ readonly schedulerTaskInvalid: \"[lifecycle] scheduler.schedule must return an object with a cancel() function\"; /** Explains that reading a scheduler's methods failed at an injection boundary. */ readonly schedulerAccessorFailed: \"[lifecycle] scheduler accessor failed\"; /** Explains that an injected value is not a valid lifecycle scheduler. */ readonly schedulerInvalid: \"[lifecycle] scheduler must provide now() and schedule() functions\"; /** Stable message for scheduler values whose runtime type is not numeric. */ readonly schedulerNumberType: \"[lifecycle] scheduler time value must be a number\"; /** Stable message for scheduler values outside the finite numeric domain. */ readonly schedulerNumberRange: \"[lifecycle] scheduler time value must be finite\"; /** Stable message for scheduler delays that are finite but negative. */ readonly schedulerDelayRange: \"[lifecycle] scheduler delay must be non-negative\"; /** Stable message for scheduler arithmetic whose finite operands produce an infinite target. */ readonly schedulerTimeOverflow: \"[lifecycle] scheduler time arithmetic must remain finite\"; /** Explains that a generation timeout option could not be read at its admission boundary. */ readonly generationTimeoutAccessorFailed: \"[lifecycle] generation timeout option getter failed\"; };",
          "declarationLine": 2,
          "source": "packages/lifecycle/dist/error-text.d.ts",
          "purpose": "Stable lifecycle diagnostics shared by scheduler and cancellation boundaries.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleErrorText.",
          "advanced": "No additional advanced behavior is declared for LifecycleErrorText.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for LifecycleErrorText."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for LifecycleErrorText.",
          "examples": [
            "import { LifecycleErrorText, type ILifecycleErrorText } from '@migaia/lifecycle'",
            "error.message === LifecycleErrorText.disposeTransactionFailed",
            "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "LifecycleErrorText",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const LifecycleErrorText: { /** Fallback message for a provisional registration/cleanup wrapper around a non-Error throw. */ readonly provisionalCleanupFailed: \"[lifecycle] provisional cleanup failed\"; /** Stable finalization message for transaction-level failures. */ readonly disposeTransactionFailed: \"[lifecycle] dispose transaction failed\"; /** Explains that a disposer attempted to join the scope disposal that is waiting for it. */ readonly scopeReentrantDispose: \"[lifecycle] cannot join the same scope disposal from its disposer\"; /** Stable message for a descriptor that cannot be admitted into a release transaction. */ readonly disposeDescriptorInvalid: \"[lifecycle] release descriptor admission failed\"; /** Fallback message for a queue timeout wrapper around a non-Error primary. */ readonly mutationAdmissionTimedOut: \"[lifecycle] mutation admission timed out\"; /** Explains that generation cancellation had multiple cleanup failures. */ readonly generationCancellationFailed: \"[lifecycle] generation cancellation failed\"; /** Explains that a disposal ledger no longer accepts new item callbacks. */ readonly disposalLedgerClosed: \"[lifecycle] disposal ledger is sealed\"; /** Explains that a disposal ledger operation re-entered its active item boundary. */ readonly disposalLedgerReentrant: \"[lifecycle] disposal ledger operation is reentrant\"; /** Explains that the host lacks a usable native AbortController capability. */ readonly envUnsupported: \"[lifecycle] required host capability is unavailable\"; /** Explains that an observed cancellation source lacks its required listener methods. */ readonly abortSignalInvalid: \"[lifecycle] abort signal is invalid\"; /** Explains that a scheduled task's cancel accessor threw during admission. */ readonly schedulerTaskCancelGetterFailed: \"[lifecycle] scheduler task cancel getter failed\"; /** Explains that a scheduler returned a task without the required cancel function. */ readonly schedulerTaskInvalid: \"[lifecycle] scheduler.schedule must return an object with a cancel() function\"; /** Explains that reading a scheduler's methods failed at an injection boundary. */ readonly schedulerAccessorFailed: \"[lifecycle] scheduler accessor failed\"; /** Explains that an injected value is not a valid lifecycle scheduler. */ readonly schedulerInvalid: \"[lifecycle] scheduler must provide now() and schedule() functions\"; /** Stable message for scheduler values whose runtime type is not numeric. */ readonly schedulerNumberType: \"[lifecycle] scheduler time value must be a number\"; /** Stable message for scheduler values outside the finite numeric domain. */ readonly schedulerNumberRange: \"[lifecycle] scheduler time value must be finite\"; /** Stable message for scheduler delays that are finite but negative. */ readonly schedulerDelayRange: \"[lifecycle] scheduler delay must be non-negative\"; /** Stable message for scheduler arithmetic whose finite operands produce an infinite target. */ readonly schedulerTimeOverflow: \"[lifecycle] scheduler time arithmetic must remain finite\"; /** Explains that a generation timeout option could not be read at its admission boundary. */ readonly generationTimeoutAccessorFailed: \"[lifecycle] generation timeout option getter failed\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use LifecycleErrorText when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when LifecycleErrorText does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Stable lifecycle diagnostics shared by scheduler and cancellation boundaries."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import LifecycleErrorText from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use LifecycleErrorText when its const contract and declared inputs match the result you need. Choose a different public symbol when LifecycleErrorText does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleErrorText."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for LifecycleErrorText."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/error-text.d.ts:LifecycleErrorText:export declare const LifecycleErrorText: { /** Fallback message for a provisional registration/cleanup wrapper around a non-Error throw. */ readonly provisionalCleanupFailed: \"[lifecycle] provisional cleanup failed\"; /** Stable finalization message for transaction-level failures. */ readonly disposeTransactionFailed: \"[lifecycle] dispose transaction failed\"; /** Explains that a disposer attempted to join the scope disposal that is waiting for it. */ readonly scopeReentrantDispose: \"[lifecycle] cannot join the same scope disposal from its disposer\"; /** Stable message for a descriptor that cannot be admitted into a release transaction. */ readonly disposeDescriptorInvalid: \"[lifecycle] release descriptor admission failed\"; /** Fallback message for a queue timeout wrapper around a non-Error primary. */ readonly mutationAdmissionTimedOut: \"[lifecycle] mutation admission timed out\"; /** Explains that generation cancellation had multiple cleanup failures. */ readonly generationCancellationFailed: \"[lifecycle] generation cancellation failed\"; /** Explains that a disposal ledger no longer accepts new item callbacks. */ readonly disposalLedgerClosed: \"[lifecycle] disposal ledger is sealed\"; /** Explains that a disposal ledger operation re-entered its active item boundary. */ readonly disposalLedgerReentrant: \"[lifecycle] disposal ledger operation is reentrant\"; /** Explains that the host lacks a usable native AbortController capability. */ readonly envUnsupported: \"[lifecycle] required host capability is unavailable\"; /** Explains that an observed cancellation source lacks its required listener methods. */ readonly abortSignalInvalid: \"[lifecycle] abort signal is invalid\"; /** Explains that a scheduled task's cancel accessor threw during admission. */ readonly schedulerTaskCancelGetterFailed: \"[lifecycle] scheduler task cancel getter failed\"; /** Explains that a scheduler returned a task without the required cancel function. */ readonly schedulerTaskInvalid: \"[lifecycle] scheduler.schedule must return an object with a cancel() function\"; /** Explains that reading a scheduler's methods failed at an injection boundary. */ readonly schedulerAccessorFailed: \"[lifecycle] scheduler accessor failed\"; /** Explains that an injected value is not a valid lifecycle scheduler. */ readonly schedulerInvalid: \"[lifecycle] scheduler must provide now() and schedule() functions\"; /** Stable message for scheduler values whose runtime type is not numeric. */ readonly schedulerNumberType: \"[lifecycle] scheduler time value must be a number\"; /** Stable message for scheduler values outside the finite numeric domain. */ readonly schedulerNumberRange: \"[lifecycle] scheduler time value must be finite\"; /** Stable message for scheduler delays that are finite but negative. */ readonly schedulerDelayRange: \"[lifecycle] scheduler delay must be non-negative\"; /** Stable message for scheduler arithmetic whose finite operands produce an infinite target. */ readonly schedulerTimeOverflow: \"[lifecycle] scheduler time arithmetic must remain finite\"; /** Explains that a generation timeout option could not be read at its admission boundary. */ readonly generationTimeoutAccessorFailed: \"[lifecycle] generation timeout option getter failed\"; };",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 36,
          "configuration": [],
          "fragment": "lifecycle-index--LifecycleErrorText"
        },
        {
          "name": "LifecycleState",
          "kind": "const",
          "signature": "export declare const LifecycleState: { readonly open: \"open\"; readonly closing: \"closing\"; readonly terminal: \"terminal\"; };",
          "declarationLine": 2,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "Container lifecycle states shared by every lifecycle scope implementation.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleState.",
          "advanced": "No additional advanced behavior is declared for LifecycleState.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for LifecycleState."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for LifecycleState.",
          "examples": [
            "import {\n  LifecycleState,\n  LifecycleUnitState,\n  LifecycleErrorPolicy,\n  ThenableProbeKind,\n  DisposeTransactionKind\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "LifecycleState",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const LifecycleState: { readonly open: \"open\"; readonly closing: \"closing\"; readonly terminal: \"terminal\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use LifecycleState when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when LifecycleState does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Container lifecycle states shared by every lifecycle scope implementation."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import LifecycleState from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use LifecycleState when its const contract and declared inputs match the result you need. Choose a different public symbol when LifecycleState does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleState."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for LifecycleState."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:LifecycleState:export declare const LifecycleState: { readonly open: \"open\"; readonly closing: \"closing\"; readonly terminal: \"terminal\"; };",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "lifecycle-index--LifecycleState"
        },
        {
          "name": "LifecycleUnitState",
          "kind": "const",
          "signature": "export declare const LifecycleUnitState: { readonly idle: \"idle\"; readonly loading: \"loading\"; readonly loaded: \"loaded\"; readonly failed: \"failed\"; };",
          "declarationLine": 8,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "Loading states for lifecycle units.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleUnitState.",
          "advanced": "No additional advanced behavior is declared for LifecycleUnitState.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for LifecycleUnitState."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for LifecycleUnitState.",
          "examples": [
            "import {\n  LifecycleState,\n  LifecycleUnitState,\n  LifecycleErrorPolicy,\n  ThenableProbeKind,\n  DisposeTransactionKind\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "LifecycleUnitState",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const LifecycleUnitState: { readonly idle: \"idle\"; readonly loading: \"loading\"; readonly loaded: \"loaded\"; readonly failed: \"failed\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use LifecycleUnitState when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when LifecycleUnitState does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Loading states for lifecycle units."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import LifecycleUnitState from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use LifecycleUnitState when its const contract and declared inputs match the result you need. Choose a different public symbol when LifecycleUnitState does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LifecycleUnitState."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for LifecycleUnitState."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:LifecycleUnitState:export declare const LifecycleUnitState: { readonly idle: \"idle\"; readonly loading: \"loading\"; readonly loaded: \"loaded\"; readonly failed: \"failed\"; };",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "lifecycle-index--LifecycleUnitState"
        },
        {
          "name": "ThenableProbeKind",
          "kind": "const",
          "signature": "export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; readonly promise: \"promise\"; };",
          "declarationLine": 22,
          "source": "packages/lifecycle/dist/state-constants.d.ts",
          "purpose": "Internal thenable probe outcomes; getter failures remain distinct from plain values.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ThenableProbeKind.",
          "advanced": "No additional advanced behavior is declared for ThenableProbeKind.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for ThenableProbeKind."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ThenableProbeKind.",
          "examples": [
            "import {\n  LifecycleState,\n  LifecycleUnitState,\n  LifecycleErrorPolicy,\n  ThenableProbeKind,\n  DisposeTransactionKind\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "ThenableProbeKind",
          "exportPath": ".",
          "overloadSignatures": [
            "export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; readonly promise: \"promise\"; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ThenableProbeKind when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ThenableProbeKind does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Internal thenable probe outcomes; getter failures remain distinct from plain values."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ThenableProbeKind from @migaia/lifecycle, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ThenableProbeKind when its const contract and declared inputs match the result you need. Choose a different public symbol when ThenableProbeKind does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ThenableProbeKind."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ThenableProbeKind."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/state-constants.d.ts:ThenableProbeKind:export declare const ThenableProbeKind: { readonly notThenable: \"not-thenable\"; readonly thenable: \"thenable\"; readonly failed: \"failed\"; readonly promise: \"promise\"; };",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "lifecycle-index--ThenableProbeKind"
        }
      ],
      "aliases": [
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:containAsyncRejection:export declare function containAsyncRejection(value: unknown, onRejected: (error: unknown) => void): void;",
          "name": "containAsyncRejection",
          "signature": "export declare function containAsyncRejection(value: unknown, onRejected: (error: unknown) => void): void;",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--containAsyncRejection"
        },
        {
          "identity": "packages/lifecycle/dist/abort.d.ts:createAbortController:export declare function createAbortController(): IAbortController;",
          "name": "createAbortController",
          "signature": "export declare function createAbortController(): IAbortController;",
          "source": "packages/lifecycle/dist/abort.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./abort",
          "ownerModule": "abort",
          "ownerFragment": "lifecycle-abort--createAbortController"
        },
        {
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:createDisposeTransaction:export declare function createDisposeTransaction(mode: IDisposeTransactionMode, options?: IDisposeTransactionOptions): IDisposeTransaction;",
          "name": "createDisposeTransaction",
          "signature": "export declare function createDisposeTransaction(mode: IDisposeTransactionMode, options?: IDisposeTransactionOptions): IDisposeTransaction;",
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--createDisposeTransaction"
        },
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:createErrorCollector:export declare function createErrorCollector(policy: IErrorPolicy, report: ((error: unknown) => void) | undefined): IErrorCollector;",
          "name": "createErrorCollector",
          "signature": "export declare function createErrorCollector(policy: IErrorPolicy, report: ((error: unknown) => void) | undefined): IErrorCollector;",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--createErrorCollector"
        },
        {
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:createGenerationController:export declare function createGenerationController(options?: IGenerationControllerOptions): IGenerationController;",
          "name": "createGenerationController",
          "signature": "export declare function createGenerationController(options?: IGenerationControllerOptions): IGenerationController;",
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./generation",
          "ownerModule": "generation",
          "ownerFragment": "lifecycle-generation--createGenerationController"
        },
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:createLifecycleError:export declare function createLifecycleError(code: string, message: string, options?: { readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; }): ILifecycleError;",
          "name": "createLifecycleError",
          "signature": "export declare function createLifecycleError(code: string, message: string, options?: { readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; }): ILifecycleError;",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--createLifecycleError"
        },
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:createLifecycleRangeError:export declare function createLifecycleRangeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): RangeError;",
          "name": "createLifecycleRangeError",
          "signature": "export declare function createLifecycleRangeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): RangeError;",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--createLifecycleRangeError"
        },
        {
          "identity": "packages/lifecycle/dist/lifecycle-scope.d.ts:createLifecycleScope:export declare function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope;",
          "name": "createLifecycleScope",
          "signature": "export declare function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope;",
          "source": "packages/lifecycle/dist/lifecycle-scope.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scope",
          "ownerModule": "scope",
          "ownerFragment": "lifecycle-scope--createLifecycleScope"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:createManualScheduler:export declare function createManualScheduler(): IManualScheduler;",
          "name": "createManualScheduler",
          "signature": "export declare function createManualScheduler(): IManualScheduler;",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--createManualScheduler"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createObjectLeaseRegistry:export declare function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>;",
          "name": "createObjectLeaseRegistry",
          "signature": "export declare function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>;",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--createObjectLeaseRegistry"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createPendingTracker:export declare function createPendingTracker(): IPendingTracker;",
          "name": "createPendingTracker",
          "signature": "export declare function createPendingTracker(): IPendingTracker;",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--createPendingTracker"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createQuiescenceTracker:export declare function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>;",
          "name": "createQuiescenceTracker",
          "signature": "export declare function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>;",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--createQuiescenceTracker"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createStringLeaseRegistry:export declare function createStringLeaseRegistry(): ILeaseRegistry<string>;",
          "name": "createStringLeaseRegistry",
          "signature": "export declare function createStringLeaseRegistry(): ILeaseRegistry<string>;",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--createStringLeaseRegistry"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createStringQuiescenceTracker:export declare function createStringQuiescenceTracker(): IQuiescenceTracker<string>;",
          "name": "createStringQuiescenceTracker",
          "signature": "export declare function createStringQuiescenceTracker(): IQuiescenceTracker<string>;",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--createStringQuiescenceTracker"
        },
        {
          "identity": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts:createSyncStartedDisposalLedger:export declare function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger;",
          "name": "createSyncStartedDisposalLedger",
          "signature": "export declare function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger;",
          "source": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--createSyncStartedDisposalLedger"
        },
        {
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:executeReleaseDescriptor:export declare function executeReleaseDescriptor(descriptor: IReleaseDescriptor, context: IReleaseContext): Promise<readonly unknown[]>;",
          "name": "executeReleaseDescriptor",
          "signature": "export declare function executeReleaseDescriptor(descriptor: IReleaseDescriptor, context: IReleaseContext): Promise<readonly unknown[]>;",
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--executeReleaseDescriptor"
        },
        {
          "identity": "packages/lifecycle/dist/abort.d.ts:IAbortController:export type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "name": "IAbortController",
          "signature": "export type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "source": "packages/lifecycle/dist/abort.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./abort",
          "ownerModule": "abort",
          "ownerFragment": "lifecycle-abort--IAbortController"
        },
        {
          "identity": "packages/lifecycle/dist/abort.d.ts:IAbortSignal:export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "name": "IAbortSignal",
          "signature": "export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "source": "packages/lifecycle/dist/abort.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./abort",
          "ownerModule": "abort",
          "ownerFragment": "lifecycle-abort--IAbortSignal"
        },
        {
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeItem:export type IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor; };",
          "name": "IDisposeItem",
          "signature": "export type IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor; };",
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--IDisposeItem"
        },
        {
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeTransaction:export type IDisposeTransaction = { readonly mode: IDisposeTransactionMode; run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>; };",
          "name": "IDisposeTransaction",
          "signature": "export type IDisposeTransaction = { readonly mode: IDisposeTransactionMode; run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>; };",
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--IDisposeTransaction"
        },
        {
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeTransactionMode:export type IDisposeTransactionMode = { readonly kind: typeof DisposeTransactionKind.order; } | { readonly kind: typeof DisposeTransactionKind.plan; };",
          "name": "IDisposeTransactionMode",
          "signature": "export type IDisposeTransactionMode = { readonly kind: typeof DisposeTransactionKind.order; } | { readonly kind: typeof DisposeTransactionKind.plan; };",
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--IDisposeTransactionMode"
        },
        {
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeTransactionOptions:export type IDisposeTransactionOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Absolute deadline shared, unchanged, across every item in this run (L-T32). */ readonly deadlineAt?: number; /** * Scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）；缺省 `systemScheduler`。 Forwarded into * every item's `context.scheduler` and into the graceful-phase `boundedWait`. */ readonly scheduler?: ILifecycleScheduler; /** Forwarded into `context.signal` for every item; aborted automatically once `run()` starts. */ readonly signal?: IAbortSignal; /** Owner-bound self-join capability, supplied only by a LifecycleScope owner. */ readonly disposer?: IDisposerContext; /** Awaited after every item settles, so in-flight work started by a release can still be drained. */ readonly pending?: { drain(): Promise<void>; }; };",
          "name": "IDisposeTransactionOptions",
          "signature": "export type IDisposeTransactionOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Absolute deadline shared, unchanged, across every item in this run (L-T32). */ readonly deadlineAt?: number; /** * Scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）；缺省 `systemScheduler`。 Forwarded into * every item's `context.scheduler` and into the graceful-phase `boundedWait`. */ readonly scheduler?: ILifecycleScheduler; /** Forwarded into `context.signal` for every item; aborted automatically once `run()` starts. */ readonly signal?: IAbortSignal; /** Owner-bound self-join capability, supplied only by a LifecycleScope owner. */ readonly disposer?: IDisposerContext; /** Awaited after every item settles, so in-flight work started by a release can still be drained. */ readonly pending?: { drain(): Promise<void>; }; };",
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--IDisposeTransactionOptions"
        },
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:IErrorCollector:export type IErrorCollector = { readonly policy: IErrorPolicy; /** Records one item's failure. For `report`, this is also where the reporter is invoked. */ add(source: string, error: unknown): void; /** * Finalizes collection. `collect`/`report`/`firstError`-with-no-error return the collected list * (empty for `report`, since those were already handed to the reporter per `add`); `throw` and * `firstError`-with-an-error throw instead of returning. */ finalize(message: string): readonly ICollectedError[]; };",
          "name": "IErrorCollector",
          "signature": "export type IErrorCollector = { readonly policy: IErrorPolicy; /** Records one item's failure. For `report`, this is also where the reporter is invoked. */ add(source: string, error: unknown): void; /** * Finalizes collection. `collect`/`report`/`firstError`-with-no-error return the collected list * (empty for `report`, since those were already handed to the reporter per `add`); `throw` and * `firstError`-with-an-error throw instead of returning. */ finalize(message: string): readonly ICollectedError[]; };",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--IErrorCollector"
        },
        {
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationController:export type IGenerationController = { readonly generation: number; readonly disposed: boolean; /** * Starts a new generation, superseding whatever was active. Optionally auto-aborts after * `timeoutMs`. */ begin(options?: { readonly timeoutMs?: number; }): IGenerationRequest; isCurrent(token: IGenerationToken): boolean; /** * Invalidates the current generation without disposing the controller — a later `begin()` still * works. */ supersede(reason?: unknown): void; /** * Adopts `value` under `token`'s generation. Returns `true` and keeps `value` with the caller if * that generation is still current. Otherwise releases `value` via `release` and returns `false` * (L-T6) — a release failure is funneled to `onReleaseError` and never mutates controller state, * so it cannot poison whichever generation is current now (L-T30). */ adopt<T>(token: IGenerationToken, value: T, release: (value: T) => void | PromiseLike<void>, onReleaseError?: (error: unknown) => void): boolean; dispose(reason?: unknown): void; };",
          "name": "IGenerationController",
          "signature": "export type IGenerationController = { readonly generation: number; readonly disposed: boolean; /** * Starts a new generation, superseding whatever was active. Optionally auto-aborts after * `timeoutMs`. */ begin(options?: { readonly timeoutMs?: number; }): IGenerationRequest; isCurrent(token: IGenerationToken): boolean; /** * Invalidates the current generation without disposing the controller — a later `begin()` still * works. */ supersede(reason?: unknown): void; /** * Adopts `value` under `token`'s generation. Returns `true` and keeps `value` with the caller if * that generation is still current. Otherwise releases `value` via `release` and returns `false` * (L-T6) — a release failure is funneled to `onReleaseError` and never mutates controller state, * so it cannot poison whichever generation is current now (L-T30). */ adopt<T>(token: IGenerationToken, value: T, release: (value: T) => void | PromiseLike<void>, onReleaseError?: (error: unknown) => void): boolean; dispose(reason?: unknown): void; };",
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./generation",
          "ownerModule": "generation",
          "ownerFragment": "lifecycle-generation--IGenerationController"
        },
        {
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationControllerOptions:export type IGenerationControllerOptions = { /** When this aborts, the currently-active generation's `signal` aborts too (L-T31). */ readonly parentSignal?: IAbortSignal; /** * Diagnostic channel fired from `adopt()` whenever `token` is no longer current — before the * release attempt, regardless of whether it succeeds. The value is a * `GENERATION_SUPERSEDED`-tagged error used purely as a `(source, code)`-bearing carrier; this is * explicitly _not_ a failure signal (§4.10.2), just an observability hook for callers that want * to notice the race without treating it as an error. */ readonly onSuperseded?: (info: ILifecycleError) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；`begin({ timeoutMs })` 的超时计时经它。 */ readonly scheduler?: ILifecycleScheduler; };",
          "name": "IGenerationControllerOptions",
          "signature": "export type IGenerationControllerOptions = { /** When this aborts, the currently-active generation's `signal` aborts too (L-T31). */ readonly parentSignal?: IAbortSignal; /** * Diagnostic channel fired from `adopt()` whenever `token` is no longer current — before the * release attempt, regardless of whether it succeeds. The value is a * `GENERATION_SUPERSEDED`-tagged error used purely as a `(source, code)`-bearing carrier; this is * explicitly _not_ a failure signal (§4.10.2), just an observability hook for callers that want * to notice the race without treating it as an error. */ readonly onSuperseded?: (info: ILifecycleError) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；`begin({ timeoutMs })` 的超时计时经它。 */ readonly scheduler?: ILifecycleScheduler; };",
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./generation",
          "ownerModule": "generation",
          "ownerFragment": "lifecycle-generation--IGenerationControllerOptions"
        },
        {
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationRequest:export type IGenerationRequest = { readonly generation: number; readonly token: IGenerationToken; readonly signal: IAbortSignal; };",
          "name": "IGenerationRequest",
          "signature": "export type IGenerationRequest = { readonly generation: number; readonly token: IGenerationToken; readonly signal: IAbortSignal; };",
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./generation",
          "ownerModule": "generation",
          "ownerFragment": "lifecycle-generation--IGenerationRequest"
        },
        {
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationToken:export type IGenerationToken = object;",
          "name": "IGenerationToken",
          "signature": "export type IGenerationToken = object;",
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./generation",
          "ownerModule": "generation",
          "ownerFragment": "lifecycle-generation--IGenerationToken"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:ILeaseRegistry:export type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>;",
          "name": "ILeaseRegistry",
          "signature": "export type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>;",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--ILeaseRegistry"
        },
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:ILifecycleError:export type ILifecycleError = Error & { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly cause?: unknown; /** 冻结的次要失败快照（如 cleanup 失败），不与 `cause` 混用；primary 保持原错误身份。 */ readonly errors?: readonly unknown[]; };",
          "name": "ILifecycleError",
          "signature": "export type ILifecycleError = Error & { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly cause?: unknown; /** 冻结的次要失败快照（如 cleanup 失败），不与 `cause` 混用；primary 保持原错误身份。 */ readonly errors?: readonly unknown[]; };",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--ILifecycleError"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:ILifecycleScheduler:export type ILifecycleScheduler = { /** 单调不递减毫秒（连续两次可相等）；只比较差值，不解释为 Unix epoch。 */ now(): number; /** 排程一个回调；`delayMs` 有限、非负；回调至多执行一次。 */ schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "name": "ILifecycleScheduler",
          "signature": "export type ILifecycleScheduler = { /** 单调不递减毫秒（连续两次可相等）；只比较差值，不解释为 Unix epoch。 */ now(): number; /** 排程一个回调；`delayMs` 有限、非负；回调至多执行一次。 */ schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--ILifecycleScheduler"
        },
        {
          "identity": "packages/lifecycle/dist/lifecycle-scope.d.ts:ILifecycleScope:export type ILifecycleScope = ILifecycleOwner & { readonly lifecycle: ILifecycleState; /** Unregisters a resource without releasing it — the caller has already released it independently. */ release(resource: unknown): boolean; /** Synchronous, idempotent, never calls user code (L-T27). */ close(): void; /** * Always asynchronous (D-1) — there is no synchronous `dispose()` on this type. Resolves to the * collected errors for the `collect` policy (empty for the other three, which throw/report * internally instead). * * Concurrent external calls reuse the same published Promise even while release is in flight. * Only a synchronous call from an active disposer callback is rejected as reentrant, preventing * that callback from self-awaiting the Promise it is currently helping to settle. */ dispose(): Promise<readonly ICollectedError[]>; };",
          "name": "ILifecycleScope",
          "signature": "export type ILifecycleScope = ILifecycleOwner & { readonly lifecycle: ILifecycleState; /** Unregisters a resource without releasing it — the caller has already released it independently. */ release(resource: unknown): boolean; /** Synchronous, idempotent, never calls user code (L-T27). */ close(): void; /** * Always asynchronous (D-1) — there is no synchronous `dispose()` on this type. Resolves to the * collected errors for the `collect` policy (empty for the other three, which throw/report * internally instead). * * Concurrent external calls reuse the same published Promise even while release is in flight. * Only a synchronous call from an active disposer callback is rejected as reentrant, preventing * that callback from self-awaiting the Promise it is currently helping to settle. */ dispose(): Promise<readonly ICollectedError[]>; };",
          "source": "packages/lifecycle/dist/lifecycle-scope.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scope",
          "ownerModule": "scope",
          "ownerFragment": "lifecycle-scope--ILifecycleScope"
        },
        {
          "identity": "packages/lifecycle/dist/lifecycle-scope.d.ts:ILifecycleScopeOptions:export type ILifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Shared absolute deadline forwarded to every owned resource's release (L-T32). */ readonly deadlineAt?: number; /** * 时间域来源（R-9）：graceful timeout / deadline 经它驱动，缺省 `systemScheduler`。透传给 * `DisposeTransaction`，使注入方（如 plugin-host）的 teardown 与 mutation queue 处于同一时间域。 */ readonly scheduler?: ILifecycleScheduler; };",
          "name": "ILifecycleScopeOptions",
          "signature": "export type ILifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Shared absolute deadline forwarded to every owned resource's release (L-T32). */ readonly deadlineAt?: number; /** * 时间域来源（R-9）：graceful timeout / deadline 经它驱动，缺省 `systemScheduler`。透传给 * `DisposeTransaction`，使注入方（如 plugin-host）的 teardown 与 mutation queue 处于同一时间域。 */ readonly scheduler?: ILifecycleScheduler; };",
          "source": "packages/lifecycle/dist/lifecycle-scope.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scope",
          "ownerModule": "scope",
          "ownerFragment": "lifecycle-scope--ILifecycleScopeOptions"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:IManualScheduler:export type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void; };",
          "name": "IManualScheduler",
          "signature": "export type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void; };",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--IManualScheduler"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:IPendingTracker:export type IPendingTracker = { /** Registers `promise` as in-flight; auto-releases on settle. Returns `promise` unchanged. */ track<T>(promise: Promise<T>): Promise<T>; /** * Waits until nothing is in flight, looping via `whenZeroOnce()` so work started while draining * is never missed (L-T10). */ drain(): Promise<void>; readonly size: number; };",
          "name": "IPendingTracker",
          "signature": "export type IPendingTracker = { /** Registers `promise` as in-flight; auto-releases on settle. Returns `promise` unchanged. */ track<T>(promise: Promise<T>): Promise<T>; /** * Waits until nothing is in flight, looping via `whenZeroOnce()` so work started while draining * is never missed (L-T10). */ drain(): Promise<void>; readonly size: number; };",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--IPendingTracker"
        },
        {
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:IQuiescenceTracker:export type IQuiescenceTracker<TKey> = { retain(key: TKey): IDisposer; count(key: TKey): number; /** Strict, exclusive drain: throws synchronously (not a rejected Promise) unless `isSealed(key)`. */ whenZero(key: TKey): Promise<void>; /** Non-exclusive: resolves the next time count reaches zero; does not block new `retain()` calls. */ whenZeroOnce(key: TKey): Promise<void>; /** Blocks further `retain()` calls for `key`. Idempotent. */ seal(key: TKey): void; isSealed(key: TKey): boolean; /** Removes a zero-count key and returns whether a state entry was removed. */ forget(key: TKey): boolean; };",
          "name": "IQuiescenceTracker",
          "signature": "export type IQuiescenceTracker<TKey> = { retain(key: TKey): IDisposer; count(key: TKey): number; /** Strict, exclusive drain: throws synchronously (not a rejected Promise) unless `isSealed(key)`. */ whenZero(key: TKey): Promise<void>; /** Non-exclusive: resolves the next time count reaches zero; does not block new `retain()` calls. */ whenZeroOnce(key: TKey): Promise<void>; /** Blocks further `retain()` calls for `key`. Idempotent. */ seal(key: TKey): void; isSealed(key: TKey): boolean; /** Removes a zero-count key and returns whether a state entry was removed. */ forget(key: TKey): boolean; };",
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./quiescence",
          "ownerModule": "quiescence",
          "ownerFragment": "lifecycle-quiescence--IQuiescenceTracker"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:IScheduledTask:export type IScheduledTask = { cancel(): void; };",
          "name": "IScheduledTask",
          "signature": "export type IScheduledTask = { cancel(): void; };",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--IScheduledTask"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:ISchedulerSnapshot:export type ISchedulerSnapshot = ILifecycleScheduler;",
          "name": "ISchedulerSnapshot",
          "signature": "export type ISchedulerSnapshot = ILifecycleScheduler;",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--ISchedulerSnapshot"
        },
        {
          "identity": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts:ISyncStartedDisposalLedger:export type ISyncStartedDisposalLedger = { readonly lifecycle: ILifecycleState; readonly pending: number; start(source: string, callback: () => unknown): void; seal(): ISyncStartedDisposalOutcome; whenTerminal(): Promise<void>; };",
          "name": "ISyncStartedDisposalLedger",
          "signature": "export type ISyncStartedDisposalLedger = { readonly lifecycle: ILifecycleState; readonly pending: number; start(source: string, callback: () => unknown): void; seal(): ISyncStartedDisposalOutcome; whenTerminal(): Promise<void>; };",
          "source": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--ISyncStartedDisposalLedger"
        },
        {
          "identity": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts:ISyncStartedDisposalOutcome:export type ISyncStartedDisposalOutcome = Readonly<{ synchronousErrors: readonly ICollectedError[]; completion: Promise<readonly ICollectedError[]>; }>;",
          "name": "ISyncStartedDisposalOutcome",
          "signature": "export type ISyncStartedDisposalOutcome = Readonly<{ synchronousErrors: readonly ICollectedError[]; completion: Promise<readonly ICollectedError[]>; }>;",
          "source": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./disposal",
          "ownerModule": "disposal",
          "ownerFragment": "lifecycle-disposal--ISyncStartedDisposalOutcome"
        },
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:LIFECYCLE_SOURCE:export declare const LIFECYCLE_SOURCE = \"@migaia/lifecycle\";",
          "name": "LIFECYCLE_SOURCE",
          "signature": "export declare const LIFECYCLE_SOURCE = \"@migaia/lifecycle\";",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--LIFECYCLE_SOURCE"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:snapshotScheduler:export declare function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined;",
          "name": "snapshotScheduler",
          "signature": "export declare function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined;",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--snapshotScheduler"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:systemScheduler:export declare const systemScheduler: ILifecycleScheduler;",
          "name": "systemScheduler",
          "signature": "export declare const systemScheduler: ILifecycleScheduler;",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--systemScheduler"
        },
        {
          "identity": "packages/lifecycle/dist/errors.d.ts:tagLifecycleError:export declare function tagLifecycleError<E extends Error>(error: E, code: string): E;",
          "name": "tagLifecycleError",
          "signature": "export declare function tagLifecycleError<E extends Error>(error: E, code: string): E;",
          "source": "packages/lifecycle/dist/errors.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./errors",
          "ownerModule": "errors",
          "ownerFragment": "lifecycle-errors--tagLifecycleError"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:validateSchedulerDelay:export declare function validateSchedulerDelay(value: unknown, label?: string): number;",
          "name": "validateSchedulerDelay",
          "signature": "export declare function validateSchedulerDelay(value: unknown, label?: string): number;",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--validateSchedulerDelay"
        },
        {
          "identity": "packages/lifecycle/dist/scheduler.d.ts:validateSchedulerTime:export declare function validateSchedulerTime(value: unknown, label: string): number;",
          "name": "validateSchedulerTime",
          "signature": "export declare function validateSchedulerTime(value: unknown, label: string): number;",
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "exportPath": ".",
          "ownerExportPath": "./scheduler",
          "ownerModule": "scheduler",
          "ownerFragment": "lifecycle-scheduler--validateSchedulerTime"
        }
      ]
    },
    {
      "id": "lifecycle:abort",
      "library": "lifecycle",
      "module": "abort",
      "exportPath": "./abort",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "createAbortController",
          "kind": "function",
          "signature": "export declare function createAbortController(): IAbortController;",
          "declarationLine": 17,
          "source": "packages/lifecycle/dist/abort.d.ts",
          "purpose": "Creates one actual host-native AbortController instance in the current realm.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createAbortController.",
          "advanced": "No additional advanced behavior is declared for createAbortController.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IAbortController",
          "errors": ["No documented errors are declared for createAbortController."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createAbortController.",
          "examples": [
            "const controller = createAbortController()\ncontroller.signal.addEventListener('abort', () => console.log('aborted'))\ncontroller.abort('reason')"
          ],
          "exportPath": "./abort",
          "overloadSignatures": [
            "export declare function createAbortController(): IAbortController;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IAbortController"],
          "whenToUse": "Use createAbortController when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createAbortController does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates one actual host-native AbortController instance in the current realm."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createAbortController from @migaia/lifecycle/abort, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createAbortController when its function contract and declared inputs match the result you need. Choose a different public symbol when createAbortController does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createAbortController."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createAbortController."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/abort.d.ts:createAbortController:export declare function createAbortController(): IAbortController;",
          "guidance": [
            {
              "id": "createabortcontroller",
              "heading": "createAbortController",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createAbortController(): IAbortController\n// type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(...); removeEventListener(...) };\n// type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void };"
                },
                {
                  "type": "paragraph",
                  "text": "本包委托当前宿主的原生 `AbortController`，返回真实 host instance（结构上与 DOM/Node 的 `AbortSignal` 兼容，可互相传递）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const controller = createAbortController()\ncontroller.signal.addEventListener('abort', () => console.log('aborted'))\ncontroller.abort('reason')"
                },
                {
                  "type": "paragraph",
                  "text": "无构造参数，也不接受 custom factory。实际对象由当前 realm 的 host-owned `globalThis.AbortController` 构造；lifecycle 不返回 facade、Proxy 或自建 EventTarget。宿主 constructor 缺失、getter throw、不可构造、构造失败或结果不满足最小 controller/signal shape时，抛保留原始 cause 的 `ENV_UNSUPPORTED`。"
                },
                {
                  "type": "paragraph",
                  "text": "V2 intentional breaking behavior："
                },
                {
                  "type": "list",
                  "items": [
                    "`abort()` 与 `abort(undefined)` 使用宿主默认 reason，通常是原生 `DOMException`（name 为 `AbortError`），不再保留 V1 的 `undefined`。",
                    "non-`undefined` explicit reason保持 `===` identity；把 structural parent signal 的 `undefined` reason转发给native child时，同样规范化为宿主 `AbortError`。",
                    "重复 `abort()` 幂等，不覆盖第一次固化的 reason。",
                    "外部 listener throw走宿主 EventTarget error channel；`try/catch controller.abort()` 不能同步捕获它，lifecycle也不再提供 `ABORT_LISTENER_FAILED`。",
                    "Lifecycle 自己登记的 parent/cancellation callback 经过 module-private observed-subscription：处理 already-aborted、registration-time abort、post-registration recheck和幂等remove，并把自身 callback/read/cleanup failure交给对应 operation的collect/report边界。它不代理、枚举或捕获同一 signal 上的外部 listener。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "如果业务需要旧式“listener错误由调用 abort 的栈聚合抛出”，应使用业务自己拥有的回调编排器；不要包裹或 monkey-patch native `AbortSignal`。"
                }
              ]
            }
          ],
          "usageScore": 82,
          "configuration": [],
          "fragment": "lifecycle-abort--createAbortController"
        },
        {
          "name": "IAbortController",
          "kind": "type",
          "signature": "export type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "declarationLine": 12,
          "source": "packages/lifecycle/dist/abort.d.ts",
          "purpose": "Structural controller returned by the public factory.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortController.",
          "advanced": "No additional advanced behavior is declared for IAbortController.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortController."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortController.",
          "examples": [
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "function createAbortController(): IAbortController\n// type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(...); removeEventListener(...) };\n// type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void };"
          ],
          "exportPath": "./abort",
          "overloadSignatures": [
            "export type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortController when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortController does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Structural controller returned by the public factory."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortController from @migaia/lifecycle/abort, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortController when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortController does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortController."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortController."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/abort.d.ts:IAbortController:export type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void; };",
          "guidance": [],
          "usageScore": 32,
          "configuration": [],
          "fragment": "lifecycle-abort--IAbortController"
        },
        {
          "name": "IAbortSignal",
          "kind": "type",
          "signature": "export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "declarationLine": 3,
          "source": "packages/lifecycle/dist/abort.d.ts",
          "purpose": "Structural abort signal consumed by lifecycle owners without importing DOM or Node types.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignal.",
          "advanced": "No additional advanced behavior is declared for IAbortSignal.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IAbortSignal."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IAbortSignal.",
          "examples": [
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}",
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}",
            "function createProvisionalScope(options?: IProvisionalScopeOptions): IProvisionalScope\n\ntype IProvisionalScopeOptions = { readonly parentSignal?: IAbortSignal }\n\ntype IProvisionalScope = {\n  readonly signal: IAbortSignal\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  commitTo(parent: ILifecycleOwner): Promise<void>\n  rollback(): Promise<void>\n}\n\ntype ILifecycleOwner = { own<T>(resource: T, descriptor: IReleaseDescriptor): T }",
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "function createAbortController(): IAbortController\n// type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(...); removeEventListener(...) };\n// type IAbortController = { readonly signal: IAbortSignal; abort(reason?: unknown): void };"
          ],
          "exportPath": "./abort",
          "overloadSignatures": [
            "export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IAbortSignal when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IAbortSignal does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Structural abort signal consumed by lifecycle owners without importing DOM or Node types."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IAbortSignal from @migaia/lifecycle/abort, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IAbortSignal when its type contract and declared inputs match the result you need. Choose a different public symbol when IAbortSignal does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IAbortSignal."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IAbortSignal."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/abort.d.ts:IAbortSignal:export type IAbortSignal = { readonly aborted: boolean; readonly reason?: unknown; addEventListener(type: 'abort', listener: () => void, options?: { readonly once?: boolean; }): void; removeEventListener(type: 'abort', listener: () => void): void; };",
          "guidance": [],
          "usageScore": 80,
          "configuration": [],
          "fragment": "lifecycle-abort--IAbortSignal"
        },
        {
          "name": "IObservedAbortFailureSink",
          "kind": "type",
          "signature": "export type IObservedAbortFailureSink = (error: unknown) => void;",
          "declarationLine": 10,
          "source": "packages/lifecycle/dist/observed-subscription.d.ts",
          "purpose": "Operation-owned sink for callback, reason-read, and cleanup failures.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObservedAbortFailureSink.",
          "advanced": "No additional advanced behavior is declared for IObservedAbortFailureSink.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObservedAbortFailureSink."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObservedAbortFailureSink.",
          "examples": [
            "import {\n  createAbortController,\n  observeAbortSubscription,\n  type IObservedAbortFailureSink,\n  type IObservedAbortSubscription\n} from '@migaia/lifecycle/abort'\nimport { snapshotScheduler } from '@migaia/lifecycle/scheduler'\nimport { createPendingTracker } from '@migaia/lifecycle/quiescence'\nimport { createLifecycleScope } from '@migaia/lifecycle/scope'\nimport { createGenerationController } from '@migaia/lifecycle/generation'\nimport { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\nimport { probeThenable } from '@migaia/lifecycle/errors'"
          ],
          "aliasOf": "IObservedAbortFailureSink",
          "exportPath": "./abort",
          "overloadSignatures": [
            "export type IObservedAbortFailureSink = (error: unknown) => void;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObservedAbortFailureSink when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObservedAbortFailureSink does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Operation-owned sink for callback, reason-read, and cleanup failures."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObservedAbortFailureSink from @migaia/lifecycle/abort, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObservedAbortFailureSink when its type contract and declared inputs match the result you need. Choose a different public symbol when IObservedAbortFailureSink does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObservedAbortFailureSink."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObservedAbortFailureSink."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/observed-subscription.d.ts:IObservedAbortFailureSink:export type IObservedAbortFailureSink = (error: unknown) => void;",
          "guidance": [],
          "usageScore": 12,
          "configuration": [],
          "fragment": "lifecycle-abort--IObservedAbortFailureSink"
        },
        {
          "name": "IObservedAbortSubscription",
          "kind": "type",
          "signature": "export type IObservedAbortSubscription = { unsubscribe(): void; /** Re-attempts cleanup after a host stores a listener after invoking it during registration. */ retryRegistrationCleanup(): void; readonly failures: readonly unknown[]; };",
          "declarationLine": 3,
          "source": "packages/lifecycle/dist/observed-subscription.d.ts",
          "purpose": "Idempotent handle for one lifecycle-owned abort registration.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObservedAbortSubscription.",
          "advanced": "No additional advanced behavior is declared for IObservedAbortSubscription.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IObservedAbortSubscription."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IObservedAbortSubscription.",
          "examples": [
            "import {\n  createAbortController,\n  observeAbortSubscription,\n  type IObservedAbortFailureSink,\n  type IObservedAbortSubscription\n} from '@migaia/lifecycle/abort'\nimport { snapshotScheduler } from '@migaia/lifecycle/scheduler'\nimport { createPendingTracker } from '@migaia/lifecycle/quiescence'\nimport { createLifecycleScope } from '@migaia/lifecycle/scope'\nimport { createGenerationController } from '@migaia/lifecycle/generation'\nimport { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\nimport { probeThenable } from '@migaia/lifecycle/errors'"
          ],
          "aliasOf": "IObservedAbortSubscription",
          "exportPath": "./abort",
          "overloadSignatures": [
            "export type IObservedAbortSubscription = { unsubscribe(): void; /** Re-attempts cleanup after a host stores a listener after invoking it during registration. */ retryRegistrationCleanup(): void; readonly failures: readonly unknown[]; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IObservedAbortSubscription when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IObservedAbortSubscription does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Idempotent handle for one lifecycle-owned abort registration."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IObservedAbortSubscription from @migaia/lifecycle/abort, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IObservedAbortSubscription when its type contract and declared inputs match the result you need. Choose a different public symbol when IObservedAbortSubscription does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IObservedAbortSubscription."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IObservedAbortSubscription."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/observed-subscription.d.ts:IObservedAbortSubscription:export type IObservedAbortSubscription = { unsubscribe(): void; /** Re-attempts cleanup after a host stores a listener after invoking it during registration. */ retryRegistrationCleanup(): void; readonly failures: readonly unknown[]; };",
          "guidance": [],
          "usageScore": 12,
          "configuration": [],
          "fragment": "lifecycle-abort--IObservedAbortSubscription"
        },
        {
          "name": "observeAbortSubscription",
          "kind": "function",
          "signature": "export declare function observeAbortSubscription(signal: IAbortSignal, callback: (reason: unknown) => void, onFailure: IObservedAbortFailureSink): IObservedAbortSubscription;",
          "declarationLine": 16,
          "source": "packages/lifecycle/dist/observed-subscription.d.ts",
          "purpose": "Registers one lifecycle-owned abort callback with a hostile-signal-safe receiver snapshot. Registration races are closed by a post-add state recheck; callback and cleanup failures are delivered to the owning operation and never rethrown through native EventTarget dispatch.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for observeAbortSubscription.",
          "advanced": "No additional advanced behavior is declared for observeAbortSubscription.",
          "parameters": [
            "signal: IAbortSignal",
            "callback: (reason: unknown) => void",
            "onFailure: IObservedAbortFailureSink"
          ],
          "parameterDetails": [
            {
              "name": "signal",
              "type": "IAbortSignal",
              "optional": false
            },
            {
              "name": "callback",
              "type": "(reason: unknown) => void",
              "optional": false
            },
            {
              "name": "onFailure",
              "type": "IObservedAbortFailureSink",
              "optional": false
            }
          ],
          "returns": "IObservedAbortSubscription",
          "errors": ["No documented errors are declared for observeAbortSubscription."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for observeAbortSubscription.",
          "examples": [],
          "aliasOf": "observeAbortSubscription",
          "exportPath": "./abort",
          "overloadSignatures": [
            "export declare function observeAbortSubscription(signal: IAbortSignal, callback: (reason: unknown) => void, onFailure: IObservedAbortFailureSink): IObservedAbortSubscription;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "signal",
                "type": "IAbortSignal",
                "optional": false
              },
              {
                "name": "callback",
                "type": "(reason: unknown) => void",
                "optional": false
              },
              {
                "name": "onFailure",
                "type": "IObservedAbortFailureSink",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IObservedAbortSubscription"],
          "whenToUse": "Use observeAbortSubscription when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when observeAbortSubscription does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Registers one lifecycle-owned abort callback with a hostile-signal-safe receiver snapshot. Registration races are closed by a post-add state recheck; callback and cleanup failures are delivered to the owning operation and never rethrown through native EventTarget dispatch."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import observeAbortSubscription from @migaia/lifecycle/abort, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use observeAbortSubscription when its function contract and declared inputs match the result you need. Choose a different public symbol when observeAbortSubscription does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for observeAbortSubscription."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for observeAbortSubscription."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/observed-subscription.d.ts:observeAbortSubscription:export declare function observeAbortSubscription(signal: IAbortSignal, callback: (reason: unknown) => void, onFailure: IObservedAbortFailureSink): IObservedAbortSubscription;",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "lifecycle-abort--observeAbortSubscription"
        }
      ],
      "aliases": []
    },
    {
      "id": "lifecycle:disposal",
      "library": "lifecycle",
      "module": "disposal",
      "exportPath": "./disposal",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "createDisposeTransaction",
          "kind": "function",
          "signature": "export declare function createDisposeTransaction(mode: IDisposeTransactionMode, options?: IDisposeTransactionOptions): IDisposeTransaction;",
          "declarationLine": 49,
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "purpose": "createDisposeTransaction is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createDisposeTransaction.",
          "advanced": "No additional advanced behavior is declared for createDisposeTransaction.",
          "parameters": [
            "mode: IDisposeTransactionMode",
            "options: IDisposeTransactionOptions (optional)"
          ],
          "parameterDetails": [
            {
              "name": "mode",
              "type": "IDisposeTransactionMode",
              "optional": false
            },
            {
              "name": "options",
              "type": "IDisposeTransactionOptions",
              "optional": true
            }
          ],
          "returns": "IDisposeTransaction",
          "errors": ["No documented errors are declared for createDisposeTransaction."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createDisposeTransaction.",
          "examples": [
            "const transaction = createDisposeTransaction(\n  { kind: 'plan' },\n  { errorPolicy: 'collect', signal: controller.signal }\n)\nconst failures = await transaction.run([\n  { source: 'db', descriptor: dbDescriptor },\n  { source: 'socket', descriptor: socketDescriptor }\n])",
            "import {\n  createDisposeTransaction,\n  createManualScheduler,\n  type IDisposeItem\n} from '@migaia/lifecycle'\n\nconst scheduler = createManualScheduler()\nconst items: IDisposeItem[] = [\n  {\n    source: 'socket',\n    descriptor: {\n      order: 10,\n      graceful: (ctx) => flushSocket(),\n      gracefulTimeoutMs: 100,\n      force: () => forceCloseSocket()\n    }\n  },\n  { source: 'db', descriptor: { order: 5, force: () => closeDb() } }\n]\nconst transaction = createDisposeTransaction(\n  { kind: 'order' },\n  { errorPolicy: 'collect', scheduler }\n)\nconst runPromise = transaction.run(items)\nscheduler.advance(100) // 驱动 socket 的 graceful 超时降级到 force\nconst failures = await runPromise"
          ],
          "aliasOf": "createDisposeTransaction",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export declare function createDisposeTransaction(mode: IDisposeTransactionMode, options?: IDisposeTransactionOptions): IDisposeTransaction;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "mode",
                "type": "IDisposeTransactionMode",
                "optional": false
              },
              {
                "name": "options",
                "type": "IDisposeTransactionOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IDisposeTransaction"],
          "whenToUse": "Use createDisposeTransaction when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createDisposeTransaction does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createDisposeTransaction is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createDisposeTransaction from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createDisposeTransaction when its function contract and declared inputs match the result you need. Choose a different public symbol when createDisposeTransaction does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createDisposeTransaction."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createDisposeTransaction."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "errorPolicy",
              "type": "IErrorPolicy",
              "optional": true,
              "description": ""
            },
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": ""
            },
            {
              "name": "deadlineAt",
              "type": "number",
              "optional": true,
              "description": "Absolute deadline shared, unchanged, across every item in this run (L-T32)."
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "Scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）；缺省 `systemScheduler`。 Forwarded into * every item's `context.scheduler` and into the graceful-phase `boundedWait`."
            },
            {
              "name": "signal",
              "type": "IAbortSignal",
              "optional": true,
              "description": "Forwarded into `context.signal` for every item; aborted automatically once `run()` starts."
            },
            {
              "name": "disposer",
              "type": "IDisposerContext",
              "optional": true,
              "description": "Owner-bound self-join capability, supplied only by a LifecycleScope owner."
            },
            {
              "name": "pending",
              "type": "{ drain(): Promise<void>; }",
              "optional": true,
              "description": "Awaited after every item settles, so in-flight work started by a release can still be drained."
            }
          ],
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:createDisposeTransaction:export declare function createDisposeTransaction(mode: IDisposeTransactionMode, options?: IDisposeTransactionOptions): IDisposeTransaction;",
          "guidance": [
            {
              "id": "createdisposetransaction",
              "heading": "createDisposeTransaction",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "编排多个资源的释放，`order` 弱排序或 `plan` 强序列二选一，同一实例不能混用："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const transaction = createDisposeTransaction(\n  { kind: 'plan' },\n  { errorPolicy: 'collect', signal: controller.signal }\n)\nconst failures = await transaction.run([\n  { source: 'db', descriptor: dbDescriptor },\n  { source: 'socket', descriptor: socketDescriptor }\n])"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`{ kind: 'order' }`：按每个 item 的 `descriptor.order`（缺省 `0`）降序分组释放，稳定排序保留同序调用方给定的相对顺序；只在此模式读取 `order`。",
                    "`{ kind: 'plan' }`：严格按 `items` 给定顺序执行，从不读取 `order` 字段。",
                    "`signal?`：转发进每个 item 的 `context.signal`；`run()` 开始时自动镜像调用方传入信号的中止状态；无 `signal` 时 item 收到一个永不中止的惰性信号。",
                    "`deadlineAt`/`scheduler`：本次 `run()` 内所有 item 共享，不逐 item 重算。",
                    "`pending?`：每个 item 释放完后调用 `pending.drain()` 等待其触发的在途工作排空（典型传 `createPendingTracker()`）。",
                    "descriptor 校验（`order`/`custom`/`graceful`/`gracefulTimeoutMs`/`force` 的类型与访问）逐项独立进行——一个 item 的非法 descriptor（贴 `INVALID_OPTION` 码）不阻断其余 item 的释放，会作为该 item 的失败计入错误策略。",
                    "`errorPolicy` 语义同 `createErrorCollector`；`throw` 策略下单错原样抛出，多错聚合为携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`。"
                  ]
                }
              ]
            },
            {
              "id": "createerrorcollector",
              "heading": "createErrorCollector",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
                },
                {
                  "type": "paragraph",
                  "text": "按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 102,
          "configuration": [
            {
              "name": "errorPolicy",
              "type": "IErrorPolicy",
              "optional": true,
              "description": "`errorPolicy` 语义同 `createErrorCollector`；`throw` 策略下单错原样抛出，多错聚合为携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`。",
              "descriptionEn": "",
              "descriptionZh": "`errorPolicy` 语义同 `createErrorCollector`；`throw` 策略下单错原样抛出，多错聚合为携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`。"
            },
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer.",
              "descriptionEn": "Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity.",
              "descriptionZh": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer."
            },
            {
              "name": "deadlineAt",
              "type": "number",
              "optional": true,
              "description": "`gracefulTimeoutMs?: number` —— 优雅阶段自己的预算，会被事务共享的绝对 `deadlineAt` 进一步封顶（取两者较小值）。",
              "descriptionEn": "Absolute deadline shared, unchanged, across every item in this run (L-T32).",
              "descriptionZh": "`gracefulTimeoutMs?: number` —— 优雅阶段自己的预算，会被事务共享的绝对 `deadlineAt` 进一步封顶（取两者较小值）。"
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer.",
              "descriptionEn": "",
              "descriptionZh": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer."
            },
            {
              "name": "signal",
              "type": "IAbortSignal",
              "optional": true,
              "description": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer.",
              "descriptionEn": "Forwarded into `context.signal` for every item; aborted automatically once `run()` starts.",
              "descriptionZh": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer."
            },
            {
              "name": "disposer",
              "type": "IDisposerContext",
              "optional": true,
              "description": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer.",
              "descriptionEn": "Owner-bound self-join capability, supplied only by a LifecycleScope owner.",
              "descriptionZh": "`IReleaseContext`（回调收到的第一参数）：`signal: IAbortSignal`（容器进入 closing 时中止）、`deadlineAt: number | undefined`、`scheduler?: ILifecycleScheduler`（产出 `deadlineAt` 的时间域，缺省 `systemScheduler`）、`report(error: unknown): void`（诊断通道，自身抛错被吞掉）、`disposer?: IDisposerContext`。由 `LifecycleScope` 驱动的 disposer 才有 `disposer."
            },
            {
              "name": "pending",
              "type": "{ drain(): Promise<void>; }",
              "optional": true,
              "description": "`pending?`：每个 item 释放完后调用 `pending.drain()` 等待其触发的在途工作排空（典型传 `createPendingTracker()`）。",
              "descriptionEn": "Awaited after every item settles, so in-flight work started by a release can still be drained.",
              "descriptionZh": "`pending?`：每个 item 释放完后调用 `pending.drain()` 等待其触发的在途工作排空（典型传 `createPendingTracker()`）。"
            }
          ],
          "fragment": "lifecycle-disposal--createDisposeTransaction"
        },
        {
          "name": "createSyncStartedDisposalLedger",
          "kind": "function",
          "signature": "export declare function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger;",
          "declarationLine": 20,
          "source": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts",
          "purpose": "Creates one sync-start, async-completion disposal ledger.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createSyncStartedDisposalLedger.",
          "advanced": "No additional advanced behavior is declared for createSyncStartedDisposalLedger.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "ISyncStartedDisposalLedger",
          "errors": ["No documented errors are declared for createSyncStartedDisposalLedger."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createSyncStartedDisposalLedger.",
          "examples": [
            "import { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\n\nconst ledger = createSyncStartedDisposalLedger()\n\n// 调用顺序由领域层决定；每个 callback 在 start() 的当前 stack 执行。\nledger.start('connection', () => connection.close())\nledger.start('cache', () => cache.flush())\n\nconst outcome = ledger.seal()\nif (outcome.synchronousErrors.length === 1) {\n  throw outcome.synchronousErrors[0].error\n}\n\nconst finalErrors = await outcome.completion\n// completion 永远 resolve；领域层在这里决定是否 reject/aggregate/report。",
            "import { createSyncStartedDisposalLedger } from '@migaia/lifecycle'\n\nconst ledger = createSyncStartedDisposalLedger()\n\nfunction disposeRegistry(): void {\n  ledger.start('cache', () => cache.dispose())\n  ledger.start('worker', () => worker.dispose())\n  ledger.seal()\n}\n\ndisposeRegistry()\nconst { synchronousErrors, completion } = ledger.seal()\nconst allErrors = await completion",
            "import { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\n\nconst ledger = createSyncStartedDisposalLedger()\nledger.start('socket', () => socket.close()) // callback 在当前 stack 立即执行\nledger.start('cache', () => cache.flush())\n\nconst outcome = ledger.seal() // open → closing；之后不再接受 start()\nif (outcome.synchronousErrors.length > 0) {\n  // 领域层决定 throw、report、firstError 或 AggregateError 投影。\n}\nconst allErrors = await outcome.completion // 永远 resolve raw error ledger，不因 item failure reject\nawait ledger.whenTerminal()"
          ],
          "aliasOf": "createSyncStartedDisposalLedger",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export declare function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["ISyncStartedDisposalLedger"],
          "whenToUse": "Use createSyncStartedDisposalLedger when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createSyncStartedDisposalLedger does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates one sync-start, async-completion disposal ledger."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createSyncStartedDisposalLedger from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createSyncStartedDisposalLedger when its function contract and declared inputs match the result you need. Choose a different public symbol when createSyncStartedDisposalLedger does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createSyncStartedDisposalLedger."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createSyncStartedDisposalLedger."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts:createSyncStartedDisposalLedger:export declare function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger;",
          "guidance": [
            {
              "id": "createsyncstarteddisposalledger",
              "heading": "createSyncStartedDisposalLedger",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger\n\ntype ISyncStartedDisposalLedger = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly pending: number\n  start(source: string, callback: () => unknown): void\n  seal(): ISyncStartedDisposalOutcome\n  whenTerminal(): Promise<void>\n}\n\ntype ISyncStartedDisposalOutcome = Readonly<{\n  synchronousErrors: readonly ICollectedError[]\n  completion: Promise<readonly ICollectedError[]>\n}>"
                },
                {
                  "type": "paragraph",
                  "text": "用于“同步启动全部 disposer，但异步统一等待”的领域容器。Ledger 是 pending、raw error 与 terminal 的唯一事实源；调用方仍负责生成顺序计划、清空领域 registry，以及把 raw errors 投影成自己的 throw/report/aggregate 契约。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createSyncStartedDisposalLedger } from '@migaia/lifecycle/disposal'\n\nconst ledger = createSyncStartedDisposalLedger()\n\n// 调用顺序由领域层决定；每个 callback 在 start() 的当前 stack 执行。\nledger.start('connection', () => connection.close())\nledger.start('cache', () => cache.flush())\n\nconst outcome = ledger.seal()\nif (outcome.synchronousErrors.length === 1) {\n  throw outcome.synchronousErrors[0].error\n}\n\nconst finalErrors = await outcome.completion\n// completion 永远 resolve；领域层在这里决定是否 reject/aggregate/report。"
                },
                {
                  "type": "paragraph",
                  "text": "状态和 identity："
                },
                {
                  "type": "list",
                  "items": [
                    "初始 `open`；`start()` 不改变 lifecycle。",
                    "首次 `seal()` 同步进入 `closing`，冻结当时全部 synchronous errors，并返回唯一 outcome；重复 `seal()` 返回同一对象，`outcome.completion` 也保持同一 Promise。",
                    "已 seal 且 `pending === 0` 时进入 `terminal`；否则最后一个 thenable settle 后进入 `terminal`。`whenTerminal()` 只表达 terminal 等待，不承诺 Promise 对象 identity。",
                    "合法 thenable 永不 settle 时保持 `closing`；Ledger 不自造 timeout、abort、retry 或强制 terminal。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "Callback 与 thenable："
                },
                {
                  "type": "list",
                  "items": [
                    "`start()` 先校验 `source` 为 string、`callback` 为 function；非法输入同步抛 `INVALID_OPTION`，且不调用 callback。",
                    "callback 同步 throw 或读取 `.then` getter throw：记录进 `synchronousErrors` 与最终 ledger；`start()` 不重抛 item failure，因此调用方仍能启动后续 item。",
                    "thenable 的 `.then` 只读取一次，并以原 thenable 为 receiver 调用；resolve/reject first-settlement-wins。Rejection按 settlement observation order进入最终 ledger。",
                    "每条错误是冻结的 `{ source, error }`；原始 `error` identity不被 Ledger替换。`completion` 因 item failure也不会 reject，避免 Ledger抢走领域错误政策。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "重入和关闭边界："
                },
                {
                  "type": "list",
                  "items": [
                    "当前 item callback/thenable probe 尚未结束时重入 `start()` 或 `seal()`，同步抛 `SCOPE_REENTRANT_OWN`（`disposalLedgerReentrant` 文案）；外层 `start()` 把它当作当前 item 的同步 failure收集。",
                    "seal 后再 `start()` 同步抛 `SCOPE_CLOSED`（`disposalLedgerClosed` 文案），不调用新 callback。",
                    "Ledger 没有 rollback：进入 closing 后只能等待 admitted thenables settle。"
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "错误政策边界：Ledger 固定采用 raw `collect`。`firstError` 是最终 ledger 的第一条 observation；`throw`、`report`、`AggregateError` message/source/code和reporter failure containment全部属于调用 adapter，不应反向写入 Ledger。"
                }
              ]
            }
          ],
          "usageScore": 96,
          "configuration": [],
          "fragment": "lifecycle-disposal--createSyncStartedDisposalLedger"
        },
        {
          "name": "executeReleaseDescriptor",
          "kind": "function",
          "signature": "export declare function executeReleaseDescriptor(descriptor: IReleaseDescriptor, context: IReleaseContext): Promise<readonly unknown[]>;",
          "declarationLine": 7,
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "purpose": "Admits a public release descriptor once before executing its degrade chain.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for executeReleaseDescriptor.",
          "advanced": "No additional advanced behavior is declared for executeReleaseDescriptor.",
          "parameters": ["descriptor: IReleaseDescriptor", "context: IReleaseContext"],
          "parameterDetails": [
            {
              "name": "descriptor",
              "type": "IReleaseDescriptor",
              "optional": false
            },
            {
              "name": "context",
              "type": "IReleaseContext",
              "optional": false
            }
          ],
          "returns": "Promise<readonly unknown[]>",
          "errors": ["No documented errors are declared for executeReleaseDescriptor."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for executeReleaseDescriptor.",
          "examples": [
            "const errors = await executeReleaseDescriptor(descriptor, {\n  signal: controller.signal,\n  deadlineAt: undefined,\n  report: (e) => console.error(e)\n})"
          ],
          "aliasOf": "executeReleaseDescriptor",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export declare function executeReleaseDescriptor(descriptor: IReleaseDescriptor, context: IReleaseContext): Promise<readonly unknown[]>;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "descriptor",
                "type": "IReleaseDescriptor",
                "optional": false
              },
              {
                "name": "context",
                "type": "IReleaseContext",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["Promise<readonly unknown[]>"],
          "whenToUse": "Use executeReleaseDescriptor when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when executeReleaseDescriptor does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Admits a public release descriptor once before executing its degrade chain."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import executeReleaseDescriptor from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use executeReleaseDescriptor when its function contract and declared inputs match the result you need. Choose a different public symbol when executeReleaseDescriptor does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for executeReleaseDescriptor."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for executeReleaseDescriptor."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:executeReleaseDescriptor:export declare function executeReleaseDescriptor(descriptor: IReleaseDescriptor, context: IReleaseContext): Promise<readonly unknown[]>;",
          "guidance": [
            {
              "id": "executereleasedescriptor",
              "heading": "executeReleaseDescriptor",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function executeReleaseDescriptor(\n  descriptor: IReleaseDescriptor,\n  context: IReleaseContext\n): Promise<readonly unknown[]>"
                },
                {
                  "type": "paragraph",
                  "text": "独立运行单个 descriptor 的完整降级链（`custom` → 否则 `graceful`（带超时竞速）→ `force`），不经过完整事务，常用于测试或自定义编排："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const errors = await executeReleaseDescriptor(descriptor, {\n  signal: controller.signal,\n  deadlineAt: undefined,\n  report: (e) => console.error(e)\n})"
                },
                {
                  "type": "paragraph",
                  "text": "`descriptor`、`context` 均必填，`context` 无可选项——必须直接构造完整对象。返回全部失败错误的数组（成功为空数组），从不抛出；`graceful` 超时时会通过 `context.report` 上报一条诊断（`DEADLINE_EXCEEDED` 或空转到 `force`），`force` 失败时额外通过 `report` 上报一条 `RELEASE_FORCE_FAILED` 标签的错误（原始错误仍原样进入返回数组）。"
                }
              ]
            }
          ],
          "usageScore": 58,
          "configuration": [],
          "fragment": "lifecycle-disposal--executeReleaseDescriptor"
        },
        {
          "name": "IDisposeItem",
          "kind": "type",
          "signature": "export type IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor; };",
          "declarationLine": 8,
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "purpose": "IDisposeItem is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeItem.",
          "advanced": "No additional advanced behavior is declared for IDisposeItem.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDisposeItem."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDisposeItem.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}",
            "import {\n  createDisposeTransaction,\n  createManualScheduler,\n  type IDisposeItem\n} from '@migaia/lifecycle'\n\nconst scheduler = createManualScheduler()\nconst items: IDisposeItem[] = [\n  {\n    source: 'socket',\n    descriptor: {\n      order: 10,\n      graceful: (ctx) => flushSocket(),\n      gracefulTimeoutMs: 100,\n      force: () => forceCloseSocket()\n    }\n  },\n  { source: 'db', descriptor: { order: 5, force: () => closeDb() } }\n]\nconst transaction = createDisposeTransaction(\n  { kind: 'order' },\n  { errorPolicy: 'collect', scheduler }\n)\nconst runPromise = transaction.run(items)\nscheduler.advance(100) // 驱动 socket 的 graceful 超时降级到 force\nconst failures = await runPromise"
          ],
          "aliasOf": "IDisposeItem",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export type IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDisposeItem when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDisposeItem does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IDisposeItem is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDisposeItem from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDisposeItem when its type contract and declared inputs match the result you need. Choose a different public symbol when IDisposeItem does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeItem."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDisposeItem."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeItem:export type IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor; };",
          "guidance": [],
          "usageScore": 52,
          "configuration": [],
          "fragment": "lifecycle-disposal--IDisposeItem"
        },
        {
          "name": "IDisposeTransaction",
          "kind": "type",
          "signature": "export type IDisposeTransaction = { readonly mode: IDisposeTransactionMode; run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>; };",
          "declarationLine": 45,
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "purpose": "IDisposeTransaction is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransaction.",
          "advanced": "No additional advanced behavior is declared for IDisposeTransaction.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDisposeTransaction."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDisposeTransaction.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}"
          ],
          "aliasOf": "IDisposeTransaction",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export type IDisposeTransaction = { readonly mode: IDisposeTransactionMode; run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDisposeTransaction when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDisposeTransaction does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IDisposeTransaction is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDisposeTransaction from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDisposeTransaction when its type contract and declared inputs match the result you need. Choose a different public symbol when IDisposeTransaction does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransaction."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDisposeTransaction."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeTransaction:export type IDisposeTransaction = { readonly mode: IDisposeTransactionMode; run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-disposal--IDisposeTransaction"
        },
        {
          "name": "IDisposeTransactionMode",
          "kind": "type",
          "signature": "export type IDisposeTransactionMode = { readonly kind: typeof DisposeTransactionKind.order; } | { readonly kind: typeof DisposeTransactionKind.plan; };",
          "declarationLine": 21,
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "purpose": "`order` (weak): `DisposeTransaction` groups by `descriptor.order` (missing = `0`, higher values first) and reads no other ordering signal — callers must already hand items in the sequence ties should break in (LIFO), since the group sort is stable. `plan` (strong): items run in exactly the given sequence; `descriptor.order` is never read. §4.6: a single transaction instance never mixes the two.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransactionMode.",
          "advanced": "No additional advanced behavior is declared for IDisposeTransactionMode.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDisposeTransactionMode."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDisposeTransactionMode.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'"
          ],
          "aliasOf": "IDisposeTransactionMode",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export type IDisposeTransactionMode = { readonly kind: typeof DisposeTransactionKind.order; } | { readonly kind: typeof DisposeTransactionKind.plan; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDisposeTransactionMode when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDisposeTransactionMode does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "`order` (weak): `DisposeTransaction` groups by `descriptor.order` (missing = `0`, higher values first) and reads no other ordering signal — callers must already hand items in the sequence ties should break in (LIFO), since the group sort is stable. `plan` (strong): items run in exactly the given sequence; `descriptor.order` is never read. §4.6: a single transaction instance never mixes the two."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDisposeTransactionMode from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDisposeTransactionMode when its type contract and declared inputs match the result you need. Choose a different public symbol when IDisposeTransactionMode does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransactionMode."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDisposeTransactionMode."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeTransactionMode:export type IDisposeTransactionMode = { readonly kind: typeof DisposeTransactionKind.order; } | { readonly kind: typeof DisposeTransactionKind.plan; };",
          "guidance": [],
          "usageScore": 14,
          "configuration": [],
          "fragment": "lifecycle-disposal--IDisposeTransactionMode"
        },
        {
          "name": "IDisposeTransactionOptions",
          "kind": "type",
          "signature": "export type IDisposeTransactionOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Absolute deadline shared, unchanged, across every item in this run (L-T32). */ readonly deadlineAt?: number; /** * Scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）；缺省 `systemScheduler`。 Forwarded into * every item's `context.scheduler` and into the graceful-phase `boundedWait`. */ readonly scheduler?: ILifecycleScheduler; /** Forwarded into `context.signal` for every item; aborted automatically once `run()` starts. */ readonly signal?: IAbortSignal; /** Owner-bound self-join capability, supplied only by a LifecycleScope owner. */ readonly disposer?: IDisposerContext; /** Awaited after every item settles, so in-flight work started by a release can still be drained. */ readonly pending?: { drain(): Promise<void>; }; };",
          "declarationLine": 26,
          "source": "packages/lifecycle/dist/dispose-transaction.d.ts",
          "purpose": "IDisposeTransactionOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransactionOptions.",
          "advanced": "No additional advanced behavior is declared for IDisposeTransactionOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IDisposeTransactionOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IDisposeTransactionOptions.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}"
          ],
          "aliasOf": "IDisposeTransactionOptions",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export type IDisposeTransactionOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Absolute deadline shared, unchanged, across every item in this run (L-T32). */ readonly deadlineAt?: number; /** * Scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）；缺省 `systemScheduler`。 Forwarded into * every item's `context.scheduler` and into the graceful-phase `boundedWait`. */ readonly scheduler?: ILifecycleScheduler; /** Forwarded into `context.signal` for every item; aborted automatically once `run()` starts. */ readonly signal?: IAbortSignal; /** Owner-bound self-join capability, supplied only by a LifecycleScope owner. */ readonly disposer?: IDisposerContext; /** Awaited after every item settles, so in-flight work started by a release can still be drained. */ readonly pending?: { drain(): Promise<void>; }; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IDisposeTransactionOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IDisposeTransactionOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IDisposeTransactionOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IDisposeTransactionOptions from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IDisposeTransactionOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IDisposeTransactionOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IDisposeTransactionOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IDisposeTransactionOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/dispose-transaction.d.ts:IDisposeTransactionOptions:export type IDisposeTransactionOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Absolute deadline shared, unchanged, across every item in this run (L-T32). */ readonly deadlineAt?: number; /** * Scheduler whose `now()` produced `deadlineAt`（R-9 时间域契约）；缺省 `systemScheduler`。 Forwarded into * every item's `context.scheduler` and into the graceful-phase `boundedWait`. */ readonly scheduler?: ILifecycleScheduler; /** Forwarded into `context.signal` for every item; aborted automatically once `run()` starts. */ readonly signal?: IAbortSignal; /** Owner-bound self-join capability, supplied only by a LifecycleScope owner. */ readonly disposer?: IDisposerContext; /** Awaited after every item settles, so in-flight work started by a release can still be drained. */ readonly pending?: { drain(): Promise<void>; }; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-disposal--IDisposeTransactionOptions"
        },
        {
          "name": "ISyncStartedDisposalLedger",
          "kind": "type",
          "signature": "export type ISyncStartedDisposalLedger = { readonly lifecycle: ILifecycleState; readonly pending: number; start(source: string, callback: () => unknown): void; seal(): ISyncStartedDisposalOutcome; whenTerminal(): Promise<void>; };",
          "declarationLine": 12,
          "source": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts",
          "purpose": "Lifecycle-owned ledger that starts every callback synchronously, then observes all async settlements until a sealed plan reaches terminal. Domain adapters own ordering and error projection; this primitive owns only admission, pending, and raw error observation.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncStartedDisposalLedger.",
          "advanced": "No additional advanced behavior is declared for ISyncStartedDisposalLedger.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ISyncStartedDisposalLedger."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ISyncStartedDisposalLedger.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger\n\ntype ISyncStartedDisposalLedger = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly pending: number\n  start(source: string, callback: () => unknown): void\n  seal(): ISyncStartedDisposalOutcome\n  whenTerminal(): Promise<void>\n}\n\ntype ISyncStartedDisposalOutcome = Readonly<{\n  synchronousErrors: readonly ICollectedError[]\n  completion: Promise<readonly ICollectedError[]>\n}>"
          ],
          "aliasOf": "ISyncStartedDisposalLedger",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export type ISyncStartedDisposalLedger = { readonly lifecycle: ILifecycleState; readonly pending: number; start(source: string, callback: () => unknown): void; seal(): ISyncStartedDisposalOutcome; whenTerminal(): Promise<void>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ISyncStartedDisposalLedger when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ISyncStartedDisposalLedger does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Lifecycle-owned ledger that starts every callback synchronously, then observes all async settlements until a sealed plan reaches terminal. Domain adapters own ordering and error projection; this primitive owns only admission, pending, and raw error observation."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ISyncStartedDisposalLedger from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ISyncStartedDisposalLedger when its type contract and declared inputs match the result you need. Choose a different public symbol when ISyncStartedDisposalLedger does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncStartedDisposalLedger."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ISyncStartedDisposalLedger."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts:ISyncStartedDisposalLedger:export type ISyncStartedDisposalLedger = { readonly lifecycle: ILifecycleState; readonly pending: number; start(source: string, callback: () => unknown): void; seal(): ISyncStartedDisposalOutcome; whenTerminal(): Promise<void>; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-disposal--ISyncStartedDisposalLedger"
        },
        {
          "name": "ISyncStartedDisposalOutcome",
          "kind": "type",
          "signature": "export type ISyncStartedDisposalOutcome = Readonly<{ synchronousErrors: readonly ICollectedError[]; completion: Promise<readonly ICollectedError[]>; }>;",
          "declarationLine": 3,
          "source": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts",
          "purpose": "Raw outcome exposed to adapters after a complete synchronous-start disposal plan.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncStartedDisposalOutcome.",
          "advanced": "No additional advanced behavior is declared for ISyncStartedDisposalOutcome.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ISyncStartedDisposalOutcome."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ISyncStartedDisposalOutcome.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createSyncStartedDisposalLedger(): ISyncStartedDisposalLedger\n\ntype ISyncStartedDisposalLedger = {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  readonly pending: number\n  start(source: string, callback: () => unknown): void\n  seal(): ISyncStartedDisposalOutcome\n  whenTerminal(): Promise<void>\n}\n\ntype ISyncStartedDisposalOutcome = Readonly<{\n  synchronousErrors: readonly ICollectedError[]\n  completion: Promise<readonly ICollectedError[]>\n}>"
          ],
          "aliasOf": "ISyncStartedDisposalOutcome",
          "exportPath": "./disposal",
          "overloadSignatures": [
            "export type ISyncStartedDisposalOutcome = Readonly<{ synchronousErrors: readonly ICollectedError[]; completion: Promise<readonly ICollectedError[]>; }>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ISyncStartedDisposalOutcome when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ISyncStartedDisposalOutcome does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Raw outcome exposed to adapters after a complete synchronous-start disposal plan."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ISyncStartedDisposalOutcome from @migaia/lifecycle/disposal, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ISyncStartedDisposalOutcome when its type contract and declared inputs match the result you need. Choose a different public symbol when ISyncStartedDisposalOutcome does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISyncStartedDisposalOutcome."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ISyncStartedDisposalOutcome."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/sync-started-disposal-ledger.d.ts:ISyncStartedDisposalOutcome:export type ISyncStartedDisposalOutcome = Readonly<{ synchronousErrors: readonly ICollectedError[]; completion: Promise<readonly ICollectedError[]>; }>;",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-disposal--ISyncStartedDisposalOutcome"
        }
      ],
      "aliases": []
    },
    {
      "id": "lifecycle:errors",
      "library": "lifecycle",
      "module": "errors",
      "exportPath": "./errors",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "containAsyncRejection",
          "kind": "function",
          "signature": "export declare function containAsyncRejection(value: unknown, onRejected: (error: unknown) => void): void;",
          "declarationLine": 61,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "Ported from `@migaia/capability`'s `containAsyncRejection`. `() => void` in TypeScript still accepts an `async` function. This guards a diagnostic/release callback's return value for a thenable and observes its rejection, so a callback that turns out to be `async` behind our backs doesn't surface as an unhandled rejection one microtask later (L-T17, L-T26, L-T35).",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for containAsyncRejection.",
          "advanced": "No additional advanced behavior is declared for containAsyncRejection.",
          "parameters": ["value: unknown", "onRejected: (error: unknown) => void"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "onRejected",
              "type": "(error: unknown) => void",
              "optional": false
            }
          ],
          "returns": "void",
          "errors": ["No documented errors are declared for containAsyncRejection."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for containAsyncRejection.",
          "examples": ["containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export declare function containAsyncRejection(value: unknown, onRejected: (error: unknown) => void): void;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "onRejected",
                "type": "(error: unknown) => void",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["void"],
          "whenToUse": "Use containAsyncRejection when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when containAsyncRejection does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Ported from `@migaia/capability`'s `containAsyncRejection`. `() => void` in TypeScript still accepts an `async` function. This guards a diagnostic/release callback's return value for a thenable and observes its rejection, so a callback that turns out to be `async` behind our backs doesn't surface as an unhandled rejection one microtask later (L-T17, L-T26, L-T35)."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import containAsyncRejection from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use containAsyncRejection when its function contract and declared inputs match the result you need. Choose a different public symbol when containAsyncRejection does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for containAsyncRejection."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for containAsyncRejection."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:containAsyncRejection:export declare function containAsyncRejection(value: unknown, onRejected: (error: unknown) => void): void;",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "containasyncrejection",
              "heading": "containAsyncRejection",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function containAsyncRejection(value: unknown, onRejected: (error: unknown) => void): void"
                },
                {
                  "type": "paragraph",
                  "text": "保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "`value`、`onRejected` 均必填。非 thenable 时直接返回，不调用 `onRejected`；`onRejected` 自身抛错被吞掉（最后一道错误边界）。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 58,
          "configuration": [],
          "fragment": "lifecycle-errors--containAsyncRejection"
        },
        {
          "name": "createErrorCollector",
          "kind": "function",
          "signature": "export declare function createErrorCollector(policy: IErrorPolicy, report: ((error: unknown) => void) | undefined): IErrorCollector;",
          "declarationLine": 74,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "createErrorCollector is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createErrorCollector.",
          "advanced": "No additional advanced behavior is declared for createErrorCollector.",
          "parameters": ["policy: IErrorPolicy", "report: ((error: unknown) => void) | undefined"],
          "parameterDetails": [
            {
              "name": "policy",
              "type": "IErrorPolicy",
              "optional": false
            },
            {
              "name": "report",
              "type": "((error: unknown) => void) | undefined",
              "optional": false
            }
          ],
          "returns": "IErrorCollector",
          "errors": ["No documented errors are declared for createErrorCollector."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createErrorCollector.",
          "examples": [
            "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export declare function createErrorCollector(policy: IErrorPolicy, report: ((error: unknown) => void) | undefined): IErrorCollector;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "policy",
                "type": "IErrorPolicy",
                "optional": false
              },
              {
                "name": "report",
                "type": "((error: unknown) => void) | undefined",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["IErrorCollector"],
          "whenToUse": "Use createErrorCollector when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createErrorCollector does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createErrorCollector is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createErrorCollector from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createErrorCollector when its function contract and declared inputs match the result you need. Choose a different public symbol when createErrorCollector does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createErrorCollector."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createErrorCollector."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:createErrorCollector:export declare function createErrorCollector(policy: IErrorPolicy, report: ((error: unknown) => void) | undefined): IErrorCollector;",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "createerrorcollector",
              "heading": "createErrorCollector",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
                },
                {
                  "type": "paragraph",
                  "text": "按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 62,
          "configuration": [],
          "fragment": "lifecycle-errors--createErrorCollector"
        },
        {
          "name": "createLifecycleError",
          "kind": "function",
          "signature": "export declare function createLifecycleError(code: string, message: string, options?: { readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; }): ILifecycleError;",
          "declarationLine": 25,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "Builds a `(source, code)`-tagged error without ever touching `stack` — the engine populates it at construction and this function never reassigns it, so the original throw site is always visible.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleError.",
          "advanced": "No additional advanced behavior is declared for createLifecycleError.",
          "parameters": [
            "code: string",
            "message: string",
            "options: { readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; } (optional)"
          ],
          "parameterDetails": [
            {
              "name": "code",
              "type": "string",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; }",
              "optional": true
            }
          ],
          "returns": "ILifecycleError",
          "errors": ["No documented errors are declared for createLifecycleError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createLifecycleError.",
          "examples": [
            "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export declare function createLifecycleError(code: string, message: string, options?: { readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; }): ILifecycleError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "code",
                "type": "string",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["ILifecycleError"],
          "whenToUse": "Use createLifecycleError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createLifecycleError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Builds a `(source, code)`-tagged error without ever touching `stack` — the engine populates it at construction and this function never reassigns it, so the original throw site is always visible."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createLifecycleError from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createLifecycleError when its function contract and declared inputs match the result you need. Choose a different public symbol when createLifecycleError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createLifecycleError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:createLifecycleError:export declare function createLifecycleError(code: string, message: string, options?: { readonly cause?: unknown; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly errors?: readonly unknown[]; }): ILifecycleError;",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "createlifecycleerror",
              "heading": "createLifecycleError",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleError(\n  code: string,\n  message: string,\n  options?: {\n    readonly cause?: unknown\n    readonly phase?: string\n    readonly detail?: Readonly<Record<string, unknown>>\n    readonly errors?: readonly unknown[]\n  }\n): ILifecycleError // Error & { source, code, phase?, detail?, cause?, errors? }"
                },
                {
                  "type": "paragraph",
                  "text": "构造一个携带 `(source, code)` 身份的原生 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "`code`、`message` 均必填（建议 `code` 取自 `LifecycleErrorCode`）。`options.errors` 非空时会冻结后挂到 `.errors`。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 58,
          "configuration": [],
          "fragment": "lifecycle-errors--createLifecycleError"
        },
        {
          "name": "createLifecycleFailure",
          "kind": "function",
          "signature": "export declare function createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError;",
          "declarationLine": 42,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "Converts an arbitrary cleanup/listener throw into a lifecycle-boundary failure. Extensible `Error` objects are tagged in place so callers retain identity and native type; primitive throws (or objects that reject tagging) are wrapped with the original reachable through `cause`.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleFailure.",
          "advanced": "No additional advanced behavior is declared for createLifecycleFailure.",
          "parameters": ["code: string", "message: string", "error: unknown"],
          "parameterDetails": [
            {
              "name": "code",
              "type": "string",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            },
            {
              "name": "error",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "ILifecycleError",
          "errors": ["No documented errors are declared for createLifecycleFailure."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createLifecycleFailure.",
          "examples": [
            "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export declare function createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "code",
                "type": "string",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              },
              {
                "name": "error",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["ILifecycleError"],
          "whenToUse": "Use createLifecycleFailure when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createLifecycleFailure does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Converts an arbitrary cleanup/listener throw into a lifecycle-boundary failure. Extensible `Error` objects are tagged in place so callers retain identity and native type; primitive throws (or objects that reject tagging) are wrapped with the original reachable through `cause`."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createLifecycleFailure from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createLifecycleFailure when its function contract and declared inputs match the result you need. Choose a different public symbol when createLifecycleFailure does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleFailure."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createLifecycleFailure."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:createLifecycleFailure:export declare function createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError;",
          "guidance": [
            {
              "id": "createlifecycletypeerror-createlifecyclefailure",
              "heading": "createLifecycleTypeError / createLifecycleFailure",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "二者只从 `@migaia/lifecycle/errors` leaf 导出："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nfunction createLifecycleTypeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): TypeError\n\nfunction createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` 创建保留原生 `TypeError` runtime type 的附码错误，并可携带 `cause`/`detail`。`createLifecycleFailure` 用在捕获边界：可扩展的原生 `Error` 会尽量原位附码并保持 `===` identity；primitive、冻结对象或不可重标记错误会被包装，原始抛出物仍可从 `cause` 到达。两者都不改写原始 `stack`。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 50,
          "configuration": [],
          "fragment": "lifecycle-errors--createLifecycleFailure"
        },
        {
          "name": "createLifecycleRangeError",
          "kind": "function",
          "signature": "export declare function createLifecycleRangeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): RangeError;",
          "declarationLine": 44,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "入参校验错误：原生 `RangeError` + 指定 code（`docs/contracts/error-codes.md` §2.2 保持原生类型）。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleRangeError.",
          "advanced": "No additional advanced behavior is declared for createLifecycleRangeError.",
          "parameters": [
            "code: string",
            "message: string",
            "options: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; } (optional)"
          ],
          "parameterDetails": [
            {
              "name": "code",
              "type": "string",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }",
              "optional": true
            }
          ],
          "returns": "RangeError",
          "errors": ["No documented errors are declared for createLifecycleRangeError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createLifecycleRangeError.",
          "examples": [
            "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export declare function createLifecycleRangeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): RangeError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "code",
                "type": "string",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["RangeError"],
          "whenToUse": "Use createLifecycleRangeError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createLifecycleRangeError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "入参校验错误：原生 `RangeError` + 指定 code（`docs/contracts/error-codes.md` §2.2 保持原生类型）。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createLifecycleRangeError from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createLifecycleRangeError when its function contract and declared inputs match the result you need. Choose a different public symbol when createLifecycleRangeError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleRangeError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createLifecycleRangeError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:createLifecycleRangeError:export declare function createLifecycleRangeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): RangeError;",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "createlifecyclerangeerror",
              "heading": "createLifecycleRangeError",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleRangeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): RangeError"
                },
                {
                  "type": "paragraph",
                  "text": "同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "`code`、`message` 均必填。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 58,
          "configuration": [],
          "fragment": "lifecycle-errors--createLifecycleRangeError"
        },
        {
          "name": "createLifecycleTypeError",
          "kind": "function",
          "signature": "export declare function createLifecycleTypeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): TypeError;",
          "declarationLine": 49,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "Creates a lifecycle-tagged native TypeError without replacing its runtime type.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleTypeError.",
          "advanced": "No additional advanced behavior is declared for createLifecycleTypeError.",
          "parameters": [
            "code: string",
            "message: string",
            "options: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; } (optional)"
          ],
          "parameterDetails": [
            {
              "name": "code",
              "type": "string",
              "optional": false
            },
            {
              "name": "message",
              "type": "string",
              "optional": false
            },
            {
              "name": "options",
              "type": "{ readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }",
              "optional": true
            }
          ],
          "returns": "TypeError",
          "errors": ["No documented errors are declared for createLifecycleTypeError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createLifecycleTypeError.",
          "examples": [
            "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export declare function createLifecycleTypeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): TypeError;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "code",
                "type": "string",
                "optional": false
              },
              {
                "name": "message",
                "type": "string",
                "optional": false
              },
              {
                "name": "options",
                "type": "{ readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["TypeError"],
          "whenToUse": "Use createLifecycleTypeError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createLifecycleTypeError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Creates a lifecycle-tagged native TypeError without replacing its runtime type."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createLifecycleTypeError from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createLifecycleTypeError when its function contract and declared inputs match the result you need. Choose a different public symbol when createLifecycleTypeError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleTypeError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createLifecycleTypeError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:createLifecycleTypeError:export declare function createLifecycleTypeError(code: string, message: string, options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>>; }): TypeError;",
          "guidance": [
            {
              "id": "createlifecycletypeerror-createlifecyclefailure",
              "heading": "createLifecycleTypeError / createLifecycleFailure",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "二者只从 `@migaia/lifecycle/errors` leaf 导出："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nfunction createLifecycleTypeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): TypeError\n\nfunction createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` 创建保留原生 `TypeError` runtime type 的附码错误，并可携带 `cause`/`detail`。`createLifecycleFailure` 用在捕获边界：可扩展的原生 `Error` 会尽量原位附码并保持 `===` identity；primitive、冻结对象或不可重标记错误会被包装，原始抛出物仍可从 `cause` 到达。两者都不改写原始 `stack`。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 50,
          "configuration": [],
          "fragment": "lifecycle-errors--createLifecycleTypeError"
        },
        {
          "name": "IErrorCollector",
          "kind": "type",
          "signature": "export type IErrorCollector = { readonly policy: IErrorPolicy; /** Records one item's failure. For `report`, this is also where the reporter is invoked. */ add(source: string, error: unknown): void; /** * Finalizes collection. `collect`/`report`/`firstError`-with-no-error return the collected list * (empty for `report`, since those were already handed to the reporter per `add`); `throw` and * `firstError`-with-an-error throw instead of returning. */ finalize(message: string): readonly ICollectedError[]; };",
          "declarationLine": 63,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "Collects errors under one of the four policies (D-4) and produces the correct finalize behavior.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorCollector.",
          "advanced": "No additional advanced behavior is declared for IErrorCollector.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IErrorCollector."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IErrorCollector.",
          "examples": [
            "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'",
            "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export type IErrorCollector = { readonly policy: IErrorPolicy; /** Records one item's failure. For `report`, this is also where the reporter is invoked. */ add(source: string, error: unknown): void; /** * Finalizes collection. `collect`/`report`/`firstError`-with-no-error return the collected list * (empty for `report`, since those were already handed to the reporter per `add`); `throw` and * `firstError`-with-an-error throw instead of returning. */ finalize(message: string): readonly ICollectedError[]; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IErrorCollector when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IErrorCollector does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Collects errors under one of the four policies (D-4) and produces the correct finalize behavior."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IErrorCollector from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IErrorCollector when its type contract and declared inputs match the result you need. Choose a different public symbol when IErrorCollector does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IErrorCollector."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IErrorCollector."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:IErrorCollector:export type IErrorCollector = { readonly policy: IErrorPolicy; /** Records one item's failure. For `report`, this is also where the reporter is invoked. */ add(source: string, error: unknown): void; /** * Finalizes collection. `collect`/`report`/`firstError`-with-no-error return the collected list * (empty for `report`, since those were already handed to the reporter per `add`); `throw` and * `firstError`-with-an-error throw instead of returning. */ finalize(message: string): readonly ICollectedError[]; };",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "createerrorcollector",
              "heading": "createErrorCollector",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createErrorCollector(\n  policy: 'throw' | 'collect' | 'report' | 'firstError',\n  report: ((error: unknown) => void) | undefined\n): IErrorCollector\n\ntype IErrorCollector = {\n  readonly policy: IErrorPolicy\n  add(source: string, error: unknown): void\n  finalize(message: string): readonly ICollectedError[]\n}"
                },
                {
                  "type": "paragraph",
                  "text": "按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "`policy`、`report`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`'report'` 策略立即调用 `report`；`'firstError'` 只记住第一个错误，其余经 `report` 观测）。`finalize(message)`：`'collect'` 返回收集到的数组；`'report'` 返回空数组（已在 `add` 时上报）；`'throw'` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`；`'firstError'` 有错误时抛首个，否则返回空数组。"
                }
              ]
            }
          ],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-errors--IErrorCollector"
        },
        {
          "name": "ILifecycleError",
          "kind": "type",
          "signature": "export type ILifecycleError = Error & { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly cause?: unknown; /** 冻结的次要失败快照（如 cleanup 失败），不与 `cause` 混用；primary 保持原错误身份。 */ readonly errors?: readonly unknown[]; };",
          "declarationLine": 12,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "The structural error contract every module in the workspace follows (`docs/contracts/error-codes.md` §2). Attached as properties on a real `Error`, never used to replace it — callers that branch on `instanceof DOMException`/`RangeError`/etc. keep working.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleError.",
          "advanced": "No additional advanced behavior is declared for ILifecycleError.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleError.",
          "examples": [
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}",
            "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'",
            "function createLifecycleError(\n  code: string,\n  message: string,\n  options?: {\n    readonly cause?: unknown\n    readonly phase?: string\n    readonly detail?: Readonly<Record<string, unknown>>\n    readonly errors?: readonly unknown[]\n  }\n): ILifecycleError // Error & { source, code, phase?, detail?, cause?, errors? }",
            "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nfunction createLifecycleTypeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): TypeError\n\nfunction createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export type ILifecycleError = Error & { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly cause?: unknown; /** 冻结的次要失败快照（如 cleanup 失败），不与 `cause` 混用；primary 保持原错误身份。 */ readonly errors?: readonly unknown[]; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleError when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "The structural error contract every module in the workspace follows (`docs/contracts/error-codes.md` §2). Attached as properties on a real `Error`, never used to replace it — callers that branch on `instanceof DOMException`/`RangeError`/etc. keep working."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleError from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleError when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:ILifecycleError:export type ILifecycleError = Error & { readonly source: string; readonly code: string; readonly phase?: string; readonly detail?: Readonly<Record<string, unknown>>; readonly cause?: unknown; /** 冻结的次要失败快照（如 cleanup 失败），不与 `cause` 混用；primary 保持原错误身份。 */ readonly errors?: readonly unknown[]; };",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "createlifecycleerror",
              "heading": "createLifecycleError",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleError(\n  code: string,\n  message: string,\n  options?: {\n    readonly cause?: unknown\n    readonly phase?: string\n    readonly detail?: Readonly<Record<string, unknown>>\n    readonly errors?: readonly unknown[]\n  }\n): ILifecycleError // Error & { source, code, phase?, detail?, cause?, errors? }"
                },
                {
                  "type": "paragraph",
                  "text": "构造一个携带 `(source, code)` 身份的原生 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "`code`、`message` 均必填（建议 `code` 取自 `LifecycleErrorCode`）。`options.errors` 非空时会冻结后挂到 `.errors`。"
                }
              ]
            },
            {
              "id": "createlifecycletypeerror-createlifecyclefailure",
              "heading": "createLifecycleTypeError / createLifecycleFailure",
              "blocks": [
                {
                  "type": "paragraph",
                  "text": "二者只从 `@migaia/lifecycle/errors` leaf 导出："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nfunction createLifecycleTypeError(\n  code: string,\n  message: string,\n  options?: { readonly cause?: unknown; readonly detail?: Readonly<Record<string, unknown>> }\n): TypeError\n\nfunction createLifecycleFailure(code: string, message: string, error: unknown): ILifecycleError"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` 创建保留原生 `TypeError` runtime type 的附码错误，并可携带 `cause`/`detail`。`createLifecycleFailure` 用在捕获边界：可扩展的原生 `Error` 会尽量原位附码并保持 `===` identity；primitive、冻结对象或不可重标记错误会被包装，原始抛出物仍可从 `cause` 到达。两者都不改写原始 `stack`。"
                }
              ]
            },
            {
              "id": "错误码",
              "heading": "错误码",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { LifecycleErrorCode, type ILifecycleErrorCode } from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "稳定错误码表，20 个码，唯一声明处 `src/error-code.ts`，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "table",
                  "headers": ["`LifecycleErrorCode` 键", "码值", "触发条件"],
                  "rows": [
                    [
                      "`scopeClosed`",
                      "`SCOPE_CLOSED`",
                      "`close()` 之后调用 `own()`（异步/同步 scope）或某个 registry 的 `retain()`"
                    ],
                    [
                      "`scopeTerminal`",
                      "`SCOPE_TERMINAL`",
                      "容器已到达 `terminal` 之后，任何登记/激活操作"
                    ],
                    [
                      "`scopeReentrantDispose`",
                      "`SCOPE_REENTRANT_DISPOSE`",
                      "disposer 内部重入本 scope 的 `dispose()`"
                    ],
                    [
                      "`scopeReentrantOwn`",
                      "`SCOPE_REENTRANT_OWN`",
                      "disposer 内部调用本 scope 的 `own()`"
                    ],
                    [
                      "`scopeSyncViolation`",
                      "`SCOPE_SYNC_VIOLATION`",
                      "`SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor、收到一个异步 scope 实例作为资源、或某回调返回了 thenable"
                    ],
                    [
                      "`scopeDisposalFailed`",
                      "`SCOPE_DISPOSAL_FAILED`",
                      "`throw` 错误策略在多个资源释放失败时的 `AggregateError` 聚合出口"
                    ],
                    [
                      "`unitStartFailed`",
                      "`UNIT_START_FAILED`",
                      "`LifecycleUnit.start()` 返回的 thenable 被 reject（单元进入 `failed`）"
                    ],
                    [
                      "`generationSuperseded`",
                      "`GENERATION_SUPERSEDED`",
                      "一次异步操作的结果在 `adopt()` 提交点已被更晚的 generation 取代（非失败信号，仅诊断）"
                    ],
                    [
                      "`generationCancellationFailed`",
                      "`GENERATION_CANCELLATION_FAILED`",
                      "generation 取消期间的 timer、parent listener 或 signal cleanup 失败"
                    ],
                    [
                      "`generationDisposed`",
                      "`GENERATION_DISPOSED`",
                      "在已 `dispose()` 的 `GenerationController` 上调用 `begin()`"
                    ],
                    ["`quiescenceSealed`", "`QUIESCENCE_SEALED`", "`seal()` 之后又调用 `retain()`"],
                    [
                      "`quiescenceUnsealedWait`",
                      "`QUIESCENCE_UNSEALED_WAIT`",
                      "未先 `seal(key)` 就调用严格 `whenZero(key)`（同步抛出，非 rejected Promise）"
                    ],
                    [
                      "`provisionalSettled`",
                      "`PROVISIONAL_SETTLED`",
                      "同一个 `ProvisionalScope` 二次 `commitTo()`/`rollback()`，或交叉调用"
                    ],
                    [
                      "`provisionalParentClosed`",
                      "`PROVISIONAL_PARENT_CLOSED`",
                      "`commitTo()` 的目标 parent 已 `closing`/`terminal`，拒绝接收资源"
                    ],
                    [
                      "`queueAdmissionTimeout`",
                      "`QUEUE_ADMISSION_TIMEOUT`",
                      "mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`"
                    ],
                    [
                      "`queueSelfDependency`",
                      "`QUEUE_SELF_DEPENDENCY`",
                      "同一 `owner` 在自己尚未完成时又向同一队列提交新任务且被等待，会死锁"
                    ],
                    [
                      "`releaseForceFailed`",
                      "`RELEASE_FORCE_FAILED`",
                      "descriptor 的 `force` 抛出/reject（契约要求 `force` 必须无条件成功）"
                    ],
                    [
                      "`deadlineExceeded`",
                      "`DEADLINE_EXCEEDED`",
                      "共享绝对 deadline 已过去，但仍有资源被要求进入新的 graceful 阶段（诊断码，正常降级路径不抛这个码）"
                    ],
                    [
                      "`envUnsupported`",
                      "`ENV_UNSUPPORTED`",
                      "`systemScheduler` 依赖的宿主能力（`performance.now`/`setTimeout`/`clearTimeout`）缺失"
                    ],
                    [
                      "`invalidOption`",
                      "`INVALID_OPTION`",
                      "scheduler 收到非法时间/延迟参数，或 descriptor 字段类型非法"
                    ]
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "逐条设计动机见源码 `src/error-code.ts` 的 JSDoc；调用方应始终以 `error.code === LifecycleErrorCode.xxx` 判别，不要硬编码码值字符串。"
                }
              ]
            }
          ],
          "usageScore": 46,
          "configuration": [],
          "fragment": "lifecycle-errors--ILifecycleError"
        },
        {
          "name": "LIFECYCLE_SOURCE",
          "kind": "const",
          "signature": "export declare const LIFECYCLE_SOURCE = \"@migaia/lifecycle\";",
          "declarationLine": 6,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "`source` value stamped onto every error this module throws.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LIFECYCLE_SOURCE.",
          "advanced": "No additional advanced behavior is declared for LIFECYCLE_SOURCE.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for LIFECYCLE_SOURCE."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for LIFECYCLE_SOURCE.",
          "examples": [
            "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'",
            "const LIFECYCLE_SOURCE: '@migaia/lifecycle'",
            "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'",
            "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
          ],
          "exportPath": "./errors",
          "overloadSignatures": ["export declare const LIFECYCLE_SOURCE = \"@migaia/lifecycle\";"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use LIFECYCLE_SOURCE when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when LIFECYCLE_SOURCE does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "`source` value stamped onto every error this module throws."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import LIFECYCLE_SOURCE from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use LIFECYCLE_SOURCE when its const contract and declared inputs match the result you need. Choose a different public symbol when LIFECYCLE_SOURCE does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for LIFECYCLE_SOURCE."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for LIFECYCLE_SOURCE."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:LIFECYCLE_SOURCE:export declare const LIFECYCLE_SOURCE = \"@migaia/lifecycle\";",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "lifecycle-source",
              "heading": "LIFECYCLE_SOURCE",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const LIFECYCLE_SOURCE: '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "本包所有错误的 `source` 字段固定值，字符串常量，无调用。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 54,
          "configuration": [],
          "fragment": "lifecycle-errors--LIFECYCLE_SOURCE"
        },
        {
          "name": "tagLifecycleError",
          "kind": "function",
          "signature": "export declare function tagLifecycleError<E extends Error>(error: E, code: string): E;",
          "declarationLine": 36,
          "source": "packages/lifecycle/dist/errors.d.ts",
          "purpose": "Stamps `(source, code)` onto an error object we did not construct ourselves (e.g. a built-in `AggregateError`) without touching `stack`, `message`, or `errors`. Used where the error's _type_ matters to callers (`AggregateError`) so `createLifecycleError`'s plain `Error` cannot be used.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for tagLifecycleError.",
          "advanced": "No additional advanced behavior is declared for tagLifecycleError.",
          "parameters": ["error: E", "code: string"],
          "parameterDetails": [
            {
              "name": "error",
              "type": "E",
              "optional": false
            },
            {
              "name": "code",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "E",
          "errors": ["No documented errors are declared for tagLifecycleError."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for tagLifecycleError.",
          "examples": [
            "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
          ],
          "exportPath": "./errors",
          "overloadSignatures": [
            "export declare function tagLifecycleError<E extends Error>(error: E, code: string): E;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "error",
                "type": "E",
                "optional": false
              },
              {
                "name": "code",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["E"],
          "whenToUse": "Use tagLifecycleError when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when tagLifecycleError does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Stamps `(source, code)` onto an error object we did not construct ourselves (e.g. a built-in `AggregateError`) without touching `stack`, `message`, or `errors`. Used where the error's _type_ matters to callers (`AggregateError`) so `createLifecycleError`'s plain `Error` cannot be used."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import tagLifecycleError from @migaia/lifecycle/errors, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use tagLifecycleError when its function contract and declared inputs match the result you need. Choose a different public symbol when tagLifecycleError does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for tagLifecycleError."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for tagLifecycleError."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/errors.d.ts:tagLifecycleError:export declare function tagLifecycleError<E extends Error>(error: E, code: string): E;",
          "guidance": [
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  type ILifecycleError,\n  type IThenableProbe,\n  type IErrorCollector\n} from '@migaia/lifecycle'"
                }
              ]
            },
            {
              "id": "taglifecycleerror",
              "heading": "tagLifecycleError",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function tagLifecycleError<E extends Error>(error: E, code: string): E"
                },
                {
                  "type": "paragraph",
                  "text": "给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "`error`、`code` 均必填。返回同一个对象（原地修改，保留原生类型如 `AggregateError`）。"
                }
              ]
            },
            {
              "id": "错误基础设施模块",
              "heading": "错误基础设施模块",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import {\n  LIFECYCLE_SOURCE,\n  createLifecycleError,\n  createLifecycleRangeError,\n  tagLifecycleError,\n  containAsyncRejection,\n  probeThenable,\n  assimilateCapturedThen,\n  createErrorCollector,\n  LifecycleErrorCode,\n  LifecycleErrorText\n} from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "`LIFECYCLE_SOURCE`｜3 秒上手 —— 本包所有错误的 `source` 字段固定值："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "LIFECYCLE_SOURCE // '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "字符串常量，无调用。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleError`｜5 秒上手 —— 构造一个携带 `(source, code)` 身份的 `Error`，从不改写 `stack`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleError(LifecycleErrorCode.scopeClosed, '[lifecycle] scope is closing')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code: string`（必填，建议取自 `LifecycleErrorCode`）、`message: string`（必填）、第三参数选项：`cause?: unknown`、`phase?: string`、`detail?: Readonly<Record<string, unknown>>`、`errors?: readonly unknown[]`（非空时冻结挂到 `.errors`）。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleRangeError`｜3 秒上手 —— 同上但产出原生 `RangeError`（保留原生类型，用于入参越界场景）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "throw createLifecycleRangeError(LifecycleErrorCode.invalidOption, 'delayMs must be >= 0')"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`code`、`message`（均必填）、第三参数选项 `{ cause?: unknown; detail?: Readonly<Record<string, unknown>> }`。"
                },
                {
                  "type": "paragraph",
                  "text": "`createLifecycleTypeError` / `createLifecycleFailure`｜5 秒上手 —— `/errors` leaf 的边界构造器：前者创建保留原生类型的 `TypeError`；后者尽量给既有 Error 原位附码，primitive/不可扩展错误则包装并通过 `cause` 保留。"
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { createLifecycleFailure, createLifecycleTypeError } from '@migaia/lifecycle/errors'\n\nthrow createLifecycleTypeError(LifecycleErrorCode.invalidOption, 'invalid option')\n// catch (cause) { throw createLifecycleFailure(code, message, cause); }"
                },
                {
                  "type": "paragraph",
                  "text": "`tagLifecycleError`｜5 秒上手 —— 给一个已存在（非本包构造）的错误对象就地贴上 `(source, code)`，常用于给原生 `AggregateError` 打标："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "tagLifecycleError(new AggregateError(errors, 'msg'), LifecycleErrorCode.scopeDisposalFailed)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`error: E extends Error`（必填）、`code: string`（必填）。返回同一个对象（原地修改）。"
                },
                {
                  "type": "paragraph",
                  "text": "`containAsyncRejection`｜5 秒上手 —— 保护性地探测某个返回值是否是 thenable，是则观察其 rejection 而不让其变成未处理拒绝："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "containAsyncRejection(maybeAsyncCallbackResult, (error) => report(error))"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`value: unknown`（必填）、`onRejected: (error: unknown) => void`（必填）。非 thenable 时直接返回，不调用 `onRejected`。"
                },
                {
                  "type": "paragraph",
                  "text": "`probeThenable`｜5 秒上手 —— 只读一次 `value.then`，返回判别结果（不是布尔值，是否 thenable/是否 getter 抛错都会区分）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const probe = probeThenable(result)\nif (probe.kind === 'thenable') {\n  /* probe.thenFn 已捕获，可安全 apply 一次 */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项。三种结果：`{ kind: 'not-thenable' }`、`{ kind: 'thenable', thenFn }`、`{ kind: 'failed', error }`（读 `.then` 本身抛错）。"
                },
                {
                  "type": "paragraph",
                  "text": "`assimilateCapturedThen`｜5 秒上手 —— 把 `probeThenable` 捕获到的 `thenFn` 安全地 apply 成一个真正的 `Promise`，且只调用一次："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const promise = assimilateCapturedThen<T>(probe.thenFn, thenableValue)"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`thenFn`（必填，来自 `probeThenable` 的 `thenFn`）、`thenable: unknown`（必填，作为 `this` receiver 传给 `thenFn`）。无可选项。"
                },
                {
                  "type": "paragraph",
                  "text": "`createErrorCollector`｜10 秒上手 —— 按四种错误策略收集/上报错误，`createDisposeTransaction`/`createSyncLifecycleScope` 内部都用它："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const collector = createErrorCollector('collect', undefined)\ncollector.add('item-1', error)\nconst collected = collector.finalize('teardown failed') // 'collect' 策略下返回 ICollectedError[]"
                },
                {
                  "type": "paragraph",
                  "text": "参数：`policy: 'throw' | 'collect' | 'report' | 'firstError'`（必填）、`report: ((error: unknown) => void) | undefined`（必填，显式传 `undefined` 表示无诊断通道）。`add(source, error)` 按策略即时处理（`report` 策略立即调用 `report`）；`finalize(message)`：`collect` 返回收集到的数组，`report` 返回空数组（已在 `add` 时上报），`throw` 单错原样抛、多错聚合成携带 `SCOPE_DISPOSAL_FAILED` 码的 `AggregateError`，`firstError` 有错误时抛首个（其余经 `report` 观测），否则返回空数组。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorCode`｜10 秒上手 —— 稳定错误码表（20 个码），唯一声明处，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "paragraph",
                  "text": "全部取值：`scopeClosed`(`SCOPE_CLOSED`)、`scopeTerminal`(`SCOPE_TERMINAL`)、`scopeReentrantDispose`(`SCOPE_REENTRANT_DISPOSE`)、`scopeReentrantOwn`(`SCOPE_REENTRANT_OWN`)、`scopeSyncViolation`(`SCOPE_SYNC_VIOLATION`)、`scopeDisposalFailed`(`SCOPE_DISPOSAL_FAILED`)、`unitStartFailed`(`UNIT_START_FAILED`)、`generationSuperseded`(`GENERATION_SUPERSEDED`)、`generationCancellationFailed`(`GENERATION_CANCELLATION_FAILED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`generationDisposed`(`GENERATION_DISPOSED`)、`quiescenceSealed`(`QUIESCENCE_SEALED`)、`quiescenceUnsealedWait`(`QUIESCENCE_UNSEALED_WAIT`)、`provisionalSettled`(`PROVISIONAL_SETTLED`)、`provisionalParentClosed`(`PROVISIONAL_PARENT_CLOSED`)、`queueAdmissionTimeout`(`QUEUE_ADMISSION_TIMEOUT`)、`queueSelfDependency`(`QUEUE_SELF_DEPENDENCY`)、`releaseForceFailed`(`RELEASE_FORCE_FAILED`)、`deadlineExceeded`(`DEADLINE_EXCEEDED`)、"
                },
                {
                  "type": "paragraph",
                  "text": "`envUnsupported`(`ENV_UNSUPPORTED`)、`invalidOption`(`INVALID_OPTION`)。逐条语义见 USEGUIDE.md。"
                },
                {
                  "type": "paragraph",
                  "text": "`LifecycleErrorText`｜3 秒上手 —— 稳定诊断消息表（本包内部错误信息的唯一声明处，公开是为了让调用方按文本断言/比对）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "error.message === LifecycleErrorText.disposeTransactionFailed"
                },
                {
                  "type": "paragraph",
                  "text": "全部键：`provisionalCleanupFailed`、`disposeTransactionFailed`、`disposeDescriptorInvalid`、`mutationAdmissionTimedOut`、`generationCancellationFailed`、`disposalLedgerClosed`、`disposalLedgerReentrant`、`envUnsupported`、`abortSignalInvalid`、`schedulerTaskCancelGetterFailed`、`schedulerTaskInvalid`、`schedulerAccessorFailed`、`schedulerInvalid`、`schedulerNumberType`、`schedulerNumberRange`、`schedulerDelayRange`、"
                },
                {
                  "type": "paragraph",
                  "text": "`schedulerTimeOverflow`、`generationTimeoutAccessorFailed`。"
                }
              ]
            }
          ],
          "usageScore": 58,
          "configuration": [],
          "fragment": "lifecycle-errors--tagLifecycleError"
        }
      ],
      "aliases": []
    },
    {
      "id": "lifecycle:generation",
      "library": "lifecycle",
      "module": "generation",
      "exportPath": "./generation",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "createGenerationController",
          "kind": "function",
          "signature": "export declare function createGenerationController(options?: IGenerationControllerOptions): IGenerationController;",
          "declarationLine": 54,
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "purpose": "Merges `@migaia/reactive`'s `RequestControllerImpl`+`GenerationController` with `web-rpc`'s `OperationScope`: token identity, an `AbortSignal` per generation, an optional per-generation timeout, and automatic abort when a parent's closing signal fires.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createGenerationController.",
          "advanced": "No additional advanced behavior is declared for createGenerationController.",
          "parameters": ["options: IGenerationControllerOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "IGenerationControllerOptions",
              "optional": true
            }
          ],
          "returns": "IGenerationController",
          "errors": ["No documented errors are declared for createGenerationController."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createGenerationController.",
          "examples": [
            "const shutdown = new AbortController()\nconst generations = createGenerationController({ parentSignal: shutdown.signal })\nconst request = generations.begin({ timeoutMs: 5000 }) // 超过 5s 自动 abort 当前代\nconst result = await fetchWithSignal(request.signal)\nif (generations.adopt(request.token, result, (v) => v.close())) {\n  use(result) // 仍是当前代，安全采用\n} // 否则内部已调用 release() 回收 result，返回 false",
            "import { createLifecycleScope, createGenerationController } from '@migaia/lifecycle'\n\nconst scope = createLifecycleScope({ errorPolicy: 'collect' })\nconst generations = createGenerationController()\n\nasync function loadUser(id: string) {\n  const request = generations.begin({ timeoutMs: 5000 })\n  const user = await fetchUser(id, { signal: request.signal })\n  if (!generations.adopt(request.token, user, (u) => u.dispose?.())) return // 已过期，静默丢弃\n  scope.own(user, { force: () => user.dispose?.() })\n}"
          ],
          "exportPath": "./generation",
          "overloadSignatures": [
            "export declare function createGenerationController(options?: IGenerationControllerOptions): IGenerationController;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "IGenerationControllerOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["IGenerationController"],
          "whenToUse": "Use createGenerationController when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createGenerationController does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Merges `@migaia/reactive`'s `RequestControllerImpl`+`GenerationController` with `web-rpc`'s `OperationScope`: token identity, an `AbortSignal` per generation, an optional per-generation timeout, and automatic abort when a parent's closing signal fires."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createGenerationController from @migaia/lifecycle/generation, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createGenerationController when its function contract and declared inputs match the result you need. Choose a different public symbol when createGenerationController does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createGenerationController."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createGenerationController."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "parentSignal",
              "type": "IAbortSignal",
              "optional": true,
              "description": "When this aborts, the currently-active generation's `signal` aborts too (L-T31)."
            },
            {
              "name": "onSuperseded",
              "type": "(info: ILifecycleError) => void",
              "optional": true,
              "description": "Diagnostic channel fired from `adopt()` whenever `token` is no longer current — before the * release attempt, regardless of whether it succeeds. The value is a * `GENERATION_SUPERSEDED`-tagged error used purely as a `(source, code)`-bearing carrier; this is * explicitly _not_ a failure signal (§4.10.2), just an observability hook for callers that want * to notice the race without treating it as an error."
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "Runtime-neutral scheduler（默认 `systemScheduler`）；`begin({ timeoutMs })` 的超时计时经它。"
            }
          ],
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:createGenerationController:export declare function createGenerationController(options?: IGenerationControllerOptions): IGenerationController;",
          "guidance": [
            {
              "id": "creategenerationcontroller",
              "heading": "createGenerationController",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}"
                },
                {
                  "type": "paragraph",
                  "text": "代数 + 每代一个 `AbortSignal`，新一代自动使旧一代失效："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const shutdown = new AbortController()\nconst generations = createGenerationController({ parentSignal: shutdown.signal })\nconst request = generations.begin({ timeoutMs: 5000 }) // 超过 5s 自动 abort 当前代\nconst result = await fetchWithSignal(request.signal)\nif (generations.adopt(request.token, result, (v) => v.close())) {\n  use(result) // 仍是当前代，安全采用\n} // 否则内部已调用 release() 回收 result，返回 false"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`begin(options?)`：使当前活跃代（若存在）立即失效（触发其 `signal` 中止），`generation` 自增，返回新的 `{ generation, token, signal }`。`options.timeoutMs?` 非法（非有限/负数）抛 `INVALID_OPTION`；超过该毫秒数后当前代的 `signal` 自动中止。控制器已 `dispose()` 后调用抛 `GENERATION_DISPOSED`。",
                    "`isCurrent(token)`：`token` 是否仍是当前代；`dispose()` 之后恒为 `false`。",
                    "`supersede(reason?)`：作废当前代（其 `signal` 中止）但控制器本身仍可用，之后 `begin()` 仍能开新代；对已 `dispose()` 的控制器调用是空操作。",
                    "`adopt(token, value, release, onReleaseError?)`：`token` 仍是当前代时返回 `true`，`value` 留给调用方；否则调用 `release(value)` 回收并返回 `false`——`release` 本身抛出/reject 只会经 `onReleaseError` 上报，绝不反过来污染\"当前是哪一代\"这一状态（不会抛出、不会重新计入错误策略）。若提供了 `onSuperseded`，在过期路径上先额外触发一次携带 `GENERATION_SUPERSEDED` 码的诊断信息（非失败信号）。",
                    "`dispose(reason?)`：终态；之后 `begin()` 恒抛 `GENERATION_DISPOSED`，`isCurrent()` 恒 `false`。",
                    "`parentSignal` 中止时，当前活跃代同步中止（清理 timer、移除 parent listener、abort 自身 controller）；清理过程中任一步失败会聚合为 `GENERATION_CANCELLATION_FAILED`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 96,
          "configuration": [
            {
              "name": "parentSignal",
              "type": "IAbortSignal",
              "optional": true,
              "description": "`parentSignal` 中止时，当前活跃代同步中止（清理 timer、移除 parent listener、abort 自身 controller）；清理过程中任一步失败会聚合为 `GENERATION_CANCELLATION_FAILED`。",
              "descriptionEn": "When this aborts, the currently-active generation's `signal` aborts too (L-T31).",
              "descriptionZh": "`parentSignal` 中止时，当前活跃代同步中止（清理 timer、移除 parent listener、abort 自身 controller）；清理过程中任一步失败会聚合为 `GENERATION_CANCELLATION_FAILED`。"
            },
            {
              "name": "onSuperseded",
              "type": "(info: ILifecycleError) => void",
              "optional": true,
              "description": "`adopt(token, value, release, onReleaseError?)`：`token` 仍是当前代时返回 `true`，`value` 留给调用方；否则调用 `release(value)` 回收并返回 `false`——`release` 本身抛出/reject 只会经 `onReleaseError` 上报，绝不反过来污染\"当前是哪一代\"这一状态（不会抛出、不会重新计入错误策略）。若提供了 `onSuperseded`，在过期路径上先额外触发一次携带 `GENERATION_SUPERSEDED` 码的诊断信息（非失败信号）。",
              "descriptionEn": "Diagnostic channel fired from `adopt()` whenever `token` is no longer current — before the * release attempt, regardless of whether it succeeds. The value is a * `GENERATION_SUPERSEDED`-tagged error used purely as a `(source, code)`-bearing carrier; this is * explicitly _not_ a failure signal (§4.10.2), just an observability hook for callers that want * to notice the race without treating it as an error.",
              "descriptionZh": "`adopt(token, value, release, onReleaseError?)`：`token` 仍是当前代时返回 `true`，`value` 留给调用方；否则调用 `release(value)` 回收并返回 `false`——`release` 本身抛出/reject 只会经 `onReleaseError` 上报，绝不反过来污染\"当前是哪一代\"这一状态（不会抛出、不会重新计入错误策略）。若提供了 `onSuperseded`，在过期路径上先额外触发一次携带 `GENERATION_SUPERSEDED` 码的诊断信息（非失败信号）。"
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "`@migaia/lifecycle/scheduler` — scheduler、snapshot与时间校验",
              "descriptionEn": "",
              "descriptionZh": "`@migaia/lifecycle/scheduler` — scheduler、snapshot与时间校验"
            }
          ],
          "fragment": "lifecycle-generation--createGenerationController"
        },
        {
          "name": "IGenerationController",
          "kind": "type",
          "signature": "export type IGenerationController = { readonly generation: number; readonly disposed: boolean; /** * Starts a new generation, superseding whatever was active. Optionally auto-aborts after * `timeoutMs`. */ begin(options?: { readonly timeoutMs?: number; }): IGenerationRequest; isCurrent(token: IGenerationToken): boolean; /** * Invalidates the current generation without disposing the controller — a later `begin()` still * works. */ supersede(reason?: unknown): void; /** * Adopts `value` under `token`'s generation. Returns `true` and keeps `value` with the caller if * that generation is still current. Otherwise releases `value` via `release` and returns `false` * (L-T6) — a release failure is funneled to `onReleaseError` and never mutates controller state, * so it cannot poison whichever generation is current now (L-T30). */ adopt<T>(token: IGenerationToken, value: T, release: (value: T) => void | PromiseLike<void>, onReleaseError?: (error: unknown) => void): boolean; dispose(reason?: unknown): void; };",
          "declarationLine": 24,
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "purpose": "IGenerationController is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationController.",
          "advanced": "No additional advanced behavior is declared for IGenerationController.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IGenerationController."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IGenerationController.",
          "examples": [
            "import {\n  createLifecycleUnit,\n  type ILifecycleUnit,\n  type ILifecycleUnitOptions,\n  createGenerationController,\n  type IGenerationController,\n  type IGenerationControllerOptions,\n  type IGenerationRequest,\n  type IGenerationToken\n} from '@migaia/lifecycle'",
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}"
          ],
          "exportPath": "./generation",
          "overloadSignatures": [
            "export type IGenerationController = { readonly generation: number; readonly disposed: boolean; /** * Starts a new generation, superseding whatever was active. Optionally auto-aborts after * `timeoutMs`. */ begin(options?: { readonly timeoutMs?: number; }): IGenerationRequest; isCurrent(token: IGenerationToken): boolean; /** * Invalidates the current generation without disposing the controller — a later `begin()` still * works. */ supersede(reason?: unknown): void; /** * Adopts `value` under `token`'s generation. Returns `true` and keeps `value` with the caller if * that generation is still current. Otherwise releases `value` via `release` and returns `false` * (L-T6) — a release failure is funneled to `onReleaseError` and never mutates controller state, * so it cannot poison whichever generation is current now (L-T30). */ adopt<T>(token: IGenerationToken, value: T, release: (value: T) => void | PromiseLike<void>, onReleaseError?: (error: unknown) => void): boolean; dispose(reason?: unknown): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IGenerationController when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IGenerationController does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IGenerationController is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IGenerationController from @migaia/lifecycle/generation, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IGenerationController when its type contract and declared inputs match the result you need. Choose a different public symbol when IGenerationController does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationController."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IGenerationController."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationController:export type IGenerationController = { readonly generation: number; readonly disposed: boolean; /** * Starts a new generation, superseding whatever was active. Optionally auto-aborts after * `timeoutMs`. */ begin(options?: { readonly timeoutMs?: number; }): IGenerationRequest; isCurrent(token: IGenerationToken): boolean; /** * Invalidates the current generation without disposing the controller — a later `begin()` still * works. */ supersede(reason?: unknown): void; /** * Adopts `value` under `token`'s generation. Returns `true` and keeps `value` with the caller if * that generation is still current. Otherwise releases `value` via `release` and returns `false` * (L-T6) — a release failure is funneled to `onReleaseError` and never mutates controller state, * so it cannot poison whichever generation is current now (L-T30). */ adopt<T>(token: IGenerationToken, value: T, release: (value: T) => void | PromiseLike<void>, onReleaseError?: (error: unknown) => void): boolean; dispose(reason?: unknown): void; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-generation--IGenerationController"
        },
        {
          "name": "IGenerationControllerOptions",
          "kind": "type",
          "signature": "export type IGenerationControllerOptions = { /** When this aborts, the currently-active generation's `signal` aborts too (L-T31). */ readonly parentSignal?: IAbortSignal; /** * Diagnostic channel fired from `adopt()` whenever `token` is no longer current — before the * release attempt, regardless of whether it succeeds. The value is a * `GENERATION_SUPERSEDED`-tagged error used purely as a `(source, code)`-bearing carrier; this is * explicitly _not_ a failure signal (§4.10.2), just an observability hook for callers that want * to notice the race without treating it as an error. */ readonly onSuperseded?: (info: ILifecycleError) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；`begin({ timeoutMs })` 的超时计时经它。 */ readonly scheduler?: ILifecycleScheduler; };",
          "declarationLine": 10,
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "purpose": "IGenerationControllerOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationControllerOptions.",
          "advanced": "No additional advanced behavior is declared for IGenerationControllerOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IGenerationControllerOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IGenerationControllerOptions.",
          "examples": [
            "import {\n  createLifecycleUnit,\n  type ILifecycleUnit,\n  type ILifecycleUnitOptions,\n  createGenerationController,\n  type IGenerationController,\n  type IGenerationControllerOptions,\n  type IGenerationRequest,\n  type IGenerationToken\n} from '@migaia/lifecycle'",
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}"
          ],
          "exportPath": "./generation",
          "overloadSignatures": [
            "export type IGenerationControllerOptions = { /** When this aborts, the currently-active generation's `signal` aborts too (L-T31). */ readonly parentSignal?: IAbortSignal; /** * Diagnostic channel fired from `adopt()` whenever `token` is no longer current — before the * release attempt, regardless of whether it succeeds. The value is a * `GENERATION_SUPERSEDED`-tagged error used purely as a `(source, code)`-bearing carrier; this is * explicitly _not_ a failure signal (§4.10.2), just an observability hook for callers that want * to notice the race without treating it as an error. */ readonly onSuperseded?: (info: ILifecycleError) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；`begin({ timeoutMs })` 的超时计时经它。 */ readonly scheduler?: ILifecycleScheduler; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IGenerationControllerOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IGenerationControllerOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IGenerationControllerOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IGenerationControllerOptions from @migaia/lifecycle/generation, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IGenerationControllerOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when IGenerationControllerOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationControllerOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IGenerationControllerOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationControllerOptions:export type IGenerationControllerOptions = { /** When this aborts, the currently-active generation's `signal` aborts too (L-T31). */ readonly parentSignal?: IAbortSignal; /** * Diagnostic channel fired from `adopt()` whenever `token` is no longer current — before the * release attempt, regardless of whether it succeeds. The value is a * `GENERATION_SUPERSEDED`-tagged error used purely as a `(source, code)`-bearing carrier; this is * explicitly _not_ a failure signal (§4.10.2), just an observability hook for callers that want * to notice the race without treating it as an error. */ readonly onSuperseded?: (info: ILifecycleError) => void; /** Runtime-neutral scheduler（默认 `systemScheduler`）；`begin({ timeoutMs })` 的超时计时经它。 */ readonly scheduler?: ILifecycleScheduler; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-generation--IGenerationControllerOptions"
        },
        {
          "name": "IGenerationRequest",
          "kind": "type",
          "signature": "export type IGenerationRequest = { readonly generation: number; readonly token: IGenerationToken; readonly signal: IAbortSignal; };",
          "declarationLine": 5,
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "purpose": "IGenerationRequest is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationRequest.",
          "advanced": "No additional advanced behavior is declared for IGenerationRequest.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IGenerationRequest."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IGenerationRequest.",
          "examples": [
            "import {\n  createLifecycleUnit,\n  type ILifecycleUnit,\n  type ILifecycleUnitOptions,\n  createGenerationController,\n  type IGenerationController,\n  type IGenerationControllerOptions,\n  type IGenerationRequest,\n  type IGenerationToken\n} from '@migaia/lifecycle'",
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}"
          ],
          "exportPath": "./generation",
          "overloadSignatures": [
            "export type IGenerationRequest = { readonly generation: number; readonly token: IGenerationToken; readonly signal: IAbortSignal; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IGenerationRequest when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IGenerationRequest does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IGenerationRequest is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IGenerationRequest from @migaia/lifecycle/generation, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IGenerationRequest when its type contract and declared inputs match the result you need. Choose a different public symbol when IGenerationRequest does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationRequest."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IGenerationRequest."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationRequest:export type IGenerationRequest = { readonly generation: number; readonly token: IGenerationToken; readonly signal: IAbortSignal; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-generation--IGenerationRequest"
        },
        {
          "name": "IGenerationToken",
          "kind": "type",
          "signature": "export type IGenerationToken = object;",
          "declarationLine": 4,
          "source": "packages/lifecycle/dist/generation-controller.d.ts",
          "purpose": "IGenerationToken is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationToken.",
          "advanced": "No additional advanced behavior is declared for IGenerationToken.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IGenerationToken."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IGenerationToken.",
          "examples": [
            "import {\n  createLifecycleUnit,\n  type ILifecycleUnit,\n  type ILifecycleUnitOptions,\n  createGenerationController,\n  type IGenerationController,\n  type IGenerationControllerOptions,\n  type IGenerationRequest,\n  type IGenerationToken\n} from '@migaia/lifecycle'",
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}"
          ],
          "exportPath": "./generation",
          "overloadSignatures": ["export type IGenerationToken = object;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IGenerationToken when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IGenerationToken does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IGenerationToken is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IGenerationToken from @migaia/lifecycle/generation, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IGenerationToken when its type contract and declared inputs match the result you need. Choose a different public symbol when IGenerationToken does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IGenerationToken."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IGenerationToken."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/generation-controller.d.ts:IGenerationToken:export type IGenerationToken = object;",
          "guidance": [],
          "usageScore": 36,
          "configuration": [],
          "fragment": "lifecycle-generation--IGenerationToken"
        }
      ],
      "aliases": []
    },
    {
      "id": "lifecycle:quiescence",
      "library": "lifecycle",
      "module": "quiescence",
      "exportPath": "./quiescence",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "createObjectLeaseRegistry",
          "kind": "function",
          "signature": "export declare function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>;",
          "declarationLine": 31,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "createObjectLeaseRegistry is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createObjectLeaseRegistry.",
          "advanced": "No additional advanced behavior is declared for createObjectLeaseRegistry.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "ILeaseRegistry<TKey>",
          "errors": ["No documented errors are declared for createObjectLeaseRegistry."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createObjectLeaseRegistry.",
          "examples": [
            "const leases = createObjectLeaseRegistry<object>() // 内部就是 createQuiescenceTracker()"
          ],
          "exportPath": "./quiescence",
          "overloadSignatures": [
            "export declare function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["ILeaseRegistry<TKey>"],
          "whenToUse": "Use createObjectLeaseRegistry when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createObjectLeaseRegistry does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createObjectLeaseRegistry is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createObjectLeaseRegistry from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createObjectLeaseRegistry when its function contract and declared inputs match the result you need. Choose a different public symbol when createObjectLeaseRegistry does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createObjectLeaseRegistry."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createObjectLeaseRegistry."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createObjectLeaseRegistry:export declare function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>;",
          "guidance": [],
          "usageScore": 54,
          "configuration": [],
          "fragment": "lifecycle-quiescence--createObjectLeaseRegistry"
        },
        {
          "name": "createPendingTracker",
          "kind": "function",
          "signature": "export declare function createPendingTracker(): IPendingTracker;",
          "declarationLine": 47,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "createPendingTracker is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createPendingTracker.",
          "advanced": "No additional advanced behavior is declared for createPendingTracker.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IPendingTracker",
          "errors": ["No documented errors are declared for createPendingTracker."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createPendingTracker.",
          "examples": [
            "const pending = createPendingTracker()\npending.track(doWork()) // 原样返回传入的 promise\nawait pending.drain() // 等到 size 归零；排空期间新增的 track() 不会被漏计（内部循环 whenZeroOnce）\npending.size // 当前在途数量",
            "import { createPendingTracker, createMutationQueue } from '@migaia/lifecycle'\n\nconst pending = createPendingTracker()\nconst queue = createMutationQueue({ queueAdmissionTimeoutMs: 3000 })\n\nasync function applyBatch(mutations: Array<() => Promise<void>>) {\n  for (const mutation of mutations) {\n    pending.track(queue.enqueue(mutation, { owner: 'batch' }))\n  }\n  await pending.drain() // 等到本批（含批内动态追加的）全部落地\n}",
            "import { createLifecycleScope, createPendingTracker } from '@migaia/lifecycle'\n\nconst scope = createLifecycleScope({ errorPolicy: 'collect' })\nconst pending = createPendingTracker()\n\nfunction handle(request: Request): Promise<Response> {\n  return pending.track(route(request))\n}\n\nasync function shutdown() {\n  await pending.drain()\n  return scope.dispose()\n}"
          ],
          "exportPath": "./quiescence",
          "overloadSignatures": [
            "export declare function createPendingTracker(): IPendingTracker;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IPendingTracker"],
          "whenToUse": "Use createPendingTracker when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createPendingTracker does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createPendingTracker is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createPendingTracker from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createPendingTracker when its function contract and declared inputs match the result you need. Choose a different public symbol when createPendingTracker does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createPendingTracker."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createPendingTracker."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createPendingTracker:export declare function createPendingTracker(): IPendingTracker;",
          "guidance": [
            {
              "id": "creatependingtracker",
              "heading": "createPendingTracker",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createPendingTracker(): IPendingTracker\n\ntype IPendingTracker = {\n  track<T>(promise: Promise<T>): Promise<T>\n  drain(): Promise<void>\n  readonly size: number\n}"
                },
                {
                  "type": "paragraph",
                  "text": "只暴露 `track`/`drain`/`size` 的窄接口，用于追踪并排空在途 Promise（内部基于一个字符串键 `createStringQuiescenceTracker()`）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const pending = createPendingTracker()\npending.track(doWork()) // 原样返回传入的 promise\nawait pending.drain() // 等到 size 归零；排空期间新增的 track() 不会被漏计（内部循环 whenZeroOnce）\npending.size // 当前在途数量"
                },
                {
                  "type": "paragraph",
                  "text": "无构造参数。`track(promise)` 结算后（无论 resolve 还是 reject）自动释放内部租约；`drain()` 永不抛出（内部忽略释放函数自身的异常，`release` 本身也不会抛）；`drain()` 内部循环调用 `whenZeroOnce()`，因此排空期间新 `track()` 进来的 promise 也会被等到。"
                }
              ]
            }
          ],
          "usageScore": 122,
          "configuration": [],
          "fragment": "lifecycle-quiescence--createPendingTracker"
        },
        {
          "name": "createQuiescenceTracker",
          "kind": "function",
          "signature": "export declare function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>;",
          "declarationLine": 19,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "Object-keyed variant, `WeakMap`-backed so entries are collected once the key is unreachable elsewhere.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createQuiescenceTracker.",
          "advanced": "No additional advanced behavior is declared for createQuiescenceTracker.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IQuiescenceTracker<TKey>",
          "errors": ["No documented errors are declared for createQuiescenceTracker."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createQuiescenceTracker.",
          "examples": [
            "const tracker = createQuiescenceTracker<object>() // 或 createStringQuiescenceTracker()\nconst release = tracker.retain(key)\ntracker.count(key) // 1\ntracker.seal(key) // 之后 retain(key) 抛 QUIESCENCE_SEALED\nrelease()\nawait tracker.whenZero(key) // 必须先 seal，否则同步抛 QUIESCENCE_UNSEALED_WAIT",
            "const leases = createObjectLeaseRegistry<object>() // 内部就是 createQuiescenceTracker()"
          ],
          "exportPath": "./quiescence",
          "overloadSignatures": [
            "export declare function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IQuiescenceTracker<TKey>"],
          "whenToUse": "Use createQuiescenceTracker when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createQuiescenceTracker does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Object-keyed variant, `WeakMap`-backed so entries are collected once the key is unreachable elsewhere."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createQuiescenceTracker from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createQuiescenceTracker when its function contract and declared inputs match the result you need. Choose a different public symbol when createQuiescenceTracker does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createQuiescenceTracker."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createQuiescenceTracker."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createQuiescenceTracker:export declare function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>;",
          "guidance": [],
          "usageScore": 64,
          "configuration": [],
          "fragment": "lifecycle-quiescence--createQuiescenceTracker"
        },
        {
          "name": "createStringLeaseRegistry",
          "kind": "function",
          "signature": "export declare function createStringLeaseRegistry(): ILeaseRegistry<string>;",
          "declarationLine": 32,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "createStringLeaseRegistry is part of this module's public function contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStringLeaseRegistry.",
          "advanced": "No additional advanced behavior is declared for createStringLeaseRegistry.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "ILeaseRegistry<string>",
          "errors": ["No documented errors are declared for createStringLeaseRegistry."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createStringLeaseRegistry.",
          "examples": [],
          "exportPath": "./quiescence",
          "overloadSignatures": [
            "export declare function createStringLeaseRegistry(): ILeaseRegistry<string>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["ILeaseRegistry<string>"],
          "whenToUse": "Use createStringLeaseRegistry when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createStringLeaseRegistry does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "createStringLeaseRegistry is part of this module's public function contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createStringLeaseRegistry from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createStringLeaseRegistry when its function contract and declared inputs match the result you need. Choose a different public symbol when createStringLeaseRegistry does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStringLeaseRegistry."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createStringLeaseRegistry."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createStringLeaseRegistry:export declare function createStringLeaseRegistry(): ILeaseRegistry<string>;",
          "guidance": [],
          "usageScore": 52,
          "configuration": [],
          "fragment": "lifecycle-quiescence--createStringLeaseRegistry"
        },
        {
          "name": "createStringQuiescenceTracker",
          "kind": "function",
          "signature": "export declare function createStringQuiescenceTracker(): IQuiescenceTracker<string>;",
          "declarationLine": 24,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "String-keyed variant. `WeakMap` cannot take string keys, so pristine entries are pruned explicitly.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStringQuiescenceTracker.",
          "advanced": "No additional advanced behavior is declared for createStringQuiescenceTracker.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IQuiescenceTracker<string>",
          "errors": ["No documented errors are declared for createStringQuiescenceTracker."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createStringQuiescenceTracker.",
          "examples": [
            "const tracker = createQuiescenceTracker<object>() // 或 createStringQuiescenceTracker()\nconst release = tracker.retain(key)\ntracker.count(key) // 1\ntracker.seal(key) // 之后 retain(key) 抛 QUIESCENCE_SEALED\nrelease()\nawait tracker.whenZero(key) // 必须先 seal，否则同步抛 QUIESCENCE_UNSEALED_WAIT"
          ],
          "exportPath": "./quiescence",
          "overloadSignatures": [
            "export declare function createStringQuiescenceTracker(): IQuiescenceTracker<string>;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IQuiescenceTracker<string>"],
          "whenToUse": "Use createStringQuiescenceTracker when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createStringQuiescenceTracker does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "String-keyed variant. `WeakMap` cannot take string keys, so pristine entries are pruned explicitly."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createStringQuiescenceTracker from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createStringQuiescenceTracker when its function contract and declared inputs match the result you need. Choose a different public symbol when createStringQuiescenceTracker does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createStringQuiescenceTracker."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createStringQuiescenceTracker."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:createStringQuiescenceTracker:export declare function createStringQuiescenceTracker(): IQuiescenceTracker<string>;",
          "guidance": [],
          "usageScore": 66,
          "configuration": [],
          "fragment": "lifecycle-quiescence--createStringQuiescenceTracker"
        },
        {
          "name": "ILeaseRegistry",
          "kind": "type",
          "signature": "export type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>;",
          "declarationLine": 30,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "`LeaseRegistry` — the same engine as `QuiescenceTracker`, re-exported under the name lease call sites reach for. §4.8 requires it to pass `whenZero()`/`whenZeroOnce()`/`seal()` straight through rather than hiding them behind `count()` polling.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILeaseRegistry.",
          "advanced": "No additional advanced behavior is declared for ILeaseRegistry.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILeaseRegistry."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILeaseRegistry.",
          "examples": [
            "import {\n  createQuiescenceTracker,\n  createStringQuiescenceTracker,\n  createObjectLeaseRegistry,\n  createStringLeaseRegistry,\n  createPendingTracker,\n  type IQuiescenceTracker,\n  type ILeaseRegistry,\n  type IPendingTracker\n} from '@migaia/lifecycle'",
            "function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>\nfunction createStringLeaseRegistry(): ILeaseRegistry<string>\n// type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>"
          ],
          "exportPath": "./quiescence",
          "overloadSignatures": ["export type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILeaseRegistry when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILeaseRegistry does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "`LeaseRegistry` — the same engine as `QuiescenceTracker`, re-exported under the name lease call sites reach for. §4.8 requires it to pass `whenZero()`/`whenZeroOnce()`/`seal()` straight through rather than hiding them behind `count()` polling."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILeaseRegistry from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILeaseRegistry when its type contract and declared inputs match the result you need. Choose a different public symbol when ILeaseRegistry does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILeaseRegistry."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILeaseRegistry."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:ILeaseRegistry:export type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>;",
          "guidance": [],
          "usageScore": 34,
          "configuration": [],
          "fragment": "lifecycle-quiescence--ILeaseRegistry"
        },
        {
          "name": "IPendingTracker",
          "kind": "type",
          "signature": "export type IPendingTracker = { /** Registers `promise` as in-flight; auto-releases on settle. Returns `promise` unchanged. */ track<T>(promise: Promise<T>): Promise<T>; /** * Waits until nothing is in flight, looping via `whenZeroOnce()` so work started while draining * is never missed (L-T10). */ drain(): Promise<void>; readonly size: number; };",
          "declarationLine": 37,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "The narrow facade for in-flight operations: unlike `LeaseRegistry`, it never exposes lease semantics (retain/seal) to domain callers — only `track()`/`drain()`/`size`.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPendingTracker.",
          "advanced": "No additional advanced behavior is declared for IPendingTracker.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IPendingTracker."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IPendingTracker.",
          "examples": [
            "import {\n  createQuiescenceTracker,\n  createStringQuiescenceTracker,\n  createObjectLeaseRegistry,\n  createStringLeaseRegistry,\n  createPendingTracker,\n  type IQuiescenceTracker,\n  type ILeaseRegistry,\n  type IPendingTracker\n} from '@migaia/lifecycle'",
            "function createPendingTracker(): IPendingTracker\n\ntype IPendingTracker = {\n  track<T>(promise: Promise<T>): Promise<T>\n  drain(): Promise<void>\n  readonly size: number\n}"
          ],
          "exportPath": "./quiescence",
          "overloadSignatures": [
            "export type IPendingTracker = { /** Registers `promise` as in-flight; auto-releases on settle. Returns `promise` unchanged. */ track<T>(promise: Promise<T>): Promise<T>; /** * Waits until nothing is in flight, looping via `whenZeroOnce()` so work started while draining * is never missed (L-T10). */ drain(): Promise<void>; readonly size: number; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IPendingTracker when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IPendingTracker does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "The narrow facade for in-flight operations: unlike `LeaseRegistry`, it never exposes lease semantics (retain/seal) to domain callers — only `track()`/`drain()`/`size`."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IPendingTracker from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IPendingTracker when its type contract and declared inputs match the result you need. Choose a different public symbol when IPendingTracker does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IPendingTracker."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IPendingTracker."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:IPendingTracker:export type IPendingTracker = { /** Registers `promise` as in-flight; auto-releases on settle. Returns `promise` unchanged. */ track<T>(promise: Promise<T>): Promise<T>; /** * Waits until nothing is in flight, looping via `whenZeroOnce()` so work started while draining * is never missed (L-T10). */ drain(): Promise<void>; readonly size: number; };",
          "guidance": [],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-quiescence--IPendingTracker"
        },
        {
          "name": "IQuiescenceTracker",
          "kind": "type",
          "signature": "export type IQuiescenceTracker<TKey> = { retain(key: TKey): IDisposer; count(key: TKey): number; /** Strict, exclusive drain: throws synchronously (not a rejected Promise) unless `isSealed(key)`. */ whenZero(key: TKey): Promise<void>; /** Non-exclusive: resolves the next time count reaches zero; does not block new `retain()` calls. */ whenZeroOnce(key: TKey): Promise<void>; /** Blocks further `retain()` calls for `key`. Idempotent. */ seal(key: TKey): void; isSealed(key: TKey): boolean; /** Removes a zero-count key and returns whether a state entry was removed. */ forget(key: TKey): boolean; };",
          "declarationLine": 2,
          "source": "packages/lifecycle/dist/quiescence-tracker.d.ts",
          "purpose": "IQuiescenceTracker is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IQuiescenceTracker.",
          "advanced": "No additional advanced behavior is declared for IQuiescenceTracker.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IQuiescenceTracker."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IQuiescenceTracker.",
          "examples": [
            "import {\n  createQuiescenceTracker,\n  createStringQuiescenceTracker,\n  createObjectLeaseRegistry,\n  createStringLeaseRegistry,\n  createPendingTracker,\n  type IQuiescenceTracker,\n  type ILeaseRegistry,\n  type IPendingTracker\n} from '@migaia/lifecycle'",
            "function createQuiescenceTracker<TKey extends object>(): IQuiescenceTracker<TKey>\nfunction createStringQuiescenceTracker(): IQuiescenceTracker<string>\n\ntype IQuiescenceTracker<TKey> = {\n  retain(key: TKey): IDisposer // IDisposer = () => void\n  count(key: TKey): number\n  whenZero(key: TKey): Promise<void> // 严格独占等待\n  whenZeroOnce(key: TKey): Promise<void> // 非独占等待\n  seal(key: TKey): void\n  isSealed(key: TKey): boolean\n  forget(key: TKey): boolean\n}",
            "function createObjectLeaseRegistry<TKey extends object>(): ILeaseRegistry<TKey>\nfunction createStringLeaseRegistry(): ILeaseRegistry<string>\n// type ILeaseRegistry<TKey> = IQuiescenceTracker<TKey>"
          ],
          "exportPath": "./quiescence",
          "overloadSignatures": [
            "export type IQuiescenceTracker<TKey> = { retain(key: TKey): IDisposer; count(key: TKey): number; /** Strict, exclusive drain: throws synchronously (not a rejected Promise) unless `isSealed(key)`. */ whenZero(key: TKey): Promise<void>; /** Non-exclusive: resolves the next time count reaches zero; does not block new `retain()` calls. */ whenZeroOnce(key: TKey): Promise<void>; /** Blocks further `retain()` calls for `key`. Idempotent. */ seal(key: TKey): void; isSealed(key: TKey): boolean; /** Removes a zero-count key and returns whether a state entry was removed. */ forget(key: TKey): boolean; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IQuiescenceTracker when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IQuiescenceTracker does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "IQuiescenceTracker is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IQuiescenceTracker from @migaia/lifecycle/quiescence, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IQuiescenceTracker when its type contract and declared inputs match the result you need. Choose a different public symbol when IQuiescenceTracker does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IQuiescenceTracker."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IQuiescenceTracker."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/quiescence-tracker.d.ts:IQuiescenceTracker:export type IQuiescenceTracker<TKey> = { retain(key: TKey): IDisposer; count(key: TKey): number; /** Strict, exclusive drain: throws synchronously (not a rejected Promise) unless `isSealed(key)`. */ whenZero(key: TKey): Promise<void>; /** Non-exclusive: resolves the next time count reaches zero; does not block new `retain()` calls. */ whenZeroOnce(key: TKey): Promise<void>; /** Blocks further `retain()` calls for `key`. Idempotent. */ seal(key: TKey): void; isSealed(key: TKey): boolean; /** Removes a zero-count key and returns whether a state entry was removed. */ forget(key: TKey): boolean; };",
          "guidance": [],
          "usageScore": 44,
          "configuration": [],
          "fragment": "lifecycle-quiescence--IQuiescenceTracker"
        }
      ],
      "aliases": []
    },
    {
      "id": "lifecycle:scheduler",
      "library": "lifecycle",
      "module": "scheduler",
      "exportPath": "./scheduler",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "addSchedulerTime",
          "kind": "function",
          "signature": "export declare function addSchedulerTime(base: number, delta: number, label: string): number;",
          "declarationLine": 6,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Rejects a scheduler time target when finite operands overflow during addition.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for addSchedulerTime.",
          "advanced": "No additional advanced behavior is declared for addSchedulerTime.",
          "parameters": ["base: number", "delta: number", "label: string"],
          "parameterDetails": [
            {
              "name": "base",
              "type": "number",
              "optional": false
            },
            {
              "name": "delta",
              "type": "number",
              "optional": false
            },
            {
              "name": "label",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "number",
          "errors": ["No documented errors are declared for addSchedulerTime."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for addSchedulerTime.",
          "examples": [
            "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 结果溢出为 Infinity 时抛 RangeError",
            "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 两个有限数相加；溢出 Infinity 时抛 RangeError"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export declare function addSchedulerTime(base: number, delta: number, label: string): number;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "base",
                "type": "number",
                "optional": false
              },
              {
                "name": "delta",
                "type": "number",
                "optional": false
              },
              {
                "name": "label",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["number"],
          "whenToUse": "Use addSchedulerTime when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when addSchedulerTime does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Rejects a scheduler time target when finite operands overflow during addition."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import addSchedulerTime from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use addSchedulerTime when its function contract and declared inputs match the result you need. Choose a different public symbol when addSchedulerTime does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for addSchedulerTime."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for addSchedulerTime."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:addSchedulerTime:export declare function addSchedulerTime(base: number, delta: number, label: string): number;",
          "guidance": [],
          "usageScore": 60,
          "configuration": [],
          "fragment": "lifecycle-scheduler--addSchedulerTime"
        },
        {
          "name": "createManualScheduler",
          "kind": "function",
          "signature": "export declare function createManualScheduler(): IManualScheduler;",
          "declarationLine": 52,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "创建手动时钟调度器（测试/benchmark 专用，不依赖真实时间）。 `now()` 返回虚拟时钟；`schedule()` 只登记不触发；`advance(ms)` 把虚拟时间推进 `ms` 并同步执行所有到期回调（按到期时刻升序，同时刻按登记顺序）。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createManualScheduler.",
          "advanced": "No additional advanced behavior is declared for createManualScheduler.",
          "parameters": ["This function declaration has no parameters."],
          "parameterDetails": [],
          "returns": "IManualScheduler",
          "errors": ["No documented errors are declared for createManualScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createManualScheduler.",
          "examples": [
            "const scheduler = createManualScheduler()\nconst task = scheduler.schedule(() => console.log('fired'), 100)\nscheduler.advance(100) // 一次性 flush 所有到期回调（含到期回调内部再排的到期任务）",
            "import {\n  createDisposeTransaction,\n  createManualScheduler,\n  type IDisposeItem\n} from '@migaia/lifecycle'\n\nconst scheduler = createManualScheduler()\nconst items: IDisposeItem[] = [\n  {\n    source: 'socket',\n    descriptor: {\n      order: 10,\n      graceful: (ctx) => flushSocket(),\n      gracefulTimeoutMs: 100,\n      force: () => forceCloseSocket()\n    }\n  },\n  { source: 'db', descriptor: { order: 5, force: () => closeDb() } }\n]\nconst transaction = createDisposeTransaction(\n  { kind: 'order' },\n  { errorPolicy: 'collect', scheduler }\n)\nconst runPromise = transaction.run(items)\nscheduler.advance(100) // 驱动 socket 的 graceful 超时降级到 force\nconst failures = await runPromise"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export declare function createManualScheduler(): IManualScheduler;"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": ["IManualScheduler"],
          "whenToUse": "Use createManualScheduler when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createManualScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "创建手动时钟调度器（测试/benchmark 专用，不依赖真实时间）。 `now()` 返回虚拟时钟；`schedule()` 只登记不触发；`advance(ms)` 把虚拟时间推进 `ms` 并同步执行所有到期回调（按到期时刻升序，同时刻按登记顺序）。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createManualScheduler from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createManualScheduler when its function contract and declared inputs match the result you need. Choose a different public symbol when createManualScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createManualScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createManualScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:createManualScheduler:export declare function createManualScheduler(): IManualScheduler;",
          "guidance": [
            {
              "id": "createmanualscheduler",
              "heading": "createManualScheduler",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createManualScheduler(): IManualScheduler\n// type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void };"
                },
                {
                  "type": "paragraph",
                  "text": "单测里把时间变成确定性的虚拟时钟："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scheduler = createManualScheduler()\nconst task = scheduler.schedule(() => console.log('fired'), 100)\nscheduler.advance(100) // 一次性 flush 所有到期回调（含到期回调内部再排的到期任务）"
                },
                {
                  "type": "paragraph",
                  "text": "无入参。`advance(ms)` 按到期时刻升序（同刻按登记顺序）依次执行所有到期回调，单次循环超过 10000 个 flush 任务会抛 `INVALID_OPTION`（runaway guard）；`ms`/`schedule()` 的 `delayMs` 必须是有限非负数，否则抛 `INVALID_OPTION`（`RangeError`/`TypeError` 原生类型，贴该码）。"
                }
              ]
            }
          ],
          "usageScore": 90,
          "configuration": [],
          "fragment": "lifecycle-scheduler--createManualScheduler"
        },
        {
          "name": "ILifecycleScheduler",
          "kind": "type",
          "signature": "export type ILifecycleScheduler = { /** 单调不递减毫秒（连续两次可相等）；只比较差值，不解释为 Unix epoch。 */ now(): number; /** 排程一个回调；`delayMs` 有限、非负；回调至多执行一次。 */ schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "declarationLine": 12,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Runtime-neutral 调度器契约：单调时钟 + 有界延迟排程。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleScheduler.",
          "advanced": "No additional advanced behavior is declared for ILifecycleScheduler.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleScheduler.",
          "examples": [
            "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}",
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}",
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}",
            "function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue\n\ntype IMutationQueueOptions = {\n  readonly queueAdmissionTimeoutMs?: number | false // 默认 undefined（只诊断，从不因排队超时而拒绝）\n  readonly admissionDiagnosticMs?: number | false // 默认 1000\n  readonly onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IEnqueueOptions = {\n  readonly owner?: string\n  readonly queueAdmissionTimeoutMs?: number | false // 覆盖本次任务的队列默认值\n}\n\ntype IMutationQueue = {\n  enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>\n  readonly size: number\n}",
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "const systemScheduler: ILifecycleScheduler // { now(): number; schedule(cb, delayMs): IScheduledTask }",
            "function createManualScheduler(): IManualScheduler\n// type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void };",
            "function boundedWait(\n  task: PromiseLike<unknown>,\n  deadlineAt: number,\n  options?: { readonly scheduler?: ILifecycleScheduler } // 默认 systemScheduler\n): Promise<boolean>"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export type ILifecycleScheduler = { /** 单调不递减毫秒（连续两次可相等）；只比较差值，不解释为 Unix epoch。 */ now(): number; /** 排程一个回调；`delayMs` 有限、非负；回调至多执行一次。 */ schedule(callback: () => void, delayMs: number): IScheduledTask; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleScheduler when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Runtime-neutral 调度器契约：单调时钟 + 有界延迟排程。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleScheduler from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleScheduler when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:ILifecycleScheduler:export type ILifecycleScheduler = { /** 单调不递减毫秒（连续两次可相等）；只比较差值，不解释为 Unix epoch。 */ now(): number; /** 排程一个回调；`delayMs` 有限、非负；回调至多执行一次。 */ schedule(callback: () => void, delayMs: number): IScheduledTask; };",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 110,
          "configuration": [],
          "fragment": "lifecycle-scheduler--ILifecycleScheduler"
        },
        {
          "name": "IManualScheduler",
          "kind": "type",
          "signature": "export type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void; };",
          "declarationLine": 44,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "手动时钟调度器：测试唯一替代实现，`advance(ms)` 推进虚拟时间并同步 flush 到期回调。非生产 API。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IManualScheduler.",
          "advanced": "No additional advanced behavior is declared for IManualScheduler.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IManualScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IManualScheduler.",
          "examples": [
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "function createManualScheduler(): IManualScheduler\n// type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void };"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IManualScheduler when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IManualScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "手动时钟调度器：测试唯一替代实现，`advance(ms)` 推进虚拟时间并同步 flush 到期回调。非生产 API。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IManualScheduler from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IManualScheduler when its type contract and declared inputs match the result you need. Choose a different public symbol when IManualScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IManualScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IManualScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:IManualScheduler:export type IManualScheduler = ILifecycleScheduler & { advance(ms: number): void; };",
          "guidance": [],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-scheduler--IManualScheduler"
        },
        {
          "name": "IScheduledTask",
          "kind": "type",
          "signature": "export type IScheduledTask = { cancel(): void; };",
          "declarationLine": 8,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "一个已排程任务的可取消句柄。`cancel()` 幂等；cancel 后回调不再执行。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IScheduledTask.",
          "advanced": "No additional advanced behavior is declared for IScheduledTask.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for IScheduledTask."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for IScheduledTask.",
          "examples": [
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "const systemScheduler: ILifecycleScheduler // { now(): number; schedule(cb, delayMs): IScheduledTask }"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": ["export type IScheduledTask = { cancel(): void; };"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use IScheduledTask when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when IScheduledTask does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "一个已排程任务的可取消句柄。`cancel()` 幂等；cancel 后回调不再执行。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import IScheduledTask from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use IScheduledTask when its type contract and declared inputs match the result you need. Choose a different public symbol when IScheduledTask does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for IScheduledTask."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for IScheduledTask."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:IScheduledTask:export type IScheduledTask = { cancel(): void; };",
          "guidance": [],
          "usageScore": 24,
          "configuration": [],
          "fragment": "lifecycle-scheduler--IScheduledTask"
        },
        {
          "name": "ISchedulerSnapshot",
          "kind": "type",
          "signature": "export type ISchedulerSnapshot = ILifecycleScheduler;",
          "declarationLine": 19,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Captured scheduler contract with stable methods and receiver.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISchedulerSnapshot.",
          "advanced": "No additional advanced behavior is declared for ISchedulerSnapshot.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ISchedulerSnapshot."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ISchedulerSnapshot.",
          "examples": [
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined",
            "function resolveScheduler(value: unknown): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined,\n  fallback: ISchedulerSnapshot\n): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined\n): ISchedulerSnapshot | undefined"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": ["export type ISchedulerSnapshot = ILifecycleScheduler;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ISchedulerSnapshot when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ISchedulerSnapshot does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Captured scheduler contract with stable methods and receiver."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ISchedulerSnapshot from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ISchedulerSnapshot when its type contract and declared inputs match the result you need. Choose a different public symbol when ISchedulerSnapshot does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ISchedulerSnapshot."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ISchedulerSnapshot."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:ISchedulerSnapshot:export type ISchedulerSnapshot = ILifecycleScheduler;",
          "guidance": [
            {
              "id": "resolvescheduler-resolvescheduleroption",
              "heading": "resolveScheduler / resolveSchedulerOption",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function resolveScheduler(value: unknown): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined,\n  fallback: ISchedulerSnapshot\n): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined\n): ISchedulerSnapshot | undefined"
                },
                {
                  "type": "paragraph",
                  "text": "内部/自定义 scheduler 实现复用的边界解析函数：`resolveScheduler(value)` 对非法值抛 `SCHEDULER_INVALID` 文案的 `INVALID_OPTION` 错误（而不是像 `snapshotScheduler` 那样返回 `undefined`）；`resolveSchedulerOption(options, fallback?)` 读取 `options.scheduler`，未提供时返回 `fallback`（若调用形态未传 `fallback` 则返回 `undefined`），读取该属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
                }
              ]
            }
          ],
          "usageScore": 46,
          "configuration": [],
          "fragment": "lifecycle-scheduler--ISchedulerSnapshot"
        },
        {
          "name": "resolveScheduler",
          "kind": "function",
          "signature": "export declare function resolveScheduler(value: unknown): ISchedulerSnapshot;",
          "declarationLine": 35,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Resolve an injected scheduler at its factory boundary, retaining lifecycle error identity.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for resolveScheduler.",
          "advanced": "No additional advanced behavior is declared for resolveScheduler.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "ISchedulerSnapshot",
          "errors": ["No documented errors are declared for resolveScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for resolveScheduler.",
          "examples": [],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export declare function resolveScheduler(value: unknown): ISchedulerSnapshot;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["ISchedulerSnapshot"],
          "whenToUse": "Use resolveScheduler when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when resolveScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Resolve an injected scheduler at its factory boundary, retaining lifecycle error identity."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import resolveScheduler from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use resolveScheduler when its function contract and declared inputs match the result you need. Choose a different public symbol when resolveScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for resolveScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for resolveScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:resolveScheduler:export declare function resolveScheduler(value: unknown): ISchedulerSnapshot;",
          "guidance": [
            {
              "id": "resolvescheduler-resolvescheduleroption",
              "heading": "resolveScheduler / resolveSchedulerOption",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function resolveScheduler(value: unknown): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined,\n  fallback: ISchedulerSnapshot\n): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined\n): ISchedulerSnapshot | undefined"
                },
                {
                  "type": "paragraph",
                  "text": "内部/自定义 scheduler 实现复用的边界解析函数：`resolveScheduler(value)` 对非法值抛 `SCHEDULER_INVALID` 文案的 `INVALID_OPTION` 错误（而不是像 `snapshotScheduler` 那样返回 `undefined`）；`resolveSchedulerOption(options, fallback?)` 读取 `options.scheduler`，未提供时返回 `fallback`（若调用形态未传 `fallback` 则返回 `undefined`），读取该属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
                }
              ]
            }
          ],
          "usageScore": 44,
          "configuration": [],
          "fragment": "lifecycle-scheduler--resolveScheduler"
        },
        {
          "name": "resolveSchedulerOption",
          "kind": "function",
          "signature": "export declare function resolveSchedulerOption(options: { readonly scheduler?: unknown; } | null | undefined, fallback: ISchedulerSnapshot): ISchedulerSnapshot;\nexport declare function resolveSchedulerOption(options: { readonly scheduler?: unknown; } | null | undefined): ISchedulerSnapshot | undefined;",
          "declarationLine": 27,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Reads one public scheduler option and snapshots its scheduler methods before a lifecycle object is created. A getter failure is an invalid-option boundary failure; the native `TypeError` preserves the exact thrown value through `cause` and prevents scheduler work from starting. Reads one optional public scheduler option without inventing a default scheduler.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for resolveSchedulerOption.",
          "advanced": "No additional advanced behavior is declared for resolveSchedulerOption.",
          "parameters": [
            "options: { readonly scheduler?: unknown; } | null | undefined",
            "fallback: ISchedulerSnapshot"
          ],
          "parameterDetails": [
            {
              "name": "options",
              "type": "{ readonly scheduler?: unknown; } | null | undefined",
              "optional": false
            },
            {
              "name": "fallback",
              "type": "ISchedulerSnapshot",
              "optional": false
            }
          ],
          "returns": "ISchedulerSnapshot | ISchedulerSnapshot | undefined",
          "errors": ["No documented errors are declared for resolveSchedulerOption."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for resolveSchedulerOption.",
          "examples": [],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export declare function resolveSchedulerOption(options: { readonly scheduler?: unknown; } | null | undefined, fallback: ISchedulerSnapshot): ISchedulerSnapshot;",
            "export declare function resolveSchedulerOption(options: { readonly scheduler?: unknown; } | null | undefined): ISchedulerSnapshot | undefined;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "{ readonly scheduler?: unknown; } | null | undefined",
                "optional": false
              },
              {
                "name": "fallback",
                "type": "ISchedulerSnapshot",
                "optional": false
              }
            ],
            [
              {
                "name": "options",
                "type": "{ readonly scheduler?: unknown; } | null | undefined",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["ISchedulerSnapshot", "ISchedulerSnapshot | undefined"],
          "whenToUse": "Use resolveSchedulerOption when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when resolveSchedulerOption does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Reads one public scheduler option and snapshots its scheduler methods before a lifecycle object is created. A getter failure is an invalid-option boundary failure; the native `TypeError` preserves the exact thrown value through `cause` and prevents scheduler work from starting. Reads one optional public scheduler option without inventing a default scheduler."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import resolveSchedulerOption from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use resolveSchedulerOption when its function contract and declared inputs match the result you need. Choose a different public symbol when resolveSchedulerOption does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for resolveSchedulerOption."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for resolveSchedulerOption."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:resolveSchedulerOption:export declare function resolveSchedulerOption(options: { readonly scheduler?: unknown; } | null | undefined, fallback: ISchedulerSnapshot): ISchedulerSnapshot;\nexport declare function resolveSchedulerOption(options: { readonly scheduler?: unknown; } | null | undefined): ISchedulerSnapshot | undefined;",
          "guidance": [
            {
              "id": "resolvescheduler-resolvescheduleroption",
              "heading": "resolveScheduler / resolveSchedulerOption",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function resolveScheduler(value: unknown): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined,\n  fallback: ISchedulerSnapshot\n): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined\n): ISchedulerSnapshot | undefined"
                },
                {
                  "type": "paragraph",
                  "text": "内部/自定义 scheduler 实现复用的边界解析函数：`resolveScheduler(value)` 对非法值抛 `SCHEDULER_INVALID` 文案的 `INVALID_OPTION` 错误（而不是像 `snapshotScheduler` 那样返回 `undefined`）；`resolveSchedulerOption(options, fallback?)` 读取 `options.scheduler`，未提供时返回 `fallback`（若调用形态未传 `fallback` 则返回 `undefined`），读取该属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
                }
              ]
            }
          ],
          "usageScore": 48,
          "configuration": [],
          "fragment": "lifecycle-scheduler--resolveSchedulerOption"
        },
        {
          "name": "snapshotScheduler",
          "kind": "function",
          "signature": "export declare function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined;",
          "declarationLine": 21,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Read scheduler accessors once while preserving their original receiver.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for snapshotScheduler.",
          "advanced": "No additional advanced behavior is declared for snapshotScheduler.",
          "parameters": ["value: unknown"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            }
          ],
          "returns": "ISchedulerSnapshot | undefined",
          "errors": ["No documented errors are declared for snapshotScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for snapshotScheduler.",
          "examples": [
            "const snap = snapshotScheduler(candidate) // 不是合法 scheduler 时返回 undefined，而不是抛错"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export declare function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["ISchedulerSnapshot | undefined"],
          "whenToUse": "Use snapshotScheduler when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when snapshotScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Read scheduler accessors once while preserving their original receiver."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import snapshotScheduler from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use snapshotScheduler when its function contract and declared inputs match the result you need. Choose a different public symbol when snapshotScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for snapshotScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for snapshotScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:snapshotScheduler:export declare function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined;",
          "guidance": [
            {
              "id": "snapshotscheduler",
              "heading": "snapshotScheduler",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function snapshotScheduler(value: unknown): ISchedulerSnapshot | undefined"
                },
                {
                  "type": "paragraph",
                  "text": "把任意 duck-typed 值快照成标准 `ILifecycleScheduler`（校验并锁定 accessor，防止 hostile getter 二次读取）："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const snap = snapshotScheduler(candidate) // 不是合法 scheduler 时返回 undefined，而不是抛错"
                },
                {
                  "type": "paragraph",
                  "text": "单参数 `value: unknown`，无选项；`now`/`schedule` 都不是函数时返回 `undefined`；读取 `now`/`schedule` 属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
                }
              ]
            },
            {
              "id": "resolvescheduler-resolvescheduleroption",
              "heading": "resolveScheduler / resolveSchedulerOption",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function resolveScheduler(value: unknown): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined,\n  fallback: ISchedulerSnapshot\n): ISchedulerSnapshot\nfunction resolveSchedulerOption(\n  options: { readonly scheduler?: unknown } | null | undefined\n): ISchedulerSnapshot | undefined"
                },
                {
                  "type": "paragraph",
                  "text": "内部/自定义 scheduler 实现复用的边界解析函数：`resolveScheduler(value)` 对非法值抛 `SCHEDULER_INVALID` 文案的 `INVALID_OPTION` 错误（而不是像 `snapshotScheduler` 那样返回 `undefined`）；`resolveSchedulerOption(options, fallback?)` 读取 `options.scheduler`，未提供时返回 `fallback`（若调用形态未传 `fallback` 则返回 `undefined`），读取该属性本身抛错时抛 `TypeError`（`INVALID_OPTION`）。"
                }
              ]
            }
          ],
          "usageScore": 70,
          "configuration": [],
          "fragment": "lifecycle-scheduler--snapshotScheduler"
        },
        {
          "name": "systemScheduler",
          "kind": "const",
          "signature": "export declare const systemScheduler: ILifecycleScheduler;",
          "declarationLine": 42,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Lifecycle 提供的默认调度器：统一 `performance.now()`（单调）做时钟、`setTimeout`/`clearTimeout` 做排程。 纯常量对象，无全局 setter、无可变 singleton；模块加载零副作用，只有 `schedule()` 才创建 timer。能力检测在首次调用 `now()`/`schedule()` 时惰性 fail-fast：宿主能力缺失抛 `ENV_UNSUPPORTED`，不静默退化。",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for systemScheduler.",
          "advanced": "No additional advanced behavior is declared for systemScheduler.",
          "parameters": ["This const declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this const.",
          "errors": ["No documented errors are declared for systemScheduler."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for systemScheduler.",
          "examples": [
            "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}",
            "function createDisposeTransaction(\n  mode: { readonly kind: 'order' } | { readonly kind: 'plan' },\n  options?: IDisposeTransactionOptions\n): IDisposeTransaction\n\ntype IDisposeItem = { readonly source: string; readonly descriptor: IReleaseDescriptor }\n\ntype IDisposeTransactionOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n  readonly signal?: IAbortSignal\n  readonly pending?: { drain(): Promise<void> }\n}\n\ntype IDisposeTransaction = {\n  readonly mode: { readonly kind: 'order' } | { readonly kind: 'plan' }\n  run(items: readonly IDisposeItem[]): Promise<readonly ICollectedError[]>\n}",
            "function createGenerationController(options?: IGenerationControllerOptions): IGenerationController\n\ntype IGenerationControllerOptions = {\n  readonly parentSignal?: IAbortSignal\n  readonly onSuperseded?: (info: ILifecycleError) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IGenerationToken = object\n\ntype IGenerationRequest = {\n  readonly generation: number\n  readonly token: IGenerationToken\n  readonly signal: IAbortSignal\n}\n\ntype IGenerationController = {\n  readonly generation: number\n  readonly disposed: boolean\n  begin(options?: { readonly timeoutMs?: number }): IGenerationRequest\n  isCurrent(token: IGenerationToken): boolean\n  supersede(reason?: unknown): void\n  adopt<T>(\n    token: IGenerationToken,\n    value: T,\n    release: (value: T) => void | PromiseLike<void>,\n    onReleaseError?: (error: unknown) => void\n  ): boolean\n  dispose(reason?: unknown): void\n}",
            "function createMutationQueue(options?: IMutationQueueOptions): IMutationQueue\n\ntype IMutationQueueOptions = {\n  readonly queueAdmissionTimeoutMs?: number | false // 默认 undefined（只诊断，从不因排队超时而拒绝）\n  readonly admissionDiagnosticMs?: number | false // 默认 1000\n  readonly onAdmissionDiagnostic?: (info: { owner: string | undefined; waitedMs: number }) => void\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype IEnqueueOptions = {\n  readonly owner?: string\n  readonly queueAdmissionTimeoutMs?: number | false // 覆盖本次任务的队列默认值\n}\n\ntype IMutationQueue = {\n  enqueue<T>(task: () => T | PromiseLike<T>, options?: IEnqueueOptions): Promise<T>\n  readonly size: number\n}",
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController,\n  type ILifecycleScheduler,\n  type IScheduledTask,\n  type ISchedulerSnapshot,\n  type IManualScheduler,\n  type IAbortSignal,\n  type IAbortController,\n  type ITerminalController\n} from '@migaia/lifecycle'\n\n// 仅 leaf entry 导出的 scheduler 组合工具：\nimport {\n  addSchedulerTime,\n  resolveScheduler,\n  resolveSchedulerOption\n} from '@migaia/lifecycle/scheduler'",
            "const systemScheduler: ILifecycleScheduler // { now(): number; schedule(cb, delayMs): IScheduledTask }",
            "systemScheduler.now() // 单调递增毫秒数",
            "function boundedWait(\n  task: PromiseLike<unknown>,\n  deadlineAt: number,\n  options?: { readonly scheduler?: ILifecycleScheduler } // 默认 systemScheduler\n): Promise<boolean>",
            "import {\n  systemScheduler,\n  createManualScheduler,\n  snapshotScheduler,\n  validateSchedulerDelay,\n  validateSchedulerTime,\n  createAbortController,\n  boundedWait,\n  createTerminalController\n} from '@migaia/lifecycle'"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": ["export declare const systemScheduler: ILifecycleScheduler;"],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use systemScheduler when its const contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when systemScheduler does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Lifecycle 提供的默认调度器：统一 `performance.now()`（单调）做时钟、`setTimeout`/`clearTimeout` 做排程。 纯常量对象，无全局 setter、无可变 singleton；模块加载零副作用，只有 `schedule()` 才创建 timer。能力检测在首次调用 `now()`/`schedule()` 时惰性 fail-fast：宿主能力缺失抛 `ENV_UNSUPPORTED`，不静默退化。"
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import systemScheduler from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use systemScheduler when its const contract and declared inputs match the result you need. Choose a different public symbol when systemScheduler does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained const example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for systemScheduler."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for systemScheduler."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:systemScheduler:export declare const systemScheduler: ILifecycleScheduler;",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            },
            {
              "id": "systemscheduler",
              "heading": "systemScheduler",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const systemScheduler: ILifecycleScheduler // { now(): number; schedule(cb, delayMs): IScheduledTask }"
                },
                {
                  "type": "paragraph",
                  "text": "默认调度器，`now()` 用 `performance.now()`、`schedule()` 用 `setTimeout`/`clearTimeout`，一般不用手动传，除非要换成 `createManualScheduler()`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "systemScheduler.now() // 单调递增毫秒数"
                },
                {
                  "type": "paragraph",
                  "text": "无配置，纯常量对象；宿主缺 `performance.now`/`setTimeout`/`clearTimeout` 时，首次调用对应方法才 fail-fast 抛 `ENV_UNSUPPORTED`。"
                }
              ]
            },
            {
              "id": "错误码",
              "heading": "错误码",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "import { LifecycleErrorCode, type ILifecycleErrorCode } from '@migaia/lifecycle'"
                },
                {
                  "type": "paragraph",
                  "text": "稳定错误码表，20 个码，唯一声明处 `src/error-code.ts`，`source` 恒为 `'@migaia/lifecycle'`："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "if (error.code === LifecycleErrorCode.scopeClosed) {\n  /* ... */\n}"
                },
                {
                  "type": "table",
                  "headers": ["`LifecycleErrorCode` 键", "码值", "触发条件"],
                  "rows": [
                    [
                      "`scopeClosed`",
                      "`SCOPE_CLOSED`",
                      "`close()` 之后调用 `own()`（异步/同步 scope）或某个 registry 的 `retain()`"
                    ],
                    [
                      "`scopeTerminal`",
                      "`SCOPE_TERMINAL`",
                      "容器已到达 `terminal` 之后，任何登记/激活操作"
                    ],
                    [
                      "`scopeReentrantDispose`",
                      "`SCOPE_REENTRANT_DISPOSE`",
                      "disposer 内部重入本 scope 的 `dispose()`"
                    ],
                    [
                      "`scopeReentrantOwn`",
                      "`SCOPE_REENTRANT_OWN`",
                      "disposer 内部调用本 scope 的 `own()`"
                    ],
                    [
                      "`scopeSyncViolation`",
                      "`SCOPE_SYNC_VIOLATION`",
                      "`SyncLifecycleScope` 收到 `syncSafe !== true` 的 descriptor、收到一个异步 scope 实例作为资源、或某回调返回了 thenable"
                    ],
                    [
                      "`scopeDisposalFailed`",
                      "`SCOPE_DISPOSAL_FAILED`",
                      "`throw` 错误策略在多个资源释放失败时的 `AggregateError` 聚合出口"
                    ],
                    [
                      "`unitStartFailed`",
                      "`UNIT_START_FAILED`",
                      "`LifecycleUnit.start()` 返回的 thenable 被 reject（单元进入 `failed`）"
                    ],
                    [
                      "`generationSuperseded`",
                      "`GENERATION_SUPERSEDED`",
                      "一次异步操作的结果在 `adopt()` 提交点已被更晚的 generation 取代（非失败信号，仅诊断）"
                    ],
                    [
                      "`generationCancellationFailed`",
                      "`GENERATION_CANCELLATION_FAILED`",
                      "generation 取消期间的 timer、parent listener 或 signal cleanup 失败"
                    ],
                    [
                      "`generationDisposed`",
                      "`GENERATION_DISPOSED`",
                      "在已 `dispose()` 的 `GenerationController` 上调用 `begin()`"
                    ],
                    ["`quiescenceSealed`", "`QUIESCENCE_SEALED`", "`seal()` 之后又调用 `retain()`"],
                    [
                      "`quiescenceUnsealedWait`",
                      "`QUIESCENCE_UNSEALED_WAIT`",
                      "未先 `seal(key)` 就调用严格 `whenZero(key)`（同步抛出，非 rejected Promise）"
                    ],
                    [
                      "`provisionalSettled`",
                      "`PROVISIONAL_SETTLED`",
                      "同一个 `ProvisionalScope` 二次 `commitTo()`/`rollback()`，或交叉调用"
                    ],
                    [
                      "`provisionalParentClosed`",
                      "`PROVISIONAL_PARENT_CLOSED`",
                      "`commitTo()` 的目标 parent 已 `closing`/`terminal`，拒绝接收资源"
                    ],
                    [
                      "`queueAdmissionTimeout`",
                      "`QUEUE_ADMISSION_TIMEOUT`",
                      "mutation 在队列中等待超过配置的 `queueAdmissionTimeoutMs`"
                    ],
                    [
                      "`queueSelfDependency`",
                      "`QUEUE_SELF_DEPENDENCY`",
                      "同一 `owner` 在自己尚未完成时又向同一队列提交新任务且被等待，会死锁"
                    ],
                    [
                      "`releaseForceFailed`",
                      "`RELEASE_FORCE_FAILED`",
                      "descriptor 的 `force` 抛出/reject（契约要求 `force` 必须无条件成功）"
                    ],
                    [
                      "`deadlineExceeded`",
                      "`DEADLINE_EXCEEDED`",
                      "共享绝对 deadline 已过去，但仍有资源被要求进入新的 graceful 阶段（诊断码，正常降级路径不抛这个码）"
                    ],
                    [
                      "`envUnsupported`",
                      "`ENV_UNSUPPORTED`",
                      "`systemScheduler` 依赖的宿主能力（`performance.now`/`setTimeout`/`clearTimeout`）缺失"
                    ],
                    [
                      "`invalidOption`",
                      "`INVALID_OPTION`",
                      "scheduler 收到非法时间/延迟参数，或 descriptor 字段类型非法"
                    ]
                  ]
                },
                {
                  "type": "paragraph",
                  "text": "逐条设计动机见源码 `src/error-code.ts` 的 JSDoc；调用方应始终以 `error.code === LifecycleErrorCode.xxx` 判别，不要硬编码码值字符串。"
                }
              ]
            }
          ],
          "usageScore": 130,
          "configuration": [],
          "fragment": "lifecycle-scheduler--systemScheduler"
        },
        {
          "name": "validateSchedulerDelay",
          "kind": "function",
          "signature": "export declare function validateSchedulerDelay(value: unknown, label?: string): number;",
          "declarationLine": 4,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Canonical runtime check for finite, non-negative scheduler delays.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for validateSchedulerDelay.",
          "advanced": "No additional advanced behavior is declared for validateSchedulerDelay.",
          "parameters": ["value: unknown", "label: string (optional)"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "label",
              "type": "string",
              "optional": true
            }
          ],
          "returns": "number",
          "errors": ["No documented errors are declared for validateSchedulerDelay."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for validateSchedulerDelay.",
          "examples": [
            "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 结果溢出为 Infinity 时抛 RangeError",
            "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 两个有限数相加；溢出 Infinity 时抛 RangeError"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export declare function validateSchedulerDelay(value: unknown, label?: string): number;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "label",
                "type": "string",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["number"],
          "whenToUse": "Use validateSchedulerDelay when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when validateSchedulerDelay does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Canonical runtime check for finite, non-negative scheduler delays."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import validateSchedulerDelay from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use validateSchedulerDelay when its function contract and declared inputs match the result you need. Choose a different public symbol when validateSchedulerDelay does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for validateSchedulerDelay."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for validateSchedulerDelay."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:validateSchedulerDelay:export declare function validateSchedulerDelay(value: unknown, label?: string): number;",
          "guidance": [],
          "usageScore": 64,
          "configuration": [],
          "fragment": "lifecycle-scheduler--validateSchedulerDelay"
        },
        {
          "name": "validateSchedulerTime",
          "kind": "function",
          "signature": "export declare function validateSchedulerTime(value: unknown, label: string): number;",
          "declarationLine": 2,
          "source": "packages/lifecycle/dist/scheduler.d.ts",
          "purpose": "Canonical runtime check for scheduler-produced and scheduler-consumed numeric values.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for validateSchedulerTime.",
          "advanced": "No additional advanced behavior is declared for validateSchedulerTime.",
          "parameters": ["value: unknown", "label: string"],
          "parameterDetails": [
            {
              "name": "value",
              "type": "unknown",
              "optional": false
            },
            {
              "name": "label",
              "type": "string",
              "optional": false
            }
          ],
          "returns": "number",
          "errors": ["No documented errors are declared for validateSchedulerTime."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for validateSchedulerTime.",
          "examples": [
            "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 结果溢出为 Infinity 时抛 RangeError",
            "validateSchedulerTime(value, 'deadlineAt') // 非 number 抛 TypeError，非有限抛 RangeError\nvalidateSchedulerDelay(value) // 在上面基础上还要求 >= 0，否则抛 RangeError\naddSchedulerTime(now, delayMs, 'deadlineAt') // 两个有限数相加；溢出 Infinity 时抛 RangeError"
          ],
          "exportPath": "./scheduler",
          "overloadSignatures": [
            "export declare function validateSchedulerTime(value: unknown, label: string): number;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "value",
                "type": "unknown",
                "optional": false
              },
              {
                "name": "label",
                "type": "string",
                "optional": false
              }
            ]
          ],
          "overloadReturns": ["number"],
          "whenToUse": "Use validateSchedulerTime when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when validateSchedulerTime does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "Canonical runtime check for scheduler-produced and scheduler-consumed numeric values."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import validateSchedulerTime from @migaia/lifecycle/scheduler, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use validateSchedulerTime when its function contract and declared inputs match the result you need. Choose a different public symbol when validateSchedulerTime does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for validateSchedulerTime."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for validateSchedulerTime."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/scheduler.d.ts:validateSchedulerTime:export declare function validateSchedulerTime(value: unknown, label: string): number;",
          "guidance": [],
          "usageScore": 68,
          "configuration": [],
          "fragment": "lifecycle-scheduler--validateSchedulerTime"
        }
      ],
      "aliases": []
    },
    {
      "id": "lifecycle:scope",
      "library": "lifecycle",
      "module": "scope",
      "exportPath": "./scope",
      "sections": [
        "introduction",
        "getting-started",
        "when-to-use",
        "quick-implementation",
        "core-usage",
        "advanced-usage"
      ],
      "guidePath": "/en/guides/lifecycle/getting-started",
      "architecturePath": "/en/architecture/lifecycle",
      "symbols": [
        {
          "name": "createLifecycleScope",
          "kind": "function",
          "signature": "export declare function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope;",
          "declarationLine": 36,
          "source": "packages/lifecycle/dist/lifecycle-scope.d.ts",
          "purpose": "The general-purpose, async-only scope. Ported from `@migaia/reactive`'s `LifecycleScopeImpl` baseline (sync/async reentrancy guard, atomic splice+reverse teardown) with the sync path removed per D-1 and error handling generalized to the four §4.5 policies.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleScope.",
          "advanced": "No additional advanced behavior is declared for createLifecycleScope.",
          "parameters": ["options: ILifecycleScopeOptions (optional)"],
          "parameterDetails": [
            {
              "name": "options",
              "type": "ILifecycleScopeOptions",
              "optional": true
            }
          ],
          "returns": "ILifecycleScope",
          "errors": ["No documented errors are declared for createLifecycleScope."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for createLifecycleScope.",
          "examples": [
            "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛",
            "import { createLifecycleScope, createGenerationController } from '@migaia/lifecycle'\n\nconst scope = createLifecycleScope({ errorPolicy: 'collect' })\nconst generations = createGenerationController()\n\nasync function loadUser(id: string) {\n  const request = generations.begin({ timeoutMs: 5000 })\n  const user = await fetchUser(id, { signal: request.signal })\n  if (!generations.adopt(request.token, user, (u) => u.dispose?.())) return // 已过期，静默丢弃\n  scope.own(user, { force: () => user.dispose?.() })\n}",
            "import { createLifecycleScope, createProvisionalScope } from '@migaia/lifecycle'\n\nconst rootScope = createLifecycleScope()\n\nasync function setupModule() {\n  const provisional = createProvisionalScope()\n  try {\n    const db = provisional.own(await connectDb(), { force: (ctx) => closeDb(db) })\n    const cache = provisional.own(await connectCache(), { force: (ctx) => closeCache(cache) })\n    await provisional.commitTo(rootScope) // 全部转移给 rootScope，之后随 rootScope.dispose() 释放\n  } catch (error) {\n    await provisional.rollback() // 任一步失败：已注册的资源逆序释放，冒泡原错误\n    throw error\n  }\n}",
            "import { createLifecycleScope, createPendingTracker } from '@migaia/lifecycle'\n\nconst scope = createLifecycleScope({ errorPolicy: 'collect' })\nconst pending = createPendingTracker()\n\nfunction handle(request: Request): Promise<Response> {\n  return pending.track(route(request))\n}\n\nasync function shutdown() {\n  await pending.drain()\n  return scope.dispose()\n}"
          ],
          "exportPath": "./scope",
          "overloadSignatures": [
            "export declare function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope;"
          ],
          "overloadParameterDetails": [
            [
              {
                "name": "options",
                "type": "ILifecycleScopeOptions",
                "optional": true
              }
            ]
          ],
          "overloadReturns": ["ILifecycleScope"],
          "whenToUse": "Use createLifecycleScope when its function contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when createLifecycleScope does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "The general-purpose, async-only scope. Ported from `@migaia/reactive`'s `LifecycleScopeImpl` baseline (sync/async reentrancy guard, atomic splice+reverse teardown) with the sync path removed per D-1 and error handling generalized to the four §4.5 policies."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import createLifecycleScope from @migaia/lifecycle/scope, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use createLifecycleScope when its function contract and declared inputs match the result you need. Choose a different public symbol when createLifecycleScope does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained function example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for createLifecycleScope."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for createLifecycleScope."
            }
          ],
          "declaredConfiguration": [
            {
              "name": "errorPolicy",
              "type": "IErrorPolicy",
              "optional": true,
              "description": ""
            },
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": ""
            },
            {
              "name": "deadlineAt",
              "type": "number",
              "optional": true,
              "description": "Shared absolute deadline forwarded to every owned resource's release (L-T32)."
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "时间域来源（R-9）：graceful timeout / deadline 经它驱动，缺省 `systemScheduler`。透传给 * `DisposeTransaction`，使注入方（如 plugin-host）的 teardown 与 mutation queue 处于同一时间域。"
            }
          ],
          "identity": "packages/lifecycle/dist/lifecycle-scope.d.ts:createLifecycleScope:export declare function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope;",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 144,
          "configuration": [
            {
              "name": "errorPolicy",
              "type": "IErrorPolicy",
              "optional": true,
              "description": "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
              "descriptionEn": "",
              "descriptionZh": "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。"
            },
            {
              "name": "report",
              "type": "(error: unknown) => void",
              "optional": true,
              "description": "`errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError'` —— 默认 `'throw'`",
              "descriptionEn": "Diagnostic channel for a failed `start()`, tagged `UNIT_START_FAILED` with the original failure * on `cause`. `unit.error` itself always stays the caller's raw, untagged value — this is a side * channel for callers that want a `(source, code)`-bearing signal without losing that identity.",
              "descriptionZh": "`errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError'` —— 默认 `'throw'`"
            },
            {
              "name": "deadlineAt",
              "type": "number",
              "optional": true,
              "description": "`deadlineAt?: number` —— 本次 `dispose()` 内所有资源共享的绝对释放截止时间",
              "descriptionEn": "Shared absolute deadline forwarded to every owned resource's release (L-T32).",
              "descriptionZh": "`deadlineAt?: number` —— 本次 `dispose()` 内所有资源共享的绝对释放截止时间"
            },
            {
              "name": "scheduler",
              "type": "ILifecycleScheduler",
              "optional": true,
              "description": "`@migaia/lifecycle/scheduler` — scheduler、snapshot与时间校验",
              "descriptionEn": "",
              "descriptionZh": "`@migaia/lifecycle/scheduler` — scheduler、snapshot与时间校验"
            }
          ],
          "fragment": "lifecycle-scope--createLifecycleScope"
        },
        {
          "name": "ILifecycleScope",
          "kind": "type",
          "signature": "export type ILifecycleScope = ILifecycleOwner & { readonly lifecycle: ILifecycleState; /** Unregisters a resource without releasing it — the caller has already released it independently. */ release(resource: unknown): boolean; /** Synchronous, idempotent, never calls user code (L-T27). */ close(): void; /** * Always asynchronous (D-1) — there is no synchronous `dispose()` on this type. Resolves to the * collected errors for the `collect` policy (empty for the other three, which throw/report * internally instead). * * Concurrent external calls reuse the same published Promise even while release is in flight. * Only a synchronous call from an active disposer callback is rejected as reentrant, preventing * that callback from self-awaiting the Promise it is currently helping to settle. */ dispose(): Promise<readonly ICollectedError[]>; };",
          "declarationLine": 14,
          "source": "packages/lifecycle/dist/lifecycle-scope.d.ts",
          "purpose": "ILifecycleScope is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleScope.",
          "advanced": "No additional advanced behavior is declared for ILifecycleScope.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleScope."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleScope.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
          ],
          "exportPath": "./scope",
          "overloadSignatures": [
            "export type ILifecycleScope = ILifecycleOwner & { readonly lifecycle: ILifecycleState; /** Unregisters a resource without releasing it — the caller has already released it independently. */ release(resource: unknown): boolean; /** Synchronous, idempotent, never calls user code (L-T27). */ close(): void; /** * Always asynchronous (D-1) — there is no synchronous `dispose()` on this type. Resolves to the * collected errors for the `collect` policy (empty for the other three, which throw/report * internally instead). * * Concurrent external calls reuse the same published Promise even while release is in flight. * Only a synchronous call from an active disposer callback is rejected as reentrant, preventing * that callback from self-awaiting the Promise it is currently helping to settle. */ dispose(): Promise<readonly ICollectedError[]>; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleScope when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleScope does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleScope is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleScope from @migaia/lifecycle/scope, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleScope when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleScope does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleScope."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleScope."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/lifecycle-scope.d.ts:ILifecycleScope:export type ILifecycleScope = ILifecycleOwner & { readonly lifecycle: ILifecycleState; /** Unregisters a resource without releasing it — the caller has already released it independently. */ release(resource: unknown): boolean; /** Synchronous, idempotent, never calls user code (L-T27). */ close(): void; /** * Always asynchronous (D-1) — there is no synchronous `dispose()` on this type. Resolves to the * collected errors for the `collect` policy (empty for the other three, which throw/report * internally instead). * * Concurrent external calls reuse the same published Promise even while release is in flight. * Only a synchronous call from an active disposer callback is rejected as reentrant, preventing * that callback from self-awaiting the Promise it is currently helping to settle. */ dispose(): Promise<readonly ICollectedError[]>; };",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 28,
          "configuration": [],
          "fragment": "lifecycle-scope--ILifecycleScope"
        },
        {
          "name": "ILifecycleScopeOptions",
          "kind": "type",
          "signature": "export type ILifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Shared absolute deadline forwarded to every owned resource's release (L-T32). */ readonly deadlineAt?: number; /** * 时间域来源（R-9）：graceful timeout / deadline 经它驱动，缺省 `systemScheduler`。透传给 * `DisposeTransaction`，使注入方（如 plugin-host）的 teardown 与 mutation queue 处于同一时间域。 */ readonly scheduler?: ILifecycleScheduler; };",
          "declarationLine": 3,
          "source": "packages/lifecycle/dist/lifecycle-scope.d.ts",
          "purpose": "ILifecycleScopeOptions is part of this module's public type contract.",
          "core": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleScopeOptions.",
          "advanced": "No additional advanced behavior is declared for ILifecycleScopeOptions.",
          "parameters": ["This type declaration has no parameters."],
          "parameterDetails": [],
          "returns": "No explicit return annotation is declared for this type.",
          "errors": ["No documented errors are declared for ILifecycleScopeOptions."],
          "lifecycleConcurrency": "No lifecycle or concurrency behavior is declared for ILifecycleScopeOptions.",
          "examples": [
            "import {\n  createLifecycleScope,\n  createSyncLifecycleScope,\n  executeReleaseDescriptor,\n  createDisposeTransaction,\n  createSyncStartedDisposalLedger,\n  type IReleaseDescriptor,\n  type IDisposerContext,\n  type IReleaseContext,\n  type ILifecycleScope,\n  type ILifecycleScopeOptions,\n  type ISyncLifecycleScope,\n  type ISyncLifecycleScopeOptions,\n  type ISyncReleaseDescriptor,\n  type IDisposeItem,\n  type IDisposeTransaction,\n  type IDisposeTransactionMode,\n  type IDisposeTransactionOptions,\n  type ISyncStartedDisposalLedger,\n  type ISyncStartedDisposalOutcome\n} from '@migaia/lifecycle'",
            "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
          ],
          "exportPath": "./scope",
          "overloadSignatures": [
            "export type ILifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Shared absolute deadline forwarded to every owned resource's release (L-T32). */ readonly deadlineAt?: number; /** * 时间域来源（R-9）：graceful timeout / deadline 经它驱动，缺省 `systemScheduler`。透传给 * `DisposeTransaction`，使注入方（如 plugin-host）的 teardown 与 mutation queue 处于同一时间域。 */ readonly scheduler?: ILifecycleScheduler; };"
          ],
          "overloadParameterDetails": [[]],
          "overloadReturns": [],
          "whenToUse": "Use ILifecycleScopeOptions when its type contract and declared inputs match the result you need.",
          "notUse": "Choose a different public symbol when ILifecycleScopeOptions does not match the required inputs or return contract.",
          "sections": [
            {
              "id": "introduction",
              "title": "介绍",
              "content": "ILifecycleScopeOptions is part of this module's public type contract."
            },
            {
              "id": "getting-started",
              "title": "上手",
              "content": "Import ILifecycleScopeOptions from @migaia/lifecycle/scope, then follow a maintained task example for your use case."
            },
            {
              "id": "when-to-use",
              "title": "适用场景",
              "content": "Use ILifecycleScopeOptions when its type contract and declared inputs match the result you need. Choose a different public symbol when ILifecycleScopeOptions does not match the required inputs or return contract."
            },
            {
              "id": "quick-implementation",
              "title": "快速实现",
              "content": "Use a maintained type example that demonstrates real inputs and observable behavior."
            },
            {
              "id": "core-usage",
              "title": "核心用法",
              "content": "The structured inputs, output, errors, and lifecycle facts below define the public contract for ILifecycleScopeOptions."
            },
            {
              "id": "advanced-usage",
              "title": "高阶用法",
              "content": "No additional advanced behavior is declared for ILifecycleScopeOptions."
            }
          ],
          "declaredConfiguration": [],
          "identity": "packages/lifecycle/dist/lifecycle-scope.d.ts:ILifecycleScopeOptions:export type ILifecycleScopeOptions = { readonly errorPolicy?: IErrorPolicy; readonly report?: (error: unknown) => void; /** Shared absolute deadline forwarded to every owned resource's release (L-T32). */ readonly deadlineAt?: number; /** * 时间域来源（R-9）：graceful timeout / deadline 经它驱动，缺省 `systemScheduler`。透传给 * `DisposeTransaction`，使注入方（如 plugin-host）的 teardown 与 mutation queue 处于同一时间域。 */ readonly scheduler?: ILifecycleScheduler; };",
          "guidance": [
            {
              "id": "createlifecyclescope",
              "heading": "createLifecycleScope",
              "blocks": [
                {
                  "type": "code",
                  "language": "ts",
                  "code": "function createLifecycleScope(options?: ILifecycleScopeOptions): ILifecycleScope\n\ntype ILifecycleScopeOptions = {\n  readonly errorPolicy?: 'throw' | 'collect' | 'report' | 'firstError' // 默认 'throw'\n  readonly report?: (error: unknown) => void\n  readonly deadlineAt?: number\n  readonly scheduler?: ILifecycleScheduler // 默认 systemScheduler\n}\n\ntype ILifecycleScope = ILifecycleOwner & {\n  readonly lifecycle: 'open' | 'closing' | 'terminal'\n  own<T>(resource: T, descriptor: IReleaseDescriptor): T\n  release(resource: unknown): boolean\n  close(): void\n  dispose(): Promise<readonly ICollectedError[]>\n}"
                },
                {
                  "type": "paragraph",
                  "text": "通用异步所有权容器："
                },
                {
                  "type": "code",
                  "language": "ts",
                  "code": "const scope = createLifecycleScope()\nconst connection = scope.own(\n  { close: () => undefined },\n  {\n    force: (ctx) => connection.close()\n  }\n)\nscope.close() // 同步、幂等，之后 own() 抛 SCOPE_CLOSED\nconst failures = await scope.dispose() // 逆序释放，'throw' 策略下失败即抛"
                },
                {
                  "type": "paragraph",
                  "text": "边界行为："
                },
                {
                  "type": "list",
                  "items": [
                    "`own(resource, descriptor)`：容器处于 `closing`/`terminal` 时抛 `SCOPE_CLOSED`/`SCOPE_TERMINAL`；在 disposer 内部重入调用抛 `SCOPE_REENTRANT_OWN`。",
                    "`release(resource)`：反注册但不释放（调用方已自行释放），返回是否找到并移除了该条目；若曾设置 `gcFallback`，同时 unregister 对应的 `FinalizationRegistry` 条目。",
                    "`close()`：同步、幂等；`open → closing`。",
                    "`dispose()`：逆序（LIFO）释放全部已注册资源；并发多次调用复用同一个 Promise；需要在 disposer 中表达 owner self-join 时使用 `context.disposer.join()`，该操作抛 `SCOPE_REENTRANT_DISPOSE` 且不改变 canonical Promise；容器已 `terminal` 时立即 resolve 空数组。`errorPolicy: 'collect'` 时返回值为收集到的 `ICollectedError[]`；其余三种策略要么抛出要么内部上报，返回空数组。",
                    "环境支持时自动挂载 `[Symbol.asyncDispose]`。"
                  ]
                }
              ]
            }
          ],
          "usageScore": 30,
          "configuration": [],
          "fragment": "lifecycle-scope--ILifecycleScopeOptions"
        }
      ],
      "aliases": []
    }
  ]
}
